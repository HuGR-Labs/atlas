// @atlas/adapter-io — src/ast.ts  (ADAPT-AST-1: fold item/block AST units into the spatial tree)
//
// The raw ast adapter: an ADDITIVE refinement over a `FileTree` that folds sub-file item/block units
// onto the spatial rail. SKELETON — signature frozen, body deferred to the ADAPT-AST WP.

import type { FileTree } from '@atlas/index';

/** Fold sub-file item/block AST units into the spatial `FileTree` (additive refinement, ADAPT-AST-1). */
export function foldAstUnits(tree: FileTree): FileTree {
  void tree;
  throw new Error('unimplemented: ADAPT-AST-1 — fold item/block AST units into the spatial tree');
}
