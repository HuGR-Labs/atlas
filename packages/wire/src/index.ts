// @atlas/wire — barrel
//
// The ONE shared handler assembly (constitution WIRE-1) — imported by every entrypoint (CLI, MCP).

export { assembleHandler } from './wire.js';
export type { WireConfig, WiredHandler } from './wire.js';
