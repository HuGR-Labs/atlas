"```atlas-fact\n{\"claim\": \"The `owner` and `globs` fields are deliberately typed as plain `string` and `string[]` instead of their true nominal types `seat` and `Glob[]`, because those types live outside the ratified contracts membership and are owned elsewhere, so they are transcribed in their underlying string form rather than invented as new exported contract types.\"}\n```\n"