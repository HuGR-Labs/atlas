"STEP 1 — REASONING (scratch)\n\nCandidates drafted from the bytes, then attacked:\n\n- *\"Every staged row gets scope+tier\"* — the loop exists, but whether it passes rests on `driveMine`'s behavior outside these bytes; the premise assertion (`rows.length === FRONTIER.length`) is also externally decided. Weak.\n- *\"T0 row survives even under the mutant\"* — asserted nowhere in code; lives only in the comment's appeal to leg 2 in `mine.ts`. Discarded (comment about unseen code).\n- *\"`commitStaging` is the only staging door\"* — explicit claim about `store.ts`, not visible here. Discarded.\n- *\"Refusal happens despite same family\"* — both literals are `'advisory'` in the same test; checkable. Survives, but low actionability.\n- *\"Leg 3 calls the real `@atlas/knowledge` upsert, not a mock\"* — derivable from the import and call site. Survives, mild.\n- *\"Byte-identical `projection.json` assertion\"* — raw `readFileSync` before/after with `toBe`; stricter than the parsed-object equality next to it. Survives.\n- **Winner:** the TEETH test's fault injection is fully in-byte: the wrapped `DiskStore` literal forwards `commitStaging: (d) => real.commitProjection(d)` (put/get are behavior-neutral passthroughs), the aliased store is handed to a plain `driveMine` pass, and the assertions show the governed file's bytes changed and `current.size > 1`. Nothing in the file's name or the test signatures telegraphs a self-mutation case proving the boundary property is load-bearing; it changes how a reviewer treats refactors of the store doors or mine's write path. Re-checked item by item against the code: literal present, driveMine call uses it, both expectations present. Survives.\n\nSTEP 2 — RESULT\n\n```atlas-fact\n{\"claim\": \"The file contains a fault-injection ('teeth') test that aliases the disk store's staging-commit door onto the projection-commit door (commitStaging forwarded to real.commitProjection) and demonstrates that a plain mine pass then rewrites governed projection bytes and adds rows to governed knowledge, proving the staging/projection file separation asserted in the first test is a load-bearing property rather than an incidental path.\"}\n```"