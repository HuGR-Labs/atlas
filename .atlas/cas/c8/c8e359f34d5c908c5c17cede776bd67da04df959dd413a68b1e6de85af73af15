"```atlas-fact\n{\"claim\": \"Despite its 'never drifts' title, REQ-GROUND-5b mandates that a whitespace-only reformat OF the cited unit MUST drift the fact — the spec deliberately refuses any whitespace normalizer (cheap normalization would also erase whitespace that is semantically significant in TS/TSX: strings, templates, regex literals, JSX text, ASI), preferring a false alarm costing one re-ground over silently serving HOLDS on a stale fact, and it classifies nearby comment edits by blank-line contiguity rather than file position, so a change to a comment contiguous with the declaration drifts while a blank-line-separated license header above it does not.\"}\n```"