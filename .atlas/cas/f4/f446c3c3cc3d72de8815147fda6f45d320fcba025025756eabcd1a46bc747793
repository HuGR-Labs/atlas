"STEP 1 — REASONING (scratch):\n\nReading the bytes closely. Candidates drafted and tested:\n\n1. *\"Exactly two SCNs (GROUND-4-3, GROUND-9-2) are `gen: residue`, parametric on symbolic thresholds pending reconciliation.\"* — Survives verification (I scanned every SCN's gen tag; only those two carry `residue`, confirmed by the ledger), but much of its framing leans on pointers to `req-grd.md`/spec A-1/A-13 contents not shown here. Weaker candidate.\n\n2. *\"A single dangling citation empties the WHOLE grounding (REQ-GROUND-3a).\"* — Present in the scenario clauses, but partly motivated by past-tense amendment history (\"was executed…\"). The current normative clause is here though. Plausible but secondary.\n\n3. *\"The drift oracle is deliberately byte-exact — no normalizer; a whitespace reformat of the cited unit is required to DRIFT.\"* — Refutation pass: is this derivable from THIS unit alone? Yes: the fixture table pins `whitespace reformat OF the unit ⇒ sh-arr-0R — it MOVES`; the AMENDED note states \"hashes the raw source slice, NFC only; there is no normalize step\"; the REQ-GROUND-5b amendment explains why (cheap normalization erases whitespace that is SEMANTIC in TS/TSX — template literals, regex, JSX, ASI); SCN-GROUND-5b-1's Given/Then literally asserts reformat ⇒ DRIFTED \"which is the oracle's stated limit, not a defect\"; the teeth name \"a normalizer is landed\" as the mutant; held-out SCN-GROUND-5b-2 mirrors it (in-unit reindent ⇒ DRIFTED). No contradiction anywhere in the unit. Must phrase precisely: NFC *is* applied, so \"zero normalization\" would be false; the correct form is \"NFC-only, no whitespace/content normalizer.\" Action-changing: anyone implementing from the block name would plausibly build a whitespace-tolerant hash, and their goldens would contradict this spec in BOTH directions (false-fresh on reformat AND erasing semantic whitespace inside literals). Counterintuitive, fully self-contained, verified.\n\nCandidate 3 survives; candidates 1–2 discarded as weaker/partially externally anchored. Emitting 3.\n\nSTEP 2 — RESULT:\n\n```atlas-fact\n{\"claim\": \"The subtreeHash drift oracle is deliberately byte-exact — it hashes the cited unit's raw source slice with no normalizer beyond NFC — so the spec REQUIRES a whitespace reformat of the cited unit to verdict DRIFTED (pinned in both fixture universes) and names landing a normalizer as the defect its teeth exist to catch, because cheap whitespace normalization would also erase whitespace that is semantic in TS/TSX (string/template literals, regex, JSX text, ASI).\"}\n```"