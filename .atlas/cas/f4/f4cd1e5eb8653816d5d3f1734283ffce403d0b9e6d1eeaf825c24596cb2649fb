"Looking at the bytes: this is a self-contained HTML spec page whose \"code\" is its CSS palette system, HTML structure, and inline mermaid literals. Most prose (GEN-1..8, KNOW-13, ADR-0012, `reference/atlas-genesis.md`) asserts things about files not shown — discarded per the rules. The pipeline semantics live in prose/diagrams that restate the document's own thesis. Scanning the `<style>` block instead:\n\n**Draft candidate:** dark mode fails to restyle the diagram panels.\n\n**Refutation pass:**\n- Base `:root` defines 16 custom properties, including `--score-paper: #EAE5D8` and `--score-line: #B9B09A`.\n- The `@media (prefers-color-scheme: dark) :root` block lists exactly 14 overrides — paper through block — and **no** `--score-*` entries. Checked item by item.\n- `:root[data-theme=\"dark\"]` repeats the same 14; again no `--score-*`. Verified.\n- `.score` uses `background: var(--score-paper); border: … var(--score-line)` — so by plain cascade/inheritance (no external knowledge needed), those panels keep the cream light values under *either* dark trigger.\n- Supporting literal: `.score-cap { color: #6a6350 }` is a hardcoded hex, not a variable, so it also never adapts. Both `<pre class=\"mermaid\">` init directives additionally pin `'background':'#EAE5D8'` literally.\n- Not derivable from path/title (\"Genesis bootstrap over existing repo\" says nothing about theming). Not a restatement of structure. No dependency on unseen files or stale comments. Survives.\n\n```atlas-fact\n{\"claim\": \"The dark palettes (both the prefers-color-scheme override and [data-theme=\\\"dark\\\"]) omit --score-paper and --score-line, which exist only in the base :root, so the .score mermaid-diagram panels keep their light cream #EAE5D8 background — and .score-cap keeps its hardcoded #6a6350 text — even in dark mode.\"}\n```"