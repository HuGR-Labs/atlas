"```atlas-fact\n{\"claim\": \"Within this single file the behavioral cases call `isScope` imported directly from the relative source path `../src/write/authz.js`, while the forbidden-export fence inspects a separate `import * as knowledge from '@atlas/knowledge'` namespace object, so the two assertion groups target different import resolutions — meaning a rename or move of `src/write/authz.ts` breaks the coercion table even if the package's public surface is unchanged, and an export added to that source module is invisible to the FORBIDDEN check unless it flows through the package entry.\"}\n```"