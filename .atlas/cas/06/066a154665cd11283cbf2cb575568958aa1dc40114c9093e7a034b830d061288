"Looking at the bytes: the interesting lever here is how the injected replay leg's return values relate to the asserted row outcomes. `STILL_HOLDS` returns `'proven'` yet the test expects `outcome === 're-proven'`; `CHANGED` — semantically named \"the test changed\" — is wired as `() => 'abstain'`, and the assertion demands `outcome === 'broken'` with a `/no longer appears/` reason. Meanwhile the only paths the suite treats as `undefined`/uncounted are justified/unsealed seals (test 6), not replay abstentions. So within this unit, a replay that merely abstains is expected to land as hard breakage — a mapping invisible from the helper's name (`CHANGED`) or the `TestVacuityReplay` type alone, and one that matters to anyone extending the verdict set or consuming rows. Refutation pass: no numeric/exhaustive claim involved; the mapping is stated in executable assertions, not a comment about unseen code; nothing in the bytes contradicts it. Survives.\n\n```atlas-fact\n{\"claim\": \"In this suite, a replay leg returning the verdict 'abstain' (the CHANGED leg) is asserted to yield a row outcome of 'broken' with a /no longer appears/ reason — abstention is counted as breakage, not as the 'unverifiable' or undefined/uncounted paths reserved for missing witnesses, unwired replays, or justified/unsealed seals.\"}\n```"