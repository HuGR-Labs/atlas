"STEP 1 — scratch reasoning (not stored): Scanning the structural fields rather than the prose pointers. Checking each WP's `deps:` line across the file: TOOLS `[ ]`, 8.27.GEN `[ WP-8.27.TOOLS ]`, 28-a `[ ]`, 28-b `[ ]`, 28-c `[ ]`, 8.29 `[ ]`, 8.30 `[ ]`, 8.31 `[ ]`; matching `parallel_group` lines: `[P]` everywhere except 8.27.GEN's `[ ]`. Refutation checks: the claim rests only on these literal array literals in this unit — not on the header note about roadmap-DAG edges (which references campaigns not shown), not on the epic preambles' prose, and not on any pointer target. Uniqueness verified item-by-item over all eight dep/group pairs, so the exhaustive framing holds. It is not a restatement of any title or anchor, and it changes dispatch behavior (one serialized pair in an otherwise fully parallel campaign). Alternative candidates (uniform repair budgets, shared action surfaces, rationale being digest-free `# ptr`) were rejected as low-novelty conventions or near-restatements; the golden-suffix anomaly in WP-8.30 was considered but its actionability leans on scenario content defined in an unshown file, so it fails the re-derivation bar. The deps/parallel-group fact survives.\n\n```atlas-fact\n{\"claim\": \"WP-8.27.GEN is the only work package in this campaign whose deps list is non-empty — it depends on WP-8.27.TOOLS and is accordingly the sole WP not assigned to parallel_group [P], while every other WP declares deps: [] and shares group P.\"}\n```"