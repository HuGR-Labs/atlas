"```atlas-fact\n{\"claim\": \"Beyond the honesty/equality checks, the 8-process contention test enforces a separate liveness floor — completed >= Math.ceil(W * 0.75), i.e. at least 6 of the 8 concurrent passes must fully finish their frontier — so a run where most passes end in honest, visible 'contended' refusals still fails the test even if every reported candidate matches disk.\"}\n```"