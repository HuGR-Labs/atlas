"STEP 1 — Reasoning (scratch):\n\nThe unit is a ratified design doc, so nearly every sentence points *outside* these bytes: other files (`fixtures.mjs`, `fleiss.mjs`, `symbol-reverse.ts`), issue numbers (#232, #178, #189, #196b), arXiv IDs, ADR-0017, and measured numbers about external index builds (96.7%, 80.86%, 32.5%, 4.25%). Applying the refutation pass:\n\n- **Index-build dependence of negation recall** — discarded: its truth rests entirely on issue-tagged measurements of unseen indexes/builds.\n- **\"`validated` seal is cut, genesis ships proven-only\"** — discarded: rests on ADR-0017 CORRECTION 3, not visible here.\n- **\"detectionRates() already computes these three, unit-proven\"** — discarded: a claim about unseen code.\n- **Exact counts (29/30, 10+10, n=1352)** — discarded per the counting rule; they describe external experiments.\n- **The core mechanism** — survives scrutiny: the blockquote (\"The LLM is the SUBJECT under measurement, not the instrument… no LLM judge ever sits in the correctness loop\"), the fixture design (planted-false pairs deliberately *mention* the anchored code so a shallow grounding check passes, yet assert falsehoods), the three-label answer space (GROUNDED_TRUE / HALLUCINATED / ABSTAIN), and \"Scoring is a string compare against the planted label — zero model in the scoring loop\" are all fully stated in these bytes with no dependency on unseen files or issue numbers. The κ-panel's demotion to optional secondary (\"never a headline; cutting them entirely is acceptable\") is likewise stated in-byte. Nothing here requires trusting a pointer elsewhere.\n- Name check: \"benchmark-methodology\" reveals none of this — an engineer could not infer subject-vs-instrument, the grounding-vs-truth trap in the fixtures, or string-compare scoring from the path alone, and it decisively changes what they build (no judge pipeline; planted labels + arithmetic).\n\nSurvives. STEP 2 — emit:\n\n```atlas-fact\n{\"claim\": \"The ratified methodology makes the LLM the measured subject rather than the judge: fixtures are planted (code, fact) pairs whose labels are fixed by construction — the false ones deliberately mention the anchored code so a shallow grounding check passes, yet assert falsehoods — the subject answers GROUNDED_TRUE/HALLUCINATED/ABSTAIN, every score is a plain string-compare against the planted label with zero model in the scoring loop, and LLM-judge panel statistics survive only as an optional secondary signal, never the headline.\"}\n```"