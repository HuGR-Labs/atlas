"STEP 1 — scratch reasoning (not persisted):\n\nCandidates considered and refuted/survived:\n\n- \"Elision via `…` splits quotes into fragments\": the logic lives in `../lib/quote-coverage.mjs` (imported `containsQuote`), not in these bytes — discarded.\n- \"Unpinned uncovered invariants fail the gate\": depends on `pinProblems()` semantics from an unseen module — discarded.\n- \"Later-carrier hits are reported but never accepted\": visible in code (`diverged.set` fires regardless of `elsewhere`, which only decorates the message), but the loud header comment already states it, so it's low-information — discarded.\n- **Env-coupling of the ledger and pins**: survives. The code shows `LEDGER` and `PINS` are each selected by the ternary `process.env.REQ_CLAUSE_ROOT !== undefined ? JSON.parse(process.env.REQ_CLAUSE_<X> ?? '{}') : readFileSync(join(ROOT, ...))`. So overriding `REQ_CLAUSE_ROOT` does not merely relocate the root — it swaps both sidecar files for env vars, defaulting each to an empty set when unset. A test fixture therefore runs with *zero* exemptions/pins unless injected via `REQ_CLAUSE_LEDGER`/`REQ_CLAUSE_PINS`; there is no fallback to any `harness/*.json`. Fully checkable from these bytes (the two ternaries plus the `?? '{}'` defaults); not asserted by any comment; no counts involved; changes what a test author or fixture builder must do. Also note the trigger is `!== undefined`, so even an empty-string `ROOT` flips the mode.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"Setting the REQ_CLAUSE_ROOT env var does not just relocate the repo root — it switches the ledger and text pins from their harness/*.json files to the REQ_CLAUSE_LEDGER and REQ_CLAUSE_PINS env vars (each defaulting to empty when unset), so a fixture run starts with zero exemptions and zero pins rather than inheriting any file.\"}\n```"