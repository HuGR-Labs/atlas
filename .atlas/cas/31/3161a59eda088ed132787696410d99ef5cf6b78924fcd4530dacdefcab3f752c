"**Step 1 — free reasoning (scratch):**\n\nReading the bytes closely, candidates considered:\n\n1. *\"7b sabotages globals via manual monkey-patching with try/finally, not vi.spyOn/stubGlobal.\"* — True and visible, but it is essentially a plain summary of the control flow as written; a reader of the body sees it immediately. Discard as too close to restatement.\n2. *\"The env stand-in is a Proxy whose `get` trap throws on every property read, so any env var access fails.\"* — True, but again a direct description of the literal in front of the reader. Discard.\n3. *\"Seat identity comes from `@atlas/kernel`'s `asHash`.\"* — The code calls `asHash` once, only to build a `TranscriptRef` fixture in 10b-d; nothing in these bytes ties `redispatch`'s seat derivation to it (that's the header comment's claim about the impl, which lives elsewhere). Resting on a comment about unseen code. Discard.\n4. *\"The no-resume surface test pins the export list exhaustively.\"* — Try to refute: look at the actual assertions in SCN-PERSIST-10b-a-1. `surface.filter(RESUME.test)` must be `[]`; `surface` must **contain** `'redispatch'` and `'replay'`; `.resume`/`.continueFrom` must be `undefined`. There is **no** `expect(surface).toEqual([...])` and no length check. The inline comment says the surface is \"*exactly* redispatch + replay,\" but the code never enforces exactness — only containment-plus-absence. This contrast lives entirely within the shown bytes (comment text AND assertion code are both here), is checkable by reading the assertions themselves, needs no external knowledge, and makes no count claims beyond what I verified item-by-item (two `toContain`s, zero equality-on-surface assertions). It would change what an engineer does: adding a third, non-resume-named export to the module would sail through this test despite the scenario's framing. Survives.\n\n**Step 2 — result:**\n\n```atlas-fact\n{\"claim\": \"In SCN-PERSIST-10b-a-1, the code only asserts that the module's runtime key set contains 'redispatch' and 'replay' and matches none of the resume-like regexes (plus .resume/.continueFrom being undefined) — it never asserts the key list equals exactly those two members, so an additional differently-named export would pass despite the comment saying the surface is 'exactly redispatch + replay'.\"}\n```"