"STEP 1 — scratch reasoning (not persisted):\n\nReading the bytes as a self-contained document. Most content here consists of assertions *about* code files that are not shown (`tools/src/reconcile.ts`, `knowledge/src/ratify/*`, `composeRuntime`, etc.). Per the refutation discipline, any candidate resting on those cross-file claims is discarded — even accurate-sounding ones (boot-crashers, TOCTOU fix, RATIFY composition) cannot be re-derived from these bytes alone.\n\nSurviving candidates must come from the document's own structure/literals:\n\n- Candidate A: \"waves 2–4 are open while 1, 5, 6 are CLOSED\" — verifiable from the six `###` headers, but partially inferable from the `[gap]` cell markers; weaker novelty.\n- Candidate B: the Plan line says **\"5 coverage waves\"**, yet the document enumerates **six** wave sections (WAVE-COV-1 through WAVE-COV-6). Refutation attempt: did I miscount? Recounted the `### WAVE-COV-` headings item by item: 1, 2, 3, 4, 5, 6 — six, confirmed. The literal \"5 coverage waves\" appears verbatim under `## Plan`. No reliance on anything outside the shown bytes; the numbers are both physically present and recounted. Not derivable from the path/name (`e2e-coverage-matrix.md` reveals nothing about wave counts or an internal inconsistency). It would change reader behavior: anyone executing the plan's fan-out instruction would be working off a stale count relative to the sixth wave (apparently added for the #99 relation family).\n- Candidates about \"all findings FIXED\" — requires exhaustively re-verifying ~16 status annotations; too fragile, and several entries lean on claims about unseen files.\n\nCandidate B survives all refutation passes. Emitting it.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"The Plan section specifies '5 coverage waves', but the document itself enumerates six wave sections (WAVE-COV-1 through WAVE-COV-6), so the plan's stated count no longer matches the matrix's own structure.\"}\n```"