"STEP 1 — scratch reasoning (not persisted):\n\nCandidates drafted from close reading:\n\n- *Two entry points*: `main(argv, deps)` returns a numeric exit code and writes via `process.stdout.write` (mocked in `beforeEach`), while `runDoctor(argv, source?, indexPlan?)` is used without `await` and yields an object with `.stdout`/`.exitCode` (TEETH-routing test, 3e, 4b). Derivable from call sites, but it's mostly mechanical plumbing.\n- *Spy-handler read-only teeth*: every leg under test, including `index` (4a), asserts `handle` was never called. True and local, but close to what the header comment advertises.\n- *Per-leg dependency asymmetry* (fused with the port-surface fact): 3b shows the four store-backed legs refuse when `DoctorSource` is absent (\"composition-root WP\"); 4b shows `index` refuses when the `indexPlan` provider is absent; 4c shows `index` **succeeding** with `{ indexPlan }` alone and output explicitly NOT containing the unwired-source refusal. Meanwhile 2b pins `Object.keys(createDoctor(fakeSource()))` to exactly `['archive', 'hotSet', 'reground', 'whyBroken']` (counted: four, alphabetically sorted, re-checked) while `[...DOCTOR_SUBCOMMANDS]` is exactly `['archive', 'why', 'hotset', 'reground', 'index']` (counted: five, re-checked).\n\nRefutation pass on the surviving candidate: Is it derivable from THESE bytes? Yes — it rests on the executable `expect(...).toEqual([...])` literals and the call/option shapes in 3b/4b/4c, not on any comment's claim about unseen code (the 2b comment's rationale is corroborated, but I don't rely on it). Does it assert exact counts? Yes — four methods and five subcommands — both recounted item-by-item against the literal arrays above. Is it a restatement of the unit's name/signature? No: the filename only promises doctor-CLI tests; nothing in the name reveals that `index` bypasses the injected `DoctorSource`/port entirely. Would it change what an engineer does? Yes: refactoring the source interface or \"completing\" `createDoctor` with an `index` method, or assuming all doctor legs share one dependency, would break these goldens and miswire the leg. Survives.\n\nSTEP 2 — emit:\n\n```atlas-fact\n{\"claim\": \"Of the five dispatched `atlas doctor` subcommands, only `index` is served by a separately injected `indexPlan` provider and succeeds with no `DoctorSource` at all, while `createDoctor(source)` exposes just the four store-backed methods `archive`, `hotSet`, `reground`, and `whyBroken` — `index` appears solely in `DOCTOR_SUBCOMMANDS`, never as a method on the injected port.\"}\n```"