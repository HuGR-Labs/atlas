"STEP 1 — REASONING (scratch)\n\nCandidates drafted and refuted:\n\n1. *\"Absent ⇒ null, malformed ⇒ throw\"* — restated by the describe-block titles themselves; a reader gets this from the test names. Discarded as a plain summary.\n\n2. *\"The inside-repo refusal fires before the file is opened\"* — supported by the ghost-path test's literal, but the test title says exactly this; it is a summary of what that `it` plainly does, and the surrounding commentary does the explaining. Discarded.\n\n3. *\"The case-variant bypass test self-skips on non-folding volumes via a runtime dev/inode probe rather than `process.platform`\"* — derivable from `it.skipIf(!FOLDS_CASE)` + `probeFolds`, but the file announces this loudly in titles, warnings, and comments; an engineer skimming the suite already meets it. Weak novelty.\n\n4. *\"loadModelConfig takes the environment as an injectable second argument\"* — inferable from call sites, but close to a signature restatement once you see one call.\n\n5. **Survivor:** Combining two distant tests yields a non-obvious structural fact. In the defaults test, `cfg?.costCap` is read and pinned to `PROVISIONAL_COST_CAP` and to the literal `0.05` — so the decimal IS present on the returned object. Yet in the `id`-seam test, `Object.keys(cfg).sort()` must strictly equal `['costCapDen', 'costCapNum', 'roles', 'timeoutMs']`. Counted: exactly four items, correctly sorted. Therefore the readable `costCap` is deliberately NOT an own enumerable key — a dual nature (property-accessible but excluded from the key set) that no name, signature, or single test line reveals, and that directly constrains anyone adding a knob or a derived field: new fields must join the pin, derived decimals must stay out of it.\n\nRefutation checks on the survivor:\n- Byte-local? Yes — both assertions (`cfg?.costCap` reads; the `toStrictEqual` key-array literal) live in this file.\n- Rests on a comment about unseen code? No — the `id` import is unseen, so I do not claim how `id` behaves; the claim stands on `Object.keys` + property reads alone.\n- Exhaustive count? Yes — four array items, counted and order-checked against JS sort of the literals. Verified.\n- More than name/signature/plain-summary? Yes — it is a synthesis across two separate test blocks.\n\nSurvives. Emitting.\n\nSTEP 2 — RESULT\n\n```atlas-fact\n{\"claim\": \"Although loadModelConfig's result exposes a readable `costCap` decimal (pinned by default to exactly 0.05), the suite requires the resolved object to have exactly four own enumerable keys — costCapDen, costCapNum, roles, timeoutMs — so the derived decimal is deliberately kept out of the own-key set.\"}\n```"