"Looking at the actual code (not the surrounding prose): the per-shape loop branches on membership in `ARRIVES_AS_THROW`, whose literal is `new Set(['cyclic'])` — exactly one member, verified. `EXPECTED_REFUSAL` maps eight names; checking item by item, `float`, `non-finite`, `NaN`, `bigint`, `symbol`, `function`, `nfc-key-collision` all map to `'canonical-form violation'`, and only `cyclic` maps to `'RangeError'`. In the throwing branch the suite demands `thrown` be defined with discriminant `'RangeError'`; in the other branch it demands `thrown` be *undefined* and that the returned record's `rejected` text yield `'canonical-form variation'`… i.e. `'canonical-form violation'` via `refusalNameOf`.\n\nRefutation checks:\n- Derivable purely from these bytes (set literal, table literal, if/else branch)? Yes.\n- Does it lean on comments about `cli/src/map.ts`, `fault.ts`, task #136, or past behavior? No — stripping all of that leaves the channel asymmetry intact in the assertions themselves.\n- Exhaustive-count risk? I re-counted the table (8 keys) and the set (1 member); I'll still phrase the \"others\" part qualitatively (\"every other shape tabulated\") rather than numerically.\n- Not inferable from the file/test names, which only promise \"refuses … with NOTHING durable\" and say nothing about *which transport* each refusal takes? Correct — nothing in the titles reveals that seven shapes must not throw while one must.\n\nIt survives.\n\n```atlas-fact\n{\"claim\": \"In this suite the refusal channel is deliberately asymmetric: the ARRIVES_AS_THROW set contains exactly one shape, 'cyclic', which must escape as a thrown RangeError, while every other shape tabulated in EXPECTED_REFUSAL is asserted NOT to throw and instead to return a record whose rejected text carries the refusal name 'canonical-form violation'.\"}\n```"