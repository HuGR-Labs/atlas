"STEP 1 — scratch reasoning (not stored):\n\nReading the bytes closely:\n\n- The header comment talks about WP numbering, sibling scenarios, and other files (`../src/emit-guard.js`, GROUND-2 rules) — per the refutation rules, none of that cross-file narrative counts.\n- Code-level observations: (a) `deps()` returns `{ isGrounded: () => grounded, driftDetect: () => freshness }` — a constant stub that ignores whatever `Grounding` is passed, so the elaborate `partial2` fixture (including `asSubtreeHash('')`) never drives the verdict through the dep; the \"empty-subtreeHash ⇒ ungrounded\" causality exists only in the comment, not in executed code here. (b) The ungrounded scenario pins `gate.gateHolds('HOLDS', partial2, SRC)).toBe('NA')` — a string sentinel, not a boolean — despite the method name `gateHolds` conventionally suggesting a boolean predicate; the companion `truthDoorHolds(...)` is separately asserted `false`.\n- Refutation pass: (b) rests only on the `expect(...).toBe('NA')` expression in these bytes — verifiable here, no external claim, no counts. It is not derivable from the file name or any visible signature (none of `gateHolds`' return type appears in this unit), and it materially constrains anyone refactoring the gate's return type or reasoning about its semantics. (a) is real but borders on summarizing the two-line stub, and its punchline leans partly on the comment's framing; (b) is the cleaner survivor. `SRC = {} as Axes` and the local-Map persistence simulation are plausible but weaker/more plainly visible.\n- Survivor: the 'NA' sentinel expectation.\n\nSTEP 2 — emit:\n\n```atlas-fact\n{\"claim\": \"When the grounding leg fails (isGrounded stubbed false with freshness 'FRESH'), this test pins that gateHolds returns the sentinel string 'NA' rather than a boolean, with truthDoorHolds separately asserted false.\"}\n```"