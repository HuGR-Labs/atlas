"```atlas-fact\n{\"claim\": \"The gate validates the integer counts in its own 'DECLARED COUNTS' header comment by reading back its own source file (via fileURLToPath(import.meta.url)) and regex-matching the three numbers against what the reachability analyser measures, so editing the header prose or the tree out of sync fails this script even when every ledger row is correct.\"}\n```"