"STEP 1 — scratch reasoning:\n\nReading the bytes: this is a Markdown measurement report, not source code. Most of its content is either (a) numeric results of a run whose raw verdicts live elsewhere (committed, not shown), (b) claims pinned to issue numbers (#178, #181, #182, #183, #232) and other files (`fixtures.mjs`, `fleiss.mjs`, the methodology doc) — all discarded under the \"comment about code you cannot see\" rule, (c) supersession/history notes about a past run not present here — discarded as stale-history claims, or (d) the headline framing already announced by the title itself (\"planted ground truth, no LLM judge\") — a restatement, discardable.\n\nSurviving candidate: the report quotes, inline and verbatim, the actual fixture code behind the single leak: `const ABSTAIN = 'NO-FACT';`. This literal declaration IS in these bytes. From it, directly readable: the constant *named* `ABSTAIN` is assigned the value `'NO-FACT'` — the identifier does not match its value. That is why the planted claim \"the sentinel string is `'ABSTAIN'`\" fooled the subject (name/value confusion) and why it is objectively a false claim. This is checkable purely by reading this unit's own bytes, involves no count, no external-file trust, no issue numbers, and is invisible from the filename `calibration-report.a1-subject.md`. It would change behavior: anyone auditing or extending the F03-class fixtures (or writing hallucination probes) learns the concrete trap — match values, never identifier names — and knows the exact literal pair involved (`'NO-FACT'` vs `\"ABSTAIN\"`).\n\nRefutation passes all clear: derivable from these bytes ✓; no reliance on unseen files/issues ✓; no exact count or exhaustive enumeration ✓; not a restatement of name/title ✓; nothing in the shown bytes contradicts it ✓.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"The report quotes the leaking fixture's actual code inline — `const ABSTAIN = 'NO-FACT'`; — showing the constant named ABSTAIN really holds the string 'NO-FACT', so the planted F03 claim that the sentinel equals \\\"ABSTAIN\\\" is genuinely false and fooled the subject only via identifier-name/value confusion.\"}\n```"