"STEP 1 — Reasoning (scratch):\n\nCandidates drafted from a close read:\n\n1. *\"The scope→territory routing decision lives in the test doubles, not exercised production logic\"* — the `cover` closures compute `territory` themselves via `scope.split('/').includes('finance')`. Survives byte-check, but its framing leans on reasoning about the unseen sealed impl's behavior; weaker.\n\n2. *\"The poison list includes prototype member names (`'toString'`, `'__proto__'`, `'constructor'`) as tier values\"* — verifiable, but the actionable implication (a naive lookup-based whitelist would leak them) is inference beyond the bytes.\n\n3. *\"The 6b golden pins served order to fixture order\"* — check item by item: fixture lists `claim:acme-arr` (T1) first, then `claim:acme-ceo` (T0), then the T2; the assertion is `toEqual(['claim:acme-arr', 'claim:acme-ceo'])`, and array `toEqual` is order-sensitive. So the test forbids a tier-severity sort (which would put the T0 first) and requires a stable, order-preserving filter. Nothing rests on comments or on unseen files; the F6 expectation (`['the ratified T0 claim', 'the ratified T1 claim']`) is consistent (T0 was listed first there). The scenario name speaks only of bounding to tier≥T1 and the ~2K bound — ordering is invisible from the name. It changes what an engineer does: \"sorting the pack by tier strictness\" is a plausible-looking improvement that this golden forbids.\n\nRefutation attempts against #3: could the order be incidental? No — exact `toEqual` on the id array makes sequence load-bearing in two places (6b and F6). Miscounted items? Re-checked: two-element expected list matches the two non-T2 fixture entries exactly. Restatement of name/types? No. Survives.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"The SCN-TOOLS-6b-1 golden pins the served pack to the cover's original ordering — the T1 'claim:acme-arr' precedes the stricter T0 'claim:acme-ceo' via an order-sensitive toEqual — so the tier≥T1 bound must be an order-preserving filter, not a sort by tier strictness.\"}\n```"