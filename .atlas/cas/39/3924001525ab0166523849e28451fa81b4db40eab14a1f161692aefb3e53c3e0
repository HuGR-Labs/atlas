"STEP 1 — scratch reasoning (not stored):\n\nReading the bytes: the register lists twenty `###` INV entries (ADAPTER-1…12, WIRE-1, CLI-1, CLI-2, CLI-3, CLI-4, CLI-7, MCP-1, MCP-2). Checking each `method-tag:` line item by item: reference-model ×15 (ADAPTER-1..6, 8..12, WIRE-1, CLI-3, CLI-4, MCP-2), PBT ×1 (ADAPTER-7), exhaustive ×4 — CLI-1, CLI-2, **CLI-7**, MCP-1.\n\nNow checking the completion report at the bottom against those bytes: it claims \"exhaustive 3 (CLI-1, CLI-2, MCP-1)\", \"reference-model 15\", \"PBT 1\", and \"every INV tagged: yes (19/19)\". Its bucket lists contain no mention of CLI-7 at all, and 0+3+1+15=19 matches its claimed total — yet the body demonstrably holds twenty tagged entries and four exhaustive tags.\n\nRefutation pass:\n- Derivable solely from these bytes? Yes — both sides of the discrepancy (the CLI-7 entry's tag line and the report's histogram) sit inside this unit. No external library/convention knowledge involved.\n- Resting on a comment's claim about unseen code? No — it's an internal inconsistency between two parts of the same shown text.\n- Exact-count admissibility? Counts verified item by item: twenty headings enumerated, four exhaustive tag lines identified by name; the report's own sum (19) confirms it accounts for only nineteen. The numeric claim is the point here and it is checked.\n- Beyond name/signature? The filename promises a method-tags register; nothing in the name reveals that its self-audit histogram drops one of its own entries — and an engineer trusting the completion report as the record for the next state gate would plan around three exhaustive tags and nineteen INVs, missing CLI-7 entirely. That changes what they do (fix the register before S3, or ensure CLI-7's golden/oracle work isn't dropped).\n\nDiscarded alternatives: the zero-formal rationale and the no-live-LLM refusal are plainly asserted in prose (restatement); the READ_SURFACE/CAMPAIGN-10.3 and ADR details lean on files not present; the CLI-5/CLI-6 numbering gap's significance depends on unseen context.\n\nSurvivor: the report/histogram inconsistency around INV-CLI-7.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"The register contradicts its own completion report: INV-CLI-7's entry is tagged exhaustive in the body, but the report's histogram lists only three exhaustive tags (CLI-1, CLI-2, MCP-1), omits CLI-7 from every bucket, and claims nineteen tagged INVs although the body contains twenty.\"}\n```"