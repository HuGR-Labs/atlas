"STEP 1 — Reasoning scratch:\n\nCandidates drafted and tested against the bytes:\n\n1. *Sentinel duplication with `mine-proposer.ts`* — rests entirely on a comment about a file not shown. Discarded.\n2. *`POOL_WIDTH` consumed by the `mine` driver* — comment's claim about other packages; not checkable here. Discarded.\n3. *Concurrency enters via `visitAll`* — comment-only assertion about task #158; no concurrency mechanism visible in these bytes. Discarded.\n4. *`ratified`/`open` always empty in reports* — true and code-visible (`emptyReport` and `toReport` both hardcode `[]`), but close to a restatement of the co-located comment, and low action-value relative to alternatives. Held as backup.\n5. *`rerun` always drives under `defaultBudget(incremental.length)`* — code-visible, but the signature already takes no budget parameter, so it's near-tautological. Discarded.\n6. **Failed `genesis` wipes the checkpoint**: the catch block literally executes `pending = null;` before returning `emptyReport(...)`. Separately, `resume` begins with `if (!pending) return emptyReport(token, identity());`. Composing the two code paths (both fully in this unit, no reliance on comments): any throw out of `deps.plan`/`drive` during a fresh `genesis` destroys the closure state that a *previously issued* `ResumeToken` depends on — so a later `resume(oldToken)` yields the empty degraded report instead of filtering `pending.sites` past the cursor and re-driving. Nothing in the `GenesisApi` surface (total/never-throw contract, \"honest partial report carrying a resumeToken\") hints that a failed fresh call also invalidates outstanding checkpoints; the `pending = null` line's adjacent comment only discusses GEN-8c totality, not this side effect. No counts or exhaustive lists involved. Survives refutation.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"A failed `genesis` call assigns `pending = null` in its catch path, silently invalidating the closure-held checkpoint behind any resume token issued by an earlier partial run on the same controller instance, so a subsequent `resume` returns an empty degraded report instead of re-driving the remaining ranked sites.\"}\n```"