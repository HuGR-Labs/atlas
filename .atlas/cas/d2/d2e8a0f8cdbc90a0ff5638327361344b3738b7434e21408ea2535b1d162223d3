"STEP 1 — Reasoning through the bytes:\n\n- The unit is a bare `package.json`: name `@atlas/adapter-io`, private, ESM, exports pointing at `dist/src`. No comments, no code beyond the manifest fields — so any surviving fact must come from the literal field values.\n- Candidate A: \"all nine `@atlas/*` deps use `*` ranges\" — countable (contracts, genesis, grounding, index, kernel, knowledge, persist, retrieval, tools = 9) and verifiable, but it's essentially a plain scan of the dependency list; low surprise value. Discard as near-restatement.\n- Candidate B: \"`typecheck` runs `tsc -b`, same as `build`\" — fully byte-checkable, but asserting that this *emits output* leans on outside knowledge of `tsc -b` semantics not visible here. The safe core (\"identical command\") is true but thin.\n- Candidate C: the package's name says \"adapter-io,\" yet its declared dependencies include `tree-sitter-typescript`, `web-tree-sitter`, and `@c4312/scip` — parser-grammar and SCIP-indexing packages sitting alongside the `@atlas/*` workspace deps, all under `dependencies` (no `devDependencies` section exists in the file). This is read directly off these bytes, involves no counts or exhaustiveness claims (\"lists … among\"), no comments, no references to other files, and it is genuinely non-obvious from the package name alone — it tells an engineer this \"adapter\" carries a TypeScript-parsing/code-intel dependency footprint, which changes expectations about its contents and install requirements.\n- Refutation pass on C: every element (dep names, their presence in `dependencies`) is literally present in the shown bytes; the only interpretive step is noticing the tension between the name and those entries, which requires no external library knowledge. Survives.\n\nSTEP 2 — Emitting the survivor:\n\n```atlas-fact\n{\"claim\": \"Despite being named as an I/O adapter, @atlas/adapter-io lists tree-sitter-typescript, web-tree-sitter, and @c4312/scip among its declared dependencies.\"}\n```"