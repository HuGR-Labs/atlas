"Looking at the bytes: the filename advertises only a REQ-MCP-1e advertised-vs-invocable guard, so anything about the second scenario's mechanics is pre-read invisible. Candidates I drafted and refuted:\n\n- *\"GOVERNANCE_SURFACE has five / READ_SURFACE has six members\"* — the arrays are imported, not shown; counts come from comments, unverifiable here. Discarded.\n- *\"Injecting a read bundle doesn't intercept write tokens\"* (the `atlas-emit` teeth test) — the assertion is in the bytes, but the fact's truth rests on the production router behaving as the test hopes, which cannot be confirmed from this unit alone. Weaker than a purely structural fact.\n- *Parameter-shape discovery* (`callTool` takes legs as 6th arg with two opaque `undefined` slots before it) — derivable from call sites, but easily re-derived by glancing at `src/server.ts`; low action-value.\n- **Survivor**: in the per-member read guard, `callTool(handler, member, READ_ARGS[member], …)` indexes a plain `Record<string, unknown>` keyed by token name, with no `satisfies`, no `keyof` binding, and no exhaustiveness check anywhere in the unit. By ordinary JS/TS semantics, any `READ_SURFACE` member lacking a `READ_ARGS` key silently yields `undefined` args — nothing in these bytes makes that loud. This is a maintenance trap that changes what an editor of this file must do when the read surface grows, and it is checkable purely from this unit's own control flow and literals.\n\n```atlas-fact\n{\"claim\": \"In the per-member read guard, each READ_SURFACE member's arguments are looked up as READ_ARGS[member] against a plain Record with no exhaustiveness check, so a READ_SURFACE token lacking a matching READ_ARGS key would reach callTool with undefined arguments rather than failing loudly.\"}\n```"