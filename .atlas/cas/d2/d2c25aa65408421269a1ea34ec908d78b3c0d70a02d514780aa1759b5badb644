"Looking closely at the bytes: the root declares `workspaces: [\"packages/*\"]`, and nearly every directory under `packages/` gets both a `packages/<name>` entry and a corresponding `node_modules/@atlas/<name>` link entry. Counting the `node_modules/@atlas/*` links: adapter-io, cli, contracts, e2e, e2e-blackbox, genesis, grounding, index, kernel, knowledge, mcp-server, memory, persist, retrieval, tools — fifteen, and the list jumps straight from `tools` to `@bufbuild/protobuf`. Yet the `packages/` section contains a sixteenth entry, `packages/wire`, which carries `\"extraneous\": true` and has no link anywhere in `node_modules`. So `@atlas/wire` exists on disk enough for npm to have read its package.json, but it is flagged as belonging to nothing and is deliberately left out of the installed graph — unlike all sibling workspaces, which are distinguishable purely within this file (none of the other fifteen carry the flag).\n\nRefutation pass: this rests entirely on keys visible in the shown bytes (`extraneous: true`, absence of a `node_modules/@atlas/wire` key across the fully-shown document); no comments, no external library knowledge, no claims about files outside the unit. The \"only/no link\" exhaustiveness was checked item by item against the complete listing. It is not a restatement of the file's name or header fields, and it is actionable: anyone importing `@atlas/wire` or expecting it in a clean `npm ci` install will find it unresolved, and a maintainer should either remove the stale directory or restore its registration. Survives.\n\n```atlas-fact\n{\"claim\": \"The @atlas/wire package appears in this lockfile only as a packages/wire entry flagged extraneous:true with no corresponding node_modules/@atlas/wire link, unlike every other workspace package, leaving it outside the installed dependency graph despite the packages/* glob.\"}\n```"