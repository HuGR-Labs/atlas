"Looking closely at the bytes: the strongest non-obvious, action-changing detail is the two-identifier trap. The unit's own shown literals verify it end-to-end: the authored JSON carries `\"id\": \"f9517988…\"`, yet the successful `emit` run returns `data.id: 20512b7622b0d8864f20311700f4091b991ea5317797ce6158371d06adca0b06` — a different hash — and step 4's readback invokes `atlas node 20512b76…`, which then echoes back `node: f9517988…` (the authored id, i.e., the nodeKey that `query` prints). Refutation pass: this is fully derivable from the transcripts and prose in this unit alone, relies on no unseen file, asserts no count or exhaustive list, and is far more than a restatement of the doc's title. Other candidates (e.g., \"no shipped command authors facts,\" \"campaign 10 not built\") depend on claims about the rest of the codebase and were discarded; the set-union/dedup note is corroborated only by narration rather than a shown transcript, making the dual-ID fact the safest survivor.\n\n```atlas-fact\n{\"claim\": \"The `id` field you author in the fact JSON is the nodeKey, not the storage handle: a successful `atlas emit` returns a different content address under `data.id` (20512b… vs the authored f951… in the shown run), and `atlas node` accepts only that returned content address.\"}\n```"