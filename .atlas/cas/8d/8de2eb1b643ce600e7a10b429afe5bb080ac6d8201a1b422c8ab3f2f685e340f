"STEP 1 — scratch reasoning (not persisted):\n\nReading the bytes: the unit exports a text table keyed by `CommitRefusal`, a thrown error class, and two constants. Most of the prose in the comments asserts things about code *outside* this unit — `mine.ts`'s `createDiskStore` wiring, `ControllerDeps.upsert`'s return shape, `run-controller`/GEN-8c, ADR-0008, KNOW-11a, `.atlas/policy.json`, the emit door. All of that is discarded per the rules: accurate-or-not, it is a fact about other files and does not re-derive at these bytes. The `unreachable untrusted branch` story is explicitly past-tense commentary — discarded.\n\nDraft candidates:\n- *\"The `untracked` case exists\"* — refute: no such key; misread.\n- *\"MINED_SCOPE is fail-closed because `actorInScope` denies by default\"* — refute: that mechanism lives elsewhere; comment-only claim.\n- *\"MINED_TIER is never forwarded from `f.tier`\"* — refute: that stamping behavior is in the driver, not here; comment-only.\n- *\"The thrown message embeds the operator guidance text\"* — survives byte inspection (template literal interpolates `STAGING_REFUSAL_TEXT[refusal]`), but it is essentially the one visible line of the constructor body — close to a summary of what the code plainly does.\n- *\"The refusal vocabulary is exactly {contended, unreadable, untrusted}\"* — enumerate the keys of the object literal: `contended`, `unreadable`, `untrusted`. Three keys, no others; recounted item-by-item. The signature `Readonly<Record<CommitRefusal, string>>` is visible, but `CommitRefusal` is an opaque type-only import — a reader cannot learn the member values from the name or signature, only from these literal keys. Because the table is typed as a total record over the union, this file doubles as the compile-time exhaustiveness checkpoint for the vocabulary. Survives: local, counted, verified, not restating any name/type, and changes what an engineer does (adding a refusal variant forces touching this table; consuming code can treat the set as closed without opening `@atlas/adapter-io`).\n\nSurvivor: the enumerated, closed three-case vocabulary.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"This file's refusal vocabulary is a closed set of exactly three cases — contended, unreadable, and untrusted — enumerated as the complete key set of STAGING_REFUSAL_TEXT, which is typed as a total Record over the opaque imported CommitRefusal type, so any new refusal variant added upstream breaks this file's compilation until guidance text is supplied here.\"}\n```"