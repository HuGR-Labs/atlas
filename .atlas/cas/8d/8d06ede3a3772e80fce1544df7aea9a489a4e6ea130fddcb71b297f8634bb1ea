"STEP 1 — scratch reasoning:\n\n- Candidates tied to comments about other modules (barrel wiring \"by the lead at SEAL\", held-out `-2` fixtures run by \"the GATE\", the fixture \"standing in for GROUND's `gateHolds`\") all assert things about code not in these bytes → discarded.\n- \"The fixture gate ignores its `_at` hash parameter\": true in the body, but the `_at:` underscore prefix in the visible signature already signals this → not novel enough.\n- \"The gate fails closed for predicate nodes via `node.kind === 'advisory'`\": restates the visible ternary; the comment's history about `undefined` reads is stale/past-tense → discarded.\n- Result-shape and persistence-equality observations merely summarize what the assertions plainly say → discarded.\n- Survivor: the store construction. `import type { Cas, CasObject } from '@atlas/kernel'` gives no hint of `Cas`'s shape, yet the code assigns `const store: Cas = new Map<Hash, CasObject>()` (and again a pre-seeded `new Map<Hash, CasObject>([...])`) and passes it straight into `createEmit`. That a bare `Map` is accepted as the `Cas` port is checkable only from these declarations, isn't implied by the unit's name or any shown signature, and is actionable (an engineer can reuse a plain `Map` as the store double instead of authoring a custom fake, and learns the persistence seam is map-shaped: `get`/`has`/`size`). No exact counts involved. Survives.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"The kernel Cas port is exercised here as a bare Map: each test constructs `const store: Cas = new Map<Hash, CasObject>` (pre-seeded in the reject case) and passes it directly into createEmit, so a plain Map satisfies the Cas interface.\"}\n```"