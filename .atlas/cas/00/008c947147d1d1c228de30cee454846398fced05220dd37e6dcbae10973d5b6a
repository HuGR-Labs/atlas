"```atlas-fact\n{\"claim\": \"In the rank-order test, the expected emission sequence is derived by re-sorting the same fixture candidates with the packer's own exposed `compare` comparator (`packer.compare`), making that first `toEqual(expected)` assertion self-referential and unable to detect a wrong comparator — the hard-coded literal order asserted immediately after is the only check in the test that actually pins the concrete rank semantics.\"}\n```"