"STEP 1 — scratch reasoning (not persisted):\n\nCandidates considered:\n\n1. \"Downward-only imports enforced by TS project references\" — rests entirely on claims about other files (tsconfigs, package sources) not shown. Discarded.\n2. \"≤400 LOC gate lives at harness/gates/godfile-guard.mjs\" — a claim about another file; unverifiable in these bytes. Discarded.\n3. \"Only two classes in the tree are error types\" — claim about the whole tree elsewhere. Discarded.\n4. \"Test runner is vitest via npm test\" — the mapping of the npm script to vitest is asserted about package.json contents not present here. Discarded.\n5. Count audit: the prose states \"Fifteen packages (nine core layers + the productization ring)\". Enumerating every package name appearing anywhere in this unit — the diagram lists contracts, kernel, persist, index, grounding, knowledge, retrieval, memory, tools, genesis, e2e (11); the ownership table lists 10 of those (no e2e row); no other package names appear. Re-checked item by item: eleven distinct named packages versus the stated fifteen, and even the layer math (L0–L8 = nine layers, with persist/index sharing L2, plus e2e) resolves to eleven, not fifteen. Both operands of this comparison — the literal \"Fifteen\" and the full enumeration — live inside this unit, so it is checkable here without trusting anything about unseen code. Survives.\n6. \"e2e gets no layer number in the diagram\" — true in the bytes but essentially a restatement of the diagram line. Discarded as too plain.\n\nCandidate 5 survives: an internal inconsistency between the headline count and the document's own enumeration, which would tell an engineer the ownership table is not exhaustive.\n\nSTEP 2 — emitting the surviving fact:\n\n```atlas-fact\n{\"claim\": \"Though the overview declares fifteen packages, the document itself names only eleven — the ten packages in the ownership table plus e2e — so the table cannot be treated as an exhaustive map of the monorepo.\"}\n```"