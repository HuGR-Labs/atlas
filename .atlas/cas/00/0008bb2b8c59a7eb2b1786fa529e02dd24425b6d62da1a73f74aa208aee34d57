"STEP 1 — scratch reasoning:\n\nReading the `SCN-KNOW-15d-1` test closely: three extra candidates are built besides `c`. Assertions fired: `primaryAnchorId(c)` twice (tautology), `primaryAnchorId(bothInFn)` ≡ `anc`, `primaryAnchorId(elsewhere)` ≠ `anc`, and finally `expect(other).toBeDefined()`. Tracing every use of `other`: declared from anchors `[sym('pkg/parser::parseHeader::Z')]`, then referenced exactly once — in the `toBeDefined()` call. Its `primaryAnchorId`/`nodeKey` are never compared to `anc`, to itself tightened, or to anything else. Yet the adjacent comment reads \"Z alone tightens to itself,\" which sounds like an asserted golden but has no backing expectation in the code.\n\nRefutation pass:\n- Derivable from these bytes? Yes — pure control-flow/dataflow reading of one `it()` body; no external library or convention needed.\n- Resting on a comment about unseen code? No — it rests on the *presence/absence of statements inside this unit*, fully checkable here. (Ironically the in-comment claim is the thing *not* backed.)\n- Exact count/enumeration risk? None — qualitative claim about one variable.\n- Restatement of name/signature/types, or plain summary? No — the test name advertises \"tightest structural unit\"; nothing signals that the single-symbol leg is a dead assertion. An engineer relying on this test for single-symbol tightening coverage would be misled, and would act differently (add a real expectation).\n\nDiscarded alternatives: `PREDICATE_SLOTS` length 13 is already in the test name; the routeWrite four-cell table is essentially the guardrail's own description; `routeWrite` living in the local `../src/write/router.js` is visible from the import block but lower impact; the fixture hash scheme is trivially plain.\n\nSurvivor: the `other` candidate in 15d-1 is asserted only for definedness — single-symbol anchor tightening is unverified in this unit despite the prose suggesting otherwise.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"In the SCN-KNOW-15d-1 test, the single-symbol candidate `other` (built from parseHeader::Z) is never compared against any expected anchor — its only use is `expect(other).toBeDefined()` — so the 'Z alone tightens to itself' behavior described in the test body is not actually verified anywhere in this unit.\"}\n```"