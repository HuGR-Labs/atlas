"STEP 1 — REASON FREELY\n\nScanning candidates against the bytes:\n\n- *\"A valid model config turns llmCalls from 0 to 2\"* — the narrative header claims measured past behavior and cites producers (`StructRef.qualifiedPath`, `createFileSourceReader`, GEN‑8c) that are not in these bytes. Discard: comment-borne, about other modules.\n- *\"With no model the run spends 2 budget\"* — the pairing (ATLAS_MODEL_CONFIG: '' → expected `llmCalls 2 · budgetSpent 2`) is literally present, but its truth rests on unseen runtime behavior of `runAtlas`/the CLI, not on this unit's own declarations. Borderline; weaker than a purely internal mechanic. Set aside.\n- *\"Every run pins ATLAS_MINE_SLOT to 'advisory'\"* — checked all four `it` blocks: 4/4 pass it. Real, but largely restated by the adjacent comment and lower impact.\n- *\"The echo stand-in passes a multi-line fenced block as a single argv element\"* — derivable from the CLAIM literal, but marginal actionability.\n- **Memoized shared fixture**: `indexedRepo()` uses `repo ??= makeFixtureRepo(...)`, and I verified every one of the four test cases obtains its repo through it (control, bite, digest, missing-binary — 4/4), with a single `repo?.cleanup()` in `afterAll`. So the suite has NO per-test repo isolation; anything one run does to the fixture is seen by the next, and per-test fixture customization is impossible without restructuring. Not guessable from the file name or any signature; changes how you extend or reason about the suite. Survives refutation: it rests only on the memoization expression, the four call sites, and the single afterAll teardown — all visible.\n\nSTEP 2 — EMIT THE RESULT\n\n```atlas-fact\n{\"claim\": \"All four test cases share one lazily-created fixture repository — indexedRepo() memoizes makeFixtureRepo via `repo ??=` and it is torn down once in afterAll — so no test in this suite runs against a fresh repo.\"}\n```"