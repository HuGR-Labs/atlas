"STEP 1 — REASON FREELY\n\nCandidate drafts and refutation passes:\n\n1. *\"Editing the subject file drifts the relation but doesn't orphan it\"* — refuted: this is telegraphed by the describe/it titles themselves (\"IDENTITY leg SURVIVES the edit…\") and by the header prose; an engineer picking the file by name plus reading titles gets it.\n\n2. *\"An intrinsic multi-anchor advisory is refused where a relation emits\"* — refuted: literally titled \"THE CONTRAST\".\n\n3. *\"The refusal exits non-zero with empty stderr\"* — survives weakly, but it's a narrow integration detail and leans on the comment's framing (\"fail-CLOSED\"); the bare assertion is visible but low-leverage.\n\n4. *\"Tests are strictly order-dependent over one shared fixture repo\"* — check against bytes:\n   - `let repo: FixtureRepo` is module-scoped; `beforeAll` at file top level creates it ONCE (`makeFixtureRepo`) and captures `genesis = repo.sha()` once.\n   - `emitFact(repo, rel)` is called exactly twice, both in the FIRST describe; the successful emission happens only in the first `it`. Every `relations …` query in the second and third describes asserts `lines` length 1 — impossible unless the first test's emission landed. Filtering to a later `it` (e.g., `vitest -t \"IDENTITY leg\"`) would find zero relations and fail.\n   - The third describe chains further: test 1 commits the edit, test 2 observes post-edit state, test 3 commits the restore and reconciles — each consumes filesystem/git state produced by its predecessor.\n   - Nothing here rests on a comment about unseen code — it's pure control flow, scoping, and literals in this file.\n   - Not an exact-count/exhaustive-enumeration claim if stated qualitatively.\n   - Not derivable from the unit's path/name, and not stated by any single test title (titles imply a *story*, but the hard mechanical coupling — one repo, cross-test side effects — is invisible until you read the scoping and call sites).\n   - Actionable: it forbids test isolation/sharding/reordering/conversion to concurrent style, and tells a debugger why running one `it` fails.\n\nSurvives. STEP 2 — EMIT.\n\n```atlas-fact\n{\"claim\": \"The tests in this file are strictly order-dependent: they all mutate a single FixtureRepo created once in a file-level beforeAll, the relation queried by the later `relations`/`reconcile` tests is only emitted inside the very first test, and the drift/fresh tests consume commits made by preceding tests — so selecting or running any test in isolation would fail.\"}\n```"