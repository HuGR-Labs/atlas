"STEP 1 — scratch reasoning:\n\n- Candidate A: the memo-key `\\0` injectivity story. The *why* (git diff/grep visibility) lives only in a comment; the code just spells `` `${repo}\\0${rev}` ``. Discard — the interesting part is comment-asserted.\n- Candidate B: `manifestOf` overriding `build` with `() => axes` and `isCandidate: () => false`. Verifiable, but essentially a restatement of adjacent lines/comments about index-adapter/init behavior elsewhere. Weak.\n- Candidate C: empty-string `rev` treated as `'HEAD'`. Verifiable in `axesFor`, but minor and close to \"what the code plainly does\" on that line.\n- Candidate D: **statefulness of `unitPrior`.** Trace the bytes: `priors` is a closure-level `Map`; `workingTreeAxes` executes `priors.clear()` then repopulates from the fold; `axesFor` invokes `workingTreeAxes` only on the HEAD legs (`''`, `'HEAD'`, or resolved-head-sha match); the non-HEAD leg returns `revIndex().axesAt(rev)` without touching `priors`; `skeleton` folds only on a memo miss; `unitPrior` is just `priors.get(...)`. Therefore: the interface member looks like a pure lookup, but it reads mutable, order-dependent instance state — `undefined` until a HEAD-leg skeleton has been built, reflecting whichever repo was folded most recently, and untouched (stale) after historical-rev builds. Fully derivable from declarations and control flow in these bytes alone, no reliance on comments about `#197`, `mine.ts`, or other files. Not inferable from the name/type (`UnitPriorSource` suggests a static source). Would change behavior: query ordering matters; sharing one source across repos silently swaps prior sets. Survives refutation.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"Despite presenting like a pure accessor, unitPrior reads mutable per-instance state: the priors map is cleared and repopulated as a side effect of every working-tree (HEAD-leg) skeleton build, so it returns undefined until such a build has run and always reflects whichever repo was folded last.\"}\n```"