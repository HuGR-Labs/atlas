"Looking at the bytes closely, most surface facts (imports, fixture helpers, the three describe blocks, cleanup logic) are either plainly visible summaries or depend on fixture files not shown. The strongest candidate is a discrepancy *inside* the unit itself: the file header declares \"The stand-in 'model' is `echo`,\" but the first R1 test actually wires `operatorConfig('true')` — verifiable from the literal argument alone, with the header text sitting in the same file. Refutation checks:\n\n- Derivable from these bytes? Yes — the string `'true'` is in the call, and the header's contradictory claim is also physically present in these bytes, so the tension is checkable without leaving the unit.\n- Does truth rest on a comment about elsewhere? The *core* fact (configures `true`, header claims `echo`) rests only on code literals. The abstention consequence is corroborated in-byte by the test's own pinned stdout strings (`'cost: llmCalls 2 · budgetSpent 2'`, `'2 site(s) visited and every one abstained'`, `'nothing was proposed or admitted'`) — no reliance on unseen `llm.ts` internals for the fact to hold.\n- Exact-count risk? The \"two\" comes from a quoted literal expectation in the bytes, not from my counting external behavior — admissible.\n- Restatement of name/signature? No — the name says nothing about which stand-in command is used or that the header misdescribes one case.\n\nThis would change what an engineer does: anyone skimming the header (or \"normalizing\" the test to use `echo` like its sibling case) would silently destroy the test's reason to exist, since a proposing stand-in never reaches the wired-but-abstained render branch.\n\n```atlas-fact\n{\"claim\": \"Contrary to the file header's blanket statement that the stand-in model is `echo`, the first R1 test deliberately wires the operator model as the command `true`, producing a run with two real LLM calls in which every site abstains so the test exercises the wired-model-but-nothing-proposed render branch.\"}\n```"