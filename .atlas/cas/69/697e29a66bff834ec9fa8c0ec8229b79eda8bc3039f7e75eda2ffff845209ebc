"Looking closely at the bytes:\n\n- The fixtures define `V_rej` literally as `{ ok: true, data: { exitCode: 2 }, guidance: G('drift needs review') }`, and test 3b asserts `renderVerdict(V_rej).exitCode).toBe(2)` — both statements are code in this unit, not comments.\n- Meanwhile `V_ok` (`ok: true`) yields exit 0 and `V_err` (`ok: false`) yields exit 1.\n- So the exit code is demonstrably *not* a function of the `ok` flag alone: a verdict flagged `ok: true` produces a non-zero exit (2).\n- Refutation pass: could this rely on unseen code? No — both the literal construction of `V_rej` with `ok: true` and the assertion of exit 2 are right here. Could it be a misread of a comment? No, the comment merely annotates the same behavior the assertions encode. Is it an exact count/exhaustive list? No, it's qualitative.\n- Is it knowable from name/signature? `renderVerdict` returning `{ stdout, exitCode }` is plain, but nothing in the names reveals that `ok: true` coexists with exit 2 — a natural assumption (`ok ⇒ 0`) is falsified here, which changes how one wires or interprets CLI exit codes downstream.\n\n```atlas-fact\n{\"claim\": \"In this unit's assertions, a Verdict whose ok flag is true can still yield exit code 2 (V_rej is built with ok:true and renderVerdict(V_rej).exitCode is expected to be 2), so the CLI's exit code is not determined by the ok flag alone.\"}\n```"