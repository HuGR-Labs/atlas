"STEP 1 — scratch reasoning (not persisted):\n\nReading `addressOf` closely: the catch block computes `message` with an `e instanceof Error ? e.message : String(e)` ternary, which superficially suggests any throwable could be converted. But the conversion guard is `if (e instanceof Error && discriminantOf(message) === CANONICAL_VIOLATION)`, where `discriminantOf` takes only the text before the first `':'` and `CANONICAL_VIOLATION` is the exact literal `'canonical-form violation'`. Consequences verifiable purely from these bytes:\n\n- The `String(e)` branch is dead on the return path — a non-`Error` throwable can never produce `{ rejected: … }` and always falls through to `throw e`, even if its stringified form begins with the magic prefix.\n- Matching is strict equality on the pre-colon prefix, so any `Error` message starting `canonical-form violation:` converts regardless of trailing prose, while a differently worded message does not.\n- Everything else (different prefix, non-Error, cyclic-value `RangeError`) escapes unchanged per the unconditional `throw e`.\n\nRefutation pass: Is any of this resting on the header comments (KERNEL-1, #136, exit codes, fault classes)? No — it's the local control flow and literals. Is it derivable from name/signature alone? No — `Addressed` with a `rejected` variant hints at conversion but nothing about the Error-instance requirement, the colon-split prefix equality, or the dead `String(e)` fallback. Is it a mere summary of \"what the code plainly does\"? It goes past the docstring's stated intent into a concrete behavioral subtlety that contradicts what the `String(e)` fallback implies. Exhaustive-count risk: none — describing the total behavior of an eight-line fully-visible function.\n\nSurviving fact: the conversion condition and its consequence for non-Error throwables.\n\nSTEP 2 — emit:\n\n```atlas-fact\n{\"claim\": \"In addressOf, a caught throw becomes a recorded refusal only if it is an Error whose message's text before the first colon exactly equals 'canonical-form violation' — so despite the String(e) fallback in the catch, a non-Error throwable can never be converted and is always rethrown unchanged.\"}\n```"