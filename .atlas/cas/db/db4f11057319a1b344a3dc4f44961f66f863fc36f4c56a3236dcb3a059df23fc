"STEP 1 — Scratch reasoning (not persisted):\n\nReading the keys as raw data: the map has ~90+ entries, all pointing into `docs/reference/atlas-*.md` anchors. Nearly every key ends in `@carrier2`. Scanning specifically for deviations: `@carrier1` appears on exactly two keys (`atlas-grounding.md#ground-7@carrier1`, `atlas-tools.md#tools-1@carrier1`), and `@carrier3` appears on exactly one (`atlas-genesis.md#gen-12@carrier3`). Cross-checking duplicates by anchor id (requiring `@` immediately after the id, so `tools-1@` does not match `tools-10@`): `gen-12` occurs twice (c2, c3), `ground-7` occurs twice (c1, c2), `tools-1` occurs twice (c1, c2). Every other anchor appears exactly once, at carrier 2.\n\nRefutation pass:\n- Facts drawn from the `why` prose (WP-GOV-1/WP-GOV-4, issue #199/#208, \"master 0a4c99d\", req-clause-guard verdict behavior, waiver ledger) all assert things about code/issues/commits NOT in these bytes — discarded regardless of accuracy.\n- Numbering-gap observations (no gen-10, ground-5/6, retr-12, tools-3/13, etc.) presume contiguity that isn't established here and their significance isn't derivable — discarded.\n- Digest width / two-field shape is close to schema-level obviousness — weak actionability, discarded.\n- The multi-pin structure survives: it rests only on the key literals themselves, is exhaustively verified by listing every key, and it changes behavior (amending gen-12, ground-7, or tools-1 text means updating two digests in this file, not one; tooling cannot assume one-pin-per-anchor or uniform carrier 2).\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"Apart from atlas-genesis.md#gen-12, which also has a carrier-3 pin, and atlas-grounding.md#ground-7 and atlas-tools.md#tools-1, which each also have a carrier-1 pin, every entry in this map is keyed at carrier 2 — making those three anchors the only ones whose text amendments require updating two digests in this file rather than one.\"}\n```"