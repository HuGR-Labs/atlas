"Looking at the bytes: this is a rendered property catalog where each `### PROP` block carries a `law`, `arbitrary`, `witness`, `teeth`, and sometimes a `note`. Scanning the notes for structural decisions rather than prose claims:\n\n- PROP-TOOLS-6's notes concern its own refusal to model the token/advisory bands — internal to that block.\n- PROP-TOOLS-15's note hands *exploitability* to an external owner (FR-12/billy) — a claim about people/process outside these bytes, so discarded.\n- PROP-TOOLS-11's note: \"the byte-identity-across-tiers arm is delegated to PROP-TOOLS-10 … PROP-TOOLS-11 renders the routing/push structure.\"\n- PROP-TOOLS-16's note: \"the CLI≡MCP ∀-input equivalence arm is DELEGATED to PROP-TOOLS-3 … this PROP renders the read-only-projection / faithful-delta / write-surface … arms.\"\n\nRefutation pass: are the delegation targets actually present in these bytes so the claim is checkable here? Yes — PROP-TOOLS-10's law visibly owns `mcp(a) ≡ poke(a) ≡ cli(a)` byte-identity, and PROP-TOOLS-3's law visibly owns `∀ x ∈ Input. cli(x) ≡ mcp(x)`. The notes point at blocks contained in this same unit, so the fact re-derives from the unit itself, not from unseen files (the method-tags citation is justification, not the fact). Checking exclusivity: only blocks 6, 11, 15, 16 have notes; only 11 and 16 delegate an arm to a sibling PROP in-file. The laws of 11 and 16 themselves contain no general transport-byte-identity-over-all-inputs conjunct, confirming the split is real in the law fields, not just asserted. Not a restatement of any block's title, and actionable: a test author or auditor must implement/verify those equivalence laws exactly once under TOOLS-3 and TOOLS-10, and must not read their absence from 11/16 as a coverage gap.\n\n```atlas-fact\n{\"claim\": \"Transport byte-identity laws are not duplicated across this catalog: PROP-TOOLS-11 and PROP-TOOLS-16 each explicitly delegate their byte/equivalence arm to a sibling property in the same file — PROP-TOOLS-10 (tri-transport mcp≡poke≡cli) and PROP-TOOLS-3 (∀x cli≡mcp), respectively — so those two sibling blocks are the sole carriers of the cross-transport equivalence laws.\"}\n```"