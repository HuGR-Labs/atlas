"STEP 1 — scratch reasoning (not persisted):\n\nCandidates drafted from close reading of the bytes:\n\n1. *\"The file defines both SECRET2 and SECRET1 literals.\"* — True and visible (`const SECRET1 = '[REDACTED]'`, used in the control test), but largely inferable from a test named \"held-out gate … fixture-1 too\"; weak actionability.\n2. *\"fetch(put()) is byte-lossless.\"* — Refuted as novel: the test title itself states \"byte-identical (no lossy compression)\", so it's mostly known from the name; only the specific sentinel bytes (0x00/0xff/0x7f) are extra detail.\n3. *\"`TranscriptRef` has exactly keys {sha, store} with store === 'cas'.\"* — Verified against the assertion `Object.keys(ptr).sort()` → `['sha','store']` and `ptr.store === 'cas'`; not derivable from imports since the type isn't shown. Viable.\n4. *\"scrub's replacement token is the literal `[REDACTED]`, with surrounding text preserved exactly.\"* — Pinned by strict whole-string assertions: `expect(text).toBe('... Authorization: Bearer [REDACTED] issued at 09:14 UTC ...')` and `expect(...).toBe('x [REDACTED] y')`. Not stated in any signature or import; not reliant on comments about other files; no counts to miscount. This changes what an implementer/reviewer of `scrub` must do (the marker string is contractual, and even the `ghp_` prefix must vanish per the `not.toContain('ghp_')` checks).\n\nRefutation pass on #4: does it lean on library knowledge? No. On a comment about elsewhere? No — the evidence is the `toBe` literals inside this unit. Exhaustive-count trap? Avoided by phrasing qualitatively. Restatement of name/signature? No — nothing in `transcript-3.5-a.heldout.test.ts` or the imported names reveals the placeholder. Survives. Choosing #4 over #3 as the more behavior-changing fact.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"These tests pin scrub()'s exact redaction output by whole-string equality: a detected credential is replaced with the literal marker \\\"[REDACTED]\\\" while every surrounding non-secret byte (e.g. 'Authorization: Bearer ' and 'issued at 09:14 UTC') must survive unchanged.\"}\n```"