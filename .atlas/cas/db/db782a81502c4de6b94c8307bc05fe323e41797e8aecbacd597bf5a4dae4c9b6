"STEP 1 — scratch reasoning (not persisted):\n\nScanning the bytes: helpers (`emptyRoot`, `mkTree`, `anchor`, `factAt`), a module-scope `subtreeOracle`, a module-scope `freshness = bindFreshness(subtreeOracle)`, and three `it` scenarios.\n\nDraft candidates and refutation pass:\n\n- *Fail-closed on missing qualifiedPath* — true (`cur !== undefined` guard inside `.every()`), but it is plainly visible in the oracle body and matches its own adjacent description; low surprise, and no test here exercises it anyway. Weak.\n- *Single module-scope binding shared by all tests* — true but a plain restatement of visible control flow. Discard.\n- *Header vs inline comment tension over `normalize`* — both sides assert things about modules/files not shown; rests on comments about elsewhere. Discard.\n- *Hashes are fabricated literals* — true, but largely inferable from names and borders on the header comment's framing about a sibling file. Discard.\n- *`factAt` hardcodes the stored freshness* — check against code: the return object contains the unconditional literal `freshness: 'FRESH'`; `factAt` is the only GroundedFact constructor in the file; every scenario builds its fact via `factAt`; SCN-KNOW-3c-2 feeds such a fact and asserts the call result is `'DRIFTED'`. So the stored field is a constant `'FRESH'` placeholder across all fixtures, including the drift-expecting scenario — nothing about the verdict comes from the fixture's stored field (that much is byte-visible; I deliberately do NOT claim how `bindFreshness` internally combines them, since its source is not shown). Signature `factAt(qualifiedPath, subtreeHash, displayLines?)` gives no hint of this, so it is not derivable from name/signature. No exact counts claimed beyond naming one specific verified scenario. Survives.\n\nSTEP 2 — emit surviving fact:\n\n```atlas-fact\n{\"claim\": \"The factAt helper unconditionally stamps every constructed GroundedFact with freshness:'FRESH', including the fixture fed to the SCN-KNOW-3c-2 scenario whose expected verdict is 'DRIFTED', so the stored freshness field is a fixed placeholder across all three scenarios and outcomes are asserted only on the value returned by bindFreshness.\"}\n```"