"STEP 1 — Reasoning scratch:\n\nCandidate A: \"GRD owns no FSPEC; the Atlas's only formal cluster is FSPEC-merge in Block KRN.\" — Refuted as out-of-bounds: the second half is a claim about *another* module (Block KRN) that cannot be checked in these bytes; only the GRD-side half is local, and the file name already suggests tag bookkeeping.\n\nCandidate B: \"Tag histogram is 11 reference-model + 2 PBT, zero formal.\" — I counted every method-tag line: 1/2/3/5/6/7/8/9/10/12/13 are reference-model, 4 and 11 are PBT; matches the report. Verifiable, but it barely changes what an engineer *does* beyond summarizing the table the file openly presents.\n\nCandidate C: \"The truth-gate only ever downgrades HOLDS→NA and is idempotent.\" — This is essentially the declared content hinted by the invariant's own label (\"monotone downgrade\"); too close to restating the named property.\n\nCandidate D: \"A whitespace-only reformat of the cited unit deliberately reads DRIFTED, and building a normalizer was refused.\" — Survives partially, but the strongest, least-guessable form is the boundary condition encoded in the actual slice expression: `src.slice(boundCommentStart(outer) ?? startIndex, endIndex)` extends the hashed region upward over the maximal **contiguous** comment run, so editing the bound doc-comment flips facts to DRIFTED, while the same bytes placed after a blank line fall outside the slice (`?? startIndex`) and stay FRESH. Both cases are stated in the up-property and the ADR-0014 amendment, consistent with each other and with the expression. Not derivable from the invariant's name alone, not an exhaustive-count claim, not resting on unseen code — the fallback in the literal carries the semantics.\n\nCandidate D survives. Emitting it.\n\n```atlas-fact\n{\"claim\": \"Drift hashing extends upward over the declaration's maximal contiguous leading comment run (its bound doc-comment, so editing that comment reads DRIFTED), but stops at the first blank line, so a blank-line-separated license header above the unit stays FRESH.\"}\n```"