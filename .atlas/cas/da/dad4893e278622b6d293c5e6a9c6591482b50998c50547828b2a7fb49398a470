"STEP 1 — scratch reasoning (not persisted):\n\nCandidate drafts and refutation passes:\n\n- *\"On RUN2's 200 facts, `predicateSlot` is absent 200/200\"* — this reports a measurement of `packages/knowledge/src/types.ts` and external run data; none of those bytes are shown. It's an accurate-sounding claim about elsewhere → discard.\n- *\"The admission engine already exists (`admit-harness.ts`) and only wiring is missing\"* — rests entirely on assertions about `genesis/src/admit-harness.ts`, `mine-gate.ts`, `compose-mine-admission.ts:75`, files not in this unit → discard.\n- *\"`PredicateSlot` is a closed 12-member vocabulary\"* — the 12 items are enumerable in the bytes (invariant, contract, precondition, postcondition, sideeffect, ownership, perf-bound, security-property, gotcha, rationale, dependency, definition — counted, 12), but \"closed/normative\" and the source location are claims about unseen code; and the enumeration is near-restatement of the doc's visible content → discard as either unverifiable or plain summary.\n- *nodeKey formula / collision-as-union* — stated design in §1, essentially a plain-content summary; the operative code is elsewhere → weak, discard.\n- **Supersession-vs-body conflict** — the unit opens with a dated banner (2026-08-16) declaring the `validated` seal CUT and ordering that every `validated`/ensemble-leg proposal in the doc be read as WITHDRAWN, leaving only the `proven` analysis standing; yet §6, unedited, still requests ratification of the original two-seal approach (`proven` **and** `validated`), and §3 still presents the two-seal framework as the recommendation. All clauses are checkable inside these bytes by direct comparison of sections; no reliance on unseen files. Not derivable from the filename, which suggests a live two-seal proposal; the lower \"Status: PROPOSAL, awaiting owner ratification\" line even points the skimmer the wrong way. It decisively changes behavior: an implementer must not build the independent-ensemble/adjudication-panel leg. Survives all refutation checks.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"The proposal is only partially in force: a dated supersession banner at the top cuts its `validated` (independent-ensemble) seal arm so that only the `proven`-seal analysis stands, yet §6 further down still asks the owner—unedited—to ratify the original two-seal approach.\"}\n```"