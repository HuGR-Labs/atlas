"Looking at the bytes: most striking candidates are (1) the 512-byte template cap with inclusive boundary, (2) `upsert` purity (input store untouched), (3) the DEDUP-over-nodeKey precedence in `writeDecision`, and (4) the shape of `stage()`'s return value.\n\nTesting each against the source:\n- The 512-cap: backed by literals (`'x'.repeat(513)` → false, `'x'.repeat(512)` → true), but the byte-vs-char distinction rests on a comment's claim (inputs are ASCII), weakening it.\n- Upsert purity: asserted (`currentNodes(s0).length` unchanged despite two CREATEs), plausible-but-partly inferable from the `upsert(store, req).store` idiom.\n- Dedup precedence: derivable from the paired `UPDATE` vs `DEDUP` assertions, solid but more of a routing-order detail.\n- The `stage()` shape: `expect('committed' in (stage(candidate()) as object)).toBe(false)` is a direct runtime assertion in these bytes. Nothing in the name `stage`, its usage, or its role (its output feeds `ratify`) suggests the staged handle omits the very field that matters downstream — a reasonable engineer would assume a staged record exposes `committed: false`. It changes what a consumer does: reading `.committed` off a staged handle yields `undefined`; the verdict exists only as `ratify()`'s return value. It rests on no comment claims about other files, involves no counts or enumerations, and isn't a restatement of name/signature.\n\nSurvivor: the `stage()`-has-no-`committed`-field fact.\n\n```atlas-fact\n{\"claim\": \"The handle returned by stage() carries no `committed` property at all — a staged proposal cannot report or perform its own commit, and a commit verdict exists only as the return value of a separate ratify() call.\"}\n```"