"STEP 1 — scratch reasoning (not persisted):\n\nCandidates drafted and refuted against the bytes:\n\n- *“Pure + total”* — asserted mainly by comments; actual throw/purity behavior depends on unseen callees (`store.get`, `rehydrateProjection`). Refuted as comment-dependent.\n- *“HEAD is read once per cover”* — plainly visible control flow (`const head = headSha?.();` above the loop); a summary of what the code obviously does. Discarded.\n- *“Empty packs are never flagged stale”* — true and code-derivable (`stale` only flips inside the row loop), but it is essentially spelled out verbatim in the comment and is close to a plain-summary consequence. Kept as backup.\n- *Sorting by `nodeId` ascending* — literal restatement of the visible sort. Discarded.\n- **Survivor:** In the loop body, two different freshness sources are used side by side: the emitted invariant gets `resolveFreshness(freshness, fact)` (the injected oracle's verdict), while the `stale` DRIFTED leg tests the raw stored field `fact.freshness === 'DRIFTED'`. So the row's carried verdict and the pack's stale signal are decided by different expressions and can diverge. Fully checkable from these two lines alone, not from any comment about unseen code; qualitative, not numeric; not inferable from the name/signature; changes how one interprets or wires the oracle vs. `stale`. Survives all refutation checks.\n\nSTEP 2 — emit result:\n\n```atlas-fact\n{\"claim\": \"Within the same loop iteration the `stale` DRIFTED leg tests the raw stored field `fact.freshness`, not the per-row verdict returned by `resolveFreshness(freshness, fact)` that is attached to the emitted invariant, so the row's carried verdict and the pack's stale signal come from two different expressions and can diverge.\"}\n```"