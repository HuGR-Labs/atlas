"STEP 1 — Scratch reasoning:\n\nReading the harness closely: `deps()` defines `upsert` as `(incoming) => { for (const f of incoming) h.grounded.set(f.id, f); return [...h.grounded.values()]; }`. That is a **Map keyed by fact id**, not an appending array. The ids come from `factFor`, which derives them deterministically from `qualifiedPath`, so both `genesis('beacon', 'rev-1d0a')` calls in SCN-GEN-7b-2 produce the same four ids (`nk-a.ts::G1` … `nk-d.ts::G4`). Any facts flowing through this harness therefore *overwrite* by key, capping `h.grounded.size` at four no matter how many times the controller upserts them.\n\nDraft fact: SCN-GEN-7b-2's \"0 duplicates / append would be 8\" assertion is satisfied by construction of the test double, and cannot fail even if the controller re-seeds duplicates.\n\nRefutation pass:\n- Derivable from these bytes? Yes — the Map-keyed `upsert` is literal code in this file, and the fixed ids are traceable through `cand` → `siteOf` → `factFor`.\n- Leaning on unseen library/controller internals? No — the claim is about the ceiling of the harness itself, not about what the controller does internally.\n- Resting on a comment's claim about elsewhere? No — the \"append would be 8\" phrasing appears in a code comment, but my claim is the inverse observation derived from the actual `upsert` implementation, not from trusting that comment.\n- Exact count risk? I verified four distinct ids item by item (G1–G4), but I'll phrase qualitatively (\"cannot accumulate duplicates\") so the fact doesn't hinge on a recount.\n- More than a restatement of name/signature? Yes — the file name and scenario names advertise an idempotency gate; nothing in them reveals that the store-side dedupe is baked into the test double, which means this golden cannot distinguish a deduplicating controller from a duplicating one. That changes what a maintainer would do when extending or trusting this golden.\n\nSurvives. Emitting.\n\n```atlas-fact\n{\"claim\": \"The harness's upsert stores facts in a Map keyed by fact id, so h.grounded can never accumulate duplicates — meaning SCN-GEN-7b-2's '0 duplicates / append would be 8' assertion holds by construction of the test double and cannot detect a controller that re-seeds duplicate facts.\"}\n```"