"STEP 1 — Reasoning:\n\nReading the bytes closely, several candidates surface:\n\n1. *Exit-code-2 rejection contract.* The code asserts `.toBe(2)` repeatedly, but the meaningful contrast (\"NOT ok(0), NOT error(1)\") lives in the comment's framing of external conventions. Weak — leans on commentary.\n2. *\"F5 FLIPPED\" reason-rendering history.* Explicitly past-tense comment about remediation before some change; not re-derivable at these bytes. Discard.\n3. *CAS persistence implied by absent `id:`.* The link between `id:` rendering and CAS persistence is a claim about the CLI/CAS modules, not visible here. Discard.\n4. *Two-repo isolation structure.* In `beforeAll`, `groundRepo` and `authzRepo` are both seeded with the byte-identical `files: { 'src/foo.ts': SRC }` map, and the ONLY argument that differs is the policy — `scopedPolicy('src')` vs `DEFAULT_POLICY`. Each suite exclusively touches its own repo. This is fully checkable against the literals in this unit, is invisible in the file name/signature, and materially changes debugging/refactoring behavior (e.g., you can't collapse the suites onto one shared repo, and a failure localizes to either the policy config or the grounding pipeline depending on which repo rejects).\n5. *Fixture contradiction (SRC returns 1, fact claims 2).* Also derivable in-unit, but the \"why it's ungrounded\" mechanism rests on how the external checker behaves; weaker than candidate 4.\n\nCandidate 4 survives verification: item-by-item, both `makeFixtureRepo` calls share the same `SRC` constant and identical file key; only the `policy` field differs. Not a restatement of the module name, no exhaustive counts, no reliance on out-of-file claims beyond mild structural inference.\n\nSTEP 2 — Emit:\n\n```atlas-fact\n{\"claim\": \"The two guardrail suites run against separate fixture repositories seeded with the byte-identical single file 'src/foo.ts' and differ only in the policy argument — scopedPolicy('src') for the grounding suite versus DEFAULT_POLICY for the authorization suite — so each suite's rejection traces to exactly one gate.\"}\n```"