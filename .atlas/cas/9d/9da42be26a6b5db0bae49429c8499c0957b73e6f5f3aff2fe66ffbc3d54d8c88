"STEP 1 — Reasoning (scratch):\n\nCandidates considered:\n1. *Authority binding (`atlas-query`, GOVERNANCE_SURFACE stays 5, not in WRITE_PATHS)* — rests on named constants/sets defined elsewhere; not re-derivable from these bytes. Discard.\n2. *`atlas emit` gives a distinct rev-mismatch refusal* — that is behavior of another unit (`emit.md`), asserted here about elsewhere. Discard per the rule about claims concerning other files.\n3. *Flag surface* — the Invocation section states outright, in this unit's own bytes: \"No INPUT flag is accepted — there is no `--id`, `--subtree-hash`, or `--rev` flag: those fields are ALWAYS computed… The one flag it honors is an OUTPUT mode: `--json`,\" and later, \"On a failed draft it is ignored and the same human error render is printed.\" This is an exhaustive claim about the command's own documented interface, made and verifiable within these bytes (invocation line shows only three positionals plus the one flag). Survives.\n4. *CREATE-vs-UPDATE determination* — interesting (a persist-nothing planner still inspects existing state), but its basis is cited via AUTHOR-10 rather than shown here. Weaker than candidate 3.\n\nSurvivor check for #3: not derivable from the name/signature alone (signature shows no flag policy); not a summary of plain function; changes what an engineer does (don't hunt for a `--rev`/`--id` override to pin or force values; instead capture `--json` for the round trip, and don't expect machine-readable JSON when the draft fails). Passes all filters.\n\nSTEP 2 — Emit result:\n\n```atlas-fact\n{\"claim\": \"`atlas draft` accepts no input flags whatsoever—there is deliberately no `--id`, `--subtree-hash`, or `--rev` override because those fields are always computed—and the only flag it honors is the output-mode `--json`, which is ignored on a failed draft so that errors always print the same human-readable render.\"}\n```"