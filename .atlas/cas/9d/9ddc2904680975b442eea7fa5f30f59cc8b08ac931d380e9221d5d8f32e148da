"STEP 1 — Reasoning (scratch):\n\nReading the bytes closely, this is a heavily amended ADR, and its amendment chain contradicts what the filename suggests. Draft candidates:\n\n- Candidate A: \"Only the dependency proven oracle exists as production code; tsc/LSP typeOracle and check synthesizers don't exist.\" — REFUTED as in-unit fact: this rests on CORRECTION 2's census of files not shown here (`verify-fact.ts`, `mine-gate.ts`) at a commit hash not visible. It's a claim about other code, not re-derivable at these bytes.\n- Candidate B: \"nodeKey folds predicateSlot into identity.\" — rests on `governed-emit.ts`, not shown. Discard.\n- Candidate C: The current ratified semantics after the amendment chain. Checking each clause against the bytes themselves: CORRECTION 3 explicitly retires the `validated` seal and converts two-seal to one-seal (\"build-leg (c) ... WITHDRAWN\"); CORRECTION 4 inverts admission (\"a proof AWARDER, never an admit gate\", abstain → admitted as `justified`, \"DROP_NO_CHECK ... retired\"); CORRECTION 5 makes `justified` a \"FIRST-CLASS seal value\" with an additive `derivation?: string`, reversing CORRECTION 4's absence-of-seal framing. Every clause is stated redundantly within this unit (Status line + three numbered corrections), needs no unseen code, and asserts no counts. Survives.\n- Does a competent engineer know this from the name? No — \"two-seal\" suggests the original proven/validated split; nothing in the title reveals that `validated` was cut pre-ship, that the surviving pair is proven/justified, or that abstention admits rather than drops. All three change implementation work (don't build an ensemble gate, don't emit DROP_NO_CHECK, add a derivation field).\n\nSurviving fact: the amendment chain's end state, fully attested inside these bytes.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"Despite its 'two-seal' title, the originally ratified `validated` LLM-ensemble seal was cut before ever shipping: after successive amendments the sound oracle only AWARDS `proven` and never gates admission, and an oracle abstention admits a grounded candidate as a first-class `justified` seal carrying its `derivation` instead of dropping it.\"}\n```"