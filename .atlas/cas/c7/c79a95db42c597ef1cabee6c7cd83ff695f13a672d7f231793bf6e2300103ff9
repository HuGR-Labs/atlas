"STEP 1 — REASONING (scratch):\n\nReading the bytes closely.\n\nCandidate A: \"applyRanking only rescores currently-active records, so archived rules are inert.\" Checkable: `rankRules(list(activeIds), events)` — yes, only activeIds are scored; archivedIds are never re-examined. Survives, but let me weigh alternatives.\n\nCandidate B: \"near-zero eviction happens even with free slots.\" Checkable in the `rankRules` loop condition (`s >= NEAR_ZERO_FRECENCY && injected.length < RULES_SLAB_SLOTS`). True, but this is loudly asserted in multiple docstrings in the same unit — low information value.\n\nCandidate C: \"`surfaceBudget` is not a live ledger reader.\" Its docstring says it \"Reads the cap + observed hit-rate for the drop-order,\" but the body is:\n```ts\nconst capTokens = surface === 'projectMem' ? MEMBER_TOK_CAP : 0;\nreturn { kind: surface, capTokens, hits: 0, hitRate: 0 };\n```\nIt takes only a `Budget['kind']`, touches no state, and hardcodes `hits: 0` / `hitRate: 0` every call. Additionally, scanning every occurrence of `ORCH_TOK_CAP` in the file: it appears only at its declaration — no expression references it, so the 800 orchestrator cap is enforced nowhere in this unit; any non-`'projectMem'` kind gets `capTokens: 0`.\n\nRefutation pass on C: derivable entirely from the function body and a scan of identifiers in the shown bytes? Yes. Rests on a comment's claim about unseen code? No — the contrast with the docstring is flavor; the truth rests on the literals `hits: 0, hitRate: 0`, the ternary, and the single occurrence of `ORCH_TOK_CAP`, all counted in these bytes. Exact-literal enumeration risk: I verified each literal item (`0`, `0`, `'projectMem'`, one occurrence of ORCH_TOK_CAP). More than a restatement of the signature? Yes — the signature `surfaceBudget(surface): Budget` suggests reading observed state; the code shows a stateless constructor returning zeros and applying only the member cap. Would it change what an engineer does? Yes — they would not wire drop-order logic to this function's hit-rate, and would not expect orchestrator-cap enforcement here.\n\nCandidate C survives. Candidates A and B discarded as weaker/loudly documented.\n\nSTEP 2 — RESULT:\n\n```atlas-fact\n{\"claim\": \"In this unit surfaceBudget is a stateless constructor that returns hits: 0 and hitRate: 0 on every call and assigns a nonzero capTokens only to the 'projectMem' kind — the exported ORCH_TOK_CAP (800) is never referenced anywhere else in the file.\"}\n```"