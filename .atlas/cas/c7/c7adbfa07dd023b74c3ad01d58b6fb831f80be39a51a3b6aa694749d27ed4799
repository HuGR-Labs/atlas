"STEP 1 — scratch reasoning (not persisted):\n\n- Candidate: \"`classifyThrown` sends any untagged thrown `Error` whose `name` is not in `ENGINE_FAULTS` to `'refused'`, not `'internal-fault'`.\"\n- Refutation pass: Is it in the bytes? Yes — the final line of `classifyThrown` is literally `return ENGINE_FAULTS.has(e.name) ? FAULT_INTERNAL : 'refused';`. Control flow, not comment-trust.\n- Does it depend on claims about other files/tasks/past behavior? No — the rule is entirely local; the surrounding prose merely agrees with it.\n- Enumeration risk? I verified the set contents item-by-item (TypeError, RangeError, ReferenceError, SyntaxError, EvalError, URIError — six entries); I can phrase it as \"the module's engine-fault set\" to stay qualitative while noting the mechanism.\n- Non-obvious from name/signature? Yes — the signature \"Classify a THROWN value into one of the three classes\" gives no hint that the *residual* for ordinary constructed Errors is `refused`; an engineer would plausibly assume untagged bugs default to `internal-fault`. It changes what they do: a leg throwing a custom `Error` subclass for a real defect must attach the `atlasFault` tag or it ships as a deliberate refusal wearing caller-facing semantics.\n- Alternatives considered and discarded: \"required-presence is not enforced up front\" (true in these bytes but the *why* leans on comments about golden tests elsewhere); \"enum is checked even when no `type` is declared\" (visible but low impact); \"duplicate module instances are why tagging is structural\" (rationale lives in prose about hazards not shown).\n- Surviving fact: the residual classification rule. Emitting.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"In classifyThrown, an untagged thrown Error whose name is not in the module's ENGINE_FAULTS set (the six JS engine error names) is classified as 'refused', not 'internal-fault', so a leg throwing a custom Error subclass for a genuine bug must tag itself via atlasFault or it files as a deliberate refusal.\"}\n```"