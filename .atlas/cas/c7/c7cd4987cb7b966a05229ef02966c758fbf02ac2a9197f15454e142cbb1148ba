"STEP 1 — scratch reasoning (not stored):\n\nReading the run section closely: `note()` is defined as `const note = (msg) => fail.push(msg)` — every note lands in the single `fail` accumulator. The tail has no severity filter: `if (fail.length > 0) { …; process.exit(1); }`.\n\nNow the candidate fact: the comment above the undeclared-edge loop says such an edge \"is not itself a layer violation … report it so the drift is visible.\" But the code pushes every undeclared edge through that same `note()` → `fail`, so it *is* fatal: one undeclared source import produces a FAIL verdict and exit 1.\n\nRefutation passes:\n- Derivable from these bytes alone? Yes — `note`'s definition, the push loop, and the unconditional exit-on-any-fail block are all in this unit. No reliance on `workspaceGraph`'s internals beyond its returned `undeclared` being iterated.\n- Resting on a comment's claim about elsewhere? No — the comment is local prose; my claim is established by the accumulator/exit control flow, which actually sharpens (arguably contradicts) the comment's \"not itself a layer violation\" framing.\n- Exact count / exhaustive enumeration? No.\n- More than a restatement of name/signature? Yes — nothing in \"harness/gates/layer-guard.mjs\" suggests manifest drift is a hard-failing condition; an engineer assuming it's advisory-reporting would be wrong about what turns CI red.\n\nSurvived. Emitting. \n\n```atlas-fact\n{\"claim\": \"An undeclared source import (an edge missing from the package manifest) is pushed by the same note() into the same fail accumulator as hard ARCH violations, so despite the adjacent comment calling it 'not itself a layer violation', a single undeclared edge makes layer-guard print FAIL and exit 1.\"}\n```"