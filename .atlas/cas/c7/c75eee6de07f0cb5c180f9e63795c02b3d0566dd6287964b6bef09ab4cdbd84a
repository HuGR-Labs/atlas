"STEP 1 — REASONING (scratch):\n\n- Candidate: citations anchor by subtree hash, not line numbers, and the hash is over raw bytes with no normalization, so reformatting the cited unit itself drifts the fact.\n- Refute against bytes: this is stated verbatim in the \"Why grounded\" section (\"anchors to a structural unit … by the hash of its subtree, **never to line numbers**\" and \"the hash is over raw bytes and there is no normalization step\"). The grounding.md link is only offered for *rationale*; the factual assertions themselves are fully present in this unit's text, including the `BROKEN` state name used elsewhere in the same document. No dependency on unseen code.\n- Not derivable from name/title (`docs/explanation/knowledge.md` → \"an explanation of Knowledge\"): nothing in the name implies unnormalized byte-hashing or reformat sensitivity.\n- No exact counts or exhaustive enumerations asserted. Not a restatement of the name or a generic summary — it specifies a concrete mechanism and a surprising failure mode that changes implementation choices (never cite lines, never normalize, expect false `BROKEN` on reformat).\n- Discarded alternatives: \"retrieval uses no embeddings/no RAG\" and \"advisory edits-in-place vs predicate supersedes\" also survive, but the anchoring/raw-byte fact is the most specific and least guessable; emitting exactly one.\n\nSTEP 2 — RESULT:\n\n```atlas-fact\n{\"claim\": \"A fact's citation anchors to a structural unit (symbol/block/file) by the hash of its subtree — never line numbers — and because that hash runs over raw bytes with no normalization step, merely reformatting the cited unit drifts the fact to BROKEN even if its code semantically didn't change.\"}\n```"