"STEP 1 — Scratch reasoning:\n\nReading the bytes: in SCN-GEN-16a-1, `boastful` is built as `cand({ self_score: 0.99, importance: 1 })` and `modest` as `cand({ self_score: 0.01, importance: 0 })`, over the shared base `{ site, grounded: true }`. Comparing the two `over` objects item by item: exactly two fields differ — `self_score` (0.99 vs 0.01) and `importance` (1 vs 0); everything else is identical via the fixture. The test then asserts `expect(boastful).toEqual(modest)`.\n\nRefutation pass:\n- Derivable from these bytes? Yes — the literals and the equality assertion are right here; no reliance on `../src/usefulness.js` internals or on any comment's claim about other files.\n- Comment-dependent? No — the test title mentions only `self_score`, but my claim rests solely on comparing the two object literals against the `toEqual` assertion, not on the title or the inline commentary.\n- Exhaustive/count risk? \"Both self_score and importance\" — I listed the differing keys directly: those two, no others. Verified.\n- Restatement of name/signature? No — the test's own name advertises independence from `self_score` only; the co-varying `importance` is invisible from the name and signature and is discoverable only by diffing the literals.\n- Actionable? Yes: an engineer adding or changing any `importance`-based logic in the gate would break this \"self_score\" test unexpectedly, and conversely cannot treat it as an isolated single-variable witness — the conflation changes how they mutate or extend the test.\n\nSurvives. Other candidates (ledgerOf's `() => 0` default calibrate being shadowed by the separately injected gate calibrate in SCN-GEN-16e-1; the lazy `servedSet` thunk) are weaker — closer to plain-code summaries with little behavioral consequence — so I drop them in favor of the conflation fact.\n\n```atlas-fact\n{\"claim\": \"In SCN-GEN-16a-1 the boastful and modest candidates differ in both self_score and importance (0.99 with 1 versus 0.01 with 0), so the asserted equality of their seed decisions constrains the gate to ignore importance as well, not just the self-assessment score the test name advertises.\"}\n```"