"**Step 1 — scratch/refutation pass**\n\nCandidates considered:\n\n1. *\"Trailers travel verbatim through clone\"* — restates SCN-PERSIST-1a-1's own assertions/title. Discard.\n2. *\"`{ clone }` type-checks against `SourceApi`\"* — barely actionable; could hold with optional extras. Weak. Discard.\n3. *\"`id()` is deterministic over plain object literals\"* — inferable from the import name and header framing; borderline restatement. Discard.\n4. *\"PROP-PERSIST-1's loop asserts nothing for datums homed only in `trailer`/`note`\"* — survives refutation (control flow verifiable: the `if`/`else if` falls through for those homes), but reads more as a gap observation than a design fact; weaker practical payoff than candidate 5.\n5. **Generator-constrained lock-in scope**: check against the bytes, not the comment. All string-producing arbitraries in this unit are enumerable: `hexKeyArb` draws from `'0123456789abcdef'`; `leafArb`'s string branch from `'abcdefghij0123456789 '`; `bodyArb`'s dictionary keys from `'abcdef'`; remaining leaves are `null`/boolean/integer (no punctuation, no decimal points since `fc.integer()` excludes floats). None of these pools contains `/`, `:`, `\\`, `.`, `;`, `,`, or `-`. Now test each LOCK_IN alternative against generatable content: `/Users/`, `/home/`, `file://` need slashes/colons — impossible; `[A-Za-z]:\\\\` needs colon+backslash — impossible; `\\.db\\b` needs a preceding dot — impossible (even a key ending in \"db\" fails without the dot); `application/octet-stream` needs hyphens — impossible; `;base64,` needs semicolon/comma in content — impossible (commas in the dump can only come from the exporter's own serialization). Verified item by item. The fact is fully re-derivable from this unit's literals and regex; the nearby comment merely narrates what the code itself proves, and I invoke nothing about KERNEL-6 or other files. It is not a restatement of the test name (\"adds 0 lock-in encodings\") — the *reason* the assertion is meaningful (inputs are pre-sanitized, so only exporter-emitted text can trip it) is invisible in the signature and changes how one extends `casArb` or interprets a green run. **Survives.**\n\nEmit the survivor.\n\n```atlas-fact\n{\"claim\": \"Every string the PROP-PERSIST-9 store generator can draw comes solely from the character pools '0123456789abcdef', 'abcdefghij0123456789 ', or 'abcdef' (with null/boolean/integer leaves), none of which can spell any LOCK_IN alternative, so the no-lock-in regex scan of the dump can only ever fail on characters exportStore itself emits, never on generated content.\"}\n```"