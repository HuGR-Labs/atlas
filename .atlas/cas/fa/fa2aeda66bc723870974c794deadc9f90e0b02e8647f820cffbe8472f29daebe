"STEP 1 — REASONING (scratch):\n\nReading the bytes closely. Candidates drafted and tested:\n\n- *Candidate 1:* \"Goldens are generated, not hand-written.\" — Refuted: this is already in the frontmatter `description` (the unit's \"signature\"); an engineer invoking the skill sees it before opening the body.\n- *Candidate 2:* \"Goldens are keyed `SCN-<MODULE>-<n>[c]-<k>`.\" — Weak: near-restatement of naming conventions, low behavioral surprise.\n- *Candidate 3:* Internal tension — Scope paragraph says `source: INV-x` while examples/self-check say `source:` the REQ. Present in the bytes, but it reads as an editorial inconsistency rather than a directive fact, and asserting it risks misreading intent.\n- *Candidate 4 (anti-rot):* The skill unconditionally mandates that reference-model goldens' conformance oracle be written **in the build language** and **reused as the mock** in unit tests, so spec↔code drift breaks the build. Check: derivable directly from the Anti-rot section (\"the one mechanically-real drift fence… adopt it wherever a reference model exists\") and confirmed again by the self-check bullet. Not mentioned anywhere in the description, so not knowable from name+signature. Its truth rests on this unit's own normative statement, not on a comment about unseen code (the ShardStore citation is attribution, not the load-bearing claim). No counts or enumerations involved. It would genuinely change what an implementer does (write one shared artifact serving as both oracle and mock, instead of separate stubs). Survives all refutations.\n\nSTEP 2 — RESULT:\n\n```atlas-fact\n{\"claim\": \"For reference-model goldens, the skill mandates the conformance oracle be written in the build language and reused as the mock in unit tests, so that any drift between the spec model and the code breaks the build.\"}\n```"