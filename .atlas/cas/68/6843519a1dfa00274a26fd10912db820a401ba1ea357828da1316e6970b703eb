"STEP 1 — Reasoning scratch:\n\nCandidates considered:\n\n1. *\"The module defines 15 exported reason constants.\"* — I counted them (15), but the count isn't the interesting part and borders on restating what's plainly visible; discard as low-value.\n\n2. *\"REJECTED_MALFORMED_SCOPE documents that scopes are used as property keys and coerce.\"* — The coercion semantics are real JS, but the *claim that the authz lookup uses the scope as a property key* rests on the string's prose about `authz` code not shown here; discard.\n\n3. *\"Wrong-typed subtreeHash is caught downstream, not here.\"* — Behavioral claim about `isGrounded`/`driftDetect`, files not in these bytes; discard.\n\n4. Substring-collision candidate: Read the two TARGET literals closely. `REJECTED_UNAUTHORIZED_TARGET` opens with `unauthorized for target:`. `REJECTED_UNVERIFIABLE_TARGET` — scanning its own literal text — contains, contiguously on one line, \"...gets \\`unauthorized for target\\` in BOTH byte-states...\". So the *literal characters of one exported reason embed the entire distinguishing phrase of the other*. Refutation attempts: (a) Am I leaning on the header comment's claim that \"suites pin them by equality on the text before the first colon\"? No — I am deriving solely from the two string literals themselves, both fully present in these bytes; the embedded phrase is physically in `REJECTED_UNVERIFIABLE_TARGET`'s value, unbroken by concatenation. (b) Is it a restatement of name/signature? No — the names look cleanly disjoint; nothing in `export const REJECTED_UNVERIFIABLE_TARGET = '...'` suggests its text quotes its sibling. (c) Exhaustive/count claim? Avoided — qualitative phrasing only. (d) Past-tense/stale-comment risk? None — the collision exists in the current bytes regardless of what any consumer does. What it changes: anyone filtering, logging, or testing these reasons by substring (`includes('unauthorized for target')`) will misclassify an unverifiable-target refusal as an authorization refusal — semantically opposite disclosures (one presumes caller authority, one denies it). Only the pre-colon prefix (`unverifiable target` vs `unauthorized for target`) discriminates them. Survives.\n\nSTEP 2 — Emit result:\n\n```atlas-fact\n{\"claim\": \"The message string of REJECTED_UNVERIFIABLE_TARGET literally contains the phrase 'unauthorized for target', so substring matching on reason text conflates it with REJECTED_UNAUTHORIZED_TARGET — only the text before the first colon ('unverifiable target') discriminates between them.\"}\n```"