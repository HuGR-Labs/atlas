"**STEP 1 — scratch reasoning**\n\nCandidate A: \"GEN-5 forbids auto-promotion.\" → Refuted as emitted-worthy: this is the entire point of the file's comments and names (`canAutoPromote`, `ROUTER_EDGES`); an engineer already knows it from the header.\n\nCandidate B: \"The interview cap is 20.\" → Restates a literal sitting next to its own name (`INTERVIEW_CAP = 20`). Discarded.\n\nCandidate C: \"No `auto_promote` edge exists; exactly one edge reaches `ratified`, via `interview`.\" → I counted `ROUTER_EDGES`: two entries, only the second has `to === 'ratified'` and it carries `via: 'interview'`. True, but largely forced by the `via` union type and the constant's obvious content; low action-value. Discarded.\n\nCandidate D: \"`makeAlign`'s `interview` drops the deferred tail.\" → Check against bytes: the body is `const { batch } = assembleInterview(open); return deps.ratifier.ratify(batch);` — `deferred` is destructured away and never surfaced, stored, or signaled. The signature `interview(open): readonly Ratified[]` gives no hint whether overflow questions are queued, rejected, or reported; only reading this body shows they silently vanish from the session. Truth rests solely on this unit's declarations and control flow — no external-module claim needed. Survives.\n\nCandidate E: \"`batch.cap` is always 20 even for short lists.\" → True (literal copied into the batch), but minor and predictable from `assembleInterview`. Discarded in favor of D.\n\n**STEP 2 — result**\n\n```atlas-fact\n{\"claim\": \"In makeAlign, the deferred tail is silently discarded: interview() destructures only the capped batch and hands it to the injected ratifier in a single call, so open questions beyond the top-20 cap receive no ratification and produce no signal back to the caller.\"}\n```"