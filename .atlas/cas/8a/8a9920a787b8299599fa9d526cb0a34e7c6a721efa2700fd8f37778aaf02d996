"Reading closely: the `(c3)` loop iterates over three literal sentinel forms — `'NO-FACT'`, `` '`NO-FACT`' ``, `'  no-fact \\n'` — and for each asserts `rec.abstained === true` while `r.stdout === result` verbatim. The lowercase `'  no-fact \\n'` variant appears nowhere in any comment (the inline comment only names the bare and backtick-wrapped cases), so requiring `abstained:true` for it entails that the wrapper's sentinel match normalizes case and surrounding whitespace, not just exact-matches `NO-FACT`. Meanwhile tests `(a)`, `(c)`, and `(c2)` establish the contrast: a normal result flows through with `abstained:false`, while error and blank envelopes produce *empty* stdout — so the sentinel is uniquely both booked as an abstention and passed through byte-for-byte, trailing newline included. Every element of the candidate fact is checkable against the assertions in these bytes; nothing rests on the `#201` reference or on claims about files not shown. Survives refutation.\n\n```atlas-fact\n{\"claim\": \"This suite pins that the wrapper flags the NO-FACT abstain sentinel even when it arrives lowercased, whitespace-padded, or markdown-backtick-wrapped ('  no-fact \\\\n' must count), yet still echoes those raw bytes to stdout verbatim — unlike error or blank envelopes, which yield empty stdout.\"}\n```"