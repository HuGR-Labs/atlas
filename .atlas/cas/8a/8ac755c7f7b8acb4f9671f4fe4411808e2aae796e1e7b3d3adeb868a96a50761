"STEP 1 — scratch:\n\n- Header comment talks about GATE protocol, WP-1.3-a.KERNEL, author-blinding, missing `-2` fixtures — all claims about process/other files. Discard as fact material.\n- What's actually in the bytes: the \"independent\" generators `evArb`/`logArb` are built from very small literal sets — `fc.constantFrom('dead','beef','cafe','f00d','0bad')` for contentHash (I count 5 literals), `fc.option(fc.constantFrom('nkX','nkY'), {nil: undefined})` for nodeKey (2 values plus absent), integer seq −9000..9000, payload integers 100–999, log length capped at 7 with dedupe-by-id in `logOf`.\n- Refutation check: is this derivable from the shown bytes alone? Yes — pure literals in `evArb`, no reliance on comments or unseen modules. Does it contradict anything here? No. Is the enumeration exact? Yes, counted item-by-item from the `constantFrom` calls. Is it more than a restatement of the name (\"heldout-krn-event\") or the test titles (\"∀-laws\")? Yes — nothing in the naming suggests the hash space is a five-string closed set. Would it change what an engineer does? Yes — anyone assessing collision/idempotency coverage or adding cases learns the laws run over a tiny vocabulary (and incidentally that test 9a's 'ZZ1'/'ZZ2' fall outside even that set).\n- Other candidates (chaining return of `append`, `logOf` dedupe shrinking logs below generation length, fp's per-nodeKey sorted-hash fingerprint shape) are weaker or partly inferences about unseen `src` semantics; the vocabulary fact is the strongest survivor.\n\nSTEP 2 — emit:\n\n```atlas-fact\n{\"claim\": \"Despite exercising universal laws, the event generator draws every contentHash from just five fixed literals ('dead', 'beef', 'cafe', 'f00d', '0bad') and every nodeKey from 'nkX', 'nkY', or absence, so all property runs operate over this small closed vocabulary rather than arbitrary hash values.\"}\n```"