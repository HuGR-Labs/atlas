"STEP 1 — Scratch reasoning:\n\nScanning candidates:\n\n- *Header claims* (WP IDs, held-out `-2` fixtures, EPIC-3-b head-resolution, sealed kernel seam): all assertions about process or other files — discarded, not re-derivable from these bytes.\n- *Oracle asymmetry* (`serializeState` from module-under-test vs local `serializeArchive`): the origins of both serializers are visible in the import outline, so this is close to signature-level knowledge. Discarded as too easily inferred.\n- *Comment-vs-code mismatch* (\"byte-identically\" respawn vs structural `toEqual`): leans on interpreting library semantics against a comment's wording. Too fragile. Discarded.\n- *`shuffle` empty-perm fallback*: verifiable control flow, but the downstream consequence for what an engineer does is thin.\n- *Unused import*: scanning every occurrence of `collect` — it appears exactly once, in the import list from `'../src/reconstruct.js'`. Checking the other nine named imports item by item: `archive` (5a–5d, PROP-5) ✓, `del` (SCN-PERSIST-5a-1) ✓, `forget` (5d-1, PROP-PERSIST-5) ✓, `mergeArchive` (5b-1, PROP-PERSIST-5) ✓, `reconstruct` (12a-1) ✓, `replayFromExport` (2a-1, 2c-1, PROP-PERSIST-2) ✓, `respawn` (5c-1, 5d-1, PROP-PERSIST-5) ✓, `rewind` (12b-1, PROP-PERSIST-12) ✓, `serializeState` (throughout) ✓. So `collect` is uniquely imported-but-unused — verifiable by full-text scan of this file alone, not dependent on any comment, and not visible from the file's outline (which shows the import but not its non-use). Actionable: dead import or a signal that `collect` in the module under test has zero coverage here. Survives.\n\nSTEP 2:\n\n```atlas-fact\n{\"claim\": \"collect, imported from '../src/reconstruct.js', is never invoked anywhere in this test file — it is the only import from that module with zero uses.\"}\n```"