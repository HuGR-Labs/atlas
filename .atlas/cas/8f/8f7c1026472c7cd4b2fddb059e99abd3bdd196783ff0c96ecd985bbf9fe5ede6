"STEP 1 — scratch reasoning (not persisted):\n\nScanning candidates:\n\n1. *Declared families include AWS access keys / exact union prefix* — the literal `(?:gh[pousr]_|xox[baprs]-|github_pat_|AKIA)` is in the bytes, but it mostly restates what \"cross-family gate\" implies; weak actionability.\n\n2. *Undeclared shapes (JWT, PEM, AWS secret key) pass through unredacted* — supported by the `expect(scrubText(undeclared)).toBe(undeclared)` loop literals, but largely inferable from \"only declared families are covered,\" which the file's own framing announces.\n\n3. *Slack over-redaction of hyphen-joined text* — pinned by literals, but the file's describe-block title already says \"the DECLARED Slack over-redaction,\" so a reader of the unit gets close to this from the names inside it.\n\n4. *Retroactive rewrite of already-emitted buffer bytes* — checking the contingent-tail test closely: `admitToBuffer(empty, '[REDACTED]')` is byte-exactly `[REDACTED]`, and then `admitToBuffer(partial, '-ABCDEF')` is byte-exactly `ghp_ABCDE[REDACTED]`. The first ten bytes of the previously returned buffer were the placeholder; the next returned buffer replaces them with raw prefix + new placeholder. Nothing in the name `admitToBuffer` or its `(buf, chunk) => Uint8Array` shape suggests the function revises prior output rather than appending. Refutation attempts: (a) does this rest on a comment? No — it is carried entirely by two consecutive `assertExactBytes` calls on the evolving buffer; (b) is it derivable from these bytes? Yes, the assertions are in this unit; (c) is it an unverified count/enumeration? No, it's a qualitative behavioral property demonstrated by concrete literals; (d) would it change what an engineer does? Yes — it means the returned buffer is not append-only/stable, so previously returned bytes must not be treated as final or safely flushed downstream, and GATE 2's \"redaction never lengthens\" bound exists precisely to make such rewrites possible. This survives.\n\nSTEP 2 — emitting the surviving fact.\n\n```atlas-fact\n{\"claim\": \"In the streaming path, admitToBuffer can rewrite bytes it previously returned rather than only appending: after '[REDACTED]' has been admitted and buffered as '[REDACTED]', admitting '-ABCDEF' returns a buffer byte-exactly equal to 'ghp_ABCDE[REDACTED]', so earlier placeholder output is retroactively revised and must not be treated as final or flushed downstream.\"}\n```"