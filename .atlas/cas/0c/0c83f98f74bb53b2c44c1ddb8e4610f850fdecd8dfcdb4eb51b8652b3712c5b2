"STEP 1 — REASONING (scratch):\n\nScanning the bytes: most of the document is prose asserting things about *other* artifacts (KERNEL-9/10/11 clauses, PERSIST-11, method-tags files, git config) — per the rules, those are facts about elsewhere and don't survive. What is genuinely *in* this unit as code is the embedded TypeScript reference model: `RefLog`, `mergeNode`, `head`, `supersededBy`, `fold`.\n\nCandidates drafted and refuted:\n\n- *\"Tie-break direction is `max`\"* — refuted: the prose explicitly pins this (\"This spec pins **`max`**\"); known from reading the unit's own narrative. Discarded.\n- *\"`RefLog.id` excludes seq by pinning it to 0\"* — borderline: mechanism visible in `{ ...e, seq: 0 }`, but the prose already states canonicalForm omits seq; close to a restatement of documented intent. Weak.\n- *\"`mergeNode` takes x's nodeKey unconditionally\"* — visible, but effectively plain from the union semantics and fold only merges same-key nodes. Doesn't change what anyone does.\n- *\"`head(): Event` can actually return `undefined`\"* — checking against the bytes: the body builds `fresh` via `.filter(...)`, sorts, and returns `[0]`. Nothing in the shown code guarantees a surviving element: `fresh` is a free boolean on `Event`, decay/staleness is referenced in the unit itself, and `supersededBy` can mark entries out; an all-stale or fully-superseded node (or an empty `Node`, which the type permits) makes the filtered array empty, so runtime yields `undefined` while the annotation promises non-nullable `Event`. Truth rests only on the body's control flow plus the literal return type — both present in these bytes. No comment claims about unseen code are involved. Not derivable from the name/signature — in fact it *contradicts* the signature. It changes what an implementer/differential-test harness must do (guard the empty case). Survives all refutation checks.\n\nSTEP 2 — RESULT:\n\n```atlas-fact\n{\"claim\": \"Despite being declared to return a non-nullable `Event`, the reference `head()` indexes `[0]` of the freshly-filtered entry list and therefore returns `undefined` at runtime for any node containing no entry that is both `fresh` and not superseded.\"}\n```"