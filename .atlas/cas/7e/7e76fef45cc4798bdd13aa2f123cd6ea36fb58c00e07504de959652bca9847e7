"STEP 1 — scratch refutation pass over these bytes:\n\nMy first instinct was to reach for the design rationale at the top of the file — but almost every load-bearing statement there is a claim *about other artifacts*: other files, issue numbers, past measurements, papers. Under this prompt's own rule, those are facts about THAT material, not about this unit's bytes, so they're all discarded regardless of accuracy.\n\nNext I checked whether anything in the operative text qualifies. The contract clauses (one fenced block, single `claim` field, bare-token abstention, block-count admission) are plainly stated in the prose — emitting them would just restate what the text openly says. The distribution claim (\"most units hold none\") rests on evidence not shown here, so it fails the derivation test too.\n\nWhat does survive is a structural fact in the body, not the comment: this shipped prompt is not a finished prompt but an uninstantiated template. The anchored-unit envelope is embedded literally in the file — an opening tag carrying `{{PATH}}` and `{{UNIT}}` attributes, a lone `{{SOURCE}}` placeholder on its own line, then a matching closing tag — sitting exactly where the analyzed unit appears when the prompt is rendered. I verified the placeholder occurrences directly against the bytes (each of the three appears exactly where described, once), so no enumeration risk. This is checkable by reading the file's own literals, independent of any comment; it goes beyond what the path name suggests (you cannot tell from \"propose.md\" that the envelope markup and placeholder vocabulary live in the artifact itself); and it changes what an engineer does — renaming or dropping any of those tokens breaks instantiation, and anyone testing or reusing the prompt must substitute all three slots rather than treating the file as ready-to-send text.\n\nIt survives. Emitting.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"The shipped prompt is an uninstantiated template: it embeds the anchored-unit envelope literally as <unit path=\\\"{{PATH}}\\\" name=\\\"{{UNIT}}\\\"> wrapping a lone {{SOURCE}} placeholder line, so the analyzed unit's source, path, and name are injected at run time rather than present in the file.\"}\n```"