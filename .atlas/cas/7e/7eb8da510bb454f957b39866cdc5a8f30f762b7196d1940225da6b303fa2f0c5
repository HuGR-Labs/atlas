"STEP 1 — Scratch reasoning (not persisted):\n\nCandidates drafted from close reading of the bytes:\n\n1. *Claim-set replacement on tier raise.* The UPDATE branch computes `raises` (req.tier present, on-lattice, ≠ 'T2', and prior.tier undefined-or-weaker) and then sets `claims: [req.claimNorm]` — full replacement — instead of the union path. Verifiable purely from the code; notably contradicts the top-of-function docstring's \"UPDATE set-unions the advisory claim in place,\" which makes it genuinely unknown to anyone trusting the summary. Strong candidate.\n\n2. *Refusals are thrown, not returned.* Two `throw` statements sit in the body: `ClosedSlotError` for a present-but-unrecognized slot (before routing even runs), and `GovernanceAuthorityError` (carrying a `reason` discriminant) when an UPDATE/SUPERSEDE displaces an incumbent whose declared tier the write weakens (`isWeakerTier(req.tier, incumbent.tier)`) or whose scope the write differs from (`req.scope !== incumbent.scope`). The signature returns `{decision, store}` and nothing in these bytes shows a refusal ever flowing through that return value. Fully checkable from declarations/control flow; no reliance on cross-file commentary. Strong candidate — and it changes caller behavior directly (try/catch required; a caller consuming only `.store`/`.decision` will not see these refusals).\n\n3. *Seal never carried forward.* Both UPDATE and SUPERSEDE strip `prior.seal` via destructuring and re-add it only from `req.seal`. Code-visible, but arguably adjacent to what the extensive inline comments announce; still valid, though I prefer a fact less dependent on reading prose around it.\n\n4. *SUPERSEDE skips re-stamping relation/transition/test-vacuity/answerRef carriers.* Verified by comparing the spreads branch-by-branch (CREATE and UPDATE stamp all four helpers; SUPERSEDE stamps none of them). Accurate, but requires enumerating across branches and borders on \"what the omission means,\" which leans partly on surrounding prose.\n\nRefutation pass: Candidate 2 survives best — every clause is checkable against the shown code (the two throw sites, the guard conditions in `authorityRefusal`, the `reason` field on the class, the shape of `UpsertResult`), it is not a restatement of the name/signature, and it contains no unverifiable counts or references to unseen files. Candidates 1, 3, 4 survive factually but are narrower; I emit one fact, choosing the interface-level surprise.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"In `upsert`, refused writes surface as thrown errors rather than through the returned UpsertResult: a present-but-unrecognized slot throws `ClosedSlotError` before routing, and an UPDATE or SUPERSEDE that displaces an incumbent whose declared tier the write weakens or whose scope the write differs from throws `GovernanceAuthorityError` carrying a machine-readable `reason` field.\"}\n```"