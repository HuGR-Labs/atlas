"STEP 1 — Reasoning (scratch):\n\nReading the bytes: `include` is `[\"src\", \"test\"]`, `outDir` is `dist`, extends the root base config, and references `../contracts` and `../kernel`.\n\nCandidates:\n1. \"It depends on `contracts` and `kernel` via project references.\" — Survives byte-check (literal in the `references` array), but it is close to a plain summary of the file's obvious purpose for a monorepo tsconfig; weaker as a surprise.\n2. \"Output goes to `dist` / extends the root base config.\" — Convention-level, near-restatement; discarded.\n3. \"`test` sources are compiled in the *same* program as `src`.\" — Directly checkable from the `include` literal; not derivable from the unit's name alone, and it contradicts the common assumption that tests live in a separate config. Consequence (type errors in test files fail this package's own compile) follows from core tsc semantics applied to these bytes, not from any invisible file or comment. No comments exist here to lean on; no counts asserted beyond what I verified item-by-item (two include entries, both named). One caution: I must NOT extend this to \"test output is emitted into dist\" since that depends on a `rootDir` setting in the unseen base config — discarded that clause.\n\nCandidate 3 survives; it is actionable (adding a broken test file breaks this package's build/typecheck).\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"The `test` directory is compiled in the same TypeScript program as `src` (both are listed in `include`), so type errors in test files fail this package's own build rather than being caught only in a separate test config.\"}\n```"