"STEP 1 — SCRATCH REASONING\n\nCandidate drafts and refutation:\n\n1. *\"The #189 fix renamed the SCIP symbol from `local S` to `sym S`.\"* — REFUTED. That is past-tense history about another module (`deriveEdges`) and an issue number; the current bytes only contain the literal `'sym S'`. Not re-derivable here as stated.\n\n2. *\"SCIP affects only dependency edges, never a file's subtreeHash.\"* — REFUTED. Pure comment claim about runtime internals (`readScipOrEmpty`, hashing) not present in these bytes.\n\n3. *\"T1 facts route to full-ratify via ATLAS_RATIFY_TOKEN.\"* — REFUTED. Comment claim about routing logic living elsewhere.\n\n4. *\"Dependency mode excludes the queried node's own fact.\"* — Checking against executable code, not prose: `factDep` is drafted on `'src/dep.ts'` and emitted (setup throws if emission fails); the test then runs `['query', 'src/dep.ts', '--by', 'dependency']` and asserts `expect(dep.stdout).not.toContain(CLAIM_DEP)` while asserting both `src/use.ts` facts ARE present (`lines.length === 2`, `toContain(CLAIM_USE)`, `toContain(CLAIM_USE2)`). Count verified: three facts exist, exactly two anchored at `src/use.ts`. Survives — the exclusion is pinned by executable expectations, independent of the \"blast radius\" terminology in comments. Not inferable from the file/test names alone, and knowing it changes how one consumes or extends the `--by dependency` door (it answers \"what depends on X,\" not \"X's facts\").\n\n5. *\"Bare query stdout is byte-identical to explicit `--by scope`.\"* — Also code-pinned (`toBe(explicit.stdout)`), but it is a narrower compat detail than candidate 4; keeping 4 as the single survivor.\n\nSTEP 2 — EMIT\n\n```atlas-fact\n{\"claim\": \"In dependency mode the queried node's own fact is deliberately excluded: the setup grounds CLAIM_DEP on src/dep.ts and emits it successfully, yet the test's `query src/dep.ts --by dependency` run asserts the pack holds both facts anchored at the dependent src/use.ts while explicitly expecting NOT to contain CLAIM_DEP.\"}\n```"