"STEP 1 — scratch reasoning:\n\nCandidates drafted and refuted:\n\n1. *\"A red test here means the limit was closed; re-pin, don't revert.\"* — This is purely the header comment's guidance/process claim. Not code. Discarded.\n\n2. *\"The boundary loop covers all split offsets from immediately-after-prefix through end-of-secret (n = 0..24).\"* — Countable, but the number serves nothing actionable and risks miscount; the qualitative content is already announced by the test titles. Weak.\n\n3. *\"`store.put` is deterministic content addressing: equal bytes yield equal hashes across calls.\"* — Asserted in code (`toBe(store.put(...))` twice), but largely inferable from the type name `Hash` plus `toGitPointer`; borderline signature-adjacent.\n\n4. *\"`admitToBuffer` is called as a fold: `(prevBuffer, chunk) → nextBuffer`.\"* — Visible in the loop usage, but the import line plus the fold idiom make this close to plain reading of the code's shape.\n\n5. **\"The suite pins the redaction placeholder as the exact literal `[REDACTED]`.\"** — Appears twice as string literals inside executable `expect(...).toBe(...)` calls (`line: exported [REDACTED] done\\n`), not in any comment. Nothing in the file name, imports, or types reveals the marker format. It is checkable solely by reading these bytes, involves no count or exhaustive enumeration, and would genuinely change what an engineer does (downstream parsers, log-scanner allowlists, or anyone renaming the marker must satisfy this exact string or break these pins). Survives refutation.\n\nSTEP 2 — emit:\n\n```atlas-fact\n{\"claim\": \"This suite pins the redaction placeholder as the exact bare literal '[REDACTED]' — a single-put of the fixture body is asserted to come back as precisely 'line: exported [REDACTED] done\\\\n' — so any change to the redaction marker format breaks these executable expectations, not just a description.\"}\n```"