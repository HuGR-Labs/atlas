"STEP 1 — scratch reasoning:\n\nScanning for facts not carried by the file's name and not just paraphrasing a `describe`/comment claim about unseen code:\n\n- Most candidate behaviors (greedy-strip witness fixture, symlink refusals, anchor splitting, signals never interpolated) are each announced nearly verbatim by their own `it()` titles or the block comments above them — restating those adds nothing beyond the visible names.\n- The digest tests: \"same template ⇒ same digest\" looks like it only pins determinism. But checking the *construction*: `templateAt` calls `mkdtempSync` on every invocation, so the two factories in the equality test load byte-identical bodies from two **distinct** temp directories. Equal digests therefore force the conclusion that the digest cannot incorporate the template's path — only its bytes. That inference comes from the fixture code itself (`mkdtempSync` per call), not from any comment, and no title or comment in the file states it.\n- Refutation pass: is it dependent on unseen code? No — it rests on `mkdtempSync`'s uniqueness guarantee and the `expect(a.digest).toBe(b.digest)` assertion, both in these bytes. Any exact count? No. Restatement of name/signature? The unit is a test file with no signature; the path-exclusion property appears nowhere in the file's prose. Actionable? Yes: relocating or copying the shipped template preserves digest identity, while any byte change (per the sibling test) breaks it — relevant to anyone reasoning about provenance records.\n\nSurvives. STEP 2 — emit:\n\n```atlas-fact\n{\"claim\": \"Because templateAt creates a fresh mkdtempSync directory on every call, the digest-equality test compares templates stored at two different filesystem paths, so the equal digests prove the factory's digest depends only on the template's bytes, not on the file's location.\"}\n```"