"STEP 1 — REASONING (scratch):\n\nCandidates drafted and refuted:\n\n1. *\"The `at` rev is fixed at producer creation\"* — refuted as insufficient: the signature `createTestVacuityProducer(units, emit, at)` already shows `at` is bound at build time; a competent engineer gets this from the signature.\n2. *\"`doc.dispose()` runs in `finally`, so WASM handles release even if emit throws\"* — true and byte-visible, but largely a restatement of the plain control flow plus the file's own comments about \"ast.ts discipline\".\n3. *\".js test files are excluded by the TEST_UNIT regex\"* — derivable from the literal, but a competent engineer would plausibly infer TS-only scanning from `parseTsDoc`; weaker than the next candidate.\n4. **Verifier tautology** — inspecting the body:\n   ```ts\n   const verify = (_unitKey, testName, shape) =>\n     facts.some((f) => f.testName === testName && f.shape === shape) ? 'proven' : 'abstain';\n   ```\n   The `_unitKey` parameter is underscore-prefixed and unused. Every proposal in the loop is built with `testName: f.testName, shape: f.shape` where `f` is drawn from the very same `facts` array the closure searches — so `facts.some(...)` is trivially satisfied for each proposal. The closure therefore returns `'proven'` for every proposal this loop constructs; the `'abstain'` arm cannot fire from this call site, and the `node === undefined` branch (reason: \"the injected sound oracle did not re-prove…\") can only trigger via causes internal to `trySoundTestVacuity` (invisible here), never via a scan mismatch. This survives refutation: it rests entirely on declarations and control flow shown in these bytes, is invisible from the name/signature, and would change what an engineer does (debugging that failure reason, testing the abstain path, or auditing the \"re-proof\" as independent).\n5. Comment-based claims about genesis, the governed door, driftDetect, Wave numbering, #95/#87 — all discarded: they assert properties of files/issues not shown here.\n\nCandidate 4 survives.\n\nSTEP 2 — RESULT:\n\n```atlas-fact\n{\"claim\": \"Inside createTestVacuityProducer the injected verify closure ignores its unitKey argument and approves any (testName, shape) pair found in the same facts list the proposing loop iterates, so verification is tautological for every proposal it builds and the 'did not re-prove' abstain outcome cannot arise from a scan mismatch at this call site.\"}\n```"