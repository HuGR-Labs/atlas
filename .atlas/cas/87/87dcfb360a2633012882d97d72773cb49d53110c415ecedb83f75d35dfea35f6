"STEP 1 — REASON FREELY\n\nReading the bytes closely.\n\nCandidate A: \"Suppressed tool calls advance the settle counter rather than freezing or resetting it, so a poke can fire on a non-navigational call.\"\nRefutation pass: In `feed`, the suppress branch does `runLen += 1` when `currentScope !== null`, and `poke` is checked after every call. Trace: nav(A) → runLen=1, no fire; then a Bash/suppress call → runLen=2 → `poke(A)` fires. Fully derivable from control flow here, no reliance on unseen code. Survives — but it's arguably close to what the doc line \"across N = 2 consecutive tool calls\" already advertises, and a careful reader of the interface might suspect it.\n\nCandidate B: \"The once-per-scope latch is written before the fallible seams run, so a thrown seam burns the poke for the session.\"\nRefutation pass: In `poke`, the order is `poked.add(scope)` → `sources.pack(scope)` → `sources.notice(...)`, all inside one try whose catch returns `null`. Searching the whole unit: `poked` is created once, only ever `.has`/`.add`ed — no `.delete`, no clear, no reset path anywhere. So if `pack` or `notice` throws, the caller gets `null` yet the scope is permanently marked poked; every later `poke(scope)` short-circuits at the `poked.has` guard. This ordering consequence appears nowhere in the interface docs (they only promise totality and \"≤1 poke / scope / session\"), and it rests entirely on statement order, the try/catch, and the Set lifecycle — all visible in THESE bytes. No dependency on comments about other files, task IDs, or absent code. Not an exact-count or enumeration claim. Not a restatement of the name/signature — from `poke(scope): Poke | null` plus \"total, never throw\" docs, nobody would predict that a *failed* attempt consumes the once-per-scope budget.\n\nCandidate B survives cleanly and is more action-changing than Candidate A (it affects how you'd implement/test the seams and whether you'd expect retries to help). Choosing B.\n\nSTEP 2 — EMIT THE RESULT\n\n```atlas-fact\n{\"claim\": \"Inside poke(), the scope is added to the once-per-session poked set before the supplied pack and notice seams are invoked, so if either seam throws, the swallowed null return still permanently exhausts that scope's single poke for the session.\"}\n```"