"STEP 1 — scratch reasoning (not persisted):\n\n- Candidates drafted from close reading:\n  - (A) The whole golden runs against `coreScip = { documents: [] }` — visible literal, but reads more as fixture description than behavior-changing insight.\n  - (B) Anchor path strings omit the fixture's `'repo'` root segment — visible in literals, but plausibly guessable from domain convention.\n  - (C) Staleness = string comparison of baseline vs `.current` — supported by the four visible `drift` calls and their expectations.\n  - (E) **Reference-identity pinning**: `expect(I.discover('core/cas/cas.ts')).toBe(I.axes.spatial.children[0]?.children[0]?.children[0])` and the three `I.forest.<axis>` → `I.axes.<axis>` `toBe` lines assert *shared live objects*, not copies. Nothing in the name/signature `composeIndex(FileTree, ScipOutput)` hints at aliasing semantics; this changes consumer behavior (mutation propagates).\n- Refutation pass on (E): derivable solely from these bytes (the `toBe` calls are literals here); not resting on any comment's claim about other files (INDEX-10, atlas-index:54 references discarded); enumeration checked — the child chain is exactly `.children[0]?.children[0]?.children[0]` and the forest lines are exactly spatial/territory/dependency, verified line by line; goes beyond a restatement of name/types; only relies on standard `toBe` = identity semantics needed to read any vitest test. Survives.\n- (A), (B), (C) survive as true but are weaker on the \"would change what they do / not guessable\" bar than (E).\n\nSTEP 2 — emitting the surviving fact:\n\n```atlas-fact\n{\"claim\": \"This test pins the composed index's surfaces to shared live objects by reference identity: I.discover('core/cas/cas.ts') must be toBe-identical to the axis node reached via I.axes.spatial.children[0]?.children[0]?.children[0], and I.forest.spatial, I.forest.territory, and I.forest.dependency are each toBe-identical to their I.axes counterparts — so discovery hands back the backing axis node itself, not a copy.\"}\n```"