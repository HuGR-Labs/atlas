"STEP 1 — REASON FREELY\n\nReading the bytes closely:\n\n- The file name (`s24-uncanonicalizable-row.blackbox.test.ts`) says only \"black-box test of an uncanonicalizable row\". Nothing about *how* the refusal reason is asserted.\n- Candidate A: \"refusals exit 2 with `status: rejected`.\" Checkable in code (`expect(run.exitCode).toBe(2)`), but largely what a reader would predict for a refusal test, and the surrounding narrative comments discuss it heavily — weaker novelty.\n- Candidate B: \"stderr must be exactly empty.\" Visible (`expect(run.stderr).toBe('')` twice), derivable, but it's a bare literal assertion seen immediately on reading; borderline restatement.\n- Candidate C: the reason-matching discipline. The helper `reasonOf` does `(rejected ?? '').split(':')[0]!` — it discards everything after the **first colon** — and *all three* refusal assertions (float CLI, NFC CLI, and the MCP `isError` result's `rejected` field) route through it before comparing with `toBe(CANONICAL_VIOLATION)` where `CANONICAL_VIOLATION = 'canonical-form violation'` contains no colon. This is fully re-derivable from the declarations and call sites in these bytes; it does not depend on any comment's claim about `guard.ts`, `#136`, ADR-0003, or other files. Consequence: appending colon-separated detail to the shipped refusal prose cannot break this suite, but changing anything before the first colon fails all three assertions across both transports. That is not knowable from the unit's name/signature and directly changes what an editor of either the reason strings or these tests can safely do.\n- Refutation pass: is C an exhaustive/numeric claim? No — I enumerated the call sites myself (two CLI `it`s + one MCP `it`) and the claim is qualitative about the mechanism. Is it a restatement of a signature? No — the signature `(rejected: string | undefined): string` hides the truncation entirely. Does it rest on out-of-file claims? No. It survives.\n\nSTEP 2 — EMIT THE RESULT\n\n```atlas-fact\n{\"claim\": \"All three refusal assertions in this suite (both CLI cases and the MCP isError result) compare for exact equality only the text before the FIRST colon of the reason — via reasonOf's split(':')[0] applied to the CLI 'reason:' line and the MCP 'rejected' field — so appending colon-separated detail to the 'canonical-form violation' reason keeps these tests green, while changing anything before the colon fails all of them.\"}\n```"