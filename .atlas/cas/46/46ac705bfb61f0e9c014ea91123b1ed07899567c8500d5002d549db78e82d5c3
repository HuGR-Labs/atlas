"STEP 1 — scratch reasoning:\n\nCandidates considered:\n\n1. *“No production callers / shipped door lives in adapter-io”* — discarded. These are comment claims about files not present in these bytes; unverifiable here.\n2. *“Both gate methods share the same predicate, differing only in the rejection string”* — derivable, but largely visible at a glance from the short `createGuard` body and heavily telegraphed by the doc comments; weak “changes what they do” value.\n3. *“Exceptions from `id()` are swallowed and mapped to a refusal”* — survives byte-check (`catch { return false; }`), but the adjacent comment already advertises totality, and the actionability (skip try/catch) is mild.\n4. *Duplicate-key write behavior* — checked against the code in `createGovernedStore.write`: `const verdict = guard.admitOnWrite(row); if (!verdict.admitted) return verdict; if (!medium.has(row.key)) medium.set(row.key, row); return verdict;` When the key already exists and the row is admitted, `set` is never called, yet the previously computed `admitted: true` verdict is still returned. Nothing in the name (`write`) or signature (`(row: StoreRow) => GuardVerdict`) hints that success is ambiguous between a fresh append and a no-op re-emit; no reliance on comments, no counts, no references outside these bytes. This changes what an engineer does: they cannot use the verdict to audit whether bytes actually landed, and must not expect an error or overwrite on duplicates.\n\nRefutation pass on candidate 4: fully re-derivable from control flow shown; comment merely corroborates; not a type/signature restatement. Survives.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"In createGovernedStore, a write whose key already exists in the medium skips the set entirely yet still returns the guard's admitted verdict, so admitted:true cannot distinguish a fresh append from a duplicate no-op.\"}\n```"