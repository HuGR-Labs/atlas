"STEP 1 — Scratch reasoning:\n\nReading the bytes closely, several candidate facts surface, and I refute them one by one:\n\n- **\"The settle window is N=2\"** — stated in the file header comment and the describe title; the executable pin (`expect(SETTLE_WINDOW).toBe(2)`) exists, but the number itself is already advertised by the unit's own labels. Weak novelty.\n- **\"Bash calls always suppress\"** — derivable (RETR-4d: Bash with `['billing']` and `[]` both suppress, while RETR-4b shows Read/Edit/Write navigate on a single path). Survivable, but it's essentially the title of RETR-4d restated.\n- **\"Pack names are `own_<leaf>`\"** — literally spelled out in the X1 describe title; restatement.\n- **The asymmetry between tool projection and poking** — this is NOT stated anywhere in a title or comment. RETR-5b shows that after exactly ONE `feed(Read(B))`, the live set is already `['n4','n5']` and explicitly excludes `n1` — projection swaps instantly. RETR-4e/4g show that the poke is null on the first call in a new scope and fires only when the same scope recurs (with `SETTLE_WINDOW` pinned to 2 by assertion). So the facet maintains two independently-timed mechanisms: the tool surface tracks scope per-call with no debounce, while the announce poke requires two consecutive same-scope calls. Cross-checking: RETR-4h confirms one intervening call destroys the streak; RETR-4a confirms a fresh facet fires on the second consecutive call, so the first call counts. Every element is checkable in these assertion literals. This synthesis changes what an implementer does: projection state and the settle counter must be updated on every feed, but gated separately.\n\nThis survives refutation: it rests on the code's own expectation literals, involves no counts I haven't verified item-by-item, and is not a restatement of any name, signature, or title.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"The live NodeTool surface tracks the current scope immediately on every call — a single Read(B) already retracts A's tools and exposes B's covering set — while the unasked announce poke is gated independently behind a two-consecutive-call settle requirement (SETTLE_WINDOW pinned to 2), so the two mechanisms update on the same feed but fire on different schedules.\"}\n```"