"```atlas-fact\n{\"claim\": \"Although the header's coverage line, the completion-report WP-partition table, and the self-check all account for exactly 16 work packages, the file body actually defines 18 full WP cards — WP-PROMOTE.CLI (carrying REQ-CLI-7a–7f) and WP-SEEDGATE.COMPOSE (carrying REQ-CLI-4d) are complete cards with their own requirements, goldens, deps, and exit predicates, yet appear nowhere in the campaign's own accounting sections.\"}\n```"