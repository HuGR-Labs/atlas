"STEP 1 — scratch reasoning:\n\nReading the flow in `buildUnitExports`: eligibility requires `[...callerSet].some((h) => h !== self)` — at least one non-self caller. But the subsequent counting loop iterates over **all** hashes in `callerSet` with no `h !== self` filter, pushing each resolvable path into `callerPaths`, and `atLeast = callerPaths.length`. Since `pathByHash` covers every document (including the defining one) and `callers` buckets *any* `reference` occurrence resolving to an in-index symbol — including a document's references to its own non-local/global symbols (only `local` symbols are skipped) — a unit that references its own export inflates its own `atLeast`.\n\nRefutation attempts:\n- Derivable from these bytes? Yes: the asymmetry between the `.some(h => h !== self)` gate and the unfiltered counting loop is plain control flow, independent of any comment.\n- Rests on a comment about elsewhere? No. I deliberately ignore the header's \"cross-unit caller\" framing and the interface doc's \"witnessed distinct caller-unit count\" phrasing; the code alone shows the count is over the whole caller set.\n- Exact count/enumeration? No — qualitative claim, no numbers asserted.\n- Restatement of name/signature? No — from `ExportCount.atLeast` plus the fan-in framing one would assume the bound counts *other* units; the bytes show otherwise.\n\nSurvives. Candidate alternatives (WeakMap memoization by `scip` identity; empty-common-prefix symbols silently dropped) are weaker — both are near-restatements of visible/annotated behavior.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"The derived atLeast counts every distinct referencing document in the caller set, including the defining unit's own references to its own export, because only the cross-unit eligibility check filters out self — the counting loop over callerSet never does.\"}\n```"