"Reading the bytes closely. The unit is a README whose own text carries the operative facts; checking each candidate against what is actually present here:\n\n- *Ten commands exist* — I counted the table rows item-by-item: init, query, emit, reconcile, doctor, mine, node, link, promote, own = 10. Matches the text. But this is close to a summary of the table itself; less surprising.\n- *Internal tension*: the table says ten, the layout line for the cli package lists eight, and the transport-parity section says \"nine commands.\" All three numbers are in these bytes — but flagging a numeric inconsistency is weaker than stating the operative consequence.\n- *Exit codes 0/1/2 with 2 = governed refusal* — present verbatim, actionable, but partially conventional.\n- **Transport-parity gap**: the bytes state plainly that `doctor`, `mine`, `node`, and `promote` are CLI-only and unreachable over MCP because MCP advertises `GOVERNANCE_SURFACE ∪ READ_SURFACE` and `READ_SURFACE` is not exported yet — five tools today. The four excluded names are enumerated right there, and the bytes explain why `promote` (a WRITER) lacks a token: it publishes through the existing `atlas-emit` door. Refutation pass: derivable from these bytes (yes — the whole claim lives in the transport-parity section); enumeration verified (four names, listed); not a restatement of the title/intro (no — the intro implies a uniform substrate, nothing about asymmetric transports); rests on no unseen module's behavior beyond the doc's own current-state assertions, which are this unit's content. Survives.\n\nThis changes what an integrator does: wiring an MCP client, they must not look for a promote/doctor/mine/node tool, and any promote flow must shell out to the CLI.\n\n```atlas-fact\n{\"claim\": \"Despite the CLI documenting ten commands, the MCP server advertises only five tools today: doctor, mine, node, and promote are CLI-only and unreachable over MCP, and promote — the only writer among the four — publishes through the existing atlas-emit door rather than having a tool token of its own, with the gap closed only by the unbuilt campaign 10.\"}\n```"