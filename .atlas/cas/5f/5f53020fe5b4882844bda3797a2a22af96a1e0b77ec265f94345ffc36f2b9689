"STEP 1 (scratch): Candidates considered — (a) order-sensitivity of the `toStrictEqual(expectedScipOutput)` golden: trivially inferred from the call, discarded; (b) the asymmetric `e.to !== null` guard hinting at a nullable edge endpoint: speculative defensive-coding inference, discarded; (c) the dual top-level `afterEach` hooks running file-wide: low impact and leans on framework semantics, discarded; (d) the substring-based prohibition in SCN-ADAPTER-2c-1: byte-true but low consequence, discarded; (e) the `planIndexers` dispatch contract: survives — every clause is a literal assertion in these bytes (`toHaveLength(3)`, per-lang `toHaveLength(1)` for ts/py/rb, the three exact tool bindings `scip-typescript`/`scip-python`/`honest-hole`, and the negative `stack-graphs` assertion), item-by-item rechecked, no reliance on the adjacent comments' rationale about other files or stale mechanisms.\n\n```atlas-fact\n{\"claim\": \"This suite pins planIndexers to a total, duplicate-free dispatch over its caller-supplied language list — ['ts','py','rb'] must yield exactly three plans bound to 'scip-typescript', 'scip-python', and 'honest-hole' respectively — and separately asserts that no returned plan is ever bound to 'stack-graphs'.\"}\n```"