"STEP 1 — scratch reasoning (not persisted):\n\nDraft candidates from close reading:\n\n1. *\"Coverage ledger says 75/75 but the trailing §GEN-CONC scenarios sit outside it\"* — checkable, but it hinges on counting REQ ids and reading an omission; fragile and low action value.\n2. *\"Obviousness is scored, never gated (ADR-0012 amendment)\"* — supported inline (4b/4c/12e/16a), but its authority is framed as an external ADR; adjacent candidates edge it out on self-containedness.\n3. *\"Held-out beacon fixtures punish memorizing acme values; GEN-16 exempt, GEN-11 PBT excluded\"* — real, but mostly procedural bookkeeping about the GATE process.\n4. **Survivor:** the admission rule itself. Reading REQ-GEN-12b/12c/12j together: admission is the mechanical conjunction *compile ∧ HOLDS-on-current ∧ flips-to-BROKEN-on-≥1-mutant*; a check that survives every mutant of its subtree is dropped as vacuous; and SCN-GEN-12j-1 is explicitly flagged \"★ the block's teeth axis\", with its own breaking mutant defined as the harness *omitting the flip conjunct* (mutation-of-the-mutation). Every element re-derives from this unit's own bytes — no reliance on referenced files, no unverified counts. It is not guessable from the path/name and directly changes what an implementer builds: the admit-harness must run mechanical mutation testing in production and discard tautologies.\n\nRefutation pass on #4: not a name/signature restatement; not exhaustive-enumeration-dependent; not a comment about code elsewhere. Survives.\n\nSTEP 2 — emit:\n\n```atlas-fact\n{\"claim\": \"In this block, admission of a proposed invariant is decided mechanically by requiring its synthesized check to compile, hold on the current code, AND flip to BROKEN on at least one mechanical mutant of the anchored subtree, so a check that survives every mutant is dropped as vacuous — SCN-GEN-12j-1, the block's designated teeth axis, guards this with a mutation-of-the-mutation whose breaking mutant is the harness itself omitting that flip conjunct.\"}\n```"