"Looking at this unit closely: it's a goldens/requirements spec for the persistence block, and nearly everything in it either (a) points at files not shown here (`persist/ref/*.ts`, `fspec-merge.md`, method-tags docs), or (b) restates the coverage ledger's own summaries. Candidates I drafted and tried to refute:\n\n- *\"Ids are blake3 of the canonical event with seq zeroed\"* — the formula is in the bytes, but its validity rests on the referenced `fspec-merge` §DOWN definition I cannot see; weaker than a fully in-byte derivation.\n- *\"Exactly 52 of 54 REQs have scenarios; 17 PBT / 34 conformance / 1 residue\"* — I recounted and it checks out, but it's a numeric restatement of the doc's own ledger, and exact counts are fragile.\n- *\"Conformance SCNs have held-out twins, PBT ones don't\"* — verifiable by scan, but it just paraphrases the doc's own closing ledger.\n- *Delegation of 10a-c/10a-d* — the absence is in-byte, but the justification leans on external dispatch/method-tags documents.\n\nThe survivor: the head-rule instantiation. Everything needed is physically present in this unit — the event table gives e1 contentHash `1c9f2a` and e2 contentHash `7e40bb`, both `fresh: true`; the header states the forced head is max-by-contentHash among FRESH entries; the parenthetical states `1c9f2a` < `7e40bb` lexicographically; and SCN-PERSIST-11e-1 asserts e2 heads in **both** merge directions. From these bytes alone one derives that e2 wins purely by hash ordering, not position — which is exactly what breaks a last-writer-wins or ours-preferring implementation. No external-file trust required, no counts asserted, not inferable from the filename alone, and it dictates what a correct fold/merge oracle must do.\n\n```atlas-fact\n{\"claim\": \"This spec forces the merged head of the colliding e1/e2 pair to be e2 in BOTH merge directions solely because e2's contentHash 7e40bb sorts lexicographically above e1's 1c9f2a under the max-by-contentHash-among-fresh rule, so any merge that resolves the head by arrival order, branch order, or last-writer-wins yields the wrong head and flips the direction-independence golden (SCN-PERSIST-11e-1) to BROKEN.\"}\n```"