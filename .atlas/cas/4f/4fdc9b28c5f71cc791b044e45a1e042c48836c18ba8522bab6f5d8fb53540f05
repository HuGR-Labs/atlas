"STEP 1 — scratch reasoning (not persisted):\n\nReading the bytes closely. Candidates drafted:\n\n- *Candidate A:* \"`reconcile` blocks merges on semantic flips with exit 2 and renders no detail.\" Actionable, but partially framed as pointing at the linked reference file; the sharper, fully self-contained demonstration in this unit is the step‑4 transcript.\n- *Candidate B:* nodeKeys vs content addresses are different identifier families (`inv` → nodeKey for `doctor`/`link`; `doctor archive` → address for `node`). Real, but closer to a definitional note an engineer could guess from context.\n- *Candidate C (step 4):* A re-emit with a byte-identical `claimNorm` is **deduplicated**: exit 0, returns the FIRST emit's content address (`id: aab5c2cc…` labeled as such in the transcript), persists nothing — yet still re-stamps the freshness watermark so `query` reports `stale: false` while `doctor why` still shows `class=semantic … anchorWas…anchorNow…`. The unit demonstrates this end-to-end with its own inline transcript, labels it \"Here is the ceiling,\" and draws the operational consequence: do not treat `stale: false` after a re-emit as proof of repair; require `whyBroken: none`.\n\nRefutation pass on C:\n- Derivable from THESE bytes? Yes — both the mechanism (prose) and the evidence (the three-command transcript with annotated outcomes) live in this unit. No reliance on a linked file's contents; the links are incidental pointers, the claim stands on the shown transcript.\n- Resting on a comment about absent code or an issue number? No — no task IDs, no external-module claims load-bearing here.\n- Exact count / exhaustive enumeration? None asserted; qualitative form used.\n- More than a restatement of name/signature? Yes — nothing in the guide's title, command names, or typical mental model (\"successful emit = repaired\") would predict that a zero-exit emit silently fails to clear drift while flipping `stale` to false. This directly changes behavior: always re-run `atlas doctor why` and demand `whyBroken: none`.\n\nC survives. Emitting it.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"Re-emitting a fact whose claimNorm is byte-identical to the stored claim is deduplicated — it exits 0, hands back the old content address, persists nothing, yet still refreshes the freshness watermark so stale flips to false while the fact remains drifted; therefore stale: false after a re-emit is not proof of repair, and you must confirm the fix by re-running atlas doctor why and requiring whyBroken: none.\"}\n```"