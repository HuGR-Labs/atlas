"Looking at the call topology in the bytes: `createDriftSource` is invoked with only `{ repoPath, resolveAnchorAt, [resolveBySubtreeAt], facts }`, and `driftAt` receives only `sbx.mb as Hash` — the topic tip string appears nowhere in any constructor or call argument. Yet the happy-path test asserts `String(p.drifted.newSha)).toBe(sbx.topicTip)` and the rename test asserts the same. Since no other input channel exists, the second endpoint of the mb→HEAD diff must be discovered by the adapter itself from `repoPath`. Checking for refutation traps: this is derived from the actual call shapes and literal assertions in the shown code, not from the header comment (which merely corroborates); it involves no unverified counts (I avoid enumerating \"two tests\" explicitly and phrase it qualitatively); and it is not a restatement of the exported surface — the signatures alone leave the second diff endpoint ambiguous, and pinning it to self-discovered repo HEAD changes how a consumer would use or test the API (e.g., it cannot measure drift against an arbitrary branch). This survives.\n\n```atlas-fact\n{\"claim\": \"The topic tip used as the drift target is never passed to the adapter — neither `createDriftSource` nor `driftAt` receives it — yet the tests assert each emitted pair's `newSha` equals `sbx.topicTip`, so `driftAt` must resolve its comparison revision itself from the configured `repoPath` rather than taking it as a parameter.\"}\n```"