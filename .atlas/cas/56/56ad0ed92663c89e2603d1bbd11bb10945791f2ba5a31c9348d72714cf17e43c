"Reading the bytes closely:\n\n- Scanning the tests, most assertions match their describe/it labels (cap law, dedup, totality, manifest ranking) — those restatements don't qualify.\n- Candidate A: In RETR-12l, `expect(p.unit).toContain('chargebacks'.length ? 'resolve' : '')`. Checking: `'chargebacks'.length` is 10, always truthy, so the ternary always yields `'resolve'`; the `''` branch is dead (and `toContain('')` would pass vacuously anyway). So the assertion is unconditionally `toContain('resolve')` — it verifies the goal text leaks into `p.unit`, not anything about the epic id. Fully checkable from these bytes alone; no reliance on unseen modules.\n- Refute A? It's not derivable from the file name; it's a genuine control-flow/literal quirk; it changes how a maintainer reads or edits that line (they might think it checks the id). Survives.\n- Candidate B (manifest ordered by hits-desc, not insertion order): supported by fixture order (3, 9, 1) vs expected `[0]` being the 9-hit memory entry. Code-derivable, but largely announced by the surrounding comments/titles; weaker than A.\n- Candidate C (poisoned `invariants` source yields empty pack without throwing): true and code-checkable, but the describe/it titles state it almost verbatim — closer to a restatement.\n\nA survives refutation best: it is non-obvious, byte-checkable, and materially affects interpretation of that assertion.\n\n```atlas-fact\n{\"claim\": \"In RETR-12l the role assertion `expect(p.unit).toContain('chargebacks'.length ? 'resolve' : '')` has an always-truthy condition ('chargebacks'.length is 10), so it unconditionally checks that the composed epic unit string contains 'resolve', and its empty-string branch is dead code that never runs.\"}\n```"