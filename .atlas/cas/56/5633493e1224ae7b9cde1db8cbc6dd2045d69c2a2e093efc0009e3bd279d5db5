"STEP 1 — scratch reasoning (not persisted):\n\nScanning the bytes: seven `it` blocks, all calling `freshness(PINNED, …)`-style comparisons and asserting `toBe<Freshness>(...)` outcomes. Enumerating every asserted fold outcome: 'FRESH' (11a-i, 11b, 11d, empty-closure happy, 11e) and 'DRIFTED' (11a-ii, 11a-iii, 11c, empty-closure drift). The literal `'STALE'` occurs exactly once in the whole file — `describeFreshness('STALE')` inside the 11f rendering guard — and never as an expected result of any `freshness()` call. This is derived from the call sites themselves, not from the header comment (which also mentions STALE, but the code independently establishes it).\n\nRefutation pass:\n- Comment-dependence? No — the enumeration is over the `it` bodies and assertions in the shown bytes.\n- Exact-count risk? The negative (\"no freshness() call yields STALE\") required checking all seven tests item by item; done. The positive (\"exactly one occurrence\") re-scanned: only `describeFreshness('STALE')`.\n- Restatement of name/signature/types? No — nothing in the file name or imports signals that the third union value is exercised only on the rendering side and is unconstrained by the fold goldens.\n- Would it change what an engineer does? Yes: an implementer authoring `freshness()` against these goldens cannot infer *when* STALE should be produced from this suite — the goldens pin only FRESH/DRIFTED — and a maintainer adding scenarios knows STALE coverage lives (if anywhere) outside this file.\n\nSurvives. Alternatives considered and discarded: argument order of `freshness()` (comparison is equality-symmetric, so order is immaterial to these assertions); the unbranded plain-string `interfaceRState` constants (visible at a glance in the constants block, low behavioral surprise); the substring-not-word `.not.toContain('true')` nuance (essentially a restatement of the assertion line itself).\n\nSTEP 2 — emit:\n\n```atlas-fact\n{\"claim\": \"Throughout this suite, the Freshness literal 'STALE' appears only once — as an argument to describeFreshness in the rendering-guard test — and no freshness() call is ever asserted to produce it, the fold's asserted outcomes being confined to 'FRESH' and 'DRIFTED'.\"}\n```"