"STEP 1 — scratch reasoning (not persisted):\n\nReading the bytes closely. Candidate facts drafted and refuted:\n\n1. *The `content` helper's conditional `nodeKey` spread exists for `exactOptionalPropertyTypes` compatibility.* — Refuted: the mechanism is in the code, but the *why* rests entirely on the comment's claim about fast-check behavior and tsconfig strictness outside these bytes.\n2. *`reseq` relabeling yields exactly seqs 97–99 in SCN-9d-1.* — Refuted: restates an assertion already spelled out in the test body; arithmetic, but plainly visible.\n3. *`AtlasState` is a Map of nodeKey → object with an `entries` map keyed by content hash.* — Borderline; derivable from `foldFingerprint`'s accessors, but essentially a summary of what the code visibly does.\n4. **Append call shape**: In SCN-KERNEL-9b-1, the inline comment describes `append(append(L,e1), e1)`, but the code calls `log.append(e1)` twice with `log` — the original receiver — as the target, never chaining on `L`. The same flat, same-receiver pattern appears in the ∀-idempotence property (`const L1 = log.append(e); const L2 = log.append(e);`), whose `it()` title declares the nested law `append(append(L,e),e)`. Verified against the bytes: both sites use the original instance; no chained call exists in the file. Consequence: if `append` returns a fresh log (pure API, plausible given `EventLog` is a `Map` and functional style elsewhere), the nested law degenerates to two independent appends from the empty base and the assertions pass vacuously. This survives all filters: byte-local (call syntax + comment text are both in the unit), non-restatable from the file name, and actionable (a reviewer/porter must chain `L.append(e)` / `L1.append(e)` to make the law implementation-agnostic).\n\nSurviving fact: the non-chained append invocation pattern contradicting the nested-law description.\n\nSTEP 2 — emit result:\n\n```atlas-fact\n{\"claim\": \"Neither the SCN-KERNEL-9b-1 golden nor the idempotence ∀-property chains append on the returned log — both call .append twice on the same original log instance (despite the inline 'append(append(L,e1), e1)' comment and the property's nested-law title) — so the doubly-nested law is genuinely exercised only if append mutates its receiver.\"}\n```"