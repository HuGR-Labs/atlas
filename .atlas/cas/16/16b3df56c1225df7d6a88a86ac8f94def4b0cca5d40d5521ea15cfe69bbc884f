"```atlas-fact\n{\"claim\": \"A successful governed emit returns two different identifiers that are asserted never equal — `out.id`, a 64-hex content address that resolves the stored node bytes through `store.get`, and `out.nodeKey`, the key under which the durable projection row is filed — with the row's `contentHash` equal to `out.id` serving as the bridge between the two lookup schemes.\"}\n```"