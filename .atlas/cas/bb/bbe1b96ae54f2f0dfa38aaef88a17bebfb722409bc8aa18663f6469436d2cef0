"```atlas-fact\n{\"claim\": \"This held-out suite defines its own linear calibration inline — `(observedHits) => observedHits * 7 + 3` — and asserts `door2Threshold` after exactly one logged hit equals `calibrate(1)` (numerically 10, compared symbolically, not hard-coded), so the threshold must be produced by applying the injected Calibrate to the node's observed hit count rather than taken from any caller-supplied score (the 250 self-score is declared locally and never enters any API call).\"}\n```"