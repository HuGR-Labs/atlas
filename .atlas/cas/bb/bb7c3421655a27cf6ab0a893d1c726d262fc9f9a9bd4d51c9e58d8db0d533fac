"STEP 1 — scratch reasoning (not stored):\n\nReading the bytes: this is a planning manifest, so nearly every statement points at artifacts *outside* these bytes (`packages/e2e/s01–s10`, `wp-9.x-compose-b.integration.test.ts:47-75`, invariant registers RETR-1/KERNEL-1/KNOW-*, the `atlas` binary internals). Candidates relying on those get discarded immediately per the rules.\n\nSurviving candidates must come from cross-reading *this document's own* sections:\n\n- Candidate A: exit-code convention `ok0/error1/rejected2`. Present in the harness contract and echoed in S2 (\"rejected, exit2\"). Verifiable in-unit, but fairly prominent near the top; weaker novelty.\n- Candidate B: only S6 carries the `[optional / if cheap]` tag. Verified by scanning all six story headings (S1–S5 have no such marker; S6 does; slicing repeats \"S6 optional\"). Survives, but modest impact.\n- Candidate C (strongest): cross-reference tension. The matrix header declares \"every invariant MUST be asserted ≥1×\". Enumerating the asserted-in column row by row: S1,S4 / S1,S3 / S2 / S2 / S3 / S2,S5 / S5 / **S6** / note*(via S1,S4). Exactly one row — KNOW-6/8, \"mining proposes, never ratifies\" — lists **solely S6**, and S6 is the one story the same document flags optional. Dropping S6 would strand that invariant. All three premises (matrix rule, table cell, optional tag) are literal bytes in this unit; no external file, no stale-comment dependency, and the nine-row enumeration was checked item by item.\n\nRefutation pass on C: not derivable from the title alone (✓ novel); not resting on unseen code (✓); enumeration verified (✓); not a restatement of name/signature (✓). Survives.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"This manifest demands every SOTA invariant be asserted at least once, yet the only story carrying the KNOW-6/8 invariant ('mining proposes, never ratifies') is S6 — the single story the same document flags '[optional / if cheap]' — so skipping S6 would leave that invariant with no asserting story.\"}\n```"