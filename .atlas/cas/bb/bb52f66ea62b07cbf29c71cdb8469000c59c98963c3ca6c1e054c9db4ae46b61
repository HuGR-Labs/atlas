"STEP 1 — scratch reasoning:\n\nDraft candidates and refutations:\n\n1. *\"This module has no production callers / was verified with 249 subprocess runs / the poke-file counterpart is unconstructed.\"* — These come from the header COMMENT and assert things about other files, probe runs, and task #36 — none of that is in these bytes. Discarded.\n\n2. *\"Receipts always have `pulled: false` and `grantsRequired: 0`.\"* — True, but already forced by the literal types on `PushReceipt`; derivable from the signature alone. Not novel.\n\n3. *\"Exactly two runtime exports.\"* — Verifiable, but an enumeration whose usefulness doesn't clearly change what an engineer does, and numeric claims are fragile. Discarded.\n\n4. Survivor candidate: how the optional `pull` port is actually reached. Reading the bodies: `onTransition`'s entire expression references only `seat.id`, `transition`, `AUTOINJECT_TIER`, literals, and `materialize(source, grounding)` — it never reads `_harness` or `opts.pull`, so boundary delivery always re-materializes from the injected source even when a pull port is injected and the harness reports native pull available. Meanwhile `pullOptimize` gates the port behind a conjunction: `harness.nativePull === 'available' && opts.pull !== undefined`; failing either branch silently falls back to `materialize`, so an injected port cannot force itself on an `unavailable` harness. Both halves are checkable in the shown control flow, involve no counts, don't lean on any comment about unseen code, and go beyond the signatures (the `PhaseHook` method signature hides the closure over `opts` entirely; the `pulled: false` literal type doesn't preclude calling the port and discarding the result). This changes what an integrator/tester does: wiring a pull port and expecting boundary pushes or pull telemetry to fire through `onTransition` will never observe it.\n\nSurvives. Emitting.\n\n```atlas-fact\n{\"claim\": \"Although createPhaseHook accepts an optional pull port, onTransition never touches it (or the harness argument) — boundary delivery always re-materializes from the injected fresh-pack source — and even pullOptimize invokes the port only when the harness simultaneously reports nativePull === 'available', silently falling back to push materialization otherwise.\"}\n```"