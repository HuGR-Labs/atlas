"**STEP 1 — scratch reasoning**\n\nReading the bytes closely: the file-header bullet list states \"abstain ⇒ the oracle drops it (no fabricated dependency survives a non-proving oracle).\" But the second test's body directly contradicts that header: it configures the oracle to return `'abstain'` while leaving `grounded: () => true`, then asserts `expect(facts).toHaveLength(1)` with `kind === 'advisory'`, `seal === undefined`, and `predicateSlot === undefined`. Only the third test (abstain **plus** `grounded: () => false`) asserts zero facts.\n\nSo the header comment is stale relative to the executable assertions in the same file: abstention alone does *not* drop the seed; it admits an unsealed advisory carrying no predicateSlot, and grounding is what gates the drop.\n\nRefutation checks:\n- Derivable from these bytes? Yes — both the header text and the contradicting assertions are in this unit; no external library knowledge needed.\n- Resting on a comment's claim about unseen code? No — quite the opposite: the fact is *about* the mismatch between an in-file comment and the in-file assertions.\n- Exact counts/enumerations? Avoided; the qualitative contradiction is the point (the single-fact assertion in test 2 was read directly).\n- More than a name/signature restatement? Yes — nothing in `wp-196a-proposer-dependency.test.ts` or the describe-title reveals that abstention admits an advisory or that the header bullet is contradicted.\n\nSurvives.\n\n**STEP 2 — result**\n\n```atlas-fact\n{\"claim\": \"The file-header bullet claiming that an abstaining dependency oracle drops the seed is contradicted by this file's own tests: with the grounded door open, abstention admits the dependency seed as an unsealed 'advisory' fact bearing neither a seal nor a predicateSlot, and only abstention combined with a closed grounded door produces zero facts.\"}\n```"