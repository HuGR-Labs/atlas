"STEP 1 — Reasoning (scratch):\n\nReading the bytes: the fixture builds a real temp-dir disk store and pushes one relation fact through `createGovernedEmit` (throwing if the door rejects it), then hands `createRelationLeg(store)` to `main` as an injected runtime. Several candidate facts considered:\n\n- *\"Fixture uses a governed-door-emitted store\"* — confirmed by code (`mkdtempSync`, `createDiskStore`, `door.emit`, `if (!out.emitted) throw`), but largely a plain description of what the fixture visibly constructs; weak surprise value.\n- *\"Direction defaults to `both`\"* — supported by `expect(defaulted).toBe(explicit)`, but the `it()` title string itself announces \"BOTH is the default\", so a title-skimming engineer already \"knows\" it; less novel.\n- *\"Invalid direction / uncomposed runtime exit 1\"* — asserted, but conventional fail-closed CLI behavior; less likely to change anyone's actions.\n- *\"Empty result exits 0\"* — the IN test first proves the edge exists (B-`in` finds it, A-`out` finds it), then queries A-`in`, which is provably empty, and asserts `emptyCode === 0`, output contains `0 edge(s)`, and output does **not** contain `' -> '`. This is a real semantic contract — emptiness is a measured success, not a miss/error — not stated by the unit's name or any signature, and it changes what a caller/script author does (they cannot use exit codes to detect \"no relations\"; only exit 1 means failure). Fully checkable from the assertions in these bytes; the adjacent comment is corroborated by, but not relied upon over, the assertions themselves.\n\nSurvives refutation. Emitting that one. (Scratch discarded — not persisted.)\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"A direction-filtered query that matches zero edges is treated as success: main exits 0 and prints a '0 edge(s)' summary with no '->' arrow in the output, so exit codes cannot be used to distinguish 'no relations' from a failed lookup.\"}\n```"