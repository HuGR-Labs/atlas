"STEP 1 — REASONING (scratch):\n\nReading the bytes closely. Candidate facts drafted and attacked:\n\n1. *\"`territories` throws on unresolvable paths instead of returning `[]`\"* — Survives derivation (visible `throw`), but it's extensively narrated in the adjacent comment block, and it borders on summarizing the visible control flow. Weak novelty.\n2. *\"Resolve/depgraph are rebuilt per call; only `build`/`createSymbolReverse` run once\"* — Verifiable from call placement (inside method bodies vs. constructor body), but it's the module's headline announcement; an engineer scanning the file picks it up immediately. Less surprising than it first appears.\n3. *\"`isRepoRoot` treats `''`, `.`, `./` as root\"* — Refuted: depends entirely on unseen `pathSegments` normalization semantics imported from `@atlas/index`; the byte-visible part is only `every(s => s === '.')`. Discard.\n4. **\"`cover` hardcodes `invariants: []` and `stale: false`\"** — Attack: derivable from these bytes? Yes — the return object literal contains `invariants: []` and `stale: false` as plain literals; no branch, no computation anywhere in `cover` can produce another value. Does it rest on a comment about elsewhere? No — I will not invoke the comment's claim about `tools/query.ts`; the fact stands on the literal alone. Exact-count risk? \"Always empty / always false\" is checked directly against the sole return statement — no other return path exists in `cover`. Restatement of name/signature? No — the signature types the field names but reveals nothing about their values; nothing in `cover(scope: string)`'s shape suggests they are inert constants. Action-changing? Yes — a consumer expecting `cover` to report drift or invariants from repo state would be wrong; these fields carry no information from this adapter.\n\nCandidate 4 survives all refutation passes.\n\nSTEP 2 — RESULT:\n\n```atlas-fact\n{\"claim\": \"In cover(), the returned invariants field is always a hardcoded empty array and stale is always the literal false — neither value is ever computed from the repository state or the resolved node.\"}\n```"