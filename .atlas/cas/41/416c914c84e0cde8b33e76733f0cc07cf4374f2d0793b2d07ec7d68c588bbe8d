"```atlas-fact\n{\"claim\": \"Although the file claims a total 18-invariant-to-18-property 1:1 render, golden SCN-KNOW-15f-2 — the move+edit similarity threshold θ boundary — is deliberately NOT rendered as a passing property anywhere in this file, carried instead as a [NEEDS RECONCILIATION] note under PROP-KNOW-15 until the DEFINE layer pins the threshold, so the real breaking mutant (θ=1.0 exact-match-only, orphaning every move+edit) currently has a deferred pass boundary.\"}\n```"