"STEP 1 — scratch reasoning (not persisted):\n\nCandidates drafted and stress-tested against the bytes:\n\n- *“Five malformed values are tested”* — visible at a glance from the loop; a plain summary, no action-changing surprise. Discarded as restatement.\n- *“Exceeding the bound abstains in exact mode”* — pinned by one test, but the reason string `exact-count-mismatch` makes it near-explicit; low value. Discarded.\n- *Issue-number claims (#153, #189, #220)* — comments referencing work outside these bytes; forbidden as basis. Discarded.\n- *Check-order precedence between `scope-open` and `exact-count-mismatch`* — not determinable: the mismatch test has zero holes, the scope-open test has matching counts; no fixture crosses them. Underspecified by the bytes. Discarded.\n- **Survivor:** the two anti-false-positive (“teeth”) fixtures construct adversarial paths that *literally satisfy a naive string prefix*: `'src/paycheck/b.ts'.startsWith('src/pay')` is true, and `'src/paycheck/hole.ts'.startsWith('src/pay')` is true (with `worldScope` overridden to `'src/pay'`). Re-derived from literals + expectations, not from the comments: if containment were done via `startsWith`/`includes`, the caller fixture would witness 2 in-scope callers and return a false `proven` (test asserts abstain), and the exact fixture would classify the hole as under-world and return a false `scope-open` abstain (test asserts `proven`). So these two fixtures jointly discriminate segment-wise scoping from prefix scoping — checkable entirely within this unit’s declarations, literals, and `toEqual` expectations. Survives all refutation passes.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"The two anti-false-positive fixtures use adversarial paths that literally begin with the scope string — 'src/paycheck/b.ts' and 'src/paycheck/hole.ts' both start with 'src/pay' — and expect, respectively, abstain instead of a false 'proven' and proven instead of a false 'scope-open', so any startsWith/includes-style containment check in verifyCount would flip both verdicts.\"}\n```"