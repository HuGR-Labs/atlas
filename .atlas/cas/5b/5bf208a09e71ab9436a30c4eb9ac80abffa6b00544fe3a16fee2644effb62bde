"**STEP 1 — Reasoning (scratch)**\n\nDraft candidates from the bytes:\n\n- *\"DAMPING=0.85 had zero src readers\"* — rejected: rests on measurements of `rank.ts`/`pprScores` internals and a 780-test count, none visible in this unit.\n- *\".atlas/config.json does not exist\"* — rejected: the absence claim is about files/branches outside the unit, established by greps the unit merely reports.\n- *\"780 tests passed with damping 0.50\"* — rejected: exact unverifiable count plus unseen suites.\n- *\"execFileSync throws EPIPE carrying stdout and status 0\"* — partially rejected: the mechanism is attributed to Node runtime behavior plus a symbol (`salvageEarlyExit`) in an unseen file; too much leans on external claims.\n- **Survivor:** Decision 1's second rule is stated normatively *in this unit itself*: \"Non-zero exit ⇒ error, never abstention,\" with the stated reason that a broken configuration must not present as \"this repo has no facts.\" It is not derivable from the path/name (which only says the model is operator-supplied via a command), involves no counts or enumerations, and does not depend on any other file's contents — it is this ADR's own declared semantic. It directly changes implementation: an engineer wiring `ModelClient.complete` over a subprocess must branch on exit status before interpreting empty output, rather than collapsing all failures into abstention.\n\nRefutation pass complete; this candidate survives.\n\n**STEP 2 — Result**\n\n```atlas-fact\n{\"claim\": \"Per this ADR's Decision 1, a non-zero exit from the operator-supplied model command must be surfaced as an error rather than mapped to abstention, so that a broken model configuration cannot silently produce a clean-looking empty run indistinguishable from a repository with no groundable facts.\"}\n```"