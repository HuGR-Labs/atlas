"STEP 1 — scratch reasoning (not persisted):\n\nScanning candidates against the actual bytes:\n\n- *Fail-closed catch swallows everything* — visible (`catch { return undefined; }`), but close to a restatement of the header's \"fail-closed\" framing.\n- *Filtered store spreads `gatedStore`, overriding only `get` and `loadProjection`* — real and checkable in the object literal, but partially advertised by the inline defence-in-depth remark; borderline.\n- *Unknown provenance values fall through into the `tracked-provable` leg* (no exhaustive switch) — derivable from control flow, but somewhat speculative in impact.\n- *Nodes whose `contentHash` misses the raw CAS produce no row at all* — checkable via the `if (fact !== undefined)` guard; plausible, but partly rests on interpreting what `undefined` from the unseen oracle means.\n- *Index-aligned join*: the loop `for (let i = 0; i < pairs.length; i++) { if (rows[i]?.outcome === 're-proven') { reProvenNodeKeys.add(pairs[i]!.node.nodeKey); ... } }` combined with `const rows = pairs.map((p) => reverifyFact(...))` shows, purely in code, that membership is decided by pairing `rows[i]` with `pairs[i]` and taking keys from the **node**, never from any field the row itself carries. Nothing in any signature or exported name reveals the join strategy; a refactor to independent `.filter()`s over rows would silently break serving. The truth rests only on this unit's declarations and control flow — no cross-file claims needed (I deliberately ignore the comment's rationale about labels/hashes elsewhere).\n\nThat last candidate survives refutation. Emitting it.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"Inside buildProvable, the sets deciding what is served are built by index-aligned pairing — rows[i]'s outcome gates pairs[i].node.nodeKey and pairs[i].node.contentHash — so no field carried by the reverifyFact result itself is ever consulted to determine membership.\"}\n```"