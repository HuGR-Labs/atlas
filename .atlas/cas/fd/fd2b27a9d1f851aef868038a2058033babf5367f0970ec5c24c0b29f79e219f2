"STEP 1 — scratch reasoning (drafted candidates, then refuted):\n\n- Candidate: “three properties override fast-check to 5000 runs” — recounted the bytes: the disjoint mergeNode law and the head law take no options; the overlapping law and both PROP-KERNEL-11 laws pass `{ numRuns: 5000 }`. True and countable, but its persuasive weight comes from the adjacent historical comments (“read green while false”), and the bare config asymmetry is close to a skim-level observation. Set aside as weaker than a deeper candidate.\n- Candidate: “the 12a-1 scenario is only `it.todo`” — plainly visible the moment the file is opened; low information gain.\n- Candidate: “split('\\n').length === 3 pins no trailing newline in toJsonl” — valid entailment, but it primarily constrains another module (`src/jsonl.ts`) rather than this unit’s own logic.\n- Candidate: “event ids stay distinct on identical payloads” — rests on the comment’s claim about `mkEvent`’s preimage (KERNEL-9, another file). Discarded.\n- Survivor: inside PROP-KERNEL-10, the oracle `expectedHead` ranks contentHashes with a bare `.sort().at(-1)` — default **lexicographic** string order — yet the law is stated as MAX-by-contentHash. These coincide only because `chOf` applies `padStart(4, '0')` and the generator caps `ch` at 999, making every hash exactly four characters wide. Raise the cap past 9999 (or drop the padding) and the oracle silently starts certifying string order instead of numeric max. Every element of this coupling is in the shown bytes (`fc.integer({min:0,max:999})`, `padStart(4,'0')`, `.sort().at(-1)`, the `toBe(expectedHead(n))` assertion); no comment trust required; not stated by any name/signature; directly changes what a maintainer must re-check before widening the generator.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"In PROP-KERNEL-10's head law, the independent oracle picks the head via a default lexicographic string sort of the contentHashes, which matches the intended numeric MAX only because the builder zero-pads every generated hash to exactly four digits under the arbitrary's 0–999 bound — widening that integer domain or removing the padStart would silently make the oracle certify string order instead of MAX-by-contentHash.\"}\n```"