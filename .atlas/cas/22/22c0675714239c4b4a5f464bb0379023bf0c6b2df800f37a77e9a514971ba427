"STEP 1 — Reasoning scratch:\n\nScanning the bytes: the header comment makes many claims about external context (goldens, WP IDs, \"anti-overfit\") — discarded as comment-about-elsewhere. Candidate facts drafted:\n\n1. *reverseAnchors is never exercised* — true (every call site is `netModel()`), but low impact on what an engineer does.\n2. *subtreeHash is `${axis}:${key}`, not a content digest* — visible in the `node` helper, but arguably plain from reading the helper line.\n3. *blastRadius is populated but never asserted on* — true in-file, but `createRetrieval` internals are unseeable, so scoping gets shaky.\n4. *SCN-INDEX-10c-2 never touches the code under test* — re-read the test body line by line: `const model = netModel();`, a manual count over `model.store`, a `.get` + identity filter over `store.values()`, and two `expects` on `model.forest.territory.children[0]`. There is **zero** invocation of `createResolve` or `createRetrieval` anywhere in that block — unlike sibling tests 10a-2 and 10b-2, which both call them. Every assertion therefore re-checks the Map/node literals `netModel()` just constructed. This survives refutation: it rests only on the statements visibly present in the test body, not on any comment or unseen module. It contradicts the header comment's implication that goldens are \"compiled into assertions against the EXISTING src,\" which is precisely why a reader trusting the name (\"item:send stored ONCE\") would misattribute real storage-behavior coverage to this test. Qualitative form, no risky counts.\n\nCandidate 4 survives.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"Despite its 'item:send stored ONCE' framing, the SCN-INDEX-10c-2 test never calls createResolve or createRetrieval: every assertion inspects only the store Map and forest node literals that netModel() just constructed, so it verifies the local fixture rather than any production resolve/retrieval behavior.\"}\n```"