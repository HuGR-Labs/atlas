"```atlas-fact\n{\"claim\": \"SCN-RETR-1-1 checks embedding-freedom without any mock or stub: it reads ../src/pack.ts as raw text at runtime and fails if anything matches forbidden patterns like /embedding/i, \\\\bvector\\\\b, \\\\bfetch\\\\b, https?://, or /similarity/i — so even a prose comment or a documentation URL inside pack.ts itself would turn the test red (the same source-grep technique recurs in SCN-RETR-7c-1 for write-path patterns).\"}\n```"