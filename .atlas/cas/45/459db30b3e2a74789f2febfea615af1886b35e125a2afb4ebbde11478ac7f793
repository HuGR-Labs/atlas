"STEP 1 — scratch reasoning (drafted, attacked, discarded):\n\n- Draft A: “Slack’s `bodyExtra: '-'` means hyphen-joined prose after a token is absorbed.” Survives mechanically (`[A-Za-z0-9-]{6,}` is greedy), but the headline consequence framing leans heavily on the comment; the bare literal is close to restatement. Held as fallback.\n- Draft B: “MAX_SEAM_CARRY equals 74.” Requires trusting multi-step arithmetic and emits an exact number; discarded per count-risk rule.\n- Draft C: “filler is `'0'` for every family.” Exhaustive-enumeration claim; I did verify no digit appears in any prefix, but its payoff is low and partly comment-carried. Discarded.\n- Draft D: “`inBody` accepts ASCII only.” True from the charCode ranges, but marginal actionability. Discarded.\n- Draft E (survivor): the per-character union lookahead in `bodyAtom`. Verified purely from code: `ANY_FAMILY_PREFIX` joins every `FAMILIES` entry’s `prefixSrc` (all families, including the shape’s own); `bodyAtom` wraps each single body-character consumption as `(?:(?!UNION)<class>)`; quantifiers are greedy. Therefore a body run must stop at the first position where any family prefix begins (before floor is met, the whole candidate simply fails). Re-derived from declarations/control flow, no dependence on comments about other files, no counts asserted. Survives.\n\nSTEP 2 — emit:\n\n```atlas-fact\n{\"claim\": \"Because bodyAtom guards every body-character consumption with a negative lookahead against ANY_FAMILY_PREFIX — the union of all declared families' prefix sources, including the shape's own — a shape's greedy body run always halts at the first position where any family prefix could begin, so a match never absorbs the opening of another credential written immediately after it.\"}\n```"