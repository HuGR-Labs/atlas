"Looking at this unit closely: it's a pure type/interface file, so nearly all of its rich content (the UTF‑16→UTF‑8 offset hazard, the decode‑then‑re‑encode digest chain, the mint/refusal behavior of `mintSpan`/`readSpan`, the per-`kind` span policy, the various [PIN]/[FLAG] resolutions) is documented in comments pointing at measurements, tasks (#159, #182, #106, #99b), and files (tree-sitter, `adapter-io/src/ast.ts`, `span.ts`, the knowledge layer) that are **not** in these bytes — all of that fails the refutation bar. Signature-visible items (`candidate: unknown`, `InterfaceRState = Rollup['rState']`, `driftDetect` returning `Freshness`, all-fields-`readonly`) don't clear the novelty bar either.\n\nWhat survives is a structural inference from the declarations themselves: `Grounding` is `{ entries: readonly GroundingEntry[] }` — a plain readonly array, which admits a zero-length receipt; nothing in the file's types brands or constrains it to be non-empty. Yet the same file also declares `isGrounded(g: Grounding): boolean`, a dedicated runtime predicate. The pair shows enforcement of the \"≥1 entry\" notion is deliberately delegated out of the type system into a runtime check — an engineer who assumes the type guarantees a usable receipt, or who looks to add compile-time enforcement here, would act differently. This is checkable purely from the two declarations, needs no comment about elsewhere, asserts no counts, and goes beyond restating any single signature.\n\n```atlas-fact\n{\"claim\": \"The `Grounding` interface's `entries` is a plain `readonly GroundingEntry[]`, so a receipt with zero entries type-checks fine — the non-empty requirement is not enforceable from this type, which is why the file additionally declares `isGrounded(g): boolean` as a separate runtime predicate.\"}\n```"