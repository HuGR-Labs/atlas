"Reading the bytes closely: the frontmatter gives me the name (GATE), inputs, and next_state (SEAL) — an engineer could guess \"quality gate before sealing\" from that alone. What the signature does *not* tell you is the internal weighting policy among the checks.\n\nDraft candidates and refutation:\n\n1. *\"Differential is subsumed by PBT because ref/*.ts are pure-type\"* — REFUTED: this rests on a parenthetical claim about `ref/*.ts` files not shown in these bytes; it's a fact about that other code, not re-derivable here.\n2. *\"Cold reviewer is lucy\"* — survives technically, but it barely changes what an engineer does and borders on a label restatement.\n3. *\"Zero-survivor, diff-scoped mutation is always run; other legs are conditional\"* — checking: the Procedure header says \"Run mutation (**always**) + each richer leg **only if its artifact exists**\"; steps 2–4 each carry explicit `(only if …)` conditionals while step 1 carries `(diff-scoped — always run)`; the Failure-modes section demands \"**require 0 survivors**\"; the Self-Check repeats \"0 surviving mutants … (always required)\"; the Completion Report emits \"0 survivors\". Consistent in four independent places within the bytes. No reliance on unseen code, no itemized count to miscount (it's a threshold, verified verbatim). Also note cold-review is likewise unconditional, so I must not claim mutation is the *only* unconditional leg — I'll avoid that overstatement. The contrast \"held-out/differential/PBT run only if artifacts exist, else recorded UNAVAILABLE, never synthesized/faked\" is directly stated (\"never synthesize one\", \"do not fake it\").\n\nCandidate 3 survives: it's a concrete operational rule (no kill-percentage threshold, changed-lines-only scope, fail-on-any-survivor, conditional richer legs) that changes execution behavior and is absent from the name/signature.\n\n```atlas-fact\n{\"claim\": \"Mutation testing is always run in this gate, scoped to the changed lines only, and any single surviving mutant fails the gate outright (zero survivors required, no percentage threshold), whereas the held-out, differential, and PBT checks each run only if their artifact was actually reserved/frozen and must otherwise be recorded as UNAVAILABLE rather than synthesized.\"}\n```"