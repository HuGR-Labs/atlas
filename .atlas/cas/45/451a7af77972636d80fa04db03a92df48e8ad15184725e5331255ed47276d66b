"STEP 1 — scratch reasoning (parsed away):\n\n- Candidate A: \"Implementation has landed (`commitStaging` exists, `mine.ts` uses staging).\" Refuted: this rests entirely on the doc's assertions about `store.ts` and `mine.ts` — files not in these bytes. Even if true, it's a fact about that code, not re-derivable here.\n- Candidate B: \"`commitStaging` is atomic and replaced `persistStaging`/`loadStaging` after zero production callers.\" Refuted: claims about `DiskStore`'s surface, a probe measurement, and task #83 — none visible here.\n- Candidate C: \"Promotion needs `origin:'promoted'` because T2∧advisory∧grounded candidates auto-accept under the KNOW-18 fast path.\" Refuted: depends on KNOW-18 semantics and WP-PROMOTE code absent from the bytes.\n- Candidate D: \"`atlas query` no longer serves mined candidates / GOVERNANCE_SURFACE stays 5.\" Refuted: claims about other modules and invariant counters not shown.\n- Candidate E (survives): the document *itself*, in its own bytes, carries two parentheticals explicitly flagging prior sentences of this same ADR as false — one admitting the status header once falsely claimed the work was still future (#87 sequenced later), one correcting a mischaracterization of `b415eab` (rows vs. bytes stamping), with the second note stating outright it is the second such false claim. Verified by reading: exactly two parentheticals characterize prior text as false (\"a false status claim…\", \"CORRECTED 2026-08-2 … SECOND false status claim\"); the other parentheticals merely update names or add WP-PROMOTE without alleging falsehood. Not derivable from the filename, which presents the ADR as a clean canonical decision. Changes what an engineer does: they must treat the parentheticals as overriding the surrounding prose rather than as asides, and cannot take any uncorrected sentence in this ADR at face value.\n\nSurvivor: E.\n\n```atlas-fact\n{\"claim\": \"This ADR carries two inline correction notes recording that its own earlier text contained false claims — a stale implementation status and a mischaracterization of commit b415eab's row-vs-bytes stamping — deliberately preserved rather than quietly amended, so its surrounding prose cannot be trusted at face value without reading the parentheticals.\"}\n```"