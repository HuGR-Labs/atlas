"STEP 1 — Reasoning (scratch):\n\nReading the bytes closely:\n\n- Candidate A: \"Ties in the blame tally break to the lexicographically smallest author.\" Verifiable: `[...tally.keys()].sort()` with strict `n > bestN` means first-seen (lexically smallest) wins ties. True, but fairly minor; keep as backup.\n- Candidate B: \"Unlisted blame paths become synthesized `Territory` keys with hardcoded tier `'T2'`, grouped by `dirname`.\" Visible in code (step 2 of `reconcile`). Backup.\n- Candidate C: **The `graph` parameter is completely inert.** Scan every occurrence of `graph` in these bytes: it is the parameter of `reconcile`, passed twice as an argument to `generatedOwner`, and inside `generatedOwner` the very first statement is `void graph;`. It never touches the tally, the map, or control flow anywhere. Therefore any `DepEdge[]` — including an empty array — yields an identical `OwnerMap`.\n  - Refutation attempt 1: Am I leaning on library knowledge? No — `void expr` is plain TS visible here.\n  - Refutation attempt 2: Does truth rest on a comment about unseen code? The doc comments *corroborate* this, but my derivation stands on the code statements themselves (`void graph;` plus exhaustive in-unit scan of `graph`'s occurrences — parameter decl, one `void`, two call-site arguments; I re-checked token by token).\n  - Refutation attempt 3: Exhaustive-enumeration risk? \"Appears nowhere else in `reconcile`'s body\" is an exhaustiveness claim, but I enumerated all occurrences in the shown bytes and re-checked; it holds.\n  - Refutation attempt 4: Is it just the name/signature restated? No — the signature prominently features `graph: readonly DepEdge[]` as a co-signal, so a competent engineer reading only names/signatures would believe the structural graph shapes the output. Knowing it is inert changes what they do (no need to build a real graph in tests/callers; flags the INDEX-15 contract as unfulfilled in this implementation).\n- Candidate C survives; it is the strongest and most action-changing.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"The structural graph argument is functionally inert: generatedOwner discards it with `void graph` and it appears nowhere else in reconcile's body, so passing any DepEdge[] — even an empty one — produces an identical OwnerMap.\"}\n```"