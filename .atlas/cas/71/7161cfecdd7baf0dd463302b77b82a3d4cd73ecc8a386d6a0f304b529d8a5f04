"STEP 1 — scratch reasoning:\n\n- Candidate A: \"`localDefs` is scoped per-document so a def in doc A never vouches for doc B.\" Refuted as too restated — the per-doc `new Set` inside the loop makes it plain from structure, and it's the headline of the F1 comments.\n- Candidate B: \"`defs` is first-definition-wins.\" Refuted — directly visible in the `!defs.has(occ.symbol)` guard and essentially announced by `definesAt`'s own doc.\n- Candidate C: \"Definition collection skips canonicalization while reference resolution applies it.\" Survives a check (defs loop sets raw `occ.symbol`; ref loop tries raw then `canonicalizeSymbol`), but it mostly restates what the CANON-AND-VERIFY comment narrates, and its practical payoff overlaps with the signature-level promise of `reverseCallers`.\n- Candidate D: the indexer gate. Verify against code only: `SUPPORTED_INDEXER = 'scip-typescript'`; `trustCollapsedLocal = opts?.indexerName === SUPPORTED_INDEXER` (exact `===`, so case/spelling sensitive; absent ⇒ false, no trusting default). `opaqueRefSet.add` occurs only inside `if (trustCollapsedLocal && !localDefs.has(...))`, and `opaqueRefs` derives solely from `opaqueRefSet` — so with any other/absent name, `opaqueRefSources()` is provably always `[]` (empty input to `sortedDeduped`). Item-by-item check that the other outputs are unaffected: the `localDefs` pre-scan is skipped when false, but the local-ref branch ends in `continue` either way; `callersBySymbol` and `holeSourceSet` are only touched on non-local refs. So `reverseCallers`, `holeSources`, `resolves`, `definesAt` are byte-identical in behavior regardless of `indexerName`. Nothing here leans on claims about other files — every needed line is in these bytes. Not derivable from the name/signature: the signature shows an optional `indexerName` but neither the exact accepted literal nor that omitting it fails closed while leaving everything else unchanged. Survives.\n- Exhaustiveness worry for D (\"always empty\", \"identical results\"): re-verified — the only write path into `opaqueRefSet` is gated; no other writes exist. Holds.\n\nEmitting D.\n\n```atlas-fact\n{\"claim\": \"Passing `indexerName` as anything other than the exact string 'scip-typescript' — including omitting `opts` entirely — leaves `opaqueRefSources()` permanently empty while `reverseCallers`, `holeSources`, `resolves`, and `definesAt` return identical results either way.\"}\n```"