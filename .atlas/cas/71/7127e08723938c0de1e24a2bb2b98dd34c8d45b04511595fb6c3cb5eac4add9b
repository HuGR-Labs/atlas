"STEP 1 — Reasoning (scratch):\n\nCandidate: the heart of this file is the *harness*, not the assertions — the \"rival publisher\" is simulated entirely in-process by wrapping the **real** `createDiskStore` and slipping a file write into the first `decide` call, with the rival's content copied from the attempt's own proposed projection.\n\nRefutation pass:\n- Derivable from these bytes? Yes: `const real = createDiskStore(casPath())`, `{ ...real, commitStaging(...) {...} }` (only `commitStaging` overridden), the guard literal `commits === 1 && decision.next !== undefined`, the `writeFileSync(join(sidecarDir, 'staging.2.json'), JSON.stringify(wire))` executed synchronously inside the callback handed back to `real.commitStaging`, and `wire = { current: [...decision.next.current.entries()], cas: [...decision.next.cas], gen: 2, identity: IDENTITY_SCHEMA }`.\n- Resting on a comment about unseen code? No — I deliberately drop the comments' claims about the protocol refusing unstamped stores or creating the sidecar dir in `publish`; every element of the claim is a declaration/literal/control-flow fact in the shown code.\n- Exact counts/enumerations? Only literals directly verified in the bytes: `commits === 1`, `staging.2.json`, `gen: 2`. No counted lists to mislabel.\n- More than a name/signature restatement? Yes — from `mine-superseded-publish.test.ts` alone one would assume a concurrency test with mocked or foreign rival data; the code shows the rival is honest, derived from the pass's own `decision.next`, fired once, in-process.\n\nSurvives. Emitting.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"The rival publisher here is not a second process or a fake store: storeWithRivalOnFirstCommit wraps the real createDiskStore, overriding only commitStaging, and on the first commit alone (guarded by `commits === 1 && decision.next !== undefined`) it synchronously writes `staging.2.json` whose contents — current, cas, gen 2 — are lifted verbatim from the attempt's own proposed projection (decision.next) and stamped with IDENTITY_SCHEMA.\"}\n```"