"STEP 1 — Reasoning:\n\nDraft candidates considered against the bytes:\n\n- *“The fast path requires six conjuncts…”* — countable (`grounded && lowRisk && t2 && advisory && !contested && !promoted`) but numeric trivia that barely changes behavior. Discarded as weak.\n- *“`isAdvisory` uses strict `undefined` comparison, so a falsy-but-defined `check` forces full ratify.”* — visible in the code, but whether this differs from a truthiness rewrite depends on `Candidate.check`'s unseen type; if it's `X | undefined` with non-falsy `X`, there's no behavioral delta. Rests partly on an unseen type. Discarded.\n- *“`isGrounded` accepts a whitespace-only hash since it only checks non-empty string.”* — visible, but largely a restatement of the predicate body and its punchline overlaps the comment narrative. Discarded.\n- *“Explicit `origin:'authored'` behaves identically to omitting `origin`.”* — true (`ctx.origin === 'promoted'` is the only test) but near-trivial. Discarded.\n- **Survivor:** the ternary `governingTier = ctx.derivedTier === undefined ? candidate.tier : strictestTier(ctx.derivedTier, candidate.tier)` is plain code in this unit. Its consequence — that the stricter joined tier is applied **only** when the caller supplies `derivedTier`, and otherwise the gate (`t2 = governingTier === 'T2'`) is decided by the candidate's own declared `tier` — is not evident from the optional-field signature alone, is checkable purely from these bytes, and changes what an integrator/reviewer does (they must actually populate `derivedTier` for the stricter join to be active at all). It asserts no exhaustive enumeration and rests on no comment's claim about other files; `strictestTier`'s internal semantics are never relied upon, only the visible call-site branching.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"In `route`, the stricter `strictestTier` join is applied only when the caller actually supplies `ctx.derivedTier`; if it is undefined, the ratification gate is decided solely by the candidate's own declared `tier`.\"}\n```"