"STEP 1 — SCRATCH\n\nReading the bytes: the whole program lives in `void (async () => { ... })();`. The returned promise is explicitly discarded with `void`. Scanning the file: there is no `try`, no `catch`, no `.catch(` anywhere — `await` appears only *inside* the IIFE, never at top level. So if `initAst()`, `composeRuntime(process.cwd())`, or `main(...)` rejects, nothing in this unit intercepts it; it becomes an unhandled promise rejection. The sole exit-status channel is the `process.exitCode = await main(...)` assignment (no `process.exit` call).\n\nRefutation pass:\n- Derivable from these bytes? The discarded promise, the absence of any catch clause, and the `exitCode` assignment are all literally present in the code structure. The \"unhandled rejection\" consequence follows from core JS semantics applied to that visible structure, not from any external framework convention.\n- Comment-dependent? No — the relevant comments talk about seams, grammar warm-up, and governance legs (claims about files I can't see); I'm not using any of them. The control-flow fact stands on the code alone.\n- Exact count/exhaustive list? I'm claiming an *absence* (zero catch handlers), which I verified by scanning every line; no miscountable enumeration involved.\n- Restatement of name/signature? No — \"bin.ts entrypoint\" tells you nothing about the fire-and-forget bootstrap or the lack of error interception; that's a latent behavioral property you'd miss on a skim.\n\nSurvives. Emitting.\n\n```atlas-fact\n{\"claim\": \"The entire bootstrap runs inside a void-discarded async IIFE with no try/catch, .catch, or top-level await anywhere in the file, so a failure in initAst(), composeRuntime(), or main() surfaces as an unhandled promise rejection rather than a handled CLI error, and exit status is communicated solely by the process.exitCode assignment.\"}\n```"