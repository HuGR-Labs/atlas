"STEP 1 — REASONING (scratch):\n\nScanning candidates:\n\n- *Header comments* (barrel wired at SEAL, held-out `-2` fixtures, `<filled-at-freeze>` digest) — all claims about other files/processes, not re-derivable from these bytes. Discard.\n- *Substrate fixture is voided*: `substrate` appears exactly twice — its construction and `void substrate;` — so it genuinely participates in no assertion. Code-checkable, but the adjacent comment already announces it, and its actionability is moderate. Weaker candidate.\n- *Monotonicity checks are `>=` (allow equality)* — near-restatement of the visible operators.\n- *`bobT7` inherits `failedWith`/`stoppedAt` from alice via spread* — visible but trivially so.\n- *Shape of the pushed recall in SCN-MEM-13a-1*: the assertion is a strict vitest `toEqual` against an object literal listing exactly four keys — `attempted`, `failedWith`, `stoppedAt`, `lesson` (counted from the literal, item by item). Since `toEqual` fails when the received object has extra *defined* keys, `pushed` cannot carry the entry's `taskId` (nor any other field of `TaskMemoryEntry`) — even though `TaskMemoryEntry` visibly includes `taskId` and the resumed unit is identified as `'T7'`. Nothing in the name `spawnRecall` or the visible types suggests the identifier is stripped from the payload; an implementer or consumer would otherwise naturally expect it echoed back. This rests only on the assertion literal plus standard `toEqual` semantics, not on any comment's claim about unseen code. Survives.\n\nEmitting the survivor.\n\nSTEP 2 — RESULT:\n\n```atlas-fact\n{\"claim\": \"In SCN-MEM-13a-1, the strict toEqual assertion pins spawnRecall's pushed fold to exactly the four fields attempted, failedWith, stoppedAt, and lesson, so the entry's taskId — present in the TaskMemoryEntry fixture — is omitted from the pushed recall payload.\"}\n```"