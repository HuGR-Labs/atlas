"STEP 1 — REASONING (scratch):\n\nReading the bytes: a small tsconfig for `packages/grounding`. Candidate facts:\n\n- *\"Builds to dist\"* — pure restatement of a visible key-value; low informational value. Weak.\n- *\"Extends the root base config\"* — plainly visible; also points attention outside the unit for anything substantive. Weak.\n- *\"Tests are emitted into the build output\"* — REFUTED: whether emission happens depends on unseen base options (`noEmit`, `rootDir`) in `../../tsconfig.base.json`; the strong form isn't re-derivable from these bytes. The weak form (\"test is in include scope\") is literal but near-restatement.\n- *\"Depends on contracts, kernel, and index via project references\"* — enumerated directly from the `references` array: exactly three entries, each checked against the shown bytes (`../contracts`, `../kernel`, `../index`). This reveals the package's intra-repo dependency topology, which is invisible from the filename and changes what an engineer does (moving/renaming siblings, build ordering, adding new internal deps). Careful to avoid unsupported extensions: don't claim the referenced packages are \"composite\" (flag not visible here — likely lives in the base config) and don't speculate on what `index` contains. Array order carries no asserted meaning — drop any layering-order claim.\n\nSurvivor: the reference-topology fact, phrased strictly from the literals in this file.\n\nSTEP 2 — RESULT:\n\n```atlas-fact\n{\"claim\": \"The grounding package is wired as a downstream consumer of its sibling packages contracts, kernel, and index, declared via TypeScript project references to ../contracts, ../kernel, and ../index.\"}\n```"