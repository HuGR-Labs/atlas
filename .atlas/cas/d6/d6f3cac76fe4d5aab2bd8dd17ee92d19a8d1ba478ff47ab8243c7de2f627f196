"STEP 1 — Reasoning:\n\nReading the bytes closely, the `MALICIOUS` fixture is built as `JSON.stringify({ ..., authz: { scopes: { __proto__: [STRANGER], src: [ACTOR] } } })`. Draft candidate: \"the serialized payload contains no `__proto__` key at all.\"\n\nTrying to refute it: Is this from the bytes or from outside knowledge? It follows from core ECMAScript semantics applied to the literal shown right here — in an object literal, a single `__proto__: value` property definition is the prototype setter (B.3.1), not the creation of an own property named `__proto__`. Since `[STRANGER]` is an object, the scopes object's `[[Prototype]]` is set to that array, and its only own enumerable property is `src`. `JSON.stringify` serializes own enumerable properties only, so the resulting string contains only `nearDup`, `t0Heuristic`, and `authz.scopes.src` — no `__proto__` key ever reaches `.atlas/policy.json`. Re-checking the literal: yes, it is a single occurrence of the `__proto__: <expr>` form (not computed, not shorthand, not duplicated), so the special semantics apply. The surrounding comment asserts the loader \"neutralizes\" a `__proto__` payload via `JSON.parse`/`Object.entries` behavior — but that is a claim about code (`loadPolicy`) not shown here, and the fixture bytes themselves undercut its premise: the test's S7b \"stranger denied\" result cannot distinguish parser-side neutralization from a poison key that was never serialized. Nothing in the visible code (no `JSON.parse('{\"__proto__\"...}')`, no `defineProperty`, no raw string embedding) avoids this. Not a count/exhaustive-enumeration claim, not a restatement of the test name (which implies a real payload exists), and it would change what an engineer does — anyone hardening this test must replace the literal with a hand-written JSON string for the payload to exist on disk at all. Survives.\n\n```atlas-fact\n{\"claim\": \"The MALICIOUS policy fixture never actually places a `__proto__` key in the serialized JSON: because a lone `__proto__:` entry in a JavaScript object literal sets the object's prototype rather than creating an own property, `JSON.stringify` emits only `nearDup`, `t0Heuristic`, and `authz.scopes.src`, so the on-disk payload carries no poison key and the S7b tests cannot distinguish loader-level neutralization from a payload that was never written.\"}\n```"