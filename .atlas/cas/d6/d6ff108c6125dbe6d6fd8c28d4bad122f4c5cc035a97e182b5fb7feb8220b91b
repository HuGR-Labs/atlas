"STEP 1 — scratch reasoning:\n\nReading the bytes: four `it` blocks exercise the door — a hit (`node <addr>`), a miss (`deadbeef`), a bare arity failure (`node` with no arg), and a loop over three hostile addresses. Scanning every `runAtlas` result: `expect(hit.stderr).toBe('')`, `expect(miss.stderr).toBe('')`, `expect(bare.stderr).toBe('')`, and inside the loop `expect(miss.stderr).toBe('')`. Every single invocation — success and failure alike — asserts stderr is *exactly* empty, while error output (`status: error`, the reason strings) is asserted on **stdout**. That last part is the surprise: conventional CLI design puts errors on stderr, but this suite pins the opposite contract.\n\nTrying to refute:\n- Derivable from these bytes alone? Yes — the four stderr assertions and the stdout `status: error` expectations are literal code, not comment claims.\n- Resting on a comment about unseen code? No — the comment about billy's 4.67GB RSS measurement describes motivation elsewhere, but my claim doesn't depend on it.\n- Exact count / exhaustive enumeration? \"Every invocation\" — I enumerated: hit, miss, bare, and all three loop iterations each carry the stderr-empty assertion. Verified item by item; safe, though I'll phrase qualitatively.\n- More than a restatement of name/signature? Yes — the unit name says nothing about stream routing; an engineer would plausibly assume errors land on stderr and write assertions accordingly, then be wrong.\n\nSurvives. (Runner-up candidates — the `'AB'.repeat(32)` uppercase-charset case hiding inside the \"path-traversal\" test, and the 10-second timing bound — also survive, but the stderr/stdout inversion is broader and more directly action-changing.)\n\nSTEP 2 — emit:\n\n```atlas-fact\n{\"claim\": \"Across every scenario this suite exercises (hit, miss, arity failure, and hostile-address attempts), the atlas subprocess's stderr is asserted to be exactly empty, with all structured output — including status:error responses — required on stdout instead.\"}\n```"