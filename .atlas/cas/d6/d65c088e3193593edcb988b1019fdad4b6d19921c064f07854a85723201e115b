"STEP 1 — REASONING (scratch):\n\nCandidate A: \"doctor persists nothing / is structurally write-incapable.\" Refuted as a *new* fact: while true and verifiable in the interface (`DoctorSource` exposes only `lineage`, `drift`, `hotSetSize`, `plan`), this is the headline claim of nearly every comment in the file and is effectively the summary of what the code plainly does — an engineer reading the frozen API already knows it. Discard.\n\nCandidate B: \"The file has zero runtime imports (both imports are `import type`).\" Verifiable from the bytes (`import type { GroundedFact } ...` and `import type { DoctorOut, ... }`). True, but weakly actionable — unlikely to change what an engineer does. Discard as low-value, though not false.\n\nCandidate C: \"`whyBroken` and `reground` return `{}` on a source miss while `archive` and `hotSet` always populate their field.\" Checked item by item against the closures: `archive` → unconditional `{ archive: source.lineage(scope) }`; `whyBroken` → `d ? { whyBroken: d } : {}`; `hotSet` → unconditional; `reground` → `{}` when `candidate === undefined`. Survives refutation — qualitative form, no risky counts, grounded purely in control flow. Valid survivor.\n\nCandidate D: \"The `over` flag uses strict `>` , so size exactly equal to budget is NOT flagged over-budget.\" Verified against the literal `over: size > budget` on the `hotSet` return line. Not derivable from the name `hotSet(budget: number)` or return type alone; no comment asserts a different comparison; no reliance on unseen modules. Boundary semantics directly affect whether a consumer alerts. Survives refutation, and is the crispest, least-restatement-like fact.\n\nChoosing D over C: C is close to describing what the code plainly does leg by leg; D pins a specific operator choice invisible in the signature that flips behavior at the boundary case.\n\nSTEP 2 — EMIT:\n\n```atlas-fact\n{\"claim\": \"The hot-set over-budget flag is computed with a strict greater-than comparison (size > budget), so a hot-set whose size exactly equals the budget is reported as not over budget.\"}\n```"