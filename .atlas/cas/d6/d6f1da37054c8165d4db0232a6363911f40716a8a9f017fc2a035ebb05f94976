"Looking at this unit: it is an ADR markdown document whose load-bearing \"declarations\" are the quoted normative clauses and the literal law formulas it writes out. From the filename alone, an engineer learns \"obviousness is scored, never gated\" — but that alone reads like \"attach a score as advisory metadata.\" Draft candidates and refutation:\n\n1. *\"mine.ts falls back to defaultGate()\"* / *\"`makeAdmitGate` had zero callers until REQ-CLI-4d\"* — rests entirely on claims about other files/modules not shown; discard per the external-reference rule.\n2. *\"35 doc files / ~14 modules mention the bar\"* — an exact/approximate count asserted by the author, not something I counted in shown bytes; discard.\n3. *\"Two surfaces need the owner, the third is a correction\"* — requires reconciling the header's three-item Amends list against the closing section; a mislabelled item makes it false; discard as fragile counting.\n4. *\"Score is never read off a proposer-written field\"* — survives partially, but its reinforcement leans on a claim about `Candidate.signals`/ADR-0011 (other files); weaker.\n5. **Totality**: the doc contains, as literals, the second law `emitted(s) ⟹ hasScore(s.obviousness)` marked `[TOTALITY]`, the amended clause wording \"an emitted fact without one is a defect, not a default,\" and the new teeth naming \"a scoreless emitted fact (totality violated)\" as build-breaking. None of that is derivable from the filename, all of it is checkable against formulas/quotes present in these bytes, no external files or counts are involved, and it materially changes implementation (score must be a mandatory, total field wired into mutation tests — not optional metadata).\n\nCandidate 5 survives.\n\n```atlas-fact\n{\"claim\": \"Scoring is not optional metadata: the ADR's second written law makes the score total — every emitted seed must carry a mechanically-computed obviousness score, an emitted fact lacking one is defined as a defect rather than a default, and the rewritten teeth treat a scoreless emitted fact (alongside a resurrected obviousness gate) as a build-breaking mutation.\"}\n```"