"STEP 1 — REASON FREELY:\n\nReading the bytes closely:\n\n- The header comment advertises \"no raw hashing here\" and frames `transcriptSha` as \"minted through the SEALED @atlas/kernel `asHash` seam.\" If I take the comment at face value, I'd assume every transcript hash flows through `asHash`.\n- But the actual expression is: `typeof w.transcriptSha === 'string' ? (w.transcriptSha as Hash) : asHash('')`. The `asHash` call sits **only** on the fallback branch. When `wp.transcriptSha` *is* a string, it is cast directly via `as Hash` — no `asHash`, no validation whatsoever. So a caller-supplied garbage string becomes a typed `Hash` by pure assertion; the sealed seam is exercised only for the empty default.\n- Refutation pass: Is this derivable from these bytes? Yes — it's the ternary itself, not anything about `@atlas/kernel` internals (which I never rely on; I only observe where `asHash` appears in the shown control flow). Does it rest on a comment's claim about unseen code? No — it rests on the opposite of a comment's implication, and the checkable evidence is the branch structure right here. Is it an exact-count/exhaustive claim? No. Is it more than a restatement of name/signature/types? Yes — the signature `meter(wp: unknown): Metering` reveals nothing about this branching, and it materially changes what a caller does (they cannot rely on `meter` to validate or canonicalize a supplied transcript hash; they must validate upstream).\n- Discarded candidates: (a) \"11-field total record\" — countable, but it partly leans on the unseen `Metering` schema definition and is close to restating the comment; (b) `gates` elements unchecked beyond `Array.isArray` — true and byte-derivable, but the transcriptSha fact is strictly more consequential; (c) non-object `wp` treated as `{}` — too close to a plain summary of the guard.\n\nThe transcriptSha candidate survives. Emitting it.\n\nSTEP 2 — RESULT:\n\n```atlas-fact\n{\"claim\": \"In meter(), the sealed asHash seam is exercised only for the fallback: a wp.transcriptSha that is already a string is blind-cast to Hash with no validation, so arbitrary strings surface as typed transcript hashes while only the missing or non-string case receives the canonical empty hash.\"}\n```"