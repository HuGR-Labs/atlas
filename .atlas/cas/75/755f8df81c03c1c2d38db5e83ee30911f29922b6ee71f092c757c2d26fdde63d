"STEP 1 — REASONING (scratch)\n\nCandidate drafts:\n\n1. *\"The advisory band uses cap-wins truncation while the other loops use greedy best-fit.\"* Refutation pass: is this from the code or the comment? The comment asserts it, but I can verify independently: the advisory loop has `let advisoryCapped = false;` and the branch `if (!advisoryCapped && fits) { … } else { advisoryCapped = true; … }` — once any row fails, the flag latches and `!advisoryCapped` is permanently false, so every later row is dropped even though `fits` is recomputed each iteration. The invariant, gotcha, and manifest loops have no such flag: their `else` branches just record/skip and the `for` continues, so a later smaller row can still pass `used + cand.cost <= OWN_CAP` after an earlier oversized one was skipped. All symbols involved (`advisoryCapped`, `OWN_CAP`, `OWN_ADVISORY_CAP`) are declared in this unit. Survives — checkable purely from control flow here.\n\n2. *\"Dropped manifest pointers never enter `pullReachable`, only `truncated`.\"* True in code (the manifest loop's `else` sets `truncated = true` without pushing `overflow`). Viable, but weaker actionability than #1.\n\n3. *\"`dedup` only matches invariants, never gotchas.\"* True (`ownIds` built only from `ownPack.invariants`; loop touches only `pack.invariants`). Viable but close to a plain reading of the loop body.\n\n4. Epic-level advisory rows are never re-checked against `OWN_ADVISORY_CAP`. True in `ownEpic` (no sub-cap reference there), but partially leans on the feature packs having been pre-sub-capped, which is cross-function reasoning inside the unit — acceptable, yet #1 is sharper.\n\nChosen: #1. It is not derivable from any name or signature, is verified line-by-line in the shown bytes (latching flag present in exactly one loop, absent from the three siblings), and would change what an engineer does (row order, not just size, determines survival once the cap bites; backfill is impossible in the advisory band but possible in the others). Exact-count risk: none — qualitative claim, no enumeration asserted.\n\nSTEP 2 — RESULT\n\n```atlas-fact\n{\"claim\": \"In composeOwn, the advisory loop latches closed at the first row that does not fit — every later advisory row is then dropped even if it would fit both OWN_ADVISORY_CAP and OWN_CAP — while the invariant, gotcha, and manifest loops keep scanning and can still admit a smaller later row after skipping an oversized one.\"}\n```"