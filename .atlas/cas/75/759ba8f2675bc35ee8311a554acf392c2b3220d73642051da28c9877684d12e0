"```atlas-fact\n{\"claim\": \"Despite the comment labeling it a structural keyword match, the test's T0 heuristic decides candidacy purely by looking up the territory's name in the hardcoded KEYWORD_HITS record ({ 'auth/': 'security-invariant' }) and testing that value's membership in T0_KEYWORDS — it never inspects globs or content, so a territory with no KEYWORD_HITS entry can never be flagged regardless of its data.\"}\n```"