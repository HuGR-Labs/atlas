"**STEP 1 — REASONING (scratch)**\n\nCandidate facts considered and tested against the bytes:\n\n- *\"MCP cell has a 60 s timeout because builds are slow\"* — the `60000` literal is code, but the rationale (18 s / 30.5 s measurements) lives only in a comment describing conditions outside these bytes. The bare timeout value is weakly actionable. Discarded.\n- *\"Assertions match the reason name before the first colon by equality\"* — true, and `reasonOf` shows it plainly, but that's a summary of what the helper obviously does once read; low surprise value. Discarded.\n- *\"CLI blanket check precedes payload validation (line ~184)\"* — rests entirely on a comment's claim about `cli.ts`, a file not shown. Explicitly disqualified.\n- *\"Fixture uses `git add -f`\"* — the flag is in the bytes, but interpreting it (harness ignores `.atlas/`) leans on unseen harness internals. Discarded.\n- *\"Committed `projection.json` alone is served, not refused\"* — real and code-pinned (CONTROL 2 asserts exit 0, no `untrusted-store`), but the header comment advertises it loudly; partially redundant with a stronger observation.\n- **Survivor:** comparing `committedStoreRepo()` and `committedStagingRepo()` line by line: both call `writeFileSync` with the *identical* second argument (`{\"current\":[],\"cas\":[]}`), differing only in the third path segment (`projection.json` vs `staging.json`). The suite then asserts opposite outcomes for these near-identical repos: every staging cell (MCP `atlas_query`, `emit`, `link`, `query`, `node`, `doctor` — checked item by item, six cells, all refuse with `untrusted-store`/exit 2/fault `refused`) versus the projection control (`query` exits 0, stdout lacks the token). So within this unit, committed filename alone — not payload content — is the entire served/refused discriminator. Fully derivable from declarations, literals, and assertions in these bytes; no reliance on cross-file comment claims; no risky numeric claims. Not inferable from the unit's name/signature, and directly actionable (e.g., don't unify the fixtures assuming content matters; preserve the asymmetry when touching gates).\n\n**STEP 2 — RESULT**\n\n```atlas-fact\n{\"claim\": \"Both committed-store fixture helpers in this file write byte-identical JSON into .atlas/, differing only in the committed filename, and the suite pins that committing it as staging.json makes every exercised door refuse as untrusted-store while committing it as projection.json leaves query exiting 0 with no refusal.\"}\n```"