"Looking at the bytes: the last test constructs a predicate seed whose `rawAnswer` literal is `'  svc holds its invariant\\n'` — deliberately dirty input with leading spaces and a trailing newline — and then asserts `(v.fact as { rawAnswer?: string }).rawAnswer` equals that *exact* literal via `toBe`. Both strings appear identically in the unit, so the seam's pass-through contract is pinned at the byte level, entirely within this file. Draft candidates and refutations: (a) \"relation/negation skip synthesis\" — supported by `synthCalls()` assertions, but the inline comments already announce it, lowering novelty; (b) \"kind union includes undefined\" — already implied by the advisory seed literal omitting `kind`; (c) \"advisory facts carry `obviousness.by = 'harness-predicate'`\" — real but thin on actionability; (d) the untrimmed `rawAnswer` — survives: it rests on two visible literals plus the strict-equality assertion, not on any comment, issue reference (#195b appears only in the title and is ignored), or unseen code; it is not a name/signature restatement; it asserts no counts or enumerations; and it would change what a consumer or modifier of the seam does (never normalize `rawAnswer`, and downstream consumers cannot assume it's clean).\n\n```atlas-fact\n{\"claim\": \"The suite pins that a seed-supplied rawAnswer is threaded onto the admitted fact verbatim — leading spaces and the trailing newline preserved — so the gate applies no trimming or normalization to it.\"}\n```"