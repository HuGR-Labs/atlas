"Looking at this unit closely: it's an ADR whose body mixes an original narrative with later inline corrections, so I need a fact grounded in the document's own literals, not its claims about other files.\n\nCandidate 1: \"atlas-diff has zero production callers in diff.ts\" — REFUTED as a fact *of this unit*: it's a claim about `packages/tools/src/diff.ts` and `reference-model-guard.mjs`, neither shown here. Discard.\n\nCandidate 2: \"ARCH-5 forbids advertising unwired doors\" — its authoritative home is `layer-guard.mjs`, not shown. Discard.\n\nCandidate 3: the operative set definitions. Checking the fenced constant block in the Decision section, item by item: GOVERNANCE_SURFACE = {init, query, emit, reconcile, link} = 5 ✓; WRITE_PATHS = {emit, link} = 2 ✓ (subset of governance ✓); READ_SURFACE = {anchors, slots, draft, check, doctor, node} = 6 ✓, matching the reconciliation header's \"SIX genuinely invocable read doors … never seven\", with `atlas-diff` absent from the corrected list ✓, and READ ∩ WRITE = ∅ ✓. These are literals in the shown bytes, they contradict the superseded original text (which claimed seven), and they're not derivable from the filename alone. They survive.\n\n```atlas-fact\n{\"claim\": \"In its reconciled form this ADR fixes READ_SURFACE at exactly six tools (atlas-anchors, atlas-slots, atlas-draft, atlas-check, atlas-doctor, atlas-node) and explicitly bars atlas-diff from the surface until it gets real transport wiring, while GOVERNANCE_SURFACE stays five, WRITE_PATHS stays two (atlas-emit, atlas-link), and the spec-conformance guard must newly enforce READ_SURFACE ∩ WRITE_PATHS == ∅.\"}\n```"