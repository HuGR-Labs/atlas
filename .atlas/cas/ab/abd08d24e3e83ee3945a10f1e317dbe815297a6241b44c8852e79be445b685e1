"STEP 1 — scratch reasoning (not persisted):\n\nReading the bytes closely. Candidate facts:\n\n1. *\"driftDetect AND-folds multi-entry receipts\"* — rests on the doc's claim about `grounding/src/ground.ts`, which is not in these bytes. Discard.\n2. *\"hugit-diff was discontinued\"* — a recorded event about an external project; the fact is about that project, not re-derivable from structure here. Discard.\n3. *\"Negations must abstain absent a completeness witness\"* — this is the document's own ratified-pending proposal content and IS in the bytes, but it is extensively summarized by the title's framing (typed tokens per shape) and the abstract; weaker novelty.\n4. **Ordinal-count tension**: The Decision states verbatim \"There are FOUR shapes\" and enumerates exactly four sub-decisions — D1 positive-intrinsic, D2 relation, D3 negation, D4 transition (confirmed item-by-item against the table: four rows). Then D5 introduces test-vacuity and labels it verbatim \"The SIXTH `GroundedFact` shape\". Four enumerated + test-vacuity = five; \"sixth\" requires six. Refutation attempt: could \"shape\" in the Decision and \"GroundedFact shape\" in D5 be different taxonomies making this consistent? Even granting that, the document still simultaneously fixes the shape count at four and assigns test-vacuity ordinal six — arithmetically irreconcilable within these bytes, forcing the conclusion that at least one shape exists outside what this ADR enumerates. None of the three amendments renumbers shapes. Survives.\n5. *Exact-number rule check*: the numbers here are the point itself, and each was counted/verified verbatim in the shown bytes (four sub-decisions, \"FOUR\", \"SIXTH\"). Admissible.\n\nFact 4 survives: it is not derivable from the title, it is checkable by reading this unit's own literals, and it changes what an engineer does — they cannot treat this ADR as the complete shape catalog when extending the fact-model enum or validating exhaustiveness.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"Although the Decision section declares there are exactly FOUR fact shapes and enumerates them as sub-decisions D1–D4 (positive-intrinsic, relation, negation, transition), sub-decision D5 designates test-vacuity as 'the SIXTH GroundedFact shape' — an ordinal that cannot be reconciled with the five shapes this document defines, implying the shape vocabulary contains at least one further shape this ADR never enumerates.\"}\n```"