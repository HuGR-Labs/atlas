"STEP 1 — scratch reasoning (not persisted):\n\nScanning the bytes: the file defines `SeedCandidate`, `SeedDecision`, `seedGate`, `Calibrate`, `SeedGateDeps`, `SeedGate`, and `bindSeedGate`.\n\nCandidates drafted:\n1. \"self_score/importance are never read\" — true and code-verifiable (only `candidate.grounded` is accessed), but it is the module's loudly documented headline and effectively a summary of the plainly intended behavior; discard as restatement.\n2. \"obviousness rides through onto seeded decisions but never enters the branch condition\" — again the ternary's two arms are visibly identical in `seeded`/`reason`, but this is exactly what ADR-0012 comments announce; borderline summary; discard.\n3. \"when `obviousness === undefined` the key is omitted rather than set to undefined\" — derivable from the ternary, but observable behavior differences are negligible (property access, JSON.stringify); low actionability; discard.\n4. \"`Calibrate` is declared locally instead of imported from @atlas/knowledge like HitsApi etc.\" — checkable in the import list, but the interesting part (\"mirrors KNOW-17\") rests on a comment about unseen code; discard.\n5. **Survivor**: in `bindSeedGate`, the wiring is literally `seed: seedGate` — the bound gate's `seed` is the standalone module function itself, so `deps.hits` and `deps.calibrate` influence only `consult`, `decay`, and `admissionThreshold`. From the signature `bindSeedGate(deps: SeedGateDeps): SeedGate` alone, an engineer could reasonably assume the injected calibrate participates in per-candidate admission; the body shows it cannot affect seeding at all. Refutation pass: derivable purely from this file's code (`seed: seedGate`, and `seedGate` touches neither dep); no reliance on comments about other files; no exact counts; requires reading the body, not just the signature. Survives.\n\nSTEP 2 — emit result:\n\n```atlas-fact\n{\"claim\": \"In bindSeedGate, the seed method is the exported seedGate function itself, so the injected hits ledger and calibrate influence only consult, decay, and admissionThreshold — no dependency configuration can change which candidates get seeded.\"}\n```"