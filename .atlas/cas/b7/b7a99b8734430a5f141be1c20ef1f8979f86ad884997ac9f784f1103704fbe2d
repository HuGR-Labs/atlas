"STEP 1 — scratch reasoning:\n\nScanning the unit: it is a vitest spec covering three facets — transport parity over one handler/schema, guidance totality, and the doctor's read-only nature. Most striking content lives in comments (#180, GATE-held `-2` fixtures, QUERY_GUIDANCE-vs-DOCTOR_GUIDANCE history, cli/src/doctor.ts callers) — all claims about files/processes NOT in these bytes, so all discarded per the rule.\n\nCandidate A: \"the malformed-input scenario works because the financeIndex double's `cover` calls `scope.split('/')` unconditionally, throwing on `{scope: 42}`.\" The unconditional `.split` IS in the bytes, but whether the throw (vs. a schema pre-validation) is what produces the rejected envelope depends on handler internals not shown — reachability unverifiable here. Discard as partially resting on unseen mechanism.\n\nCandidate B: \"the query leg needs `as unknown as ToolData` and `planFact` needs `as unknown as GroundedFact`, showing static type mismatches with the contracts.\" The double casts are visible, but inferring *why* (non-assignability vs. author habit) leans on general TS convention; weak actionability. Discard.\n\nCandidate C: the mutation-teeth compare `JSON.stringify([...medium.entries()])`, so transient write-then-delete cycles would be invisible. True arithmetic consequence of the code, but it's my own design critique/inference rather than a contained fact the unit states; borderline speculative. Discard in favor of something crisper.\n\nCandidate D: SCN-TOOLS-12c-1 contains `expect(GOVERNANCE_SURFACE.length).toBe(5);` and `expect(GOVERNANCE_SURFACE).not.toContain('atlas-doctor');` — literal code, counted and re-checked in the bytes (one numeric literal, one explicit exclusion). Refutation pass: does it depend on a comment? No — the `.toBe(5)` line stands alone; I phrase the claim about the test's assertion, not about the imported array's unseen contents. Is it from name/signature? No — the file name says nothing about pinning global surface arity. Does it change what an engineer does? Yes — anyone registering a sixth governed tool (as WP-SAMEAS reportedly did for atlas-link) turns this unrelated-seeming doctor test red and must edit this file's literal. The exact number is the point and is verified against the shown bytes. Survives.\n\nEmitting D.\n\n```atlas-fact\n{\"claim\": \"Beyond checking that the doctor exposes no write methods, SCN-TOOLS-12c-1 hard-pins GOVERNANCE_SURFACE.length to the literal 5 and asserts 'atlas-doctor' is absent, so registering any new governed tool will make this test fail until that constant is updated here.\"}\n```"