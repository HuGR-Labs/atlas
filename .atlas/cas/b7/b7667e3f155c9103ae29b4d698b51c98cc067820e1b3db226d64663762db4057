"```atlas-fact\n{\"claim\": \"In makeGateFix the returned facts map is populated with all four grounded advisories regardless of the seed argument — the seed parameter is consulted only by the loop that writes entries into the CAS/projection — so even the clean fixture exposes facts like sec-rot and mixed that were never seeded into its knowledge base.\"}\n```"