"Reading the bytes closely:\n\n- The file is a vitest suite over `verifyRelation` with a hand-built fake `SymbolReverseApi` (`feed`) and identity `pathOfHash`.\n- Scanning every assertion: the proven case uses `toEqual` on `{verdict, oracle}`; AR-5, both AR-3 cases, the dynamic-dispatch half of AR-17, and both \"soundness teeth\" cases each `toEqual` a full object **including an exact `reason`**; the four malformed-input cases assert `.reason` explicitly.\n- The one exception: inside AR-17, the *second* assertion (cross-language target `scip:python:mod#f`) ends `.verdict).toBe('abstain')` — verdict only, no reason ever checked.\n- Refutation pass: is this telegraphed by the test title or header comments? The title says \"abstain, never a proof\" but says nothing about which reason fires for the cross-language leg — so the weak pinning is not derivable from names/titles/comments. Is it resting on anything outside these bytes? No — it's pure assertion-shape inspection of this file. Did I miscount? Re-checked item by item: ten reason-pinning expects vs. this one verdict-only expect; the universal holds.\n- Other candidates discarded: the \"identity hash / strings cast as Hash\" observation is real but borders on restating the visible helper; the \"negative tests keep a truthful feed and lie only in the claim\" pattern is largely announced by the test titles themselves (\"soundness teeth\", \"even though a DIFFERENT file in the same dir does\"); comment-sourced claims about `@atlas/index`, ADR-0018, #99/#189, and §refs are about unseen code and were dropped.\n\nSurvivor: the cross-language leg's abstention reason is uniquely unpinned in this suite — actionable for anyone tightening the reason taxonomy or judging this boundary's coverage.\n\n```atlas-fact\n{\"claim\": \"In the AR-17 honest-boundary test, the cross-language assertion checks only that the verdict equals 'abstain' and never asserts a reason, making it the only abstaining case in this suite whose exact abstention reason is left unpinned (every other abstaining assertion, including the dynamic-dispatch case next to it and the four malformed-input checks, pins a specific reason).\"}\n```"