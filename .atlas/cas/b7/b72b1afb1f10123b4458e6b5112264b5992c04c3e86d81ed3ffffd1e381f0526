"Looking at this unit: a method-tag register (markdown spec) for a retrieval block, 13 invariants, each with a tag plus rationale, plus refuse-to-model and completion sections.\n\nDraft candidates and refutation passes:\n\n1. *\"Tag histogram is PBT×4 / reference-model×9\"* — I counted the body (PBT: RETR-2/6/10/12; ref-model: 1/3/4/5/7/8/9/11/13) and it matches the completion report, but the report already states this verbatim → restatement of what the document plainly says. Discard.\n2. *\"RETR-13's threshold is an open DEFINE dependency; model is threshold-parametric\"* — real and consequential, but it is asserted explicitly in three places in the doc; parroting an explicit headline claim isn't a hidden fact. Discard.\n3. *\"Only RETR-12 carries an AMENDED annotation (2026-08-03)\"* — verified by scanning (single occurrence), but weak actionability. Discard.\n4. *\"All 13 `fspec:` fields are '—'\"* — entailed by the header and FSPEC-merge section; near-restatement. Discard.\n5. Claims about `atlas-retrieval.md#retr-12` diverging, `retrieval/ref/*.ts` contents, DEFINE artifacts — all rest on other files not shown. Discarded per rule.\n\nSurviving candidate — a tension inside RETR-6's own bytes, acknowledged nowhere in the document: the oracle recipe is \"order kinds by `(pinned-desc, hitRate-asc)` … then drop from the bottom.\" Under that sort, the tail of the list is the *highest*-hitRate unpinned kind (pins sit at the head via `pinned-desc`; `hitRate-asc` puts lowest hitRate right after them). Popping from the bottom therefore drops the *most*-used kind first — the exact inverse of the up-property's \"least-used first\" (and RETR-8 pins the semantics: hits = served facts used, so high hitRate = heavily used). Notably, the enumerated PBT laws (determinism, total-order, pin-never-dropped, ceiling-holds) omit a least-used-ordering law, so a literal implementation of the tuple would pass the block's own gates while violating the stated property. Every clause used in this derivation is present in the shown bytes; no external file is trusted; no count is claimed.\n\nThis survives refutation and would change what an implementer or golden-author does (which end of the sorted list to pop, and whether to add the missing ordering assertion).\n\n```atlas-fact\n{\"claim\": \"In INV-RETR-6, the stated oracle recipe — order kinds by (pinned-desc, hitRate-asc) then 'drop from the bottom' — mechanically drops the highest-hitRate (most-used) unpinned kind first, contradicting the same invariant's 'least-used first' drop property, and the enumerated PBT laws (determinism, total-order, pin-never-dropped, ceiling-holds) omit any least-used-ordering assertion, so the block's own gates would not catch the inversion.\"}\n```"