"```atlas-fact\n{\"claim\": \"In createDiskStore's put, an input that fails either serialization — id(obj) throws, JSON.stringify(obj) throws, or stringify answers a non-string such as a bare top-level undefined — hits no filesystem call whatsoever and the method returns the one fixed sentinel hash EMPTY = asHash(''), so the returned Hash is not by itself proof that the object was stored.\"}\n```"