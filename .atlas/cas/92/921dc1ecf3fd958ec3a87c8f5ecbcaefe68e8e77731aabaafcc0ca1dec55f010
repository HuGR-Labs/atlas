"STEP 1 — REASONING (scratch)\n\nReading the bytes of `orientEvent` closely:\n\n```ts\nconst payload: OrientEventPayload = { channel, label };\nconst contentHash: Hash = id(payload);\nreturn { id: contentHash, seq: 0, nodeKey: KEY_OF[channel], contentHash, fresh: true, supersedes, payload };\n```\n\nCandidate A: \"`seq` is hardcoded to 0 and `fresh` to true.\" — Visible in the literal, but it's essentially a plain restatement of what the return statement shows; low actionability. Weak.\n\nCandidate B: \"Empty-string parent label conflates 'parent outside tail' with 'malformed parent', falling back to `prev.current`.\" — Survives refutation (fully internal: `labelOf` → `''`, `predecessorLabel` → `''`, ternary fallback), but it's a close call between this and a sharper fact.\n\nCandidate C: \"Event identity is computed from the `{channel, label}` payload alone.\" Checking against the bytes: the payload object contains exactly `channel` and `label` (confirmed by both the interface and the literal); `contentHash = id(payload)` — `supersedes` is **not** passed to `id`; and the returned `id` field aliases `contentHash`. So the `supersedes` argument, though it differentiates events, does not participate in their identity: two calls with the same channel+label but different `supersedes` produce the same `id`. Refutation attempts: (1) Does this rest on a comment? No — the payload construction, the `id(payload)` call arguments, and the `id: contentHash` aliasing are all code. (2) Does it depend on unseen kernel semantics? Only trivially — the unit's own code types the result as `Hash` and names it `contentHash`, and the structural exclusion of `supersedes` from the identity computation holds regardless. (3) Exact count/enumeration? No numeric claim. (4) Restatement of name/signature? No — the signature `orientEvent(channel, label, supersedes=[])` gives no hint that the third parameter is excluded from identity.\n\nCandidate C survives most sharply and would change what an engineer does (labels must carry their own differentiating content; lineage corrections alone don't change event identity).\n\nSTEP 2 — RESULT\n\n```atlas-fact\n{\"claim\": \"orientEvent assigns the same hash to both `id` and `contentHash` by calling `id` on only the `{channel, label}` payload, so the `supersedes` argument does not participate in event identity — two calls with identical channel and label produce identical event ids even when their supersedes arrays differ.\"}\n```"