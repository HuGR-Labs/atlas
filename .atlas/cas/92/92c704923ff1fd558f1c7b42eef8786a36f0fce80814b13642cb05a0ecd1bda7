"STEP 1 — REASONING (scratch):\n\nReading the bytes closely:\n\n- The drift test commits `src/pay/newcaller.ts` with content `'export function nc() { return 3; }\\n'` — this file defines `nc` and contains **no reference or call to X** whatsoever, despite the suggestive filename \"newcaller\".\n- Immediately after, the test asserts `lines[0]).toContain('[DRIFTED]')`.\n- So the FRESH→DRIFTED transition is triggered purely by a new file *existing* in the scope directory, not by any change to the negated relation (nothing now calls X).\n- Refutation attempts: (a) Is this just the test name? No — the name says insertion causes DRIFTED (\"insertion-sensitivity a per-unit hash lacks\"), but it does not reveal that the inserted file is semantically inert with respect to X; the code literal proves the drift tripwire ignores relevance to the negated relation. (b) Does it rest on comments about other files? No — it rests entirely on the committed string literal and the adjacent assertion, both in these bytes. (c) Exact counts/enumerations? None claimed. (d) Restatement of name/signature/types? No — it's a behavioral nuance beyond the name.\n- Other candidates considered: exit-code-2 abstention contract (heavily announced by comments and test titles); X being defined outside the scope (visible but largely narrated by the header); env-var save/restore (routine). The inert-insertion drift trigger is the one that survives cleanly and would change an engineer's interpretation of a DRIFTED verdict (coarse directory-membership tripwire, not evidence the negated relation changed).\n\nSTEP 2 — RESULT:\n\n```atlas-fact\n{\"claim\": \"The file inserted to induce drift (`src/pay/newcaller.ts`) contains only a definition of `nc` and no reference to the negated target X, yet the test asserts the negation reads [DRIFTED] — so the freshness verdict trips on any file entering the scope directory, not on any actual change to the negated calls relation.\"}\n```"