"**STEP 1 — REASONING (scratch)**\n\nThe unit is an ADR titled \"single write door → governed write doors\". From the name alone, an engineer would know: writes go through enumerated governed doors, `atlas-emit` plus `atlas-link` for sameAs. What would they *not* know?\n\nCandidates drafted:\n\n1. *GOVERNANCE_SURFACE = 5, WRITE_PATHS = 2.* — Countable in the bytes (init/query/reconcile + emit/link), but largely restates the title's \"doors (plural)\" idea and is a summary of the decision section. Weak actionability.\n2. *Refusal discriminants compared by equality, never substring.* — Present in bytes, but an implementation micro-detail unlikely to redirect an engineer.\n3. *`deriveSameAs` is rebuild-per-read, not incremental.* — Interesting, supports cheap retraction, but reads as rationale/perf trivia.\n4. **Retraction LATCHES.** — The ADR records that re-asserting a withdrawn pair is refused with the named discriminant `retracted-pair`, explicitly *not* idempotent success (unlike the assert path), and re-assertion is deliberately unbuilt. This is absent from the title, is forward-looking current decision content (not struck through, not marked superseded — unlike the stale deferral bullet, which I avoided), and directly changes operator behavior: you cannot fix a mistaken retraction by re-running `atlas-link`.\n5. *Gate fold `sameAsClassOf` is retraction-blind (pricing never shrinks).* — Also strong and non-obvious, but subtler; #4 is the crispest behavioral surprise.\n\nRefutation pass on #4:\n- Derivable from THESE bytes? Yes — stated in the Retraction decision section with the literal `retracted-pair`; cross-consistent with the append-only `sameAsRetracted` property and the `LinkOut`/CLI rendering bullets.\n- Resting on a comment about unseen code? The refusal names and latch semantics are normative content OF this document (its decision record), not commentary pointing elsewhere; no reliance on issue numbers or other files' internals.\n- Exact count/enumeration risk? None — qualitative claim, no counts asserted.\n- Stale/past-tense trap? Checked: the only superseded content is the struck-through v1-scope bullet, which I did not touch.\n- More than name/signature restatement? Yes — nothing in \"ADR-0003-governed-write-doors\" suggests irreversibility of the link operation in one direction.\n\n#4 survives. Emitting.\n\nSTEP 2 — RESULT\n\n```atlas-fact\n{\"claim\": \"Retracting a sameAs link latches permanently: re-asserting a previously withdrawn pair through the same governed atlas-link door is refused with the named discriminant `retracted-pair` (deliberately asymmetric with the assert path's idempotent success), so a mistaken retraction cannot be undone by simply re-linking.\"}\n```"