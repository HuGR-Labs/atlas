"STEP 1 — scratch reasoning:\n\nThe only executable bytes here are a two-line predicate: `typeof v === 'string' && v.length > 0`. Nearly everything else in the file is commentary asserting things about *other* files (policy.ts, governed-emit.ts, deleted implementations, defect numbers) — per the rules, all of that is discarded: it rests on comment claims about code not present in these bytes.\n\nCandidates considered and refuted:\n- \"Reached on every governed write from three production call sites\" — pure comment claim about other files; discard.\n- \"No case-folding/trimming/normalization\" — asserted mainly in the comment; while technically consistent with the body, it is an absence-of-behavior claim and largely a paraphrase of the comment, not a surprising code fact; discard.\n- \"Boxed `String` objects are rejected because the typeof check runs first\" — derivable, but effectively a restatement of how `typeof` works applied to the plainly visible first clause; marginal actionability; discard.\n- \"Empty string is rejected\" — survives. The signature `(v: unknown): v is string` and the name `isScope` do NOT reveal this: the declared predicate narrows only to `string`, and nothing in the name implies non-emptiness. Only the body's `v.length > 0` clause establishes it. It is checkable solely from these bytes, involves no counts or enumerations, and changes caller behavior: anyone passing `''` (e.g., as an \"unset\" sentinel or defaulted value) gets a fail-closed `false`, and the post-guard type `string` silently overstates less than the runtime guarantee conveys.\n\nSTEP 2 — emit:\n\n```atlas-fact\n{\"claim\": \"isScope fails closed on the empty string: the guard requires v.length > 0 in addition to the typeof check, so '' is rejected even though the declared type predicate narrows only to plain string.\"}\n```"