"Scratch reasoning (not persisted):\n\n- Unit type: a prose design document (`docs/design/atlas.md`), so \"code bytes\" = the document's own asserted content; pointers to sibling files (spec/memory/reference) are out of scope.\n- Candidate 1: \"Maestro v1 had 0 facts fed\" (§13) — past-tense claim about another system, not re-derivable from this unit. Discard.\n- Candidate 2: token-cost stats (~15× tokens, ~80% variance) (§3) — attributed external claims, not checkable here. Discard.\n- Candidate 3: \"No embeddings, no RAG — retrieval is a deterministic hashed structural index\" (§12) — survives: asserted directly in these bytes, not just name-guessable. Strong.\n- Candidate 4: anchor semantics (§6): structural-unit anchors via raw-byte BLAKE3 subtree hash, no normalization, line numbers only hints, reformat-of-cited-unit drifts fact as an *accepted* trade — asserted directly here, qualitative (no counts), highly non-obvious from title/path, and materially changes how an implementer authors/stores citations and what failure mode they must expect. Survives.\n- Candidate 5: advisory→edit-in-place vs predicate→mint-and-supersede (§7/§14) — also survives, but Candidate 4 packs more surprising, action-altering specificity (deliberately accepted false alarms).\n- Refute Candidate 4 once more: any reliance on unseen files? The parenthetical cites spec §3.1 for elaboration, but every operative clause (structural unit, subtree hash, raw bytes/no normalization, hint-only line numbers, accepted reformat false alarm, BLAKE3/Merkle dual use) is stated within this unit itself. No exact counts or exhaustive lists. Not a restatement of the title. Confirmed.\n\nSurvivor: Candidate 4.\n\n```atlas-fact\n{\"claim\": \"Fact citations anchor to a structural unit (symbol, block, or file) identified by a raw-byte BLAKE3 hash of its subtree with no normalization step — line numbers are kept only as navigation hints — and a pure reformat inside the cited unit deliberately drifts the fact to BROKEN, an accepted false-alarm trade rather than a gap.\"}\n```"