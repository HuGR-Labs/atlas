"STEP 1 — Reasoning (scratch):\n\nScanning candidates:\n\n1. *Header/comment claims* — goldens SCN-KERNEL-5a-1/5b-1, WP numbers, held-out `-2` fixtures, EPIC-3-b collisions: all assert things about files/tasks outside these bytes. Discard.\n\n2. *`ev` conditional spread* — the omission of `nodeKey` when `node` is undefined is visible in the code, but it's essentially a restatement of the literal right there; marginal value-add.\n\n3. *Serializer sorting* — visible, but plainly stated by the code and its own adjacent comment; close to a summary of what it does.\n\n4. *`roundTripFromEmpty` bridging mechanism* — the function takes a plain `EventLog` and hands it to `exportCas` only via `log as unknown as Cas`, with a second single cast (`obj as Event`) coming back on import. There is no construction, conversion, or validation of an actual `Cas` anywhere in this unit — the type bridge is purely the double cast. This is fully checkable from these bytes (the function body is four statements), is invisible from the name/signature (`roundTripFromEmpty(log: EventLog): EventLog` suggests a normal pipeline), and materially changes what a maintainer does: refactors to `Cas` or `EventLog` shapes will not fail compilation in this test, so the round-trip seam is runtime-only checked. Survives refutation.\n\n5. *Property-test bucket math (`k % 3`, max 3 nodes)* — visible but arithmetic restatement of the generator literals; low value.\n\nEmitting candidate 4.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"Inside roundTripFromEmpty, the EventLog reaches the export/import portability layer solely through a forced double cast (`log as unknown as Cas`, with imported objects cast back with `obj as Event`) — no actual Cas is ever constructed or validated in this unit, so type mismatches across that seam would not fail this test at compile time.\"}\n```"