"STEP 1 — Scratch reasoning.\n\nReading the bytes: the fixture helper `ev` constructs every `Event` used in the file, and its returned object literal hardcodes `seq: 0`, `fresh: true`, and `supersedes: []`. Scanning all construction sites: `g, h, i, w, x` at module scope and `m1, m2, m3` inside the REQ-PERSIST-5 describe block — eight call sites, all routed through `ev`. No other expression in the file produces an `Event` literal (everything else wraps them in `set(...)`, `new Map()`, or passes them to imported functions).\n\nDraft candidate: the supersede-related tests run on events that carry no actual supersedes link.\n\nRefutation pass:\n- Derivable from these bytes? Yes — the hardcoded `supersedes: []` is in the helper's body, and exhaustiveness was checked site by site (8 of 8, verified individually).\n- Resting on a comment about unseen code? No — the inline comments even claim \"m1 retained despite being superseded,\" but the *data* contradiction is visible right here: nothing in the fixtures encodes a supersedes pointer. The fact is grounded in declarations, not in the comment.\n- Exact-count risk? The exhaustive form (\"every event\") was verified against each call site; alternatively phrased qualitatively it still holds since `ev` is demonstrably the sole constructor.\n- More than name/signature restatement? The helper's signature `(id, node, ch, payload): Event` gives no hint that supersession linkage is unrepresentable; discovering it requires reading the body and cross-referencing the 5b′ scenario. It would change reviewer behavior: anyone extending or auditing REQ-PERSIST-5 coverage would learn that `archive`'s supersede semantics are exercised only on link-less events, so any logic keyed off `Event.supersedes` is untested by this file.\n\nSurvives. Emitting.\n\n```atlas-fact\n{\"claim\": \"Every event fixture in this file is built by the shared ev helper, which hardcodes supersedes: [] (and seq: 0), so the REQ-PERSIST-5 'supersede archives both' scenario exercises events that carry no actual supersedes link in their data.\"}\n```"