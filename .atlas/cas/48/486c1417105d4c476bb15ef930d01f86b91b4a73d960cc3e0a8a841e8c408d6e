"Looking at this unit: it's a work-package manifest for CAMPAIGN-6, fourteen WP cards across epics 18–25. Candidate facts to test:\n\n1. *The off-atlas threshold θ is DEFINE-supplied, parametric in SCN-RETR-13b-1* — present in bytes (WP-6.18.RETR exclusions), actionable, but arguably a restatement of that card's own exclusion line; an engineer reading the card knows it.\n\n2. *Caps use the pinned cl100k_base measure* — visible in `action_surface`/anchors, but scattered and semi-decorative; doesn't change scheduling or design decisions much.\n\n3. *Dependency topology* — checking each `deps:` line against its own `epic:`: WP-6.18.KNOW `[ ]`, WP-6.18.RETR `[WP-6.18.KNOW]`, WP-6.18.GEN `[WP-6.18.KNOW]`, WP-6.19.RETR `[ ]`, WP-6.19.TOOLS `[WP-6.19.RETR]`, WP-6.20.RETR `[ ]`, WP-6.21.RETR `[ ]`, WP-6.22.RETR `[WP-6.18.RETR]` ← cross-epic, WP-6.22.TOOLS `[WP-6.22.RETR]`, and all four MEM cards `[ ]`. Every other intra-campaign edge stays inside one epic; exactly one card points outside its own epic. Verified card-by-card against the bytes — this is structural data (deps fields), not comment prose, and not the non-authoritative `intent` carve-out. Refutation attempt: could this be inferred from names alone? No — nothing in \"WP-6.22.RETR · EPIC-22\" reveals it is blocked by an EPIC-18 card, and sibling RETR slices (19/20/21) are explicitly `[P]`/empty-deps, so assuming epics schedule independently fails here. Does it rest on claims about unseen files? No — the deps line and the exclusions note (\"the per-kind hitRate ledger itself is RETR-8 (WP-6.18.RETR, same module)\") live in this unit. Survives.\n\n```atlas-fact\n{\"claim\": \"Within this campaign file, WP-6.22.RETR is the only work package whose deps reference a card from a different epic — it lists WP-6.18.RETR, serializing EPIC-22's injection-ceiling/drop-by-hit-rate work behind the EPIC-18 hitRate-ledger slice, unlike the EPIC-19/20/21 RETR slices which carry empty deps and parallel_group [P].\"}\n```"