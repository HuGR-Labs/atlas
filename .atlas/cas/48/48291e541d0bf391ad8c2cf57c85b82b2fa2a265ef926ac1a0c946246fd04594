"STEP 1 — scratch reasoning (not stored):\n\nCandidates drafted from a close read:\n\n1. *Header comment says ground() drops dangling citations* — refuted as a fact-source: that's comment prose, and worse, it's contradicted by the unit's own assertions (see #2). Discard as stated.\n2. *ground() is fail-closed at the FACT, not the entry*: the first test feeds a fact with one resolvable citation (`QP_ARR` = `billing.ts#computeArr`, present in the `src` fixture) and one absent (`deleted.ts#gone`), then asserts `expect(g.entries).toHaveLength(0)`, `isGrounded(g) === false`, `driftDetect === 'DRIFTED'`; the CONTROL in the same test adds the missing node and gets 2 entries with FRESH verdict. This differential is fully encoded in the unit's own literals and assertions — no reliance on unseen `src/ground.ts` or on the ⚠️ comment's claims about elsewhere. Not derivable from the name/signature (`ground(f, src)` naively suggests per-citation filtering, which the top-of-file comment even describes). Survives.\n3. *Kind pass-through ('block' rides through)* — true and checkable (test 2), but lower consequence; also arguably closer to what an attentive reader might infer from \"re-derived anchor\" language. Superseded by #2 in impact.\n4. *Fuzz totality over 10k cases / survivors all equal QP_ARR* — checkable, but either numeric-exhaustive or a restatement of the fixture (src has exactly one leaf). Weaker than #2.\n\n#2 survives refutation: it is checkable by reading this unit's own arrange-act-assert code, it contradicts the plausible reading (including the file's own stale header prose), and it changes caller behavior (no partial receipts — any stale citation voids the fact's grounding entirely).\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"In this suite, ground() is fail-closed at the whole-fact level: a fact holding one resolvable citation alongside one unresolvable one grounds to zero entries (reading not-grounded/DRIFTED), while the same fact with both cited paths present re-derives two anchored entries — so no partial receipt is ever handed back.\"}\n```"