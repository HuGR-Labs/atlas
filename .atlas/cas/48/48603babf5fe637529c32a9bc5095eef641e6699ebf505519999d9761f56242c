"STEP 1 — REASONING (scratch)\n\nCandidates considered:\n\n1. *\"The header comment says the two predicates are byte-identical\"* — rests entirely on a comment's claims about `src/ratify/fastpath.ts` and `@atlas/grounding/drift.ts`, files not shown. Discard.\n2. *\"The fix landed in commit 56f0440 / issues #203/#204\"* — provenance claims about unseen history. Discard.\n3. *\"The coercion table has 11 rows, three of them positive-`.length` non-string carriers\"* — countable from the literal, but numeric and low-leverage; the labels make it self-evident to any reader. Weak.\n4. *\"The printed-table test never calls the imported `localIsGrounded`\"* — true from control flow (first `it` asserts only the restated helpers and `sealedObserved`), mildly useful but mostly a coverage nitpick.\n5. *\"The divergence-array pushes are dead once the per-row `toBe(expected)` asserts pass\"* — derivable, but a meta-observation about test redundancy rather than a fact about the system under test.\n6. **Empty-grounding semantics**: the suite explicitly constructs `{ entries: [] }` and asserts BOTH implementations return `false`. This is checkable directly in the bytes (literal construction + two `toBe(false)` assertions). It is not knowable from the file's name/signature (\"parity test\"), and it contradicts the naive implementation an engineer would write — a bare `entries.every(check)` returns vacuously `true` on `[]`, so the pinned behavior forces a length guard or equivalent. This changes what a reimplementer or reviewer does. Survives refutation: no reliance on external files, no comment-trust, no exact-count claim; the substantive evidence is the assertions themselves, not the test title.\n\nSurvivor: candidate 6, phrased around the in-file assertions.\n\nSTEP 2 — RESULT\n\n```atlas-fact\n{\"claim\": \"This suite pins both isGrounded implementations to return false for a grounding with an empty entries array ({ entries: [] }), so the gate fails closed on emptiness rather than exhibiting the vacuously-true behavior a bare entries.every(...) check would give.\"}\n```"