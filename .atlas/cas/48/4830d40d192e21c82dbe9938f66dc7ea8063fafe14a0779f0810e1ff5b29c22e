"```atlas-fact\n{\"claim\": \"Machine-checked formal specifications (FSPECs) are awarded to exactly one cluster — the CRDT OR-Set merge + supersedes cluster spanning KERNEL-9/10/11 and PERSIST-11, modeled as FSPEC-merge and keyed by cluster name rather than module id because the cluster spans two modules — while every other behavioural invariant instead receives only a lighter method-tag (exhaustive enumeration, PBT, or reference-model), so an engineer executing S2 must not author formal models beyond this single core.\"}\n```"