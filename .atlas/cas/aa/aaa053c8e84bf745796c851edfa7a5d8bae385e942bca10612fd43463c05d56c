"STEP 1 — scratch reasoning:\n\nCandidates drafted from close reading:\n\n1. *The `node` fixture helper mints each `subtreeHash` from the template string `${axis}:${key}`.* — Checked against bytes: yes, `subtreeHash: asSubtreeHash(`${axis}:${key}`)` appears in the helper body. Not a comment claim (it's executable code), no counts, no reference to unseen files. Nothing in the file name, describe blocks, or helper signature reveals how the hash is filled; an engineer could wrongly assume realistic content-derived hashes. Survives.\n\n2. *The mint-leg property calls `build` with `{ documents: [] } as unknown as ScipOutput`.* — Visible in bytes; survives refutation, but it's more of a test-harness stub detail than something that redirects action, and its significance (\"no SCIP objects attached\") leans partly on inferring `build`'s contract, which isn't shown. Weaker than #1.\n\n3. *The mint-leg walk skips the root by guarding `n.key !== '.'`.* — Directly in bytes, but it's essentially a restatement of one visible guard line; low surprise value once the loop is read.\n\n4. *Synthetic `L${i}` level naming in `chainForest`.* — True but marginal; doesn't change what anyone does.\n\n#1 is concrete, mechanically checkable solely within this unit, non-enumerative, and goes beyond name/signature knowledge: it tells a reader that hash equality in these fixtures encodes only (axis, key), never content — which would change how they write or audit assertions touching `subtreeHash`.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"The shared `node` fixture helper fills every IndexNode's subtreeHash by passing the deterministic template string `${axis}:${key}` to asSubtreeHash, so the hashes in these fixtures encode only axis and key, never file content.\"}\n```"