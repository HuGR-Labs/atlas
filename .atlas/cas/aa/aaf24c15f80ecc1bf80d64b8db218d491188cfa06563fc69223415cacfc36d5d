"Looking at the bytes: in `makeCase`'s run loop, for `t > 0` a glue separator is drawn — either `''` or one of `'_'`/`'-'` — but the append is guarded by `glue !== '' && pieceSafeAfter(openFamily(b.text), glue)`. Checking `pieceSafeAfter`: it returns `!isBodyChar(fam, piece, 0)`, and `-` is a body-extra char of the slack family while `_` is a body-extra char of the github-pat family. So when the still-open family would absorb the glue, the append simply doesn't happen — nothing replaces it — and `emitToken` then writes the next token straight onto the previous body. Refuting passes: this is pure control flow in this unit (no reliance on other files or comment claims), it's qualitative (no counts/enumerations), and it is not derivable from `makeCase`'s name/signature, which says nothing about separator elision. Survives.\n\n```atlas-fact\n{\"claim\": \"In makeCase, a drawn non-empty glue separator ('_' or '-') is silently skipped whenever pieceSafeAfter reports the still-open family's body would absorb its first character, so tokens inside a run can end up directly adjacent even on steps where a separator was chosen.\"}\n```"