"STEP 1 — Reasoning (scratch):\n\nReading the bytes: the unit consists of exactly two top-level items — a type-only `import type { Tier } from './tier.js';` and one exported interface `Territory` with four readonly members (`name: string`, `owner: string`, `tier: Tier`, `globs: readonly string[]`). No aliases, classes, functions, or runtime values exist in the file.\n\nCandidate 1: \"Declaration order of territories is significant for overlap tiebreak.\" — Refuted: this is a semantic rule asserted only in a comment, not encoded in any declaration, control flow, or type in these bytes. Discarded.\n\nCandidate 2: \"`tier` is human-ratified; `owner` MAY be generated (INDEX-15).\" — Refuted: rests entirely on the comment, including a reference to an external task number and governance process invisible here. Discarded.\n\nCandidate 3: \"`owner: seat` and `globs: Glob[]` are nominal strings owned elsewhere.\" — Refuted in its full form: the claim that `seat`/`Glob` exist and are \"owned elsewhere\" is a comment's assertion about modules not in these bytes. Discarded as stated.\n\nSurvivor (structural core of Candidate 3): By enumerating every declaration in the shown bytes, the file defines no `seat` or `Glob` types of its own and exports exactly one thing — the `Territory` interface — leaving `owner` and `globs` as bare `string`/`readonly string[]` with no nominal typing available from this module. This is checkable directly from the declarations (verified: only the type-only import and the one interface exist), goes beyond what the interface's name alone conveys, and changes behavior: an engineer looking for branded/reference types or runtime validation in this module will not find them here. Survives refutation attempts.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"Territory is this module's sole declaration and export — no `seat` or `Glob` types are defined anywhere in the file — so `owner` and `globs` carry no nominal type safety beyond their bare `string` forms.\"}\n```"