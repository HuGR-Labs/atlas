"```atlas-fact\n{\"claim\": \"Although the adversarial describe is titled as requiring the door to abstain on every dynamic channel, neither of its it-blocks ever invokes the governed emit door — they assert only on buildDynamicReach output, checking that returned witness strings end with each row's expected suffix (or, in the regression-guard row, that zero scope-dynamic witnesses exist over the repo).\"}\n```"