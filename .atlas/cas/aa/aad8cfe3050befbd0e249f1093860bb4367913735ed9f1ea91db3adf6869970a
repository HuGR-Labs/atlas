"STEP 1 — Reasoning (scratch):\n\nReading the bytes: `createDriftSource` builds a `DriftSource` whose `driftAt` loops over facts, and within each fact loops over `f.grounding.entries`, calling `entryDrift` per entry.\n\nDraft candidates and refutation passes:\n\n1. *Tags peel to commits via `^{commit}`* — the literal is here, but its meaning rests on external git semantics not visible in these bytes. Discard.\n2. *Leading `-` revs are refused without consulting git* — true and code-visible (`rev.startsWith('-')` throw before `runGit`), but largely restates the guard's own inline text; marginal value over the code shape. Weak.\n3. *Rename detection silently vanishes when `resolveBySubtreeAt` is omitted* — derivable from the optional dep and `?.` call, but the optionality is already in the deps type an engineer reads alongside the signature. Weaker than a pure behavioral surprise.\n4. *An entry intact at its own path short-circuits before the subtree-content lookup, so a byte-identical duplicate elsewhere cannot fabricate a move* — derivable from control-flow order, but close to restating the guard comment adjacent to it. Moderate.\n5. **Per-fact pair cap**: inside the entry loop, the first truthy `entryDrift` result pushes exactly one pair and then executes `break` — so remaining entries of that fact are never even evaluated. Verified against the loop body: there is no other `push`, and the `break` is unconditional after a hit. The signature `driftAt(mergeBase): readonly DriftPair[]` gives no hint that a fact with several rotted citations reports only its first drifted one; an engineer assuming exhaustive per-entry reporting (or writing a test asserting multiple pairs per fact) would act differently. Survives all refutation checks: derived from these bytes' control flow, no reliance on comments about other files, qualitative not numeric, not a name/signature restatement.\n\nSurvivor: candidate 5.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"Within driftAt, each fact contributes at most one DriftPair: the entry loop breaks immediately after the first drifted grounding entry, so any additional drifted entries of that same fact are never evaluated or reported.\"}\n```"