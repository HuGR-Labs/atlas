"STEP 1 — Reasoning (scratch):\n\nThe unit is an ADR, so almost everything in it is prose pointing at code that is not shown (`governed-emit.ts`, `tier.ts`, test files, scenario IDs, task numbers). Candidates that lean on those pointers die immediately: \"PROP-SAMEAS-1 ran 5000 times,\" \"SCN-GE-I5 failed on master,\" \"task #87/#88,\" \"router.ts moved,\" \"16 link cases stay green\" — all assert things about artifacts outside these bytes.\n\nWhat survives must be the document's own recorded content, readable right here:\n\n- Candidate A (nodeKey excludes scope/tier): present, backed by the literal formula in Context. Real, but partially inferable from the title's framing plus the formula; moderately good.\n- Candidate F (refuse downgrade rather than re-gate): present, but \"ratchet up freely\" is close to what the title already implies; weaker surprise.\n- Candidate S (any distinct refusal string is an oracle when bytes are gone): present and counterintuitive, but it is reasoning woven through an amendment narrative; more compound.\n- Candidate C (lattice totality/asymmetry): §1b states it nearly verbatim — \"an unrecognized DECLARED class is always weaker, an unrecognized INCUMBENT always strictest, and a join with garbage is T0.\" Nothing in the name or status hints at asymmetric unknown-handling; a competent engineer would naively assume uniform unknown→weakest or uniform fail-to-T0, which behaves differently. It is qualitative (no counts to miscount), it is fully contained in these bytes as the decision itself rather than as hearsay about another file, and it directly constrains how anyone implements or tests the lattice.\n\nRefutation pass on C: not a restatement of the title; not dependent on task IDs, test results, or other files' behavior; every clause appears in the shown text; no enumeration risk. Survives.\n\nSTEP 2 — Emit:\n\n```atlas-fact\n{\"claim\": \"The tier lattice this decision introduces is total over unknown values with asymmetric fail-closed semantics: an unrecognized declared class is always treated as weaker, an unrecognized incumbent class is always treated as strictest, and any join involving an unrecognized value collapses to T0.\"}\n```"