"STEP 1 — REASONING (scratch):\n\nReading the bytes closely:\n\n- **Candidate A: \"classify always returns tier 'T2'\"** — visible directly: the single return statement emits the literal `'T2'` with no branching. Survives refutation (code confirms, not just comment). But it is arguably \"what the code plainly does\" once read, and it is heavily advertised in the adjacent docs.\n- **Candidate B: segment-exact keyword matching in `isT0Candidate`** — the body splits on `'/'`, lowercases, drops empty segments, and uses `includes(seg)` (exact equality against `T0_KEYWORDS`). So `auth/x` flags, but `oauth/`, `auth-plugin/`, or `payments-v2/` do NOT — no substring/prefix matching. This is NOT derivable from the name `isT0Candidate(path: string): boolean`, rests entirely on these bytes (split/filter/includes/toLowerCase all present), makes no claims about other files or counts of the corpus, and materially changes integration behavior (silently missed flags for qualified directory names). Survives refutation.\n- **Candidate C: tierRank inversion / unknown→TIER_COUNT** — checkable (`TIER_COUNT - 1 - STRICTNESS[v]`, else `TIER_COUNT`), but more minor and closer to arithmetic restatement.\n- **Discarded:** everything resting on comments about other modules (init.ts shape, four modules rebuilding the order, L0/L4 placement, ARCHITECTURE.md claims, task #84, KNOW-* tags, method-tags-knw:64) — facts about unseen code, not re-derivable here.\n\nCandidate B survives and is the strongest: behavioral, surprising from name+signature, purely byte-local.\n\nSTEP 2 — RESULT:\n\n```atlas-fact\n{\"claim\": \"isT0Candidate flags a path only when one of its non-empty lowercased slash-separated segments exactly equals a T0_KEYWORDS entry, so names that merely contain a keyword (e.g. oauth, auth-plugin, payments-v2) do not flag.\"}\n```"