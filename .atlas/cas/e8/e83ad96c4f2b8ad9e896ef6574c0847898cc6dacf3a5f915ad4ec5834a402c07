"STEP 1 — Reasoning (scratch):\n\nReading the bytes: this is a small tsconfig extending a base config we cannot see, overriding only `outDir`, with `include: [\"src\", \"test\"]` and project references to `../contracts` and `../kernel`.\n\nCandidate A: \"Tests are emitted into dist.\" — Refuted: whether anything is emitted, and where, depends on `tsconfig.base.json` settings (e.g., `noEmit`, `emitDeclarationOnly`) not visible in these bytes. Discard.\n\nCandidate B: \"This package depends on contracts and kernel.\" — Survives literally (`references` array), but it borders on restating what the file plainly lists; the dependency-direction reading is useful yet weaker as a surprise.\n\nCandidate C: \"The test directory is pulled into the same compilation program as src.\" — Checkable directly against `\"include\": [\"src\", \"test\"]`: whatever runs this config compiles/type-checks `test` together with `src`, independent of any unseen base-config settings. Not derivable from the file's name; changes behavior (adding tests outside `src/test` silently misses checking; test-file type errors surface during package builds). Not an exhaustive count, no reliance on comments or external files. Survives.\n\nEmitting Candidate C.\n\n```atlas-fact\n{\"claim\": \"This package's include list pulls the test directory into the same TypeScript compilation program as src, so test files are type-checked together with production code rather than excluded from the build.\"}\n```"