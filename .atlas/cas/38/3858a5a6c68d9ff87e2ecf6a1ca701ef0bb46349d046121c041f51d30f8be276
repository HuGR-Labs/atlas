"STEP 1 — scratch reasoning:\n\nCandidates considered and refuted/survived:\n\n1. *“Promote is idempotent”* — the test title itself states this; restates the name. Discard.\n2. *“CAS address differs from nodeKey”* — heavily explained by the test's own inline commentary; an engineer reading the test body gets it immediately, and the “why” rests on comments about other modules. Weak actionability. Discard.\n3. *“Actor and ratify token are the same literal string”* (`CURATOR === TOKEN === 'seat:orchestrator'`) — derivable, mildly interesting, but its significance depends on how `ratify.ts` compares tokens, which is outside these bytes. Discard.\n4. *“Mine seeds zero under this fixture”* — the *reason* lives entirely in narrative comments about seams in files not shown (and the comment itself admits the reason changed between revisions); only the expected output literal is current, and the fact mostly restates the test title (“stages NOTHING”). Discard.\n5. **Survivor**: test 7 probes the CLI with `['nosuchcommand']` and asserts `exitCode === 1` plus `help.stdout.toContain('init|query|emit|reconcile|doctor|mine|node|link|promote')`. Nothing in the unit's path or name suggests this promote story pins the CLI's verb vocabulary — yet editing/adding any top-level verb forces a change here. Verifiable purely from the shown assertions: I counted the pipe-delimited literal item by item (init, query, emit, reconcile, doctor, mine, node, link, promote = nine) and confirmed the usage fragment is expected on **stdout** of a failing (exit-1) invocation, not stderr. I deliberately do not claim the literal enumerates the *entire* surface (`toContain` can't prove absence), only that this exact sequence is pinned here.\n\nRefutation pass: no reliance on comments about other files (GOVERNANCE_SURFACE/WRITE_PATHS remarks ignored); no unverified totals; not a restatement of the file name or test signatures; actionable (verb renames/additions break this file; consumers learn the error shape).\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"This promote-focused test also pins part of the CLI's command vocabulary: invoking the binary with an unknown command is asserted to exit 1 and print, on stdout, a usage fragment listing the nine verbs 'init|query|emit|reconcile|doctor|mine|node|link|promote', so adding or renaming any of those top-level commands requires editing this file.\"}\n```"