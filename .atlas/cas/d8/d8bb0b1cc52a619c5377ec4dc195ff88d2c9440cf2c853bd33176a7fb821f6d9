"**STEP 1 — scratch refutation pass**\n\nCandidates considered against the bytes:\n\n1. *\"Merge driver must self-install because .git/config doesn't clone\"* — true per PERSIST-11, but partially leans on generic git convention knowledge; less surprising to a competent engineer.\n2. *\"Notes are a mutable overlay; trailers are the only clone-present carrier\"* (PERSIST-13) — largely derivable from known git semantics (notes not fetched/pushed by default, keyed on SHA).\n3. *\"Re-invoke is redispatch + replay, never deterministic resume\"* (PERSIST-10b) — stated, but the headline rule is fairly prominent in the doc's framing.\n4. **Scrub scope gap (PERSIST-10a)** — the doc's rule sounds absolute (\"no raw credential may enter\"), but the SHIPPED-SCOPE paragraph narrows it drastically: counting the declared families item-by-item — GitHub tokens, Slack tokens, GitHub fine-grained PATs, AWS access key ids — gives exactly four, matching \"**Four**\"; and the same paragraph explicitly states JWTs, PEM private keys, and the AWS SECRET access key are NOT redacted and pass through unchanged. Fully enumerated and checkable within this unit's own literals (`ghp_…`, `xoxb-…`, `github_pat_…`, `AKIA…`); does not depend on the pointer to `packages/persist/src/scrub-shapes.ts`. Not derivable from the unit's name/title; directly action-changing (anyone consuming stored transcripts cannot assume those secret shapes were scrubbed at source). Survives.\n\nCandidate 4 survives; emit.\n\n```atlas-fact\n{\"claim\": \"Despite the blanket no-credential rule, the shipped redact-at-source control declares only four credential families — GitHub tokens, Slack tokens, GitHub fine-grained PATs, and AWS access key IDs — so JWTs, PEM private keys, and the AWS secret access key are explicitly not redacted and pass into the transcript buffer unchanged, leaving only the scanner backstop to catch them.\"}\n```"