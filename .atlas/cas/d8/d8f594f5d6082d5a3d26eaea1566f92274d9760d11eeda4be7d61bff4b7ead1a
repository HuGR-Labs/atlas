"STEP 1 — REASONING (scratch):\n\nCandidates considered:\n\n1. *\"The suite reproduces lost-update statistics (W=8, K=1000).\"* — Refuted: those numbers appear only in the header COMMENT describing past measurements and external reports (`adapter-io/test/sidecar.test.ts`, seat report); the actual tests use W=4/K=50 and one trial. Comment-based, partially about elsewhere. Discard.\n2. *\"The identity stamp is required or the #112 guard refuses writes.\"* — The causal claim rests entirely on a comment citing issue #112 and describing code not in these bytes. Only the presence of `identity: IDENTITY_SCHEMA` in `seed()` is code-visible, and that is near-restatement of what the code plainly writes. Weak. Discard as stated.\n3. *\"LEG 2 leaves the store byte-identical and creates no numbered generations.\"* — True and visible (`toBe(torn)`, `.toEqual([])`), but essentially a summary of the test's plainly written assertions, matching its name (\"leaves the store exactly as it found it\"). Discard.\n4. *\"300_000 ms timeouts.\"* — Visible but trivial; wouldn't change what anyone does. Discard.\n5. **`durable()` control-flow quirk** — Read the loop carefully:\n   ```js\n   try {\n     const w = JSON.parse(readFileSync(file, 'utf8'));\n     if (Array.isArray(w.current)) return { file, nodes: w.current.length };\n   } catch {\n     return { file, nodes: 'UNPARSEABLE' };\n   }\n   ```\n   - Existing file that throws in `JSON.parse` → `catch` → **`return` from the whole function**: the loop never reaches older generations or the `projection.json` mirror.\n   - File that parses but whose `current` is not an array → no return, falls through → loop **continues** to the next candidate.\n   \n   This asymmetry lives entirely in THESE bytes, is not inferable from the name `durable` or its signature, and contradicts the natural reading of the adjacent prose (\"the highest readable generation, then its predecessor\"). It would change what an engineer does: anyone simulating a torn *top* generation, or reasoning about what `durable(repo).nodes` yields in LEG 1's oracle (`toBe(K + accepted)`), must know the helper reports `'UNPARSEABLE'` instead of degrading to the mirror once any existing candidate is unparseable. Survives refutation: checkable purely from declarations/control flow here, qualitative (no counts), not a name/signature restatement, not dependent on any comment's claim about unseen code.\n\nEmitting candidate 5.\n\n```atlas-fact\n{\"claim\": \"In the durable() helper, an existing candidate file that fails JSON.parse terminates the entire search by immediately returning nodes: 'UNPARSEABLE' rather than falling back to older projections.N.json generations or the projection.json mirror, whereas a candidate that parses but whose current is not an array is skipped and the loop continues.\"}\n```"