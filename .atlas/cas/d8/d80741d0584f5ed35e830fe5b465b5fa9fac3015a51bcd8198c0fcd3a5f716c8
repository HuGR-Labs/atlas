"STEP 1 — Scratch reasoning:\n\nReading the gate order in `emitNegation`: malformed → global-target → deps presence → `resolveCurrent` → scope/closure branch (opaque/escape/dynamic/resolves per branch) → **gate (c)** `callersInScope.length > 0 ⇒ return REJECTED_NEGATION_REFUTED` → address minting → **gate 2 AUTHZ** (`actorInScope`) → anchor authz → commit.\n\nCandidate A: \"edgeModel is optional and stamps `''`.\" Survives byte-check (`deps.edgeModel ?? ''`; the undefined-guard covers only `axes`/`symbolReverse`/`nodeHashOfPath`), but it's fairly close to the documented §3 clause-4 story and less action-changing.\n\nCandidate B: \"Seal is stripped unless `origin === 'promoted'`.\" Confirmed by the destructure + spread ternary, but heavily telegraphed by its own comment block.\n\nCandidate C: the refutation decision happens *before* authorization. Byte-check: gate (c)'s early `return { emitted:false, rejected: REJECTED_NEGATION_REFUTED }` sits above the `// 2. AUTHZ` section where `actorInScope(deps.policy, deps.actor, authzScope)` runs; likewise every `writeAbstention(...)` call fires before any authority check. So an actor with no authority over S still receives a *decisive* verdict distinguishing \"refuted (a caller exists)\" from various abstention reasons — an unauthorized party can learn caller-existence in a scope they don't own. Not stated anywhere in the comments (they discuss authz *binding*, not *ordering*); derived purely from control-flow order in these bytes. No counts, no reliance on other files. Refutation attempts: is it just restating gate numbering? No — nothing in the name/signature suggests verdicts precede authz; and it materially changes review priorities (authz placement). Survives.\n\nSTEP 2 — Emit:\n\n```atlas-fact\n{\"claim\": \"The refutation verdict (a caller exists in the scope) is returned before the AUTHZ gate runs, so an actor with no authority over the scope still receives a definitive decision — including learning that a caller exists — since every refutation and abstention path executes ahead of the actorInScope check.\"}\n```"