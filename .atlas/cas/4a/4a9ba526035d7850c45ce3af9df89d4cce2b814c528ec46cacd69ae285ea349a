"STEP 1 — REASONING\n\nCandidates considered:\n\n1. *\"Empty-string provenance is conflated with absent provenance.\"* Survives initial check — `prov === '' ? undefined : prov` plus `if (provenance === undefined) continue` are in-unit code. But it's fairly close to the `PERSIST-14-c` comment's stated intent; weaker novelty.\n\n2. *\"The superseder scan checks every B-side entry, not just the active head.\"* Survives — the loop iterates all `nodeB.entries.values()`. Valid, but a competent engineer scanning `supersederOf` would plausibly notice this quickly.\n\n3. *\"Decayed entries can be sourced wholly from state A.\"* Reading the decayed branch closely: `signal = supersederOf(nodeB, headA) ?? headA`. When the node is entirely absent from B, `supersederOf` returns `undefined` at its first line (`if (nodeB === undefined) return undefined`), so the nullish coalescing falls back to `headA` itself — meaning `fact = headA` AND `provenance = provOf(headA)`. The entire emitted decayed entry derives from the A-side event. Notably, the function's own doc comment claims provenance for a decay is \"the archiving B-entry's `prov`\", which the fallback contradicts whenever no B-side superseder exists. Refutation attempts: does it rest on a comment's claim about unseen code? No — the fallback is literal control flow in this unit. Any exact counts or exhaustive enumerations? None. Restatement of name/signature? No — the signature `partition(a, b): VersionDelta` reveals nothing about provenance sourcing. It changes what an engineer does: they cannot assume diff output reflects B-side signals, and a decayed record appears even when B knows nothing of the node, provided headA carries recoverable provenance. Survives.\n\nSTEP 2 — RESULT\n\n```atlas-fact\n{\"claim\": \"When an A-head decays with no B-side entry superseding it — including when the node is absent from state B entirely — the decayed entry's provenance is recovered from the A-head event itself via the `supersederOf(nodeB, headA) ?? headA` fallback, so the surfaced record can be sourced wholly from state A despite the header claiming decay provenance comes from a B-side entry.\"}\n```"