"STEP 1 — scratch reasoning (not persisted):\n\nScanning the bodies rather than the comments:\n\n- `deriveSameAs` builds `const retracted = retractedPairs(projection)` and contains `if (retracted.has(pairKey(key, peer))) continue;` — withdrawn edges stop merging there. Verifiable in these bytes.\n- `sameAsClassOf`'s fixed-point loop reads only `node.sameAs ?? []`; the string `sameAsRetracted` appears nowhere in its body, and no retracted-set filtering exists in it. Verifiable in these bytes.\n- Therefore, for a projection containing a recorded retraction, the two exported folds genuinely disagree: `sameAsClassOf` still walks the withdrawn edge and widens the class. This is not inferable from either signature (`(projection) => readonly SameAs[]` vs `(projection, key) => readonly string[]`) or the names.\n- Refutation checks: the claim rests only on the two function bodies in this file — not on the comments' assertions about `governed-link.ts`, task numbers, or `PROP-SAMEAS-1`. No exact counts involved. It is not a restatement of a name/signature, and it is action-changing (choose the wrong fold and withdrawn equivalences read as live).\n- Other candidates considered and set aside: the length-prefix `pairKey` format (plainly visible literal), the min-root deterministic union rule (visible one-liner), total `find` over off-domain keys (visible guard), and `sameAsClassOf` returning non-current peers (also true in the bytes, but the retraction asymmetry is the sharper, consequence-bearing divergence). The retraction-blindness fact survives.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"Although both exported functions fold the same stored relation, `sameAsClassOf` never consults `sameAsRetracted` — unlike `deriveSameAs`, which filters its unions through `retractedPairs` — so an edge whose retraction is recorded still transitively widens the class `sameAsClassOf` returns.\"}\n```"