"STEP 1 — scratch reasoning: I scanned the header prose against the per-card data. The header asserts \"the one WP that touches `governed-emit.ts`\" is WP-10.A3.ADAPTER. But WP-10.A4.ADAPTER's own card lists `anchor: packages/adapter-io/src/governed-emit.ts` and `action_surface: [..., edit(packages/adapter-io/src/governed-emit.ts), ...]` — so two WPs, not one, declare edits to that file. This compares only bytes inside this unit (no external file, PR, or history needed), survives refutation (I re-checked both action_surface lines; A2-b.ADAPTER touches only adapter-io/test, A1.ADAPTER only the directory glob), is qualitative rather than a large unverified count, and is invisible from the unit's name/lede while directly changing review scope (A4's governed-file edit is not covered by the \"behaviour-preserving refactor, reviewed as such\" framing the header attaches to A3). Other candidates (A1.TOOLS blocking all WPs; the 6-vs-5 seam-count discrepancy) were considered; the first is already announced in bold in the lede, the second is a weaker numeric quibble. The governed-file contradiction survives best.\n\nSTEP 2:\n\n```atlas-fact\n{\"claim\": \"Although the campaign header names WP-10.A3.ADAPTER as the one WP that touches governed-emit.ts, the WP-10.A4.ADAPTER card's own action_surface also specifies edit(packages/adapter-io/src/governed-emit.ts), so two work packages declare edits to the governed write-path file.\"}\n```"