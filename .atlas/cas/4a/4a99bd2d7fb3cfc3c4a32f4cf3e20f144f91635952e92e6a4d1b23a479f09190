"STEP 1 — Reasoning scratch:\n\nReading `makeAwarenessMemo` → `sourceKey`: the private helper builds the cache key from `entry.grounding[0].subtreeHash` — only the *first* grounding anchor — falling back to `id(entry)` when the anchor list is empty.\n\nDraft fact: \"The memo's per-facet cache/drift key is derived from only the first grounding anchor's subtree hash, not the whole anchor set, with a fallback to the kernel id of the entire entry payload when grounding is empty.\"\n\nRefutation pass:\n- Derivable from these bytes? Yes — `sourceKey` is fully implemented here; no external knowledge needed about `@atlas/kernel` or `@atlas/contracts` beyond the types being strings/comparable.\n- Rests on a comment's claim about unseen code? No — although the doc comment vaguely says \"keyed on ITS OWN source subtree hash,\" the specific behavior (first-anchor-only + `id(entry)` fallback) is established by the code body itself, going beyond the comment.\n- Exact count / exhaustive enumeration? No numeric claim; the `grounding[0]` index is directly verified in the bytes.\n- More than a restatement of name/signature/types? Yes — `sourceKey` is module-private, invisible from any export's signature; nothing else reveals that later anchors are irrelevant to invalidation.\n- Consequence check: appending or mutating anchors after `grounding[0]` cannot trigger a re-roll or a `drifted` flag — confirmed by control flow, since the key comparison is the sole cache-miss condition. Survives.\n\nSTEP 2 — Emit result:\n\n```atlas-fact\n{\"claim\": \"The memoized assembler's per-facet cache key comes solely from the facet's FIRST grounding anchor's subtreeHash (falling back to the kernel id of the entire entry payload when grounding is empty), so changes to any later grounding anchor neither invalidate the cache nor mark the facet drifted.\"}\n```"