"```atlas-fact\n{\"claim\": \"Within this module the branded SubtreeHash is minted by passing encoder.hash's output straight through asSubtreeHash with no validation whatsoever — bindSubtree's body is a single expression with no length, encoding, or algorithm check — so nothing in this file constrains the digest's shape, and an injected stub encoder returning arbitrary strings would still satisfy the drift-oracle type.\"}\n```"