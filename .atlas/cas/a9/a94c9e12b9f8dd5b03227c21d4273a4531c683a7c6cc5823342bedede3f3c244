"STEP 1 — Scratch reasoning (not persisted):\n\nCandidates drafted from close reading:\n\n- *\"Existence is folded into the `unauthorized` refusal\"* — implemented in the visible `||` chain (absent endpoint returns `REJECTED_UNAUTHORIZED`), but the file's own header proclaims this loudly; low surprise value once the unit is open.\n- *\"Pair-state gate deliberately runs last\"* — code order confirms it, but it is the header's headline claim; essentially a summary of documented intent.\n- *\"Row-scope fallback only fires when `scope` is absent, not malformed\"* — visible in `rowAuthorized` (`node.scope === undefined ? fact?.scope : node.scope` plus `isScope` guard). Real, but the adjacent comment narrates it line-for-line.\n- *\"Header claims nine refusal points but ten distinct reason strings exist\"* — I counted ten literals (SAME, UNAUTHORIZED, UNVERIFIABLE, UNRATIFIED, three retract reasons, CONTENDED, UNTRUSTED, UNREADABLE); however \"points\" vs \"strings\" is an interpretive gap, and the count-history is comment narrative. Risky enumeration — discard.\n- **Survivor:** the ratify gate's input. The code builds `linkClass` from `strictestTier` over the endpoints plus every class member's stored-fact tier, then calls `stage({ tier: linkClass } as unknown as Candidate)`. The double-cast proves the object is *not* a real candidate shape — only `tier` is supplied. No key, scope, or content ever reaches `ratify` at this door. This appears nowhere in the header prose (which describes the law, not the fabricated argument), is checkable purely from these bytes, and is actionable: anyone reasoning about what influences ratification here, or evolving `Candidate`, must know the sole determinant is tier.\n\nRefutation pass: could `stage` inject the missing fields? Irrelevant — the claim concerns what *this unit* passes in, which is visibly only `{ tier }`. Does it rest on another file? No — the cast and the reduction are in the shown bytes. Exact-count risk? None. Restatement of name/signature? No — signature suggests a normal ratified write; it hides that the \"candidate\" is synthetic.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"The ratify gate feeds `ratify` a synthesized object carrying only the merged class's strictest tier (`stage({ tier: linkClass } as unknown as Candidate)`), so no node key, scope, or content participates in the KNOW-8 decision at this door — tier is the sole input.\"}\n```"