"Looking at the bytes closely: most assertions here (honest-empty on cyclic/NaN input, idempotent re-append, first-write-wins) are behavioral checks you might partially guess from a file named \"heldout-krn-store.test.ts\". What stands out as genuinely non-obvious is buried inside the otherwise black-box GATE 3b-2: alongside round-trip checks, it opens `../src/store.ts` as a UTF-8 string and asserts `(src.match(/new Map\\b/g) ?? []).length` is exactly `1`. Trying to refute: is this derivable from these bytes? Yes — the `readFileSync`, the regex literal, and the `.toBe(1)` are all right here; I'm not trusting the header comment's claims about blinding processes or `docs/requirements/goldens-krn.md`. Is it a restatement of the name/signature? No — nothing in the filename suggests a source-text grep. Is the exact count admissible? Yes — the count is the point of the assertion and is literally present. One derived consequence worth noting: because it's raw-text matching, even the phrase \"new Map\" appearing in a comment or string in `store.ts` would trip it. This changes what a competent engineer does — refactoring `store.ts` (adding a cache index, splitting stores, or mentioning the pattern in prose) silently breaks this gate for reasons unrelated to runtime behavior.\n\n```atlas-fact\n{\"claim\": \"GATE SCN-KERNEL-3b-2 embeds a white-box structural check that reads ../src/store.ts as raw text and fails unless the regex /new Map\\\\b/g matches exactly once, so any second Map instantiation — or even the phrase 'new Map' appearing in a comment or string in that file — would break this held-out test regardless of runtime behavior.\"}\n```"