"STEP 1 — scratch reasoning (not persisted):\n\nReading the bytes closely: the three exported modes all delegate to the private `dereferenceSorted`. Its body shows the real contract behind the bare `readonly Fact[]` return types: hashes are deduped through a `Set` first, then `uniq.sort()` is called with **no comparator** (default lexicographic string sort on the CAS hash), and only then are surviving hashes mapped through the store. So the array order the caller receives in *all three* modes is determined purely by the hash strings' lexicographic order — not by scope-chain depth (file→module→crate), not by the stored order of `triggers` or `blastRadius` lists, not by relevance. Nothing after `dereferenceSorted` reorders the result, so this survives refutation: it is fully checkable in this unit's own control flow, not from any comment's claim about other files, and it is not implied by the names or signatures (`byScope(path): readonly Fact[]` gives no hint that hierarchy position is discarded in the output ordering). It also implies a second checkable consequence: because dedup happens on hashes before dereferencing, a fact referenced from multiple nodes of a scope chain still appears exactly once. Draft refutations considered and discarded: the \"TOTAL, miss ⇒ empty\" behavior is largely advertised by the docstrings themselves; the `typeof` runtime guards are real but weaker (plausibly guessed); the INDEX-6/7/8 annotations and atlas-index line references concern other artifacts not visible here, so I lean only on the code body. The hash-ordering fact survives.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"All three retrieval modes return their facts ordered solely by a lexicographic sort of their content-addressed hash strings (after hash-based dedup inside the shared helper), so result order encodes no scope depth, dependency distance, or tag-list order, and identical queries always yield identical arrays.\"}\n```"