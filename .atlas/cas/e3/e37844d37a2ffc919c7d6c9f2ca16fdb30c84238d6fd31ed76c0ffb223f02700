"Scratch reasoning (never stored):\n\nCandidate 1 — \"abstention maps to `ABSTAIN_SENTINEL` in llm.ts / 0-in-300 measurement / block-count admission rejects ≥2.\" Refuted: every one of those rests on the HTML comment asserting things about files and measurements not present in these bytes. Out of bounds.\n\nCandidate 2 — \"this is the enriched sibling of propose.md and the related-context section is its only addition.\" Refuted: requires comparing against a sibling file I cannot see; the comment's claim, not the unit's bytes.\n\nCandidate 3 — \"the `<unit path=\\\"{{PATH}}\\\"…>` wrapper is never closed before the instructions, nesting them inside the target.\" Refuted: the paste boundary makes tag ownership ambiguous (the wrapper around the whole unit also needs a closer), so asserting this risks a false structural claim.\n\nSurvivor — the operative instruction text itself stipulates the full output contract: exactly one fenced `atlas-fact` block containing a JSON object with a single `claim` field, no other fence anywhere (keeping scratch reasoning unparsable), and abstention expressed as either no block or a bare `NO-FACT` line — a positively specified abstention token, not silence. None of this depends on comments, siblings, or unseen code; the name alone reveals none of it, and a parser/gate implementer must handle both accepted abstention forms and the fence-free constraint.\n\n```atlas-fact\n{\"claim\": \"Abstention is specified as a positive output token rather than silence: the prompt demands exactly one fenced atlas-fact block holding a JSON object with only a claim field, forbids any fenced block in the scratch reasoning, and accepts a missed fact as either no block at all or a bare NO-FACT on its own line.\"}\n```"