"STEP 1 — scratch reasoning (not persisted):\n\nCandidates drafted from close reading:\n\n1. *Guard is structural, not `instanceof`* — supported by the cast-and-compare line, but the adjacent comment already announces this loudly and frames it as intentional; it's the documented headline behavior of the exported helper, so it's closer to \"plainly does.\"\n2. *Non-string slots rendered by kind with vowel-aware article* — derivable from the `kind`/`shown` lines, but again the comment narrates it, and it's a small formatting detail unlikely to change actions.\n3. *Prose/count desync hazard* — the return template interpolates the vocabulary **dynamically** (`PREDICATE_SLOTS.map(...).join(', ')`) yet hard-codes the size in three places in the surrounding prose: \"not one of the closed 13\", \"under one of the 13\", \"adding a fourteenth\". Nothing in these bytes ties those numerals to the array's length — the array isn't even visible here, so the numeral is pure frozen text riding next to a live list. Verified each occurrence against the template literal. No comment flags this; the comments instead praise listing \"the vocabulary in full\". Survives refutation: fully checkable in this unit, non-obvious from the signature (`(slot: unknown) => string` tells you nothing about frozen numerals), and action-changing (widening the vocabulary silently breaks the message's internal consistency).\n4. *Door-name hard-coded in text* — discarded; judging whether \"atlas-emit / atlas-link\" can be the wrong label depends on raise sites in files not shown.\n5. *Counts of vocabulary membership* — discarded; `PREDICATE_SLOTS` is imported, not shown, so any claim about its length can't be re-derived here.\n\nSurvivor: candidate 3.\n\nSTEP 2 — emit:\n\n```atlas-fact\n{\"claim\": \"The refusal message renders the vocabulary list dynamically from the imported PREDICATE_SLOTS array, yet hard-codes the size in its own prose ('the closed 13', 'one of the 13', 'adding a fourteenth'), so nothing in this unit keeps the stated count in sync with the list it is printed beside.\"}\n```"