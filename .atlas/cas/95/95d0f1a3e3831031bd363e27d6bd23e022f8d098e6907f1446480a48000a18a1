"STEP 1 — scratch reasoning (not persisted):\n\nThe unit is an ADR: prose, not executable code, so nearly every load-bearing sentence describes behavior of files *not* shown (`governed-emit-negation.ts`, `symbol-reverse.ts`, probes, the SCIP indexer). Candidates drafted and refuted:\n\n1. *\"Gate admits iff four conditions (resolves, ¬escape, no-dynamic-reach, no reverse callers).\"* — Restatement of the decision framework the title already signals; also a spec of unseen code. Discard.\n2. *\"Pure-type targets skip the escape/dynamic legs.\"* — Real and non-obvious, but it is a normative claim about the door's implementation elsewhere; nothing in these bytes lets me re-check it beyond trusting the prose. Weak survival.\n3. *\"Both-or-neither wiring of `targetEscapes`/`dynamicReach`, else byte-identical blanket fallback.\"* — Internally stated, mechanical, no numbers. Survives, but it is the design contract of unseen code and closer to what the \"WIRED\" headings advertise.\n4. Numeric results (559/609 holes, admit=0/refute=2078/abstain=59900, 86.2 % ceiling, 37.4 %, 80.9 %, 18 654 admits…). All are measurements of a corpus/index absent from the bytes; I cannot recount or re-verify them item-by-item here. As standalone facts they fail the verification standard.\n5. **The CORRECTION block (2026-08-17)**: the ADR's own text later states that its headline \"0 FALSE-ADMITS over 18 654 admits, recall 80.9 %\" was measured on a DIST-form index and *did not transfer* — on a fresh LOCAL-form build the door false-admitted ~80 % of tsc-false negations until a further fix; post-fix, 0-false-admit holds across builds while recall swings with the build recipe (~32.5 % operating, ~4.25 % misbuild; fix = build declarations before indexing). This is checkable entirely within the unit (the correction is in the bytes, amending the earlier sections), is invisible from the title (\"RATIFIED … escape + dynload\"), and decisively changes what an engineer does: don't cite the 80.9 % figure; treat recall as build-pinned; run `tsc -b` before `scip-typescript index`. External referents (#178/#196b, oracle scripts) can be stripped without loss — the supersession and its consequence are asserted in this document itself. Survivor.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"A correction inside this ADR supersedes its own headline benchmark: the reported 0-false-admit over 18,654 admits at 80.9% recall held only for a dist-form index build (on a fresh local-form build the door false-admitted), so after the follow-up fix 0-false-admit holds across builds while recall is build-recipe-dependent (~32.5% operating, ~4.25% misbuild) and is recovered by building declarations before running scip-typescript index.\"}\n```"