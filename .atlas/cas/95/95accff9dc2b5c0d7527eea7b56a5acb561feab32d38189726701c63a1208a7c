"STEP 1 — REASON FREELY\n\nScanning the bytes for candidates:\n\n- Ladder order, grants-required=0, PUSH_TIER, spawn contract: all restated by test names/comments/assertions — discard as known-from-surface.\n- \"Byte-identity spans the full verdict (incl. guidance)\": derivable (JSON.stringify applied to verdicts; 10b-1 shows guidance shape), but modest novelty/actionability.\n- SCN-TOOLS-10c-1 wiring: the store-backed resolver literal is `resolve: (addr) => (addr === A ? (store.read(g.key)?.value ...) : undefined)` with `g = { key: id(N1 as CasObject), ... }`. So the address matched is `A` ('cas:9b21'), but the storage key actually consulted is the separately computed `id(N1)` — an indirection living entirely in this closure. Refutation attempts: (a) does it rest on a comment? No — the lambda and `g` are visible code; in fact the adjacent past-tense comment narrates the *opposite* (that resolve returned undefined each iteration), which cannot be concluded from the shown wiring since the read outcome depends on unseen store semantics — so I must not assert either runtime outcome, only the mapping. (b) Do I need string-inequality of `A` vs `id(N1)`? I will not claim the strings differ (comment-dependent); I claim only the mechanical fact: match on `A`, read at `g.key`. (c) Exact counts/exhaustive lists? None needed. (d) Restatement of name/signature? The test name says only \"write refused / no write door\" — nothing reveals this key indirection. (e) Would it change what they do? Yes: whoever audits whether the write-door teeth actually bite, edits the fixtures, or trusts the misleading inline comment needs to know that what reaches `resolveNode` at that address is the governed-store read at `id(N1)`, not the static fixture.\n\nThis survives. Emitting the code-only phrasing.\n\nSTEP 2 — EMIT THE RESULT\n\n```atlas-fact\n{\"claim\": \"In the write-refusal scenario (SCN-TOOLS-10c-1), the handler's NodeSource is wired so the node address 'cas:9b21' resolves by reading the governed store at the separately computed key id(N1), meaning what resolveNode is offered at that address comes from a store round-trip bridged inside this test's closure rather than from the static N1 fixture.\"}\n```"