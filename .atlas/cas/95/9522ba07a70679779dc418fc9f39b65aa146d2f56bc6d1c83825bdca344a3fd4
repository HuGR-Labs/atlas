"STEP 1 — scratch reasoning (not persisted):\n\nCandidates drafted from close reading:\n\n1. *\"definers are not their own callers\"* — test (a) pins that `src/def.ts`, which holds a `definition` occurrence for GREET, is absent from `reverseCallers(GREET)`. Derivable from fixture + assertion, survives refutation. But fairly inferable from \"reverse-caller\" semantics; weaker novelty.\n\n2. *\"`resolves` splits two empty-caller cases\"* — NEVER (defined, unreferenced) and UNKNOWN (referenced, undefined) both have `reverseCallers(...) === []` (tests e, c), yet `resolves(NEVER)` is `true` and `resolves(UNKNOWN)` is `false` (test f). Fully checkable in these four assertions; survives. Strong, but mostly a property of two API methods jointly.\n\n3. *Cross-package dist→src re-keying inside `createSymbolReverse`* — the fixture feeds raw dist-descriptor symbol strings (`dist/src/\\`build.d.ts\\`/nodeHashOfPath.` NESTED, `dist/\\`build.d.ts\\`/nodeHashOfPath.` FLAT) straight into `ScipOutput`; nothing else transforms them before `createSymbolReverse(crossPkgScip)`. Yet (g) asserts both referencing docs appear under `reverseCallers(SRC_DEF)` — the src-form key. So the dist→src mapping demonstrably happens inside the function under test. Meanwhile (h) pins the negative arm: the ghost dist-form (`ghost.d.ts`) whose src-form has no definition occurrence anywhere in the fixture leaves `packages/tools/src/x.ts` in `holeSources()`, yields `[]` callers for the ghost src-form string, and `resolves` false — fail-closed, no fabricated edge. Verified item-by-item against assertions (g) and (h); no reliance on comment claims about #189/#220, the negation door, or measured package counts — all stripped. Survives.\n\nRefutation sweep: candidate 3 is not a restatement of name/signature (the name says nothing about identity rewriting across published-vs-source descriptors), is grounded entirely in fixture literals and executable assertions, and would change what a consumer does (query src-form keys even when SCIP records dist-form refs; treat uncanonizable refs as holes, never edges). Chosen over candidates 1–2 for consequence and specificity.\n\n```atlas-fact\n{\"claim\": \"Inside createSymbolReverse, a cross-package reference recorded against a dist .d.ts descriptor is bucketed as a caller under the matching src-form symbol for both the nested dist/src/ and flat dist/ layouts, whereas a dist-form reference whose src-form lacks any in-index definition fails closed: the referencing document stays a hole source and no caller is fabricated for the ghost src-form symbol.\"}\n```"