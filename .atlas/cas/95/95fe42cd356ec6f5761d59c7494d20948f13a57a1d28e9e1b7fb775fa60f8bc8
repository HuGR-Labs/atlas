"STEP 1 — Reasoning:\n\n- Candidate A: \"The project has exactly one runtime dependency.\" Counting `dependencies`: yes, only `@noble/hashes`. But this is essentially a restatement of a plainly visible two-line object — low value.\n- Candidate B: \"Ten guard scripts enforce repo gates.\" Requires counting and enumerating harness files that aren't shown; the gates themselves live outside these bytes. Discard — it asserts things about invisible modules.\n- Candidate C: \"The description says 'no embeddings, no RAG'.\" True of the bytes, but it's self-descriptive metadata, not actionable.\n- Candidate D: The `postinstall` key maps to `node scripts/fix-scip-types.mjs`. This means running `npm install` in this repo automatically executes that local script — an implicit, easily-missed side effect of a routine action. It rests on nothing outside these bytes (the key–value pair is right here; no comment, no cross-file claim), and it would genuinely change an engineer's behavior (they'd inspect or account for that script before/after installs, when debugging environments or auditing supply chain).\n- Refutation pass on D: Am I leaning on unstated npm conventions? Only in the same way one interprets `tsc -b` meaning a TypeScript build — the lifecycle semantics of `postinstall` are universal tooling knowledge, and the operative fact (the hook is wired to this specific script) is fully contained in the shown JSON. No counts, no exhaustive lists, no reliance on other files' contents. Survives.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"Installing this package automatically executes the repo-local script scripts/fix-scip-types.mjs, because a postinstall lifecycle hook is wired to it in the root package.json.\"}\n```"