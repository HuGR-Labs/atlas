"STEP 1 — Reasoning scratch:\n\n- Candidates considered:\n  1. \"Abstain + grounded admits an advisory with no seal and undefined label, preserving claimNorm verbatim\" — mostly restates the test's own name (\"JUSTIFIED advisory, unsealed\"); the claimNorm bit is real but lower-value.\n  2. \"Malformed target/scope drops happen even when verifyDependency returns 'proven'\" — visible in the bytes, but largely implied by the branch-order narrative and the test setup.\n  3. \"Unwired drops carry the reason marker 'GEN-12-dep'\" — the literal `expect(a.reason).toContain('GEN-12-dep')` appears in the code itself (not only the header comment), it is not guessable from the file name, test name ('dropped (unwired)'), or any signature, and anyone matching on drop reasons or writing downstream assertions would need this exact string.\n- Refutation pass on candidate 3: it is checkable purely from these bytes (the expect line in the unwired test); it does not lean on a comment's claim about another file; it is not an exhaustive enumeration; it is not a restatement of the name. Survives.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"When verifyDependency is absent from AdmitDeps, admit() drops the dependency proposal with a reason string containing the literal marker 'GEN-12-dep', as pinned by this test.\"}\n```"