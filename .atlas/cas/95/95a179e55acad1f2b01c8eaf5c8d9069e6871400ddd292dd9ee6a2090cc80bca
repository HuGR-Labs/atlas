"STEP 1 — scratch reasoning (candidates drafted and refuted):\n\n- *Tag assignments* (PBT on TOOLS-3/TOOLLS-10, rest reference-model): plainly declared in `method-tag:` lines and restated in the completion report's own histogram — a summary of what the unit plainly says. Discarded.\n- *Amendment inside INV-TOOLS-6*: real and in-bytes, but it is prominently displayed in the section itself, and most of its substance is claims about other artifacts (ADR-0013, reference/atlas-tools.md) — weaker than an internal structural observation. Held as backup, discarded.\n- *Completion report vs. body*: the report literally asserts `refusal count: **9**`. Counting the `## Refuse-to-model` section bullet by bullet in these bytes: read-only projections/write path; security-exploitability; KNOW-5 classifier; FSPEC-merge core; ~2K budget; performance/latency; transport wire; content-address primitive; the code itself; concurrent+crashing writes — that is **ten** bulleted refusal items. Recounted item by item: still ten. The report's own number does not match the enumeration directly above it. This is fully internal to the unit (no reliance on other files, PRs, or past-tense claims), not derivable from the file's name, and materially changes what an engineer does (they cannot trust the report's ledger without reconciling it). Survives.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"The completion report records 'refusal count: 9', but the Refuse-to-model section directly above it enumerates ten bulleted refusal items, so the report's tally does not match the register body it summarizes.\"}\n```"