"STEP 1 — scratch reasoning (not persisted):\n\nCandidates drafted and tested against the bytes:\n\n1. *\"Memoized by scip object identity via a module-level WeakMap\"* — true and code-visible, but it sits in the first four lines of the function body; low surprise value. Survives but weak.\n2. *\"Unknown paths yield empty/null, never throw\"* — the empty-map outcome is visible, but totality leans on `nodeHashOfPath` never throwing, which is not shown here. Refuted (external dependency).\n3. *\"Sorted array rebuilt on every `definitionsFor` call\"* — visible (`[...].sort()` per call), but micro-perf trivia; low actionability. Discarded as too thin.\n4. *\"Earliest document in `scip.documents` wins for multi-document definitions\"* — derivable from the `!defDoc.has` guard, but such duplicates are a defensive edge; low practical impact. Discarded.\n5. *\"Collision tie-break is the lexicographically smallest symbol string\"* — verified against the line `if (prev === undefined || symbol < prev) byName.set(n, symbol)`. Crucially, this is iteration-order-independent, whereas `defDoc`, built earlier in the same function via `!defDoc.has(...)`, is first-insertion-wins. A reader pattern-matching on \"first definition wins\" (the rule the file itself announces for `defDoc`) would mispredict which exact symbol a picked name binds to when a unit defines multiple same-named globals (e.g., overloads) — and that binding flows into the admitted fact. Fully checkable in this unit's own code, no counts, no reliance on comments about other files. Survives.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"When a unit defines several distinct global symbols that share one terminal name, resolveDefFor binds that name to the lexicographically smallest symbol string (the `symbol < prev` comparison), an iteration-order-independent tie-break that differs from the first-definition-wins rule used to build defDoc in the same function.\"}\n```"