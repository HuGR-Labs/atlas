"Looking at the bytes: the imports tell me this file tests `faultOf` / `malformedArgsReason` / `missingRequiredReason` and the handler door — nothing surprising there. Digging into the bodies for something the signature doesn't reveal:\n\n- **Candidate A — the self-tag channel**: the `tagged` helper literally does `throw Object.assign(new Error(...), { atlasFault: kind })`, and the sibling test throws a *bare object* `{ atlasFault: 'refused', ... }` versus an untagged `{ message: ... }`, with `faultOf` returning `'refused'` vs `'internal-fault'` respectively. Mechanism (a structural `atlasFault` property overriding inference) is fully visible in code literals and assertions — not in the name or imports. The \"instanceof breaks across duplicate modules\" rationale is comment-only, so I drop the *why* and keep the *what*.\n- **Candidate B — off-surface totality**: `createHandler({ ['atlas-delete' as Tool]: okLeg })` + `{anything: 1}` reaching the leg with `out.ok === true`. Also visible, and actionable. But its interest partly rests on the comment's claim about `SCHEMA_OFF_SURFACE` in handler.ts (unseen); the surviving core is thinner than Candidate A.\n- **Candidate C — NaN fails the `number` predicate**, and **D — unknown types like `'geojson'` pass through unchecked**: both visible, but more like spec-table rows of the validator than a behavior-changing surprise.\n- **Candidate E — validation precedes the leg** (`expect(ran).toBe(0)`): visible, but fairly inferable from \"validator + door\" framing.\n- Refuted/discarded: header-comment claims about mutant sweeps, incident files, past bugs (\"was accepted and routed\", \"used to be declared\") — all past-tense/comment claims about unseen code; the typo'd key `acceptRegound` is a fixture artifact, not a fact.\n\nRefutation pass on A: derivable purely from these bytes' literals and expectations? Yes — the `Object.assign` construction, the bare-object throw, and both `faultOf` outcomes. Rests on no cross-file claim once the instanceof remark is stripped. No exact counts. Not a restatement of name/signature — nothing about `faultOf(ToolData)` suggests thrown values carry an overriding tag. Survives.\n\n```atlas-fact\n{\"claim\": \"A leg can declare its own fault class by throwing any value that carries an `atlasFault` property — this structural tag overrides the handler's inference, so a non-Error object tagged 'refused' stays a refusal while the same object untagged is classified 'internal-fault'.\"}\n```"