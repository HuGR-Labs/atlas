"STEP 1 — REASON FREELY\n\nCandidates drafted from a close read:\n\n1. *\"`messages` is emitted unsorted despite the header's canonical-sort claim\"* — verifiable (no `.sort` on the `messages` line; `owners`/`coChanged` do sort), but its punch comes from contradicting the file-header comment, and the inline comment already discloses it. Weaker novelty.\n\n2. *\"The `FIX_SUBJECT` regex `/^fix/i` has no delimiter, so `szzBugCommits` counts any subject starting with 'fix' (e.g. 'fixtures')\"* — derivable from the literal, but it largely restates what the regex plainly encodes once read.\n\n3. *\"`shallow` ignores its rev argument and falls back to `true` on git failure\"* — fully in-bytes (`void _r`, fallback literal `'true'`, in-file catch wrapper), but moderate action impact.\n\n4. *\"`blameConcentration` runs one `git blame` per tracked file\"* — REFUTATION PASS: is this visible in these bytes? Yes: the memo body loops `for (const f of files)` where `files = trackedAt(repo, r)` (full `ls-tree -r` listing), and the loop body calls `git(repo, ['blame','--line-porcelain', r, '--', f])` — one invocation per file, unconditional. Not dependent on any comment about elsewhere; the call count is internal truth (I avoid claiming \"subprocess,\" since `runGit`'s internals aren't shown — \"invocation\" suffices). Not derivable from the signature `blameConcentration(repo, r): number`, which suggests a single query. Not a type/name restatement. Qualitative, no counts to miscount. It survives: it changes operational expectations (cost scales linearly with tracked-file count on the first uncached call), which is exactly the kind of thing a signature hides.\n\nEmitting candidate 4.\n\nSTEP 2 — EMIT RESULT\n\n```atlas-fact\n{\"claim\": \"blameConcentration loops over the entire tracked-file listing at the rev and invokes git blame --line-porcelain separately for each file, so an uncached call costs one git invocation per tracked file rather than one overall.\"}\n```"