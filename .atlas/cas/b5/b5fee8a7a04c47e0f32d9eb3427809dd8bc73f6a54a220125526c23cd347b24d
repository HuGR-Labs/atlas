"STEP 1 — Reasoning (scratch):\n\nReading the bytes closely:\n\n- `bin` maps the command name `atlas` to `./dist/src/bin.js`.\n- `exports` contains exactly one key, `\".\"`, whose conditions point at `index.js` / `index.d.ts`.\n- Cross-checking: the string `./dist/src/bin.js` appears nowhere inside `exports`; the map exposes nothing but the root specifier. Verified item-by-item — the `exports` object has exactly one key, and the bin target is not among its values.\n\nCandidate facts considered and refuted:\n\n1. \"`*` ranges mean workspace resolution\" — refuted: that inference relies on package-manager/workspace conventions not visible in these bytes; no `workspace:` protocol is declared here.\n2. \"Seven dependencies, all wildcarded\" — technically countable, but the numeric form adds little and the actionable meaning (how resolution behaves) leans on external tooling knowledge. Discarded as weaker than the alternative.\n3. \"`build` and `typecheck` are identical scripts\" — true in the bytes, but largely a restatement of adjacent literals with marginal impact on what an engineer does.\n4. \"Private package with full publish-style metadata (main/types/bin/exports)\" — a summary of plainly visible fields, not a synthesized insight.\n\nSurvivor: the structural mismatch between the declared binary target and the exports map. It requires joining two separate fields of this same file, is fully checkable against these bytes alone (no comments exist here, and none are relied upon), uses a qualitative \"only\" whose exhaustiveness I verified (single `exports` key), and changes what an engineer does: programmatic deep imports of the bin module will not resolve through the package's public surface, and any fix means editing this map.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"The `atlas` binary entry point (`./dist/src/bin.js`) is absent from the package's `exports` map, which defines only the root specifier `.`.\"}\n```"