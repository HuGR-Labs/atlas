"STEP 1 — scratch reasoning (not persisted):\n\nCandidates drafted:\n1. *Deterministic sort by (relationKind, endpointA, endpointB, nodeKey)* — true and code-visible in the final `.sort(...)` comparator, but it is also announced verbatim in the function's own doc block, making it low-information; weaker candidate.\n2. *Silent skip of malformed rows / empty-key guard* — visible via `continue` and the early return, but largely restates the documented totality stance; marginal novelty.\n3. **Seal key-absence**: the push uses `...(typeof node.seal === 'string' ? { seal: node.seal } : {})`. If the row's `seal` is absent or non-string, the spread contributes nothing, so the constructed object simply has no `seal` property at all — not `seal: undefined`. The declared type `seal?: string` is compatible with *both* conventions, so the signature cannot tell you which; only this code idiom can. Refutation attempts: (a) is it derivable from these bytes? Yes — the conditional-spread expression is the sole construction site of `RelationEdge` in the unit; (b) does it rest on a comment's claim about unseen code? No — the idiom itself establishes the behavior; the parenthetical comment merely concurs; (c) numeric/exhaustive claim? None; (d) restatement of name/signature/types? No — `seal?: string` leaves the question open. It survives, and it changes what an engineer does: membership tests like `'seal' in edge`, exhaustive key checks, or exact-optional-property-type assignments behave differently than if the key were present with value `undefined`.\n\nSTEP 2 — emit:\n\n```atlas-fact\n{\"claim\": \"When a relation row's `seal` is absent or not a string, the emitted edge object omits the `seal` key entirely (via conditional spread) rather than setting it to `undefined`, so key presence—not value—distinguishes sealed from unsealed edges.\"}\n```"