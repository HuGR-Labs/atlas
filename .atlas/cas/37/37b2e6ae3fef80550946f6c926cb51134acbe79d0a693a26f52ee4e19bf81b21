"Looking at this unit: a requirements markdown for the knowledge block, heavy with amendments. Most candidate facts fall to the refutation pass:\n\n- The KNOW‑11a amendment's claims (\"nothing supplies `owner`\", \"`authz()` has zero production callers\") are measurements of code in `adapter-io` and elsewhere — not re-derivable at these bytes. Discard.\n- The runtime-gap closure cites `atlas promote` and `requirements-adapters.md#REQ-CLI-7a` — claims about other files. Discard.\n- The whitespace/reformat rationale leans on TS/TSX semantics and a measurement chain (`foldAstUnits → build → driftDetect`) not shown. Discard as primary fact.\n- The \"[NEEDS RECONCILIATION] has two items\" is a count I verified (INV‑KNOW‑15, INV‑KNOW‑10) but is low-actionability.\n\nWhat survives is stated *constitutively* in this unit's own amendment text: the drift mechanism for a symbol rename is anchored to a key format literally spelled out here — `<parent>::<kind>:<ordinal>[:<name>]`. Because the name is part of the key, a rename doesn't just nudge the hash; it retires the key outright, the fact fails closed, and it never re-binds automatically — re-grounding after a rename is declared to be an author action, with no rename-tracking. This contradicts the naive expectation that drift = hash movement (or that anchoring is rename-tolerant, as KNOW‑15f's \"move-aware\" phrasing might suggest), and it changes what an implementer builds: no automatic rename rebinding.\n\nChecked against the bytes: the anchor-key format, the retirement consequence, the fail-closed behavior, and the author-action/no-rename-tracking statements are all present verbatim in the AMENDED 2026-08-02 blockquote. No exact counts, no exhaustive enumerations beyond the clause's own wording, no reliance on unseen code.\n\n```atlas-fact\n{\"claim\": \"Renaming the cited symbol drifts a fact by a stronger mechanism than a hash move: the anchor key format <parent>::<kind>:<ordinal>[:<name>] embeds the symbol's name, so a rename retires the key entirely, the fact fails closed and never re-binds to the renamed unit, and re-grounding after a rename is a manual author action because there is no rename-tracking.\"}\n```"