"```atlas-fact\n{\"claim\": \"In all three traversals (`propagateDirty`, `rehashState`, and free function `eagerRehashState`), the seed/edited node is placed into the visited set up front and is never itself processed, so it never receives a dirty bit, never appears in the eager re-hash list, and remains unresolved by its own `rehashState` call — only its dependents (within the hop bound) are affected, meaning `isDirty(node)` and `rStateResolved(node)` still return false for that exact node immediately after the calls.\"}\n```"