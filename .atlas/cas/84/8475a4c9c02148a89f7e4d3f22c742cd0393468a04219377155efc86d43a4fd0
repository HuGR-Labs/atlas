"STEP 1 — scratch reasoning (not persisted):\n\nCandidates drafted from close reading:\n1. \"Assertions avoid golden digests\" — sourced mainly from the header comment; the code only shows comparisons against locally computed baselines plus one pinned literal. Comment-flavored, partially code-visible, weak actionability.\n2. \"`append` returns the log (fluent), inferred because `unionLog` returns `log.append(...)` and `roundTripFromEmpty` assigns `out = replay.append(obj)`\" — the return-type usage is visible, but concluding anything stronger (mutation semantics) requires assuming the tests pass, which isn't in the bytes. Discarded as too inferential.\n3. \"All events carry `seq: 0`\" — visible literal, but marginal actionability.\n4. **Dead local `rev`:** in the 5a-2 order-independence block, `const rev = createLog()` is populated with reversed events and then never referenced again; the log actually folded and compared is `revLog`, built separately through `r2`. Verified by scanning every occurrence of the identifier: declared once, appended-to once, never read. No reliance on comments, external files, or counts beyond this one identifier's usage. Survives refutation.\n5. Pinned literal `'ch-e2-zzz'` in 5b-2 — code-visible but low surprise.\n\nCandidate 4 survives: it is pure control-flow/liveness, invisible from the file name or any signature, and materially affects how a reader understands (or safely refactors) the order-independence assertion — the reversed-input log they see built first is not the one under test.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"In the 5a-2 order-independence assertion, the log named rev is created and filled with the reversed events but never used again; the log actually folded and compared against B0 is revLog, built independently through a second logger r2, so the reversed input sequence is constructed twice.\"}\n```"