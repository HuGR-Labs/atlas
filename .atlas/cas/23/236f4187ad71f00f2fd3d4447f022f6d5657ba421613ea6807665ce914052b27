"STEP 1 — Reasoning (scratch):\n\n- Candidate: \"Every other atlas package imports this and nothing else\" — sourced from a comment asserting something about code outside these bytes. Discard.\n- Candidate: \"Zero logic lives here / types only\" — the comment asserts it; I can confirm there are no local declarations, but I cannot confirm the sibling modules contain only types from these bytes. Discard the stronger version.\n- Candidate: \"Seven modules are re-exported\" — I counted the lines (hash, struct, tier, status, territory, pack, tool = 7) and it checks out, but it's essentially a restatement of the file's visible contents and wouldn't change what an engineer does. Prefer qualitative. Discard.\n- Candidate: \"Importing this barrel evaluates all seven modules\" — depends on ESM evaluation semantics and bundler behavior beyond the shown bytes. Risky. Discard.\n- Survivor: Inspecting the code lines themselves — all re-export statements are unfiltered wildcards (`export *`); there are zero selective/named re-exports (`export { … } from …`) and no `export type … from` anywhere. So the barrel applies no allow-list or renaming at this layer: whatever a sibling exports flows straight into the package surface. That is checkable purely against these bytes and changes what an engineer does (e.g., to keep something internal, they must not export it from a sibling — this file offers no filter).\n\nSTEP 2 — Emit result:\n\n```atlas-fact\n{\"claim\": \"No re-export in this barrel is selective: every statement is an unfiltered wildcard (`export *`), so anything exported by a listed sibling module becomes part of the package's public surface with no allow-list or renaming applied at this layer.\"}\n```"