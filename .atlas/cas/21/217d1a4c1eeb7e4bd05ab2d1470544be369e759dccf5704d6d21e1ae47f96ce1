"STEP 1 — REASON FREELY\n\nReading the bytes closely.\n\nCandidate drafts and refutation attempts:\n\n1. *\"The gate fixture's `gateHolds` ignores its third `_src` parameter.\"* — True in the bytes, but the header comment already says \"`src` is inert here,\" so this mostly restates a comment plus a visible `_`. Discard as too close to restatement.\n\n2. *\"Only the zero-entries rejection test asserts `receipt.id` is undefined; the empty-hash test omits it.\"* — I counted: the KNOW-2 block has four `it`s; test 1 asserts `id` undefined, test 2 (empty hash) asserts only `emitted:false` and `persisted` length. Verified, but the utility is thin (test-coverage trivia). Weak; set aside.\n\n3. *\"The receipt `id` is persist's return value passed through unchanged.\"* — The contrast test shows `receipt.id === asHash('cas-1')` matching what the fake `persist` returns, but that is a single data point; generalizing \"unchanged pass-through\" goes beyond one assertion. Discard as overreach.\n\n4. *\"The SCN-KNOW-1-1 downgrade is driven purely by staleness — the node's grounding fully passes `isGrounded`.\"* — Trace it: `predicateDeclaring('HOLDS', grounded)` uses the `grounded` grounding, whose single entry has `subtreeHash 'st-77'`, so the `isGrounded` fixture returns true; `mkGate(false)` supplies `fresh:false`; the gate therefore computes `true && false ? 'HOLDS' : 'NA'` → `'NA'`. Fully derivable from these bytes. Survives, but the in-test comment already gestures at \"NOT fresh.\"\n\n5. Refinement of 4 — the sharper, less-restated layer: the node fixture `predicateDeclaring` sets **both** `status:'HOLDS'` **and** `freshness:'FRESH'` on the node body. Yet the served verdict is `'NA'`. So the self-declared `freshness` field is demonstrably not shielding the node — the outcome is fixed entirely by the gate fixture's captured `fresh:false` plus the (passing) grounding. No comment or test title mentions the node's own `freshness` being inert; the file talks only about dropping the declared `status`. This is checkable purely from in-file literals and control flow: `freshness: 'FRESH'` in `predicateDeclaring`, `mkGate(false)` at the call site, `expect(recompute(node)).toBe('NA')`. It is not derivable from the unit's name/signature, which discuss status-dropping and fail-closed emission, not freshness inertia. It does not assert counts or enumerate exhaustively. It does not lean on anything outside these bytes.\n\nCandidate 5 survives. STEP 2 — EMIT THE RESULT.\n\n```atlas-fact\n{\"claim\": \"In SCN-KNOW-1-1 the predicate node's body declares `freshness: 'FRESH'` alongside its HOLDS status, yet it is served as NA — the self-declared freshness is as inert as the self-declared status, since the downgrade is produced entirely by the gate fixture's injected `fresh: false` while the node's grounding fully passes `isGrounded`.\"}\n```"