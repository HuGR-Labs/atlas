"STEP 1 — SCRATCH REASONING (not persisted)\n\nCandidates drafted while reading the bytes:\n\n1. *\"The sidecar corruption helper also wipes generation files.\"* — True per the regex in `corruptSidecar`, but the header comment already says so loudly; close to a restatement of visible intent.\n2. *\"The static-guard test greps `store.ts` imports for `routeWrite`/`upsert`.\"* — Visible in code, but the `it` name (\"reconstruct-only\") plus the inline MUTANT comments make it fairly derivable from the test's stated purpose.\n3. *\"The malformed-put test asserts the CAS root is never created.\"* — Visible (`existsSync(dir) === false`), but it's a direct transcription of the assertion line; an engineer reading the test body sees it immediately.\n4. *\"The case-variant test silently skips on case-sensitive filesystems.\"* — Refutation attempt: is this in the bytes? Yes — `if (!existsSync(join(tmp!, 'CAS'), ...)) return;` sits before any assertion; on a case-sensitive volume `join(upperRoot, H.slice(0,2), H)` cannot exist because `put` wrote under `<tmp>/cas`, so the body exits having run zero expectations. Is it derivable from the test's name/signature? No — the name promises the case-variant path \"is a HIT\"; nothing signals the test is platform-conditional or vacuously green on Linux-style volumes. Does it lean on comments about elsewhere? No — it is pure control flow of this unit. Enumeration risk? None if phrased qualitatively. Would it change what an engineer does? Yes: they would not treat this test as regression coverage for the case-variant false-miss fix on case-sensitive CI hosts, and would know the guard only actually executes on case-insensitive volumes (macOS/Windows defaults).\n5. The FIFO `mkfifo` catch-return skip is similar but more conventional (tool-availability skip); the case-variant one is the subtle, easily-missed vacuous pass.\n\nSurvivor: candidate 4. It is checkable solely from the shown control flow, contradicts nothing, and is not a restatement of the name.\n\nSTEP 2 — RESULT\n\n```atlas-fact\n{\"claim\": \"The N14(4) case-variant test body begins with an early return whenever <tmp>/CAS/<shard>/<hash> does not exist, so on a case-sensitive filesystem it executes no assertions and passes vacuously, exercising the case-variant containment fix only on case-insensitive volumes.\"}\n```"