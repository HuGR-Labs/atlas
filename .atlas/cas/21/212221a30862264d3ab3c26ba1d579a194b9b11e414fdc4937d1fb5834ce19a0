"STEP 1 — REASON FREELY\n\nReading the bytes: this is a tagging document assigning a verification method to each of 16 PST invariants, with a refuse-to-model section and a completion report.\n\nCandidate drafts and refutation passes:\n\n- *Candidate A — echo the tag histogram.* \"formal 1 · PBT 4 · reference-model 11, 0 exhaustive/n-a.\" I recounted the 16 `###` blocks item-by-item: formal ×1 (11), PBT ×4 (2, 5, 12, 14), reference-model ×11 (1, 3, 4, 6, 7, 8, 9, 10, 10a, 10b, 13) — it checks out. But this exact histogram is printed in the Completion report section of the unit itself, so emitting it is parroting the doc's own summary — discarded as a restatement of what the document plainly says.\n- *Candidate B — PERSIST-11 consumes FSPEC-merge.* True and repeated four times in the bytes, but the operative justification points at `spec/fspec-merge.md` / KERNEL-11 — artifacts not in these bytes. The in-byte residue (\"only `formal` tag\") collapses back toward Candidate A. Discarded as partially resting on unseen files.\n- *Candidate C — shared mock between PERSIST-7 and PERSIST-10b.* Both entries explicitly state they share `persist/ref/reinvoke.ts` and are COUPLED-with each other. Verifiable, but flatly written twice in the body; low information gain. Discarded.\n- *Candidate D — PERSIST-5's delete-path scoping.* The up-property text demands \"0 delete paths,\" yet the listed PBT properties in its own down-model are only grow-only monotonicity (`A ⊑ A ∪ e`), dedup-idempotence (`merge(A,A) ≡ A`), and a re-spawn round-trip — and the entry's own italic aside explicitly routes the \"0 delete paths\" arm to a structural grep in S3. \"S3\" is internally grounded: the header declares this unit sits at state S2 with `→ next_state S3`. Nothing else in the bytes contradicts this; the completion report stays silent on it. Survives.\n- Refutation pressure on D: does it lean on a comment about code I can't see? No — it describes this document's own verification-routing decision, quoted from the entry itself. Exact-count risk? I phrase qualitatively (what the PBT does *not* cover), avoiding an \"N of M\" claim. More than a name/signature restatement? Yes — the filename reveals nothing about which clauses are excluded from which property tests. It survives.\n\nSTEP 2 — EMIT THE RESULT\n\n```atlas-fact\n{\"claim\": \"Although INV-PERSIST-5's up-property demands '0 delete paths', that clause is deliberately excluded from its PBT property (which covers only grow-only monotonicity, dedup-idempotence, and a re-spawn round-trip) and is instead routed to a structural grep performed in the later S3 stage.\"}\n```"