"Reading the bytes: the key cross-function behavior here is that `foldArchiveFromRecord` filters strictly on `r.kind === 'task'` and constructs entries only via `archiveTaskFold` (which hardcodes `unit.kind: 'task'`) — yet `ResumeUnit` and `spawnRecall` both admit `'pr'` units. Since `spawnRecall` requires an exact `owner` + `unit.kind` + `unit.id` match and throws when none is found, a `'pr'` recall against an archive produced by this module's derivation function can never succeed. Checking refutations: the default-parameter self-combine in `carry` is visible in the signature itself (discarded); the `seq: 0` literal is plainly readable and barely actionable (discarded); the throw-on-miss is real and code-verified but is nearly a plain restatement of the function body once read, whereas the task-only/task-vs-pr mismatch spans two functions and is invisible in either signature alone. The surviving claim rests only on the filter literal, the constructor literal, and the matcher/throw control flow — all present in these bytes, no reliance on comments about external files.\n\n```atlas-fact\n{\"claim\": \"foldArchiveFromRecord projects only records whose kind is 'task', so an archive derived from a versioned record contains no 'pr' folds at all — meaning spawnRecall invoked with a 'pr' ResumeUnit against such an archive can never find a matching entry and will always throw.\"}\n```"