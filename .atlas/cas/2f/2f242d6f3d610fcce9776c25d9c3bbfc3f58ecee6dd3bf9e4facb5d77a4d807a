"STEP 1 — REASONING (scratch):\n\nThe unit is a goldens/spec document, so candidate facts must come from its own scenario bodies, fixtures, and tables — not from its references to issues (#104/#112/#189/#191), other files (`req-idx.md`, `id-integrity.mjs`), or measured claims about artifacts I can't see here.\n\nCandidates drafted and refuted:\n- \"Gate threshold is strictly `>` (ratio exactly 0.15 publishes but doesn't fail)\": REFUTED — 16a-1 only asserts publication at 0.15; non-failure at exactly 0.15 is never asserted, so strictness isn't re-derivable from these bytes.\n- \"Dirty-bit propagates over the full closure while hash recompute is capped\": survives, but the note under 12g states it almost verbatim; weaker novelty.\n- Ledger counts (58 REQ, 35 held-out): I recount them and they check out, but exact-count facts about the ledger are self-described bookkeeping; low action-value and miscount risk.\n- Appendix pins being non-SCN regression pins: visible, but the *reason* rests on the id-graph harness elsewhere; the bare structural note is thin.\n- SURVIVOR: the intra-family contrast in REQ-INDEX-13. Clause 13a mandates that *every* unresolvable coupling (dynamic dispatch `C ⇢ ?`, FFI, reflection, DI wiring — 13b confirms reflection/DI cases) appear as an explicit tagged `unresolved` edge. Yet SCN-INDEX-13c-3 demands that references to document-scoped SCIP `local 2` symbols produce **no edge at all** — explicitly \"no `resolved` edge … and no `unresolved` edge … either\". That's an exception to the clause family's own universal, invisible from any single heading, and it flips an implementation decision (emit-an-edge vs emit-nothing) for local symbols. The supporting numbers in its teeth (277 symbols, 6,753→2,202 edges) are claims about a `.atlas/index.scip` not in these bytes — stripped; the Given/Then alone carries the fact.\n\nSTEP 2 — RESULT:\n\n```atlas-fact\n{\"claim\": \"Within REQ-INDEX-13's rule that every unresolvable coupling (dynamic dispatch, FFI, reflection, DI wiring) must appear as an explicit tagged unresolved edge, references to SCIP document-local `local` symbols are the exception: they must produce no dependency edge at all — neither a resolved edge nor an unresolved placeholder.\"}\n```"