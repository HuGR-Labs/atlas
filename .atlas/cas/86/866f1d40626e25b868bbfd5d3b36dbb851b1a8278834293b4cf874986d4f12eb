"Reading the bytes: this unit's executable content is one `export type * from './types.js'` plus sixteen plain `export *` lines — emit, reconcile, init, anchors, slots, draft, check, guard, fault, handler, query, bands, doctor, transport, diff, push — each carrying an inline annotation.\n\nDraft candidates and refutation:\n\n- *GOVERNANCE_SURFACE has length 5 / READ_SURFACE length 6* — these numbers live in `handler.ts`, not here; the comments merely point at another file. Truth would rest on unseen code. Discard.\n- *atlas-diff is excluded from both surfaces* — again a claim about `handler.ts`'s constants and an external ledger. Discard.\n- *`bands.js` is imported by `@atlas/adapter-io`* — a claim about another package. Discard.\n- *types.js is the only type-only re-export* — checkable in the bytes, but essentially a literal restatement of one visible line versus the others; marginal behavior impact given the module is named `types`. Weak candidate.\n- *Module-per-tool asymmetry*: enumerating all sixteen re-export targets item by item, dedicated modules appear for `emit`, `reconcile`, `init`, and `query` — yet there is no `./link.js` (or any link-named module) anywhere in the list, despite the file's own annotations repeatedly naming `atlas-link` as the second governed write door. Refutation check: the absence is within THIS unit's own export list, directly re-verifiable by rereading the bytes (I listed all sixteen; none is link). The load-bearing assertion is the code asymmetry itself, not a comment's claim about unseen files — the comment text I lean on for context is present in these bytes and describes nothing I assert as true of elsewhere. It is not derivable from the file's name/signature (a reader assuming sibling-convention symmetry would wrongly expect `link.ts` to exist and be wired through the barrel), and it changes behavior: an engineer editing or auditing the `atlas-link` write path cannot find or reach its implementation via any link module in this barrel and must look inside another re-exported module instead. Survives.\n\n```atlas-fact\n{\"claim\": \"The barrel re-exports a dedicated module for each of atlas-emit, atlas-reconcile, atlas-init, and atlas-query, yet despite its own annotations naming atlas-link as the second governed write door, its re-export list contains no link module at all, so the atlas-link write path cannot be located or reached through any link.ts-style module in this barrel.\"}\n```"