"```atlas-fact\n{\"claim\": \"Inside admitPredicate, only the synthesized-check path gated by attest mints a kind:'predicate' node (via buildPredicate) — every other admitted route (a proven dependency/count/definition oracle, the type-expressible sound oracle, oracle abstention falling back through admitAbstainedAsJustified, and the no-synthesized-check semantic slot) returns a node built by buildSound/buildAdvisory/buildJustified and therefore carries kind:'advisory', several of them still wearing the LIKELY_INVARIANT label.\"}\n```"