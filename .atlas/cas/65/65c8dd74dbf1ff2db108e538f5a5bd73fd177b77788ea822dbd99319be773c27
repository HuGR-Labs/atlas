"STEP 1 — scratch reasoning:\n\nReading the body beyond the name: the first executable line is `const ROOT = process.env.ADR_CITATION_GUARD_ROOT ?? join(dirname(fileURLToPath(import.meta.url)), '..', '..')`, and every subsequent path (`DOCS`, `ADR_DIR`, `PKGS`, plus `rel()` outputs in reports) derives from that `ROOT`.\n\nDraft candidates and refutation passes:\n\n- *\"It checks #192/#172-related history\"* — those are PR/comment claims about external trees and past states; discard.\n- *\"123 .ts files cite ADRs, 26 cite ADR-0013\"* — dated snapshot pinned to a commit not in these bytes; discard.\n- *\"Duplicate ADR ids are silently first-wins (`!index.has(m[1])`) and the index is flat (no recursion below `docs/adr/`)\"* — both genuinely derivable from the code, but weaker actionability; held as backups.\n- *\"Docs walk skips nothing while the packages walk skips `dist`/`node_modules`\"* — derivable, but marginal impact.\n- *\"The scan root is env-overridable\"* — survives: it rests only on the `process.env` fallback line and the downstream `join`s; no external module, PR, or absent-file assumption involved; not an exact-count claim; not a restatement of the name (nothing about a bare gate filename suggests a redirectable root).\n\nSurvivor: the root override. An engineer assuming the gate always audits the tree it ships in would be wrong — an inherited `ADR_CITATION_GUARD_ROOT` value redirects every directory it opens, which changes both how you invoke it safely and how you exercise its refusal paths.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"Every directory this gate scans derives from a root that the environment variable ADR_CITATION_GUARD_ROOT overrides when set, falling back to the directory two levels above the script itself.\"}\n```"