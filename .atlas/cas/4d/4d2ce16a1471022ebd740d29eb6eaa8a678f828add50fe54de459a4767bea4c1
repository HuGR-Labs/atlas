"STEP 1 — scratch reasoning (not persisted):\n\nCandidates drafted from the bytes:\n\n- **Ordering of post vs. counter bump**: `ctx.port.postMessage(...)` appears before `Atomics.add(done, 0, 1)` / `Atomics.notify(done, 0)` in the handler. The *why* lives in a comment about main-thread behavior (not in these bytes), but the bare ordering and the counter layout (`new Int32Array(ctx.sab)`, index 0, +1 per job) are fully local and absent from any signature. SURVIVES if phrased without the cross-thread rationale.\n- **Lazy per-worker proposer memoization**: `cache.p ??=` with the cache hoisted outside the handler proves resolve-once-per-worker, deferred to the first job. Local and behavioral, but the parameter name `cache` telegraphs much of it, and the comment restates it. Weaker novelty.\n- **Error flattening**: the catch block sends only `name`/`message` with typeof fallbacks — but the exported `PoolAnswer` type already puts `{ ok: false; name; message }` in the signature, so much of this is known from the types. Discarded.\n- **Partial guard validation**: the entry condition checks only `parentPort` and `workerData.port`, never `sab`/`repoPath`/`env`. True and local, but low-stakes nitpick; less action-changing than the handshake ordering. Discarded.\n- **Per-worker `await initAst()`**: the \"each thread has its own registry\" rationale leans on external worker_threads semantics and comment claims; discarded.\n\nRefutation pass on the survivor: exact counts? None asserted. Comment-dependent? The claim stands on the literal statement order and the `Int32Array(ctx.sab)` / index-literal `0` in the code itself. Restatement of name/signature? No — `serve(ctx: WorkerContext): void` and `ctx.sab: SharedArrayBuffer` reveal neither the word-0 layout nor the post-before-bump discipline. It survives.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"For every job, the worker posts its answer on the message port before incrementing and notifying the Int32 completion counter at index 0 of the SharedArrayBuffer, so that counter only ever tallies answers that have already been enqueued.\"}\n```"