"STEP 1 — scratch reasoning:\n\nReading the bytes: this is a prose spec for a six-state loop BIND→RED→GREEN→REFACTOR\\*→GATE→SEAL. Candidate facts, tested against the refutation rules:\n\n- *\"134 ∀-laws / ~340 held-out fixtures exist\"* — discarded. Counts asserted about Wave P/H artifacts (commits `4612964`, `fdf105c`) not present in these bytes; cannot be recounted here.\n- *\"No gate tooling exists yet; it's all judgment\"* — tempting and action-changing, but it is the doc's claim about the absence of harness code elsewhere in the repo; doesn't re-derive from this unit alone. Discarded.\n- *\"Builder never authors tests\"* — true and in the thesis, but it's essentially the headline summary of the doc's stated thesis (\"The builder decides nothing\"); too close to a plain-summary restatement.\n- *Repair budget N=3 cap 5* — a real literal, but narrow; weaker than a structural fact.\n- **Candidate: REFACTOR is conditional.** The table row says \"**REFACTOR\\*** | *(only if refactor predicate fires)*\", the diagram marks it with `*`, and the footnote defines the predicate (duplication, LOC within 10% of the 400 cap, cyclomatic threshold) and skip behavior. Confirmed in multiple independent spots within these bytes; purely internal to the doc's own normative definitions; no reliance on unseen code; qualitative phrasing avoids fragile counts. Nothing in the unit's name (\"a governed per-WP state machine\") reveals that one state is skippable — a reader would assume a fixed six-step sequence. This changes behavior: an implementer/dispatched seat must evaluate a predicate rather than plan a refactor pass per WP.\n\nSurvives. Emitting it.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"Despite the six-state presentation, the REFACTOR state is predicate-gated and normally skipped: it runs only when a refactor predicate fires (duplication, LOC within 10% of the 400-per-file cap, or the cyclomatic threshold), with the loop otherwise proceeding directly from GREEN to GATE.\"}\n```"