"STEP 1 — Scratch reasoning (not stored):\n\nCandidate facts considered and tested against the bytes:\n\n1. *\"The handler validates args against the published schema before the leg runs.\"* — True and visible in control flow, but largely restates the comment and the natural reading of `malformedArgsReason(tool, schema(tool), args)` placement. Discarded as too close to a summary.\n\n2. *\"Internal faults swap in `INTERNAL_GUIDANCE` instead of tool guidance.\"* — Visible in code, but the surrounding comments already announce it loudly, and its truth partly leans on the comment's rationale. Weak.\n\n3. *\"`_handlerConforms` labeled 'compile-time' actually invokes `createHandler({})` at module-evaluation time.\"* — Checkable and mildly surprising, but `createHandler` is pure (no side effects), so it wouldn't change what an engineer does. Discarded on impact.\n\n4. **Rejection-shape inventory.** Walk every `ok:false` return literal in this unit: unwired leg (no `data`), `argFault` (none), `missingRequiredReason` (none), `internalReason` (none), `malformedReason` (none), governed-refusal catch fallthrough (none), both `resolveNode` rejections (none). Exactly one rejection path differs: the fail-closed write guard returns `{ ok: false, data, rejected, guidance }` — the leg's full record rides in `data` alongside `ok:false`. Verified item-by-item against the source. Not derivable from the `Verdict<ToolData>` signature alone (it doesn't say when `data` accompanies rejection). A consumer who assumes \"`ok:false` ⇒ no `data`\" or \"`data` ⇒ success\" would mis-handle governed write refusals — this changes what they do. Truth rests only on the literals in these bytes, not on any comment's claim about another file. Survives.\n\n5. *Fallbacks in `guidanceFor`/`schema` are unreachable per `Record<Tool, …>` typings.* — Type-level trivia; at runtime an off-token makes them live. Discarded.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"Every rejected verdict from handle omits data except one: when a leg's output is flagged as a fail-closed write (emitted:false or linked:false), the verdict is ok:false yet still carries the leg's full record in data, unlike every other rejection path in the unit.\"}\n```"