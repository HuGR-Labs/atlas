"STEP 1 — Reasoning (scratch):\n\nCandidates drafted and tested against the bytes:\n\n1. *“Cap boundary is inclusive (250 ok, 251 throws)”* — visible in the assertions, but the describe title (“at the exact cap boundary is injected”) already conveys acceptance-at-the-boundary; too close to a restatement. Discard.\n\n2. *“Incremental fold equals whole-log assembly”* — asserted in 12c-2, but the scenario header and test titles state this outright; largely a summary of the labeled intent. Discard.\n\n3. *“Order-independence: reversed insertion order gives byte-identical Orientation”* — real, but the scenario name (“still == replay-from-empty”, “order-independently”) telegraphs it. Discard.\n\n4. *“Parent links use contentHash while the log map keys by id”* — genuinely visible (`prev = e.contentHash` fed into `orientEvent`'s parent array, while `m.set(e.id, e)`), but it reads as a plain mechanical description of the helper's obvious control flow. Weaker candidate; set aside.\n\n5. *“injectOrientation returns the caller's own object reference on the accept path”* — the bytes contain `expect(injectOrientation(oEdge)).toBe(oEdge)`. Vitest's `toBe` is reference identity (Object.is), not deep equality — so the contract encoded here forbids returning a clone or normalized copy. Refutation checks: not derivable from the file name or any visible signature (orient.ts isn't shown); not reliant on a comment's claim about elsewhere — the assertion itself is the evidence; not a numeric/enumerative claim; not a restatement of name or types, since nothing in “cap boundary” implies pass-through-by-reference. An implementer who defensively clones before returning would fail this test — that changes what they do. Survives.\n\nSTEP 2 — Emit:\n\n```atlas-fact\n{\"claim\": \"The cap-boundary test asserts acceptance with vitest's toBe (reference identity), meaning injectOrientation must return the very same Orientation object it was given rather than an equal copy or a rebuilt/normalized one.\"}\n```"