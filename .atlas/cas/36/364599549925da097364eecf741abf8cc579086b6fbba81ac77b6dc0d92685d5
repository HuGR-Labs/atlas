"STEP 1 — Reasoning scratch:\n\n- Candidate: \"`--write` rewrites an .md/.json report pair\" (per the usage banner). Refuted as a *comment-derived* claim — checking the code, there is exactly **one** `writeFileSync` call in the entire unit (in the `--write` branch), targeting only `calibration-report.a1-subject.json`. No `.md` path is constructed or written anywhere. So the *code-true* fact is the opposite of the banner: only JSON is emitted. Survives as a code-checkable claim (one write call, verified; its literal filename verified).\n- Candidate: valid verdict tokens are exactly `'GROUNDED_TRUE'|'HALLUCINATED'|'ABSTAIN'`. Refuted — validation goes through `CATEGORIES.indexOf` from `fleiss.mjs`, which is not in these bytes; the enumeration exists only in a JSDoc comment. Discard.\n- Candidate: `leaks` entries carry an `anchor` field. Rests on `fixtures.mjs` contents not shown. Discard.\n- Candidate: `wilson` returns `[null, null]` for `n=0`. True and code-checkable, but plainly visible body behavior, low action-changing value. Weak.\n- Candidate: CLI accepts a bare verdict map or `{verdicts: ...}` wrapper (`input.verdicts ?? input`). True, minor surprise, but thin.\n- The surviving fact (single JSON-only write under `--write`) is countable and listable in these bytes: one `writeFileSync`, one `.json` literal, confirmed item-by-item; it changes what an engineer does (don't expect/hunt for a generated Markdown report).\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"Although the usage banner advertises '(re)write the .md/.json report', the --write mode contains exactly one writeFileSync call and emits only the single JSON file calibration-report.a1-subject.json — no Markdown file is produced anywhere in this script.\"}\n```"