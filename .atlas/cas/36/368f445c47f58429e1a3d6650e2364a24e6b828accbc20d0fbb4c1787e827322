"```atlas-fact\n{\"claim\": \"The codebase's single formal model, FSPEC-merge, spans two blocks: it covers the kernel cluster KERNEL-9/10/11 plus PERSIST-11, even though the persist (PST) block is otherwise standard-tier with no method-tag of its own.\"}\n```"