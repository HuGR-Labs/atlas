"STEP 1 — REASON FREELY (scratch):\n\nThis unit is a requirements document, so its \"declarations\" are the REQ entries and their scope notes. Scanning for something beyond what the requirement titles convey:\n\n- *Trailer-vs-note asymmetry* (13-b vs 13-d): true, but the titles themselves (\"trailer survives a history rewrite\", \"a rewrite orphans note-carried data\") already state it — rejected as a restatement of names.\n- *Merge properties* (11-a…g): each clause matches its title closely — nothing hidden beyond the names.\n- *Redaction scope under 10a-b*: the title says \"redact-at-source is the primary control,\" which sounds like blanket credential protection. But the scope note in the unit's own bytes enumerates exactly which families are declared — GitHub tokens (`gh[pousr]_`), Slack tokens (`xox[baprs]-`), fine-grained PATs (`github_pat_`), AWS access-key ids (`AKIA`) — and explicitly says any other shape (JWT, PEM private key, the AWS SECRET access key) is **not** redacted, enters the buffer unchanged, and is left to the scanner backstop (10a-c/d). I verified these literals and the exclusion sentence against the shown bytes; the claim rests only on text present in this unit, not on the pointer to `atlas-persist.md`. No exact count asserted beyond the enumerated families I actually read. This would change what an engineer does (e.g., they would not expect transcript buffers to be free of JWTs/PEM keys before the pre-receive hook runs).\n- Survives refutation. Emitting it.\n\nSTEP 2 — EMIT THE RESULT:\n\n```atlas-fact\n{\"claim\": \"Per its own scope note, the redact-at-source control in REQ-PERSIST-10a-b covers only the declared token families — GitHub tokens (gh[pousr]_), Slack tokens (xox[baprs]-), fine-grained PATs (github_pat_), and AWS access-key ids (AKIA) — so other credential shapes such as JWTs, PEM private keys, and the AWS SECRET access key are explicitly not redacted and enter the transcript buffer unchanged, being deferred instead to the server-side scanner backstop (REQ-PERSIST-10a-c/d).\"}\n```"