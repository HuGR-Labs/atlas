"Looking at this unit, it's a design/contract document rather than code, so nearly everything in it asserts things about files I cannot see (`llm.ts:116`, `mine-gate.ts:76`, `admit-harness.ts:334`, `store.ts get()`, `canonical.ts:52-54`, `upsert.ts`, etc.). Candidates resting on those cross-file claims get discarded per the rules — e.g., \"upsert's CREATE branch dropped `answerRef`,\" \"`store.ts get()` re-hashes every read,\" \"`llm.ts` maps invalid bytes to U+FFFD\" — all are facts about other modules, however accurately reported.\n\nWhat survives must be checkable against the document's own bytes. Candidate: the amendment striking leg (a). This is stated repeatedly and consistently *in this unit*: the AMENDMENT block, the §2 sub-heading \"(a) Answer digest on the fact — **STRUCK**\" with the strikethrough, §4 \"Shapes (frozen; leg (a) struck) … no `answerDigest`\", and §6 \"the WP ships **(b)+(c)** only.\" It's internally consistent across all mentions, not derivable from the title (which suggests binding-to-bytes, not which mechanism won), and it changes what an implementer does: they must add no digest field, because the ratified design makes `answerRef` serve that role. The rationale clause (\"the CAS id IS the digest\") is the document's own declared reasoning in these bytes, not a claim about unseen code. No exact counts or exhaustive enumerations are asserted in my claim. This survives.\n\n```atlas-fact\n{\"claim\": \"Although the contract's goal is auditing facts back to the model's exact answer bytes, the 2026-08-10 post-ratification amendment struck the planned answerDigest leg entirely, so the frozen fact shape gains only an optional answerRef (the CAS id, doubling as the digest) and implementers must add no separate digest field.\"}\n```"