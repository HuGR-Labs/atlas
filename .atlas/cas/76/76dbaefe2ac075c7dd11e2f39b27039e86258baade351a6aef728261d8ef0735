"STEP 1 — Reasoning (scratch):\n\nReading the bytes closely. The unit is a design document, so its \"declarations/literals\" are its normative statements, tables, dates, and DAG edges — anything pointing at unseen files (`mine.ts:288`, `router.ts:335`, `compose.ts:337`) is a claim about code not shown here.\n\nDraft candidates and refutation passes:\n\n- *\"First mined relation crashes the pass with DegenerateAnchorError\"* (F1) — REFUTED: its truth rests entirely on asserted behavior of `mine-decide.ts`/`router.ts` bytes not present here. Cannot re-derive at this unit alone.\n- *\"WP-219 blocks WP-96-P\"* — SURVIVES partially: the blocking edge is literally in the §4 table and §3 heading. But WP-219 is already marked DONE awaiting merge, so the actionable delta is small (\"wait for merge\"), and the justification leans on unseen scrub mechanics.\n- *\"Every promoted negation abstains scope-empty without WP-96-COMPOSE\"* — REFUTED: rests on unseen `compose.ts`/`wire.ts` behavior.\n- *\"Option A rejected, Option B ratified\"* — survives, but borders on summary of §2's plain statement.\n- **F3 (§5b)**: the owner-ratified (2026-08-11) split of negation authz-scope (`atlas:mined`) from identity-scope (directory S, `negationKey` leg unchanged), amending the previously ratified #99b/ADR-0015 D3 invariant, and explicitly enlarging WP-96-N beyond a \"small candidate + lens\" WP. Checked: stated directly in §5b's own text; consistent with the §4 table row (dep-on includes \"F3 decision\") and the DAG edge `(F3 decision) → WP-96-N`; no contradiction with §2's shape-rule or §6. Its truth is the document's own recorded decision, not a comment about unseen code. Not derivable from the doc's name/title. Changes what an engineer does: they would badly under-scope WP-96-N without it. Avoids exact-count claims.\n\nThis candidate survives. STEP 2 — emit.```atlas-fact\n{\"claim\": \"Section 5b records an owner-ratified decision (2026-08-11) to split a mined negation's authorization scope from its identity scope — binding the door's authority check to the 'atlas:mined' namespace while leaving negationKey's directory-based identity leg unchanged — and because this amends the previously ratified #99b/ADR-0015 D3 invariant, WP-96-N must own that governance split (across the negation type, the governed emit door, and the spec/ADR/goldens) before the candidate-plus-lens work, making it far larger than the thin task the §4 table row suggests.\"}\n```"