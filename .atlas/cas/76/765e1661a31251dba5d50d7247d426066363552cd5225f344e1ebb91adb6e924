"STEP 1 — Reasoning (scratch):\n\nReading the bytes: this is a small Vitest config whose only substantive content is the `test` block — three include globs, two exclude globs, `environment: 'node'`, `testTimeout: 10_000`.\n\nCandidates drafted and refuted:\n\n1. *Comment-derived claims* (goldens, WP RED/GREEN, held-out `-2` fixtures at GATE, \"no test may import dist/\") — all rest entirely on the header comment asserting things about files/processes not present in these bytes. The `exclude: ['**/dist/**']` pattern only stops test-file *discovery* under dist directories; nothing in the shown code enforces anything about imports. Discarded.\n\n2. *Timeout/environment values* — present in the bytes but a bare restatement of visible literals with little behavioral surprise; weak.\n\n3. *Absence of `globals: true` implying explicit imports of test APIs* — depends on knowledge of Vitest's default settings, which is library convention not visible here. Discarded.\n\n4. *Discovery-root claim* — the `include` array is code (a literal), not a comment. Listing it item-by-item: `'packages/*/test/**/*.test.ts'`, `'packages/*/src/**/*.test.ts'`, `'harness/**/*.test.mjs'` — exactly three patterns, re-checked against the bytes. Two details are actionable and not guessable from the filename: tests may be colocated inside package `src/` directories, and harness-directory tests must be plain-ESM `.mjs` files rather than TypeScript. This is checkable solely by reading this unit's own declarations and changes where an engineer places/writes new test files. Survives.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"Test discovery here covers colocated *.test.ts files inside packages/*/src/ and plain-ESM *.test.mjs files under the top-level harness/ directory, in addition to *.test.ts files under packages/*/test/.\"}\n```"