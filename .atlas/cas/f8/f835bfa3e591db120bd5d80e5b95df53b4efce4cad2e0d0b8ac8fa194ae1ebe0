"STEP 1 — REASONING (scratch):\n\nReading the bytes closely. Candidates drafted and refuted:\n\n1. *\"Unknown flags fold to `'true'`\"* — true, but the JSDoc and inline comment both state this explicitly, and it matches the code; borderline restatement of documented intent. Weak novelty.\n2. *Command recognition uses `Object.prototype.hasOwnProperty.call(COMMAND_LEG, …)`* — visible, but largely conventional practice; marginal action value.\n3. *\"No `--` end-of-options separator; a bare `--` folds to flag key `''`\"* — derivable (`replace(/^-+/, '')` on `--` → `''`, not in `VALUED_FLAGS`, so `flags[''] = 'true'`), but it glues two observations together and leans on absence-of-a-feature framing. Discarded for cleanliness.\n4. *\"Valued flags reject `-`-prefixed next tokens, so `--min -1` stores `'true'`\"* — fully checkable in the guard `!next.startsWith('-')` plus the follow-up fold of `-1` as flag `'1'`, and the `=`-form workaround is visible in the `eq` branch. Strong, but the JSDoc already says \"not another flag,\" making the core mechanism semi-documented.\n5. **Surplus positionals are accepted**: the ONLY arity check is `if (positionals.length < need)` — a strict lower bound. Nothing truncates or validates excess; the final `return` hands back the full `positionals` array. The `parse` JSDoc enumerates four failure modes and omits any upper-bound rejection, so this is not asserted anywhere — it must be derived from the `<` operator itself. Refutation attempts: is there any later filter or second check? No. Does the type imply it? Only vaguely (`readonly string[]`). Does it rest on other files/comments-about-elsewhere? No. Exact-count risk? None (qualitative). Is it actionable? Yes — integrators, marshallers, and test authors cannot rely on the parser rejecting extra/junk tokens; surplus args arrive in `positionals` as `ok: true`.\n\nSurvivor: candidate 5.\n\nSTEP 2 — RESULT:\n\n```atlas-fact\n{\"claim\": \"parse enforces only a minimum positional count — the arity check uses a strict `<` comparison, so surplus positionals beyond ARITY[command] are neither rejected nor truncated but silently returned in the ok result's positionals array.\"}\n```"