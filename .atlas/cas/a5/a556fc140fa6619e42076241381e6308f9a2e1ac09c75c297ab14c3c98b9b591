"STEP 1 — Reasoning (scratch):\n\nCandidate drafts considered against the bytes:\n\n1. *\"Seals ride one atomic commit\"* — supported only by header/comments describing AR-27 intent about code not shown. Discarded (comment-borne).\n2. *\"Gate 0 strips authored seals / oracle mints witnesses\"* — claims about `createGovernedEmit` internals invisible here. Discarded.\n3. *\"Only `depends-on` is provable\"* — sourced from the F3 comment reference, not re-derivable in this unit alone. Discarded.\n4. **Survivor**: Reading the actual assertions, the missing-witness forgery test does something sharper than the name \"is stripped\" suggests: (i) the fixture builder maps `witness: null` to *omitting* the property entirely (`if (opts.witness !== undefined && opts.witness !== null)`), so the input genuinely lacks the key; and (ii) alongside `expect(bytes.seal).toBeUndefined()`, it asserts `Object.prototype.hasOwnProperty.call(bytes, 'seal')).toBe(false)` — pinning that the strip results in the `seal` property being absent from the persisted CAS bytes, not merely present-with-undefined-value. A strip implemented as `seal: undefined` would satisfy every other `toBeUndefined` check in the suite but fail this one.\n5. Refutation pass: derivable directly from declarations/control flow/literals in this file? Yes — both the builder guard and the hasOwnProperty assertion are visible code. Rests on any comment's claim about unseen modules? No. Asserts a count/exhaustive list? No. Restatement of name/signature? No — \"stripped\" in the title does not convey the deletion-vs-undefined distinction or the null→key-omitted mapping. Contradicted by current code? No. Survives.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"In the missing-witness forgery case the fixture omits the witness property entirely (null maps to key-absence), and the test pins the strip as full property deletion — asserting Object.prototype.hasOwnProperty.call(bytes, 'seal') is false, not merely that bytes.seal is undefined.\"}\n```"