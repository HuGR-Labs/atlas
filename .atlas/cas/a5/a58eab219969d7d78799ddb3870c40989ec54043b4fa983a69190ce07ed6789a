"STEP 1 — REASONING (scratch)\n\nCandidates drafted from the bytes:\n\n1. *\"Provenance gate short-circuits before any parse, forcing `unreadable: false` and `identity: 'current'`\"* — true in code (`if (trusted !== undefined && !trusted())` returns before the loop). But the adjacent comment already advertises \"PROVENANCE FIRST\", and the operative semantics of `trusted()` live in `store-provenance.ts`, not shown. Weaker than it first looks.\n\n2. *\"`top` is raised by the mirror's embedded `gen` via `Math.max(top ?? 0, legacy.gen)`\"* — code-checkable, but the interface doc on `SidecarRead.top` already spells out \"∪ the mirror's own `gen`\". Borderline signature-knowledge.\n\n3. *Whole-file rejection on a single bad row* — `wire.current.every(isKeyedEntry) → undefined` is in the bytes, but it is the kind of thing a careful read of `readOne` yields directly, and its \"why\" leans on out-of-file modules (`governed-link.ts`, threat-model quotes).\n\n4. **Fallback depth.** The prose inside this very unit frames the recovery as one step: `readOne`'s doc says \"the caller below tries the PREVIOUS generation,\" and the leg-2 note says \"it now costs one extra parse… exactly one governed write behind.\" But the code is a plain `for (const entry of gens)` over the FULL descending output of `listGenerations`, with no index limit or early exit other than success — so after a failed newest generation it retries *every* older generation, and only after exhausting all of them does it touch the mirror. The code demonstrably exceeds the one-step behavior the unit's own prose claims. Verified against control flow only: loop covers all entries; mirror read sits after the loop; no break. Qualitative (no counts asserted), not derivable from the name/signature (whose doc actually understates it), and it changes what an engineer does (worst-case boot cost is O(all generation files), and the recoverable floor is the oldest surviving readable generation, not g−1 — relevant to any pruning logic added later).\n\nSurvivor: candidate 4.\n\nSTEP 2 — RESULT\n\n```atlas-fact\n{\"claim\": \"In readSidecarSet, the corrupted-newest-generation fallback is unbounded: the loop retries every progressively older generation file from the full descending listGenerations output until one parses, and only after exhausting all of them does it consult the pre-protocol mirror — not merely the single previous generation that the unit's own prose ('one extra parse', 'the PREVIOUS generation') describes.\"}\n```"