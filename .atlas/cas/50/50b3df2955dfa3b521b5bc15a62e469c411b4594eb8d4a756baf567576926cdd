"STEP 1 — REASONING (scratch):\n\nCandidates drafted and tested against the bytes:\n\n1. *\"No-op passes republish the snapshot\"* — `projection` initializes to `staged`, the loop can `continue` past every item, and the single unconditional `return { out: minted, put: puts, next: projection }` always carries `next`. Survives as derivable, but it is heavily documented in the trailing comment block, and its contract significance borders on restating the visible return. Set aside in favor of something less commented.\n\n2. *\"Guard semantics allow intra-batch repeats\"* — the skip guard tests `staged.current.has(key)` on a binding never reassigned, and `!minted.has(key)` is a negative conjunct, so minted membership *permits* processing. Derivable, but the conclusion leans on assumptions about `knowledgeUpsert` purity that aren't visible here. Discarded as partly resting on unseen behavior.\n\n3. **Receipt-vs-skip ordering** — checking statement order in the loop body: `mintIdentity` → the skip `continue` (`staged.current.has(key) && !grounded.has(key) && !minted.has(key)`) → *then* `answerReceipt(rawAnswer)` → `puts.push(receipt.obj)` / `puts.push(f)`. The `continue` provably precedes any receipt construction or `puts` push. `answerReceipt` appears nowhere else except the import. Therefore a fact suppressed by the guard contributes neither its answer CAS object nor its fact bytes to the returned `put` list. This rests only on control-flow order visible in this file — no comment needed (and no comment states it; the receipt comments only discuss scrub-before-CAS and absence for human writes). Not derivable from the name/signature, not a count/enumeration claim, not a summary of the obvious. It changes what an engineer does: anyone assuming all mined answers become durable (e.g., when adding provenance auditing or reasoning about CAS contents) would be wrong for already-staged keys.\n\nSurvivor: candidate 3.\n\nSTEP 2 — RESULT:\n\n```atlas-fact\n{\"claim\": \"In decideStaging's loop, the continue that skips a fact whose key is already staged (and neither grounded nor minted this attempt) executes before answerReceipt is ever invoked, so an already-staged fact's answer bytes never enter the returned put CAS list — only facts that survive the guard have their receipt objects and fact bodies pushed for durability.\"}\n```"