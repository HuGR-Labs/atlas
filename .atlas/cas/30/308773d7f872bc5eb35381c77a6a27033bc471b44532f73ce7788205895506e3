"STEP 1 — SCRATCH:\n\nCandidates drafted and refuted against the visible bytes:\n\n1. *\"Fails closed when the gate doesn't return HOLDS\"* — true, but this is the headline claim of every comment in the file; a reader of the file already knows it. Restates the documented contract. Discard.\n2. *\"createEmit implements only `emit`, not `absorb`\"* — true, but the return type `{ readonly emit: EmitApi['emit'] }` states this outright in the signature. Known from signature. Discard.\n3. *\"All gate failures collapse to one fixed rejection string\"* — checkable (`REJECTED` constant), but closely mirrors the comment text describing TOOLS-7b; weaker than candidate 4.\n4. *\"`at` never reaches the store\"* — trace the data flow in `emit`: `at` appears exactly once, in `gate.gateHolds(node, at)`. On the success path, `obj = node as CasObject`, `key = id(obj)`, `store.set(key, obj)`, returned `id: key`. Neither the persisted object, its key, nor the returned id derives from `at` in any way. Verified by scanning the entire function body — `at` has exactly one use site. Not stated by the name/signature (a reader seeing `emit(node, at)` could reasonably assume the sha is pinned into the write or echoed back), not asserted by any comment (comments discuss re-derivation at source@sha but never say whether the sha is recorded on persist), and actionable: anyone needing provenance of *which* sha admitted a fact must embed it in `node` or track it elsewhere — emit won't do it. Survives refutation.\n\nSTEP 2 — RESULT:\n\n```atlas-fact\n{\"claim\": \"Within createEmit's emit, the `at` hash is consumed only by the gate re-derivation check: the persisted object is `node` itself (cast to CasObject) and both the storage key and the returned id come from `id(node)`, so `at` never reaches the store or the output.\"}\n```"