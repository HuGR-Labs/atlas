"STEP 1 — scratch reasoning:\n\n- Candidate: \"`get` misses return undefined.\" Refuted as a candidate: that is visible directly in the signature (`CasObject | undefined`) and restated in the doc comment. Not novel.\n- Candidate: \"put is idempotent / dedups by content.\" Partially refuted: the interface doc comment explicitly states idempotency (\"returns the same Hash and stores nothing new\"), so much of it is known without reading the body.\n- Candidate: the `if (!cas.has(key))` guard means the *first* stored reference is retained and a later put of equal-content bytes never replaces it. Survives partially, but the doc comment's \"stores nothing new\" already gestures at this; weaker.\n- Candidate (survivor): the failure path. The signature `put(obj: CasObject): Hash` gives no hint of how failure is signaled — the type offers no out-of-band channel. Reading the body: the entire `try { key = id(obj) } catch { return EMPTY }` maps *every* canonicalization failure to one module-level sentinel, and that sentinel's literal value is `const EMPTY: Hash = asHash('')` — the empty string branded as a Hash. Verified against the bytes: the constant definition and the bare (binding-less) `catch` are both present; no reliance on comments about other files, KERNEL refs, or line citations. No counts or exhaustive lists asserted. Not derivable from the name/signature alone (which Hash is returned on failure is body-level knowledge), and it changes behavior: callers must compare against `''` or probe resolution rather than wrap in try/catch, and distinct malformations are indistinguishable.\n- Checked the counter-risk: could a legitimate `id` result collide with `''`? Unverifiable from these bytes, so the claim is scoped strictly to what the catch path returns, not to whether the sentinel is guaranteed unresolvable.\n\nSTEP 2 — emit:\n\n```atlas-fact\n{\"claim\": \"When canonicalization inside put throws, the method silently swallows the error and returns a single fixed module-level sentinel, EMPTY, which is literally the empty string branded as a Hash (asHash('')), so every kind of malformed input collapses into one value the Hash type cannot distinguish.\"}\n```"