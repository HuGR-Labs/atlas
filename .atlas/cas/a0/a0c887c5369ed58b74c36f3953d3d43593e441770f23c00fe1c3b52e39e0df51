"STEP 1 — scratch reasoning:\n\nCandidates considered and refuted/survived:\n\n1. *Coverage-table arithmetic (29 REQs).* Summing the `covers_reqs` lists gives 5+5+2+3+4+7+3 = 29, matching the total row. Survives arithmetically, but the \"73 REQs / 18 INVs\" denominators reference totals never enumerated in these bytes, and the table largely restates the per-entry lists. Weak actionability — discarded.\n\n2. *The two \"deliberate exceptions\"* (PROP-AUTH-2, PROP-MCP-3 rendered as laws despite the scope note). The scope-note prose is in the unit and their laws are visibly set-level (cardinalities, disjointness), but the claim that these INVs are classed `exhaustive`/`reference-model` lives in `method-tags-authoring.md`, not here — the classification half rests on elsewhere. Discarded as too dependent on unseen classification.\n\n3. *The 2026-07-25 dogfood payload.* The requirement to include the exact historical payload is a literal in PROP-AUTH-12's `arbitrary`, but the payload itself is not in the bytes; acting on the fact requires material outside the unit. Weaker survivor — set aside.\n\n4. *Witness/REQ gap in PROP-AUTH-1.* Witness list is {a,c,d,e} while `covers_reqs` includes 1b; every sibling property pairs same-length lists. Byte-checkable, but the \"anomaly\" framing leans on an inferred letter-to-golden convention the doc never states. Set aside.\n\n5. *Shared whole-file digest pinning.* The header defines `source` as ptr + digest of the **whole file** (first 8 hex), and I can count the literal `@sha256:ca246032` appearing identically in all seven `source:` lines (AUTH-1, AUTH-2, AUTH-8, AUTH-11, AUTH-12, MCP-3, MCP-4 — seven occurrences, verified). Since the digest is declared whole-file rather than section-scoped, the stale-trigger is any edit anywhere upstream, hitting all seven laws simultaneously. Entirely derivable from these bytes (header definition + seven literals), not implied by the filename alone, and directly changes what an editor of the upstream file must do (refresh/accept seven STALE flags at once). Survives.\n\nSTEP 2 — emit surviving fact:\n\n```atlas-fact\n{\"claim\": \"All seven properties in this unit pin the identical whole-file digest @sha256:ca246032 of method-tags-authoring.md — scoped to the entire frozen source file, not each law's own INV section — so any single edit anywhere in that upstream file renders all seven laws STALE at once.\"}\n```"