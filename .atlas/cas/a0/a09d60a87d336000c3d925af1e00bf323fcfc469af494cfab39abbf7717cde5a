"Step 1 (scratch): Candidate facts considered — (a) the gate runs as a subprocess with env-var config; true and visible in `run`, but partially anticipated by the header comment and fairly guessable for a `.gate` test. (b) stdout and stderr are concatenated into one `out` string on failure; visible, but minor. (c) pins/digests are never authored in the test file — `pinsDemandedBy` harvests them at runtime by regex-matching the gate's own refusal output (`add \"...\": { \"digest\": \"<32 hex>\"`), and `runPinned` plus several individual cases feed those harvested values back. Refutation check: the regex literal is in these bytes; scanning every `it`, no 32-hex digest is ever written literally — even the cross-carrier-move case takes its value from harvested pins; the claim rests only on this file's declarations and control flow, not on comments about #199/#208 or other files; phrased qualitatively, no count asserted. It survives: it is not implied by the filename, and it changes what an engineer does (editing the gate's message prose silently breaks the harvest regex and every pinned case).\n\n```atlas-fact\n{\"claim\": \"The test suite never hardcodes pin keys or digests: pinsDemandedBy derives them at runtime by regex-parsing the gate's own refusal output (matching `add \\\"<key>\\\": { \\\"digest\\\": \\\"<32 hex chars>\\\"`), and runPinned and the individual cases feed exactly those harvested values back, so any change to the gate's failure-message wording breaks the harvest regex and thereby every pinned case.\"}\n```"