"STEP 1 — REASONING (scratch):\n\nReading the bytes closely, candidates considered:\n\n1. **Comment-based candidates** — the header's claims about held-out goldens, \"the builder was blinded\", \"merge.ts is NOT modified\" — all assert things about files/processes not present in these bytes. Discarded per rules.\n\n2. **Fixture-level observations** — e.g., \"all events have seq=0, fresh=true, empty supersedes\" — countable (three `ev` calls, all using defaults), but it's essentially a restatement of the literals and wouldn't change what an engineer does.\n\n3. **`ev` helper's conditional spread** — `...(node === undefined ? {} : { nodeKey: ... })` genuinely omits the `nodeKey` key rather than setting it to `undefined`. Checkable in the bytes. However, the adjacent comment already announces this intent, and no fixture in this file ever exercises the `undefined` branch (all three events pass a node string), so it's dead code within this unit. Weaker candidate.\n\n4. **`degradeMerge` takes JSONL strings while `mergeAtlas` takes maps** — inferable from call sites, but its truth rests partly on TypeScript enforcement in files not shown. Weaker.\n\n5. **The `set()` helper's duplicate policy** — the body is fully visible: `if (!m.has(e.id)) m.set(e.id, e);`. The guard fires only when the id is *absent*, so on duplicate ids the FIRST occurrence is kept and later ones silently skipped. This is checkable purely by reading this unit's own code — no comment needed, no external file. It is not derivable from the name (`set`) or signature (`readonly Event[] => EventLog`); indeed the name invites assuming `new Map(pairs)` semantics, which in JavaScript keep the LAST duplicate. An engineer adding fixtures with colliding ids, or reasoning about which event survives a cross-branch overlap fed through this helper, would behave differently knowing first-wins. Survives refutation.\n\nSTEP 2 — RESULT:\n\n```atlas-fact\n{\"claim\": \"The local set() helper resolves duplicate event ids first-wins: its `if (!m.has(e.id))` guard keeps the earliest occurrence and silently drops any later event with the same id, the opposite of the last-wins behavior of building a Map from pairs directly.\"}\n```"