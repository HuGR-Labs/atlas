"Looking at the bytes closely:\n\n**Draft candidates:**\n\n1. *\"Rows emit in sorted territory order\"* — stated in the header comment and plainly visible in `[...served.keys()].sort()`. Too close to a summary; discard.\n2. *\"No served history ⇒ rate 0, never NaN\"* — explicitly the documented contract, visible in `s === 0 ? 0 : ...`. Signature/docs-level; discard.\n3. *\"Strict `>` not `>=`\"* — visible in the one-line body, but marginal as an insight beyond reading the line itself; weak candidate.\n4. **Negative-threshold edge:** trace the actual control flow. `rateOf` for an *unknown* territory hits `served.get(...) ?? 0` → `0` → short-circuit returns literal `0`. A *registered-but-unserved* territory gets `served.set(k, 0)` → same branch → `0`. Then `crossesThreshold` computes `0 > threshold` with `threshold: OffAtlasThreshold` which is the unbounded alias `number`. With any negative θ, both cases return **`true`** — the opposite of the interface comment's unconditional \"no served history ⇒ `false`\". There is no clamping, validation, or non-negativity assumption anywhere in the code.\n\n**Refutation pass on #4:** Is it derivable solely from these bytes? Yes — every step is a line of code shown: the `?? 0` fallback, the `s === 0 ? 0` short-circuit, the bare `>` comparison, and `type OffAtlasThreshold = number`. Does it rest on a comment's claim about elsewhere? No — the truth rests entirely on the code; the comment reference is incidental (and the code actually undercuts its unconditional phrasing). Is it an exact-count/exhaustive-enumeration claim? No. Is it more than a restatement of name/signature/types? Yes — it's an emergent interaction between the short-circuit, the comparison operator, and the unconstrained parameter type, invisible from `crossesThreshold(territory: string, threshold: number): boolean` alone. It would change what an engineer does: validate/clamp θ, and write a golden pinning this edge.\n\nSurvived. Emitting.\n\n```atlas-fact\n{\"claim\": \"crossesThreshold returns true for a territory with no served turns (whether unknown or registered-but-unserved) whenever the threshold argument is negative, because rateOf short-circuits to literal 0 and the predicate is a strict `>` over the unbounded number-typed OffAtlasThreshold parameter — so the no-served-history-yields-false behavior only actually holds for non-negative thresholds.\"}\n```"