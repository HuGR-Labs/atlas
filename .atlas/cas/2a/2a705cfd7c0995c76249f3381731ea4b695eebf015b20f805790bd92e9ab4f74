"STEP 1 — scratch reasoning (not persisted):\n\nScanning the bytes for facts not implied by the path/name and checkable strictly inside this unit:\n\n- The long \"REACH, MEASURED\" blockquote asserts things about `cli/src/mine-proposer.ts`, `.atlas/`, `genesis/src/seeds.ts` — all claims about files NOT shown here, keyed to task numbers (#159, #182). Per the refutation rules, discard anything resting on those.\n- Dependency structure (e.g., WP-4.10-a.GROUND has `deps: []` yet its seam-freeze names INDEX-owned identity, while WP-4.10-a.INDEX hard-depends on GROUND) is in-bytes and interesting, but partially interpretive about intended scheduling semantics.\n- Stronger, purely textual contrast: nearly every card carries a first guardrail \"no edits outside packages/<its module>/**\". I enumerated all fourteen cards (EPIC-10: 10-a GROUND/KNOW/INDEX, 10-b GROUND, 10-d GROUND; EPIC-11: 11-a GROUND/KNOW/TOOLS, 11-b GROUND/KNOW; EPIC-12: 12-a KNOW/GROUND/TOOLS, 12-b TOOLS) and checked each guardrails line. Exactly one — WP-4.10-d.GROUND — omits the boundary guardrail, and its own `action_surface` explicitly names `edit packages/adapter-io/src/prompt.ts` alongside `packages/grounding/**`; its `anchor` also names that file. Verified item by item; survives refutation: derivable from these bytes alone, not dependent on unseen code, not a restatement of the unit's name, and directly action-changing (an engineer assuming uniform package sandboxing would wrongly forbid or wrongly permit the adapter-io edit).\n\nSTEP 2 — emit:\n\n```atlas-fact\n{\"claim\": \"WP-4.10-d.GROUND is the only card in this unit whose guardrails omit the 'no edits outside packages/<module>/**' restriction, because its sanctioned action_surface explicitly includes editing packages/adapter-io/src/prompt.ts alongside packages/grounding/**.\"}\n```"