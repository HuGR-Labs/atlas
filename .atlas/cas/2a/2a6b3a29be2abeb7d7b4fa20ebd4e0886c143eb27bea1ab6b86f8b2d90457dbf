"STEP 1 — Reasoning scratch (not persisted):\n\nReading the bytes: In SCN-MCP-3's second test, `callTool` is invoked with `'atlas-not-a-tool'` and `undefined` args against a fake whose `handle` unconditionally returns `REJECT_VERDICT`, and the assertion requires `REJECT_VERDICT.rejected!` — the literal `'fail-closed: citation did not re-derive at source@sha'`, defined only in this test file — to appear verbatim in the returned content text.\n\nDraft candidate: callTool has no name-validation short-circuit; unknown names are routed into `handler.handle`, and the handler's own verdict (not a transport-generated \"unknown tool\" error) becomes the user-visible failure.\n\nRefutation attempt:\n- Could `callTool` intercept the bad name and emit its own error containing that string? No — that string literal exists nowhere except as the fake verdict's `rejected` field in these bytes, so the only path by which it can reach the asserted output is through `handle`'s return value being mapped by `verdictToResult`/`callTool`.\n- Comment reliance? The inline comment claims totality, but my derivation rests on the fake's construction (`handle: () => verdict`) plus the assertions themselves — both fully visible in this unit. I am not leaning on what the transport \"never touches\" or any external file.\n- Exact count/enumeration? None asserted.\n- Restatement of name/signature? The test title says the mapping \"is TOTAL … never a throw,\" which an engineer could satisfy by catching exceptions or by name-checking and returning a synthetic error. The mechanism pinned here — delegation to `handle` with the verdict's rejected string surfaced verbatim — goes beyond the title and would change how one implements or secures `callTool` (name gating cannot live in this layer).\n\nSurvives. Emitting. STEP 2:\n\n```atlas-fact\n{\"claim\": \"callTool does no name validation of its own: an off-surface name like 'atlas-not-a-tool' is still passed to handler.handle, proven by the test asserting that the fake verdict's rejected string — which exists only inside the handler in these bytes — appears verbatim in the returned isError text rather than any transport-generated unknown-tool message.\"}\n```"