"STEP 1 — Reading the bytes: the file extends a base config, emits to `dist`, includes `src` and `test`, and lists a `references` array. Candidates drafted:\n\n- *\"Tests are compiled into dist\"* — relies on inferred compiler/rootDir semantics not fully determined by these bytes alone. Refuted/discarded.\n- *\"It inherits strictness/composite settings from the base\"* — the base file isn't shown; anything about its contents rests on unseen bytes. Discarded.\n- *\"It depends on the contracts, kernel, index, and grounding packages\"* — check: enumerate the `references` array item by item: `../contracts`, `../kernel`, `../index`, `../grounding` — four entries, no others in the file. Exhaustive count verified directly against the shown bytes. Nothing here rests on a comment or on files I can't see; the literal paths are in this unit. It is not a restatement of the filename (`packages/knowledge/tsconfig.json` reveals none of this), and it changes engineer behavior: edits to any referenced package require rebuilding it before `knowledge` type-checks/builds correctly. Survives.\n\nSTEP 2 — Emitting the surviving fact.\n\n```atlas-fact\n{\"claim\": \"This package's TypeScript build declares project references on exactly four sibling packages — contracts, kernel, index, and grounding — so those must be built/resolvable for knowledge to compile.\"}\n```"