"```atlas-fact\n{\"claim\": \"The mcpSession tools test asserts strict deep equality (toEqual) between the server's advertised tool names and the fixed ADVERTISED_TOOLS array, so this smoke test pins the server's entire advertised surface — names and their order — not merely the five governance tools, meaning any tool added, removed, or reordered server-side fails this test until ADVERTISED_TOOLS is updated.\"}\n```"