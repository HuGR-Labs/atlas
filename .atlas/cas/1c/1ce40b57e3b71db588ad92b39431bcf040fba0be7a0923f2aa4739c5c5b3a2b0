"STEP 1 — scratch reasoning:\n\nReading the bytes: the signature `supersede(old, next): { readonly node; readonly supersededBy }` promises a return object and says nothing about failure. But the body contains a real control-flow branch: `const CAS_EMPTY = ''` … `if (supersededBy === CAS_EMPTY) throw new UnaddressablePriorError()`. So a caller whose injected store answers `put` with the empty string gets a thrown named error instead of the promised shape — and notably, only *exactly* `''` trips it (strict `===` against that one local constant); any other value flows through as a legitimate pointer.\n\nRefutation attempts:\n- Derivable from these bytes? Yes — constant literal + conditional throw are right here, not inferred from `@atlas/kernel` internals.\n- Resting on a comment's claim about elsewhere? The comments talk about `sidecar-commit.ts`, task #136, and \"nothing was written\" — I discard all of that. My claim only asserts what this file's own statements do: compare against `''`, throw. That is fully checkable in the shown code.\n- Exact count / exhaustive enumeration? None asserted.\n- Restatement of name/signature/types? No — the type surface suggests a total function returning a pair; the throwing sentinel-guarded path is invisible until you read the body. This changes what an engineer does: wrap calls, and never let a store implementation answer `''`.\n\nSurvives. Emitting.\n\n```atlas-fact\n{\"claim\": \"In the bound implementation, supersede compares store.put(old)'s answer by strict equality against the local constant CAS_EMPTY ('') and throws a named UnaddressablePriorError on that one value instead of returning the {node, supersededBy} pair its signature promises — a throwing failure mode entirely invisible in the ArchiveApi surface.\"}\n```"