"STEP 1 — Scratch reasoning:\n\nReading the bytes: `buildTestVacuityLegs` takes four parameters (`feed, store, emit, at`) and returns an object literal. Tracing each output:\n- `testVacuities: createTestVacuityLeg(store)` — consumes **only** `store`.\n- `testVacuity: createTestVacuityProducer(feed.units, emit, at)` — consumes `feed.units`, `emit`, `at`.\n- `replay: feed.replay` — passes through `feed.replay` by reference (no rebuild).\n\nCandidate A: \"`feed.replay` is returned by reference, not rebuilt.\" Survives byte-check, but it's near-restatement of the assignment and the doc text already says \"rides `feed.replay`\". Weak actionability.\n\nCandidate B: \"Each call to `feed.units()` re-runs `testUnitsOf(rawTree, axes)` — no memoization.\" Checkable from the arrow-function body, yes, but whether that matters depends on unseen cost; borderline restatement of a thunk.\n\nCandidate C: The read leg `testVacuities` is constructed exclusively from `store` — the `feed` parameter plays no role in it, despite the file header asserting all three legs share the one HEAD-units feed. Verify against the code: `createTestVacuityLeg(store)` is the entire construction; `feed` appears exactly twice in the function body, once as `feed.units` (producer) and once as `feed.replay` (passthrough), never touching `testVacuities`. This is derivable purely from the return statement in these bytes, not from trusting any comment (in fact the code diverges from the header's \"all three legs share one feed\" framing). It goes beyond the signature — a reader of `buildTestVacuityLegs(feed, store, emit, at)` could not know `store` alone suffices for the read leg — and it changes behavior: rebuilding or swapping the feed cannot alter `testVacuities`, and conversely the read leg's output is independent of the units view. Counting risk checked: exactly two `feed` usages, itemized above.\n\nCandidate C survives. STEP 2:\n\n```atlas-fact\n{\"claim\": \"Within buildTestVacuityLegs, the testVacuities read leg is built solely from the store argument — the shared feed is consumed only by the testVacuity producer (feed.units) and the replay passthrough (feed.replay), so swapping or rebuilding the feed has no effect on the read leg.\"}\n```"