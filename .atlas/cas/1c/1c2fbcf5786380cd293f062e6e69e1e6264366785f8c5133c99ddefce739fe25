"STEP 1 — SCRATCH REASONING\n\nCandidates drafted and refuted:\n\n1. *The `::` separator exclusion story (232/3000 seeds, UNIT_SEP bisect)* — REFUTED as a fact: the why lives entirely in a past-tense comment referencing other files (`resolve.ts`, `build.ts`) and historical measurement. Only the bare filter predicate (`!s.includes('/') && !s.includes('::')`) is in the code, and that's a plain literal read, not actionable insight.\n\n2. *Exactly three modes / sorted keys* — REFUTED as too close to restatement: the describe names literally say \"exactly three modes,\" and the assertion is a visible one-liner repeated four times.\n\n3. *\"10k corner-biased\" label vs `numRuns: 500`* — partially tempting, but \"corner-biased\" is unverifiable prose inside a title string, and the mismatch is a nitpick about a label rather than a mechanism fact. Discarded as weaker than candidate 4.\n\n4. *The INDEX-7 enforcement mechanism* — SURVIVES. Refutation pass:\n   - Derivable from these bytes? Yes: the test constructs `readSrc` via `readFileSync(fileURLToPath(new URL(\\`../src/${f}\\`, import.meta.url)))` over the hardcoded array `['retrieval.ts', 'resolve.ts']`, joins the contents, filters lines matching `/^\\s*import\\b|require\\(/`, then applies two regexes. All in this unit.\n   - Comment-dependent? No — it rests on the executable code, not the header's scenario-ID claims.\n   - Exact counts? \"Two hardcoded files\" — verified by counting the array literal's two string elements.\n   - Beyond name/signature? Yes: the title says \"import closure,\" which naturally suggests a dependency-graph traversal, but the code only greps the raw import/require lines of those two specific files — no transitive walk. An indirect vector dep arriving through some imported module would escape this check, and a new retrieval-path file must be manually added to the list. That changes what an engineer does (extends the list, or distrusts the guard for indirect deps).\n   - Bonus verifiable detail in same lines: the second regex bans every `require(` outright, enforcing pure-ESM imports on those files.\n\nSTEP 2 — RESULT\n\n```atlas-fact\n{\"claim\": \"The no-embeddings guarantee (SCN-INDEX-7) is enforced at runtime by reading exactly two hardcoded files (src/retrieval.ts and src/resolve.ts) via readFileSync and regex-scanning their import/require lines — it inspects only those two files' direct import statements, not a computed transitive dependency closure, and separately bans every require( call.\"}\n```"