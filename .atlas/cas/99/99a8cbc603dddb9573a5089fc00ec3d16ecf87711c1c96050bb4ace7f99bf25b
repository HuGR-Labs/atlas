"STEP 1 — scratch reasoning (not stored):\n\nCandidates drafted from a close read of the import block, fixtures, and gates:\n\n1. \"runExtract halts after exactly 20 calls / spends exactly 200\" — these are *expectations* on `../src/extract.js`, whose implementation is not in these bytes. Truth rests on invisible code passing. Discard.\n2. \"MARGINAL_WINDOW is 20 / MARGINAL_MIN_ADMITS is 4\" — pinned literals appear in assertions, but the constants' definitions live outside the shown bytes, and the numbers are already announced in the scenario name/comment. Discard.\n3. \"The `<filled-at-freeze>` digest is simulated / `-2` fixtures are held out\" — comment-only claims about activity elsewhere. Discard.\n4. \"Reading/writing `obviousness` requires `as unknown as` casts\" — the casts are real bytes, but attributing the double-cast *specifically* to the Fact type lacking the field is inference about a type declaration not shown. Too speculative. Discard.\n5. Import topology: re-scanning every import line — `@atlas/genesis` appears only in two `import type` statements (Candidate/Fact/MinedSignals, GenesisBudget); all *runtime* bindings come from `vitest`, `@atlas/kernel` (asSubtreeHash/asNodeKey), and the relative `../src/extract.js` (including the seam types as inline `type`). So the barrel supplies zero executable code to this suite, even though the domain types flow through it. This is checkable purely from the declarations in this unit, survives refutation, is not stated by name/signature, and matters operationally (barrel rewiring/relocation breaks this suite differently than assumed; the suite never exercises the public export surface at runtime). Survives.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"Although the Candidate, Fact, MinedSignals, and GenesisBudget types are imported from the @atlas/genesis barrel, every runtime binding in this suite (runExtract, makeExtract, defaultCeiling, and the two marginal-halt constants) comes from the relative ../src/extract.js, so the package barrel contributes no executable code to this file.\"}\n```"