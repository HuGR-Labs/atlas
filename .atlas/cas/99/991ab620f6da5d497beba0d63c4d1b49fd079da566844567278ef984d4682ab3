"STEP 1 — SCRATCH (not persisted):\n\nReading the KNOW-12 describe block closely. Five `it()` cases live there: 12a, 12b, 12c, 12d, 12e.\n\n- Draft candidate A: \"claimText cap is exactly 512 B.\" Refuted as *novel*: the `it()` title itself already announces \"the 512 B cap\", so it's largely known from the unit's own names; the interesting part sits in a comment. Discard.\n- Draft candidate B: \"predicate versions share one NodeKey because `id` derives only from the first fixture argument.\" True and locally checkable (`asNodeKey(nk)` ignores `claimNorm`), but it's plainly visible in the fixture signature — a competent engineer sees it immediately. Weak novelty. Discard.\n- Draft candidate C: the 12c-1 \"keeps NO lineage pointer\" case never exercises the archive. Refutation attempt: scan the block — `const editedAdvisory = advisoryNode('nk-adv', { scope: 'A' }); expect('supersededBy' in editedAdvisory).toBe(false);`. No `createStore()`, no `bindArchive()` anywhere in that case. Contrast with siblings: 12a uses `bindArchive(createStore())`, 12b likewise, 12d likewise, 12e uses both `createStore()` and `bindArchive(store)`. Counted item by item: exactly four siblings touch the store/archive, 12c does not. The `in` check's outcome is fixed by the object literal built by the local helper (own property absent, no inherited `supersededBy`), so the assertion is satisfied purely by fixture construction — no archive behavior is under test despite the scenario's archival framing. Survives refutation: derivable from control flow alone, does not depend on the comment's claim about the frozen `AdvisoryNode` type in another module, not stated in the title, and materially changes what an engineer would assume about coverage here.\n\nSurvivor: candidate C, phrased qualitatively (verified contrast against the four counted siblings, not a bare numeric claim).\n\nSTEP 2 — RESULT:\n\n```atlas-fact\n{\"claim\": \"In the KNOW-12 suite, the SCN-KNOW-12c-1 'no lineage pointer' case is the only one that never invokes createStore/bindArchive — its assertion is decided entirely by the locally built advisoryNode object literal lacking a 'supersededBy' property, not by any archive behavior.\"}\n```"