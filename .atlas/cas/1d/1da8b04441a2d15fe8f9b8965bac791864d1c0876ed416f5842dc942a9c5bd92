"STEP 1 — Scratch reasoning:\n\nReading the bytes of `draftFact`: the signature advertises a `tier: Tier = 'T1'` parameter, which invites the assumption that it is forwarded to the CLI somehow (a flag, env, or config). Checking the actual body: the subprocess argv is built as a fixed literal `['draft', anchor, slot, claim, '--json']` — there is no tier token in it, and nothing else carries the parameter into `runAtlas`. The tier only reappears after parsing, in `return { fact: { ...env.fact, tier }, ... }`, where the spread order guarantees it overwrites whatever tier the product printed. So the parameter is purely a client-side post-parse mutation; the subprocess always runs under the CLI's own default tier. Refutation attempts: (a) could `runAtlas` inject tier implicitly? Not visible, but my claim is scoped strictly to the constructed argv and the post-parse spread, both fully in these bytes. (b) Is it a stale-comment echo? No — it rests on the literal argv array and the spread expression, not on any comment's claim about `draft.ts` or KNOW-6. (c) Exact-count/enumeration risk? None — it's qualitative. (d) Restatement of name/signature? No — the signature alone suggests the opposite (that tier flows into drafting); only the body shows it never does. Other candidates examined and discarded: the `--at=` parser quirk and the T2-default/tier-not-in-identity claims rest on comments about files not shown; the `emitFact`-vs-`draftFact` exit-code asymmetry is visible but largely inferable from return types; the `subsumesLines` `includes()` looseness is real but weaker and partly depends on unseen render formats; the `let seq` global counter is plausible but low-impact. The tier-argv fact survives.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"In draftFact the tier argument never reaches the atlas draft subprocess — the argv is fixed to ['draft', anchor, slot, claim, '--json'] — and is instead stamped onto the parsed fact afterward via spread order, so the subprocess always runs with the CLI's own default tier.\"}\n```"