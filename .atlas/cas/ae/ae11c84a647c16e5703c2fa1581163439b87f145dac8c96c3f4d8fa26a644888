"```atlas-fact\n{\"claim\": \"A DriftItem's 'semantic'/'mechanical' class is decided solely by Set-membership of its fact id against the classifier's semantic list, with everything else defaulting to 'mechanical' — the classifier's mechanical list is never consulted for labelling, so the per-item labels and the returned mechanical/semantic arrays are derived through two independent paths.\"}\n```"