"STEP 1 — scratch reasoning:\n\nReading the bytes closely, candidates:\n\n1. *\"Rules decay and get archived if `hits` stays flat\"* — tempting, but its truth is pinned to invariant MEM-7 in `../reference/atlas-memory.md#invariants`, a file not shown here. Per the rubric, that's a claim about elsewhere → discard.\n2. *\"Writes omitting a field are rejected fail-closed\"* — likewise attributed to MEM-5 elsewhere → discard.\n3. *\"Knowledge scores obviousness, never gates it\"* — cited to ADR-0012, another unseen file → discard.\n4. *\"Rules are promoted from lived experience, never invented up front\"* — stated directly in Prerequisites; survives on these bytes alone, though a competent engineer might plausibly infer it from the title.\n5. *Token caps*: the Prerequisites literally state \"`≤ ~500 tok` total; orchestrator `≤ ~800`\", and Step 7 adds that an over-cap write \"is rejected — archive a stale, low-`hits` entry first.\" All three clauses sit in these bytes; nothing rests on an unseen file. Re-checked the numbers item-by-item against the text: yes, ~500 (member) and ~800 (orchestrator). Not derivable from the path/title (`write-a-project-rule` suggests concision but not numeric budgets), and it materially changes behavior — an engineer must measure remaining headroom before drafting, not just after.\n\nCandidate 5 survives.\n\n```atlas-fact\n{\"claim\": \"A new project rule is admitted only if it keeps the member's total injected project memory under a hard token cap — roughly 500 tokens for a member and 800 for an orchestrator — otherwise the write is rejected and a stale, low-hits entry must be archived first.\"}\n```"