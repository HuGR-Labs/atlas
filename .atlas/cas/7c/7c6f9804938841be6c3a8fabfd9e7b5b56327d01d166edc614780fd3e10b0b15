"Reading the bytes closely: the page contains three verbatim `promote` transcripts whose `next:` guidance line ends \"(a T2 fact is bounded OUT of the `atlas query` pack, TOOLS-6)\", yet the immediately following `atlas query src` transcript — same repository, immediately after the same promotion — prints `advisory T2 802f2bcde25d… [FRESH]: greet() returns…`, i.e. the promoted T2 candidate is served by `query` under the separately capped advisory band. The page itself adjudicates the conflict (\"Believe the transcript above it, not the sentence\"). Candidate facts I considered and rejected: \"there is no `atlas-promote` MCP tool\" (an absence claim about code not shown); \"`ATLAS_RATIFY_TOKEN` is compared with no verification of any kind\" (absence claim about unseen gate code); \"llmCalls counts sites, not model calls\" (rests on `run-controller.ts`, not in these bytes); exit-code table entries (partially present, but the load-bearing \"1 is unreachable\" leans on an unseen test seam). The guidance-line contradiction survives because both sides of it are literal, comparable blocks inside this unit, it is not derivable from the command's name or invocation shape, and it changes behavior: a consumer trusting the printed hint (or the Related-section claim that query will not show a promoted T2 fact) would wrongly assume machine proposals never enter their query pack.\n\n```atlas-fact\n{\"claim\": \"The `atlas query` transcript on this page serves the just-promoted T2 candidate as an `advisory … [FRESH]` row, even though the guidance line `atlas promote` prints in its own transcripts insists a T2 fact is bounded OUT of the `atlas query` pack — so promoted mined facts do appear in query output, as per-row-freshness proposals rather than ratified knowledge.\"}\n```"