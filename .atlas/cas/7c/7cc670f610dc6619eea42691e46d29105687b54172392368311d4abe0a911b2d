"STEP 1 — scratch reasoning (not persisted):\n\nScanning the bytes: fixtures, injected `Mint`/`SealLeg`s, `MOMENTS`, and the assertions. Comments about WP numbers, goldens docs, held-out `-2` fixtures, and other WPs all reference files/decisions outside this unit — discarded as inadmissible.\n\nCandidate A: \"output preserves candidate order (`['cn-a','cn-b']`)\". Survives byte-check, but with two near-identical candidates it reads as incidental to the admission/count intent; weaker signal of deliberate contract.\n\nCandidate B: \"`sealProbe` treats a non-empty string as 'grounded why-not'\". Restates the fixture logic plus the comment; close to a summary of what the legs plainly do.\n\nCandidate C: the off-union tags are built via double casts (`'repo-wide-sweep' as unknown as ProductionMoment`). Visible, but largely mechanical TypeScript necessity given the frozen closed-set type; an engineer would infer it from the signature alone.\n\nCandidate D: the assertion `producer.produce('enrich' as unknown as ProductionMoment, …).length).toBe(0)` — comparing literals in the bytes, `'enrich'` is a strict prefix of the admitted moment `'enrich-by-blast-radius'` listed in `MOMENTS`. So the unit actively pins exact-tag equality: any prefix/substring matching would admit it and fail this assertion. Not derivable from the name (\"moment-gated\") or signature; not stated in the comment (which only frames `''` as the sweep case); rests solely on comparing two literals and one `.toBe(0)` inside these bytes; qualitative, no counting. Refutation attempts: is it a comment claim about elsewhere? No. Overclaim? Phrased as \"asserted to produce zero facts / rules out prefix matching,\" the entailment holds within the shown bytes. Survives.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"The negative-case assertion that the tag 'enrich'—a strict prefix of the admitted moment 'enrich-by-blast-radius'—produces zero facts pins the moment gate to exact tag equality, ruling out any prefix-based matching.\"}\n```"