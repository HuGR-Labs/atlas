"STEP 1 — REASON FREELY\n\nReading the bytes closely, focusing on things not stated by names/signatures/comments.\n\nCandidates drafted:\n\n1. *\"No held-out fixtures exist\"* / *SCN provenance* — comes straight from the header comment describing files and generators not present here. Discard (comment about elsewhere).\n2. *\"The parent nodes' own `subtreeHash` is ignored by `subtreeHash()`\"* — tempting because both compared parents store `'ignored'`, but if the implementation folded the stored hash in, both sides would still match. Not conclusively derivable from these bytes. Discard.\n3. *\"The `h` helper / `asHash` import is dead code, and no `nd(...)` call ever populates `objects`\"* — checkable by scanning every call site (all omit the 5th argument). True, but low action value; a lint pass would surface it.\n4. *\"Generated trees are capped at 4 levels / 3 branches\"* — derivable from `skel(4)` and `maxLength: 3`, verifiable arithmetic. Useful but a plain reading of the literals.\n5. **Strongest candidate:** in the determinism property, `kids` are built as `hashes.map((x, i) => nd(\\`b${i}\\`, ...))` — so keys ascend strictly with index. Then `rev = [...kids].reverse()` yields `b5…b0`, and `shuf` sorts descending by key (`a.key < b.key ? 1 : -1`), which also yields `b5…b0`. Since keys are unique (`b${i}`) the comparator defines a total order, so the two arrays are element-for-element identical in **every** generated case. The second permutation assertion (`shuf`) therefore always re-tests exactly the same ordering as the first (`rev`) — the \"any permutation\" law is actually probed with only one non-identity permutation, twice.\n\nRefutation pass on candidate 5:\n- Derivable from THESE bytes alone? Yes — the key template, the map-index assignment, the comparator direction, and `reverse` semantics are all in the file. No library knowledge needed beyond JS semantics.\n- Rests on a comment's claim about unseen code? No — the comment (\"invariant under any permutation\") is in fact contradicted by what the code does, making this a code-vs-comment divergence, which is fair game since it's checkable in-file.\n- Exact-count risk? Phrased qualitatively (\"always the same ordering\", \"duplicate\"), and I traced both transformations item by item for the general case (unique keys ⇒ descending sort ≡ reverse of an index-ordered array).\n- More than name/signature restatement? Yes — it requires simulating the two transformations against the key-assignment scheme.\n\nSurvives. Emitting candidate 5.\n\n```atlas-fact\n{\"claim\": \"In the PROP-INDEX-2 determinism property, the rev and shuf child orderings are always the identical sequence — keys are assigned as b0…bn in array order, so reversing the children equals sorting them descending by key — meaning the second permutation assertion merely duplicates the first instead of probing a different permutation.\"}\n```"