"STEP 1 — scratch reasoning (not persisted):\n\nCandidate A: \"whole-file pins are 8-hex but per-block idx pins are 12-hex.\"\nRefutation attempt: Is it in these bytes? `digestModule` does `.digest('hex').slice(0, 8)` (8 chars); the stale whole-file pin literal is `'deadbeef'` (counted: 8); the expected missing-pin message literally says `no 8-hex @sha256 pins found` (code, not comment). For the idx path, the pin literal `'deadbeefcafe'` counts to 12 chars, and the expected output regex demands `[0-9a-f]{12}` for the gate-computed block digest. Both directions corroborated by literals/assertions in this file alone; no reliance on comments about unseen code, no external-library assumptions. Counts re-checked item by item. It is not derivable from the filename/signature, and it would change what someone does when authoring or validating pins (which width per pin kind). Survives.\n\nCandidate B: \"one mutated READ_SURFACE yields two violations in a single run.\" Checkable (two matchers, one `runGate()` call), survives, but weaker payoff than A.\n\nCandidate C: \"gate is spawned as a subprocess with `SPEC_CONFORMANCE_GUARD_ROOT`, output text is the contract.\" Checkable in `runGate`, but partially anticipated from a test file's nature; A is sharper.\n\nDiscard B and C; A survives verification.\n\n```atlas-fact\n{\"claim\": \"The two digest tripwires enforced by these tests use different pin widths: whole-file properties-doc @sha256 pins are eight hex characters (the fixture helper slices the SHA-256 to 8 chars and the expected missing-pin message says '8-hex @sha256 pins'), whereas per-INV-block idx pins are checked against twelve hex characters (the assertion expects a [0-9a-f]{12} block digest versus the pin deadbeefcafe).\"}\n```"