"STEP 1 — REASONING (scratch):\n\nReading the bytes closely:\n\n- The filename says \"compose-runtime\", which primes an engineer to expect runtime composition logic (factory functions, wiring, closures over a store).\n- What's actually here: 12 import statements, **every one** of them `import type` (checked line by line — `@atlas/contracts`, `@atlas/tools`, and ten local `./x.js` modules, all type-only), followed by a single `export interface ComposedRuntime { … }` declaration. No function body, no constant, no class, no value export anywhere in the file.\n- Refutation attempts: Is this a comment's claim about elsewhere? No — the header comment talks about `compose.ts` re-exporting and import sites staying untouched, which I am *not* using; my candidate rests only on the import keywords and the sole `export` keyword visible in this file. Is it an exact count/exhaustive list? I initially drafted \"12 type-only imports,\" but I'll keep it qualitative (\"every import\") since the number isn't the point — the qualitative form is still fully checkable against the bytes. Is it a restatement of name/signature/types? No — it's a module-level structural property that contradicts what the *filename* implies, and it isn't visible from any single member signature; you only see it by noticing the file contains literally nothing else.\n- Does it change what an engineer does? Yes: you cannot import or mock any runtime value from this module, there is nothing here to unit-test, and bundler/type-stripping eliminates it entirely — anyone looking for the actual composition logic must look elsewhere.\n\nSurvives. STEP 2 — emit.\n\n```atlas-fact\n{\"claim\": \"Despite the 'runtime' in its filename, compose-runtime.ts contains no executable code at all: every import in the file is a type-only `import type` statement and its sole export is the ComposedRuntime interface, with no functions, constants, or value exports anywhere.\"}\n```"