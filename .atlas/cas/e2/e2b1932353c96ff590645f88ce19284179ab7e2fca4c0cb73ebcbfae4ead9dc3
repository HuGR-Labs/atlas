"STEP 1 — scratch reasoning (not persisted):\n\nReading the control flow rather than the prose: on the happy path, the stdout write is gated only on `!isError && !emptyResult`, while `abstained` is computed independently as `isError || emptyResult || isAbstainSentinel(result)`.\n\nDraft fact: when the answer is the `NO-FACT` sentinel, the wrapper emits the answer bytes to stdout *and* marks the sidecar row `abstained: true`.\n\nRefutation attempts:\n- Does it rest on a comment's claim about other files (llm.ts, Atlas's gate, #201/#95)? No — the two statements (`writeSync` under `!isError && !emptyResult`; `abstained` including `isAbstainSentinel(result)`) sit directly in these bytes. Trace with `result === 'NO-FACT'`: `isError` false, `emptyResult` false → `writeSync(1, …)` runs; then `abstained` → true. Proven by this unit alone.\n- Exhaustive/count-based? I checked each disjunct: `isError` ⇒ no write; `emptyResult` ⇒ no write; only the sentinel disjunct permits both write and `abstained: true`. I'll phrase qualitatively anyway, avoiding \"only/every\".\n- Restating name/signature? The name (\"metered wrapper\") tells you nothing about abstention-labeling semantics vs. stdout passthrough; this is a genuine cross-path behavioral consequence.\n- Stale-comment risk? Irrelevant — the claim survives with all comments deleted.\n\nSurvived. STEP 2 — emit:\n\n```atlas-fact\n{\"claim\": \"When the model's answer matches the NO-FACT sentinel (case-insensitively, after trimming surrounding quotes/punctuation), this wrapper both writes that answer text verbatim to stdout and records the sidecar row as abstained: true, unlike error or empty-result cases which emit nothing.\"}\n```"