"STEP 1 — scratch reasoning (not persisted):\n\nReading the bytes closely, several candidate facts surface:\n\n1. **Round-robin dispatch** (`seats[slot % seats.length]`) — true, visible, but low impact; an engineer would guess some distribution scheme anyway.\n2. **`onFault` gated on `error.name === 'ModelCommandError'`** — true and visible in `makeVisitAll`; the signature `(e: Error) => void` alone wouldn't tell you the callback is selective. Solid, but relatively shallow.\n3. **Workers/ports are `unref()`'d** — visible, though partially restated by the adjacent inline comment.\n4. **The `died` liveness counter is sticky across batches** — tracing the control flow: `let died = 0;` is declared once in `createProposerPool`'s scope; the `'exit'` and `'error'` handlers increment it; `proposeAll` resets only the SharedArrayBuffer cell (`Atomics.store(done, 0, 0)`), never `died`. The wait loop's escape hatch is `if (landed < n && died > 0) break;` — evaluated on the *first* iteration after at most one `WAIT_SLICE_MS` (250 ms) wait. So after ANY past worker exit/error event on this pool, every subsequent `proposeAll` call abandons waiting after a single 250 ms slice, drains only already-arrived messages, and faults every slot whose answer hasn't landed — including sites assigned to perfectly healthy workers. Nothing in the name `proposeAll`, the signature, or the types reveals this; it falls out purely from closure lifetime plus the loop's break condition, both fully inside these bytes. No reliance on comments about other files, no counts to mislabel. Refutation attempts: could `died` be reset anywhere? Re-scanned — no. Could the break somehow not fire on iteration one? Only if `landed >= n` already, impossible for freshly posted network work. Could the death flag plausibly never be set in practice? Irrelevant — the property of the code holds regardless of whether the trigger occurs.\n\nFact 4 survives refutation best: it is non-obvious, entirely byte-checkable, and materially changes what an engineer does (they would reset `died` per pass, or treat any prior worker death as poisoning the pool for all later passes).\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"The pool's worker-death counter `died` is never reset between calls, so once any worker has fired an `exit` or `error` event — even during an earlier, otherwise successful pass — every later `proposeAll` gives up waiting after a single 250 ms slice and reports any site whose answer has not yet landed as a fault.\"}\n```"