"STEP 1 — scratch reasoning (not persisted):\n\nReading the bytes: SCN-KNOW-8b-2 stages an advisory candidate (slot `'rationale'`, no `check`) and asserts `ratify(stage(adv), { by: 'lead' }).committed` is `true`, while `{ by: '' }` is `false`. SCN-KNOW-8c-2 stages the same base candidate overridden to `tier: 'T0'` and asserts the *opposite* outcome for the identical `'lead'` token (`committed === false`) while `{ by: 'billy' }` commits.\n\nRefutation pass:\n- Derivable from these bytes? Yes — both assertions sit in this file's code; no reliance on the ratifier's implementation elsewhere. I am checking expectations written here.\n- Comment-dependence risk? The header comment does summarize \"payments/ T0 requires billy,\" but my candidate fact does not rest on that line — it re-derives from the paired assertions. Moreover, the *asymmetry* (the same `'lead'` token succeeding for T2 advisory and failing for T0) appears nowhere in the comments; it emerges only from comparing 8b-2 and 8c-2. That part survives cleanly.\n- Exact count / exhaustive enumeration? None claimed — I deliberately avoid saying \"'lead' is *never* accepted for T0\" or \"only billy can,\" since the bytes show two data points per branch, not an exhaustive actor table.\n- More than name/signature? The file name (`tier-ratify.heldout.test.ts`) and the `ratify(stage(cand), { by })` signature reveal nothing about token identities or their tier-dependent acceptance. Survives.\n- Alternative candidates considered and dropped: the \"invariants key is `undefined` rather than an empty array\" detail (plausible but closer to restating the zero-genesis scenario), and the skeleton-keys/family tension in 6b-2 (real but interpretive, risks over-reading non-enumerability semantics).\n\nThe billy/lead contrast survives every check.\n\n```atlas-fact\n{\"claim\": \"The same 'lead' ratifier token that commits a staged T2 advisory candidate in this suite is rejected for a T0-tier candidate, which instead commits when ratified by the named actor 'billy'.\"}\n```"