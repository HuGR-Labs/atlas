"STEP 1 — Reasoning (scratch):\n\nReading the bytes closely. Candidates drafted and tested:\n\n- *Differential leg unavailable/subsumed*: rests on claims about `grounding/ref/*.ts` freezing and the scaffold-freeze — files/events not in these bytes. Discard.\n- *\"38 goldens linked\"*: an asserted count I'd have to re-tally item-by-item across all 13 props; error-prone and numeric for no reason. Discard.\n- *Obviousness scored, not gated (PROP-7)*: largely given away by the prop's own title (\"obviousness scored\"). Weaker than it looks.\n- *Whole-fact collapse on unresolvable citation (PROP-3)*: mostly telegraphed by the title \"fail-closed, total resolution\".\n- *Whitespace reformat ⇒ DRIFTED under PROP-5*: check the law text in these bytes — \"FRESH ⟺ subtree(U) after δ = subtree(U) before δ (byte-identical raw source slice, NFC only)\", with the arbitrary putting \"whitespace reformat of U\" and \"edit to a CONTIGUOUS leading comment/header\" in the TOUCHING (⇒DRIFTED) class, while \"import added above\", \"blank-line-separated license header above\" sit in the NON-TOUCHING (⇒FRESH) class. The amendment confirms the current law is the byte-identical form (the normalizer-based version was replaced). Every element is stated in this unit's own law/arbitrary/amendment text; the ADR/method-tags mentions are attributions, not load-bearing. This survives: it is counter-intuitive (most engineers assume formatting is normalized away), it is not derivable from the path or the section title, it directly changes how one partitions PBT generators, and it avoids exact counts.\n\nSurviving fact identified.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"In PROP-GROUND-5, FRESH requires the cited unit's raw source slice to remain byte-identical (NFC only) — a whitespace-only reformat of the unit reads DRIFTED, as does an edit to its contiguous leading doc-comment — while line-shifting non-touching edits (an import or blank-line-separated license header added above) still read FRESH.\"}\n```"