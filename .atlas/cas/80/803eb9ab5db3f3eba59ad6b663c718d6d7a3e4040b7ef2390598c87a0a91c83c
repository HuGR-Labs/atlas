"Looking at the bytes:\n\n- Candidate A: \"dep-F is uncapped.\" True in code (`UNCAPPED.has(k) || n <= CAP`), but it is the explicit subject of a large adjacent comment block — it's essentially already advertised at this site, and the comment carries most of the signal. Weaker candidate.\n- Candidate B: \"negation rows never go through `admit`.\" Visible in `driveClaim` (calls `B.judge`), but arguably a plain summary of the dispatch chain once you read it.\n- Candidate C: the `proven` flag has two different computation paths inside this unit. Checking: `adapt` computes `proven: (a.fact as { seal?: string }).seal === 'proven'` — derived from a seal field on the Admission. `adaptJudge` returns `proven: true` unconditionally for any `'admit'` verdict. Scanning the unit for every producer of a `proven` value confirms exactly these two sites feed `DrivenRow.proven` (via `push`, `redrive`, `driveClaim`). Nothing else touches the flag. This is a genuine intra-unit asymmetry, checkable by comparing the two adapter functions' code, rests on no comment's claim about other files, involves no unverifiable count, and is invisible from the `proven: boolean` signature. It changes what a consumer does: `proven` cannot be read uniformly as \"carried a proven seal\" — negation-door admits are marked proven by fiat.\n- Candidate C survives refutation attempts.\n\n```atlas-fact\n{\"claim\": \"The `proven` flag on driven rows is computed two different ways inside this unit: `adapt` derives it from a `seal === 'proven'` field on the Admission's fact, while `adaptJudge` hardcodes it to true for every 'admit' verdict coming from the negation door.\"}\n```"