"STEP 1 — scratch reasoning (not persisted):\n\nScanning the document for anything not derivable from titles alone: most clauses state their law in heading, body, and quoted normative-clause consistently. Checking each prohibition clause's quote against its body polarity: REQ-TOOLS-1c keeps \"no back-channel write may bypass it\"; 5c keeps \"MUST NOT set any tier above `T2`\"; 11-a keeps \"A seat MUST NOT be forced…\"; 13c keeps \"It MUST NOT auto-touch…\". But REQ-TOOLS-5d's heading reads \"never auto-promote a T0\" and its body reads \"`atlas-init` shall not promote it automatically,\" while its quoted `normative-clause` string is just \"promote a `T0` automatically\" — the negation is absent from the stored quote.\n\nRefutation pass: Is this checkable in these bytes? Yes — it's a direct comparison of three adjacent lines in the 5d block plus one sibling comparison; it does not depend on any HTML comment's claims about other files (handler.ts, req-clause-guard.mjs, PRs/commits — those are discarded as out-of-unit). Is it an unverifiable exhaustive count? No — I phrase it via concrete comparisons, not \"the only one among N.\" Is it a restatement? No — a reader trusting the heading/body would assume the quote matches; the mismatch is a latent defect that would invert the law if the quote were lifted verbatim. Survives.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"REQ-TOOLS-5d's stored normative-clause quote reads just \\\"promote a `T0` automatically\\\" — dropping the prohibition its own heading (\\\"never auto-promote a T0\\\") and body (\\\"atlas-init shall not promote it automatically\\\") both state, and unlike sibling prohibition quotes such as REQ-TOOLS-5c's \\\"MUST NOT set any tier above `T2`\\\" — so re-lifting this quote verbatim yields the opposite law.\"}\n```"