"STEP 1 — Scratch reasoning (not persisted):\n\nReading the register closely. Candidate observations:\n\n- *Gate/COLD-REVIEW pending, DoD NOT MET* — plainly declared in bold in the header and footer; a near-restatement of the document's own self-description. Discard as too much a summary of what the unit plainly announces.\n- *GOVERNANCE_SURFACE=5, WRITE_PATHS=2 unchanged* — rests on claims about ADR-0003 and INV-TOOLS-1, files not in these bytes. Discard per rules.\n- *AUTH is new vs. CLI/MCP extending numbered families* — the header states this outright; borderline restatement. Discard.\n- *Anchor patterns* (`#author-N`, `#entry-cli-N`) — mechanical, low action value. Discard.\n- *method-tag fields*: inspecting every entry — INV-AUTH-1 through INV-AUTH-14, INV-CLI-5, INV-CLI-6, INV-MCP-3, INV-MCP-4 — each ends with a bare `method-tag:` line carrying no value. I listed and re-checked all entries: 14 + 2 + 2 = 18, all blank. Cross-check against the Completeness table: its predicates certify text, anchor, `behavioural`, exempt reasons, design questions, owners, contradictions, clause normativity, and outcome citations — method-tag population appears nowhere in the checklist. So the entire surface has no assigned verification method, and the register's own audit table doesn't flag that dimension.\n  - Refutation pass: derivable solely from these bytes? Yes, by direct enumeration of the entries. Resting on a comment about unseen code? No — it concerns the entries themselves. Exact-count risk? I counted the 18 entries myself and phrased qualitatively (\"every\") while the count is corroborated. More than a name/signature restatement? Yes — nothing in the filename or the table reveals that this field is uniformly unpopulated.\n  - Survives.\n\nSTEP 2 — Emit result:\n\n```atlas-fact\n{\"claim\": \"Every invariant entry in this register (AUTH-1–14, CLI-5–6, MCP-3–4) leaves its `method-tag:` field completely empty — no invariant records a verification method — and the Completeness checklist certifies text, anchors, behaviour flags, owners, and citations without ever including method-tag coverage among its predicates.\"}\n```"