"Looking at the bytes: the interesting spot is `keyOf`. Its name and signature suggest it simply forwards the fact to `nodeKey`, but the body does something more: it builds `view` as `{ ...fact, slot }`, where `slot` is a *separate* key mirroring `predicateSlot` (or `undefined` for other kinds). The spread retains the fact's own properties, so the object handed to `nodeKey` can carry both `predicateSlot` and the injected `slot` alias — nothing is deleted. Checking this against refutation: it rests only on plain object-spread/shorthand semantics visible in these lines, not on any comment's claim about other files; it is not an enumeration/count; and it is not derivable from the name `keyOf(fact): string` alone. Other candidates (e.g. \"two gens share one nodeKey\", \"a suite passes a non-string scope\") fail because their truth lives in `nodeKey`/other suites, not in these bytes; `withScope`'s key-omission is real but far closer to what its signature implies. The `keyOf` injection survives.\n\n```atlas-fact\n{\"claim\": \"In keyOf, the object passed to nodeKey is not the fact itself but { ...fact, slot }: the fact's own properties (including predicateSlot) are kept and a second, separate `slot` key — mirroring predicateSlot, or undefined — is injected into the view.\"}\n```"