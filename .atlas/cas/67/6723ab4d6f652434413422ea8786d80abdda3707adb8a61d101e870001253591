"Step 1 — Scratch reasoning (not persisted):\n\nScanning candidates against the bytes:\n\n1. *\"Every forge op takes a `via` tag\"* — visible in every method signature; restatement of the types. Discard.\n2. *\"The dossier round-trips through the adapter\"* — asserted in the test but its truth depends on `makeHostAdapter`'s implementation, which lives outside these bytes. Discard.\n3. *\"`readAfterBareClone(c2, 'N', true)` is null post-rewrite\"* — grounded in the shown arguments and assertions, and the third parameter's gating semantics are demonstrated by sibling call sites in this file. Viable, but partially spelled out by the inline `/* even with refspec */` comment.\n4. *\"`bareClone()` copies only commit state\"* — check the code directly: the body creates a fresh `makeFakeForge()` and replays exactly one thing, a loop over `commits` calling `clone.writeCommit(...)`. Nothing touches `prs` or `refspecs` when constructing the clone, so the clone begins with an empty PR map and zero configured refspecs. This is derivable from the function's control flow alone, not from any comment's claim about unseen code. The signature `bareClone(): Forge` gives no hint the clone discards push-refspec configuration, and a competent engineer assuming clone-inherits-config would write wrong tests (e.g., expecting note reads to work in a clone without re-establishing the refspec). I enumerated what is/isn't copied item-by-item from the loop body — only `commits` is replayed; both `prs` and `refspecs` are omitted. Qualitative form, no fragile count. Survives refutation.\n\nCandidate 4 is the strongest: fully self-contained in this unit's code, non-obvious from name/signature, and action-changing.\n\nStep 2 — Emit result:\n\n```atlas-fact\n{\"claim\": \"In makeFakeForge, bareClone() populates the clone solely by replaying writeCommit over the parent's commits map, so the clone inherits neither the host-side PR bodies nor the parent's configured push refspecs.\"}\n```"