"STEP 1 — SCRATCH REASONING\n\nCandidates considered and refuted/survived:\n\n1. *Env-gated substrate block* — stated loudly in the header comment and directly visible in `describe.skipIf(!RUN)`; an engineer skimming the file learns it instantly. Too close to restating the header. Discard.\n\n2. *Synthetic witness ignores `scope`* — true and code-visible in the lambda, but the adjacent comment spells out exactly this semantics (\"TRUE iff target starts with 'hot'… fixed 2\"), so it barely exceeds what the unit volunteers. Weak.\n\n3. *Dead-looking guard in AC-12* — `if (d.outcome.admitted) continue` looks unreachable after the filter, but it almost certainly performs TypeScript union narrowing so `.anchorFailed` is accessible; calling it a bug or dead code would be wrong. Discard.\n\n4. **Absence of numeric-rate assertions in the substrate block** — I enumerated every `expect` in the `describe.skipIf(!RUN)` block: AC-4 is categorical; AC-5/AC-12/AC-13/AC-9m are structural/regex/predicate checks; AC-15 is run-vs-run equality. The only assertions touching *measured rates* are: `not.toBeNull` on negation and relation false-admits, one relational `toBeGreaterThan` (teeth rerun must exceed shipped), and per-row backing predicates for dependency/count (`refInScope` true; `inScopeRefFiles >= atLeast`). There is **no** `toBe`/`toBeCloseTo`/threshold on any scored percentage — unlike the pure block's AC-3, which does use `toBeCloseTo` against fixture numbers. All of this is checkable from the assertion statements themselves in the shown bytes; the supporting comments happen to agree, but the fact does not *rest* on them. It is not derivable from the file's name/signature, and it changes what an engineer does: tuning the gate will not turn this suite red on percentage drift for negation/relation — one must read the `console.log` output.\n\nSurvivor: candidate 4. STEP 2 — RESULT\n\n```atlas-fact\n{\"claim\": \"The gated substrate suite asserts no numeric bound on any measured rate: dependency and count false-admits are enforced only by per-row predicates demanding in-scope tsc-reference backing, negation only by non-null checks plus one strict inequality (the blinded-teeth rerun must exceed the shipped gate), and relation by a lone non-null check — so the printed percentages can drift without failing any assertion in this file.\"}\n```"