"Looking at the bytes: both interfaces carry a long series of optional \"carrier\" fields, and comparing their declared types against the file's imports (`Tier` from `@atlas/contracts`, `PredicateSlot`/`Seal` from `../types.js`) reveals a split typing discipline. Candidates considered and refuted:\n\n- \"Type-only import cycle with router.ts\" — the cycle half rests on `router.ts`, which isn't shown; discarded.\n- \"The store round-trips rows unrewritten / wire serializes whole nodes\" — every such claim points at `store.ts`/`sidecar-commit.ts`/ADRs/issues outside these bytes; discarded.\n- \"Exactly N fields shared between the shapes\" — verifiable but numeric and near-restatement; weaker.\n- \"WriteRequest lacks `sameAs`/`derivedAt`/etc.\" — visible but essentially just reading the field lists.\n- Surviving candidate: `relationKind` and `shape` are declared as bare `string` in *both* interfaces, while their sibling optional carriers `slot`, `seal`, and `tier` use imported domain types. This is checkable purely from the declarations and import lines here, it's not apparent from any single signature, and it changes what an engineer does: no compile-time vocabulary enforcement or rename-safety applies to those two fields at this seam, unlike their siblings.\n\n```atlas-fact\n{\"claim\": \"In both WriteRequest and CurrentNode, the `relationKind` and `shape` carriers are declared as bare `string`, even though sibling optional carriers like `slot`, `seal`, and `tier` are typed with imported domain types (`PredicateSlot`, `Seal`, `Tier`).\"}\n```"