"Looking at the bytes: the `it` titles already announce \"mutate/delete … is rejected\", so rejection per se is visible from the names inside the unit. What goes *beyond* those titles is the specific mechanism pinned by the executable assertions: the tampered re-append does not throw and returns an ordinary snapshot (`after`), the original payload is still served under the id, and `after.size` stays at 1 — i.e., a silent first-write-wins no-op rather than an error or an overwrite. The property test independently encodes the same oracle as plain control flow (`if (!seen.has(o.id)) seen.set(o.id, e)`), so the fact rests on code, not on the comments about WP-1.1, held-out fixtures, or goldens elsewhere. Snapshot immutability (old `size`s staying fixed, `delete` on a snapshot not propagating) is also a real candidate, but the duplicate-append contract is the one that changes what a consumer/implementer does with `createLog` and isn't recoverable from the test names alone.\n\n```atlas-fact\n{\"claim\": \"Appending an event whose id already exists is a silent first-write-wins no-op rather than an error or overwrite: append returns a normal snapshot in which the originally stored payload survives and the size is unchanged, as pinned by SCN-KERNEL-4b-1's assertions and mirrored by the property oracle's 'first write sets the expected event' logic.\"}\n```"