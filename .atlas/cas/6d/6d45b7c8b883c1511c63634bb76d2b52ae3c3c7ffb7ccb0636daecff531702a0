"STEP 1 — Reasoning (scratch):\n\nReading the bytes: three SCN-5a-1 tests build an oracle from the real `@atlas/index` functions and assert the adapter matches it. Scanning for candidates:\n\n1. *Spy asserts exactly one resolve call* — restated almost verbatim by the test title (\"exactly once per call\"). Discard.\n2. *Real functions, not mocks* — visible in `realDeps()`, but it's essentially a summary of what the code plainly does and is also announced by the header comment. Weak.\n3. *Hash↔NodeKey bridging via double cast through `unknown`* — the cast is visible, but the interesting part (why it's legal: same-string brands) rests on the comment plus knowledge of `@atlas/contracts` not shown. Partially comment-dependent.\n4. *blast radius contains app.ts when querying util.ts* — the assertion `toContain(id({ file: 'src/app.ts' }))` is in the bytes, but its significance (app imports greet from util) comes from the comment describing fixture contents not present here. Leans on unseen fixture.\n5. *`cover` output pinned to `invariants: []`, `stale: false`* — visible via `toEqual`, viable, but partially mirrors the oracle construction.\n6. **Territories projection semantics** — the loop `for (const t of got) { expect(t.owner).toBe(''); expect(t.globs).toEqual([`${t.name}/**`]); }` plus `got.map(t => t.name)` vs `axes.territory.children.map(n => n.key)` is fully self-contained in these bytes. The test title says \"(owner/globs projection only)\" but does NOT reveal that owners are the empty string and globs are mechanically synthesized as `<name>/**` from the name itself. An engineer assuming owners come from real data would behave differently. Universal quantifier is safe: it's enforced by a `for…of` loop over all elements, not a counted enumeration. Survives refutation.\n\nSTEP 2 — Emitting the survivor:\n\n```atlas-fact\n{\"claim\": \"The adapter's territories() is pinned by this suite to purely synthetic projections: each returned entry's name is taken verbatim from the territory axis children's keys, its owner must be the empty string, and its globs must be exactly one '<name>/**' pattern generated from the entry's own name.\"}\n```"