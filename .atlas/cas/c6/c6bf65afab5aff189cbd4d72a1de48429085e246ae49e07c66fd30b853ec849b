"STEP 1 — scratch reasoning:\n\nReading the bytes of SCN-MEM-8c-1 closely:\n\n```ts\nconst overCap = entry('#42', { decisions: 'x'.repeat(LOGBOOK_SECTION_CAP + 1) });\nexpect(validateLogbookEntry(overCap).valid).toBe(false);\nconst store = makeLogbookStore();\nstore.append(LOGBOOK_AUTHOR, overCap);\nexpect(store.size()).toBe(0);\nstore.append(LOGBOOK_AUTHOR, entry('#42'));\nexpect(store.size()).toBe(1);\n```\n\nCandidate facts drafted:\n\n1. *LOGBOOK_AUTHOR equals the string 'orch'* — REFUTED. The literal 'orch' appears only in comments describing `../src/logbook.js`, which is not in these bytes. The tests only reference the imported constant; its value is not re-derivable here. Discard.\n2. *The real injector is never imported* — REFUTED as a fact worth emitting; the import list shows only `template.js` and `logbook.js`, but the \"sealed sibling facet\" framing is comment-borne context about other files. Discard as resting on out-of-unit claims.\n3. *`store.append` itself runs the section-cap gate* — checking: the rejected and accepted appends use the SAME author (`LOGBOOK_AUTHOR`) and the SAME prId; the only delta is `decisions` exceeding `LOGBOOK_SECTION_CAP`. Size goes 0 → 1, proving the store mutates nothing on the over-cap write yet still accepts a later valid write. Nothing in the `append(author, entry)` call shape suggests validation; this behavior is asserted only here. It survives: it is checkable by reading these assertions, it is not a restatement of any signature, and it changes implementation (validation must live inside `append`, fail-closed, without poisoning the store).\n4. *render byte-stability across distinct instances* — survives too, but candidate 3 is the more behavior-altering one and is equally verifiable; only one may be emitted.\n5. Count/exhaustiveness check: my chosen claim names one specific scenario (over-cap `decisions` rejected by append, then within-cap accepted) — verified line-by-line against the shown code, no unverified totals.\n\nSurvivor: the append-gate fact.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"The logbook store's append method enforces the section-cap gate itself, not just the standalone validator: an orchestrator-authored entry whose decisions section exceeds LOGBOOK_SECTION_CAP is rejected leaving the log at size 0, and a subsequent within-cap append under the same author succeeds.\"}\n```"