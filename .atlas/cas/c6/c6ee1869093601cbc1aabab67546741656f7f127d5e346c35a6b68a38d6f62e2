"STEP 1 — Reasoning (scratch):\n\nScanning candidates:\n\n- **\"Spawns the built binary / reads off the wire\"** — the header comment claims this, but the code only shows `mcpSession(repo.repoPath)`; the harness internals are not in these bytes. Rests on a comment about unseen code. Discard.\n- **\"Equality instead of toContain is deliberate\"** — rationale lives in comments; the code line alone just says `.toBe(...)`. Discard.\n- **Env save/restore in beforeAll/afterAll** — visible, but standard boilerplate any reader assumes; low value. Discard.\n- **Non-vacuity guard (both bands `.not.toHaveLength(0)`)** — real and code-visible, but fairly guessable from the assertion messages themselves once you open the file. Weaker candidate.\n- **The `stale` gap** — checking the bytes: `McpBody` declares `pack.invariants`, `pack.advisory`, `pack.advisoryDropped`, and `guidance.{next,invariant}` — no `stale` field anywhere. Every payload assertion in S26.4c touches band non-emptiness, nodeId membership, tier disjointness, per-row `freshness`, and `typeof advisoryDropped === 'number'`. The word `stale` appears only inside the `QUERY_DESCRIPTION` literal and in the S26.4b *string* assertions (`d`).toContain('stale')). So although the frozen description promises \"the pack-level `stale` flag\", nothing in this file's code ever reads or asserts such a field off a real response — a pack with no stale flag passes every assertion here. Verified by scanning all declarations and assertions in the shown bytes; absence is *within* this unit, not elsewhere. Qualitative, not a count. Not a restatement of name/signature, and actionable: editing or verifying stale-flag behaviour gives no protection from this suite.\n\nSurvives refutation. Emitting it.\n\n```atlas-fact\n{\"claim\": \"Although the frozen description promises a pack-level `stale` flag, the test's own response type (McpBody) and every payload assertion in S26.4c reference only invariants, advisory, advisoryDropped, and per-row freshness, so a returned pack carrying no stale flag would pass all assertions in this file.\"}\n```"