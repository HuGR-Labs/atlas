"STEP 1 — scratch reasoning (not persisted):\n\n- Candidate A: \"`head` picks MAX contentHash.\" The header *comment* asserts this, but the assertion literals independently prove it: `'gg-high' > 'cc-mid' > 'aa-low'`, `head(ab)?.contentHash === 'gg-high'`, stale-max → `'cc-mid'`, superseding `'zz-tomb'` becomes head, `'bb-x'` (lexicographically below `'cc-mid'`) correctly loses when `'gg-high'` is doubly ineligible. Survives without leaning on the comment.\n- Candidate B: seq-irrelevance. The reseq block scrambles seqs to 900/3/41 and head is still `'gg-high'` — pure code/literals, not just the comment. Merges with A.\n- Candidate C: fold is multi-valued per nodeKey, not last-write-wins. `expect(n.entries.size).toBe(2)` after colliding `a`,`b` on `NK`, plus `entries.size === 2` checks in 12b-2 — conflicts are retained and resolved only at `head`. Verified in bytes.\n- Refutation sweep: the `ev` spread-in trick's \"id-IDENTICAL\" half rests on unseen `canonical.ts` (comment-only) — discard that part. The `logOf` dead initial `new Map()` is real but inconsequential/unverifiable in effect. Line-per-event count (3) is verified but too trivial. Nothing else survives the name/signature bar.\n- Combined surviving fact (qualitative, all literals re-checked): head = lexicographically greatest contentHash among fresh ∧ ¬superseded, seq-proven-irrelevant, and fold retains all colliding entries (size 2) rather than overwriting.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"These tests pin `head` to select the entry with the lexicographically greatest contentHash among fresh, non-superseded entries — proven independent of `seq` by a reseq case (900/3/41) that leaves head at 'gg-high' — while the fold itself retains all colliding same-nodeKey entries (the collided node ends with 2 entries, checked via n.entries.size === 2), so conflict resolution happens only at head-read time, not during fold.\"}\n```"