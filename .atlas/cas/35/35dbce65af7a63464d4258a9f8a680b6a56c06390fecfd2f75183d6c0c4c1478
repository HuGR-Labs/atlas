"STEP 1 — Reasoning scratch:\n\nReading the bytes: this is a small project-reference config. Candidates:\n\n1. *\"It extends a shared base config\"* — plainly visible, low value, essentially a restatement of the `extends` line.\n2. *\"Tests are compiled together with src\"* — derivable from `include: [\"src\", \"test\"]`, but it's a near-direct read of one array literal; weak inferential value.\n3. *\"outDir is the only local override\"* — checkable (one key in `compilerOptions`), but trivially visible next to the `extends` line; a competent engineer sees it instantly.\n4. *\"The package build-depends on five specific siblings via project references: contracts, kernel, persist, knowledge, retrieval\"* — the reference set is not knowable from the filename; it dictates build/rebuild ordering. Refutation pass: am I counting correctly? Re-listing from the bytes: `{ \"../contracts\" }`, `{ \"../kernel\" }`, `{ \"../persist\" }`, `{ \"../knowledge\" }`, `{ \"../retrieval\" }` — exactly five, each verified by name. Am I leaning on unseen files? No — the array itself is the evidence; \"must be built before tools\" follows from project-reference semantics triggered by these very literals, not from any comment or external claim. No exhaustive claim about the *whole repo* is made (I do not assert these are the only packages in the repo, only the five referenced here). Survives.\n\nEmitting the survivor.\n\n```atlas-fact\n{\"claim\": \"Via its project references, the tools package declares build-time dependencies on exactly five sibling packages — contracts, kernel, persist, knowledge, and retrieval — which must be built before tools in a project-references build.\"}\n```"