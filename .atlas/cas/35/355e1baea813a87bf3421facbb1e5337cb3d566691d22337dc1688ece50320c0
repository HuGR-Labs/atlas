"STEP 1 — REASON FREELY:\n\nScanning the bytes for non-obvious, code-checkable facts:\n\n- Candidate A: \"the budget must be passed by reference (`toBe`)\". Derivable from `expect(rec.lastBudget).toBe(budget)`, but it's close to a restatement of the visible assertion.\n- Candidate B: \"the spy records `lastPrompt` but no test asserts on it\". Verifiable in these bytes, but low impact.\n- Candidate C: claims resting on the header/guard comments (\"sole model seam\", \"goldens frozen\", \"module-graph audit\") — these describe files and processes outside these bytes. Discard.\n- Candidate D: the SCN-ADAPTER-11a grep test's filtering logic — mostly a restatement of the visible control flow, and its conclusion (\"exactly one src module\") depends on files not shown. Discard.\n- Candidate E: the SCN-ADAPTER-11c-1 \"touches NO store\" assertion. Reading closely: `expect(Object.keys({ client, budget, buildPrompt }).sort())...` — the object literal is built inline from the test's own local/module variables, so its keys are `'client'`, `'budget'`, `'buildPrompt'` by construction; sorting them trivially equals `['budget', 'buildPrompt', 'client']`. Nothing from `proposer` or `createSiteProposer`'s behavior enters the expression. The assertion therefore passes unconditionally and verifies nothing about the proposer's actual dependencies — contradicting the adjacent comment's implied teeth. Refutation attempt: is any input to the expression produced by the system under test? No — `client` is the test's own spy, `budget` and `buildPrompt` are test fixtures. Survives.\n\nSTEP 2 — EMIT THE RESULT:\n\n```atlas-fact\n{\"claim\": \"In the SCN-ADAPTER-11c-1 test, the 'touches NO store / only injected deps' assertion is vacuous: it inspects an object literal constructed inline from the test's own local variables ({ client, budget, buildPrompt }), so it passes regardless of what createSiteProposer actually receives or touches.\"}\n```"