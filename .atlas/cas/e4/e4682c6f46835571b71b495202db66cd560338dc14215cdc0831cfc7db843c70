"```atlas-fact\n{\"claim\": \"Although this file's header comment advertises pinning a '` @ ` last-delimiter rule' for a `DEPENDS-ON: <target> @ <scope>` grammar, the executable assertions do the opposite: a line with a trailing ' @ <scope>' (e.g. 'DEPENDS-ON: foo @ lib') is asserted to abstain with the same DEP_UNPARSEABLE_REASON sentinel as garbage prose, and every parse that succeeds anywhere in the file ('DEPENDS-ON: ledgerModule', 'depends-on: foo', and the proposer-fed 'DEPENDS-ON: foo') uses a bare name with no ' @ ' at all.\"}\n```"