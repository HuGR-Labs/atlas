"Looking at this unit closely: the filename and title (\"Delete the dead … authz decision … and ENFORCE the closed-13 predicateSlot vocabulary\") prime an engineer to expect a wholesale deletion of a dead authz module plus a new vocabulary gate.\n\nDraft candidate 1: *the \"dead\" authz module was actually partly live.* Refutation attempt: is this derivable from these bytes? Yes — the marker table shows `KNOWLEDGE.isScope | YES` on the successful instrumented write while `KNOWLEDGE.authz` and `KNOWLEDGE.inScope` show `never`, and D1 plus correction #1 state explicitly that `isScope` is kept. I must avoid the unverifiable externals (\"three production call sites\", \"every governed write from three sites\") since those counts cannot be checked here; the qualitative form grounded in the recorded probe survives. It is not a restatement of the title (which suggests the opposite), and it changes what an engineer does (do not `rm` the file).\n\nDraft candidate 2: *the gate refuses only PRESENT-and-unrecognised slots; absent stands aside.* Also in bytes (D4, the pre-fix table row `absent → exit 0`, the s29 case list). Survives too, but candidate 1 directly contradicts the unit's own headline framing and is the correction the card itself ranks first; it is the stronger non-obvious fact.\n\nDraft candidate 3: *M6 survived its first run* — discarded, since its truth leans on `vitest.config.ts`, a file not shown here. Draft candidate 4: *\"15 → 16 header count\" / \"398 of 400 LOC\"* — exact figures asserted about unseen files, not countable in these bytes; discarded per the counting rule.\n\nCandidate 1 survives.\n\n```atlas-fact\n{\"claim\": \"The authz module slated for deletion was not wholly dead: on the instrumented shipped-binary run recorded here the isScope marker fired on a successful governed write while authz and inScope never fired, so the fix removes only those dead exports and keeps isScope as the live scope-shape guard.\"}\n```"