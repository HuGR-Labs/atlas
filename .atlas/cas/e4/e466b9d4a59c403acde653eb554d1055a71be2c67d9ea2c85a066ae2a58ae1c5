"STEP 1 — REASONING (scratch):\n\nReading the bytes closely. Candidate facts drafted and stress-tested:\n\n- *Candidate 1: \"Orientation is order-independent\"* — the describe/it titles literally say \"order-independent\" and \"fold\". Discarded: telegraphed by names.\n- *Candidate 2: \"persistAsWrittenEntry throws with /derived-only/\"* — the it-title already contains \"derived-only\". Discarded: known from the test name.\n- *Candidate 3: \"cap is 250 / over-cap is rejected\"* — both the numeric value and \"rejects\" appear in the SCN-MEM-6d-1 it-title (\"injects O_ok (240 ≤ 250) and rejects O_over (300 > 250)\"). Discarded.\n- *Candidate 4: \"folds only the tail delta, folded === 2\"* — stated outright in the SCN-MEM-12c-1 title. Discarded.\n- *Candidate 5: parents wired via `contentHash`, log keyed via `id`* — genuinely visible only in the fixture code (`prev = e.contentHash` vs `m.set(e.id, e)`), not in any name or title. Viable, but it concerns a private test fixture helper, lower action-impact.\n- *Candidate 6: `injectOrientation` returns the SAME instance it received* — `expect(injectOrientation(oOk)).toBe(oOk)` asserts reference equality (Object.is), i.e. a validated pass-through of the original object, not a copy, wrapper, or normalized payload. Nowhere in the function's name, the describe title, the it title, or any comment is the identity-return stated; the titles only cover accept/reject and the numeric threshold. Survives refutation: checkable from this unit's own assertion bytes, rests on no cross-file comment, involves no unverified counts. It changes what an implementer/consumer does (must not clone or transform; callers get referential identity).\n\nCandidate 6 survives. Emitting it.\n\nSTEP 2 — RESULT:\n\n```atlas-fact\n{\"claim\": \"injectOrientation returns the very same Orientation instance it was given when within the token cap — the test asserts reference equality via toBe — so successful injection is a validated pass-through of the original object, not a copy, wrapper, or transformed payload.\"}\n```"