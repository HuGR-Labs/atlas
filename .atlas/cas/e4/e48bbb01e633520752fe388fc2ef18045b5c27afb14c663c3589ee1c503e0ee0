"STEP 1 — scratch reasoning (not persisted):\n\nScanning the bytes for candidates beyond the file's name (\"transport writedoor e2e\"):\n\n- Candidate 1: \"GOVERNANCE_SURFACE is exactly five named tools, WRITE_PATHS exactly two.\" Survives as a counted enumeration (I listed both literal arrays), but it is largely the headline claim of the file/header and is the kind of thing the name gestures at; weaker \"changes what you do\" value than alternatives.\n- Candidate 2: \"handler.handle never throws — missing leg and throwing leg both yield structured rejected verdicts with guidance.\" Checkable via the try/catch and expects, but it's the explicit subject of section (2) and close to the comment's own framing.\n- Candidate 3: \"`handler.schema.length === 1` enforces no transport parameter via function arity.\" Real and checkable, but narrow.\n- Candidate 4 (strongest): the store's integrity enforcement is NOT write-only. The bytes show `medium.set('forged-address', …)` injecting a row directly into the backing Map, bypassing the door, followed by `expect(store.read('forged-address')).toBeUndefined()` — while the grounded row (`key === id(value)`) round-trips fine. So the medium is treated as untrusted on the read path too; you cannot smuggle a row in past the door. Nothing in the unit's name/signature implies read-side verification (a \"write door\" suggests only writes are gated).\n  - Refutation pass: derivable from these bytes? Yes — the expectations and the `grounded` helper (`key: id(value)`) are all present; no reliance on unseen modules. Comment-dependent? No — rests on the `expect(...)` calls themselves, not the prose. Exact-count risk? None; qualitative. Restatement of name/signature? No — the name points at writes/transports, not read-path integrity.\n\nCandidate 4 survives.\n\n```atlas-fact\n{\"claim\": \"The governed store enforces grounding on the read path too: the test plants a row directly into the backing Map (bypassing the write door) and expects store.read to return undefined for it, so the medium cannot be used to smuggle ungrounded data into the store — only rows whose key matches id(value) round-trip.\"}\n```"