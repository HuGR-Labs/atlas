"Scratch (refutation pass):\n\n- Draft: \"The gate downgrades HOLDS→NA based on grounded∧FRESH.\" — Refuted: this is the stated purpose, restatement of the docs/laws.\n- Draft: \"Both injected deps run for every HOLDS candidate (driftDetect before isGrounded).\" — True from control flow, but low impact; mostly evaluation-order trivia.\n- Draft: \"The candidate argument is checked as a raw string against a whitelist; objects collapse to NA.\" — Survives: directly checkable at `typeof candidate === 'string' && (STATUSES as readonly string[]).includes(candidate)`; not derivable from the `candidate: unknown` signature, and the surrounding prose (\"reads the candidate's incoming Status VERDICT\") suggests field-reading that the code does not do. Whitelist contents verified item-by-item in the `STATUSES` literal: 'HOLDS', 'BROKEN', 'NA', 'advisory' — exactly four. Actionable: passing a structured candidate record (or wrong casing) silently yields 'NA' instead of inspecting fields.\n\nSurvivor: the coercion behavior of the candidate argument.\n\n```atlas-fact\n{\"claim\": \"Although the first parameter is named `candidate`, the gate never inspects it as a structure — coerceStatus requires the value itself to be exactly one of the four whitelisted strings ('HOLDS', 'BROKEN', 'NA', 'advisory'), so a structured candidate object, or any unrecognized value or casing, silently coerces to 'NA' rather than being examined.\"}\n```"