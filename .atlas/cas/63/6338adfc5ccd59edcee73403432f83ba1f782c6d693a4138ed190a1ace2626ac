"STEP 1 — scratch reasoning (not persisted):\n\nReading the bytes closely. Candidate facts drafted and stress-tested against the source:\n\n- **Candidate A ('' is the generation sentinel):** In 15b, `gen = T('cas','','T1',…)` and the expectation is `toBe('charlie')` — so `''` is demonstrably not retained as an owner. Survives byte-checking, but it's partially mirrored by the adjacent code comment and is fairly visible at first glance of the test body. Keep as backup.\n- **Candidate B (property never exercises contested majorities):** In PROP-INDEX-15, the blame array is built as `['z/a.ts','z/b.ts','z/c.ts'].map((p) => ({ path: p, authors: [dom!] }))` — every path's `authors` array contains exactly the one element `dom`. No fixture in the property ever mixes authors. The only mixed-attribution fixture in the whole unit is the handwritten `casBlame` (charlie×4, dana×1). So the ∀-law's \"generated = blame majority\" clause is only ever checked under *unanimous* attribution; split/tie resolution is never property-tested. This rests purely on the generator lambda in these bytes — no comment claims it, no external module needed. Re-checked item by item: three paths, all `authors: [dom!]`; the only other `BlameEntry[]` in the file is `casBlame`. Survives.\n- **Candidate C (dom ≠ ovr guaranteed):** `fc.uniqueArray(…, {minLength:2, maxLength:2})` implies distinctness, but confirming that requires leaning on fast-check's documented semantics beyond what's literally in the bytes — weaker grounding than B. Discard in favor of B.\n- **Candidate D (all calls pass NO_GRAPH):** True (verified each `reconcile` call site), but low-surprise given the file's stated blame-centric scope. Discard as marginal.\n- **Candidate E (comparator never returns 0):** `(a[0]! < b[0]! ? -1 : 1)` indeed violates strict weak ordering on equality, but no fixture here ever produces colliding names, so it changes nothing observable in this unit. Discard.\n\nB survives all refutation passes: it is qualitative, derived from declarations/control flow/literals in this unit alone, not from any comment's claim about unseen code, and it materially changes how one assesses or extends the property's strength.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"In the PROP-INDEX-15 property, every synthesized blame entry attributes its path to the single author dom alone, so the ∀-law only ever exercises unanimous attribution — contested blame majorities like the golden's charlie/dana split are never exercised by the property.\"}\n```"