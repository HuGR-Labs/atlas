"```atlas-fact\n{\"claim\": \"Although this is the method-tag assignment sheet for the AUTHORING block, it assigns zero `formal` tags: all 18 INV entries (AUTH-1 through AUTH-14, CLI-5, CLI-6, MCP-3, MCP-4) are tagged only PBT, exhaustive, or reference-model — 5, 8, and 5 respectively — and every `fspec` field in the unit reads '—'.\"}\n```"