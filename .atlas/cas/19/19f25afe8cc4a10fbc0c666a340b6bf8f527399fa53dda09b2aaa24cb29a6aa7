"STEP 1 — scratch reasoning (not persisted):\n\nCandidate A: \"`doctor` sub-dispatches to DoctorApi legs at runtime.\" Refuted: that rests on a comment pointing at `src/doctor.ts`, which is not in these bytes. Discard.\n\nCandidate B: \"`authorityOf` derives write authority from the frozen `WRITE_PATHS` import rather than re-typing the list.\" Mostly restates the signature plus a comment's framing; an engineer reading the cast-and-includes line learns little beyond what the name implies. Weak.\n\nCandidate C: \"In `deriveStatus`, the governance probes run before the `ok===false` check, so a verdict can classify `rejected` even with `ok:true`.\" True of the control flow, but largely spelled out in the adjacent comment and marginal in impact. Held as backup.\n\nCandidate D: the header comment on `COMMANDS` asserts \"EXACTLY these seventeen, no more,\" yet the array literal right below enumerates: init, query, emit, reconcile, doctor, mine, node, link, promote, own, relations, negations, transitions, transition, test-vacuities, test-vacuity, verify-fact, verify-store, derive-relations, anchors, slots, draft. Counting item by item: that is 22 elements, not 17. Both the claim and the array are inside these bytes, so the mismatch is checkable purely by counting the shown literal — no reliance on unseen files, PRs, or history. The file itself even warns that a prose count beside the array becomes \"a second oracle\" that drifts (citing the old \"all six\"/\"all EIGHT\" errors), and this header is exactly such a drifted second oracle. An engineer extending the surface, writing totality proofs, or trusting the doc for the size of the enumerated union would be misled; the fix is to correct or drop the prose count. Survives all refutation passes; the exact numbers are ones I counted and recounted against the bytes.\n\nSTEP 2 — emit surviving fact:\n\n```atlas-fact\n{\"claim\": \"The header comment on COMMANDS declares the surface to be 'EXACTLY these seventeen, no more', but the array literal beneath it actually enumerates twenty-two command names, so the prose count has drifted from the code and only COMMANDS.length is trustworthy.\"}\n```"