"STEP 1 — Scratch reasoning:\n\nReading the two atomic-commit doubles side by side in `makeStoreSpy`:\n\n```ts\ncommitProjection(decide) {\n  const decision = decide(persists.length > 0 ? persists[persists.length - 1]! : emptyStore());\n  ...\n}\n...\ncommitStaging(decide) {\n  const decision = decide(emptyStore());\n  return { settled: true, out: decision.out };\n}\n```\n\nCandidate facts drafted and tested:\n\n- *\"`put` runs before the projection is published\"* — stated almost verbatim by the adjacent comment; the control flow confirms it, but it's the comment's headline claim, weaker as a novel finding. Set aside.\n- *\"A decision with no `next` skips its whole `put` list\"* — confirmed by the early `return` preceding the `for (... decision.put ?? [])` loop; real, but again explicitly narrated by the comment directly above it.\n- *\"commitStaging decides over a fresh `emptyStore()` while commitProjection decides over the last persisted projection\"* — this asymmetry appears NOWHERE in any comment. The nearby prose discusses the staging door generally and a deleted-door history (a claim about other files/tasks — correctly ignored). It is checkable purely from these two declarations three lines apart. Its consequence — that successive `commitProjection` calls chain onto prior state while `commitStaging` never accumulates anything (its `decision.next` isn't even stored) — is exactly the kind of thing that would silently break anyone extending this harness for staging-stateful teeth, and is invisible from the parallel-looking signatures.\n\nRefutation checks: derivable solely from these bytes (yes); no reliance on cross-file or issue-number commentary (yes); no counts or exhaustive enumerations (none asserted); not a restatement of names/signatures — the signatures are structurally identical, and the divergence is only in the argument handed to `decide` (yes). Survives.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"In makeStoreSpy, commitProjection hands the decide callback the most recently persisted projection (so consecutive commits chain onto prior state), whereas commitStaging always hands it a fresh emptyStore() and discards the decision's next, so no staging state ever accumulates across calls.\"}\n```"