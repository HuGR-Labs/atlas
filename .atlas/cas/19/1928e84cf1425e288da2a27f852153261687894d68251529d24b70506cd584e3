"STEP 1 — Reasoning (scratch):\n\nThe unit is a spec/reference doc whose \"bytes\" are its normative schema, invariant list, API surface, and acceptance criteria. Candidate facts:\n\n- *Candidate A:* \"Dependency-axis nodes' `subtreeHash` is the identity of their path, not a content hash, so the axis is excluded from the Merkle freshness model.\" Refutation pass: Is it in these bytes? Yes — stated in \"Shared rollup mechanics\" (\"The dependency axis is the exception to this paragraph\"), in INDEX-17 itself, and operationalized in Acceptance 13 (\"Editing a file's content … leaves its `dependency`-axis node's `subtreeHash` byte-identical\"; anchors resolved against it are \"never treated as FRESH\"). Does it lean on unseen code? The parenthetical citing `packages/index/src/build.ts` / `packages/grounding/src/drift.ts` does, but the claim stands without it. Exact count/enumeration? None. Restatement of name/signature? No — the signature's uniform `rollup(axis, key)` invites exactly the wrong assumption; the carve-out contradicts it. Survives.\n\n- *Candidate B:* Territory ownership generated from graph + git-blame, manifest as override only, tier human-ratified. Present in bytes (territory section + INDEX-15). Strong, but closer to a headline design decision already emphasized in prose; also fine but less of a trap relative to the API's apparent uniformity.\n\n- *Candidate C:* maxHops=2 lazy/eager split; 15% T0 unresolved-edge gate; raw-source hashing with no normalizer. All present and verifiable, but each is a parameter detail; A is the fact that flips the mental model of the whole unit (everything-hashed/Merkle-everywhere) for one axis, and is easy to get catastrophically wrong if missed (treating dependency-axis resolution as grounding).\n\nRefutation summary: A survives — multi-stated within the shown bytes, internally consistent with the rState fold description, qualitative, non-obvious, and action-changing (don't key freshness/grounding off the dependency axis despite the dual-rollup API).\n\nSTEP 2 — Emit result:\n\n```atlas-fact\n{\"claim\": \"Despite every axis carrying a dual Merkle rollup, a dependency-axis node's subtreeHash is defined as the identity of its path (id({file: path})) and carries no content, so editing a file leaves its dependency-axis hash byte-identical, the axis is explicitly excluded from the leaf-to-root re-hash model, and it must never be used as a freshness oracle or treated as a grounding surface.\"}\n```"