"STEP 1 — SCRATCH\n\nReading the bytes closely:\n\n- Header comments talk about GATE/blinding/overfitting — all claims about process and other files (`src/source.ts`). Not usable.\n- Fixtures and assertions are fully visible. Scanning for something non-obvious beyond names/signatures:\n  - 9b-2 proves `exportStore` returns a JSON string (it's fed to `JSON.parse`) and `importStore` consumes that string back — fairly expected from the names, low value.\n  - 1a-2's exclusion of the PR-only fact rests entirely on `rebuilt.has(id(prOnly))`, which quietly assumes `id()` is deterministic across construction and lookup — but that leans on unseen `@atlas/kernel` semantics. Discard.\n  - The `homeHint: 'store'` field on `safe` is declared but no assertion reads it specifically — weak, borderline restatement of the fixture.\n  - **The asymmetry in 1b-2**: the *positive* expectation checks structure (`expect(violations[0]?.value).toEqual(M)`), while the *negative* expectation checks reference identity (`violations.some((v) => v.value === safe)`). By plain JS semantics, `===` is true only for the very same object reference — so if `soleHomeViolations` ever returned violation records containing a deep copy of `safe` (structurally equal, different reference), this negative assertion would pass vacuously. Fully derivable from these lines themselves; no comment trusted, no library knowledge beyond JS semantics, no counts or enumerations. It's not a restatement of the test name, and it changes what a reviewer/refactorer does: they'd know to strengthen the guard to a structural check if the producer ever stops returning references.\n\nSurvives refutation. Emitting.\n\n```atlas-fact\n{\"claim\": \"In SCN-PERSIST-1b-2, the assertion that the multi-homed entry `safe` was not flagged checks reference identity (`violations.some((v) => v.value === safe)`), unlike the structural `toEqual` used for the expected violation, so a violation record holding a deep copy of `safe` would evade this negative check.\"}\n```"