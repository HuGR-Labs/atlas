"STEP 1 — scratch reasoning (not persisted):\n\nCandidates drafted from close reading:\n\n1. *\"The no-leg fall-through is detected by planting a sentinel 'off-surface' rejection in the fake handler and asserting it echoes into the MCP result text.\"* — Plant (`fakeHandler.handle` returns `rejected: 'off-surface'`) and assertion (`toContain('off-surface')`) are both in these bytes. Survives, but largely paraphrases the test's own title/teeth comment; weaker than a better candidate below.\n\n2. *\"Negation `nodeKey` is a bare string while the abstention id is wrapped in `asNodeKey`.\"* — Observable asymmetry, but whether branding is *required* rests on typedefs imported from unseen modules. Refuted as too dependent on invisible types; the observable is mere usage style.\n\n3. *\"Parity is checked by re-invoking `negationsVerdict` inside the test, so it proves MCP equals the builder, not cross-transport truth.\"* — Derivable, but the stronger half (what it *cannot* catch) leans on unseen CLI code. Discarded per the rules.\n\n4. **Strongest:** `cannedSchema` — returned by `fakeHandler.schema` for *any* tool — produces `{ properties: {}, additionalProperties: false }` with **no** `required`. Yet the second test demands the advertised negations tool satisfy `toEqual(NEGATIONS_INPUT_SCHEMA)` with `required: ['scope']` and a boolean `abstained` property. Those shapes cannot coincide. Therefore, purely from these bytes, the advertised negations input schema cannot be sourced from the wired handler's `schema()` hook — it must be composed on the leg/injection path. Refutation attempts: could `NEGATIONS_INPUT_SCHEMA` equal the canned output? No — `required` presence and `abstained` property visibly conflict. Could the conclusion rest on the header comment? No — it re-derives from the planted `cannedSchema` body vs. the assertions, both in-unit. Survives.\n\nSTEP 2 — emitting the surviving fact:\n\n```atlas-fact\n{\"claim\": \"The fake handler's schema() hook returns an empty-properties, required-less schema for every tool, so the test's expectation that the advertised atlas-negations inputSchema deep-equals NEGATIONS_INPUT_SCHEMA (with required ['scope'] and a boolean abstained property) proves the negations tool's advertised schema is composed outside the wired handler's schema hook.\"}\n```"