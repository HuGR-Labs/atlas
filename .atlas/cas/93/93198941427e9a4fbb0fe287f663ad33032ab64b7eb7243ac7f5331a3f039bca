"STEP 1 — REASONING (scratch):\n\nReading the bytes closely. Candidates:\n\n1. *The GAN rule: generator WPs aren't sealable until an evaluator passes them.* Check §3 against the §2 roster: generators = charlie, patty (both 4 EXECUTE) ✓; evaluators = lucy, billy, bobby, frankie (all 5 VERIFY) ✓. Fully contained in these bytes, no reliance on unseen files. Not guessable from the filename/title. Changes what an engineer builds (a hard gate in orchestration). Survives.\n2. *Conductor bounces under-decided plans upstream.* Also in-bytes (§1), viable, but overlaps with candidate 1 in theme; only one fact allowed.\n3. *Conductor design lives in the consumer repo (`docs/design/orchestration.md` there)* — a claim about ANOTHER repo not shown; discard per rules.\n4. *\"No self-declared role field\"* leans on §ARCHITECTURE 5.1, not visible here; the roster-is-fixed part survives but is weaker than candidate 1.\n5. *archie creates vs bobby verifies architecture* — real and subtle, but candidate 1 is more load-bearing and equally verifiable.\n\nRe-check candidate 1 item by item: charlie→FORGE generator ✓, patty→ATELIER generator ✓, four evaluators named ✓, \"not sealable until… passing ResultCard\" stated verbatim ✓, \"the Conductor enforces this\" stated ✓. Not a restatement of title/name. Survives.\n\nSTEP 2 — RESULT:\n\n```atlas-fact\n{\"claim\": \"A generator seat's work package (charlie or patty) is not sealable until a matching evaluator seat (lucy, billy, bobby, or frankie) returns a passing ResultCard, and the Conductor enforces this gate structurally rather than as a habit.\"}\n```"