"STEP 1 — scratch reasoning (not persisted):\n\nReading the bytes: nothing in the name `godfile-guard.test.mjs` reveals *how* the suite aims the guard at its fixtures. The `run()` helper spawns the guard as a genuine child process (`execFileSync(process.execPath, [GUARD], ...)`) and selects the target tree exclusively through `env: { ...process.env, GODFILE_GUARD_ROOT: root }`, where `root` is an `mkdtempSync` directory that `beforeAll` turns into a real git repo. No comment documents this env-var contract — it exists only in the code.\n\nRefutation attempts:\n- Candidate \"guard boundaries are exclusive (>400/>600)\": the exclusivity lives in the test's *assertions* about an implementation not shown in these bytes; the test title already announces it. Weak — partially restates the visible test name.\n- Candidate \".d.ts is exempt / dist/ ignored\": these are expectations about the unseen guard's behavior; the exemption logic is not in this unit. Discard.\n- Candidate \"fixtures lack a trailing newline\": true and code-visible (`.join('\\n')` with nothing appended), but it is largely spelled out by the adjacent comment, making it closer to a paraphrase of an in-unit remark than a discovered mechanic.\n- Candidate \"GODFILE_GUARD_ROOT invocation contract\": fully checkable in these bytes — the literal env key, the tempdir creation, the child-process spawn, and the control test expecting fixture-scoped counts (`/1 packages/`) all sit here. Not derivable from the name, not comment-borne, and it changes what an engineer does (they cannot point the guard at another tree by cd-ing; they must set the env var, and any refactor of the guard's root discovery must preserve it).\n- No exact-count or exhaustive-enumeration claims involved; the qualitative form avoids the miscount trap.\n\nThe env-var/temp-repo invocation fact survives every refutation check.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"The suite exercises the real guard as a spawned child process aimed at a disposable temp-directory fixture repo solely through the GODFILE_GUARD_ROOT environment variable, never at the process's working directory or the harness's own checkout.\"}\n```"