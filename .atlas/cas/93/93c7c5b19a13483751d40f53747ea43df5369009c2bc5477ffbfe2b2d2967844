"STEP 1 — Reasoning (scratch):\n\nCandidate drafts considered against the visible bytes:\n\n- *\"Exit 2 means governed refusal, exit 1 usage error\"* — the exit-1 semantics appear only in a comment; only codes 0 and 2 are asserted in code. Discarded (comment-carried).\n- *\"The reason line carries the error's message verbatim\"* — asserted in code (`line(out(),'reason')).toBe(err.message)`), but this largely restates what the filename and describe title advertise. Weak.\n- *\"Foreign errors re-propagate with identity intact\"* — supported by `rejects.toBe(foreign)`, but the test title states it outright; low marginal information.\n- *\"The `as never` cast implies the error's declared constructor parameter type doesn't accept a plain string\"* — inferable, but the interesting part (message derivation from the tag) leans on a comment about a class defined elsewhere. Discarded.\n- *\"On the success path, `runMineArms` resolves to a structured `{ exitCode, stdout }` object that main consumes\"* — checkable purely in these bytes: `mockResolvedValueOnce({ exitCode: 0, stdout: 'status: ok\\n' })`, then `code === 0` and the captured output contains `status: ok`. Since the mock feeds the *real* `main`, the shape is constrained by main's actual consumption, not arbitrary. Nothing in the unit's name (\"cas-unaddressable-refusal\") or imports reveals this success-path seam contract. It changes what an engineer does: any refactor or additional mock at this boundary must preserve the `{ exitCode, stdout }` shape, and mine-side code must not print directly or output would double up.\n\nSurvivor: the `{ exitCode, stdout }` seam contract. It is derived from control flow and literals in the shown bytes, involves no exact counts, and does not rest on any cross-file comment.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"On the success path, `atlas mine` does not print directly: `runMineArms` hands `main` a structured `{ exitCode, stdout }` result whose `stdout` is echoed verbatim to output and whose `exitCode` becomes `main`'s return value.\"}\n```"