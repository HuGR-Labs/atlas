"STEP 1 — scratch reasoning (candidates drafted and refuted):\n\n- *Candidate 1:* \"GOVERNANCE_SURFACE stays 5 / WRITE_PATHS stays 2\" — stated prominently in the header and repeated; an engineer reading the title (\"planners, not new write doors\") would already infer this. Discarded as low-novelty.\n- *Candidate 2:* \"`author.ts` is kept because it doubles as an adversarial fixture factory\" — the operative clause is in these bytes, but the supporting substance (fixture names, \"~28 consumers\", WP-10.A5.E2E measurement) asserts properties of a file/work not shown here; partially resting on claims about elsewhere. Weaker survivor; set aside.\n- *Candidate 3:* \"Exactly five rejected alternatives\" — countable (a–e) but the count itself isn't the useful fact. Discarded.\n- *Candidate 4 (survivor):* The doc explicitly rejects a fast planner-side hash and bans cached digest tables (AUTHOR-1), stating the accepted cost \"planners pay a full index build,\" precisely because divergence between planner derivation and the emit truth-gate re-derivation is indistinguishable from the drift Atlas exists to catch. Verified against the bytes: the rejected-alternative (b) text, the C1/AUTHOR-1 quoted block, and the door list (`anchors · slots · draft · check` — four names, counted) are all present in this unit; no reliance on unseen files or stale claims. Not derivable from the title — the title implies cheap read-side planners, while the bytes mandate deliberately expensive, uncached recomputation. This changes implementer behavior: no memoization, no lighter hasher, and latency pressure must not spawn a second grounding computer.\n\nSTEP 2 — surviving fact:\n\n```atlas-fact\n{\"claim\": \"Although the new authoring planners (anchors, slots, draft, check) look like lightweight read helpers, the ADR mandates that every planner derivation pay a full index build through a single grounding seam, having explicitly rejected a fast planner-side hash and banning any cached digest table (AUTHOR-1) so a planner verdict can never diverge from the emit-time truth-gate re-derivation.\"}\n```"