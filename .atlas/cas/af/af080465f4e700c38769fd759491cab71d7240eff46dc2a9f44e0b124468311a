"STEP 1 — scratch reasoning (not persisted):\n\nDraft candidates and refutation passes:\n\n- *\"`DAMPING` is pinned to 0.85\"* — the literal `toBe(0.85)` is in these bytes, so it's checkable. But it's near-restatement of a named constant's value and low actionability; weaker candidate.\n- *\"Budget stays 4 regardless of node count\"* — supported by `expect(a).toBe(4)` / `expect(b).toBe(4)`, but essentially restates the `frontierBudget`/GEN-3 describe titles.\n- *\"Thin history zeroes mined signals in the output\"* — derive independently of comments: the injected `shallowHistory` fake still routes `signals` through `loudSignals` (nonzero-shaped mined data is supplied), yet the test demands `cands.length > 0` AND `for (const c of cands) expect(c.signals.szzBugCommits).toBe(0)`. So the contract is: candidates are still emitted, and the history leg arrives **zeroed inside the retained `signals` shape**, not omitted. Cross-checked pieces: `expect(MIN_COMMITS).toBe(2)` (threshold literal), `commitCount: () => 1` (tripping fixture), `expect(thin.reason).toBe('low-commit-count')` (exact reason string), `expect(thin.thin).toBe(true)` — all present as code literals/assertions in this file. The \"every candidate\" quantifier mirrors the actual `for…of` loop in the bytes, not an invented census. Nothing here leans on another file, a PR number, or a stale past-tense claim; the describe title (\"degrades to structural centrality\") does not state that the `Candidate.signals` field survives with zeros, so this is not a restatement. Survives.\n- *\"Seeds emit st-* not nk-* hashes\"* — true per F2 assertions, but largely echoes the F2 block's own framing; discard as redundant.\n- *\"Personalization order invariance\"* — restates the SCN-GEN-11a test intent visibly in its body; discard.\n\nSurvivor: the thin-history contract detail (zeroed `szzBugCommits` in still-emitted candidates, gated at `MIN_COMMITS === 2` with reason `'low-commit-count'`).\n\nSTEP 2 — emit:\n\n```atlas-fact\n{\"claim\": \"When the history source falls below the pinned MIN_COMMITS threshold of 2 commits, probeHistory flags it thin with reason 'low-commit-count', and createMine still returns a candidate list whose every candidate carries signals.szzBugCommits === 0 — the mined history signals are zeroed inside the retained output shape rather than omitted.\"}\n```"