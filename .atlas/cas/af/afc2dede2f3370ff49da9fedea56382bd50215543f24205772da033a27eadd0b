"Looking closely at the bytes: the file's name and the imported identifiers (`createDriftFold`, `MAX_HOPS`) tell you it tests drift-state behavior, but reveal nothing about the session's observable contract, which exists here only through call sites and `expect` literals.\n\nDraft candidates and refutation:\n\n1. *Edited node A is not self-dirty* — grounded in `expect(fold.isDirty(A)).toBe(false)`, but largely restated by the adjacent comment; weaker novelty.\n2. *Staleness decided per-call, not sticky* — 5a queries the same `'F'` twice on one fold expecting opposite outcomes; grounded in literals, but the inline \"teeth\" comment already states the equal-anchor case.\n3. *Optional second constructor arg* — visible (`createDriftFold([])` vs `createDriftFold(chain(), [...])`), but it's a plain arity observation with modest action value.\n4. *Lazy-resolve counter increments exactly once* — the `resolvesBefore + 1` literal pins a precise count; verifiable in the bytes; comments only say \"on read,\" not the exact-once contract.\n5. *The capped re-hash preserves beyond-cap dirty/unresolved state* — checking the 12h sequence: `propagateDirty(A)` → `rehashState(A)` → `expect(isDirty(D)).toBe(true)` and `rStateResolved(D)` false until `queryState(D)`. The ordering and both post-rehash assertions are in the code itself, not dependent on any comment's claim about unseen files. This constrains implementation: an eager re-hash that cleared or resolved deep dirty bits would break this test. Survives refutation — it is not derivable from the unit's name, not an exact-enumeration risk (specific nodes A/D cited verbatim), and rests on call order plus expectation literals in this file alone.\n\nSurviving fact: the 12h ordering proves `rehashState(A)` neither clears nor resolves drift state beyond the hop cap.\n\n```atlas-fact\n{\"claim\": \"In SCN-INDEX-12h-1 the eager rehashState(A) call runs after propagateDirty(A), yet the test still asserts isDirty(D) is true and rStateResolved(D) is false afterward, pinning that the capped re-hash neither clears nor resolves drift state on nodes beyond maxHops until a later queryState(D).\"}\n```"