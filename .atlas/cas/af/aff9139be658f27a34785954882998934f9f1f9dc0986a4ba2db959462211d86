"STEP 1 — scratch reasoning:\n\nReading the bytes closely. The filename and title tell me the mechanism (a materialized-view watermark, `builtAt` vs live HEAD) but say nothing about its *granularity*. Draft candidate: \"query.stale is deliberately repo-GLOBAL — it flips true on any HEAD advance, even commits touching nothing under the queried scope.\"\n\nTrying to refute:\n- Derivable from these bytes? Yes — it is stated verbatim in the Decision's Consequences (\"Granularity is repo-GLOBAL, not scope-scoped (deliberate)... even a commit touching nothing under the queried scope\"), including the explicit rationale for deferring a scope-scoped diff. No reliance on unseen code.\n- Does it rest on a claim about another file/module? No — this is the ADR's own decision content, not a report about `projection-query-index.ts` or test files. The later amendments *confirm* it (\"still repo-GLOBAL, still computed exactly as clauses 1-3 say\") rather than reverse it, and the amendment's row C (stale:true while zero facts drifted) is internally consistent with it.\n- Exact count/enumeration risk? None — \"any HEAD advance\" is qualitative and is precisely the point; no counted totals involved (I am avoiding the unverifiable 199/14/timing figures, which are measurement claims about runs I cannot recount).\n- More than a restatement of name/signature? Yes — \"watermark\" in the title does not reveal repo-wide granularity or the accepted false-positive direction, both of which would change how a consumer treats `stale: false` vs `stale: true`.\n\nSurvives. STEP 2 — emit:\n\n```atlas-fact\n{\"claim\": \"The `stale` flag in `atlas query` packs is deliberately repo-GLOBAL rather than scope-scoped: it flips to true after ANY advance of HEAD, even a commit touching nothing under the queried scope, trading a dishonest false-negative for an honest-conservative false-positive ('reconcile first') because a per-query tree diff on the read path was deferred.\"}\n```"