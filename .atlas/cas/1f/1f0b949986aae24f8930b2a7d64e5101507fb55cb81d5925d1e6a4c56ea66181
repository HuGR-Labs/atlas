"STEP 1 — Reasoning (scratch):\n\nReading the bytes closely:\n\n- Candidate A: \"`isStale([])` returns false.\" Trivially derivable from `.some` semantics any engineer knows; refuted as unremarkable.\n- Candidate B: \"`dropOrder` sorts by hitRate ascending in warm mode.\" Restates the name/docstring (\"hitRate-asc\" is in the signature's contract); refuted as already-known.\n- Candidate C: \"`dropCounter` dedupes kinds within a turn via `new Set(turn)`.\" Real and code-visible, but low impact — turns are naturally sets of kinds; unlikely to change what anyone does.\n- Candidate D: **`resolveCeiling` can return `sum > ceiling`.** Trace the code: the drop loop iterates `order = orderDroppable(injections)`, and `orderDroppable` filters `!isPin(r.kind)` — so `awareness` and `protocols.safetyCritical` are structurally unreachable as victims. If the pins alone exceed the ceiling, every droppable kind gets dropped, the loop exhausts, and the function returns `{ survivors: [pins], sum }` with `sum > ceiling`. The signature and name (\"Enforce the ~5K ceiling\", `sum` field) invite the assumption `result.sum <= ceiling`; the code proves that postcondition does not hold unconditionally. This is checkable entirely from this unit's control flow (loop source, the filter, the break condition) — not resting on any comment's claim about external files. Survives.\n- Refutation pass on D: Is it just a comment restatement? No — the comment notes it parenthetically, but the fact re-derives from the visible loop + filter + `PINS` literal ('awareness', 'protocols.safetyCritical', exactly two members — counted and verified). Would it change what they do? Yes: a consumer cannot assert `sum ≤ ceiling` on the result and must handle pin-driven overflow downstream. Survives.\n\nSTEP 2 — Emit:\n\n```atlas-fact\n{\"claim\": \"In resolveCeiling, the drop loop only iterates kinds produced by orderDroppable, which excludes the pinned kinds 'awareness' and 'protocols.safetyCritical', so if the pinned injections alone exceed the ceiling nothing further can drop and the returned sum can exceed the ceiling.\"}\n```"