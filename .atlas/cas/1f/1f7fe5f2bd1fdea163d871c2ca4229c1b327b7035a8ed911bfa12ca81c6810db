"STEP 1 — Scratch reasoning (not persisted):\n\nScanning the bytes for candidates beyond the filename/test titles:\n\n- **Ordering constraint of the opt-in suite** (`initAst()` called only in the second describe's `beforeAll`, first suite hook-less): structurally present, but its significance rests on Vitest's in-file execution-order semantics — framework knowledge not visible here. Discard per rubric.\n- **JSON.stringify-based determinism check**: plainly visible in the code; marginal actionability. Weak.\n- **`unitName` rejoining split segments before unescaping**: real, but narrow and mostly restated by the adjacent comment. Weak-moderate.\n- **Pinned kind vocabulary + import exclusion**: verify against literals —\n  - `SRC[0]` is `\"import { z } from 'zod';\"`.\n  - `ADAPT-AST-items` asserts `items.map(unitName)).toEqual(['foo', 'Bar', 'q'])` — the import yields nothing.\n  - Kinds asserted: `['function_declaration', 'class_declaration', 'lexical_declaration']` — so `const q = 3;` maps to `lexical_declaration` (non-obvious; many engineers would guess `variable_declaration`).\n  - `ADAPT-AST-blocks` asserts `method_definition` twice for the class methods.\n  \n  All four literals are present in this unit's assertions; the import-exclusion is checkable by comparing `SRC` to the expected array. No reliance on comments about other files. Verified item-by-item. Survives.\n\nEmitting the survivor.\n\n```atlas-fact\n{\"claim\": \"The suite's expected arrays literally pin that the fixture's leading import statement produces no folded unit, that a top-level `const` is emitted with the parser kind `lexical_declaration`, and that class methods fold as `method_definition` blocks.\"}\n```"