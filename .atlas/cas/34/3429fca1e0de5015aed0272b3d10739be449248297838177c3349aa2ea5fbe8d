"STEP 1 — scratch reasoning:\n\nScanning candidates:\n- Header-comment claims (\"held-out −2 fixtures not transcribed\", \"FLAG digest simulated\") are commentary about things outside these bytes → discard.\n- `decideMechanisms([]) == one LLM call` rests only on a comment's interpretation → discard.\n- `DEFAULT_SAMPLES === 1`, `DEFAULT_CEGIS_K === 1`: pinned by asserts, but essentially announced by the test titles themselves → weak novelty.\n- `defaultKind` conjunction (checkable ∧ tier≥T1): visible in asserts, but fairly telegraphed by the scenario name.\n- `makeQueryDb` memoization: solid (factory counter hits 1 after 20 gets; `builds()` accessor exists) — but largely pre-bulleted in the header.\n- Scope-concatenation gotcha in `candOf` (`${scope}${id}`, trailing slash required): real and code-only, but test-harness-local impact.\n- `chooseAnalyzer` polarity: `expect(chooseAnalyzer(true)).toBe('semgrep')` and `expect(chooseAnalyzer(false)).toBe('codeql')` are bare literals in these bytes. From the bare signature `boolean → analyzer`, no engineer could know which polarity means what — and inverting it silently flips the cheap/expensive order. Not a restatement of name/signature; checkable by reading the two expects themselves, not a comment about unseen code; no risky count/enumeration. Survives refutation.\n\nSTEP 2:\n\n```atlas-fact\n{\"claim\": \"This suite pins the polarity of chooseAnalyzer's boolean argument — passing true returns 'semgrep' while false returns 'codeql' as the fallback — a mapping invisible in the function's signature that a caller could silently invert, flipping the cheap-to-expensive analyzer order.\"}\n```"