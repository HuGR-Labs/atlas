"STEP 1 — scratch reasoning:\n\n- Candidate A: \"the leg rehydrates the projection on every call.\" Visible in the arrow body (`rehydrateProjection(store)` inside the returned closure). Plausible, but it is essentially a restatement of the one-line body once read; borderline \"plainly what the code does.\"\n- Candidate B: \"`relationsVerdict` runtime-guards `unit` as a non-empty string despite declaring `unit: string`.\" Check: the bytes show `if (typeof unit !== 'string' || unit.length === 0)` returning a structured `{ ok:false, rejected, guidance }` before any direction parsing or `leg` call, with no throw path. Under the declared signature this branch looks dead/redundant, so a competent engineer reading only the signature would not expect it — and might delete it as dead code, or assume an empty unit yields an empty result set rather than a rejection. Refutation pass: does the truth rest on the comment's story about MCP coercing to `''` / `cli.ts` arity floors? No — I can state the guard's existence and fail-closed behavior purely from the shown control flow, without citing anything outside these bytes. No counts or exhaustive enumerations asserted. Not a restatement of name/signature/types — it actually contradicts what the signature implies. Survives.\n- Candidates C–E (case-sensitive direction matching, unit-before-direction error precedence, `rejected` field shape) are all weaker restatements of directly visible lines with less capacity to change what an engineer does.\n\nSurviving fact: B, phrased strictly from code-visible behavior.\n\nSTEP 2 — emit:\n\n```atlas-fact\n{\"claim\": \"Although relationsVerdict's signature declares unit as a plain string, its body re-validates at runtime that unit is a non-empty string and fails closed with a structured ok:false verdict (carrying rejected plus guidance) instead of throwing or ever calling the relation leg.\"}\n```"