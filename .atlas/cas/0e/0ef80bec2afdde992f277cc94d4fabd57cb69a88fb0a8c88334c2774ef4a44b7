"Reading the bytes: this is a results JSON, so every field is literal data. Cross-checking internal consistency first — `n.total` 20 = 10 true + 10 false ✓; the verdict list holds exactly 20 entries (T01–T10, F01–F10) ✓; `catch.caught`=9 plus `false_admit.leaked`=1 accounts for all 10 false fixtures ✓; `catch.rate` 0.9 and `false_admit.rate` 0.1 agree with those counts ✓.\n\nDraft candidate A: \"the report supersedes a prior 96.7% live score.\" The `supersedes` string is present, but its *truth* concerns a prior run/artifact not shown here — it's provenance about other code/results, not re-derivable at these bytes alone. Discard per the rule.\n\nDraft candidate B: \"`run.prompt_digest` equals `instrument.fixtures_sha256_16`.\" Both literals are visibly `01c7bc130eb7f384`, so it's byte-checkable — but it's essentially a restatement of two adjacent literals and barely changes what anyone does. Discard as low-value/trivial.\n\nDraft candidate C (the survivor): scanning `verdicts` item by item — T01…T10 all GROUNDED_TRUE; among the false-labeled entries, F01 (HALLUCINATED), F02 (HALLUCINATED), **F03 (GROUNDED_TRUE)**, F04–F10 (all HALLUCINATED). Exactly one false fixture escaped the HALLUCINATED verdict, and it is precisely the fixture enumerated in `leaks`: id F03, falseKind `wrong-constant`, anchor `isAbstainToken`. Count re-verified: leaks array length 1; only one `GROUNDED_TRUE` appears under a `\"label\": \"false\"` entry. Nothing else in the file contradicts it. Not derivable from the path/filename, which reveals nothing about *which* fixture leaked or its anchor symbol. It is actionable: an engineer tuning the adjudication probes should start at F03 / the `isAbstainToken` anchor, since the aggregate rates hide where the miss actually lives. Survives refutation.\n\n```atlas-fact\n{\"claim\": \"Of the ten false fixtures, exactly one — F03 — was adjudicated GROUNDED_TRUE rather than HALLUCINATED, and it is recorded as the sole leak, a wrong-constant claim anchored at isAbstainToken.\"}\n```"