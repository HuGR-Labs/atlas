"STEP 1 — scratch reasoning (not persisted):\n\nReading the bytes closely. Candidates drafted and refuted:\n\n- Candidate: \"WP-7.26-c depends on -a and -b because of shared handler.ts\" — the reason cites the wave-plan §Conflict-map, i.e., a pointer's claim about another document. The bare `deps` list is in-bytes, but the load-bearing justification is external. Weak.\n- Candidate: \"Acceptance SCNs map 1:1 onto source_reqs in every WP\" — requires counting and merely restates the ledger's own declared partition policy. Not surprising/actionable beyond what the ledger already says.\n- Candidate: \"All five WPs owned by charlie (FORGE)\" — plainly visible, low impact on what an implementer does.\n- Candidate: **internal conflict on the write-path count in WP-7.26-a.TOOLS.** Refutation pass: guardrail line reads verbatim \"no new write path may surface; writePaths must stay == 1 (atlas-emit)\". Exit predicate of the SAME WP reads verbatim \"writePaths == 2 (WRITE_PATHS == {atlas-emit, atlas-link})\". Intent says \"WRITE_PATHS = {atlas-emit, atlas-link}\", anchor says \"{atlas-emit, atlas-link}\", action forbids paths \"outside {atlas-emit, atlas-link}\". Both numbers are literals I located and re-checked in the shown bytes; no reliance on other files, no stale-comment risk, not derivable from the filename or WP ids. The same metric (`writePaths`) cannot simultaneously equal 1 and 2 — an executor obeying the guardrail fails the exit predicate and vice versa. Survives.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"Within WP-7.26-a.TOOLS the governed write-path count is self-contradictory: its guardrail requires writePaths == 1 (atlas-emit only), while the same work package's intent, action, anchor, and exit_predicate all fix WRITE_PATHS to {atlas-emit, atlas-link}, with the exit_predicate explicitly demanding writePaths == 2.\"}\n```"