"STEP 1 — Reasoning (scratch):\n\nScanning candidates:\n\n- \"injectFor scopes to the owner\" / \"assembleHeader drops consultable kinds\" / \"put rejects cross-partition writes\" — these are the explicit subjects of the `describe` blocks; asserting them is close to summarizing what the tests plainly state. Discard as near-restatements.\n- Anything about `../src/inject.js` internals, the barrel wiring, SEAL, held-out `-2` fixtures, or WP-6.24-a/-b ownership — rests on comments about code/files not in these bytes. Discard.\n- \"All five Awareness slabs share one aliased `facet` object\" — true and visible, but low action-value.\n- Fixture-composition detail: enumerating `running` item by item — `[0]` alice/project pmA, `[1]` alice/task T7, `[2]` alice/pr #41, `[3]` **orch**/logbook #41. The logbook is owned by `orch`, not the probed seat `alice`, while the task and pr entries are alice-owned. Verified against the literal; qualitative form, no miscount risk. This is invisible from the file name and the call signatures (`assembleHeader(running, 'alice', …)` gives no hint), and it is actionable: the auto-injection exclusion is exercised across mixed ownerships, so anyone extending these tests knows the exclusion must hold by kind over *all* owners, not merely by owner-scoping.\n\nThis survives refutation: derivable solely from the array literal in these bytes, not from a comment, not numeric-exhaustive beyond what I listed and rechecked, and not a restatement of name/signature.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"In the `running` store used by the auto-injection and recall tests, the logbook entry is owned by `orch` rather than by the probed seat `alice`, unlike the sibling task and pr entries.\"}\n```"