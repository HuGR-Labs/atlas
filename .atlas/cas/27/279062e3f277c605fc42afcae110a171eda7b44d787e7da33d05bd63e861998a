"STEP 1 — Reasoning (scratch):\n\nCandidates considered and refuted/survived:\n\n- *“55/55 REQs, 0 orphans/doubles”* — I did recount the table (12+9+6+5+3+2+3+6+6+3 = 55) and the epic `reqs:` lists match it, and the families (ADAPTER-1..12, CLI-1..4, WIRE-1, MCP-1..2) are disjoint. But this is printed verbatim in the doc’s Coverage section, so it mostly restates the artifact; also “55” leans on the header’s claim about `requirements-adapters.md`, which is not in these bytes. Weak.\n- *“Contradiction: EPIC-1-a says real output arrives in ‘EPIC-1-b / CAMPAIGN-9.2’ but EPIC-1-b is tagged CAMPAIGN-9.1”* — on reread, the slash reads as “EPIC-1-b (init leg), and later CAMPAIGN-9.2 (emit/reconcile legs)” for the remaining legs of the five-leg handler; not a clean contradiction. Discarded.\n- *Counts about other files* (“55 REQs”, “56 SCNs”, CAMPAIGN-1..8, TOOLS-8, PERSIST-*, SPIDR, bobby/BLUEPRINT, D4/D5/D6 “as already logged”) — all claims about artifacts outside this unit. Discarded per rules.\n- *“Only EPIC-1-a lacks adapter-io”* — refuted: EPIC-7’s vertical (wire → mcp-server) also omits adapter-io. The precise survivor is about the *demo basis* (fixtures, no adapter), not the module list.\n- Survivor: EPIC-1-a’s own demo line declares it self-contained against Verdict + argv fixtures with **no adapter**, proving only shared-handler identity (`cliHandler === mcpHandler`) and deterministic render/exit; the campaign’s real-repo payoff (`atlas init` true skeleton) sits on EPIC-1-b. All grounded in this unit’s text, qualitative, and it changes execution ordering expectations (finish 1-a ⇒ still no real indexing).\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"EPIC-1-a, the Interface split that opens CAMPAIGN-9.1, is deliberately adapter-free — demoed self-contained against Verdict + argv fixtures, yielding only deterministic render/exit and the cliHandler === mcpHandler shared-instance proof — so the campaign's real-repo payoff (atlas init printing the true skeleton) belongs to EPIC-1-b, not to this first epic.\"}\n```"