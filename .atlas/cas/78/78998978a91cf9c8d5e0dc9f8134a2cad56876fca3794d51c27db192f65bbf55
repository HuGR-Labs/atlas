"Step 1 — scratch reasoning (not persisted):\n\nDraft candidates and refutations:\n\n- *\"SCN-GL-3 has no teeth on the authz conjunction\"* — rests on the inline comment's measurement claim plus POLICY contents living in an unseen support file. Not re-derivable from these bytes alone. Discard.\n- *\"Retried links after refusal succeed on the same store\"* — true (GL-6/GL-8), but it is plainly displayed by the assertions themselves; borders on summarizing what the code visibly does. Weak.\n- *\"GL-4 omits the ratifyToken key entirely\"* — visible at a glance; its deeper mutant-killing significance leans on the header's comment. Discard.\n- *\"'T3' cast evidences a closed tier union\"* — conclusion concerns an unseen external type; inferred via general TS mechanics. Discard.\n- *\"Positional nodeKey convention\"* — candidate: `fixture`'s array argument maps positionally to `'n0','n1','n2',…`, nowhere documented in this file. Refutation attempt: could keys derive from claims? SCN-GL-8 defeats this — its order `[alpha, gamma, beta]` is deliberately non-alphabetical, and the whole two-hop choreography (billy links `n0`–`n1`, attacker's own node is `n2`) only closes if `n0`=alpha, `n1`=gamma, `n2`=beta by array index. Cross-checked against every other case: GL-10 (`addressOf(T2_B)` must hit index 1), GL-11 (index 0), GL-12 (asserts the third element surfaces as `'n2'`), GL-13 (blinding epsilon at index 2 breaks `n0`–`n1`), GL-9b/9c (seeded-edge dicts keyed `'n0'`/`'n2'`). All consistent only under positional assignment. Survives: checkable in these bytes, not stated by any name/comment, and directly governs how one extends fixtures or targets `blindTo(addressOf(...))`.\n\nStep 2 — emit:\n\n```atlas-fact\n{\"claim\": \"The nodeKeys 'n0','n1','n2' used throughout are assigned positionally by the fixture helper's array argument rather than derived from any fact field — forced by SCN-GL-8's deliberately non-alphabetical order [alpha, gamma, beta] and relied on invisibly by every addressOf/blindTo target and seeded-edge key in the file.\"}\n```"