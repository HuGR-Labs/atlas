"STEP 1 — REASONING (scratch):\n\nReading the bytes closely:\n\n- Every fixture flows through `factAt`, which hard-codes `freshness: 'DRIFTED'` — so m1, m2, m3, s1, s2 all carry the identical stored freshness marker. I checked each: five `drifted(...)` constructions, all via `factAt`.\n- Yet the expected outcomes diverge: mA/mB/mC must land `mechanical` (their table entries re-hash back to the stored `st-*` hash) and sA/sB must land `semantic` (their entries re-hash to `st-CHANGED`). The only variable that differs between the two groups is the injected `reHashAt` table.\n- Refutation attempt: is \"the split is recomputed, not read from the stored field\" resting on the header comment's claim about inert legs? No — it is independently forced by the code itself: uniform `freshness: 'DRIFTED'` across inputs plus divergent asserted outputs means the classification cannot be a function of that stored field within these bytes. The comment agrees, but the fact re-derives from the fixture literals and the assertions alone.\n- Is it derivable from name/signature? No — `reconcile.know5.test.ts` and the `bindReconcile(reDerives): DriftedFact[] => {...}` shape reveal nothing about which fixture leg drives the arm.\n- Counts: I avoid leaning on \"3 of 5\"; I phrase qualitatively (\"each\"), though I did verify the five constructions individually.\n- Not a plain summary of what the code does line-by-line; it's an inference linking fixture construction to asserted outcomes.\n\nSurvives. Emitting.\n\n```atlas-fact\n{\"claim\": \"Each drifted fixture in this suite is constructed with the identical stored `freshness: 'DRIFTED'` value, so its landing in the mechanical versus semantic bucket is decided solely by whether the injected re-hash table maps the fact's newSha back to the anchor's stored subtreeHash — the stored freshness leg plays no role in the split.\"}\n```"