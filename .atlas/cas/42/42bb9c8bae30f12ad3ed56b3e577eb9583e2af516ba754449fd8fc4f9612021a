"STEP 1 — Reasoning (scratch):\n\n- Candidate A: \"The fixture is checked out on `topic`, so reads are rev-scoped.\" → Refuted: this rests entirely on a comment's claim about `makeGitSbx`, whose bytes (`./harness/git-sbx.js`) are not shown. Cannot re-derive from this unit. Discard.\n- Candidate B: \"Only the hotspot oracle uses `--follow`; messages/szz/owners/coChanged oracles run without it.\" → Survives verification against the literals (one `--follow` among four log invocations), but it's a minor oracle detail; weaker actionability.\n- Candidate C: The 8c guard. The header comment advertises \"a fact-store write-spy records 0 mints\", yet the entire test body contains no spy/mock at all (imports are only vitest, child_process, fs, url, the source module, and the harness). The actual mechanism is fully visible in code: `readFileSync('../src/git-history.ts')`, filter to lines starting with `import`, then `expect(imports).not.toMatch(/knowledge|store|upsert/i)`. This is checkable purely from these bytes, is not a restatement of the file name, and directly changes what an engineer does: refactoring `git-history.ts` to import anything named/pathed with \"store\"/\"knowledge\"/\"upsert\" trips this acceptance test even if runtime behavior is fine, while prose/comments mentioning stores cannot trip it — and no spy harness exists to extend. The header-vs-body mismatch confirms this is exactly the kind of thing a signature skim would miss.\n- Re-check C against refutation criteria: not reliant on a comment's claim about unseen code (I assert only what the test checks, not whether git-history.ts currently violates it); the enumeration of three forbidden tokens is verified against the regex literal; the absence of a spy is verified by scanning every line of the test body. Survives.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"Despite the file header describing SCN-ADAPTER-8c as a fact-store write-spy recording zero mints, the test contains no runtime spy at all — it statically reads ../src/git-history.ts, filters the text down to lines beginning with 'import', and asserts those lines match neither 'knowledge', 'store' nor 'upsert' (case-insensitive), so only import statements can trip the guard.\"}\n```"