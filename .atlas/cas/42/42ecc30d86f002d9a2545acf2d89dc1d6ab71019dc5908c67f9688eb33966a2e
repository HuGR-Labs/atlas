"STEP 1 — scratch reasoning:\n\nI trace every call site of `gateHolds`/`truthDoorHolds` in the file. `deps(g, f)` builds constant closures (`isGrounded: () => g`, `driftDetect: () => f`), so the injected verdicts are fixed per call. Checking where `partialReceipt` appears: only in SCN-GROUND-6-1, in both invocations, always paired with `deps(false, 'FRESH')`. Its expected results (`'NA'`, `false`, untouched store) are exactly what the law (a) automaton predicts for `isGrounded=false` regardless of receipt contents — the law loop in SCN-GROUND-4-1 proves output depends only on `(g, f, input status)`. Meanwhile `groundedReceipt` is used with various `(g, f)` pairs, but its *contents* are likewise never distinguished from any other receipt. Refutation attempts: (1) Does this rely on another file? No — all call sites, the `deps` factory, and the expected literals are in these bytes; I enumerated each occurrence of `partialReceipt`. (2) Is it a stale comment echo? The inline note (\"g_partial ⇒ isGrounded=false\") hints at the injection, but the stronger cross-cutting fact — that *no* call site ever couples the bad receipt with a truthy `isGrounded`, so the suite cannot detect whether the gate body reads the receipt at all — comes from tracing control flow, not from trusting a comment about elsewhere. (3) Exhaustive-count risk? I phrase it qualitatively (\"only ever … would still hold\") after verifying each occurrence individually. (4) Restatement of name/signature? No — the file name and `bindGate`/`GateDeps` signatures suggest the gate consumes a receipt; the fact that this suite's assertions are invariant to receipt inspection is invisible without reading the call graph. Other candidates (the `'advisory'` member of `ALL_STATUS`; the absence of any PBT library despite \"PBT\" labels; `validateTemplate`'s positive/negative examples) survive less well: several merely restate literals or lean on the header comment's framing. The receipt-inertness fact survives.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"In this suite the ungrounded partialReceipt fixture is only ever passed with an injected isGrounded=false, so every assertion would still hold even if gateHolds and truthDoorHolds ignored the receipt argument entirely — receipt-level grounding detection is wholly delegated to the injected seam and never pinned by this file's own expectations.\"}\n```"