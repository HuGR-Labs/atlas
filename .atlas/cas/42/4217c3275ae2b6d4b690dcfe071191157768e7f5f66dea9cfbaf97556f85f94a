"Looking at the declared unions rather than the surrounding prose:\n\n- `Tool` is enumerated in-file as exactly `'atlas-init' | 'atlas-query' | 'atlas-emit' | 'atlas-reconcile' | 'atlas-link'` — five literals, no authoring/projection member.\n- `ToolData` is enumerated in-file as `InitOut | QueryOut | EmitOut | ReconcileOut | LinkOut | QueryEnvelope | AnchorsOut`.\n\nMapping members to tools item by item: `InitOut`↔`atlas-init`, `QueryOut`/`QueryEnvelope`↔`atlas-query`, `EmitOut`↔`atlas-emit`, `ReconcileOut`↔`atlas-reconcile`, `LinkOut`↔`atlas-link`. Every member resolves except **`AnchorsOut`**, which appears in the payload union while no `Tool` literal corresponds to it — the anchors result rides in `Verdict.data`'s union even though `handle(tool: Tool, …)` can, per these types, never be invoked with a tool that produces it. This is checkable purely by reading the two union declarations in this unit; it is not stated by any single signature, and it contradicts the natural assumption that `ToolData` aligns one-to-one with the `Tool` vocabulary. It matters operationally: exhaustive `Tool → payload` mappings can't reach `AnchorsOut`, while consumers narrowing over `ToolData` must still handle it.\n\nRefutation passes: derivable solely from these bytes (two enumerated type literals compared); rests on no comment's claim about unseen files; no unverified counts asserted (membership verified by listing each member).\n\n```atlas-fact\n{\"claim\": \"In this file, the ToolData payload union includes AnchorsOut even though the closed Tool union enumerates only atlas-init, atlas-query, atlas-emit, atlas-reconcile, and atlas-link — no tool literal exists that produces the anchors result, so handle() can never return it per these types while ToolData consumers must still handle it.\"}\n```"