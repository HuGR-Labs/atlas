"STEP 1 — Reasoning scratch:\n\nReading the unit closely: the anchor is named `subtreeHash`, which invites the assumption that it hashes a normalized AST subtree (structure, not bytes). But the spec repeatedly and affirmatively states — in the scope section, in GROUND-5, and in Acceptance item 1 — that the hash is computed over the **raw source slice** with no normalization, so even a whitespace-only reformat *of the cited unit* flips freshness to DRIFTED. Candidate fact drafted: \"reformatting the cited unit drifts its facts, by deliberate design.\"\n\nRefutation pass:\n- Derivable from these bytes? Yes — it is asserted as current normative behavior in GROUND-5 (\"A reformat OF the cited unit DOES drift it\") and tested in Acceptance 1. Not leaning on library knowledge.\n- Does it rest on a comment's claim about unseen code? The amendment history mentions a normalizer \"never built\" elsewhere — I must NOT lean on that absence claim. But my fact doesn't need it: the affirmative invariant plus acceptance criterion in THIS unit establish the behavior. Survives if phrased from those.\n- Exhaustive count/enumeration? No — qualitative form only.\n- More than a name/signature restatement? Yes — nothing in `driftDetect(grounding, src): Freshness` or the name `subtreeHash` reveals raw-byte (not structure-normalized) hashing or that the false alarm is an accepted asymmetry trade (one re-ground costs less than serving HOLDS on a stale fact). An engineer assuming AST-normalized hashing would wrongly \"fix\" this by adding a normalization pass.\n\nSurvived. Emitting.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"A pure whitespace-only reformat of the cited unit deliberately flips its grounding to DRIFTED — an accepted false alarm, not a defect — because the subtreeHash oracle hashes the unit's raw source slice with no normalization step, since any cheap normalizer over raw text would also erase whitespace that is semantically significant in TypeScript/TSX (string, template and regex literals, JSX text, ASI), and the trade is asymmetric: a false alarm costs one re-ground while a false negative lets the truth gate serve HOLDS on a stale fact.\"}\n```"