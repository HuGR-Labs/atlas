"STEP 1 — scratch reasoning:\n\nCandidates considered against the bytes:\n\n1. *\"Segment-wise scope containment ('sr' ≠ 'src')\"* — the split('/') call is visible, but the actual containment semantics live in `isPrefix` from `./anchor-match.js`, whose implementation is not in these bytes. The strong claim doesn't fully re-derive here. Discard.\n2. *\"Empty-string scope behaves as no filter\"* — derivable (`typeof scope === 'string' && scope.length > 0 ? scope : undefined`), but it's essentially a restatement of the guard the docstring also spells out; plausible but weaker than the freshness behavior.\n3. *\"Absent `abstained` ledger returns [] instead of throwing\"* — visible (`if (ledger === undefined) return [];`), but close to what the name/optional-shape suggests and heavily advertised in prose.\n4. *\"Omitting `freshnessOf` yields DRIFTED for every row\"* — the signature only shows the parameter is optional; nothing in the name or types reveals the default verdict. The code line `const freshness = freshnessOf?.(node) ?? 'DRIFTED';` makes this fully checkable within this unit: no recompute supplied ⇒ every emitted `GroundedNegation.freshness` is the literal `'DRIFTED'`, never `'FRESH'`. It is not an exact-count/exhaustive-list claim, not dependent on another module's behavior, not past-tense, and it changes what a caller does (they must wire the adapter-supplied recompute or their entire negation directory reads as drifted). Survives refutation.\n\nSTEP 2 — emit:\n\n```atlas-fact\n{\"claim\": \"If the optional freshnessOf callback is not supplied to negationsOf, every returned negation gets the literal freshness value 'DRIFTED', because the code coalesces a missing recompute to 'DRIFTED' rather than ever defaulting to 'FRESH'.\"}\n```"