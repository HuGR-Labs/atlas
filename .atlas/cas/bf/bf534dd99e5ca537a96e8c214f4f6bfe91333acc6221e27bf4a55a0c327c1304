"Looking at this unit closely: it's a reference page for `atlas doctor`, whose name and subcommand signatures (`archive|why|hotset|reground|index`) suggest a read-only diagnostic suite. Most prose restates the read-only invariant or points at other modules (`DOCTOR_SUBCOMMANDS`, `REAL_INDEXER`, `mergeScip`, `#178`, blackbox test paths) — claims about code outside these bytes, discarded.\n\nDraft candidates and refutation passes:\n\n- *\"Committed-store gate takes down `index`\"* — asserted in-prose and consistent with the exit-code table here; plausible survivor, but its grounding text lives in `query.md`.\n- *\"`tsc -b` before `scip-typescript`\"* — the recipe is in-byes, but the justification cites an external measurement (#178) and a third-party binary's internal resolution behavior; weaker per the rules.\n- *\"`archive` is the only shipped command printing a content address\"* — exhaustive claim over the whole CLI surface, not verifiable from these bytes; discarded.\n- *Single-dump overwrite* — check the shown literals directly: in the four-language example, the Python leg prints `run: scip-python index --output .atlas/index.scip` and the TypeScript leg prints `run: scip-typescript index --output .atlas/index.scip` — identical output path, confirmed by reading the bytes, and the `note:` line states the second overwrites the first. Nothing in the command's name or invocation signature hints that a polyglot repository cannot hold indexes for two languages at once, and following the printed commands naively silently discards the first language's edges. This survives: it is grounded in the unit's own literals and example transcripts, requires no trust in external code, makes no unverified count claims, and would change what an engineer does (sequence or scope their indexing rather than run both commands back-to-back).\n\n```atlas-fact\n{\"claim\": \"In a repository with more than one supported language, every indexer command the `doctor index` leg prints writes the same single dump path `.atlas/index.scip`, so running them sequentially leaves only the last-run language's index.\"}\n```"