"STEP 1 — scratch reasoning (not persisted):\n\nReading the bytes: `relationWitnessTrusted` (private, but its effect is fully exposed through exported `stripForgedRelationSeal`) returns true only under a four-conjunct test. Two of them are `rw.relationKind === 'depends-on'` and `rw.relationKind === node.relationKind`. Conjoining them logically forces `node.relationKind === 'depends-on'`.\n\nRefutation attempts:\n- Resting on a comment? No — the conclusion follows mechanically from the two visible comparisons plus the strip logic (`seal !== 'proven'` passthrough, then `if (!trusted) strip seal+witness`). No reliance on ADR/issue references or other files.\n- Exact-count trap? The claim is qualitative (\"only `'depends-on'`\"), and I verified the singleton by reading the literal: any other kind makes the second conjunct contradict the first, so trusted is false and the seal/witness are destructured away. Item checked against the bytes.\n- Guessable from name/signature? No — `relationWitnessTrusted(node: RelationNode): boolean` and `stripForgedRelationSeal<T>(node: T): T` say nothing about a per-kind provability lockstep; nothing else in the signatures hints that a sealed relation of any non-`depends-on` kind can never survive this door regardless of its witness contents.\n- Restating what the code plainly does? No — it's a derived invariant requiring combining two conjuncts across two functions.\n\nSurvived. Emitting.\n\n```atlas-fact\n{\"claim\": \"A relation retains its `seal:'proven'` (and its witness) through stripForgedRelationSeal only if its relationKind is 'depends-on': the trust test demands the witness's relationKind be exactly 'depends-on' and equal the node's own relationKind, so a proven-sealed relation of any other kind is unconditionally stripped of both seal and witness.\"}\n```"