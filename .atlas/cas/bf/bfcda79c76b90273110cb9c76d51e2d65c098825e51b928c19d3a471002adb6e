"STEP 1 — scratch reasoning (discarded candidates):\n- *Comment-sourced claims* (e.g., \"adapter-io exports `SCIP_INDEX_REL`\", \"REQ-INDEX-3a mandates per-language pinned binaries\", \"TOOLS-12 semantics\") all point outside these bytes — discarded.\n- *\"2 indexed document(s)\"* is an exact-count assertion, but it's trivially tied to the two-entry fixture array and isn't the surprising part — weaker than the structural fact behind it.\n- *Version-pin-as-shape* (`\\d+\\.\\d+\\.\\d+` regex instead of a literal) is derivable and mildly useful, but it's already flagged by its adjacent comment and is less action-changing.\n- *`ATLAS_MINE_SLOT: 'advisory'`* env pinning is visible and real, but secondary.\n- Survivor, checked against the CODE itself: `plannedOutput()` locates the `run:` line, splits on whitespace, and takes the token at `indexOf('--output') + 1` (returning `''` otherwise); test 2 asserts that token is non-empty and that `readFileSync(join(repoPath, planned))` equals `readFileSync(join(repoPath, SCIP_REL))`. So the printed plan's write target is validated positionally AND byte-for-byte against the literal `.atlas/index.scip` — none of this is inferable from the file's name/signature, it rests only on visible code, and it means flag reordering, `--output=` syntax, or any path drift breaks the test. Survives refutation.\n\n```atlas-fact\n{\"claim\": \"Test 2 scrapes the planned write target as the whitespace-delimited token immediately after `--output` in the printed `run:` line and requires that reading the repo-relative file at that scraped path yields bytes equal to `.atlas/index.scip` — so a `--output=` flag style, reordered flags, or any divergence between the printed path and the reader's literal path makes the scrape empty or the comparison unequal, failing the test.\"}\n```"