"STEP 1 — scratch reasoning:\n\n- Candidate A: \"Tests mutate global process.env and restore in afterAll.\" True and visible, but close to boilerplate; low actionability.\n- Candidate B: \"Ids are 64-hex content hashes.\" Checkable via the `idOf` regex, but largely restates the file's stated theme (content-addressed dedup).\n- Candidate C: **cross-test sequential dependency**. Trace the bytes: `beforeAll` creates one `repo` and only *drafts* facts; `emitFact` calls live inside individual `it` blocks. Refutation attempt: maybe `draftFact` also persists, so A2's expected `C1; C1-reworded` wouldn't need D1? Counter-check against these bytes: D0 asserts `invLines(query(repo))` equals exactly `[inv T1 ${F.id} [FRESH]: C1]` — a single line, no reworded body — which is only possible if the beforeAll drafts persisted nothing. Therefore the `C1-reworded` body in A2's expected array can originate solely from D1's `emitFact(repo, Frew)`, and the PAYOFF test's 3-node count likewise accumulates every prior test's emissions. Since `repo` is never recreated between tests, the suite is provably order-dependent despite the \"blackbox scenarios\" framing the filename suggests. Survives refutation; derived from control flow + expected literals, not from any comment's claim about unseen files.\n\n```atlas-fact\n{\"claim\": \"Despite reading as independent blackbox scenarios, the tests are strictly order-dependent: the fixture repo and its durable store are created once in beforeAll and never reset between tests, and A2's expected query output contains 'C1-reworded', which only the earlier D1 test's emitFact call writes (D0's exact single-line expectation proves the beforeAll drafts alone persist nothing), so reordering, skipping, or running any test in isolation breaks later assertions.\"}\n```"