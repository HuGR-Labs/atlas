"STEP 1 — scratch reasoning:\n\nCandidates drafted while tracing control flow:\n\n1. *\"`byTrigger` is dormant / returns []\"* — the stronger form depends on `@atlas/index`'s handling of an empty `triggers` map, which is not in these bytes. The weak form (empty map literal) is visible, but it is loudly announced by adjacent comments, making it low-value. Discard.\n\n2. *\"Packs always carry `subsumes`/`sameAs` = []\"* — already revealed by the `RetrievalEnvelope` type (`readonly never[]`). Derivable from signature/types alone. Discard.\n\n3. *\"`blastRadius` can contain hashes absent from `factStore`\"* — the two `if`s in the loop are independent (missing CAS read still appends to the anchor multimap). True and in-code, but downstream consequence depends on unseen `@atlas/index` resolution behavior. Weaker actionability. Set aside.\n\n4. *\"Double rehydration per `retrievalPack` call\"* — trace: `retrievalPack` → `buildRetrievalModel(axes, store)`, whose first line runs `currentNodes(rehydrateProjection(store))`; then the same call hands off to `packFromModel`, whose body runs `currentNodes(rehydrateProjection(store))` again. Verified at both call sites in the shown bytes; count \"twice\" checked item-by-item. No comment acknowledges this duplication (comments discuss freshness-per-query as intended behavior, not the repeated cost). Refutation pass: not resting on any external library behavior (only that the two expressions execute, which the code shows); not a comment claim about other files; not derivable from the name/signature; actionable — a caller issuing multiple queries over an unchanged tree should build the model once and drive `packFromModel` directly, halving the projection work. Survives.\n\nSTEP 2 — emit surviving fact:\n\n```atlas-fact\n{\"claim\": \"Every retrievalPack call executes rehydrateProjection(store) twice — once inside buildRetrievalModel and again inside packFromModel — so callers issuing multiple queries can halve that work by building the model once and calling packFromModel directly.\"}\n```"