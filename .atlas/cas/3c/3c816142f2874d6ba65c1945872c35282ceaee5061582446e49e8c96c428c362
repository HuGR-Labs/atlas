"STEP 1 — scratch reasoning:\n\nCandidates drafted from close reading of the bytes:\n\n- *Fixture rebuilt per test via mkdtemp/beforeEach* — true, but standard vitest hygiene; an engineer would likely guess it. Weak novelty.\n- *Ratchet anti-rot behavior* — pinned in tests, but the mechanism (env-carried JSON ledger) overlaps with a stronger candidate below; also the describe heading already advertises \"ratchet can only shrink.\"\n- *\"2 REQ, 3 SCN, 1 INV, 1 PROP, 1 WP\"* — I counted against the fixture: REQ-BLK-1a/1b = 2; SCN-BLK-1a-1, -1a-2, -1b-1 = 3; INV-BLK-1, PROP-BLK-1, WP-1.A.BLK = 1 each. Verified, but it's a narrow output-format detail.\n- *Black-box subprocess contract* — strongest survivor: `runGate` spawns `id-integrity.mjs` via `execFileSync(process.execPath, [GATE], ...)`, passes configuration ONLY through `ID_INTEGRITY_ROOT` (always) and `ID_INTEGRITY_KNOWN` (when provided) — I checked, no other channel exists — and captures failures as `` `${e.stdout ?? ''}${e.stderr ?? ''}` `` — streams concatenated, never separated. Assertions then regex-match exact human phrasing (`/ID-1 duplicate definition: 'REQ-BLK-1a' is defined 2×/`, `/ID-2 dangling id citation: .../`) and check `not.toContain('✗')`. Nothing here relies on a comment's claim about another file; every element is a literal in this unit.\n\nRefutation pass: derivable from these bytes? Yes, entirely. Comment-dependent? No. Exhaustive claims? The env-var list is complete and verified in `runGate`. Restatement of name/signature? No — the filename reveals nothing about subprocess spawning, the env-var protocol, stream merging, or that message TEXT is load-bearing.\n\nImplication that changes what you do: refactoring the gate's internals is safe, but rewording violation messages, changing the ✗ marker, renaming either env var, or shifting output between stdout/stderr breaks this suite with zero behavioral change — and stray stderr noise can flip the false-alarm assertions.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"The gate is exercised only as a black-box child process configured exclusively through the ID_INTEGRITY_ROOT and ID_INTEGRITY_KNOWN environment variables, and the suite pins its human-readable output text (exact violation phrasings and the absence of '✗') against stdout and stderr merged into one buffer, so rewording messages, renaming those env vars, or moving output between streams breaks the tests even when detection behavior is unchanged.\"}\n```"