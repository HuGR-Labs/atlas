"Reading the bytes: the function body starts with `const filter = typeof unit === 'string' && unit.length > 0 ? unit : undefined;`, and later `if (filter !== undefined && u !== filter) continue;`. So a `unit` argument of `\"\"` fails the `length > 0` guard, collapses `filter` to `undefined`, and the filter check never fires — meaning `testVacuitiesOf(p, \"\")` returns every test-vacuity fact, byte-identical to omitting the argument, not an empty result set. Drafting candidates: (a) the empty-string-collapse — survives, it's pure control flow on these lines, not a comment claim about other files; (b) the conditional-spread seal omission — also code-visible but largely echoed by its adjacent comment; (c) skip-don't-throw on malformed carriers — visible but explicitly advertised in the docstring, closer to a known contract. Candidate (a) is the least guessable from the `unit?: string` signature alone and changes caller behavior (forwarded user input must be normalized upstream). Verified once more against the two lines above: holds.\n\n```atlas-fact\n{\"claim\": \"Passing an empty string as the unit argument behaves exactly like omitting it — the length > 0 guard in testVacuitiesOf collapses '' to an absent filter, so every test-vacuity fact in the projection is returned unfiltered rather than none.\"}\n```"