"Reading the bytes closely:\n\n- The loop gives every visited site exactly one boolean in the `window`: `admitted` starts `false` and is set `true` **only** inside the `verdict.emitted` branch. Both the `seed === null` path and the `'abstain' in seed` path leave it `false`, as does a gate rejection — yet all three still `window.push(admitted)` and consume a `visited` tick.\n- So the GEN-2e halt (`window.length >= 20 && admitsIn(window) < 4`, constants verified in the exports: 20 and 4) is driven by *any* non-emission, not just gate rejections — an abstention-heavy stretch alone trips it, with budget left unspent (it's a `break`, not a drain).\n- Checking refutations: this is pure control flow in the shown bytes (no reliance on other files or task refs); the numbers are the visible exported literals; it is not a restatement of `runExtract`'s signature, which says nothing about how abstentions feed the halt heuristic; and it is consistent with, not contradicted by, the surrounding comments. Other candidates (copy-before-sort, no try/catch around the injected calls, ceiling-zero behavior) are either weaker, more conventionally inferable, or rest partly on a doc-comment's framing rather than the flow itself.\n\nThe surviving fact:\n\n```atlas-fact\n{\"claim\": \"In runExtract's marginal-value halt, every non-emitted outcome — including a plain model abstention (null), a malformed-answer abstention, and a gate rejection — pushes false into the trailing-20 window, so a streak without any emitted fact stops the sweep before the budget ceiling is spent.\"}\n```"