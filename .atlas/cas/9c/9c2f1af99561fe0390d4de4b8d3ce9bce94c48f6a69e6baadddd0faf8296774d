"Looking closely at the bytes: this is a frozen design-contract document, so nearly everything points at code outside the unit (`router.ts:197-208`, `types.ts:162`, `store.ts:47`, task numbers, ADR-0003, commit hashes) — candidates resting on those pointers die immediately under the refutation pass, since they describe *other* files, not these bytes.\n\nDrafting candidates from what the document itself specifies:\n\n1. *\"nodeKey excludes the claim body\"* — explicitly tagged \"(Already coded: …)\" pointing at `types.ts`. External. **Discard.**\n2. *\"always-merge removed from both call-sites (router.ts:197-208, 394-397)\"* — a claim about other files' contents and their line ranges. Not re-derivable here. **Discard.**\n3. *\"Wave 2 built in `d290bd2`\"* — commit/task provenance about elsewhere. **Discard.**\n4. *\"SCN-KNOW-15h-1 asserts only `nearDuplicateProbe(...) === true`\"* — describes a test file not shown. **Discard.**\n5. *\"Subsumes carries explicit `broader`/`narrower` field names\"* — close to a restatement of the one type literal shown. Weak. **Discard.**\n6. *\"Emission is the FULL pairwise set, not a transitive reduction\"* — this lives entirely inside DP-2's own definition, adjacent to the in-byte `Subsumes` literal: every pair passing gates (1–4) is emitted, with the crate⊃mod⊃fn chain producing the long crate⊃fn edge as well, and the old nearest-target behavior named as a dropped artifact. No external pointer needed; the arithmetic of the worked example checks out (three levels ⇒ three unordered pairs). An engineer assuming \"nearest ancestor only\" — the common default — would build and consume this relation wrong. Survives.\n7. Retraction-semantics candidate (append-only `sameAsRetracted`, retraction-blind `sameAsClassOf`) — semantically rich but saturated with external refs (#83, `write/link.ts`, `governed-link.ts`, ADR-0003) and supersession caveats; too much rests on assertions about unseen modules. **Discard** as riskier than #6.\n\n#6 survives: it is self-contained in the unit's own normative text, non-obvious from the title \"Dedup & Identity\", and directly changes both implementation (emit all valid pairs) and consumption (expect transitively redundant edges).\n\n```atlas-fact\n{\"claim\": \"Per DP-2, deriveSubsumes emits the complete pairwise set — every current-node pair passing all four gates gets a Subsumes edge, so a crate::module::function chain yields the direct crate-over-function edge too — rather than a transitive reduction or nearest-ancestor targeting.\"}\n```"