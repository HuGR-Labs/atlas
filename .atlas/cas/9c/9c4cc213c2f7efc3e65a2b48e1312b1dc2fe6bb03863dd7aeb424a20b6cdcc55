"Looking at the bytes: the Invocation section declares `<mergeBase>` as *required*, which invites the assumption that it is validated. But the \"Things worth knowing\" section explicitly measures otherwise — `atlas reconcile deadbeef`, `atlas reconcile not-a-sha-at-all`, and even an empty string all exit `0` with `status: ok` in a repo that has real drift against its true base, because an unresolvable rev produces an empty drift set, and the exit-code table maps \"no semantic flip\" to `0`. Checking refutation angles: this doesn't lean on a comment about another module (the `deriveStatus`/`packages/cli/src/map.ts` reference and the `TOOLS-8` tag do, and are discarded); it isn't a restatement of the signature (the signature suggests validation); it isn't an unverified count or enumeration (I phrase it qualitatively); and its evidence — concrete inputs and observed outcomes — lives entirely in this unit. It survives, and it is actionable: anyone wiring this into CI must resolve the merge base themselves first.\n\n```atlas-fact\n{\"claim\": \"atlas reconcile does not validate <mergeBase>: an unresolvable argument like deadbeef or not-a-sha-at-all yields an empty drift set and exits 0 with status ok even when real drift exists, so CI callers must resolve the merge base themselves.\"}\n```"