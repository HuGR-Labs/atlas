"STEP 1 — scratch reasoning:\n\nCandidates drafted and refuted:\n\n- *“SCN-KNOW-3b-1 was vacuous / real-mint lives in `freshness.know3.realmint.test.ts`”* — this rests entirely on comments asserting things about files not shown and history (“used to be titled”, “REQ amended”). Discarded.\n- *“bindFreshness narrows STALE→DRIFTED”* — the mechanism lives in `../src/lifecycle/freshness.js`; here it appears only as an expectation plus a test title that already states it. Restates the name; discards.\n- *“displayLines never affects the verdict”* — again, the inertness of `displayLines` inside `bindFreshness` is asserted, not implemented, in these bytes; and the test title says as much. Discarded.\n- *Fixture-oracle control flow* — survives. Reading the code: `mkTree` places units only under `spatial.children` (territory/dependency are empty roots); `subtreeOracle.driftDetect` builds its lookup exclusively from `src.spatial.children`, then requires **every** grounding entry's `anchor.qualifiedPath` to resolve there (`cur !== undefined`) with an equal hash before returning FRESH. So an anchor absent from the spatial rail — including one that exists conceptually on another rail — cannot ever be FRESH under this fixture; it fails closed to DRIFTED. None of that (rail-scoping, missing-key fail-closed) is recoverable from the fixture's name or the call signature, and it changes what a test author does: anchoring a fixture fact anywhere but the spatial rail silently guarantees DRIFTED, and multi-anchor facts need every anchor resolvable. Qualitative form, no counts, fully checkable in the shown bytes.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"The fixture oracle resolves anchors only against the spatial axis's child-node keys and fails closed, so any grounding entry whose anchor is absent from that rail — or whose subtreeHash differs — forces the whole fact to DRIFTED.\"}\n```"