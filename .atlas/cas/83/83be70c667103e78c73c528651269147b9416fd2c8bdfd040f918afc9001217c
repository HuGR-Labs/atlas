"Looking at the actual declarations (not the comment apparatus): `invariants` and `advisory` are both declared as exactly `readonly PackInvariant[]`, and `PackInvariant` itself contains only `nodeId`, `tier` (bare `Tier`, un-narrowed), `claim`, and `freshness` — no band tag, no brand, no discriminated generic. Scanning the whole unit confirms there is no conditional type, branded key, or constrained parameter anywhere that could tie a row to a band.\n\nTrying to refute candidates:\n- \"8 injection kinds\" — a plain restatement of a visible union literal; discarded.\n- \"`stale` is a global watermark vs per-fact `freshness`\" — rests entirely on comment claims citing ADR-0002/N11, not on code; discarded.\n- \"`advisoryDropped = 0` means nothing was looked-at-vs-dropped\" — comment claim about issue #130 semantics; discarded.\n- \"Type-only module\" — true but near-mundane summary of the declaration forms; weaker than the band finding.\n- Band-enforcement candidate survives: identical element types plus an unconstrained `tier` means the type system offers no guard on which rows land in which array — checkable purely from these declarations.\n\n```atlas-fact\n{\"claim\": \"Both Pack.invariants and Pack.advisory are declared with the identical element type readonly PackInvariant[], and PackInvariant carries no field marking which band a row belongs to (its tier is a bare, un-narrowed Tier), so the governing/advisory band separation is purely a producer-side convention that nothing in this contract's types actually enforces.\"}\n```"