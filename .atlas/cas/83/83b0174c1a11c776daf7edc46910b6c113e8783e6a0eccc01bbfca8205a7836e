"STEP 1 — Reasoning (scratch): Candidates drafted and refuted:\n\n- *“The staged fact carries both `scope: 'pkg/payments'` and `authzScope: MINED_SCOPE`”* — this is asserted by `expect`s, but its truth depends entirely on `decideStaging`'s unseen implementation in another file. Refuted under the rules.\n- *“The mint recomputes ids from the poisoned `'SHOULD-BE-REMINTED'` input”* — supported mainly by a comment; no assertion in this unit inspects the reminted id. Refuted.\n- *“primaryAnchor binds to the subject endpoint END_A”* — essentially announced by the test name/comment; the verification lives in `@atlas/knowledge`, not here. Refuted.\n- *“Type alias `Candidate` is used before its declaration”* — true and byte-visible, but standard TS hoisting every competent engineer knows; not action-changing. Refuted.\n- *Survivor:* both fixture factories end in `} satisfies RelationNode) as unknown as Fact;` and `} satisfies NegationNode) as unknown as Fact;`. The `satisfies` clause only checks against the knowledge node types; the `as unknown as Fact` bridge erases all checking against the `Fact` parameter type `decideStaging` declares. This is fully checkable in these bytes, is invisible from the suite's name/signature alone, and is actionable: tightening `Fact` or adding input validation will break these fixtures at runtime, never at compile time — the seam here is deliberately type-blind.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"Both the relation and negation fixtures reach decideStaging's Fact-typed parameter only by casting their knowledge-node literals through `unknown` to the genesis `Fact` type, so this suite gets no compile-time assurance that those shapes actually conform to Fact — any drift breaks at runtime, not in the type checker.\"}\n```"