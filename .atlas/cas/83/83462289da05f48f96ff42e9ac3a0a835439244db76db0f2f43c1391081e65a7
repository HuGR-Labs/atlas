"STEP 1 — scratch (discarded candidates):\n\n- \"Store has no delete\" — only asserted in a header comment about code not shown here. Discarded.\n- \"MAX_SEAM_CARRY === 74\" — literally pinned via `expect(MAX_SEAM_CARRY).toBe(74)` in these bytes; valid, but a runner discovers it trivially and it changes little about design decisions. Held as backup.\n- \"put() scrubs on ingest / identical hashes across chunkings\" — visible, but close to what the filename already advertises. Discarded as near-restatement.\n- Contingent-seam leg: `admitToBuffer(new Uint8Array(0), enc.encode('[REDACTED]'))` must yield `[REDACTED]`; then `admitToBuffer(partial, enc.encode(text.slice(at)))` (chunk `_SYNTHETICNOTAREALTOKEN01`) must yield `ghp_ABCDE[REDACTED]`. Checking refutation: the bytes `ghp_ABCDE` are a substring of neither argument of the second call (`[REDACTED]` and `_SYNTHETICNOTAREALTOKEN01` — verified letter by letter). So the required output cannot be a pure fold of the two arguments; the suite mandates state carried with the returned buffer (corroborated in-file by `seamCarryOf(buf)` reading metadata off the buffer), and that state lets a committed `[REDACTED]` be retroactively rewritten to raw input. Survives: derivable solely from the calls, literals, and assertions in this file; not a count/enumeration; far beyond the unit's name.\n\nSTEP 2 — surviving fact:\n\n```atlas-fact\n{\"claim\": \"The suite requires admitToBuffer to carry hidden state with its returned buffer: a buffer holding '[REDACTED]' for the ambiguous prefix '[REDACTED]' must become 'ghp_ABCDE[REDACTED]' when fed the next chunk, even though those raw bytes appear in neither of that call's arguments — meaning a committed placeholder can be retroactively un-redacted, so the streamed buffer cannot be treated as a pure fold of its inputs.\"}\n```"