"STEP 1 — Scratch reasoning:\n\nScanning the bytes for candidates:\n\n- *“Default pass visits sites but seeds nothing”* — this is the headline finding, spelled out in the header comment and the `it()` titles; largely a restatement of the unit’s own narrative. Discard.\n- *“Nonexistent repo exits 0 with structural blame”* — true per case 5’s assertions, but case 5’s title announces it verbatim (“TOTALITY preserved: a non-existent repo path is a clean, empty pass…”). Semi-announced. Weak.\n- *“Case 3 defeats tsc via `as unknown as MineDeps`”* — the cast is visible, but a reader sees it on the very line, and its stated rationale leans on a comment’s claim about the tsconfig (unseen). Marginal actionability. Discard.\n- *“Env is the only pinned seam”* — half comment-assertion about `mine.js` defaults (unseen). Discard.\n- **Hard-coded frontier size**: counting the literals tying the run to exactly 2 sites — case 1: `expect(report.budgetSpent).toBe(2)`; case 3: `expect(plan.sites.length).toBe(2)`; case 4: `toContain('cost: llmCalls 2 · budgetSpent 2')` and `toContain('2 site(s) visited and every one abstained')`. That is four separate bare-literal assertions spread across three cases, with no shared constant in this file (case 3’s budget ceiling is dynamic via `plan.sites.length`, not a literal). Re-checked item by item: four. None of the titles or comments flag this coupling; it is purely byte-local (all four literals are in the shown code), it is not the name/signature, and it changes what an editor of `mine-fixtures` or the ranking must do — touch the fixture’s dep graph and four independent assertions in three different cases go red unless updated in lockstep. Survives refutation.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"The expected mined-frontier size of two sites is hardcoded as bare literals in four separate assertions across three different cases (case 1's report.budgetSpent toBe(2), case 3's plan.sites.length toBe(2), and case 4's two stdout substrings 'cost: llmCalls 2 · budgetSpent 2' and '2 site(s) visited and every one abstained') with no shared constant, so any change to the fixture's ranked frontier requires four synchronized edits.\"}\n```"