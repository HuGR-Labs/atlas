"STEP 1 — SCRATCH (not stored):\n\nReading the bytes closely:\n\n- Candidate A: \"The emit call passes `NaN as unknown as Hash` and still gets `emitted: true`.\" — Fully in-byte (`.emit(negation(), NaN as unknown as Hash)` followed by `expect(out.emitted).toBe(true)`). True, but it is a plainly visible literal once the body is read; low decision-value compared to candidate B.\n- Candidate B: The fixture `negation()` sets `edgeModel: 'ignored'`, yet `admitUnder(E1)` + read with `bindFreshnessOracle(axesWithScope(), E1)` expects `FRESH`, and the same store read with E2 expects `DRIFTED`. The two reads use structurally identical axes (same pure constructors, same folded hashes) and the same store; the ONLY varying input is the oracle's model string. If the stored model were the node's own `'ignored'`, the E1 read could not come back FRESH while the E2 read comes back DRIFTED — both would match/mismatch identically. So the value that survives to read time must be the emit-time configuration model (E1), not the node's field. Refutation attempts: (i) does this lean on comments? No — the deduction is forced by the assertion literals and the fixture literal alone; the comment merely corroborates. (ii) Is it about elsewhere? It is *demonstrated* by this unit's own literals and expectations, anchored here. (iii) Exhaustive counts? None. (iv) Name/signature restatement? No — the file name says nothing about the fixture's `edgeModel: 'ignored'` being overridden.\n- Candidate C (fail-closed absent oracle): true, but it is literally the headline of test 4 — a summary of what the code plainly does.\n- Candidate D (identity nodeHashOfPath): visible literal, flagged by its own comment; weak.\n\nB survives and is the most action-changing: fixture authors must not assume the node's `edgeModel` field is authoritative.\n\nSTEP 2 — RESULT:\n\n```atlas-fact\n{\"claim\": \"The NegationNode fixture is emitted with edgeModel set to the literal 'ignored', yet the admitted negation reads FRESH under the emit-time model E1 and DRIFTED under E2 — so the persisted edge model provably comes from the emit door's configuration, not from the node's own field.\"}\n```"