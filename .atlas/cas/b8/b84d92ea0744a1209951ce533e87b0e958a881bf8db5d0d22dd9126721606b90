"Looking at this unit: it's the Atlas tools reference doc. Signatures alone (`atlas-emit <node> --at <sha> → EmitOut`, etc.) tell an engineer the write door exists and is fail-closed — but not *where* the enforcement physically lives.\n\nDraft candidates:\n- *Exit 2 only on semantic drift* — refuted: printed directly in the `atlas-reconcile` signature comment; known from signature.\n- *Stale-flag two-part definition* — partially external: leans on `freshness`/DRIFTED vocabulary (N11/ADR-0002) defined outside these bytes; weaker.\n- *`--retract` mode of atlas-link, append-only evidence* — plausible, but the mechanics lean on ADR-0003 §Retraction phrasing; keep as runner-up.\n- *TOOLS-15 structural enforcement* — survives: the mechanism (append-only/permissioned medium **plus** read-side content-address integrity check rejecting un-emitted rows) is stated in full in the TOOLS-15 bullet and operationalized in acceptance item 13, entirely within these bytes. No exact counts, no reliance on unseen files. Not derivable from any tool name/signature, and clearly behavior-changing: it tells an engineer that bypassing `atlas-emit` via shell/filesystem write is futile by construction — the row either can't land or is never served — and that the guarantee belongs to the storage layer, not tool convention.\n\n```atlas-fact\n{\"claim\": \"The single-write-door is enforced at the storage layer, not by tool convention: the store medium is append-only/permissioned and every read performs a content-address integrity check that refuses to serve any row not written through atlas-emit, so a direct shell/filesystem write that bypasses the door either cannot land or is never surfaced as a served fact.\"}\n```"