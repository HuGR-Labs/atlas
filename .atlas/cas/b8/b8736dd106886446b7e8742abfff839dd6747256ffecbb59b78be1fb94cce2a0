"STEP 1 — scratch reasoning:\n\nCandidate A: the fsync timing numbers (~23 ms/~21 ms) — these live only in a comment describing measurements on a machine I cannot see; not re-derivable from the bytes. Discard.\n\nCandidate B: \"ENOSPC/EACCES propagate rather than retry.\" The code does show publish catching only EEXIST and rethrowing, with no try/catch in `commitLoop`. Survives as code-checkable, but it is fairly close to what a careful reader of the error-handling shape would infer; weaker than C.\n\nCandidate C: the no-op fast path. Lines: `const snapshot = read.projection ?? emptyStore(); ... if (decision.next === read.projection) return { settled: true, out: decision.out };`. From these two lines alone: `decide` receives the durable head's projection object whenever one exists, and returning that *same object reference* short-circuits the entire publish (no temp, no fsyncs, no link), reporting success. Direct consequence: an implementation that mutates the snapshot in place and returns it would be classified as a no-op and its edits never reach disk. Fully checkable within this unit's control flow; no reliance on cross-file comment claims; qualitative, no counted enumeration. Also clearly beyond the name/signature — nothing about `commitSidecar(ctx, decide)` suggests a reference-identity elision gate, and it changes what a caller must do (rebind via a fresh object, never mutate). Survives.\n\nCandidate D: superseded-vs-lost arithmetic (`headNow < gen + RETAINED_GENERATIONS`) — also code-checkable, but it is heavily narrated by adjacent commentary and is more a design rationale than a \"changes what you do\" operational fact for a typical engineer; C is sharper.\n\nSTEP 2 — emit surviving fact:\n\n```atlas-fact\n{\"claim\": \"In commitLoop, a decision that returns the very projection object it was handed — reference-identical (===) to the durable head's projection — settles as successful with no file written at all, so a decide implementation that mutates the snapshot in place instead of rebinding to a fresh object would have its changes silently discarded.\"}\n```"