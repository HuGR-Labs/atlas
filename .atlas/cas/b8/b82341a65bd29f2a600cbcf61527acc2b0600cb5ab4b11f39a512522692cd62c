"STEP 1 — scratch reasoning:\n\nReading the bytes closely, several surface-level things are obvious from the name (\"upsert-seal-carry\"): it pins that `WriteRequest.seal` lands on the stored row, and that UPDATE/SUPERSEDE take the seal from the incoming write only. Those carry-semantics are heavily signposted by the describe-block titles, so stating them adds little.\n\nWhat the name does **not** telegraph is the strictness of the absence assertions. Beyond `expect(row.seal).toBeUndefined()`, three separate cases (CREATE-without-seal, the UPDATE no-carry-forward case, and the SUPERSEDE no-carry-forward case) additionally assert `Object.prototype.hasOwnProperty.call(row, 'seal')).toBe(false)` — demanding the key be genuinely absent from the row object, not merely undefined-valued. This is checkable purely in the shown code (literal assertions, plus the factories' conditional spread `...(seal !== undefined ? { seal } : {})` proving omission is representable), rests on no comment about other files, and makes no counted/exhaustive claim. It is actionable: an implementation writing `seal: req.seal` unconditionally (yielding `seal: undefined`) or spreading a prior row (`{ ...prior, ... }`) would satisfy every `toBeUndefined()` yet fail these `hasOwnProperty` checks — so it forces conditional-spread/delete style in the reducer. No candidate relying on the header comments' claims about `wp-196a`, `atlas mine` measurements, or `router.ts:139` survives, since those concern bytes not shown. This one survives.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"When a write omits `seal`, these tests demand the stored CurrentNode genuinely lack the property — asserted via Object.prototype.hasOwnProperty.call(row, 'seal') being false, not just row.seal being undefined — so a reducer that unconditionally assigns `seal: undefined` or spreads a prior row's seal forward would pass the value checks yet fail.\"}\n```"