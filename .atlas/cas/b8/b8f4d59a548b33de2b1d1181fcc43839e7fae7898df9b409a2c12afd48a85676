"STEP 1 — Reasoning (scratch):\n\nScanning candidates:\n\n1. \"Goldens transcribed from docs/requirements/goldens-adapters.md\" — comment claims about a file not in these bytes. Discard.\n2. \"Fixtures come from a shared harness (`makeFixRepo`, `T_ref`)\" — visible in the import, but largely a plain restatement of the header and imports; low action value. Weak.\n3. \"`src/generated.ts` absence is asserted only in SCN-1c, not SCN-1a\" — checkable in bytes but marginal action value, and its significance (gitignored vs. merely absent) depends on unseen harness content. Discard.\n4. **WP-N8 test (a) pins the fail-closed empty result to the exact literal `{ path: '.', children: [] }`.** Refutation pass: Is it in the bytes? Yes — `expect(walk).toStrictEqual({ path: '.', children: [] })` is a direct assertion, not comment-derived. Does it lean on unseen code? No — it's this unit's own expectation, and `toStrictEqual` makes it exhaustive (no extra keys, no children). Is it non-obvious from the name `walkFileTree`? Yes — nothing in the name suggests the sentinel empty tree uses the root path `'.'` specifically (vs. the absolute input path, `''`, or some marker), and consumers/implementers matching on the root node would need to know this. Would it change what they do? Yes — anyone consuming or reimplementing `walkFileTree` must treat `{ path: '.', children: [] }` as the total/fail-closed return shape. Survives.\n5. \"Determinism test compares `JSON.stringify` output, making it sensitive to key insertion order, unlike `toStrictEqual`\" — true and in the bytes, but the action delta over \"tests determinism\" is small. Runner-up, discarded in favor of 4.\n\nSTEP 2 — Emitting the surviving fact:\n\n```atlas-fact\n{\"claim\": \"The WP-N8 boot-guard suite pins walkFileTree's fail-closed behavior on a non-git directory to the exact empty-tree shape { path: '.', children: [] }, i.e. a lone root node whose path is the literal '.' with no children.\"}\n```"