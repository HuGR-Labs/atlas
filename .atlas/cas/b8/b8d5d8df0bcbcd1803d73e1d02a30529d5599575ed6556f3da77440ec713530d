"STEP 1 — Reasoning scratch:\n\nReading the bytes: this is a flat JSON map of ~50 `REQ-*` keys, each pinned with an `@hash`, whose values describe why a quoted normative clause fails byte-exactness against some invariant. Nearly every value terminates in the same provenance bracket: `[pre-existing at master e4882a3]`.\n\nDraft candidates and refutation passes:\n\n1. *\"atlas-grounding.md contains 'span' zero times\"* — this is asserted inside REQ-GROUND-1d's value, but it is a claim about ANOTHER file not shown here. Even if accurate, it does not re-derive at this unit's bytes. Discard.\n2. *\"(91 of 173 cited anchors have more than one)\"* — an external statistic embedded in REQ-TOOLS-9b's prose; unverifiable from these bytes, and a numeric claim I cannot recount here. Discard.\n3. *\"The PERSIST-14 series skips letter 'e'\"* — literally true of the keys (a, b, c, d, f), but whether `-e` was removed or never existed is not decidable from these bytes, and the gap may be meaningless. Weak actionability; discard in favor of a sharper candidate.\n4. **Re-keying candidate:** scanning for `[was` yields exactly two occurrences: REQ-GROUND-5b (`[was d3adbcea @ master e4882a3]`) and REQ-KNOW-3b (`[was 1c57cb24 @ master e4882a3]`). Both of those values also contain the identical annotation `CO-AMENDED 2026-08-09 (ADR-0014, rule #198)`; no other value mentions rule #198 or that date. Every remaining value uses the plain `[pre-existing at master e4882a3]` tag. Refutation check: is any of this resting on a comment about unseen code? No — the `[was …]` tags and the shared amendment annotation are data IN this file, string-checkable item by item. Is the enumeration safe? Yes: two occurrences of `[was`, verified across all keys. Is it more than a name/signature restatement? The filename tells you it's a clause ledger; nothing in the name reveals that two rows are re-keyed descendants of older IDs under one common amendment event. Would it change what an engineer does? Yes: tooling or humans resolving the old IDs d3adbcea / 1c57cb24 must remap them, and anyone auditing the ledger's churn can tie both renames to a single sweep rather than independent edits. Survives.\n\nSTEP 2 — Emit:\n\n```atlas-fact\n{\"claim\": \"Only two entries in this ledger record a former identity — REQ-GROUND-5b (was d3adbcea) and REQ-KNOW-3b (was 1c57cb24) — and both attribute their re-key to the same CO-AMENDED 2026-08-09 (ADR-0014, rule #198) event, while every other entry carries only the plain '[pre-existing at master e4882a3]' tag.\"}\n```"