"STEP 1 — scratch reasoning\n\nCandidates drafted from close reading:\n\n1. **Model config must live outside the repo** — the code does `mkdtempSync(tmpdir())`, but the *reason* (`atlas mine` refusing in-repo configs, ACE guard) exists only in the comment and concerns behavior of another module not shown. Discard: rests on a comment's claim about code not in these bytes.\n2. **Zero metered spend / `echo` stand-in** — the `cmd: 'echo'` literal is in the bytes, but this is essentially restated in the narrative and the test titles; low surprise, largely known from context. Weak candidate.\n3. **CAS shard layout** (`.atlas/cas/<addr[0:2]>/<addr>`, 64 lowercase-hex address) — fully derivable from `join(...)` and the `/^[0-9a-f]{64}$/` literal. Survives, but is fairly mundane plumbing.\n4. **Two distinct `scope` legs on one stored fact** — the expects literally pin `stored.scope === 'atlas:mined'` and `stored.witness.scope === 'src'` with `not.toBe` between them, plus an exact `toEqual` on the whole witness (`{slot:'dependency', target:'util/greet().', scope:'src'}`), and the payoff invokes `verify-fact ... --scope` with the *witness's* value, not the top-level one. Every value is a literal I re-checked in the bytes; nothing leans on external library knowledge; no counts beyond literals present. It goes well beyond the unit's name/signature, and it changes what a consumer does: passing the wrong leg (e.g. `'atlas:mined'`) as the verify scope would be a real bug the test is specifically built to catch.\n\nRefutation pass on #4: contradicted anywhere? No. Resting on unseen code? No — every assertion is in-file. Exact enumeration risk? Only quoted literals, all verified. More than a name/signature restatement? Yes — the name says nothing about dual scopes. **#4 survives.**\n\nSTEP 2 — result\n\n```atlas-fact\n{\"claim\": \"A promoted fact's stored bytes carry two distinct scope legs asserted unequal — top-level scope 'atlas:mined' versus witness.scope 'src' — and the model-free re-proof feeds the witness's 'src' value to verify-fact, not the authz scope.\"}\n```"