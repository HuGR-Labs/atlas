"STEP 1 — Scratch reasoning:\n\nReading the bytes: this is a Vitest suite for the count-slot branch of `admit`. From the filename alone an engineer would guess: unwired → drop, malformed `atLeast` → drop, ungrounded → drop, `proven` → admit sealed. The non-obvious part lives in the second test: with `verifyCount: () => 'abstain'` and the default grounded doors, the assertions read `a.outcome` = `'admitted'`, `a.fact.kind` = `'advisory'`, `seal` undefined, `label` undefined. So abstention produces *admission* as an unsealed advisory, not a drop — that inversion is pinned by the `expect` calls themselves, not only by the header prose (which cites an external contract document I cannot see, so I won't lean on it).\n\nRefutation pass:\n- Derivable from these bytes? Yes — the arrange/act/assert block spells out the abstain→advisory→admitted expectation directly in code.\n- Resting on a comment's claim about elsewhere? No — I'm resting on the assertions (`expect(a.outcome).toBe('admitted')`, `expect(a.fact.kind).toBe('advisory')`, `seal`/`label` undefined).\n- Exact count / exhaustive enumeration? No — purely qualitative.\n- More than a name/signature restatement? Yes — nothing in \"count-leg test\" implies that a non-proven oracle verdict still yields `'admitted'`.\n\nIt survives. (Runner-up — the spy showing `synthesize` is never called even on the unwired-drop path — is also code-visible, but the abstain-inversion is the fact most likely to change downstream assumptions.)\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"An 'abstain' verdict from verifyCount does not drop a count proposal: this suite asserts the seed is still admitted (outcome 'admitted') as an advisory-kind fact carrying neither a seal nor a label, provided the grounding door passes.\"}\n```"