"STEP 1 — Scratch reasoning (not stored):\n\nCandidates drafted and refuted:\n\n- *\"reverifyNegation ignores primaryAnchor\"* — only asserted by a comment about code in another module (`reverify-store.js`); not re-derivable from these bytes. Discard.\n- *\"Before the fix, proven negations were dumped to unverifiable\"* — past-tense history about code not shown (#240 narrative). Discard.\n- *\"Only `seal:'proven'` is in scope; others return undefined\"* — supported by assertions here, but largely restated by the test's own name (\"undefined, never a bucket\"). Weak novelty.\n- *\"Proven + T0 ⇒ TAMPERED, proven + T2 passes\"* — visible in two assertions, but essentially announced by the test titles. Marginal.\n- **Survivor:** the local `scopeHasDocs` helper implements the gate as element-wise segment-prefix comparison (`split('/')` + `every((seg,i) => a[i] === seg)`). Tracing it: for scope `'src/a.ts'`, `s = ['src','a.ts']` and document `'src/a.ts'` yields `true`. So despite being described as checking \"documents under directory scope\", the predicate equally accepts a scope that names an *exact existing file path* (any segment-prefix of an existing document path passes), while still rejecting partial segments (`'sr'` vs `'src/…'`). This is derived purely from the helper's control flow and literals in this file — no reliance on the comment's framing, no counting, no external claims. An engineer assuming directory-only semantics (e.g., writing a negative test with a file-path scope expecting rejection) would behave differently. Survives.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"This unit's scopeHasDocs gate checks scope as a whole-path-segment prefix of each document path, so a scope naming an existing file exactly (e.g. 'src/a.ts') passes the 'directory has documents' check, not just genuine directories.\"}\n```"