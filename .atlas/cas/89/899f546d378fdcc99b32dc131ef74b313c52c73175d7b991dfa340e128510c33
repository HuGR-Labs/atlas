"Looking at the bytes: the three `it` blocks share a single module-scoped `repo`. Test 2 performs `repo.commit(...)` to push HEAD past the watermark; test 3 contains **no** commit call of its own yet asserts `pack?.stale === true` — which can only hold if HEAD is already ahead of the genesis-time `builtAt`. So the MCP test's passing state is manufactured by its predecessor, a coupling confirmed by the control flow (shared variable, absent mutation), not just the inline comment. Other candidates (env-var save/restore mechanics, exact expected line format) are either plainly visible restatements or rest on the header comment's claims about unseen code (`atlas reconcile`, KNOW-8 routing), which don't re-derive here.\n\n```atlas-fact\n{\"claim\": \"The MCP-parity test performs no commit of its own; its expectation of stale:true holds only because the preceding test advanced HEAD via repo.commit on the shared module-scoped fixture, so running or reordering the tests in isolation would break it.\"}\n```"