"STEP 1 — scratch reasoning:\n\n- Candidates leaning on comments about other files (`governed-emit-incumbent.ts`, `wire.ts`, `mine.ts`, ARCH-10's incumbent guard, the mutant test) are discarded — those assert facts about code not shown here.\n- Candidate: \"derivedTier is deliberately absent on CREATE.\" The *rationale* is comment-only, but the mechanism is in code: `...(derivedTier !== undefined ? { derivedTier } : {})` omits the key rather than setting it to `undefined`. Partially survives, but it's thin on its own.\n- Candidate: the load-bearing default pair. Reading the bytes: `DOOR_RATIFY_CTX = { contested: false, lowRisk: true }` is spread first and unconditionally, and neither conditional spread touches those keys, so *every* returned context — including `ratifyCtxFor(undefined)` with no origin — carries exactly these two fixed values; only `derivedTier`/`origin` vary, and their keys vanish when the arguments are `undefined`. Nothing in the signature (`derivedTier, origin?) => RatifyContext`) hints that the output is pre-seeded with these particular verdicts; an engineer assuming a neutral/empty baseline would be wrong. Verified by counting the constant's two literal properties against the spread order — no override possible.\n- This survives: it is checkable solely from the declarations and control flow in this unit, involves no external-module claims, and its exact enumeration (the two fixed keys) was counted in the shown bytes.\n\nSTEP 2 — emit:\n\n```atlas-fact\n{\"claim\": \"Every RatifyContext returned by ratifyCtxFor is unconditionally seeded with the hard-coded pair { contested: false, lowRisk: true } — even when both arguments are omitted — and the derivedTier/origin keys are left out entirely (not set to undefined) whenever their arguments are undefined.\"}\n```"