"STEP 1 — Reasoning (scratch):\n\nReading the bytes: the path and title (`WP-SEC-2.KNOW — owner fence added…`) present this as an executed, green, gate-passing hotfix card — filled `outputs`, `provenance`, `exit_predicate`, a RED→GREEN teeth proof. A competent engineer skimming the name/title would assume this card describes live, current behavior and could be followed or cited as-is.\n\nBut the very first lines of the file — before the title block — declare the opposite: the entire change described herein was REVERTED (banner dated 2026-08-03), the referenced code no longer contains `isOwner`/the `owner` field per the banner's own account, and the file is deliberately kept only as a historical record because its own Measurement section became the evidence for reversing it.\n\nRefutation pass:\n- Is it derivable from THESE bytes? Yes — the reversal banner is verbatim in this unit; no external lookup needed to establish that the card self-declares itself reverted/historical.\n- Resting on a comment's claim about unshown code? The banner mentions PR/issue numbers and other files, but I am not asserting those; the load-bearing fact is the unit's own declared status, readable directly in the shown bytes.\n- Exact counts/enumerations? None used in my claim (no \"16 assertions\", no caller counts).\n- More than a name/signature restatement or plain-summary? Yes — the name and title actively suggest the opposite of what the bytes' opening banner establishes, and acting on the card vs. treating it as inert history changes what an engineer does.\n\nSurvives. STEP 2:\n\n```atlas-fact\n{\"claim\": \"Although this work-package card presents itself as an executed, fully-tested fix with filled outputs, provenance and a green exit predicate, its opening banner declares the entire change REVERTED and the file kept only as a historical record, so its action and acceptance sections must not be treated as current guidance.\"}\n```"