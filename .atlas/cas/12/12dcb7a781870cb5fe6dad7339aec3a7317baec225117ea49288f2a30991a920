"```atlas-fact\n{\"claim\": \"Within this file, deps.readAdvisory is consumed at exactly one executable site — the terminal handler-dispatch line's withNote wrapper — so every command intercepted before that point (including node, which calls the injected handler directly via resolveNode, plus promote, own, relations, negations, transitions, draft, verify-fact, doctor, and the rest) returns without the advisory note ever being appended to its output.\"}\n```"