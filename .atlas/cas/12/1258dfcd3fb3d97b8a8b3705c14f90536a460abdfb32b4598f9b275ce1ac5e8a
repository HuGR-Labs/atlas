"Looking closely at the bytes:\n\n- The suite's title already announces the tri-state (HOLDS/BROKEN/NA), so the mere existence of three verdicts is signature-level knowledge.\n- What is *not* recoverable from any name or type import is **which inputs map to which verdict**: the assertions in SCN-KNOW-16a-1 pin `unrecognized-op|x` → `'NA'` while `exists|mod:missing` and `exists|dep:auth->cache` (against indexes lacking those entities) → `'BROKEN'`.\n- Refutation pass: this rests only on `expect(...)` calls inside this unit — no comment about docs, other files, or task numbers. No exhaustive count is claimed (I'm citing two specific observed mappings, both present verbatim). It goes beyond restating `evaluate`'s signature or the verdict-name enum: it fixes the *semantics* of the NA branch (indeterminate ≠ failed).\n- Would it change what an engineer does? Yes: a typo'd or unsupported operation silently degrades to the indeterminate verdict rather than the failure verdict, so anything gating on BROKEN (e.g., the 16e reconcile feed) will not fire for malformed checks — tooling must validate operation names upstream, and consumers must treat NA distinctly from BROKEN.\n\nSurviving fact identified; emitting it.\n\n```atlas-fact\n{\"claim\": \"An index-query whose operation is unrecognized evaluates to 'NA' rather than 'BROKEN', whereas a well-formed query over an entity absent from the index evaluates to 'BROKEN' — unknown check syntax yields the indeterminate verdict, not the failure verdict.\"}\n```"