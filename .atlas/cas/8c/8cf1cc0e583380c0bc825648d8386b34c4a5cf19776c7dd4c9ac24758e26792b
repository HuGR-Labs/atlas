"Step 1 — scratch reasoning: Candidates considered: (a) the exact BASE_CAP literals — visible data, close to restating constants; (b) budget()'s lexicographic row ordering — visible but low impact; (c) eager snapshot semantics of `records` — plausible but arguably plain from the loop placement; (d) the effective tuning range: `DEFAULT_TUNE_GAIN = 1`, `tunedCap = round(base*(1 + gain*hitRate))`, and structurally `hitCount(k) ≤ served(k)` since both increment in the same per-record loop, so `hitRate ∈ [0,1]`, giving caps in `[round(base), round(2·base)] = [base, 2×base]`. Refutation check for (d): none of this rests on any comment — the constant, the formula, and the coupled increment structure are all in these bytes; nothing about other files is needed; no counting/enumeration risk. Not derivable from the `tunedCap(base, hitRate, gain?)` signature alone, and materially changes how one reasons about post-tuning token budgets (bounded growth, exact floor). Survives.\n\n```atlas-fact\n{\"claim\": \"Because DEFAULT_TUNE_GAIN is 1 and hitRate can never exceed 1 (a kind's hit count is drawn only from its own served records), the tuned cap under defaults is confined to [base, 2×base] — a fully-used kind at most doubles its sweet-spot cap, while a never-served kind stays exactly at base.\"}\n```"