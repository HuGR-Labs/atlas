"Looking closely at the bytes, most scenarios here (partitioning, ranking, K-cap, honest meta) follow predictably from the file's name and purpose. The standout is SCN-RETR-10d: it does something no filename or signature suggests — it opens the filesystem, reads the *implementation's own source text*, and regex-scans the raw characters for forbidden tokens. Refutation passes:\n\n- **Derivable from these bytes?** Yes — the `readFileSync(fileURLToPath(new URL('../src/relate.ts', import.meta.url)), 'utf8')` call, the regex array, and the `not.toMatch` loop are all literally present. The fact is about what *this test does*, not about what relate.ts contains (which I cannot see).\n- **Comment-dependent?** No. The inline comment annotates, but the fact rests on the code itself: the read, the pattern list, the assertion, plus the separate `expect(out instanceof Promise).toBe(false)` synchrony check.\n- **Exact-count risk?** I enumerated the array item by item: fetch, openai, anthropic, claude, `http(s)://`, async, await, Promise, dynamic `import(`, WebSocket — ten patterns, verified twice. Because it scans the raw utf8 string (not an AST), even comments or string literals in relate.ts would trip it.\n- **Restatement of name/signature?** No — nothing about \"wp-2.8-b-retr.relate.test.ts\" implies source-text policing of a sibling file, nor the practical consequence that adding the word \"async\" or a URL anywhere in relate.ts (even in prose) fails the test.\n- **Changes what they do?** Yes: anyone editing or relocating `src/relate.ts` must keep every character of the file free of these substrings and keep `relate()` synchronous, or this test fails regardless of runtime behavior.\n\n```atlas-fact\n{\"claim\": \"The no-LLM scenario enforces itself by raw source text: the test readFileSyncs ../src/relate.ts and asserts the file's characters match none of ten forbidden regexes (fetch, openai, anthropic, claude, http(s)://, async, await, Promise, dynamic import, WebSocket) — so even a comment or string literal in the implementation trips the test — and it separately asserts relate() is synchronous by checking its return value is not a Promise.\"}\n```"