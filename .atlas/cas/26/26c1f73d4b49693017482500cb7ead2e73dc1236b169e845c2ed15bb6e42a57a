"```atlas-fact\n{\"claim\": \"Although RequiredAdvisoryField pins seven required fields, the executable code in this unit never reads 'owner' or 'scope' at all — hasRequiredFields checks only claimText and claimNorm as non-empty strings plus provenance and grounding as non-null, and validateTemplate adds only a byte-cap and slot-membership test, so neither field influences any reject/persist decision made here.\"}\n```"