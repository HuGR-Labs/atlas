"STEP 1 — scratch reasoning:\n\nCandidates drafted from a close read:\n\n- Candidate 1: “The store is durable / git-backed.” Refuted: the only storage in these bytes is a closure-local `new Map<Hash, Uint8Array>()`; nothing here touches disk, git, or any other module for persistence. The git-propagation language lives in comments about systems outside this unit — inadmissible as a code fact, and the in-memory nature is itself the interesting counterpoint.\n- Candidate 2: “`put` hashes the scrubbed copy, not the caller’s original bytes.” Verified against the code: `const admitted = scrub(body); const h = contentHash(admitted); … objects.set(h, Uint8Array.from(admitted));` — both the returned hash and the stored bytes derive solely from `admitted`, never from `body`. Not inferable from the signature `put(body): Hash`, where the conventional assumption is hash-of-input. Phrased mechanically (call order), it does not depend on `scrub`’s unseen internals — only on the fact that the call happens before hashing, which is in these bytes. Survives.\n- Candidate 3: “`fetch` mutates/returns live internal state.” Refuted as a fact: it returns `Uint8Array.from(body)`, a copy — that’s near-standard and arguably derivable from the immutability intent; weak novelty.\n- Candidate 4: exact-count claims (e.g., enumerating exports or branches). Nothing here needs an exhaustive count; avoided per guidance.\n\nSurvivor: Candidate 2. It is checkable purely from this unit’s declarations and control flow, it is invisible in the name/signature, and it changes what a caller may assume (the returned `Hash` addresses redacted content; naive `fetch(put(b)) ≡ b` reasoning about the offered bytes is wrong at the hash level).\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"Inside `put`, the returned hash and the stored bytes are derived entirely from `scrub(body)`'s output rather than from the caller's original argument, so the Hash addresses redacted content, not the body as offered.\"}\n```"