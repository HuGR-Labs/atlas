"STEP 1 — REASONING (scratch):\n\nCandidates considered and refuted/discarded:\n\n- *Central-thesis summaries* (planners persist nothing; GOVERNANCE_SURFACE frozen at five; two transports): stated plainly in Purpose/Boundary — too close to a summary of what the unit plainly announces. Discarded.\n- *READ_SURFACE = {anchors, slots, draft, check, doctor, node}, `diff` excluded*: the exclusion rationale leans on `packages/tools/src/diff.ts`, ARCH-5, and wiring status of files not shown. Discarded as resting on claims about unseen code.\n- *Retraction latch (`retracted-pair` refusal)*: described behavior lives in ADR-0003 §Retraction / runtime folds not in these bytes. Discarded.\n- *`commitStaging`/`loadStaging`/`atlas promote` details*: explicit claims about `cli/src/mine.ts`, `promote.ts`, task numbers — code I cannot see. Discarded.\n- *Exact-count candidates* (15 acceptance checks; six READ_SURFACE members; five governance tools): countable, but the surrounding substance either summarizes the doc or cites elsewhere. Weak action value. Discarded.\n- *Invariant ordering oddity* (AUTHOR-13 placed in Composition before AUTHOR-11 in Confirmation): true in these bytes but trivia; wouldn't change what an engineer does. Discarded.\n\nSurvivor — **AUTHOR-13**: the clause is fully self-contained in this unit's bytes: retiring/superseding a fact MUST be expressed as a **draft variant** carried through `atlas-emit` under its **full** gate set, and **no retire/delete write door MUST exist**. Corroborated within the same bytes by the Boundary bullet fixing WRITE_PATHS at `{atlas-emit, atlas-link}`. Checks:\n\n- Derivable from THESE bytes: yes — verbatim invariant text.\n- Comment-about-elsewhere dependency: no; the normative rule itself is the content here (I drop the \"(cites FR-A4)\" pointer).\n- Exact count/exhaustive enumeration: none asserted.\n- Restatement of name/signature/plain purpose: no — the title \"authoring surface\" suggests producing facts; nothing in the name implies that *removal* of facts is deliberately denied a dedicated door, which is easy to miss among fourteen invariants.\n- Changes what they do: yes — an engineer extending Atlas to correct wrong facts would naturally reach for a `retire`/`delete` command; the spec instead forces a draft variant through the existing governed emit door with all gates, matching the A-D3 pattern of retract-as-mode rather than new tool.\n\nSurvives. Emitting.\n\nSTEP 2 — RESULT:\n\n```atlas-fact\n{\"claim\": \"The surface deliberately provides no retire or delete write door: retiring or superseding an existing fact is specified as a draft variant carrying the superseded authoring state, which must be persisted through atlas-emit under its full set of gates.\"}\n```"