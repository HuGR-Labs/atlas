"STEP 1 — scratch reasoning (not persisted):\n\nReading the bytes closely:\n\n- Candidate 1: “Read-side scoping keys off the derived primary anchor, never the declared scope.” The first test shows a write declared `scope: 'public'` being served by `servedClaimsUnder('src/payments')`, and the stub guarantees the claim came from store readback. But the *mechanism* (“never looks at declared scope”) is asserted mainly by the comment; the bytes only prove the claim is served under the payments-scoped query. Partially interpretive — keep only the observable half.\n- Candidate 2: “`ratifyCtxFor(undefined)` omits the `derivedTier` key entirely rather than setting it undefined.” Directly asserted via `not.toHaveProperty`. Real, but low operational weight relative to others.\n- Candidate 3: segment-wise prefix matching (`src/paymentsX` → `public`). Checkable from the literal expectation, but fairly predictable for a longest-prefix design.\n- Candidate 4: **the opt-in posture of the anchor narrowing.** Test 1 accepts alice's payments-anchored, `public`-declared write and serves it under the `src/payments` query; test 2 refuses the *identical* write with discriminant `unauthorized for anchor` solely because `twoScopePolicy({'src/payments': 'payments'})` was passed; test 4 shows an unbound prefix (`src/shipping`) behaves exactly as before; test 5 asserts `scopeOwnsAnchor(no-map-policy, 'anything', PAY_ANCHOR) === true`. Every clause is carried by assertion literals in this unit, not by comments. Survives refutation: not a count, not name/signature restatement (the filename says \"derivation\", saying nothing about scope-binding posture), and it changes what an operator/implementer does — the guard is inert until an admin declares prefixes.\n\nCandidate 4 survives; folding in the observable half of candidate 1 (accepted write is also served under the payments-scoped read) strengthens it without leaving the bytes.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"The identical write — alice, holding only `public`, emitting an advisory anchored at `src/payments/charge.ts::charge` with declared scope `public` — both emits and is then served by a `src/payments`-scoped projection query while no anchor bindings are configured, yet is refused with `unauthorized for anchor` once an admin binds that prefix to `payments`, so the ARCH-9 anchor narrowing is opt-in per declared prefix rather than a default denial.\"}\n```"