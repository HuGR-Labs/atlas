"STEP 1 — REASON FREELY:\n\nReading the bytes closely. Candidates drafted and tested:\n\n- *\"`replay` defensively copies the arrays (`[...cp.llmOutputs]`), so mutating the view cannot corrupt the checkpoint.\"* — True and code-visible, but marginal actionability; hold as backup.\n- *\"There is no `resume` anywhere on the surface.\"* — Verifiable from the declarations, but it's essentially a restatement of the interface's members plus a comment's insistence; weak.\n- *\"The seat ID is a content hash via the kernel `id` seam.\"* — Refuted: whether `id` is a content hash is a COMMENT'S claim about `@atlas/kernel`, not visible here. Only the call shape is visible.\n- *Extraction-policy candidate:* `redispatch` never uses `record` directly — it routes through the unexported `briefOf`, whose three branches fully determine the hashing input: bare string → the string itself; object possessing a `seatBrief` key → only that field's value; anything else → the entire record. Verified branch by branch against the body — those are all the paths, no fourth case. Consequence derivable from these bytes alone: for records lacking a `seatBrief` key, irrelevant extra fields change the input fed to `id` (and thus plausibly the seat), whereas for records having the key, everything except `seatBrief` is ignored. Nothing here rests on another file, a task number, or past tense. `briefOf` is unexported, so no signature reveals this policy. Survives.\n\nSTEP 2 — EMIT THE RESULT:\n\n```atlas-fact\n{\"claim\": \"The input to the seat-identity computation is decided by an unexported helper, briefOf, whose exhaustive branching means a bare-string record is used as-is, an object with a seatBrief key contributes only that field's value, and any other record contributes its entirety — so extra fields alter the seat input only for records lacking the seatBrief key.\"}\n```"