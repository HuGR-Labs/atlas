"STEP 1 — Reasoning (scratch):\n\nCandidates drafted and tested against the bytes:\n\n1. *Cross-unit isolation via byte-offset checkpoints* — the mechanism is visible (`sidecarByteLength`, `readSidecarSince`, offset slicing), but the motivating rationale lives in comments about `metered-claude.mjs`'s `deriveSiteKey` and multi-package path collisions — code not present here. The mechanism survives, but its framing leans on unseen files.\n2. *Fact lookup by `primaryAnchor` instead of ledger fact id* — the filter `row.primaryAnchor === path` is in-code, but the *why* rests entirely on comments about `coverage.ts` and the write door minting different ids — unverifiable here. Weakened.\n3. *Config must live outside corpus repos* — the writing code is here, but the refusal behavior (`loadModelConfig` exits 2) is asserted about another module. Discarded.\n4. *`runMineOnce` error contract* — verified purely in this unit's control flow: the `catch` around `execFileSync` re-throws **only** when `e.status === null || e.status === undefined`; otherwise the non-zero status is absorbed into `status = e.status` and returned alongside merged output. So callers cannot rely on exceptions to detect a failed mine run — they must inspect `exitCode`. Not inferable from the name/signature, changes what a caller does, and is checkable by reading the catch block itself. Survives.\n5. *`parseArgv` blindly consumes the next token* — true in-code but low impact; superseded by #4.\n\n#4 survives refutation: it rests on this function's own branching, not on any comment's claim about absent code, and involves no counts or enumerations.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"runMineOnce never propagates a non-zero `atlas mine` exit as an exception — it folds the status and merged output into the returned {stdout, exitCode} — and re-throws only when the spawn never ran (exit status null or undefined), i.e. a driver misconfiguration rather than a run outcome.\"}\n```"