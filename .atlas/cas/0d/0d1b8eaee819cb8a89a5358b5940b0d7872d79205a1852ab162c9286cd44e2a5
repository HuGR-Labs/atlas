"STEP 1 — Scratch reasoning (not persisted):\n\nCandidates drafted and refuted:\n\n- *\"Persist only runs when grounded\"* — plainly visible ternary short-circuit; restates the name/docstring intent. Discarded.\n- *\"`id` is truly absent on reject\"* — the reject literal `{ emitted: false }` indeed omits the key, but the co-located docstring announces exactly this; it's effectively part of how the surface presents itself. Weak novelty.\n- *\"Totality is delegated — no try/catch around deps\"* — checkable in the body (bare ternary, no guards), but the docstring explicitly conditions \"no throw\" on \"given a pure/total `deps`\", so it's already disclosed at the signature level. Discarded as near-restatement.\n- **Zero-runtime-dependency candidate:** scanning the declarations, the file contains exactly three import statements — `@atlas/contracts` (Hash), `@atlas/grounding` (GroundApi), `../types.js` (GroundedFact) — and every one carries the `type` keyword. Verified item by item: 3 of 3 are `import type`; there are no value imports, dynamic imports, or side-effect imports; `bindEmit` touches only its parameter. Consequence checkable in these bytes alone: nothing from those modules executes from this file; the compiled unit's only runtime behavior flows through the `deps` argument. This is not derivable from the name `emit.ts` or the `bindEmit(deps): EmitApi` signature, it changes practice (tests must inject fakes — the real gate/hash cannot be reached via this module; the file cannot participate in a runtime import cycle), and it rests on the import declarations themselves, not on any comment's assertion about unseen code. Survives.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"All three imports in this file are type-only (`import type`), so the compiled module has zero runtime dependencies on @atlas/contracts, @atlas/grounding, or ../types.js — the real grounding predicate and hashing never execute from this file, making bindEmit's injected argument their sole runtime entry point.\"}\n```"