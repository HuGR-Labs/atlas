"STEP 1 — scratch reasoning:\n\nReading the bytes closely. Candidates:\n\n1. *\"The fixture is described as git-free but actually initializes a git repo.\"* Refute attempt: is the contradiction fully in-unit? The header prose (\"A temp git-free repo\") is in these bytes, and the code literally calls `execFileSync('git', ['init', '-q'])`, sets user.email/user.name, and runs `git add -A`. Nothing leans on unseen modules — the contradiction is between this unit's own header and this unit's own `beforeAll`. Survives. Consequence an engineer cares about: the test requires a `git` binary at runtime and tracked files; anyone rebuilding the fixture from the header line alone would omit the git steps and break the setup.\n\n2. *\"Header says no `.scip`, but setup writes a real empty-documents index.scip.\"* Also in-unit (header prose vs. `writeFileSync(join(tempRepo, '.atlas', 'index.scip'), serializeSCIP(scip))`). Survives too — but it's the same genre of header-vs-code contradiction as #1, and #1 is the one with an external-environment consequence (binary dependency).\n\n3. *\"Query via `main` currently renders a malformed-args rejection.\"* The justification rests entirely on a comment's claim about `cli.ts` and wired legs not shown. The assertions here only prove status:/next: render and no sentinel. Discard.\n\n4. *\"Path 'src' excludes 'src' itself in returned territories.\"* Encoded in assertions, true, but it's closer to restating what the test plainly asserts; lower impact than #1. Discard as the single pick.\n\n5. *\"mine test chdirs and restores cwd.\"* Visible but mundane, plainly stated by the code. Discard.\n\n#1 survives refutation: checkable by reading the unit itself, not derivable from the file name or any signature, and actionable (git binary required; the \"git-free\" header line is contradicted by the very code below it).\n\nSTEP 2 — emit:\n\n```atlas-fact\n{\"claim\": \"Although the file header describes the fixture as a 'git-free' temp repo, beforeAll actually invokes the git binary — git init, user.name/email config, and git add -A — so the temp directory becomes a real git repository with all files staged before composeRuntime runs.\"}\n```"