"STEP 1 — scratch reasoning (not stored):\n\nReading the control flow closely: `parsePolicy` requires **both** legs to validate — `if (t0Heuristic === undefined || authz === undefined) return defaultPolicy();`. Inside `parseAuthz`, the *optional* `anchors` map is treated strictly: present-but-not-a-record → `undefined`; any single entry with a non-string or empty owner, or an empty/`__proto__` prefix or `__proto__` owner → `undefined` immediately. Each of those propagates up and replaces the **entire** policy — including every valid `scopes` declaration — with the empty-scope deny-all default, since `actorInScope` on `{}` always returns `false`.\n\nRefutation pass:\n- Derivable from these bytes alone? Yes — pure control flow of `parsePolicy`/`parseAuthz`; no external file needed.\n- Resting on a comment about elsewhere (issues #242/#186, other packages)? Stripped those out; the surviving claim is verified line-by-line in this unit.\n- Exact counts/enumerations? Kept qualitative (\"one bad entry\", \"either leg\") — no totals asserted beyond what the branches show.\n- Restatement of name/signature? No — `loadPolicy(repoPath): AtlasPolicy` suggests a typed load; nothing in the signature reveals that a typo in one leg (even the *optional* `anchors` map) silently reverts *unrelated* valid config to deny-all. That changes what an editor of `policy.json` must do (validate the whole file; debug \"why are all writes denied\" from any single malformed key).\n- Contradicted by code? No — the branch structure confirms it directly.\n\nSurvives. Emitting.\n\nSTEP 2:\n\n```atlas-fact\n{\"claim\": \"Policy validation is all-or-nothing: a single malformed entry in the optional `anchors` map (e.g., an empty owner string) — or any invalid value in either top-level leg — makes parsing discard the entire policy, including all otherwise-valid scope declarations, and revert to the deny-all default.\"}\n```"