"STEP 1 — Reasoning (scratch):\n\nReading the bytes: the unit builds a five-record `MemoryStore` fixture and runs two tests: a `deepEqual` round-trip through `exportMemory`/`importMemory`, and a \"no lock-in\" test asserting the exported dump string avoids host references via three negative regex assertions: `/file:\\/\\//`, `/\\/Users\\//`, and `/[a-z]+:\\/\\//`.\n\nCandidate facts considered:\n\n1. *\"Archived-ness is encoded as `stoppedAt: 'archived'`\"* — plainly visible in the literal; essentially a summary of what the code shows. Discarded as too close to restatement.\n2. *\"The fixture has exactly 5 entries across 4 kinds/3 owners\"* — I counted them (alice/project, bob/project, orch/logbook, alice/pr, bob/task), and it's verifiable, but it's just re-describing the visible fixture declaration. Weak.\n3. *\"In the no-lock-in test, the `/file:\\/\\//` assertion is logically subsumed by `/[a-z]+:\\/\\//`\"* — checking this against the bytes: `\"file://\"` consists of lowercase letters (`file`) immediately followed by `://`, so any string matching `/file:\\/\\//` necessarily matches `/[a-z]+:\\/\\//`. Therefore if the third assertion passes, the first must also pass — the file-specific check can never independently fail; only the `/\\/Users\\//` assertion contributes independent coverage beyond the scheme ban. This rests solely on comparing three regex literals in this unit — no external files, no comment claims, no counts to miscount. It survives refutation: not derivable from the name/signature (\"held-out gate… round-trips\"), not a summary of intent, and it changes what an engineer does (they'd know one assertion is dead weight when reading coverage failures or refactoring the test).\n\nSurvivor: fact 3.\n\nSTEP 2 — Emit result:\n\n```atlas-fact\n{\"claim\": \"In the no-lock-in test, the assertion expect(dump).not.toMatch(/file:\\\\/\\\\//) is logically subsumed by the broader expect(dump).not.toMatch(/[a-z]+:\\\\/\\\\//) assertion — any 'file://' occurrence necessarily matches the generic scheme pattern — so only the /\\\\/Users\\\\// check adds coverage beyond banning all schemes.\"}\n```"