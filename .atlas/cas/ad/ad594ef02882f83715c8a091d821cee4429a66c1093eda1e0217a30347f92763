"STEP 1 — Scratch reasoning (not stored):\n\nCandidates drafted and refuted:\n\n1. *\"Fixture packages contain three source files\"* — the number 3 appears only as an assertion about runtime output of unseen driver/fixture code; the fixture contents themselves aren't in these bytes. Discarded.\n2. *\"The pre-fix code joined cost against the whole accumulated sidecar\"* — pure comment testimony about past/unseen code in `bench-driver.mjs`. Doesn't re-derive here. Discarded.\n3. *\"runCorpus runs units sequentially\"* — comment's claim about another file's behavior. Discarded.\n4. *\"nodeKey is content-addressed on (anchor path, claim)\"* — comment explaining unseen store logic. Discarded.\n5. *\"parseFullMineReport refuses a 0-seeded report without a 'mine:' line\"* — describes behavior living entirely in `mine-report.mjs`; the test only gestures at it. Discarded.\n6. **Surviving candidate:** the stateful fake `claude` is generated inline in this file as a string literal, and its pricing logic is readable directly in those embedded bytes: `total_cost_usd: (unitBase + n) * 0.01` with `unitBase = unit === 'pkg-b' ? 1000 : 0`, where `unit` is the last segment of `process.cwd()`, and `n` comes from a counter file updated via plain read→increment→write with no lock. This is checkable purely by reading the template literal in this unit, is invisible from the file's name/test signatures, and materially changes how one reasons about (or modifies) the isolation guarantees — e.g., renaming the fixture label silently breaks the pricing separation, and no ordering assumption is needed.\n\nVerified the literals (1000, 0.01, `'pkg-b'`) item-by-item against the embedded script text; kept the claim qualitative about the mechanism rather than asserting exact call totals, which cannot be counted from these bytes.\n\n```atlas-fact\n{\"claim\": \"The cross-unit isolation test generates an inline fake `claude` whose per-call price is `(unitBase + n) * 0.01`, where `unitBase` is 1000 only when the process cwd's final path segment is exactly `pkg-b` (otherwise 0) and `n` comes from a shared counter file updated via a non-atomic read-increment-write — so pkg-a/pkg-b cost separation holds by construction rather than by counter ordering.\"}\n```"