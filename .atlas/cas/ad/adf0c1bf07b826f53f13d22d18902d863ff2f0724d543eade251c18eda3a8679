"STEP 1 — Reasoning (scratch):\n\nReading the bytes: scripts are `{\"build\": \"tsc -b\", \"typecheck\": \"tsc -b\"}`. Candidate fact: the typecheck script is the identical command as build. Refutation pass — both string literals are present in this unit, so the equality is checkable here without trusting anything outside these bytes (no comments exist to lean on, and no tsconfig semantics need be invoked). It's a cross-field relation, not a restatement of the package name or path. It avoids numeric/exhaustive claims. An engineer seeing only the filename couldn't know that \"typecheck\" here aliases \"build,\" and it changes what they do (editing one must edit both; there is no distinct check-only step defined). Survives. Alternative candidates (\"private:true means unpublished,\" \"exports only maps '.'\") are direct field readings — restatements of plainly visible content; discarded.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"The typecheck script runs the exact same `tsc -b` command as the build script, so building and type-checking this package are not separate operations.\"}\n```"