"STEP 1 — REASONING (scratch)\n\nCandidate A: \"The positive-control test requires the forged attacker claim to be SERVED when `.atlas` is not force-added.\"\n- Check against bytes: `poisonedRepo({ commitAtlas: false })` skips only `git add -f .atlas`; the test then asserts `expect(verdict.ok).toBe(true)` AND `expect(invariantsOf(verdict).map((i) => i.claim)).toContain(FORGED_CLAIM)` where `FORGED_CLAIM` is the literal attacker string. Both assertions are directly in the code, no comment needed. ✔\n- Refute via comments-about-elsewhere? The mechanism (re-proof filter, travel-by-reproof) lives in comments referencing unseen modules, but the fact itself rests only on the paired assertions in this file. I can phrase it without importing any comment's claims. ✔\n- Exact count/enumeration? None claimed. ✔\n- Beyond name/signature? The unit name \"store-provenance.test.ts\" suggests it proves forged/committed projections are never served; nothing in the name reveals that one test mandates the opposite for the uncommitted population. Non-obvious and actionable: tightening the read path to drop unwitnessed rows unconditionally would break this control by construction. ✔ — SURVIVES.\n\nCandidate B: \"Without the trust seam, exactly 2 nodes load.\" Count verified (two `put` calls, two rows). True, but it mostly restates the fixture's construction and is less decision-changing than A. Discard as weaker.\n\nCandidate C: \"`persistProjection` throws /TRACKED BY GIT/ while `commitProjection` returns `{settled:false, refusal:'untrusted'}`.\" True and in bytes, but largely visible at a glance from the assertions themselves and closer to a plain summary of those two tests. Discard as weaker than A.\n\nSTEP 2 — RESULT\n\n```atlas-fact\n{\"claim\": \"A deliberate positive control asserts that the byte-identical hand-forged .atlas store, when NOT force-added to git, is still fully served — verdict ok:true with the attacker's FORGED_CLAIM among the returned invariants — so any change that filters unwitnessed rows regardless of commit status would break this test rather than strengthen the guarantee.\"}\n```"