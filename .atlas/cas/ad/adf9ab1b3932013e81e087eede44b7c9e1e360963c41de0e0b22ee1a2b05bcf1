"STEP 1 — scratch reasoning (not persisted):\n\nCandidate drafts and refutation passes:\n\n1. *\"Advisories update in place via claim set-union; predicates supersede\"* — supported by code literals (`Set` comparison containing both claim norms, `supersededBy` assertions), but this is loudly telegraphed throughout the header prose; low information gain beyond name/comments.\n\n2. *\"routeWrite never returns REJECT; range is exactly four decisions\"* — the set literal `['DEDUP','CREATE','UPDATE','SUPERSEDE']` is code, and 'REJECT' appears nowhere as an emitted value. Survives, but the header's \"TOTAL · DETERMINISTIC · MUTUALLY-EXCLUSIVE\" framing makes it semi-derivable; also the \"why\" rests on a comment citing another work package, which I'd have to strip.\n\n3. *\"After SUPERSEDE, the current node keeps the nodeKey, carries the new hash, and points back to the prior content hash, with prior bytes retained in CAS\"* — every clause verified against literals (`contentHash` → `'ch-d33'`, `supersededBy` → `'ch-p00'`, `cas.has` checks, one-node-per-key filter). Survives, but largely narrated by the header.\n\n4. *\"Only some of the enumerated route cells have pinned expected outputs\"* — checking closely: the exhaustive loop runs 2×2×2×2 = 16 combinations (verified: `[false,true]` × `['advisory','predicate']` × `[false,true]` × `[false,true]`, no skips), each asserted only for set-membership and self-determinism. Direct `routeWrite(...).toBe(<literal>)` pins appear in exactly one `it` block: I count the expects line by line — DEDUP, CREATE, UPDATE, SUPERSEDE, CREATE = **5**, and all five inputs are pairwise distinct (they differ in family or flags). So 5 of 16 cells carry pinned goldens; 11 do not. Recounted twice. Crucially, the header advertises \"exhaustive-enumeration teeth\" without revealing that most cells lack expected values — a competent engineer would plausibly assume full golden coverage. This is checkable purely from this unit's control flow and literals, is an exact count I actually verified, and would change what a maintainer does (where to add regression goldens). Survives.\n\nEmitting the survivor.\n\n```atlas-fact\n{\"claim\": \"Although routeWrite is exhaustively exercised over all sixteen combinations of its four routing inputs, only five of those cells carry pinned expected decisions; the remaining eleven are asserted only to land in the four-value {DEDUP, CREATE, UPDATE, SUPERSEDE} set and to be deterministic.\"}\n```"