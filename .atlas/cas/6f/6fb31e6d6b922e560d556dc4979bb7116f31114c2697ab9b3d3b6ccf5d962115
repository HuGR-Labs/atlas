"Reading the bytes: the executable content here is small — `BILLY = 'billy'`, `stage` returning `{ node: candidate }`, and `ratify`'s three-line body: reject empty `by`, reject `tier === 'T0'` when `by !== BILLY`, otherwise commit. The long header comments mostly assert things about *other* files (`adapter-io/compose.ts`, `cli/src/mine.ts`, `DiskStore.commitStaging`, ADRs, task #83) or about what \"used to\" be true — per the rules those are discarded, since they don't re-derive at these bytes.\n\nDrafting candidates and refuting:\n\n1. *\"The explorer never calls `stage`\"* — refuted: comment-only claim about other modules.\n2. *\"The token comes from an env var\"* — refuted: comment's claim about `adapter-io/compose.ts`, not visible here.\n3. *\"A-D4/task #83 measured X\"* — refuted: past-tense comment about elsewhere.\n4. *\"`stage` holds the candidate by reference, so later mutation of the candidate shows through `Staged.node`\"* — survivable from `return { node: candidate }`, but low impact and close to what the one-line body plainly shows.\n5. *\"Despite the gate/token vocabulary, `ratify`'s whole decision is two string comparisons on values the caller supplies: any non-empty `by` commits, and only a node whose own `tier` field is exactly `'T0'` additionally requires `by` to strictly equal `'billy'`\"* — checking against the code: line 1 rejects only `by.length === 0`; line 2's BILLY check fires only under strict `staged.node.tier === 'T0'`; line 3 commits everything else. `BILLY` is the lowercase literal `'billy'`, compared with `!==`, so `'Billy'`/`' billy'` fail only the T0 branch while committing any other tier. No hashing, lookup, async step, or other check exists in the function — verified by reading the full body, not a comment. Not a restatement of the signature (whose \"token/gate\" naming suggests verification that is absent); actionable because it tells an engineer `committed: true` must never be treated as authorization evidence and that real verification has to live upstream. Survives.\n\nEmitting the survivor:\n\n```atlas-fact\n{\"claim\": \"Despite its gate/token vocabulary, ratify's entire decision is two string comparisons on caller-supplied values: any non-empty token.by commits, with the sole extra requirement — applied only when the staged node's own tier property is exactly the string 'T0' — that token.by strictly equal the literal 'billy', so 'Billy', ' billy', or any other non-empty spelling commits nothing at T0 yet commits every other tier freely.\"}\n```"