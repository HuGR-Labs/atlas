"Looking at these bytes: this is a project-referencing TypeScript config. Candidates drafted: (1) it extends the repo-root base config — visible, but largely guessable convention; (2) `include` covers both `src` and `test`, so tests are compiled in this unit — visible and mildly actionable, but fairly plain from the literal; (3) the `references` array names two specific siblings, `adapter-io` and `tools`, making this package's build dependent on those projects' order — this is not inferable from the file's name or location, it is read directly from the literals here, involves no comment claims or counts beyond what I verified (two entries, both named), and changes what an engineer does (rebuild/reference those packages before this one). Candidate 3 survives refutation best.\n\n```atlas-fact\n{\"claim\": \"This package is tied to its sibling packages via TypeScript project references pointing at ../adapter-io and ../tools, so those two projects must be built before (or alongside) this one rather than being independent.\"}\n```"