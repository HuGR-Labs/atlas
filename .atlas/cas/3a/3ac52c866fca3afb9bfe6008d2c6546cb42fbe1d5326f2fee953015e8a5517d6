"STEP 1 — Reasoning (scratch):\n\nCandidate drafts and refutation passes:\n\n1. *\"Flags are silently ignored, folded into the argument bag\"* — the doc cites `packages/cli/src/parse.ts` for this. Rests on an unseen file. **Discard.**\n2. *\"init alone is exempt from the committed-store refusal every other command enforces\"* — asserted via `packages/cli/src/cli.ts` and contrasted with `query.md`, neither of which is in these bytes. Even if true, it's a fact about other modules, not re-derivable here. **Discard.**\n3. *\"Blast radius is computed but unrendered by the CLI, readable over MCP\"* — depends on naming the external type `InitOut` and on MCP-side behavior not shown. **Discard.**\n4. *\"It used to exit 0 and print the full territory block for any path\"* — past-tense historical claim; explicitly flagged as the kind of thing current code/docs may not support. **Discard.**\n5. *\"Only two refusal causes exist\"* (missing path, no-such-path) — enumerable from the bullets, but the \"nothing else\" clause leans on the exemption claim that itself points outside the unit. Fragile framing. Set aside.\n6. *Rerun behavior* — directly verifiable by comparing the two transcripts shown in the unit: second run exits 0, swaps the `created … denying .atlas/*` line for `.atlas/* already denied … — nothing to do`, and the `data:` block gains `territory: .atlas` and `territory: .gitignore` (files that did not exist during the first walk). Survives all checks: contained in these bytes, no external dependency, qualitative, not a restatement of name/signature, and actionable (reruns are safe and non-destructive; the walk precedes the ignore write).\n\nCandidate 6 survives. Emitting it.\n\n```atlas-fact\n{\"claim\": \"Re-running `atlas init .` on the same repository exits 0 again and reports the ignore rule as 'already denied' rather than rewriting it, with the newly created `.gitignore` (and `.atlas`) appearing in the territory list only on that second run.\"}\n```"