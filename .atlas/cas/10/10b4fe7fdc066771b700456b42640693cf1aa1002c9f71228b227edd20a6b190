"STEP 1 — scratch reasoning (drafted candidates and tried to refute):\n\n- *Candidate: \"A mutant once made 221 files / 1614 tests stay green while `atlas link` crashed.\"* — Refuted: pure comment narrative about a past measurement and code not shown. Discarded.\n- *Candidate: \"task #144 folded the unknown-node reason into unauthorized.\"* — Rests on a comment plus an issue number about another module's history; only the *assertions* here are code. Discarded as stated.\n- *Candidate: \"authz identity flows via ATLAS_ACTOR/ATLAS_RATIFY_TOKEN env vars.\"* — Survives refutation (fully visible in control flow), but it is plainly visible in `beforeAll`/`finally` blocks the moment anyone opens the file; lower marginal value.\n- *Candidate: \"`void joined;` is dead code.\"* — True from the bytes but changes nothing an engineer does.\n- *Survivor:* The T3/T3b assertions themselves — exit 2, `status: rejected`, lowercase contains `unauthorized`, and **negative** assertions that stdout contains neither `unknown node` nor the literal probed key `not-a-real-nodekey` — pin that a missing endpoint must get the *same* refusal class as an out-of-scope actor, with no discriminant and no echo. Every token in that claim is a literal in this unit's code, no count is asserted beyond what I verified item-by-item in the two tests, and it is genuinely counterintuitive: an engineer improving CLI error messages to be \"helpful\" (naming the missing key) would break this suite without knowing why from the file name alone.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"In T3 and T3b, linking a nonexistent nodeKey must fail with the same generic 'unauthorized' exit-2 refusal as an out-of-scope actor, and the assertions explicitly forbid stdout from containing 'unknown node' or echoing the probed key 'not-a-real-nodekey' — so any future change that makes the CLI's refusal more helpful by naming or distinguishing absent nodes will break this suite.\"}\n```"