"STEP 1 — scratch reasoning:\n\nReading the bytes: the interesting surface is what the `expect(...)` calls pin about the harness, since `admit`/`runSite` themselves live elsewhere. Most scenario bodies restate their own `it()` titles (12c, 12f, 12g, 12h, 12i add little beyond their names).\n\nCandidate A: \"12b and 12j assert the identical HOLDS/flips admitted-dropped pair\" — true (compared the four expects line by line), but it's test-suite redundancy; wouldn't materially change what an engineer does. Weak.\n\nCandidate B: \"on the BROKEN path `calls.teeth` stays 0\" — verifiable (12d), and 12b/12j show flips gates the HOLDS path. Real ordering info, but largely an elaboration of \"refined ≤K then dropped\".\n\nCandidate C (SCN-GEN-12k-2): the expects pin that when `expressible('contract')` is true, `calls.synthesize` is 0, `diagnosed` is 1, and the admitted result's `fact.kind` is `'advisory'` with `'check' in a.fact === false` and `'status' in a.fact === false`, while `predicateSlot === 'contract'` survives. The title only says the slot \"prefers sound type-checker/LSP\" — nothing in the name or signature prepares a reader for the output being retyped to an advisory with no check/status payload on this path, for a *predicate* input. Refutation attempts: (a) is it comment-derived? The nearby comment motivates it via \"#200\"/\"visible twin\", but every clause I'd claim maps directly to an executable `expect` in these bytes, so I strip the rationale and keep only what the asserts state. (b) exact enumeration risk? I only claim zero synthesize calls (asserted `toBe(0)`) and absence of two named keys (asserted with `in` negations) — no exhaustive field list claimed. (c) restatement of title/types? The advisory-retyping and missing check/status are not in the title or any visible signature. Survives.\n\nSTEP 2 — emit:\n\n```atlas-fact\n{\"claim\": \"In SCN-GEN-12k-2, when the TypeOracle deems the slot 'contract' expressible, admit makes zero synthesize calls and the admitted fact comes back with kind 'advisory' carrying predicateSlot 'contract' but neither a check nor a status field, even though the input was a predicate proposal.\"}\n```"