"```atlas-fact\n{\"claim\": \"In the Atlas grounding model, staleness keys on a BLAKE3 hash over the cited unit's own raw subtree bytes, so an added import, a blank-line-separated file/license header above the unit, or an unrelated rename elsewhere all leave the fact FRESH — yet a pure reformat inside the unit flips it stale (an accepted false alarm), and a comment contiguous with the cited declaration (no blank line) counts as its bound doc-comment whose edit flips the unit too.\"}\n```"