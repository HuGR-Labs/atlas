"STEP 1 — scratch reasoning (not persisted):\n\nCandidates drafted from the bytes:\n\n1. *\"Threshold = hits × 10\"* — true of the `calibrate` literal, but it's an arbitrary fixture constant; low actionability.\n2. *\"No self-score input exists in `HitsDeps`\"* — checkable (`{ servedSet, archive, calibrate }`), but it largely restates scenario 17b's own describe title (\"never the proposer self-score\").\n3. *\"Post-decay `logHit` reports hits === 1\"* — the assertion pins this, but since A had 0 hits before decay, this doesn't distinguish \"reset\" from \"continuation\"; the strong \"history discarded\" reading overreaches what the asserts show.\n4. **Survivor**: In 17d-1, `bindHits` is constructed as `deps(p.snapshot, sink.archive)` → `{ servedSet, archive, calibrate }`. The fixture's `set` mutator is *never* passed in, and the fixture closure only exposes it on the returned object. So the only way A is inside the served set for the final `decay` is the explicit `p.set([A])` call immediately preceding it — `logHit`'s re-admission cannot itself write back into the pack. Derived entirely from declarations and call arguments in these bytes (no reliance on header/comment claims about produce/router or hits.ts internals), not stated by the describe titles or signatures, and materially changes how an integrator wires serving vs. ledger re-entry.\n\nRefutation pass on #4: exact-value risk none (no counted enumeration beyond the single `p.set([A])` occurrence, verified present); not comment-dependent; not a name/signature restatement. Survives.\n\nSTEP 2 — emit:\n\n```atlas-fact\n{\"claim\": \"The bound ledger is constructed with only the pack's read accessor (snapshot) and the archive sink—the fixture's set mutator is never injected—so the re-entered fact is present for the final decay solely because the test explicitly calls p.set([A]); logHit's re-admission cannot itself alter the served snapshot.\"}\n```"