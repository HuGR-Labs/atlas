"Looking at this unit: it's a ratified ADR, so its payload is the set of decision rules it states — the checkable surface is the policy text itself. Filtering candidates:\n\n- **\"5126 edges / 677 docs / ~2.9s\"** — exact counts and timings asserted about runs of binaries not present in these bytes; cannot recount or re-measure here. Discard.\n- **\"`calls` unprovable because `ScipSymbolRole` is `'definition' | 'reference'`\"** — the *reason* leans on a type definition in another file (`packages/knowledge/src/types.ts`) not shown; also partly inferable from the filename's scoping to `depends-on`. Discard.\n- **\"Write door has no oracle; false-but-shape-valid witnesses caught at read-side reverify\"** — hinges on internals of the door and `reverify-store.ts`, neither visible here. Discard.\n- **D-a supersede semantics, F2 fail-loud budget** — real policy, but largely conventional/inferable from \"proven strictly stronger\" and \"exhaustive\" framing. Weaker.\n- **The \"No refute\" boundary** — stated outright in the unit's own honest-boundary section, consistent with everything else in the doc (dynamic edges abstain, negation is a separate future family): absence is deliberate abstention, never a negative fact. Nothing in the bytes contradicts it; no counts; no reliance on unseen code; and it is invisible from the filename while materially changing how one interprets relation-store output (missing edge ≠ independence). Survives.\n\n```atlas-fact\n{\"claim\": \"The sound depends-on projection abstains rather than refutes: an absent A→B edge is never emitted as a 'these do not depend' fact (negation is deferred to a separate future family), so a missing edge in the relation store must not be read as evidence of independence.\"}\n```"