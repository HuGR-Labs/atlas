"STEP 1 — Scratch reasoning (not persisted):\n\nCandidate drafts considered:\n\n- *Closed slot vocabulary has exactly 12 entries.* Verified by counting the table (invariant…definition = 12), but the actionable core (\"adding a slot bumps cv\") is adjacent-rule text; the count itself is enumerable but bulky to assert. Weaker than alternatives.\n- *Advisory edits in place, predicate supersedes.* Present in KNOW-12, but fairly guessable once you know the two families exist; lower surprise.\n- *Write-time dedup is only contentHash/nodeKey; near-duplicates coexist with a read-only subsumes relation.* Strong, but partially leans on `docs/design/dedup-identity.md` for the model's framing.\n- **Rename paradox (survivor):** Two claims sit in direct tension inside these bytes, and BOTH are stated here: KNOW-15's write-decision bullets say the anchor id is name-stripped/move-aware, so \"a rename is a MOVE, not delete+create\" — identity survives; while KNOW-3 (amended, ratified) plus Acceptance #1 say a rename OF the cited symbol ⇒ DRIFTED, because the drift anchor key format `<parent>::<kind>:<ordinal>[:<name>]` embeds the name and resolution fails closed, with no rename-tracking. Refutation attempts: (a) Is either leg resting on another file? No — the anchor-key format and both behaviors are spelled out inline in this unit. (b) Do the bytes contradict each other? No — they govern orthogonal axes: identity (create/update) vs freshness (drift status); the doc even flags the name-insensitive form as applying to `primaryAnchorId` specifically. (c) Restatement of name/signature? No — the filename suggests nothing about rename semantics, and the natural assumption (\"hash anchor ⇒ rename-safe\" or \"rename ⇒ orphaned node\") is wrong in opposite directions on the two axes. (d) Exact counts/exhaustive lists? None asserted. Survives.\n\nSTEP 2 — Emit result:\n\n```atlas-fact\n{\"claim\": \"Renaming or moving the symbol a fact is anchored to preserves the fact's node identity (the anchor id is name-stripped, so it re-anchors instead of spawning a new node), yet the fact still flips to DRIFTED, because the drift anchor key embeds the symbol's name and unresolvable anchors fail closed.\"}\n```"