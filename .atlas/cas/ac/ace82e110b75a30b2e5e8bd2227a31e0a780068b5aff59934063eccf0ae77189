"```atlas-fact\n{\"claim\": \"Although its title asserts that no off-seam digest call site exists anywhere in this WP's source, the SCN-GROUND-10b-1 gate actually greps only the two explicitly listed files ../src/subtree.ts and ../src/drift.ts, so any raw-hashing import or call site in any other package file would pass this check unnoticed unless that file is added to the loop's array.\"}\n```"