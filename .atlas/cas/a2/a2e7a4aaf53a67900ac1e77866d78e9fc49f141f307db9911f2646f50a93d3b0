"STEP 1 — REASONING (scratch)\n\nReading the bytes closely:\n\n- The signature `isUntrustedStore(trusted: SidecarTrust | undefined): boolean` tells me nothing about what `SidecarTrust` *is* — it's an imported type from `./store-provenance.js`, not defined here.\n- Candidate 1: \"`isUntrustedStore` returns false when the seam is absent.\" Check the body: `return trusted !== undefined && !trusted();` — yes, `undefined ⇒ false`. But this is fairly guessable from the guard idiom; a competent engineer scanning the body would likely predict it.\n- Candidate 2: \"`SidecarTrust` is a callable predicate, not a flag/value.\" Check the body: `!trusted()` — it is invoked as a zero-argument function at predicate-evaluation time. This is NOT recoverable from the name or signature alone (the type is opaque here); an engineer could reasonably assume an enum, boolean field, or trust object. It's derived purely from these bytes' control flow, not from any comment's claim about other files. It would change what someone does: anything implementing/mocking `SidecarTrust` from this side must be a function, and each call to `isUntrustedStore` re-executes the underlying check.\n- Candidate 3: the message literal's pre-colon text equals the class's `reason` discriminant. True in the bytes, but its *actionability* rests entirely on the comment's claim about `reasonOf` elsewhere — the in-byte part alone is a structural coincidence whose significance I cannot ground here. Discard per the rule about comment-carried significance.\n- Candidate 4: `refuseUntrustedRead` throws vs `readProvenanceRefusal` returns. Both behaviors are essentially announced by their names/return types and doc comments; weak.\n- Combining candidate 2 with the absent-seam default gives the strongest surviving, code-checkable claim: the parameter is a thunk invoked per call, and no seam ⇒ treated as trusted.\n\nRefutation pass on the survivor: derivable from these bytes? Yes (`trusted !== undefined && !trusted()`). Comment-dependent? No. Exhaustive count? No. Restatement of name/signature? No — callability of an imported type is invisible until the body. Survives.\n\nSTEP 2 — RESULT\n\n```atlas-fact\n{\"claim\": \"The `trusted` parameter is a zero-argument predicate function that isUntrustedStore invokes at call time (not a stored flag), and when the seam is undefined the predicate returns false, so a build without the provenance seam treats every store as trusted.\"}\n```"