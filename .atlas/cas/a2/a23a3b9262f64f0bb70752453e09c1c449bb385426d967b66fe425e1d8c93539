"STEP 1 — Reasoning (scratch):\n\nReading the bytes closely, several candidates surface:\n\n1. **\"The renderer must never mention `atlas node`\"** — supported by `not.toContain('atlas node <addr>')`, but the *why* lives in a comment about another module's address scheme. The assertion itself is in-byte, but the interesting half is commentary-driven.\n\n2. **\"Predicate gotchas render an empty claim\"** — pinned by `toContain('  gotcha T1 p1: \\n')` despite the fixture carrying check text `'x'`. Genuine quirk, but narrow.\n\n3. **Fail-closed on `{}`** — `main(['own','src'], {})` → exit 1 + \"atlas runtime is not composed yet\". In-byte, but partially motivated by a comment's hypothetical scenario.\n\n4. **Verbatim scope forwarding** — the strongest survivor. The bytes show `main(['own', 'packages/cli/src'], { own })` where `own` is a locally-defined recording stub, and the assertion `expect(seen).toEqual(['packages/cli/src'])`. This establishes two things not visible in any import signature: (a) `main` accepts an injectable legs object as a second parameter (also evidenced by `{}` producing the uncomposed path), and (b) the dispatcher forwards the scope string **unmodified** — a relative path arrives at the leg still relative, with no cwd resolution or normalization. The exact-count element (\"once, single-element array\") is directly verified by the `toEqual` literal.\n\nRefutation checks: candidate 4 rests only on declarations, control flow, and literals in this unit — the recording stub, the invocation, the equality assertion. No reliance on comments about other files (the `@atlas/retrieval` history remark is discarded). Not a restatement of the file name or imports; `main`'s DI seam and no-normalization contract cannot be inferred from the import line. Survives.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"The CLI entrypoint `main` takes an injectable legs object as a second argument, and is pinned by test to hand the user-typed scope to the injected `own` leg completely unmodified — the recorded call equals the literal relative path 'packages/cli/src', with no cwd-based rewriting or normalization applied by the dispatcher.\"}\n```"