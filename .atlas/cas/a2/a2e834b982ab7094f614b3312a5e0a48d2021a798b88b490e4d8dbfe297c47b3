"Looking at these bytes: this is a design document whose load-bearing, non-obvious content must come from its own normative statements, not from its many citations to unseen files (`author.ts:24-31`, `fs.ts:38`, task #83, the dogfood repo, `wp-campaign-10.md`'s 16-WP count, etc.) — all of those fail the \"checkable at these bytes\" test.\n\nDraft candidate: the resolution of coupling C1. Refutation pass: the design matrix shows DP-5 marked **X** in exactly the FR-A1, FR-A2, and FR-A6 rows (verified cell by cell), and §3.3 states verbatim that this coupling is *accepted and ordered* — resolved by sequencing (build DP-5 first, freeze as WP-AUTH-0, nothing else may start before it), normative as INV-AUTH-1, with the rejected alternatives (two computers, a cached hash table) listed in the AUTH-1 ratification row. All of that is contained in this unit itself; no reliance on unseen code or comment claims. It is not derivable from the path name \"authoring.md,\" it carries no unverified counts, and it directly changes what an implementer does (do not start planners first; do not introduce a second grounding computation or cache). It survives.\n\n```atlas-fact\n{\"claim\": \"The design's central coupling — the single grounding computer DP-5 serving FR-A1, FR-A2, and FR-A6 simultaneously — is deliberately accepted and resolved by ordering rather than decoupling: WP-AUTH-0 must build and freeze that one grounding seam first, with nothing else in the campaign allowed to start before it, and this sequencing is made normative as INV-AUTH-1 (explicitly rejecting a second computer or a cached hash table as drift-producing alternatives).\"}\n```"