"STEP 1 — SCRATCH\n\nReading the bytes closely, the striking structural detail is in the fixture wiring, not the assertions:\n\n```ts\nlet ws: Workspace | undefined;\nafterEach(() => { ws?.dispose(); ws = undefined; });\n\nfunction negDoor(callers, holes, opts = {}) {\n  ws ??= freshWorkspace();\n  return createGovernedEmit({ store: ws.store, ... });\n}\n```\n\nCandidate fact: `negDoor()` does **not** build an isolated environment per call — it lazily memoizes one `Workspace` (`ws ??= freshWorkspace()`) and hands every door in the same test the *same* on-disk store, so admitted rows and abstentions persist across successive door instances within a test, and are wiped only by the `afterEach` hook between tests.\n\nRefutation pass:\n- Derivable from these bytes alone? Yes — the `??=` memoization and `store: ws.store` are plain code; no library knowledge needed.\n- Resting on a comment's claim about unseen code? No. (A comment in the supersede test even notes the reuse, but the mechanism is fully visible in the declarations and control flow itself.)\n- Exact count / exhaustive enumeration? No numbers claimed.\n- More than a restatement of name/signature? Yes — nothing about `negDoor(callers, holes, opts)`'s signature hints that consecutive doors share durable state. And it demonstrably matters: the SUPERSEDES test's second admit (`closedDoor`) only proves deletion-of-abstention because it hits the *same* store the first door wrote to; with per-call isolation that assertion would be vacuous.\n- Actionable? Yes — anyone extending this suite must know door instances are not isolated within a test: projection reads like `currentOf(ws!)` / `abstainedOf(ws!)` see cumulative state across doors, and test authors cannot rely on a fresh store per `negDoor()` call.\n\nSurvives. Emitting.\n\nSTEP 2 — RESULT\n\n```atlas-fact\n{\"claim\": \"Within a single test, every door built by negDoor() shares one lazily-memoized Workspace (via `ws ??= freshWorkspace()`), so admitted facts and abstentions persist across successive door instances in that test and are cleared only by the afterEach dispose.\"}\n```"