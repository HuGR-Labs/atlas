"STEP 1 — Reasoning (scratch):\n\nReading the bytes: this is a small tsconfig extending a base config two levels up, overriding only `outDir` to `dist`, listing `include` as `[\"src\", \"test\"]`, and holding a single project reference to `../contracts`.\n\nCandidates drafted and refuted:\n\n1. *\"The only compilerOptions override is `outDir`\"* — verifiable (one key), but essentially a restatement of the file's literal content with little action-changing value. Discarded as marginal.\n2. *\"Kernel depends on the contracts package / must build after it\"* — the `references` entry is in the bytes, but interpreting it as a build-ordering dependency leans on TypeScript project-reference conventions plus unseen properties of the referenced project and the base config (e.g., composite mode), none of which are visible here. Discarded.\n3. *\"Tests are part of this project's own compilation\"* — the `include` array literally contains both `src` and `test` (verified by reading the array; two entries, no miscount risk if phrased qualitatively). Not derivable from the filename/path alone; many layouts isolate tests in a separate config, so an engineer assuming that here would behave differently (e.g., expect `tsc` on this project to also surface errors in `test/`). No comments exist to contradict it; nothing rests on unseen files. Survives.\n\nEmitting the surviving fact.\n\n```atlas-fact\n{\"claim\": \"The include array lists both 'src' and 'test', so this package's test files are compiled and type-checked as part of this same project's build rather than being handled by a separate configuration.\"}\n```"