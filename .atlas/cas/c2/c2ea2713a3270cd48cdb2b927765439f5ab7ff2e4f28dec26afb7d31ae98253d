"STEP 1 — Reasoning (scratch):\n\nReading the bytes: this barrel mixes two distinct re-export styles. Three runtime modules — `./source.js`, `./reconstruct.js`, `./merge.js` — are re-exported through explicit, hand-curated named lists (plus explicit `export type` for `PortableSource`/`SourceApi`/`DriverRegistration`/`SetupResult`). Every other runtime module — provenance, attach, metering, host-adapter, placement, transcript-store, scrub, reinvoke, diff (nine of them) — is pulled in with a blanket `export *`; `types.js` alone uses the type-only star form.\n\nRefutation attempts:\n- Comments here describe other files (co-location, sealed kernel seam, WP task IDs) — per instructions, discard anything resting on those.\n- Candidate: \"the comment says source.js keeps its model private\" — that's a comment's claim about code not shown; discard.\n- Candidate: exact total export counts — countable but fragile and not the point; prefer the qualitative split, which I did enumerate line-by-line: the three explicitly listed modules are exactly source/reconstruct/merge; the nine star-exported ones I listed individually and recount to nine. Verified.\n- Is the surviving candidate just a restatement of the file's role? Knowing \"this is a barrel\" doesn't tell you which modules are curated vs. pass-through — and the difference is actionable: adding a symbol to any of the nine star-exported files silently publishes it as package API with no edit here, while source/reconstruct/merge require a deliberate barrel edit. That follows from these bytes' own export statements plus ordinary language semantics, not from unseen code or comments.\n\nSurvivor: the curation asymmetry between the three explicit-list modules and the nine blanket-star modules.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"In this barrel only source.js, reconstruct.js, and merge.js are re-exported via explicit named lists; the nine other runtime modules (provenance, attach, metering, host-adapter, placement, transcript-store, scrub, reinvoke, diff) are pulled in with blanket 'export *', so any symbol those files export automatically joins the package's public surface without an edit to this file.\"}\n```"