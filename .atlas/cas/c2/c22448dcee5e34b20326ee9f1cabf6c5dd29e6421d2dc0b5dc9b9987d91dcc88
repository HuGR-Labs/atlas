"STEP 1 — Reasoning (scratch):\n\nCandidates drafted from close reading:\n\n- *Witness arrays are sorted copies* (`[...bucket].sort()`) — true and in the bytes, but low impact; mostly an API nicety.\n- *Un-locatable/no-range/unparseable cases become escape witnesses* — genuinely in the control flow, but the header comment loudly announces it, and the interesting contrast (\"engine treats it as safe\") is a claim about code not shown.\n- *Both occurrence symbols and the queried target pass through `canonicalizeSymbol`* — checkable, but partially restates the documented contract.\n- *Indexer-identity gate*: `const SUPPORTED_INDEXER = 'scip-typescript';` plus `if (idx.metadata?.toolInfo?.name !== SUPPORTED_INDEXER) return undefined;`. Refutation attempt: is this derivable from name/signature? No — the signature exposes only `scipPath`/`repoPath` and a nullable return; nothing hints that dump *provenance* is checked. Does it rest on a comment's claim about elsewhere? The surrounding comment invokes ADRs/issues, but the fact stands entirely on the literal and the comparison in the code. Any count/enumeration issue? None — it's a single equality condition. Does reading the dump successfully but finding another tool's name yield `undefined`? Yes, directly in control flow. Survives.\n\nThis is the strongest survivor: a hard, silent enablement switch keyed on a magic string inside the dump's metadata — knowing it changes integration behavior (which indexer must produce the dump, and how to interpret getting `undefined`).\n\nSTEP 2 — Emit result:\n\n```atlas-fact\n{\"claim\": \"Even a fully readable SCIP dump causes buildTargetEscapes to return undefined unless the dump's metadata.toolInfo.name exactly equals the hardcoded string 'scip-typescript', so the leg silently degrades to the caller's fallback for any other indexer.\"}\n```"