"STEP 1 — REASONING (scratch):\n\nCandidates drafted from close reading:\n\n1. *escape/unescape inverse ordering* — visible, but it's a mechanical restatement of two adjacent one-liners; low actionability.\n2. *`isLocalSymbol` uses `'local '` with trailing space* — visible literal, but near-restatement of what the predicate plainly does; weak.\n3. *Dependency-axis leaf hashes are self-keyed* — in `dependencyAxis`, each child is built as `{ key: h, subtreeHash: asSubtreeHash(h) }` where `h` came from `docHash(doc.relativePath)` → `nodeHashOfPath(path)` → `id({ file: path })`. So a leaf's hash is computed from the very value that is its key — a pure function of the path string, touching no content. Refutation attempts: (a) Is it from another file? No — the mapping expression is right here; the root is separate (its own folded hash, key `'dependency'`), so \"leaf\" is accurate. (b) Does it lean on a comment's claim about unseen modules? No — I restrict the claim to what the code computes; I do not cite the external freshness-oracle machinery. (c) Exhaustive count? \"Every leaf\" — verified: the single `.map` covers all children, each with `children: []`. (d) Restates name/signature? No — `dependencyAxis(edges): IndexNode` reveals nothing about self-keyed hashes. Survives.\n4. *`canonicalizeSymbol` only rewrites backtick/space-shaped dist descriptors, else unchanged* — code-visible and actionable, but largely narrated by its own comment; candidate 3 is more consequential for downstream behavior.\n\nCandidate 3 survives refutation.\n\nSTEP 2 — RESULT:\n\n```atlas-fact\n{\"claim\": \"In the dependency axis built by dependencyAxis, every leaf node's subtreeHash is computed from the same value used as its key — the hashed file path produced by nodeHashOfPath/id({file}) — so those leaf hashes commit to nothing beyond the path string and are invariant under any change to the file's contents.\"}\n```"