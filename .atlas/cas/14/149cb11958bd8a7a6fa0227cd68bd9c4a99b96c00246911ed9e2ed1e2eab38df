"```atlas-fact\n{\"claim\": \"In importCas, the re-hash/content-address verification (via addresses()) runs only on entries whose keys match the 64-char lowercase-hex HASH_SHAPE regex; any entry keyed otherwise is inserted into the returned store verbatim with no integrity check at all, so the 'fail closed' guarantee covers only hash-looking keys, not every entry in the bundle.\"}\n```"