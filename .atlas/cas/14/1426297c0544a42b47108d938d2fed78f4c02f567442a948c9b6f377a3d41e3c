"STEP 1 — Reasoning scratch (discarded candidates):\n\n- *Header-comment claims* (#182 history, worker-thread grammar nulling, \"verified RED before commit\"): all rest on a comment narrating other files (`bin.ts`, `mine-worker.ts`) and past events — not re-derivable from these bytes. Discarded.\n- *Exact six-site rank ordering*: present in an expect literal, but it is essentially a restatement of that literal's contents; the deeper causal story lives in comments about elsewhere. Weak candidate, discarded in favor of something less transcriptional.\n- *`llmCalls 6` = one call per site*: derivable by comparing two literals, but entangled with the same enumerated-list risk and partly redundant with the expected array. Discarded.\n- *Fail-open on misspelled arm*: survives. It is NOT carried by the comment alone — the two assertion literals in the third test carry it entirely: `ATLAS_FRONTIER: 'symbols'` (vs the valid `'symbol'` used in the guard test) with `expect(typo.exitCode).toBe(0)` and `toEqual(['file','file'])` (two entries, matching the two fixture files, which I counted). Nothing about input-validation semantics is inferable from the unit's name or the helper signatures, and it changes what an engineer does: adding strict env validation, or assuming bad enum values throw, collides with a contract this suite actively pins. Verified item-by-item against the bytes; no dependence on claims about unseen code.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"This suite pins that setting ATLAS_FRONTIER to the misspelled value 'symbols' makes the run exit 0 with only two file-kind sites — an unrecognized arm name silently falls back to file-granularity behavior rather than failing.\"}\n```"