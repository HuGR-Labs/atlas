"STEP 1 — scratch reasoning (not persisted):\n\nCandidate drafts and refutation passes:\n\n- *“The pool contains NFC-equivalent pairs”* — asserted mainly by the header comment; the literals are visually ambiguous without inspecting raw code points. Weaker than it looks; discard in favor of something sharper.\n- *“hasNfcKeyCollision drops side-index keys and undefined-valued keys before checking”* — true and locally checkable (`Object.keys(o).filter((k) => !SIDE_INDEX.has(k) && o[k] !== undefined)`), but it borders on restating what the filter line plainly does.\n- *“Both exclusion filters are unreachable for generator-produced data”* — verify item by item against the bytes:\n  - Every key in any generated structure traces to `keyArb`, i.e. a join of 1–3 tokens from `['a','b','c','d','é','é','ñ','ñ']`. The three side-index names in `SIDE_INDEX` — checked against the set literal, exactly `grounding`, `status`, `freshness` — each require letters absent from the pool (g/r/o/u/i/s/t/u… / f/r/s/h…), so no generated key can equal any of them. ✔\n  - The undefined-value filter can never fire: `leafArb` is exactly `null | boolean | integer | string`, and `d1`, `d2`, `jsonObjArb` compose only leaves, arrays, and dictionaries of those — `undefined` is nowhere in the generated closure. ✔\n  - Consequence: on `jsonObjArb` output, `hasNfcKeyCollision` returning `true` always reflects a real NFC collision among generated keys, never a filtered-key case. This is derived purely from this unit's literals, control flow, and combinator arguments — no reliance on comments' claims about other files.\n- Survived refutation. Emitting it.```atlas-fact\n{\"claim\": \"For any value produced by jsonObjArb, neither exclusion in hasNfcKeyCollision can ever fire — the KEY_TOKENS pool (a–d plus the é/ñ forms) cannot spell any of the three side-index names 'grounding', 'status', or 'freshness', and no arbitrary in this file generates undefined — so a true result on generated input always indicates a genuine NFC key collision, not a filtered-key artifact.\"}\n```"