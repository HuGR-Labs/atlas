"Looking at this roadmap document, I'll draft candidate facts and test each against the bytes.\n\n**Candidates drafted and tested:**\n\n1. *\"The campaign DAG is acyclic with topological layers {1,2}→{3,4}→{5}→{6,8}→{7}.\"* — Stated almost verbatim in the Coverage section; largely a restatement of the doc's own summary line. Weak.\n\n2. *\"The governance surface is exactly five tools.\"* — Asserted in EPIC-26-a, but the five tools are never enumerated in these bytes, so the exact count cannot be verified item-by-item here. Discarded per the exhaustive-enumeration rule.\n\n3. *\"EPIC-10-a's demo expectations were amended (reformatting reads DRIFTED).\"* — The HTML comment's behavioral claim rests on \"the shipped product\" and ADR-0014, things not present in these bytes; the amendment's truth about product behavior cannot be re-derived here. Discarded.\n\n4. *\"Eager drift propagation caps at maxHops=2 (EPIC-7-b).\"* — A literal in the vertical line, verifiable, actionable. Viable.\n\n5. *\"The standing coverage gate fails when a T0 territory's unresolved-edge ratio exceeds 15% (EPIC-9-b).\"* — Checking the bytes: EPIC-9-b's demoable clause reads \"push a build whose T0 unresolved-edge ratio > 15%, the standing gate fails.\" This is a concrete parameter value and a tier scope written directly in this unit. It is not derivable from the epic's name (\"standing coverage gate on unresolved edges\"), which reveals only that some gate exists — not the threshold, the comparison operator, or the T0 scoping. It would change what an engineer does (they must hard-code the 15% ceiling at T0 in the gate). No reliance on comments about unseen code; it is a literal in the unit itself. Survives.\n\nBetween #4 and #5, both survive, but I emit one — the surviving fact chosen is the 15% T0 ceiling from EPIC-9-b.\n\n```atlas-fact\n{\"claim\": \"The standing coverage gate fails a build when a territory's T0 unresolved-edge ratio exceeds 15 percent, per the demoable criterion recorded under EPIC-9-b.\"}\n```"