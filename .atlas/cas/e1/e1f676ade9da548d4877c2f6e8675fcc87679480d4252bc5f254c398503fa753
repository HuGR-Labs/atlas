"STEP 1 — scratch reasoning (not persisted):\n\n- Candidate A: “export embeds no host paths.” Refuted as a fact — restates the test names/HOST_REF constant; an engineer infers it from the describe block already.\n- Candidate B: “the repo lacks packages/kernel/ref/”. Discarded — that’s a comment’s claim about code outside these bytes (and about a path that isn’t shown).\n- Candidate C: “export is deterministic across key order.” Survives checkability (the `.toBe` assertion is in the bytes) but it’s close to what “portable/stable dump” already implies; weaker than the next candidate.\n- Candidate D: import’s integrity semantics. Checking against the executable lines only: the tampered-body test builds `{format:'atlas-okf',version:1,objects:{[id(a)]:b}}` and asserts a throw matching `/not addressed by its content/` — so import verifies, not trusts. But the third test then shows the guard CANNOT be “re-derive `id(body)` always”: the legit event fixture uses key `eventId(content)`, the code asserts `eventId(content) !== id(ev)`, and `importCas(exportCas(log)).get(ev.id)` succeeds — so the embedded `id` field must be honoured as the address. The forged sibling (`{...ev, id:'f'.repeat(64)}` under the honest key) throws. Also verified: `bodyArb`’s dictionary keys come from `'abcdef'` only, so no generated body can ever carry an `\"id\"` field — the two mechanisms are genuinely distinct within these bytes. This whole contract is checkable from the unit’s own assertions/literals, is invisible in the file name/signature, and directly changes what an implementer writes (a naive always-rederive guard would reject every event log; no guard at all fabricates stores). D survives.\n- Exhaustive-count risk: none — the emitted fact is qualitative.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"importCas rejects dumped entries not addressed by their content (throwing /not addressed by its content/), yet must still round-trip EventLog entries filed under eventId — asserted here to differ from id(body) — so it honours a body's embedded id field as its address rather than blindly re-deriving it.\"}\n```"