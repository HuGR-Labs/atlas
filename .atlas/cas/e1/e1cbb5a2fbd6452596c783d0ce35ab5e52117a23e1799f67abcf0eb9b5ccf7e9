"Looking at the bytes: the fixture maintains two separate storage structures — a private `cas: Map` that exclusively backs `put`/`get`, and the `persists` array of published projections. Tracing who writes which: `stage` and `stageWithKey` call `store.put`, populating the CAS map; `stageRowWithoutBytes` deliberately skips it; but `seedProjection` (and `persistProjection`) push snapshots directly into `persists`, never touching the `cas` map. So a seeded incumbent row carries a `contentHash` that `store.get` cannot resolve — the seeded side is byte-less exactly like the pruned-CAS state `stageRowWithoutBytes` simulates, which is not advertised by the method's name or signature. Checking refutation: this rests only on the bodies of `seedProjection`, `persistProjection`, `put`, and `get` in this unit; no comment about external code is needed; no counts or enumerations are asserted; and it goes beyond the signature (`(rows) => void`). The alternative candidates — `realKey`'s `predicateSlot → slot` rename (already flagged in the adjacent comment) and the row's `claims: [claimNorm]` vs. the fact's `claims: []` divergence — survive too, but the seed/CAS split is the least self-advertised and most action-changing.\n\n```atlas-fact\n{\"claim\": \"seedProjection installs an incumbent snapshot straight into the persisted list without going through the store's put path, so the CAS map backing store.get is never populated for seeded rows and their bytes are unretrievable unless separately staged.\"}\n```"