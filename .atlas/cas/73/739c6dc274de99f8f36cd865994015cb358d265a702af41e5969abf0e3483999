"STEP 1 — scratch reasoning:\n\nReading the bytes: the guard ladder, the two scoped operations, and the branch structure are all fully visible. Candidates drafted and tested:\n\n- *\"There is no refute path — every failure abstains\"*: true in the code (every non-proven return goes through `abstain` or the two inline proven literals), but it is the headline of the header comment and effectively the module's self-description; low surprise, borderline restatement.\n- *\"Lower-bound proven results never touch `holeSources()`\"*: byte-checkable (the `!exact` branch contains no `holeSources` reference), but again the central documented design point.\n- *\"Whitespace-only scope/target strings pass guard 0\"* (checks are `.length === 0`, no trim): derivable, but marginal actionability.\n- *\"Exact-mode gate is keyed on `worldScope`, the count on `sourceScope`\"*: checking the two operative lines — `countInScope(reverse.reverseCallers(target), pathOfHash, sourceScope)` versus `anyInScope(reverse.holeSources(), pathOfHash, worldScope)` — the two scopes enter as independent free variables, and the entire function body contains no comparison, containment test, or normalization relating them. The comment defines \"closed world\" as `holeSources() ∩ worldScope == ∅` but nowhere assigns anyone the duty of making `worldScope` cover `sourceScope`. So a hole lying under `sourceScope` but outside `worldScope` never trips the gate, and the exact branch proceeds straight to the equality test. This is derivable purely from this unit's own dataflow and control flow (the relevant \"absence\" is within the shown body, fully inspectable), it is not implied by the name `verifyCount` or the flat `CountClaim` shape, and it changes what a caller/reviewer must do: supply a `worldScope` that covers `sourceScope`, or the exact verdict's soundness is silently on them. Survives refutation attempts: no other line constrains the scopes; nothing in the code contradicts it.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"In exact mode the closed-world gate tests reverse.holeSources() against worldScope while the witness count is taken under sourceScope, and nothing in this unit's body ties the two scopes together, so exact-mode soundness silently depends on the caller supplying a worldScope that covers sourceScope.\"}\n```"