"```atlas-fact\n{\"claim\": \"The it-blocks are sequentially stateful, not independent: T2's assertions (query returning a single surviving edge, and a second retraction of B–C being refused as already-retracted) can only pass because T1 already performed that retraction against the same module-level splitRepo on disk, so reordering or isolating these tests (e.g. .only or shuffled runs) breaks them.\"}\n```"