"```atlas-fact\n{\"claim\": \"This nominally pure-black-box suite configures the CLI under test through ambient global state: it overrides ATLAS_ACTOR and ATLAS_RATIFY_TOKEN directly on the test runner's own process.env in beforeAll (saving prior values and deleting-or-restoring them in afterAll), so all five tests share that environment-based configuration rather than receiving it through any visible command-line argument.\"}\n```"