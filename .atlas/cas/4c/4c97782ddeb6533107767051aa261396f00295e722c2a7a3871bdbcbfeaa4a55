"STEP 1 — Scratch reasoning:\n\nScanning candidates:\n\n- \"Blanket falls back when only one v2 leg is wired\" — announced verbatim in the it-title (\"HALF-GATE … FALLS BACK … scope-open\"); restates the test's declared purpose. Discard.\n- \"escape-open precedes scope-dynamic\" — likewise the it-title states the ordering outright. Discard.\n- \"`definesAt` is unused on this path\" — rests entirely on a comment's claim (#196d) about behavior outside these bytes. Discard.\n- \"Identity-hashed fakes\" — visible in code (`p as unknown as Hash`), but the header comment advertises it prominently, and its downstream implication (∩ S) leans on unseen gate internals. Weak.\n- \"All 8 emit calls pass `NaN as unknown as Hash`\" — verified by counting all eight call sites, but marginal actionability, and it's a fragile exact-enumeration form. Discard in favor of something stronger.\n- Grounding-anchor assertion ('directory', 'sh-' + S) — partially revealed by its own it-title (\"SAME §3 directory anchor\"), and the mechanism credit belongs to unseen emit code. Discard.\n\nSurvivor: the fixtures set `id: 'ignored'` on every negation node, yet the suite's lookups (`currentOf`, `abstainedOf`) successfully retrieve records under `String(negationKey(KIND, TARGET, S))` — including positive assertions (`family === 'negation'`, abstention reasons, the #220 phantom lookup with a different target). Within these bytes that demonstrates record identity is derived from the relationKind/target/scope triple, not the node's `id` field. No comment asserts this; no title states it; it changes what an engineer does (how to fetch/verify records, and that mutating `id` is safe but mutating kind/target/scope is not). It survives the counter-checks: derivable from declarations, constants, and assertion literals present here; no exact counts; no reliance on out-of-file claims.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"Every fixture negation node carries a throwaway id ('ignored'), yet the suite's lookups succeed under String(negationKey(relationKind, target, scope)) — showing projected negation records are keyed by the kind/target/scope triple rather than by the node's id field.\"}\n```"