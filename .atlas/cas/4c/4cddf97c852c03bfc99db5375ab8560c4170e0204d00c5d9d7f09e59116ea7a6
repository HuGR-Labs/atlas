"Looking at the bytes: the file is an acceptance suite around `createRevIndex(repoPath, { runGit })`. The signature alone tells me git is shelled out and injectable, and the section comments advertise that some retry/classifier machinery exists (while pointing at files like `build.ts` and `git-drift.ts` that aren't shown — those parts I must ignore).\n\nDraft candidate: the *specific* retry semantics encoded in the assertions, not the prose. Checking against the code only:\n- `flakyGit` injects failures purely via `err.stderr` (message kept generic), and delegates everything else to real git, so `adds` cleanly counts `worktree add` attempts.\n- Transient-once test: stderr `could not lock ref: another git process seems to be running` → `expect(flaky.adds()).toBe(2)` — one thrown attempt plus one successful retry.\n- Exhausted test: stderr `Unable to create worktree lock: File exists` on every attempt → `expect(flaky.adds()).toBe(4)` — asserted immediately after the single `axesAt` call, before any other git-touching call, so exactly four attempts belong to that one call.\n- Bad-rev test: stderr `fatal: invalid reference: …` → `expect(flaky.adds()).toBe(1)` — again asserted right after the single `axesAt`, before `resolveAnchorAt` re-invokes git.\n\nAll three numbers are literals I counted directly in the assertions, one scenario per test, no cross-contamination. Refutation checks: none of this rests on a comment's claim about unshown code (the counts and the differing stderr strings are all in these bytes); it's not a restatement of the name (`rev-index.test.ts` / \"arbitrary-rev code index\" says nothing about asymmetric retry policy); and it would change what an engineer does — any `runGit` wrapper, mock, or latency budget must reproduce stderr faithfully and account for up to four spawn attempts under contention versus a single fatal attempt for a bad rev.\n\n```atlas-fact\n{\"claim\": \"In createRevIndex, a transient lock-contention stderr from `git worktree add` is automatically retried — recovering on the second attempt after one failure, or giving up after exactly four failed attempts — whereas an 'invalid reference' stderr aborts after exactly one attempt with no retry.\"}\n```"