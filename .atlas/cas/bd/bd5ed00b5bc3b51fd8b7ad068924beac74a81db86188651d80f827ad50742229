"```atlas-fact\n{\"claim\": \"A model command that exits cleanly (status 0) without reading its piped prompt still gets its partial stdout returned as the claim, while the same early-exit behavior under a non-zero exit status still throws a 'nonzero-exit' failure — so EPIPE-on-write must be salvaged conditionally, never treated as either a blanket error or a blanket catch.\"}\n```"