"```atlas-fact\n{\"claim\": \"The coverage ledger is internally stale relative to the body it summarizes: it claims '64/64 SCN' teeth and a gen histogram of 18 exhaustive + 45 conformance + 1 residue (with 'conformance 45' actually counting REQs, not scenarios), but the document defines 110 distinct '### SCN-KNOW-*' scenario headings — the 45 Wave-H held-out '-2' fixtures (appended in the section after the ledger) plus the unpaired SCN-KNOW-16a-3 push the real population to 91 conformance + 18 exhaustive + 1 residue — so the ledger's completeness accounting understates the file by 46 scenarios and cannot be taken at face value.\"}\n```"