"STEP 1 — REASON FREELY\n\nScanning the bytes for candidates:\n\n1. Candidate: \"Golden ids are symbolic, no hex digest is ever asserted.\" The header comment says this, but I can also verify it in the code: every `id(...)` comparison is relational (`toBe(id(...))`, `not.toBe('acme-fact-001')`) — no digest literal appears in any `expect`. However, this largely restates the file's own header commentary; borderline as \"already known\"/restatement.\n\n2. Candidate: \"Measured digests bcee1c10… / a065e84a… diverged before the guard.\" This is entirely a past-tense comment claim about a base commit (`80318d0`) and prior behavior not present in these bytes. Discard.\n\n3. Candidate: \"NFC fixtures use `\\u` escapes on purpose because literals could be re-normalized.\" The escapes themselves are visible (`'caf\\u00e9'`), but the *reason* is a comment's rationale about hypothetical editors/transports, not code. The bare fact \"escapes are used\" survives, but it is weak and easily misread as style.\n\n4. Candidate: \"id ≡ hex(blake3(utf8(canonicalForm))).\" Fully visible in code: the test imports `blake3`/`bytesToHex` from @noble and asserts `id(x)` equals `bytesToHex(blake3(canonicalForm(x)))`, plus `defaultEncoder.hash(canonicalForm(obj))` in SCN-KERNEL-1b. Strong, but arguably close to what one would infer a \"canonical identity\" suite pins.\n\n5. Candidate: the KERNEL-2a block is a *source-scan* test: it calls `readFileSync(fileURLToPath(new URL('../src/canonical.ts', import.meta.url)), 'utf8')` and asserts the file's raw text does not match `/blake3|sha256|createHash/`. This is a filesystem-based architecture check embedded in a unit-test file — completely invisible from the path `canonical.test.ts`. It means refactors (renaming/moving `canonical.ts`, adding a digest import there, running under bundlers without `node:fs`) break tests for non-behavioral reasons. Meanwhile the same file legitimately imports `blake3` itself for independent recomputation — so a reviewer must not flag that import against this rule. Both halves are checkable in these bytes: the `readFileSync` call and regex, and the `import { blake3 }` at top. No reliance on comment claims about elsewhere (I phrase it as \"the suite runs this scan,\" not \"canonical.ts complies\"). No counts/exhaustive enumerations. Survives.\n\nSTEP 2 — EMIT THE RESULT\n\n```atlas-fact\n{\"claim\": \"This suite embeds a filesystem-based source-scan check that reads ../src/canonical.ts off disk with node:fs and fails if its raw text matches /blake3|sha256|createHash/, even though the test file itself imports blake3 from @noble to independently recompute expected digests.\"}\n```"