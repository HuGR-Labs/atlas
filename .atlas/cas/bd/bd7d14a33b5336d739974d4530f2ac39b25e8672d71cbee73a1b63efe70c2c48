"STEP 1 — REASONING (scratch):\n\nReading the bytes: this is a pure barrel — eight statements total, no local declarations. Seven use the runtime star form (`export *`): gate.js, emit-guard.js, subtree.js, span.js, drift.js, freshness.js, ground.js. Exactly one uses the type-only form: `export type * from './types.js'`. No named re-exports, no aliases, no local code.\n\nCandidate A: \"Seven runtime modules are re-exported.\" — I did count 7, but the count itself isn't the point and the instruction says prefer qualitative form. Weakened.\n\nCandidate B: Claims built on the comments (WP task tags, \"frozen interfaces co-located\", \"shared by ≥2 impl files\", what Campaign-4 did) — all assert things about OTHER files or past work not visible here. Discarded.\n\nCandidate C: Collision-exclusion semantics of `export *` — counterfactual, depends on unseen modules' contents. Discarded.\n\nCandidate D (survivor): An aggregate structural property readable only by scanning ALL the statements together: the barrel contributes zero names of its own, surfaces every runtime module wholesale via unfiltered star re-exports, and holds out types.js as the sole type-only re-export. Not stated by any single line's signature, not claimed by the comments, checkable item-by-item against these bytes (verified: 7× `export *`, 1× `export type *`, nothing else). Actionable: any export added inside those seven modules automatically joins the package-root public surface, and types.js can never contribute runtime bindings through this root.\n\nRefutation pass on D: not reliant on any comment's claim about elsewhere; not an exhaustive count assertion in the risky sense; more than a restatement of the filename \"barrel\". Survives.\n\nSTEP 2 — RESULT:\n\n```atlas-fact\n{\"claim\": \"The barrel contributes no declarations of its own and re-exports every runtime module wholesale via the unfiltered star form, with types.js as the sole module surfaced through a type-only re-export (`export type *`), so nothing in types.js can ever contribute a runtime binding to the package root.\"}\n```"