"Reading the bytes: this is a pure re-export barrel. Almost everything is `export * from './…'`; the interesting anomaly is the pair of lines touching `./types.js`: an explicit named **value** export (`isSemanticSlot`) immediately followed by `export type * from './types.js'`. Refuting candidates:\n\n- Everything in the comments about WP numbers, issue refs, ADRs, \"never stored,\" \"fail-closed,\" reachability.mjs's `declaringModule` behavior, adapter-io files — all assertions about code/tooling **not in these bytes**. Discard.\n- Exact-count candidates (\"24 star re-exports\") — countable but fragile and low-value. Discard in favor of a qualitative form.\n- The structural consequence of the two `types.js` lines survives refutation: it is checkable purely from the two export declarations themselves (no other explicit named export exists anywhere in the file), it isn't a restatement of any signature, and it changes what an engineer does (they cannot import any *runtime* binding from `types.js` other than `isSemanticSlot` via the package root — those names arrive type-only).\n\n```atlas-fact\n{\"claim\": \"Through this barrel, `isSemanticSlot` is the only runtime value exposed from `./types.js` — the same module is also star-re-exported as type-only, so every other export of types.js reaches consumers of the package root as a type-only binding.\"}\n```"