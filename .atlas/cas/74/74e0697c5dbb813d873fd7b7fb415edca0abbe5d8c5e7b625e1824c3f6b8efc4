"STEP 1 — REASONING (scratch):\n\nScanning candidates against the bytes:\n\n- *“Uses the real build()/driftDetect instead of fixtures”* — restates the file name and header comment. Discard.\n- *“Unit content is written by hand into the FileTree”* — true in the literals (`content: unitSrc` child vs `above + unitSrc + '\\n'` file), but the header comment already announces it, and the interesting implication depends on how `build()` treats provided child content, which isn't visible here. Weak.\n- *“FRESH cases edit only `above`, DRIFTED cases edit only `unitSrc`/name”* — checkable, but it's a summary of the fixture's parameterization, low actionability. Held as backup.\n- *Missing-key behavior*: the rename test contains the paired assertions `expect(findNode(after.spatial, unitPath(FILE, 'parseHeader'))).toBeUndefined()` followed by `expect(freshness(fact, after)).toBe('DRIFTED')`. This encodes, in executable code, that an anchor whose key no longer exists in the rebuilt index resolves to `DRIFTED` — not an exception, not `FRESH`. Not inferable from the file name (`freshness.know3.realmint.test.ts`) or the imports/helpers alone; it's a behavioral contract witnessed by this unit's own assertions, not by a comment pointing elsewhere. Survives refutation: the two expectations are code, item-checked, no counts, no reliance on out-of-file claims.\n\nSTEP 2 — EMIT:\n\n```atlas-fact\n{\"claim\": \"The rename scenario pairs a findNode(...).toBeUndefined() assertion with a DRIFTED verdict, encoding that a fact whose anchor key no longer exists in the rebuilt index fails closed to DRIFTED rather than throwing or staying FRESH.\"}\n```"