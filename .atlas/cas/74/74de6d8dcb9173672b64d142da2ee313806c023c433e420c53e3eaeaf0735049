"STEP 1 — REASONING (scratch)\n\nCandidates drafted from close reading of the bytes:\n\n- *Min-key tie-breaks in `correspondence`* — verifiable in the sort/first-wins loops, but it's essentially what the code plainly does and the header comment states; a summary once read.\n- *The `d > 0` filter in `structuralFrontier` is a no-op* — true (every `degree` entry comes from `bump`, hence ≥ 1), but it changes nothing about observable ranking behavior; weak impact.\n- *`st = subtreeOfKey.get(h) ?? h` can put a node-identity hash in the `subtreeHash` slot* — genuinely in the bytes and contradicts the adjacent comment's absolutist wording, but it only manifests in narrow coincidence cases; borderline.\n- *`resolveSiteKey`'s failure semantics* — traced carefully:\n  1. `direct` found and (`inGraph` absent or passes) → return `direct`.\n  2. unit-site file fallback passes → return `file`.\n  3. otherwise `return direct ?? \\`unresolved:${site.subtreeHash}\\``.\n  \n  So if `direct !== undefined` but `inGraph(direct)` is **false**, and the file fallback doesn't apply/pass, execution reaches line 3 with `direct` still defined — and returns that very key the caller's predicate rejected. The `unresolved:` sentinel fires **only** when `keyOfSubtree` has no entry at all. This rests solely on the control flow shown; it holds regardless of what `isUnitSite`/`nodeHashOfPath` do externally, involves no counts or enumerations, and is invisible from the signature (which suggests \"optional membership filter\"), yet it changes integration behavior (callers cannot treat the return as guaranteed-in-graph when `inGraph` is supplied). Survives refutation.\n\nSTEP 2 — RESULT\n\n```atlas-fact\n{\"claim\": \"When resolveSiteKey's optional inGraph predicate rejects a site's directly-resolved key, the function still returns that rejected key (via the final `direct ?? ...` fall-through) rather than the `unresolved:` sentinel, which is produced only when keyOfSubtree has no entry for the site's subtreeHash at all.\"}\n```"