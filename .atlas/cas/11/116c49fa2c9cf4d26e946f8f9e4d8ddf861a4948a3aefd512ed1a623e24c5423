"```atlas-fact\n{\"claim\": \"Beyond behavioral checks, the first scenario reads ../src/canonical.ts and ../src/index.ts from disk at test time and asserts neither file's raw text matches /blake3|sha256|createHash/, so the test hard-depends on those files existing at those relative paths and would fail even if those token names appeared only inside comments or strings in them.\"}\n```"