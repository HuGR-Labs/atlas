"STEP 1 — REASONING (scratch):\n\nThis unit is pure documentation — no declarations, control flow, or types. Candidates drafted and tested against the refutation criteria:\n\n1. *\"Three files used to sit in harness/gates/ that exited 0 having checked nothing\"* — REFUTED: past-tense history about files not shown; the current state (they live in `lib/`) is only asserted, not demonstrated.\n2. *\"BSD `date` lacks `%N`, causing eight concurrent calls to share filenames\"* — REFUTED: a war story about a removed shim; the buggy code is not in these bytes.\n3. *\"The old grep returned 60 lines\"* — REFUTED: an exact count about output of a command run against files not shown; unverifiable here.\n4. *\"No probe is wired into `ci.yml`; `package.json` exposes no probe script\"* — REFUTED: claims about the absence of wiring in two files not present in this unit.\n5. *\"Six gates exist\"* — counted the list (godfile-guard, layer-guard, reference-model-guard, spec-conformance-guard, id-integrity, command-doc-guard = 6, matching \"Six today\"), but this is essentially an inventory restatement the doc plainly offers, low behavioral impact.\n6. **The one-way dependency invariant**: \"Harness code MUST NOT import `@atlas/*` product code,\" with the verification command (`grep -rnE \"^\\s*(import|export).*from '@atlas/\" harness/`) given as a literal in these bytes. Survives: it is stated normatively in the unit itself, not merely a comment about unseen code; it is not derivable from the path/name; it directly changes engineer behavior (never resolve an `@atlas/*` module from `harness/`); no counts, no reliance on external files' contents. The surrounding justification mentions other files, so the emitted claim sticks strictly to the invariant and the shipped command, dropping the \"60 lines\" figure and the mechanization pointer.\n\nSurvivor: #6.\n\nSTEP 2 — RESULT:\n\n```atlas-fact\n{\"claim\": \"The harness enforces a strict one-way dependency invariant — harness code must never import @atlas/* product modules (Atlas consumes the harness purely as dev tooling) — and the README ships the exact import-anchored grep command that must return nothing when run against harness/.\"}\n```"