"STEP 1 — scratch reasoning (not stored):\n\nCandidates drafted from the bytes, then attacked:\n\n- *\"Off-lattice tiers land in neither band\"* — REFUTED. Depends on the internals of `atLeastT1`/`isAdvisory`, which live in `@atlas/tools` and are only asserted by comments. Not re-derivable from this unit's bytes.\n- *\"199 mined facts / ADR-0013 history\"* — REFUTED. Pure comment narrative about other files and past behavior; nothing here re-derives it.\n- *\"GOTCHA_SLOTS includes 'rationale'\"* — SURVIVES technically (literal verified: `['gotcha', 'rationale']`), but it is weaker than another finding; parked.\n- *\"Cost is computed from different sources per band\"* — true and visible (`inv.claim.length` vs `r.node.claims.join('; ').length`), but low decision impact.\n- *\"advisoryBand applies no slot routing\"* — structurally visible, medium impact.\n- **Survivor:** the freshness asymmetry. `governingInvariants` and `advisoryBand` both take a `RowFreshness` parameter and invoke it per row; `governingGotchas` takes no oracle at all and gates its T1 test by feeding the literal `'FRESH'` into `factToInvariant`. Re-checked against the bytes: the signature `governingGotchas(rows: readonly Row[])` has no freshness param; the filter line contains the hard-coded `'FRESH'`; the other two band builders each declare `freshnessOf: RowFreshness`. Exactly three band-building exports exist — counted. The claim rests only on visible signatures, control flow, and a literal, not on any comment's assertion about unseen code. It is not a restatement of any name or signature (the *mechanism* — a fabricated verdict satisfying the shape so the tier predicate can run — is invisible without reading the body), and it would change what an engineer does: any freshness-gating, caching, or staleness-audit work on briefings must treat the gotchas band differently, and retrofitting freshness there requires a signature change.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"Among the three band-building exports in this module, governingGotchas is the only one that takes no RowFreshness argument and instead feeds the hard-coded 'FRESH' verdict into factToInvariant for its T1 gate, so its membership decision never consults the caller's freshness oracle.\"}\n```"