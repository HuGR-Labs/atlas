"```atlas-fact\n{\"claim\": \"Running the genesis-output probe against a store with zero rows exits 2 (UNEVALUABLE), not 0: its row-examining expectations (GOC-2/-3/-4/-5/-7) degrade instead of printing a vacuous PASS, so 'nothing failed' and 'every expectation held' are deliberately distinct outcomes — a gap found when four checks printed PASS against a bare directory during development.\"}\n```"