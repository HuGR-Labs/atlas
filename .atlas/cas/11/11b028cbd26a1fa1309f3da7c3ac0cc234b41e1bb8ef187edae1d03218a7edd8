"**STEP 1 — Reasoning (scratch)**\n\nReading the bytes: the entire \"truth\" machinery in this unit is the local fixture `gateWithPresent`. Its `gateHolds(node, _at)` body references only `node.kind`, `node.claimNorm`, and the closed-over `present` set — the `_at: Hash` parameter is never used, and `node.grounding` / `subtreeHash` / the passed `PEOPLE_SHA` are never touched by it.\n\nChecking how each test seeds the gate:\n- SCN-TOOLS-7a-2: `gateWithPresent(new Set([n3.claimNorm]))` — the set is populated from the very node under test.\n- SCN-TOOLS-7b-2: `gateWithPresent(new Set([prior.claimNorm]))` — seeded from the *other* constructed node, so the reject hinges purely on the claim-text difference (\"Jane Roe\" vs \"John Doe\").\n\nRefutation passes:\n- Fully derivable from these bytes (declarations + control flow + literals); no reliance on `src/emit.ts` internals, other files, or task IDs.\n- Not resting on a comment: the header advertises \"re-derive at source@sha\", but the code shows the fixture never consults the sha or the grounding receipt — the opposite emphasis of the prose, derived from the code itself.\n- No unverified counts: I checked both `it()` blocks individually; \"each test\" is verified item-by-item.\n- Not a restatement of the name/signature: the names suggest source-pinned re-derivation; the discovery is that the gate is a self-seeded string-membership stub with the hash parameter discarded.\n\nSurviving candidate: the gate fixture's decision mechanics (unused `_at`, claimNorm-string membership, sets seeded from the test's own nodes).\n\n**STEP 2 — Result**\n\n```atlas-fact\n{\"claim\": \"The fixture's gateHolds never uses its `_at` hash argument nor the node's grounding receipt — both scenarios decide HOLDS vs NA purely on whether the advisory node's claimNorm string sits in a set that each test populates from the very node(s) it constructs, so the sha f7e8d9 and reference/people.md are never actually inspected.\"}\n```"