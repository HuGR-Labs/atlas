"STEP 1 — scratch reasoning:\n\nReading the body closely, the interface doc for `axesAt` says \"memoized — a rev is immutable so it is built at most once,\" and a competent engineer reading the name/signature would naturally assume both successful *and* failing lookups are cached (an empty snapshot is still a snapshot).\n\nChecking the actual control flow refutes that assumption for failures:\n- `cache.set(rev, axes)` appears ONLY inside the `try`, immediately after `attemptBuild` succeeds.\n- Both failure exits — the `isDeterministicGitError` branch and the retries-exhausted fall-through — add the rev to `unresolved` and return `EMPTY_AXES` WITHOUT touching `cache`.\n- Nothing else short-circuits: `axesAt` checks only `cache.get(rev)`; `unresolved` is consulted only in `axesIfResolved`, AFTER it has already called `axesAt` (which re-runs the whole loop).\n- Therefore every subsequent call for the same bad/exhausted rev misses the cache and re-runs the entire `mkdirSync` → worktree `add --detach` → `walkFileTree` + `deriveGroundingAxes` → `remove --force` + `rmSync` cycle (up to `GIT_MAX_ATTEMPTS` attempts if errors are transient).\n\nRefutation pass:\n- Derivable from THESE bytes? Yes — pure control flow of `axesAt`/`attemptBuild`.\n- Resting on a comment about elsewhere (#73, drift.ts, SCIP parity)? No — I'm contradicting the local \"built at most once\" wording using the code itself.\n- Exhaustive count? I avoid exact numbers; the constant name suffices qualitatively.\n- More than a restatement of name/signature/types? Yes — the signature/doc imply memoization of the returned snapshot; the code memoizes successes only, so repeated calls on a failing rev pay full git-subprocess cost each time (and a test counting checkouts via the injected `runGit` seam would see more than one per failed rev).\n\nSurvives. STEP 2:\n\n```atlas-fact\n{\"claim\": \"Failed revs are not memoized: cache.set runs only after a successful build, so every later axesAt or reader call on a bad or retries-exhausted rev re-runs the entire throwaway-worktree checkout-build-teardown cycle instead of being served from cache.\"}\n```"