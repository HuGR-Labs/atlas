"STEP 1 — Reasoning (scratch):\n\nReading the bytes: `foldArms` computes `const failed = arms.some((a) => a.pass.report.resumeToken !== undefined || a.pass.refusal !== undefined);` and returns `exitCode: failed ? 1 : 0`.\n\nDraft candidates:\n\n- **\"Single-arm runs render byte-identical to the frozen fold\"** — rests on the comment's claim about `foldVerdict` and the bench harness, neither visible here. The delegation `if (arms.length === 1) return foldVerdict(...)` is visible, but the interesting part (byte-identity) is comment-only. Discard.\n- **\"Union total mirrors the durable store\"** — entirely a comment claim about a store not present in these bytes. Discard.\n- **\"`ratified` is always 0 today\"** — past-tense/runtime claim from a comment about behavior not shown. Discard.\n- **\"`{ ...deps, slot }` clobbers a caller-provided slot\"** — mechanically true from spread order, but its *impact* (that `slot` is honored downstream) depends on `MineDeps`'s definition in `./mine.js`, not shown. Borderline; weaker than an alternative.\n- **Failure-signal asymmetry** — the failure predicate reads `resumeToken` off `a.pass.report` but `refusal` off `a.pass`. Both access chains are literally in the bytes; the consequence (either signal ⇒ exitCode 1 for the whole run) follows from the visible ternary. No reliance on unseen files, no counts beyond the two verified disjuncts, not a restatement of the signature (`foldArms(arms, ceiling?): CliVerdict` reveals nothing about where failure lives). Survives refutation. An engineer writing stubs or diagnosing an unexpected exit 1 needs to know refusal attaches to the pass while the resume token attaches to the report.\n\nEmitting the survivor.\n\n```atlas-fact\n{\"claim\": \"A multi-arm fold marks the entire run failed (exit 1) if any arm's pass carries a refusal or its report carries a resumeToken, and the two failure signals are checked on different objects — refusal on the pass itself, resumeToken on pass.report.\"}\n```"