"STEP 1 — scratch reasoning (drafted then refuted candidates):\n\n- *\"`rank` always returns zeroed signals\"* — visible directly in the return statement (`signals: ZERO_SIGNALS`), borderline a restatement of what the code plainly does; also hinted by the interface doc wording. Discarded as weak.\n- *\"Upstream `IndexNode` fields get erased by `canonNode`\"* — whether anything is erased depends on the `IndexNode` type imported from `@atlas/index`, which is not in these bytes. Discarded.\n- *\"The `e.to !== null` check vs `e.to ?? ''` inconsistency is a latent bug\"* — depends on the `DepEdge` type definition not shown. Discarded.\n- *\"Under `provableFirst`, rank order diverges from ppr order\"* — true and code-checkable, but conditional on a caller-supplied option; weaker than an unconditional property.\n- *Fixed-point mass behavior* — drafted: every division in `pprScores` uses bigint `/`, which truncates (per-edge `share`, `danglingShare`, and both damping terms), the remainders go nowhere, and `rank` emits raw `score / FP` with no renormalization step anywhere. Verified item by item in the loop body: three separate truncating divisions per round, no compensating add-back, no final normalize. Survives refutation: rests only on language semantics (bigint division truncates) and literals present here (`FP = 1_000_000_000n`, `PPR_ITERATIONS` loop); no reliance on comments about other files. Not derivable from the name/signature `rank(graph, personalization): readonly Candidate[]`, and materially changes how a consumer may treat `ppr` (not a probability; cross-graph comparisons and calibrated thresholds are unsafe). Avoids overclaiming \"always strictly less than 1\" (a degenerate single-self-loop graph conserves mass exactly), so phrased qualitatively.\n\nSurvivor: the truncation/no-renormalization property of the emitted `ppr` values.\n\n```atlas-fact\n{\"claim\": \"The fixed-point personalized PageRank discards the remainder at every integer division (each out-edge share, the dangling-mass redistribution, and both damping terms), and rank returns the raw accumulated scores divided by the 1e9 scale with no renormalization step, so the emitted ppr values are not a conserved probability distribution and need not sum to 1.\"}\n```"