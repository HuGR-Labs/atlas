"Looking at the bytes: the factory `predicateFactFor` stamps every fact with `id: asNodeKey(\\`nk-${c.site.qualifiedPath}\\`)` — the `id` field is literally cast as a *NodeKey*, strongly suggesting it IS node identity. But the \"no over-collapse — same site\" test pushes two facts built from the **same** `cand` (hence byte-identical `id` values) and asserts `rows.length === 2`, `rows[0].nodeKey !== rows[1].nodeKey`, and even distinct `contentHash`es. So within this unit's own code, the fact's `id` field demonstrably does not govern row identity — the staged rows' `nodeKey`/`contentHash` diverge despite identical `id`s. Checking the refutation criteria: this is read directly off the factory literal plus the assertions (not off any comment's claim about mine-decide.ts, router.ts, or issue numbers); it involves an exact pair/count I verified item-by-item (two facts in, two rows asserted); and it is not derivable from the file's name (\"mine-predicate-check-scrub.test\") or its imports/signatures. It would change what an engineer does — e.g., anyone assuming `f.id` controls dedup or addressing when touching this staging path would be wrong, and the twin-collision test's same-nodeKey result only makes sense once you stop reading `id` as the address.\n\n```atlas-fact\n{\"claim\": \"Two predicate facts built by predicateFactFor with byte-identical id values (same site) are asserted to stage as two separate rows with distinct nodeKeys and distinct contentHashes, so the fact's own id field does not determine node identity in this staging path.\"}\n```"