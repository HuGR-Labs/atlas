"STEP 1 — Reasoning (scratch):\n\nScanning the unit: it is a pure requirements listing where each REQ id carries a descriptive title, so anything whose content merely equals an entry's title (BLAKE3 rollups in REQ-INDEX-2a, \"maxHops=2\" in REQ-INDEX-12i's own title, \"no stack-graphs or LSIF\" in 3c's title) is largely a restatement of the entry's name — discarded.\n\nCandidate: the 15% T0 ceiling in REQ-INDEX-16b. The number is present in these bytes, but it sits inside the quoted normative clause of a single requirement; borderline restatement-plus-quote. Weak.\n\nCandidate worth testing: the trailing `[NEEDS RECONCILIATION]` section combined with REQ-INDEX-15a. Verify against the shown bytes: REQ-INDEX-15a's shall-sentence reads \"the index shall generate territory owner from the structural graph and git-blame authorship,\" while its own quoted normative clause reads \"Territory `owner` SHOULD be generated…\" — a genuine shall/SHOULD mismatch visible by comparing two lines in this same file. The document then explicitly flags INV-INDEX-15 under `[NEEDS RECONCILIATION]` asking for confirmation with DEFINE on whether owner-generation is a hard mandate or a recommended default. Refutation checks: (a) derivable purely from these bytes — yes, the contradiction and the flag are both physically present here, no reliance on `reference/atlas-index.md`; (b) no external file, issue, or past-tense comment dependency — none; (c) no counts or enumerations asserted — none; (d) not derivable from the unit's name/title, which give no hint any requirement is contested — correct; (e) changes what the reader does — yes, an implementer must not enforce REQ-INDEX-15a as a hard MUST-level gate without resolving the flagged conflict first. Survives.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"REQ-INDEX-15a is explicitly flagged '[NEEDS RECONCILIATION]' in this unit because, despite its 'shall generate' wording, its own normative clause only reads 'owner SHOULD be generated', leaving it unresolved whether territory-owner generation is a hard mandate or merely a recommended default pending confirmation with DEFINE.\"}\n```"