"STEP 1 — Reasoning (scratch):\n\nReading the bytes of SCN-PERSIST-7b-2 closely: `remoteKvCache` is declared as a **local const** inside the test callback with a throwing `get()`. Tracing every reference to it in the shown bytes: it appears exactly twice as code — its declaration and `void remoteKvCache;`. It is never passed to `redispatch(...)` or `replay(...)`; every call site passes only `record`/`cp` literals. Since `../src/reinvoke.js` is imported as a module, it cannot reach this local binding by any channel visible here. Therefore the only mechanism that can actually *throw* inside the try block is the swapped `process.env` Proxy; the KV stub is inert scaffolding.\n\nDraft candidate: \"the remote-KV-cache guard in 7b-2 is unwired, so the test only detects non-git reads via process.env.\"\n\nRefutation attempts:\n- Derivable from these bytes? Yes — pure local dataflow; no reliance on other files.\n- Resting on a comment's claim? No — in fact it *contradicts* the comment's \"teeth\" claim (\"a mutant that read … the remote cache would have THROWN\"), which is unsupported by the code since the stub is unreachable from the SUT. My claim stands on the declarations/call sites themselves.\n- Exact count/enumeration risk? I avoid counts; \"never passed to redispatch or replay\" is verified across every visible call site (all are shown).\n- Restatement of name/signature? No — the header advertises \"host-env **and** remote KV cache\" as guarded channels; the code shows only one channel is actually instrumented. This would change what an engineer does (e.g., they would not credit this test with coverage against remote-cache reads).\n\nSurvives. STEP 2:\n\n```atlas-fact\n{\"claim\": \"In the SCN-PERSIST-7b-2 test, the throwing 'remote KV cache' stub is a local object never passed to or referenced by redispatch or replay (its only use is `void remoteKvCache;`), so the git-purity check there can actually catch non-git state reads only through the swapped process.env proxy, not through any remote cache.\"}\n```"