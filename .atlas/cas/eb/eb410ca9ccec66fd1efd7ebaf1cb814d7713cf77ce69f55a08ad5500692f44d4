"STEP 1 — scratch reasoning (not persisted):\n\nScanning the bytes for something beyond name/signature:\n\n- *Candidate: \"`routeWrite` never returns REJECT\"* — verified by enumerating every `return`: `DEDUP`, `CREATE`, `UPDATE`, `SUPERSEDE`/`CREATE`. True and byte-checkable, but the header comment already flags it (\"the upsert route never returns REJECT\"), so it's half-known.\n- *Candidate: \"`writeDecision` collapses family to `'predicate' | 'advisory'` via `candidate.check`, so the four widened `NodeFamily` values are unreachable at the front door\"* — verified at `const family: NodeFamily = candidate.check ? 'predicate' : 'advisory';`. Solid, but the docblock directly above spells out exactly this derivation, so a careful reader likely knows it.\n- *Candidate: DEDUP ordering suppresses the anchor refusal.* Tracing `writeDecision`: `contentHashHit` is resolved and, if true, the function returns `'DEDUP'` **before** `nodeKey(candidate)` is ever evaluated — and `nodeKey` is the only path to `primaryAnchorId`, the sole thrower of `DegenerateAnchorError`. So an exact-byte duplicate is routed DEDUP without the degenerate-grounding gate ever running. Refutation attempts: (a) could `id(candidate)` throw first? It's `defaultEncoder.hash(canonicalForm(·))` over the candidate — no anchor-shape check visible in that path; (b) is the consequence stated in any comment? The docblock says \"DEDUP short-circuits,\" but nowhere states that the short-circuit skips the identity/degenerate-anchor gate itself; the `DEGENERATE_ANCHOR_REASON` prose claims the gate \"reads the GROUNDING … and nothing else,\" which this ordering quietly contradicts in effect (a replayed payload bypasses it). Survives: purely derivable from statement order, non-obvious, and changes what an engineer does (reordering legs changes surfaced errors; refusal tests must guarantee a CAS miss).\n- Rejected: `normalizeCheck` missing kind/body separator (harmless given the two visible kind names), 13-slot count (comment-restated), `every()` poisoning (already narrated inline).\n\nSurvivor: the DEDUP-before-`nodeKey` ordering and its interaction with `DegenerateAnchorError`.\n\nSTEP 2 — emit:\n\n```atlas-fact\n{\"claim\": \"In writeDecision, the content-hash DEDUP check returns before nodeKey() is ever computed, so a candidate whose grounding would trigger DegenerateAnchorError is silently routed DEDUP instead of refused whenever its exact content already exists in the CAS.\"}\n```"