"STEP 1 — SCRATCH REASONING\n\nCandidates considered and stress-tested against the bytes:\n\n1. *\"Exit codes come from execFileSync's status\"* — stated only in a comment; the mechanism lives in `../lib/adr-citation-fixtures.mjs`, not shown. Discarded (comment claims about unseen code).\n2. *\"The gate walks packages/**/*.ts too\"* — supported partly by the #192 comment; the code-side evidence (regex over the OK line, `> 0` checks) exists, but the framing leans on commentary. Weaker candidates exist.\n3. *\"Fenced blocks are still scanned\"* — fully byte-supported (`'```\\nADR-0099\\n```'` → expects `docs/note.md:2 → ADR-0099`). Survives, but essentially handed to the reader by its own `it()` title.\n4. *Refusal-code mapping* — cross-checked fixture by fixture:\n   - bare scratch → `A-0`; docs/ with a .md file, no `adr/` → `A-2`;\n   - **`docs/adr/` existing but EMPTY, nothing else → `A-1`** (individual test AND batch `a1`);\n   - **`docs/adr/` existing and equally EMPTY, plus `docs/x.md` → `A-3`** (batch `a3`);\n   - `docs/adr/README.md` + prose → `A-3`.\n   \n   The decisive contrast: two fixtures with identical `docs/adr/` state (present, empty) get different codes, differing only in whether any `.md` exists elsewhere under `docs/`. So A-1 vs A-3 hinges on the emptiness of the entire docs walk, not on `docs/adr/`'s existence or contents — counterintuitive (one would naively map \"adr dir empty\" straight to A-3), verified item-by-item across all six fixture shapes, qualitative in form, and requiring no trust in the comment's causal story about check ordering inside the unseen gate.\n\nCandidate 4 survives refutation cleanly; candidates 1–2 fail the comment-independence test, 3 is low-gain. Emitting 4.\n\nSTEP 2 — RESULT\n\n```atlas-fact\n{\"claim\": \"An existing but empty docs/adr/ directory yields refusal A-1, not A-3, when no markdown exists anywhere else under docs/ — a fixture with an identically empty docs/adr/ plus one prose file gets A-3 — so the A-1/A-3 boundary is whether the whole docs walk finds any .md file, not whether docs/adr/ exists or holds anything.\"}\n```"