"STEP 1 — scratch reasoning:\n\n- Candidate A: \"The blame test uses `--line-porcelain` because `-l` truncates/prefixes boundary commits.\" Refuted: the *reason* lives entirely in a comment describing git CLI behavior not visible in these bytes; only the flag choice itself is code.\n- Candidate B: \"greet's body goes hi → hello between mb and topic.\" Refuted-ish: this is stated in the header summary and is essentially the test's advertised purpose — close to a restatement of the file's documented intent.\n- Candidate C: \"r0 is asserted equal to mainTip.\" Survives technically (it's a code assertion), but the header's phrase \"blame(greet) at r0/mainTip\" already gestures at this aliasing; weaker novelty.\n- Candidate D: \"After makeGitSbx(), the repo's HEAD resolves to topicTip, not mainTip — the suite enforces the fixture is left checked out on topic.\" Check: derivable purely from the second `it()` block's code (`rev-parse HEAD` compared against `sbx.topicTip`)? Yes. Does it lean on any comment's claim about elsewhere? No. Numeric/exhaustive? Phrased qualitatively, no count claimed. Already implied by the name/signature or header? No — notably the header's own enumeration of what the suite asserts omits the HEAD-on-topic check entirely, so even the file's own docstring doesn't advertise it. It would change what an engineer does (any consumer assuming the sandbox starts on `main` would be wrong).\n- D survives. Emitting it. STEP 2 result below.\n\n```atlas-fact\n{\"claim\": \"Beyond checking topology SHAs, this suite enforces that a freshly built git-sbx sandbox is left checked out on the topic branch: it runs `git rev-parse HEAD` and requires it to equal topicTip, not mainTip — an invariant the file's own header summary never mentions.\"}\n```"