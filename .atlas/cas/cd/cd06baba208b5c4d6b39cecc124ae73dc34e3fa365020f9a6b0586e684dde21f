"**STEP 1 — scratch reasoning (candidates tried and refuted):**\n\n- *Candidate: \"the attack grounds at paths outside the `scopedPolicy('src')` scope\"* — depends on guessing the semantics of `scopedPolicy` from `./support.js`, which isn't shown. Discarded.\n- *Candidate: \"tests are order-dependent via shared repo/projection\"* — plausible and mostly visible, but rests partly on assumed `makeFixtureRepo` behavior and framework ordering conventions not in these bytes. Weaker than a purely local alternative.\n- *Candidate: fixture/env handling details* — plain restatements of visible setup code.\n- *Survivor:* the fourth test is titled \"two symbols in the SAME file still share a prefix,\" but its `sites` literal is `[['src/billing.ts','charge'], ['src/billing.ts','charge']]` — the identical site repeated. Verified item-by-item against the bytes: both entries match exactly, and `FILES` shows `src/billing.ts` defines only `charge`. So the \"no over-bricking\" case never exercises two *distinct* co-file symbols — a coverage gap checkable purely from this unit, invisible from the test's name, and actionable for anyone assessing whether the fix avoids false refusals.\n\n**STEP 2 — result:**\n\n```atlas-fact\n{\"claim\": \"Despite its title referring to two symbols in the SAME file, the no-brick test passes the identical site ['src/billing.ts','charge'] twice, so it never actually exercises two distinct symbols sharing one file.\"}\n```"