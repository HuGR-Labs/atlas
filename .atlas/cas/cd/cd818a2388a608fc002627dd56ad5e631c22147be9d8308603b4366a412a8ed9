"STEP 1 — Reasoning (scratch):\n\nCandidates considered against the bytes:\n\n1. *\"Unknown flags are rejected\"* — supported by the `--hurry` transcript (exit 1, structured error). Survives, but the framing \"the one command that does not inherit the parser's behaviour\" leans on a contrast with other commands not shown here. Weaker phrasing possible, but not the strongest candidate.\n2. *`--retract=false` is an error* — directly shown by a real run (exit 1). True and local, but lower action-impact.\n3. *Class-wide authorization boundary* — shown in the unauthorized refusal literal; strong, but partly carried by prose inside a quoted string.\n4. *Monotone retraction bars the pair forever* — demonstrated by two real runs: asserting after a retraction hits the `retracted-pair` gate (exit 2), re-retracting hits `already-retracted`. The refusal text states re-assertion would require deleting the retraction record and that relinking \"needs a new pair of keys, not a silent un-erase.\" Nothing in the name or signature `link <a> <b> [--retract]` suggests `--retract` is a one-way ratchet rather than a toggle; an engineer would naturally assume they could re-link. Fully checkable within these bytes, no reliance on other files, no counts or enumerations. This survives refutation best.\n\nSTEP 2 — Emitting the surviving fact:\n\n```atlas-fact\n{\"claim\": \"Retraction is monotone: once a sameAs equivalence between two nodeKeys is retracted, `atlas link` permanently refuses both re-assertion (retracted-pair) and re-retraction (already-retracted) for that pair, so relinking the two facts requires a brand-new pair of keys rather than flipping the edge back.\"}\n```"