"STEP 1 — Reasoning scratch:\n\nCandidates drafted and tested against the bytes:\n\n1. *\"Normalization is length-preserving\"* — verifiable by counting replacement chars (NUL→space, `type`→4 spaces, `[]`→2 spaces), but it is loudly documented in the comment and is essentially what the function's doc already advertises. Weak novelty; discarded.\n\n2. *\"Fold path skips normalization\"* — checking call sites of `normalizeForGrammarGaps`: defined once, called exactly once, inside `parseTsDoc` (`parser.parse(normalizeForGrammarGaps(content))`). Meanwhile `itemsOf` — the path reached by `refine` ← `foldAstUnits`/`foldAstUnitsWithPriors` — calls `parser.parse(src)` on raw content, and `refine` passes `node.content` through untouched. No normalization occurs anywhere else in this file. Consequence checkable here: a file containing any of the three rescued constructs yields `root.hasError` → `return []` in `itemsOf` → `refine` returns the node unchanged, while `parseTsDoc` on identical bytes returns a `ParsedDoc`. Survives all refutation tests: derivable purely from this unit's control flow, no reliance on out-of-file comments, qualitative (\"only\") rather than a risky count, and not inferable from names/signatures — an engineer would reasonably assume parity since `parseTsDoc`'s contract makes the rescue so prominent. Changes what they'd do (e.g., expecting fold recall gains from editing the normalizer, or debugging why the fold abstains where `parseTsDoc` succeeds).\n\n3. Symlink-leaf short-circuit, fresh Parser-per-parse, ordinal bucketing — all visible at a glance in the relevant function bodies or restatements of adjacent comments; weaker than #2. Discarded.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"The AST fold path (itemsOf, reached via foldAstUnits/foldAstUnitsWithPriors) parses the raw file content directly, so normalizeForGrammarGaps is applied only inside parseTsDoc — files carrying the three rescued constructs still fail closed to an unchanged tree under the fold.\"}\n```"