"STEP 1 — scratch reasoning (not stored):\n\nCandidates drafted and refuted against the bytes:\n\n1. *The MECHANISMS registry drift (`genesis/rank.ts:82`) lists `'stack-graphs'`.* — REFUTED: that is a claim about a different file not shown here; even if true, it doesn't re-derive from these bytes.\n2. *The register lists INV-CLI-7 and INV-CLI-4 out of numeric order, after the MCP entries.* — Survives as derivable, but it's cosmetic ordering that wouldn't change an engineer's actions much.\n3. *No INV-CLI-5 or INV-CLI-6 exists.* — Derivable, but low impact; gaps may be intentional.\n4. *Count check: the register holds 20 INV sections, each flagged `behavioural: true`, while the Completion report asserts \"19 behavioural · 0 exempt\".* — Enumeration: ADAPTER-1..12 (12) + WIRE-1 (1) + CLI-1,2,3 (3) + MCP-1,2 (2) + CLI-7 (1) + CLI-4 (1) = 20. Re-checked heading by heading; every entry carries `behavioural: true`; none is marked exempt or withdrawn (even INV-CLI-4, though narrowed, keeps `behavioural: true`). The report's own tally therefore contradicts the register body by one. SURVIVES: fully internal to the unit (no reliance on unseen code), exact count verified item-by-item, invisible from the file's name alone, and directly actionable — an engineer preparing the S0 sign-off / `freeze/adapters-v0` tag must reconcile the off-by-one before trusting the completion report.\n\nSTEP 2 — emitting the surviving fact.\n\n```atlas-fact\n{\"claim\": \"The register body contains twenty INV entries (INV-ADAPTER-1..12, INV-WIRE-1, INV-CLI-1..3, INV-CLI-7, INV-CLI-4, INV-MCP-1..2), every one flagged behavioural:true, yet the Completion report tallies '19 behavioural · 0 exempt' — an off-by-one self-contradiction in a document whose STATUS declares it S0 freeze-READY pending sign-off.\"}\n```"