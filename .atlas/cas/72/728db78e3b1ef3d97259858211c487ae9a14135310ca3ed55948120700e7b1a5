"STEP 1 — Reasoning scratch (drafting and refuting candidates):\n\n- *Candidate: “put rejects NFC-colliding objects.”* Refuted as stated: whether put actually behaves that way depends on `src/store.ts`, which is not in these bytes; here it’s only an expectation. Weakened form (“the test expects the literal `''` sentinel”) survives technically but barely changes engineer behavior beyond the comment’s explicit statement.\n- *Candidate: “dedup makes re-put return same hash.”* Again an expectation about invisible code, and essentially a restatement of the visible assertion.\n- *Candidate: “property-test objects are `{kind, body}` records, unlike the N/K/M fixture shapes.”* True in bytes but low-impact.\n- *Candidate: the structural single-store enforcement.* Survives every refutation pass: the second test literally calls `readFileSync` on `../src/store.ts`, runs `(src.match(/new Map\\b/g) ?? []).length`, and asserts `toBe(1)`. This mechanism (a runtime textual grep of sibling source, where ANY occurrence of `new Map` anywhere in the file text—comments and strings included—counts toward the total) is fully checkable in these bytes, is not implied by the test name “no second store,” and materially changes what an engineer touching `store.ts` must avoid. The exact value `1` is directly present and verified in the shown assertion.\n\nSurvivor confirmed.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"SCN-KERNEL-3b-1 enforces 'exactly one store' structurally at runtime by reading ../src/store.ts as raw text and asserting that the regex /new Map\\\\b/g matches exactly once, so any additional textual occurrence of `new Map` anywhere in that file—including comments or strings—fails the test.\"}\n```"