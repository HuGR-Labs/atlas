"Looking at the bytes: the suite's headline theme is \"honest unresolved edges\" (SCN-INDEX-3e-1 demands unresolvable refs become `unresolved`/`to:null`). Against that backdrop, the SCN-INDEX-13c-3 case stands out: its fixture places both a `definition` and a `reference` of `local 2` **inside the same document** (twice, once per file), and the assertion is `expect(edges).toEqual([])` — not \"no resolved edges\", not \"unresolved with null to\", but a completely empty list. That contradicts the natural reading of the sibling law (record, don't guess) and of the test's own title (\"never fabricates a cross-document edge\"), which alone would permit `unresolved` or self-edges.\n\nRefutation pass:\n- Resting on comments about `build.ts` internals, the scip.proto grammar, or the pre-fix `Map` bug? Discarded those parts — the surviving claim rests only on the fixture literals and the `toEqual([])` assertion, both in these bytes.\n- Library/convention knowledge? None needed; the expected value is literal.\n- Exact enumeration? \"empty edge list\" is the literal assertion, verified in the shown bytes.\n- Restatement of name/signature? No — the name suggests anti-fabrication, not total exclusion; the stricter semantics go beyond it.\n\nSurvives.\n\n```atlas-fact\n{\"claim\": \"In SCN-INDEX-13c-3, each document's own definition-plus-reference pair on the `local 2` symbol is nevertheless expected to yield a completely empty edge list — `local` symbols must be excluded entirely, producing not even an `unresolved` record, unlike every other unresolvable reference in this suite.\"}\n```"