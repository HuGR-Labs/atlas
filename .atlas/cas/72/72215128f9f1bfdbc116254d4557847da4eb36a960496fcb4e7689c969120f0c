"STEP 1 — REASONING (scratch)\n\nScanning the bytes: imports give bare names from `../src/merge.js` with no signatures, so everything about how the merge surface is used must come from call sites in this file.\n\nDraft candidates and refutation attempts:\n\n- \"ev sets fresh: true and payload {ch}\" — visible literals, true, but marginal action-change. Weak.\n- \"set() is first-write-wins on duplicate id\" — visible in code (`if (!m.has(e.id))`), not in the signature; plausible but low impact.\n- \"Property generator derives nodeKey as k%2 and seq as k%3\" — largely restates the adjacent comment; discard as comment-paraphrase.\n- \"logArb can produce empty logs\" — depends on fast-check's default minLength, i.e., library knowledge not in these bytes; discard.\n- \"Scenario e rebuilds e1/e2 locally with seq=5 while module-level e1/e2 use default seq=0\" — true in bytes, but confusing trivia rather than decision-changing.\n- Candidate: **no merge call ever supplies a base/common-ancestor argument.** Checking item by item: `mergeDriver(oursJsonl, theirsJsonl)` — 2 args; `degradeMerge(oursJsonl, theirsJsonl)` — 2 args; every `mergeAtlas(...)` call (tests b, c, d, e ×4, g, and the three property tests including nested associative forms) — exactly two side inputs each. No third argument anywhere in the file. This survives: it is fully checkable in the shown bytes (call sites, not comments), it is NOT derivable from the import list alone (no signatures shown), and it contradicts what the \"plain 3-way text merge\" framing would lead an engineer to assume — the degradation scenario is exercised without any ancestor input. Verified against every call site; no miscount.\n\nSurvivor selected.\n\nSTEP 2 — RESULT\n\n```atlas-fact\n{\"claim\": \"Although framed around git's 3-way merge, no call in this file ever passes a base/common-ancestor argument — mergeDriver, degradeMerge, and mergeAtlas are all invoked with only the two branch sides, so even the 'bypassed 3-way text merge' scenario is exercised base-less.\"}\n```"