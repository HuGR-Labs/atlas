"Reading the bytes: four exports — `Hash`, `Encoder`, `SubtreeHash`, `NodeKey`. Every single declaration is `export type` or `export interface`. There is no `const`, `function`, `class`, default export, or any statement with runtime effect anywhere in the file — including `Encoder`, which despite being the \"swappable digest seam\" is only an interface with a method signature and no body, default implementation, factory, or algorithm constant.\n\nDraft candidate: \"this file is compile-time-only vocabulary; nothing here executes or computes a hash.\"\nRefutation pass:\n- Derivable from these bytes? Yes — I can enumerate every top-level statement and confirm each is a type alias or interface. No comment-trust needed; the absence of any value declaration is directly observable in the shown unit.\n- Resting on a comment's claim about elsewhere? The comments mention \"@atlas/kernel mints values,\" but my claim doesn't depend on that — it stands on the declaration forms alone. I will strip any reference to where implementations live.\n- Exact count/enumeration risk? I could say \"all four exports,\" and I did verify item by item (two type aliases + two... no, one interface and three type aliases). The qualitative form is safer: \"every export is a type or interface.\"\n- More than a restatement of name/signature? Individually the signatures are types, but the meta-fact that the *entire* unit — including the thing named like a hasher — contains zero executable code is not carried by any single signature and contradicts what the filename `hash.ts` suggests. It changes behavior: don't import runtime functionality from here.\n\nIt survives.\n\n```atlas-fact\n{\"claim\": \"Despite its hash.ts name, this unit contains no runtime code at all — every export is a type alias or a bare interface (Encoder included), so nothing in this file itself computes or produces a hash.\"}\n```"