"STEP 1 — Reasoning scratch:\n\nCandidates drafted and attacked:\n\n1. *\"nodeKey(T0) and nodeKey(T2) are identical\"* — present as literals in the transcript, but the document itself annotates the second line with \"← identical\"; this is close to a restatement of what the bytes plainly display and highlight. Weak as \"not already knowable.\"\n2. *\"Every tier's ratifier gate is a string comparison against `process.env.ATLAS_RATIFY_TOKEN`\"* — the conclusion is asserted here, but its evidence (`compose.ts:120`, black-box test line numbers, `support.ts:26`) lives entirely in files not shown. A fact about that code, not this unit. Discarded.\n3. *\"upsert throws instead of returning `REJECT` because the sole caller discards the decision\"* — the decisive evidence is `governed-emit.ts:304`, not in these bytes. Discarded under the checkability rule.\n4. *\"Suite delta is exactly ten scenarios\"* — the arithmetic checks out (+1 file, +10 tests), but \"ten new scenarios\" is never itemized in these bytes; the doc only names ~4 scenario IDs. Cannot re-derive the count here. Discarded.\n5. **Count mismatch**: the Status line says verbatim \"Four items are left OPEN and are listed, by name, in §'What the owner still has to ratify'\", yet that section enumerates items **1 through 5** (door supplies `derivedTier`; ARCH-D3b CREATE leg; `scope`↔`primaryAnchor` binding; verifiable ratifier token; architecture-doc decision table). Recounted item by item: five numbered entries against a claimed four. Fully internal to the unit — no reliance on any unseen file, issue number, or past-tense claim. Not inferable from the filename, which says nothing about open-item counts. Actionable: anyone triaging outstanding ratifications from the Status line alone under-tracks the listed work, and the ADR needs reconciliation before leaving Proposed.\n\nCandidate 5 survives every refutation attempt.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"The Status line of ADR-0010 states that four items remain open and are listed in the 'What the owner still has to ratify' section, but that section actually enumerates five numbered items.\"}\n```"