"STEP 1 — REASONING (scratch)\n\nCandidates drafted while reading:\n\n1. \"`projectionTrap` covers persist/load/commit\" → the trap name already implies throwing doors; the *reason* it has three members leans on the comment's claims about `governed-emit.ts` / `governed-link.ts`, files not in these bytes. Refuted as comment-dependent.\n\n2. \"`readStaging` yields the empty projection when nothing is staged\" → the justification (\"the commit protocol decides over `emptyStore()`\") rests on `sidecar-commit.ts` behavior not shown here; within these bytes I can only see the fakes' behavior, not the product's. Refuted.\n\n3. \"`NO_MODEL_ENV` pins `XDG_CONFIG_HOME` to a nonexistent path\" → the literal is visible, but the *why* (homedir fall-through in `modelConfigPath`) is entirely comment-asserted about unseen code, and the bare literal value is nearly a restatement of what reading the constant shows anyway. Weak actionability. Set aside.\n\n4. \"`depsOf` omits the `budget` key unless provided\" → true and visible in the conditional spread, but low impact; a reader of the signature could plausibly infer it. Weak.\n\n5. \"`factFor` returns something typed `Fact` but built by force-cast\" → checking the bytes: the body constructs an ad-hoc object literal (`kind: 'advisory'`, hand-rolled `grounding`, etc.) and pushes it through `as unknown as Fact`. Same pattern in `anchorOf` (`as unknown as {...}`). This means the compiler never validates these fixture facts against `Fact`'s real declared shape — a signature reading `(...): Fact` actively misleads; drift in `Fact`'s schema will not break compilation of these fixtures. Survives refutation: it is derivable solely from this unit's declarations/casts, not from any comment about other files, involves no counts or enumerations, and is not a restatement of the name or signature. It changes what an engineer does: they cannot treat these fixtures as type-checked witnesses of `Fact`'s structure, and edits to `Fact` must be checked against this file manually.\n\nSurvivor: candidate 5.\n\nSTEP 2 — RESULT\n\n```atlas-fact\n{\"claim\": \"Although `factFor` is declared to return `Fact`, it fabricates the value from an ad-hoc object literal forced through an `as unknown as Fact` double cast, so the compiler never checks these fixture facts against `Fact`'s real shape and type drift in `Fact` will not surface here.\"}\n```"