"STEP 1 — Reasoning (scratch):\n\nReading the assertions, not the prose: the file is *named* \"read-provenance-refusal\", so a competent engineer would assume its query cases pin `ok:false` refusals. The code says otherwise for most of them:\n\n- Case 2: `poisonedRepo()` (default: commits `projection.json` + CAS via `git add -f`), then `handle('atlas-query', …)` → `expect(v.ok).toBe(true)`, forged claim absent.\n- Case 3: same fixture, `by: 'dependency'` → `expect(v.ok).toBe(true)`.\n- Case 4: same fixture **plus** writing and committing `.atlas/staging.json` → the only `atlas-query` case asserting `expect(v.ok).toBe(false)`.\n- Control case 8: `poisonedRepo({ commitAtlas: false })` → `ok:true` and `toContain(FORGED_CLAIM)`.\n\nAttempted refutation: could this rest on the header/travel comments rather than code? No — each point maps to a literal `expect` statement in the shown bytes. Could \"only one query case refuses\" be a miscount? Re-checked item by item: exactly one `it` block issues an `atlas-query` handle call expecting `ok:false`, and it is the staging-committed one; the other `ok:false`/throw assertions sit on different doors (`resolveNode`, doctor, emit), so the claim must stay scoped to `atlas-query` legs. It is not a restatement of the filename — it contradicts the natural reading of it — and knowing it changes what an implementer/porter must build (a filtered serve plus one narrow refusal population, not a blanket refusal). Survives.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"Despite the suite's refusal-themed name, every `atlas-query` leg over the default `poisonedRepo()` fixture (projection+cas committed) asserts `ok:true` serving with the forged row dropped, and the flat `ok:false` provenance refusal is pinned only in the single query case that additionally commits `.atlas/staging.json`.\"}\n```"