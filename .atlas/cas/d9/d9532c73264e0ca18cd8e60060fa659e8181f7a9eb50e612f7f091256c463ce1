"STEP 1 — scratch reasoning:\n\nScanning the bytes: the interesting machinery is not the tests themselves (they restate the describe titles) but the `legWired` predicate and its helper `surface`.\n\n- Candidate A: \"GOVERNANCE_SURFACE has exactly five members: init/query/emit/reconcile/link.\" Refuted — the set is an imported symbol iterated opaquely; the member list comes only from header comments describing another module. Discard.\n- Candidate B: \"assembleHandler is the sole assembly point.\" That's the comment's claim about CLI/MCP files not shown here. Discard.\n- Candidate C: the re-import identity check (`await import('../src/wire.js')` → `toBe`). Real and in-code, but largely a restatement of the surrounding comment and of low maintenance impact. Weak survivor; discard in favor of stronger.\n- Candidate D: the `legWired` criterion. Signature `(h, tool) => boolean` reveals nothing. The body shows: probe is `h.handle(tool, {})` with an empty-args literal; the predicate is a *negation* — wired ⇔ `!(rejected ?? '').includes('not wired at this seam')`. So success, or any rejection whose text lacks that exact substring, still registers the leg as wired. Domain failures count as evidence of wiring. Moreover the phrase is spelled out raw in this file (once in `legWired`, once in the `atlas-diff` expectation) and appears nowhere in the import list — no shared constant is imported, so the test is coupled to the production error wording by a raw substring. All checkable purely from these bytes; the neighboring comment mirrors the code rather than the code depending on the comment. Survives refutation.\n- Exact-count check: I considered saying the literal occurs \"twice\"; safer to state qualitatively (\"inline, not imported\") since comment text also mentions a longer variant of the phrase.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"A leg is classified as wired purely by negation: legWired probes h.handle(tool, {}) with empty arguments and counts the leg as wired unless the verdict's rejection text contains the hard-coded literal 'not wired at this seam', so a successful call or any other rejection still counts as wired — and that phrase is written raw inside this test file (never imported), coupling the suite to the exact production fail-closed wording.\"}\n```"