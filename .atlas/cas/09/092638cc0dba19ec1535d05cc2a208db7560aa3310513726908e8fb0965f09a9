"STEP 1 — Reasoning scratch:\n\nReading the control flow around the ratchet: violations are collected with deduped keys `${code} ${key}` into `violations` and mirrored in `seen`. Then `fresh = violations.filter(v => !KNOWN.has(v.key))` and `stale = [...KNOWN.keys()].filter(k => !seen.has(k))`, followed by `if (fresh.length > 0 || stale.length > 0) … process.exit(1)` — with an explicit message telling the operator to delete the stale entry from KNOWN.\n\nDraft candidate A: the ledger is not a suppression list; it fails in both directions — new violations AND entries that stopped firing.\n\nRefutation attempts:\n- Derivable from these bytes? Yes — the two-sided exit condition is plain control flow in this unit, not a claim about another file.\n- Does it rest on a comment's claim about unseen code? No. The header comments narrate the rationale (and mention past removed entries), but the behavior is re-derived entirely from the `fresh`/`stale` computation and the shared `exit(1)` branch. The comments about previously removed ID-5/REQ-TOOLS-6d entries describe history I cannot verify, so I exclude those parts; the surviving core is the fail-on-staleness mechanism itself.\n- Exact count / exhaustive enumeration? No numeric claim needed; qualitative form survives.\n- More than a restatement of name/signature? Yes — nothing in `id-integrity.mjs` implies that fixing a defect keeps the gate red until the gate file's own hardcoded map is edited. An engineer treating KNOWN as a conventional known-failures allowlist (fix the code, leave the ledger alone) would ship a permanently red gate.\n\nCandidate B (env-var swap of ROOT and KNOWN for fixture testing) also survives refutation, but A is the property that changes day-to-day behavior: it inverts the usual semantics of a baseline ledger. Emitting A.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"The hardcoded KNOWN defects map is a shrink-only ratchet enforced in both directions: the gate exits nonzero not only on new violations but also whenever a recorded KNOWN entry no longer fires because it was fixed, so repairing a ratcheted defect leaves CI red until that entry is manually deleted from this gate file.\"}\n```"