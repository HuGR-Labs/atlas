"STEP 1 — Reasoning (scratch):\n\nCandidate facts drafted from the bytes:\n\n1. *\"Budget defaults to min(frontier_size, 200) sites with a trailing-20 admit-rate < 20% halt.\"* — Present verbatim in GEN-2. Survives, but it's partially hinted by the visible `--budget N` flag; less surprising.\n2. *\"Interview is capped at 20 questions, remainder deferred or defaulted to T0-strict deny.\"* — Stated in S3. Survives refutation; actionable but secondary.\n3. *Anti-vacuity teeth rule* — GEN-12 states a synthesized `check` must return `HOLDS` on current code AND flip to `BROKEN` on a mechanically-mutated counterfactual of the anchored subtree; a check no mutant breaks is vacuous and dropped. Restated in Acceptance item 12. Refutation check: fully contained in these bytes (normative invariant + acceptance criterion), not a comment asserting something about another file; no counts or exhaustive lists to verify; nothing in the name (`atlas-genesis`) or the surface signatures (`scan/mine/extract/interview/handoff`) hints at mutation-testing-based admission — a naive reader would assume \"HOLDS everywhere\" means a great check, when the spec treats it as disqualifying. This directly changes implementation behavior (you must build mechanical mutation counterfactuals into the admission harness and drop tautologies).\n\nFact 3 survives; it is the strongest non-obvious, behavior-changing claim re-derivable from this unit alone.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"In S2 extraction, a synthesized predicate check is admitted only if it returns HOLDS on the current code and flips to BROKEN under a mechanically-mutated counterfactual of its anchored subtree — a check that survives every mutant is judged vacuous and dropped.\"}\n```"