"**STEP 1 — Reasoning / refutation pass**\n\nCandidates considered:\n\n1. *Exit codes come from `execFileSync`'s thrown `status`, not shells.* — Implemented in `run()`'s try/catch, but it's essentially a restatement of what the code plainly does and the comment already announces it. Discard.\n2. *Fixtures symlink `packages`/`node_modules` back at the repo.* — Visible in `scratchRoot()`, but directly announced by a comment and plainly coded. Discard.\n3. *The real-corpus run is memoized (`realRun ??= run(REPO)`).* — True and code-derivable, but a fairly ordinary caching pattern once seen. Weak on \"changes what they do.\"\n4. **The earned-declaration check runs the stripped gate ONCE, not per declaration.** Test against the bytes: the code does `.replace(FROZEN→{})` and `.replace(UNVERIFIABLE→{})` **simultaneously**, writes one `all-verified.mjs`, makes exactly **one** `execFileSync` call, collects the `✗ <key>` lines into a Set, and computes `unearned` by set difference. Yet the block comment above it claims the proof works by promoting declarations \"one at a time, by deleting it from a COPY of the gate.\" The current code contradicts that description — a reader trusting the comment would expect N runs and N red/green verdicts, when in fact there is one run and per-block verdicts are inferred from which keys appear in the failure output. This survives every filter: derivable purely from these bytes (the replaces, the single run, the Set/filter logic), no reliance on unseen files, no counts beyond \"one run\" which is directly countable in the code, and it is neither a name/signature restatement nor a summary of the obvious. Actionable: anyone touching the test's cost model, its 180 s cap, or its coupling to the gate's `✗ <file>#N` output format needs to know the mechanism is one combined run, not iteration.\n\nSurvives. Emitting.\n\n**STEP 2 — Result**\n\n```atlas-fact\n{\"claim\": \"Despite its comment describing promotion 'one at a time', the earned-declaration test actually empties the FROZEN and UNVERIFIABLE maps in one pass, runs that single stripped gate copy exactly once, and flags any extracted declaration missing from the run's `✗ <file>#N` failure lines as unearned.\"}\n```"