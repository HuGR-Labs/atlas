"```atlas-fact\n{\"claim\": \"In verifyNegation, the counterexample search over reverseCallers(target) runs before the completeness check over holeSources()/opaqueRefSources(), so a 'refuted' verdict can be emitted even when the scope contains unresolved or collapsed references — scope hole-freeness gates only the 'proven' verdict and the 'scope-open' abstention (which therefore only occurs when no caller was witnessed).\"}\n```"