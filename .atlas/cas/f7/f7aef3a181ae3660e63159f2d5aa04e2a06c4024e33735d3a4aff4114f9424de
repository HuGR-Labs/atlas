"STEP 1 — Scratch reasoning (not persisted):\n\nCandidates drafted from a close read:\n\n- *Cold-start drop order is exactly [poke, protocols.advisory, pack, own, projectMem, orientation]* — verified against the `SPEC_COLD_DROP_FIRST` literal item by item (six entries, no pins). Survives verification, but it is the test's headline assertion sitting in a named constant; an engineer skimming the body sees it immediately, and it borders on summarizing what the code plainly declares.\n- *\"related\" is excluded because it's outside the frozen InjectionKind vocab* — the exclusion is visible, but the reason rests entirely on comments about `@atlas/contracts`, a module not in these bytes. Discarded.\n- *Tie-break κ is the cold-start rank in the oracle but never asserted* — visible in the comparator and tie test, but the test name itself already announces \"determinism only,\" so much of it is knowable from names. Weaker candidate.\n- *Whole-kind drops cause undershoot (sum 3650 ≪ 5000)* — inferable from `r.sum` assertion plus dropped list, but somewhat derivable from the drop-list shape itself.\n- **servePack's full behavioral contract** — checked clause by clause against the bytes, ignoring the explanatory comments: `expect(reground).toHaveBeenCalledTimes(1)` (callback fires once, stale path only), `expect(servePack(stalePack, stillStale)).toBeNull()` (fail-closed null, not a throw, when re-grounding leaves staleness), `expect(servePack(fresh, reground2)).toBe(fresh)` (reference-identical passthrough), `expect(reground2).not.toHaveBeenCalled()` (callback skipped entirely for fresh packs). None of this is recoverable from the bare name `servePack` or its argument shapes; the reference-identity requirement in particular would punish a naive `{...p}` clone implementation. Every clause is an executed assertion in the shown code, not a comment's claim about elsewhere. Counts (\"once\", \"never\") are the literal point of the `vi.fn` call-count assertions and are directly verified.\n\nThe servePack contract survives refutation best: it is non-obvious, entirely checkable in these bytes, and materially changes how one implements or calls the function.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"servePack invokes the re-grounding callback exactly once and only for stale packs, returns null (never serves) if the re-grounded pack is still stale, and returns a fresh pack as the identical object without ever calling the callback.\"}\n```"