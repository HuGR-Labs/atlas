"STEP 1 — Reasoning (scratch):\n\nReading the bytes closely, several candidate facts surface:\n\n1. *runLoop's stop-order*: fixpoint is checked before marginal-value, so a zero-revision round with sub-ε value stops as `'fixpoint'`. True and derivable, but fairly close to what the doc-comment already states explicitly — borderline restatement.\n\n2. *reviewRound's accounting*: it computes `calls = Math.min(cands.length, budgetLeft)` **before** extraction and passes the **entire untruncated** `cands` array to `extract` every round — the charge is a static estimate, not metered spend, and budget-truncation is delegated to the `GenesisBudget` carrier. Derivable purely from the code body. Strong candidate.\n\n3. *expandRound's fabricated ranking*: `newCands` are built via `neighbours.map((site, i) => ({ site, signals: ZERO_SIGNALS, ppr: 1 / (i + 1), rank: i }))`, where `ZERO_SIGNALS` is a literal defined in this same unit (zeros and empty arrays). So the re-extraction consumes candidates whose PPR score and signals are **synthetic positional placeholders**, not measured ranking data — despite the surrounding design language about ranked frontiers. Fully checkable in these bytes; nothing rests on out-of-file claims. Nothing is mislabelled; no counts asserted. A competent engineer reading only the name/signature (`expandRound(m, seedSites): RoundFn`) would assume real PPR/signal data threads through; discovering it doesn't changes what they'd do when touching either seam.\n\nRefutation passes: candidate 3 survives cleanly — it rests on the unit's own literal and control flow, not on any comment's assertion about another file, and it's not a restatement of name/signature/types. Candidates 1 and 2 are weaker (2 is good but 3 is more behavior-changing and less guessable). Going with 3.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"expandRound fabricates the Candidates it feeds back into propose→verify: each related site gets empty placeholder signals (the ZERO_SIGNALS literal) and a synthetic score of ppr = 1/(i + 1) with rank = position, so re-extraction runs on invented ranking data rather than any measured PPR or signals.\"}\n```"