"**STEP 1 — scratch reasoning**\n\nReading the bytes: nearly every substantive assertion in the leading HTML comment points *outside* this unit — `UnitDepsApi.candidatesFor`, `@atlas/index`, `unitScopeOf`/`llm.ts`, `parseDependencyClaim`, ADR numbers, issue numbers (#95, #201/#202), the paper citation. All of those are claims about other code and are discarded per the rubric; they do not re-derive at these bytes.\n\nWhat remains checkable is the operative prompt body itself. Candidate facts drafted and stress-tested:\n\n- *\"Comments are stripped before reaching the model\"* — asserted only in the comment about external preprocessing; not checkable here. **Discarded.**\n- *\"An out-of-list name is treated as unparseable and abstains\"* — the enforcement half lives in `parseDependencyClaim` (comment-only). Only the *instruction* to copy verbatim is byte-present. **Discarded as written.**\n- *\"Exactly four placeholders\"* — countable, but it's a structural restatement with no behavioral consequence. **Discarded.**\n- Surviving candidate: the output contract itself is fully stated in the body bytes — \"pick the ONE dependency…\", \"MUST be copied VERBATIM from the candidate list above\", \"If the candidate list is empty, or none of its entries is a non-obvious dependency… output the single token `NO-FACT`\", and \"Output EITHER exactly one line `DEPENDS-ON: <name>` … OR the single token `NO-FACT`\". This caps throughput at one dependency fact per run, makes expectedness-from-name-and-role the explicit acceptance filter, and fixes the sole alternative token. None of that follows from the filename alone, it rests on no external reference, and it involves no counted enumeration beyond the explicitly stated \"one\"/\"single\". Scoped strictly to what the template *instructs* (not to downstream parsing guarantees, which live elsewhere).\n\nSurvives refutation. Emitting.\n\n**STEP 2 — result**\n\n```atlas-fact\n{\"claim\": \"The template caps each run at a single dependency pick — one 'DEPENDS-ON: <name>' line whose name must be copied verbatim from the supplied candidate list — and allows only the token 'NO-FACT' as the alternative whenever the list is empty or every candidate would already be obvious from the unit's name and role.\"}\n```"