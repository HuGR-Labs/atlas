"**STEP 1 — scratch reasoning**\n\nCandidates drafted from close reading:\n\n- *\"Once the cap bites, no backfill occurs — smaller later candidates also go to tail.\"* Refuted as a pick: the `fill()` docstring itself states the stop-on-cap behavior (\"at which point the cap WINS (stop)\"), so an engineer reading the signature/docs already gets this.\n- *\"mergedPack stamps the result with axes[0].axisHash, not a derived hash of the union.\"* Survives — plainly visible in the `assemble(name, axes[0]!.axisHash, axes, PACK_CAP)` call, surprising, actionable (identity/caching implications). Kept as runner-up.\n- *\"Pack staleness ignores tail-dropped stale candidates.\"* Survives (`emitted.some(...)` only), but lower impact.\n- **Strongest:** the loud invariant repeated in the file header and the `fill()` docstring — \"every T0 invariant in full,\" cap biting only in the T1 band — is *not* what the loop enforces. The `for (const c of [...t0, ...t1])` loop applies the same `used + c.tokenEstimate <= cap` test to every candidate with no tier special-case: T0 items are sorted by `compare` (hits/ppr), not by size, so if T0's own cumulative `tokenEstimate` exceeds `cap` (or a single fat T0 row doesn't fit the remainder), that T0 candidate flips `capped = true` and lands in the `tail` together with all T1 rows. This is checkable purely from the loop body and literals (`PACK_CAP = 2000`, index-supplied unbounded `tokenEstimate`) — no reliance on any comment's claim about unseen code. An engineer trusting \"T0 in full\" would behave differently than the bytes dictate.\n\nSurvivor: the T0-vs-cap fact.\n\n**STEP 2 — result**\n\n```atlas-fact\n{\"claim\": \"The fill() loop enforces the token cap identically on T0 and T1 candidates with no tier special-case, so when T0's own cumulative tokenEstimate exceeds the cap, the overflowing T0 candidates are pushed onto the pull-reachable tail alongside the truncated T1 rows rather than being guaranteed full inclusion.\"}\n```"