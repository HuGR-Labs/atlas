"STEP 1 — scratch reasoning:\n\nReading the bytes: the executable content of a CODEOWNERS file is its rule lines. Stripping comments, I count the actual rules:\n\n1. `/.atlas/policy.json @HuGR-Labs/atlas-admins`\n2. `/CODEOWNERS @HuGR-Labs/atlas-admins`\n3. `/.github/workflows/ @HuGR-Labs/atlas-admins`\n4. `/harness/gates/ @HuGR-Labs/atlas-admins`\n5. `/harness/lib/ @HuGR-Labs/atlas-admins`\n\nFive rules, one team. Re-checked line by line against the raw bytes — five confirmed.\n\nCandidate A: \"All rules assign one team.\" Refuted as trivial — it's a plain summary of what the file visibly does; no engineer action changes.\n\nCandidate B: Comments' claims about external things — branch protection settings, `gh api` endpoint behavior, `lexing.mjs`, `layer-guard.mjs`, past PRs. These assert facts about code/settings NOT in these bytes. Discarded per the rules.\n\nCandidate C: The prose block says \"So four paths are owned\" and enumerates exactly 1–4 (policy, this file, workflows, gate scripts). But the rules define five owned paths. Checking item by item: policy ✓ (item 1), CODEOWNERS ✓ (item 2), workflows ✓ (item 3), gates ✓ (item 4, \"gate scripts\"), **lib ✗ — absent from the enumeration**. Cross-check: the later prose itself splits enforcement into \"all THREE halves: the workflow …, the script …, and the library,\" confirming lib is counted as its own thing — so \"four paths\" cannot absorb lib into item 4. The discrepancy lives entirely inside this unit: both the enumerated list and the five rule lines are bytes I can see and have recounted. It is not a stale claim about elsewhere; it is an internal contradiction, checkable here. It would change what a competent engineer does (trust the rules over the prose; fix the comment).\n\nSurvives. STEP 2:\n\n```atlas-fact\n{\"claim\": \"The file's prose states that 'four paths are owned,' yet the rules below define five owned paths — /harness/lib/ carries an owner line but is missing from that enumerated list.\"}\n```"