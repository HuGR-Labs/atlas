"STEP 1 — Reasoning (scratch):\n\nReading the bytes: the test bodies contain literal sorted-array expectations inside `toEqual([...])` calls — these are code, not comment claims. I verified item-by-item: the surface array literal is `['atlas-emit', 'atlas-init', 'atlas-link', 'atlas-query', 'atlas-reconcile']` (5 items, matching `.toBe(5)`), `WRITE_PATHS` must sort to `['atlas-emit', 'atlas-link']` (matching `.toBe(2)`), and there is a code-level `expect(GOVERNANCE_SURFACE).not.toContain('atlas-delete')`. Refutation pass: none of this depends on another file, a PR number, or past behavior — the arrays and negation live in these bytes; the comments merely echo what the code asserts, and they agree. It is not derivable from the file's name or the imported symbols' signatures alone, and it is actionable: anyone adding a tool or write door to the layer breaks these pins. Other candidates examined and discarded: the `catch { return true }` in `ungroundedRows` (true but low-impact, local helper nuance); the projection-handle no-write/set/put teeth (close to restating the scenario title); the 15c count of 1 (trivially re-derived from two visible mutations); the JSON.stringify purity comparison (restates the describe block).\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"This suite hard-pins the layer's exported sets in code: GOVERNANCE_SURFACE must sort to exactly ['atlas-emit','atlas-init','atlas-link','atlas-query','atlas-reconcile'] and WRITE_PATHS to exactly ['atlas-emit','atlas-link'], with an explicit assertion that 'atlas-delete' is absent, so registering any sixth tool or third write path fails these tests.\"}\n```"