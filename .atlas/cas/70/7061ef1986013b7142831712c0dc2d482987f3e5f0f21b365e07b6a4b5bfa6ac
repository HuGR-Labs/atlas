"STEP 1 — Reasoning scratch:\n\nCandidate drafts considered and refuted/survived:\n\n1. *Only `ts` and `py` rows exist in `REAL_INDEXER`.* — Survives verification (exactly two keys, listed), but it is the literal contents of a table sitting in plain sight; reads as a plain-data summary rather than something the signature hides.\n2. *The `existsSync` guard in `readScipOrEmpty` is semantically redundant with the surrounding try/catch.* — True and derivable (missing path ⇒ `statSync` ENOENT ⇒ caught ⇒ same `{documents:[]}`), but low stakes; wouldn't materially change what an engineer does.\n3. *Throws are never cached.* — Derivable (`set` is reached only after `deserializeSCIP` returns), but partially advertised by the name \"Cached\" and the doc block; weaker than candidate 4.\n4. **Staleness predicate is (mtimeMs, size) equality only.** Refutation pass: is it in the bytes? Yes — `cached !== undefined && cached.mtimeMs === stat.mtimeMs && cached.size === stat.size` gates `return cached.raw`, and the only `set` stores exactly `{mtimeMs, size, raw}`; no byte hashing/comparison appears anywhere in the unit, and the Map is module-private so no external eviction/mutation exists. Does it rest on a comment's claim about elsewhere? No — I am not using the APFS/NFS/#211 narrative, only the visible predicate and its mechanical consequence (same-tick, same-length rewrite ⇒ predicate still true ⇒ stale `raw` returned). Not a name/signature restatement: `decodeScipCached(scipPath)` reveals nothing about invalidation policy. It changes what an engineer does: anyone introducing a mid-process re-read of a mutated dump cannot trust this cache. SURVIVES.\n\nEmitting the survivor.\n\n```atlas-fact\n{\"claim\": \"decodeScipCached validates its memo entry solely by comparing the path's current stat mtimeMs and size against the values stored at the last successful decode — the bytes themselves are never hashed or compared for validation — so a rewrite of the dump that lands on the same mtime tick with an identical byte length will be served the previous decode from the cache.\"}\n```"