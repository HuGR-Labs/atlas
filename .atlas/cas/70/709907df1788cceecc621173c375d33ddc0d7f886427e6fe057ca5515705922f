"Reading the bytes: this unit is a self-contained HTML design draft. Scanning for what survives refutation:\n\n- Candidate \"Build is the only territory-only class\" — REFUTED: Discovery is also territory-only (both Definição and Orquestração are project-only; three classes carry both chips).\n- Candidate \"Revisão has four owners\" — verified by scanning all seven owner cells (walt, archie, orquestrador, charlie·patty, lucy·bobby·billy·frankie, jimmy, rosie), but it barely changes what anyone does.\n- Candidate about the scope-chip colors colliding with the kind column colors (`--seal` for both `.sc.proj` and the Knowledge header; `--accent` for both `.sc.terr` and the Memory header) — verifiable in CSS, but low decision impact.\n- Candidate about ADR-0012 / \"lei do atlas\" parentheticals — DISCARDED: those assert rules about artifacts outside these bytes.\n- Strongest survivor: the gate section explicitly defines admission as passing exactly two doors (grounding truth, storage-harm secrets/PII), states \"Utilidade não é porta,\" marks non-obvious as \"Pontua, nunca barra,\" and the verdict line admits true-but-obvious facts with a low score, ranked down at retrieval. This contradicts the natural reading of the page's own \"no-junk bar\" title and changes implementation (no usefulness gate at write time). All components of this claim are literals inside this file; the count of two doors was checked against the two `.door` divs.\n\n```atlas-fact\n{\"claim\": \"Despite the 'no-junk bar' framing, this spec makes obviosity explicitly a non-gate: the only rejection conditions are failed grounding (source@sha) and store-harmful secrets/PII, while true-but-obvious facts are admitted with a low stored score and lose only at retrieval-time ranking — so an implementer must not add a usefulness/obviosity check at write time.\"}\n```"