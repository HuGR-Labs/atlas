"STEP 1 — Reasoning (scratch):\n\nReading closely, this is a *methodology* doc whose name (\"staleness A2 methodology\") tells you nothing about its most consequential content. Draft candidates:\n\n1. *\"freshness() has zero production callers\"* — discarded: a claim about `packages/grounding/src/freshness.ts` / `compose.ts`, not visible in these bytes.\n2. *\"P4 (NFD→NFC) is the strongest case\"* — partially leans on Unicode semantics not in the bytes; weaker actionability.\n3. *\"true_stale_caught=6/6 is a tautology and must never be published alone; report 4/4 + NOT SUPPORTED instead\"* — survives refutation: the doc itself argues `bytes changed ⇒ hash changed ⇒ DRIFTED` holds for any hash function (every invalidating entry edits bytes inside the hashed extent), and its own table shows an unconditional-DRIFTED constant checker tying the real oracle at 6/6 while scoring 0/4 on preserving cases. Internal counts check out: 4 preserving + 6 invalidating = 10 entries; the always-DRIFTED row reads 6/6 stale, 0/4 fresh. This is not a restatement of the filename, requires no outside library knowledge, and directly changes what a consumer does (how they report/quote the axis's numbers).\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"This doc mandates that A2 never be reported as a single staleness-detection score: true_stale_caught=6/6 is declared a tautology (any unconditional-DRIFTED checker scores identically, since every invalidating case edits bytes inside the hashed extent), so the only reportable claims are non-touching precision 4/4 and semantic cross-file staleness NOT SUPPORTED.\"}\n```"