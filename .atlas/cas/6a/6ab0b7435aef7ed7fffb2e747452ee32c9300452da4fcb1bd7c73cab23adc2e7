"STEP 1 — Scratch reasoning:\n\nCandidates considered against the bytes:\n\n1. *\"The fail-closed leg of `guard.ts` is unguarded / has zero production callers\"* — rejected. These are assertions about `packages/tools/src/guard.ts` and repo-wide callers, none of which are in the shown bytes. Even if accurate, they're facts about other code, not re-derivable from this document's own text.\n2. *\"1717 tests pass / 27% mutation survival / grep counts are sound\"* — all external measurements recorded in the doc's tables; not checkable within the unit, and mostly visible on first read anyway.\n3. *\"The doc contains a correction dated 2026-08-02\"* — true and in-bounds, but weak: it's announced in a huge banner heading; a skimmer sees it instantly.\n4. **Internal contradiction between §6 and §2.1's correction** — survives scrutiny. Reading both passages verbatim in the unit:\n   - §6 (Blind spots) states: \"**§2.1 is now attributed** (2x2 clean-vs-mutant on the two files: 0/2 vs 2/2). What remains unmeasured is whether any *semantic* e2e assertion could be made to catch it.\"\n   - §2.1's correction block explicitly retracts that very experiment: \"THIS ATTRIBUTION IS WRONG… The paragraph below originally read *'So the mutant **is** causal and reproducible (2/2 vs 0/2), not load.'* **It is not causal.** … The 2×2 above was HOST LOAD,\" adding that a rebuilt mutant produced 13/13 green three times and a stderr probe got **0 hits** across the story.\n   \n   Both texts are present in these bytes; the contradiction is between them, requiring no trust in any comment about unseen code. Refutation attempts: could §6 refer to a different experiment? No — identical numbers (0/2 vs 2/2) on the same two files. Could the retention be deliberate? Nothing in the bytes marks §6 as stale or superseded. The mismatch survives.\n   \n   Why it changes behavior: a reviewer planning follow-up work from §6 alone would treat the timing-attribution as settled science and hunt for a semantic e2e detector, when the document itself later invalidated that attribution and relocated the open question entirely (reference model; shipped store unmeasured).\n\nSTEP 2 — Emitting the surviving fact:\n\n```atlas-fact\n{\"claim\": \"Section 6's blind-spots list still presents the §2.1 result as settled ('now attributed', a 2×2 of 0/2 versus 2/2), even though section 2.1's own correction block explicitly retracts that same 2×2 as host-load coincidence after a rebuilt mutant ran green three times and a probe showed the guarded function is never reached.\"}\n```"