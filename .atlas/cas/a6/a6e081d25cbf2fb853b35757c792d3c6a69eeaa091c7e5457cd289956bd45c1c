"Looking at the bytes: the interesting surfaces are `shouldEscalate`/`decideMechanisms`, `defaultKind`, `plan` scoping, `makeQueryDb`, and `makeBudget`.\n\nDraft candidates and refutation passes:\n\n- **Conjunction gate** (`highValue ∧ uncertain`) for escalation — pinned by SCN-GEN-13b-2 assertions, but the `it` title itself spells out \"high-value ∧ uncertain\"; largely pre-stated by the embedded name. Weak novelty.\n- **Query DB memoization** (`builds === 1` after 30 `get()`s) — real, but the title \"built once across 30 checks\" already declares it.\n- **Prefix scoping in `plan(frontier, 'billing/')`** — the exact retention (`['billing/charge','billing/reverse']`) and triple negative assertions are byte-verifiable, but the mechanism (prefix vs. contains) is underdetermined by the fixtures; and the title announces the scoping outcome anyway.\n- **`defaultKind`: checkable T2 stays advisory while checkable T1 becomes predicate** — the body's third assertion goes beyond the title (which only names A2/B2). Viable, but requires hedging since the T0-checkable case is never exercised for `defaultKind`.\n- **`makeBudget` escalation is side-effect-free w.r.t. the cost report** — in the first `makeBudget` test, a full T0 escalation fires (mechanisms include `'refuter'`), yet the very next line asserts `expect(api.report()).toEqual([])`. Nothing in any `describe`/`it` title hints at this; the `{ signal, cost }` wiring in the signature actively invites the wrong assumption that escalation auto-logs costs. Cross-checked against SCN-GEN-13k-2, where report entries appear *only* after manual `cost.record` calls — consistent, no contradiction. Rests solely on assertions present in these bytes, no comment-about-elsewhere dependence, no risky count (a single exact `[]` equality, directly read).\n\nThe last candidate survives cleanly and is the one most likely to change an integrator's behavior (they must account for costs themselves; `escalate()` contributes nothing to the breakdown).\n\n```atlas-fact\n{\"claim\": \"In the makeBudget API, an escalate() decision writes nothing to the cost ledger — after escalating a T0 site whose mechanisms include 'refuter', api.report() still returns an empty array, so per-stage costs reach the report only via explicit cost.record calls.\"}\n```"