"STEP 1 — reasoning through candidates:\n\n- *Candidate A:* \"`recall` returns [] for an unqualified query.\" Refutation pass: the early return `if (f.owner === undefined && ...) return []` sits directly in these bytes, alongside `asRecallFilter` producing an empty filter for non-object/null queries. Truth rests on the code's control flow, not a comment about another file. The four fields in the guard match exactly the four optional fields declared on `RecallFilter` (owner, kind, taskId, prId) — I listed them item-by-item and they agree. Not derivable from the name/signature alone (`recall(store, query: unknown): readonly MemoryRecord[]` gives no hint that a bare query yields silence rather than the full store). Behavior-changing: someone calling `recall(store, {})` to dump consultable memory gets nothing. **Survives.**\n\n- *Candidate B:* \"`compose` always emits `budgets: []` and passes surfaces through verbatim.\" Also code-visible (`retrieval: surfaces, budgets: []`). Valid, but largely anticipated by the interface's `[PINNED]` annotation and the compose docblock; weaker surprise.\n\n- *Candidate C:* \"`makeInject` binds `recall` to the construction-time store while the other three methods forward the caller's store arg.\" Visible (`recall: (query) => recall(store, query)` vs `(s, seat) => ...`), but partially inferable from the frozen interface where `recall` alone takes no store parameter, and the docblock names it.\n\n- *Candidate D:* \"taskId/prId filters match across kinds\" (no kind gate in `matchesRecall`). True in the bytes, but marginal impact and easy to misread as intended-vs-accidental.\n\nCandidate A survives cleanly: re-derivable from this unit's declarations and control flow, exact-field conjunction verified, not a restatement of name/signature, and it changes how one must call the sole consultable-memory read path.\n\nSTEP 2 — emit:\n\n```atlas-fact\n{\"claim\": \"A recall query lacking every recognized selector (owner, kind, taskId, prId) short-circuits to an empty array rather than returning the store, so consultable memory surfaces only under an explicit selector.\"}\n```"