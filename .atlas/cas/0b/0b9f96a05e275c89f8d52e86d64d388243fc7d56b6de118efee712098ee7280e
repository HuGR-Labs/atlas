"STEP 1 — Scratch reasoning:\n\n- Candidate 1: \"Ties break by rule-id ascending.\" Refuted on novelty grounds: the `it()` title itself states \"the R6/R7 tie breaks by rule-id asc\" — already knowable from the test's name.\n- Candidate 2: \"Near-zero eviction happens despite free slots.\" The `it()` title for SCN-MEM-7c-1 also states this outright, and proving \"free slots\" strictly would require pinning `RULES_SLAB_SLOTS`, whose value never appears in these bytes. Discarded.\n- Candidate 3: \"attemptDelete throws rather than returning a receipt.\" Partially disclosed by the title (\"a delete is rejected\") and low-impact; the throw-vs-result distinction is thin.\n- Candidate 4: Duplicate-event counting semantics. Reading the fixtures and SCN-MEM-7a-1's code: `hitsAt('R5', 8, 4)` constructs FOUR governing events for the same rule at the SAME wave, and the assertion demands `citedHitCount('R5', prior)` === 4. Neither the describe/it titles nor any inline comment states that repeated same-wave citations accumulate individually — the titles discuss only the mention-vs-cited distinction. Yet this is executable-code-derivable (event construction + literal `toBe(4)`), it is qualitative (no risky enumeration), and it changes implementation: an aggregator that de-duplicates by (ruleId, wave) or tracks \"last cited wave\" would fail. Survives refutation.\n\nSTEP 2 — Emit the surviving fact:\n\n```atlas-fact\n{\"claim\": \"The suite treats the ledger as a per-event log with no de-duplication: hitsAt emits several governing events for the same rule at the same wave, and citedHitCount must return the full event count (four same-wave citations register as four hits), so implementations may not collapse repeats by (ruleId, wave).\"}\n```"