"STEP 1 — REASON FREELY\n\nReading the bytes closely:\n\n- Candidate A: \"`isGrounded` rejects empty receipts and `driftDetect` reads them DRIFTED.\" Refuted — those functions live in sealed sibling files (`drift.ts`) not shown; that truth rests entirely on comments' claims about code I cannot see here. Discard.\n- Candidate B: \"`findByKey` refuses a node whose `subtreeHash` equals its own `key`.\" Survives as locally checkable (`String(n.subtreeHash) === n.key ? undefined : ...`), but it is somewhat restated by its own adjacent prose and is less action-shifting than the control-flow fact below.\n- Candidate C: The control flow of `ground()` itself. The file-header comment says an unresolvable citation \"is DROPPED\", and the docstring frames the amendment — but the CODE is unambiguous: `if (subtreeHash === undefined) return { entries: [] };`. There is no `continue`; the very first unresolvable citation aborts the whole call, and because the early return constructs a *fresh* `{ entries: [] }` rather than returning the local `entries`, every entry already accumulated from earlier successfully-resolved citations is thrown away too. From name + signature (`ground(node, src): Grounding`) nothing reveals all-or-nothing semantics versus per-entry filtering — indeed the header's \"DROPPED\" wording suggests the opposite. An engineer expecting partial receipts (e.g., writing tests asserting surviving entries alongside a dangling one) would be wrong.\n- Refutation pass on C: Is it derivable from these bytes? Yes — pure control flow, no external module needed. Does its truth rest on a comment about elsewhere? No — I state only what the code does locally (early return of a new empty list), not why downstream gates care. Exact counts/exhaustive enumerations? None asserted. More than a name/signature restatement? Yes — the discard-accumulated-work detail is invisible in the signature.\n\nC survives.\n\nSTEP 2 — EMIT THE RESULT\n\n```atlas-fact\n{\"claim\": \"If any single citation's qualifiedPath fails to resolve, ground() immediately returns a brand-new { entries: [] }, discarding every entry already accumulated from earlier successfully-resolved citations, rather than dropping or skipping just the failed citation.\"}\n```"