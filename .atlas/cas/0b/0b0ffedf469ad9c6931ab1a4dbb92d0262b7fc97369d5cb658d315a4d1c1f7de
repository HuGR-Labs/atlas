"STEP 1 — scratch reasoning (not persisted):\n\nReading the bytes: every `GroundedFact` in the file comes from `factAt`, whose object literal hard-codes `freshness: 'DRIFTED'` for all callers — the five `m*` facts and the four `s*` facts alike. Yet the tests assert a 5/4 mechanical/semantic split with different outcomes (exit 0 vs exit 2). Since the freshness field is identical across all nine inputs, it cannot be the discriminator; the only per-fact variation that tracks the split is the stored-sha/new-sha pairing consulted through `reHashAt`'s table (hits `st-0n` for mechanical, shared miss placeholder `st-X` for semantic).\n\nRefutation pass:\n- Derivable from these bytes? Yes — `factAt` is the sole constructor of facts here (verified: m1–m5 and s1–s4 all call it; no inline `GroundedFact` literals exist), and the split expectations are in the `it` bodies. No reliance on unseen library internals.\n- Resting on a comment's claim about elsewhere? No — the header comment's references to docs/goldens files are ignored; my claim concerns only the fixture code itself.\n- Exact-count/exhaustive risk? I enumerated all nine constructions; phrased qualitatively (\"cannot derive from freshness\"), avoiding fragile counts as the point.\n- Restatement of name/signature/plain summary? No — the test names mention drift and exit codes, but nothing telegraphs that freshness is uniform-and-therefore-non-discriminating; that requires synthesizing `factAt`'s body against the asserted split.\n\nSurvives. Emitting.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"All nine fixture facts are stamped with the identical freshness value 'DRIFTED' by the shared factAt helper, so the mechanical-versus-semantic split the tests assert cannot derive from the freshness field — it can only follow from the per-fact hash data supplied through the reDerives lookup table.\"}\n```"