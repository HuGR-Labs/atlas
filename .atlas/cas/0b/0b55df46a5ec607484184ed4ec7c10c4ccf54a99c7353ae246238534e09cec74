"Reading the bytes: the file pins four behaviors — init's empty-genesis skeleton, the T0-flagging classifier, the stage/ratify split, and the confidence fast-path. Most striking claims are already telegraphed by the `it` names (\"requires billy\", \"heuristics only flag\", \"auto-accepts\"), so those are out. Scratching candidates:\n\n- *\"T0 needs the 'billy' token\"* — refuted as a candidate: literally the test's title; an engineer knows this from the name.\n- *\"'payments/' is flagged as a T0 candidate\"* — real and byte-only (`expect(classify(territory('payments/')).t0Candidate).toBe(true)`), but the sibling fact below is more directly actionable about the API's own contract.\n- *\"stage() hands back the same object\"* — `expect(staged.node).toBe(c)` uses vitest's `toBe`, i.e. reference identity: staging snapshots nothing. Nothing in the test name (\"the explorer writes only staged candidates\") reveals aliasing semantics; a reasonable engineer would assume a copy or wrapper. Mutating the candidate after staging silently mutates the staged record. Refutation pass: not library lore (plain `===` semantics), not comment-derived (the adjacent comment speaks to the *absence* of a commit key, which the `'committed' in ...` code assertion independently pins), no counts or enumerations involved, and it is not a restatement of any signature shown. Survives.\n- *\"'regions' appears in the allowed skeleton-key set but in no fixture\"* — true in the bytes, but too weak (\"permitted, not required\") to change what anyone does.\n\nSurvivor: the by-reference staging contract.\n\n```atlas-fact\n{\"claim\": \"stage() stores the candidate by reference — the test pins staged.node to be the identical object passed in (vitest toBe), not a copy — and the returned staging record carries no 'committed' key, so post-stage mutations of the original candidate are visible in staging.\"}\n```"