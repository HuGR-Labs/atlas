"STEP 1 — REASONING (scratch):\n\nReading the bytes closely:\n\n- Candidate A: \"Direction 2 only requires a nonzero exit, never pinning exit 1 despite the header narrative claiming exit 1.\" Check: the assertions are `expect(r.exitCode).not.toBe(0)`, no `'status: ok'`, and the reason-line discriminants. True of the code, and the discrepancy with the comment is visible in-file. Viable but relatively weak — it's about assertion laxity, easily noticed on a second read.\n\n- Candidate B: \"`later` — used as the clean-gate baseline in DIRECTION 3 — is the sha of the commit that itself introduces the blocking semantic flip.\" Trace it: in `beforeAll`, `later = repo.commit({ 'src/foo.ts': 'export const foo = 99;\\n// semantic change\\n' })` — that is exactly the flip that makes DIRECTION 1 exit 2 against `genesis`. Yet DIRECTION 3 runs `runAtlas(..., ['reconcile', later])` and asserts `exitCode === 0` and `status: ok`. Refutation attempts: Is this derivable from these bytes? Yes — both the assignment and the assertions are in-file; no reliance on unseen code or on the header comment's framing of `later` as merely \"a real later base\". Does it rest on a comment's claim about elsewhere? No. Exact count/enumeration? None asserted. Restatement of name/signature? No — the file name and describe title say nothing about the baseline sha's provenance. The behavioral entailment (drift *before* the named base does not block the gate; the flip-introducing commit is itself a legitimate green baseline) follows directly from the in-file contrast: exit 2 against `genesis`, exit 0 against `later`, with the flip lying between them. This survives, and it would change what an engineer does: anyone touching the fixture ordering or `reconcile`'s base semantics must know `later` plays a deliberate dual role, and that \"green\" here means \"nothing drifted since the base,\" not \"no drift exists.\"\n\n- Candidates C–I (env save/restore hygiene, `reasonOf` fallback string, mid-test repo mutation, `scopedPolicy('src')`) — all plainly visible boilerplate or minor; discarded.\n\nSurvivor: Candidate B.\n\nSTEP 2 — EMIT:\n\n```atlas-fact\n{\"claim\": \"The `later` rev asserted clean (exit 0, `status: ok`) in DIRECTION 3 is the same sha captured in `beforeAll` from the commit introducing the blocking `src/foo.ts` semantic flip (foo 1→99), so this test pins that reconciling against the flip-introducing commit itself yields an honest green gate — drift predating the named base is not what the gate reports on.\"}\n```"