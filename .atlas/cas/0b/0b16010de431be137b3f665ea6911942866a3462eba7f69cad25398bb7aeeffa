"STEP 1 — scratch reasoning:\n\nReading the verification path closely: `verify()` spawns the CLI with `spawnSync(process.execPath, [CLI, ...inv.argv], { cwd: repo, encoding: 'utf8', env })` — no `stdio` override — and then the *only* output channel ever touched is `(res.stdout ?? '').split('\\n')`. `res.stderr` appears nowhere in the file; `res.status` is used solely for the optional `# exit N` check. So despite the header rhetoric about blocks being \"diffed line by line,\" the comparison is stdout-only.\n\nRefutation attempts:\n- Is it derivable from these bytes? Yes — the sole read of `res.stdout` and total absence of `res.stderr` in the shown code settles it; no external knowledge needed.\n- Does it rest on a comment's claim about unseen code? No — it rests on what the code does and does not reference, right here. (Note the header's \"every line … match\" claim doesn't contradict this; it simply never specifies the stream, so there's no comment/code conflict to worry about.)\n- Exact count or exhaustive list? No numbers asserted.\n- Restatement of name/signature? No — \"doc-transcript-guard\" and `verify(t)` reveal nothing about which pipe is compared; an engineer would plausibly assume both streams matter.\n\nIt survives, and it changes behavior: authors must ensure documented output is produced on stdout (or the diff reports `<end of output>`), and reviewers know stderr-only drift can never turn the gate red.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"The gate compares only the built CLI's stdout against the documented lines — the spawned process's stderr is never read anywhere in the script, so output the binary emits on stderr is invisible to every diff.\"}\n```"