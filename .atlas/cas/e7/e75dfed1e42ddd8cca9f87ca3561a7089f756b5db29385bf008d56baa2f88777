"```atlas-fact\n{\"claim\": \"These assertions pin a parser whose flag values are always strings and which never rejects malformed flags: a valued flag left without a value (`--at` before another flag, or a trailing bare `--at` at end of argv) folds to the literal string 'true' rather than erroring, and even a type-invalid unknown-flag value like `--depth=notanumber` still yields an ok result carrying the raw string 'notanumber'.\"}\n```"