"Looking at this test file, I'll draft candidates and try to refute them.\n\n**Draft candidates:**\n\n1. *\"Goldens are symbolic; no assertion depends on hash values\"* — While true (all assertions compare nodeKey/provenance strings, lengths, or run-to-run serialization equality), the header comment explicitly announces this, so an engineer reading the file already knows it. Weakened — discard.\n\n2. *\"All fixture events have seq 0\"* — Checkable (every `ev()` call omits `seq`), but marginal actionability. Hold.\n\n3. *\"The 14-f shuffle puts superseding events before their predecessors\"* — True (reversing `[...A_EVENTS, ...B_DELTA]` yields hq19-decay, ttl-v2, … before hq19-v1, ttl-v1), but niche.\n\n4. **Supersede addressing**: Examining `ev()`: `id` is minted as `asHash(\\`id-${contentHash}\\`)` (prefixed), while `contentHash: asHash(contentHash)` is unprefixed, and `supersedes: (o.supersedes ?? []).map(asHash)` hashes the given strings *exactly like a contentHash* — with no `id-` prefix. Checking both call sites that use `supersedes`: `ttl-v2` passes `['ttl-v1']` and `hq19-decay` passes `['hq19-v1']` — precisely the contentHash string arguments of the targeted events (`ev(K.ttl, 'ttl-v1', …)`, `ev(K.hq19, 'hq19-v1', …)`), **not** their id forms (`id-ttl-v1`). So within this fixture convention, supersession links are content-addressed. This rests on nothing outside these bytes — it's pure construction logic in `ev()` plus the literals at the call sites, and I've checked both occurrences. The `supersedes?: readonly string[]` signature alone gives no hint whether to pass ids or content hashes; passing the wrong form would mint a hash matching nothing in the fixture. This changes what an engineer does when extending the fixtures or tracing supersession resolution. Survives refutation.\n\n```atlas-fact\n{\"claim\": \"In the ev() fixture helper, supersedes entries are hashed directly with asHash (exactly like a contentHash, with no 'id-' prefix, unlike event ids which are minted as asHash(`id-${contentHash}`)), and the call sites pass the contentHash literals of the targeted events ('ttl-v1', 'hq19-v1'), so supersession links in these fixtures are content-addressed rather than id-addressed.\"}\n```"