"```atlas-fact\n{\"claim\": \"This suite pins that `atlas own` is total over empty scopes — both a path naming no indexed unit and a real indexed unit with nothing filed exit 0 with 'status: ok' and empty stderr — and distinguishes the two cases only by stdout wording ('names no unit in the code index' vs 'NO fact is filed under it yet', the latter still printing its 'contains' terrain lines and zero inv rows).\"}\n```"