"Looking at this unit, I need a fact grounded strictly in these bytes.\n\n**Drafting candidates:**\n\n1. *\"bareClone copies only commits, not PRs\"* — visible in the literal (loop over `commits`, no `prs` copy), but the test title \"fetches zero host-side PR data\" already conveys this. Weak novelty.\n\n2. *\"Forge requires test-instrumentation methods\"* — the typed literal shows `calls()` and `hostSidePRCount()` are part of the port contract, but that's close to summarizing the object literal.\n\n3. **The squash/cherry-pick pair:** The unit name and test titles advertise \"independent rewrite op: squash, cherry-pick\" — suggesting two distinct operations. Reading the actual calls: SCN-13b contains `const c4 = rewrite(c, 'SHA4'); // squash` and SCN-13d contains `const c4 = rewrite(c, 'SHA4'); // cherry-pick`. These are **identical invocations** — same function, same argument shape (source commit + new SHA string), and **no operation-kind parameter anywhere**. Exactly two `rewrite` call sites exist in the file; I checked both. All behavioral divergence between the scenarios comes from what is read afterward: trailer-carried `D2` survives onto SHA4 (13b), while note-carried `N2` orphans (13d).\n\n**Refutation pass on candidate 3:**\n- Derivable from these bytes? Yes — both call literals are present and identical; no reliance on `src/placement.js` internals (I do *not* claim rewrite lacks internal branching, only that these tests exercise none).\n- Comment reliance? The inline comments say \"squash mints a fresh SHA\" / \"cherry-pick mints a fresh SHA\" — my claim rests on the calls themselves, not on those assertions.\n- Count/exhaustiveness risk? \"Both\" spans exactly two call sites, verified individually. Phrased qualitatively.\n- Restatement of name/signature? No — the names actively imply distinct ops; the byte-level reality (identical calls, narrative-only distinction) corrects that natural misreading and is decision-relevant: a single rewrite behavior satisfies these gates, and nothing here pins op-specific code paths.\n\nIt survives.\n\n```atlas-fact\n{\"claim\": \"Although labelled as distinct operations, the squash and cherry-pick scenarios invoke rewrite identically as rewrite(c, 'SHA4') with no operation-kind argument, so all divergence between them comes from the later reads: the trailer-carried datum D2 survives onto the new SHA while the note-carried datum N2 orphans.\"}\n```"