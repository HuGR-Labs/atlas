"STEP 1 — scratch reasoning:\n\nCandidates drafted and attacked:\n\n- *\"The helper keeps a black-box boundary by only using libs for input placement\"* — rests entirely on the header comment's assertions about boundaries and other files; not code-checkable here. Discard.\n- *\"`atlas mine` today stages 200 candidates\"* — past-tense/amended commentary about another file (`mine.ts`) and a measured count I cannot recount in these bytes. Discard.\n- *\"Bytes and row are ordered so CAS durability precedes publication\"* — the ordering claim leans on \"the protocol\" outside this unit; what the code shows is only that `put` and `next` ride one `commitStaging` call. Too dependent on invisible protocol semantics. Discard.\n- *\"The CAS lives at `<repo>/.atlas/cas`\"* — true (the `join` literal is right there) and non-obvious from the signature, but low impact on what an engineer does.\n- *\"`family` is hardcoded to `'advisory'` even when the fact's kind branch admits `'predicate'`\"* — visible in bytes, but whether family↔kind are meant to correspond is not decidable here; risky semantic leap. Discard.\n- **Survivor:** the spread `const f = { ...fact, scope: MINED_SCOPE, tier: MINED_TIER }` re-stamps exactly those two fields, and every downstream artifact derives from `f` or the imported constants: `id(f ...)` (hash), `put: [f]` (bytes), `req.scope/req.tier` (row literals). Checking item by item, the original `fact` object is referenced nowhere except inside that spread — so the caller's `scope` and `tier` are provably inert inputs. This is verified by the code literals themselves, not by trusting the docstring's prose; it is not an exhaustive-enumeration claim beyond the two fields I can see overwritten; and it goes well beyond the name/signature (an engineer would reasonably assume a `GroundedFact` param's fields flow through). It changes behavior: nobody can use this helper to stage a differently-scoped candidate, and tests must not assert scope passthrough. Refutation attempt fails — no code path reaches back to the un-stamped original.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"Although stageCandidate accepts a GroundedFact, the caller's own scope and tier values are entirely inert: the spread re-stamps both fields with the imported MINED_SCOPE/MINED_TIER before hashing and writing, and the original fact object is used nowhere else, so those two argument fields affect neither the stored CAS bytes nor the staged row.\"}\n```"