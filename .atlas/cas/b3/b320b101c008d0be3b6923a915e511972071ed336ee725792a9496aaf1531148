"STEP 1 — scratch reasoning (not persisted):\n\nScanning candidates:\n\n1. *\"Refusal causes exit code 1\"* — restates `foldVerdict`'s visible `failed` expression; an engineer reading the signature + body sees it directly. Weak.\n\n2. *\"Ledger precedence vs fallback in `seededCount`\"* — trace the code: `ledgerSeededIds` guards only `if (r.coverage === undefined) return undefined`. If `coverage` IS present, it unconditionally builds and returns a set — possibly empty (zero `'seeded'` rows). Back in `seededCount`, the expression is `ledgerSeededIds(r)?.size ?? r.seeded.length`. A returned set is never nullish, so `0` (empty set size) short-circuits the `??` and shadows `r.seeded.length`. Therefore the legacy count applies **only** when `coverage` is wholly absent — not when the ledger is merely empty of `seeded` rows. Refutation attempts: (a) leaning on external library knowledge? No — pure TS nullish-coalescing semantics on these bytes. (b) resting on a comment's claim about elsewhere? No — the #237 comment happens to agree (\"only when `r.coverage` absent\"), but the fact re-derives from the guard plus `??` alone. (c) exact-count/enumeration claim? No. (d) restatement of name/signature? No — the signature `ReadonlySet<string> | undefined` doesn't reveal that *presence-with-zero-seeds* behaves like presence-with-N, not like absence. This changes behavior-relevant conclusions (e.g., a rerun pass can honestly report 0 while `report.seeded` holds earlier-pass rows, by design).\n\n3. *\"Cross-site fact-id dedup\"* — visible from the Set-building loop; less surprising, weaker than #2.\n\n4. *\"Head coverage line always printed\"* — visible from unconditional spread; near-restatement.\n\nCandidate 2 survives all refutation checks.\n\nSTEP 2 — emit:\n\n```atlas-fact\n{\"claim\": \"Because ledgerSeededIds returns a set (never undefined) whenever coverage exists, seededCount yields 0 for a ledger containing no 'seeded' rows even when report.seeded is nonempty — the r.seeded.length fallback fires only when coverage is entirely undefined.\"}\n```"