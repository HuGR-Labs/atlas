"```atlas-fact\n{\"claim\": \"These tests pin fail-closed grounding at the whole-fact level: a unit citing one resolvable symbol and one missing file must produce a zero-entry grounding that fails isGrounded and reads DRIFTED (the surviving citation is dropped too), while the identical unit over an index containing both symbols yields two entries that pass isGrounded and read FRESH.\"}\n```"