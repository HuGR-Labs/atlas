"STEP 1 — Reasoning scratch (discarded candidates included):\n\n- *\"`runAtlas` maps a signal-killed child to exit code 1\"* — true (`res.status ?? 1`) and in the bytes, but the interface doc already advertises it and the consequence is mild.\n- *\"`env` in `runAtlas` overlays rather than replaces the parent environment\"* — derivable from `{ ...childEnv(), ...env }`, but it's also called out verbatim in a comment, making it low-surprise.\n- *\"The provenance tripwire refuses committed stores\"* — rests entirely on the long comment's claims about `adapter-io/src/store-provenance.ts` and other files not present here. Discarded per the rules.\n- **Survivor**: the `writeFileSync(join(repoPath, '.gitignore'), '.atlas/*\\n!.atlas/policy.json\\n!.atlas/index.scip\\n')` literal. Nothing in the name `makeFixtureRepo` or the `FixtureSpec → FixtureRepo` signature hints that the fixture selectively un-tracks the product's own data directory. Verified against the literal line by line: one blanket exclude (`.atlas/*`), exactly two negations (`policy.json`, `index.scip`) — count checked, item by item. The consequence clause references only in-byte mechanics: `commit()` runs `git(repoPath, 'add', '-A')` in this same unit, and the ignore rules come from the string literal above it. No reliance on any comment's claims about other modules. A story author who drops extra artifacts under `.atlas/` (or expects `commit()` to snapshot an emitted store) will behave differently than they'd assume.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"makeFixtureRepo writes a root .gitignore into every fixture that excludes everything under .atlas/ from version control except exactly .atlas/policy.json and .atlas/index.scip, so any other file placed in .atlas/ stays untracked by the fixture's git add -A commits.\"}\n```"