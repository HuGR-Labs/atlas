"```atlas-fact\n{\"claim\": \"In this fixture the seedFact helper hardcodes `path: 'src/util.ts'` into every grounding entry regardless of the anchor passed, so the mechanical fact — whose anchor is the keep.ts node asserted to have qualifiedPath 'src/keep.ts' — still carries a grounding entry whose path field points at util.ts, and no assertion in the file ever reads that path field.\"}\n```"