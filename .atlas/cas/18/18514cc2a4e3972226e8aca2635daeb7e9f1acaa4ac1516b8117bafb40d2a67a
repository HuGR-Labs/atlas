"STEP 1 — Scratch reasoning (not persisted):\n\nCandidates considered and refuted/survived:\n\n- *\"The 'mine:' line only appears on empty passes\"* — the header asserts this, but it's a claim about `mine-render.ts`, which is not in these bytes. Discard as comment-about-elsewhere.\n- *\"In the seeded-run fallback, `sites` is copied from `budgetSpent` and `candidates` from `seeded`\"* — true and checkable, but the comments announce this synthesis explicitly and the ternary makes it plain; too close to restating what the code (and its comments) plainly do.\n- *\"`allAbstained` comes from an unanchored whole-text substring scan, independent of the parsed rows\"* — derivable, but the unanchored literal is immediately visible at `ALL_ABSTAINED.test(text)`; borderline plain reading.\n- **Survivor:** the `SITE_ROW` regex is `/^site: (\\{.*\\})$/gm` — flags `g`,`m` only, **no `s` (dotAll)**, so `.` cannot cross a newline. Consequence traced through the code: `matchAll` yields nothing for a `site:` row whose JSON object spans multiple lines; since only *matched* strings ever enter the loop body, such a row reaches neither the `rows.push` path nor the `catch`/shape-check `malformed.push` path. It is silently absent from the returned ledger — not flagged — despite `malformed` existing precisely to make broken ledger rows visible. This rests only on the regex literal, the flags, and the loop/catch control flow in this unit; no exact counts asserted; not derivable from the export names/signatures; and it changes what a maintainer or consumer does (absence of `malformed` ≠ every row parsed healthily).\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"A `site:` row whose JSON object spans multiple lines can never match the SITE_ROW regex (it lacks the dotAll flag, so `.` stops at the newline), and because only matched lines reach the JSON.parse/malformed handling, such a row is omitted from both `rows` and `malformed` — it silently vanishes from the parsed ledger instead of being reported as malformed.\"}\n```"