"STEP 1 (scratch): The unit is a goldens/spec document whose own bookkeeping is part of the \"code\". Scanning for non-obvious, internally verifiable facts:\n\n- Draft A: \"Held-out budget is named B″ because B′ is burned by SCN-RETR-6b-2\" — verifiable in-bytes (6b-2 does use \"Fixture B′\"), but the Wave-H \"Naming\" paragraph states it explicitly, so it's low-surprise. Discard.\n- Draft B: debounce is call-count-based, not wall-clock — plainly stated in the Given/When/Then bodies; a summary would surface it. Discard.\n- Draft C: the coverage ledger's tallies exclude the SCN-RETR-12m amendment scenarios. Refutation attempt: is this checkable in-bytes? Yes — count the base-section scenario headings: 65 headings precede/among the ledger excluding the four 12m blocks; the amendment block sits *before* the ledger yet the ledger reads verbatim \"SCN count: 65\"; the enumerated guard list (…12d, 12e, 12j, 12k, 13b…) contains no 12m; the gen-histogram conformance enumeration (RETR-1 ×1 … RETR-13 ×4) contains no RETR-12 entry although all four 12m scenarios are tagged `gen: conformance`. Recounted item-by-item: 12m-1 (happy), 12m-2/-3/-4 (guards), all `[AMENDED 2026-08-03]`, all `gen: conformance`. The discrepancy survives every refutation test: it rests only on bytes shown here, the counts were hand-verified, and it is not a restatement of the title. It would change what an auditor/builder does — trusting \"65/65\" or the 26-item guard list misses three live guard scenarios. Survives.\n\nSTEP 2: emit.\n\n```atlas-fact\n{\"claim\": \"The coverage ledger predates the REQ-RETR-12m amendment: it reports 'SCN count: 65' with no 12m entries in its guard list or gen-histogram conformance enumeration, even though four SCN-RETR-12m scenarios (added 2026-08-03; one happy, three guards, all gen: conformance) sit in the document body ahead of that ledger.\"}\n```"