"STEP 1 — scratch reasoning (not persisted)\n\nCandidates drafted from the bytes:\n\n1. *“The walker follows symlinks.”* Refuted by the code itself: `leafFor` branches on `entry.mode === GIT_MODE_SYMLINK` ('120000') and takes the `gitBlobOrSkip` leg instead of `readFileOrSkip(join(repoPath, entry.path))`; the header comment's history narrative (\"used to readFileSync every tracked path\") is past-tense commentary, but the current control flow independently confirms the opposite behavior — symlink targets on disk are never opened in this unit. Discard the candidate, keep the corrected version.\n\n2. *Corrected candidate:* “A mode-120000 entry yields a leaf whose `content` is the blob text pulled from the object database (`cat-file blob <oid>`), and the target path on disk is never read.” Checkability against THESE bytes: the constant `'120000'` is a literal here; `leafFor`'s symlink branch calls only `gitBlobOrSkip` (whose body runs `['cat-file', 'blob', oid])` and constructs `{ path, children: [], content: target, symlink: true }`; `readFileSync` appears solely inside `readFileOrSkip`, which is invoked only on the non-symlink branch. No reliance on any comment's claim about another module. Survives.\n\n3. *“walkFileTree never throws; non-git dirs yield an empty tree.”* Derivable (`gitLsFiles` try/catch → `[]`, all leaf readers swallow errors), but partially anchored on the comment naming other files (`compose.ts`, `wire.ts`) whose behavior I cannot see; the pure-code residue (“fails to empty set”) is weaker than candidate 2 and overlaps the comment more than the code's distinctive mechanism. Discard in favor of 2.\n\n4. *Exact-enumeration candidates* (record parse rules, first-tab split, ASCII sort in `freeze`) — each is verifiable in the bytes, but they are close to restating what the code plainly does line-by-line and none changes downstream behavior as much as 2. Discard.\n\nRefutation pass on 2: Does it depend on anything outside the shown declarations/control flow/literals? No — mode literal, branch structure, `cat-file` argv array, and the sole placement of `readFileSync` are all in-unit. Is it more than name/signature? Yes — the signature `walkFileTree(repoPath): FileTree` reveals nothing about content sourcing per entry kind. Would it change what an engineer does? Yes — consumers cannot treat a symlink leaf's `content` as resolvable source bytes or attempt to re-read the target; the marker exists precisely so downstream folds exclude such text. Survives.\n\nSTEP 2 — emit\n\n```atlas-fact\n{\"claim\": \"For a git index entry with mode 120000, walkFileTree produces a leaf whose content is the stored link-target text fetched from the object database via `git cat-file blob <oid>` (after a /^[0-9a-f]{40,64}$/ charset check), and the symlink's on-disk target path is never opened — only non-symlink entries get their content from working-tree reads.\"}\n```"