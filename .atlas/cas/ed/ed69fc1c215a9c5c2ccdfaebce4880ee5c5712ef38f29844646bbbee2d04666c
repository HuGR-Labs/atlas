"STEP 1 — REASONING (scratch)\n\nCandidates drafted and refuted:\n\n- *\"The digest is NFC-normalized\"* — REFUTED. The normalization happens inside kernel `id`/canonicalizer, not in these bytes; the code here only calls `id(...)`. Rests on another module's behavior.\n- *\"Symlinked source is always refused, even intra-repo\"* — true behaviorally, but it leans on POSIX semantics of `O_NOFOLLOW` plus a long comment narrating intent; the distinctive claim is mostly carried by the prose, not uniquely by the control flow. Weaker.\n- *\"evidenceSpan is file-granular (0..length)\"* — verifiable in code (`mintSpan(bytes, 0, bytes.length)` plus the reader returning the whole file), but it is essentially a restatement of the design loudly documented in the interface block; low marginal information beyond name/docs.\n- *\"Slot validation runs on the comment-STRIPPED template\"* — verifiable (`assertUsable(stripComments(raw), ...)`), but marginal impact on what an engineer does.\n- **Survivor**: In `createPromptFactory`, two adjacent lines show an asymmetry: `const template = assertUsable(stripComments(raw), ...)` feeds `build`, while `const digest = id({ promptTemplate: raw } as never)` hashes the PRE-strip text. Nothing in the name/signature (`digest: Hash`, \"digest of the template it was built from\") reveals which string is hashed; the code itself shows the hashed input includes the justification comments that are never sent. Consequence an engineer must act on: the digest audits the versioned artifact, not the exact interpolated bytes, and editing a comment rotates the digest without changing the sent text. Derived entirely from declarations/control flow in these bytes; no external-file claims; no counts.\n\nRefutation passes all fail to break it. Emitting.\n\n```atlas-fact\n{\"claim\": \"In createPromptFactory, the returned digest is computed over the template text as read (raw, before stripComments removes the justification comments), while build interpolates the stripped text — so the digest does not witness the exact prompt text sent to the model.\"}\n```"