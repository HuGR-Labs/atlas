"STEP 1 — REASONING (scratch)\n\nCandidates drafted from the bytes:\n\n1. *\"CARRIER-3 pins literal hex digests\"* — refuted: the test's own title says \"LITERAL digests, pinned\", so this is knowable from the name alone.\n\n2. *\"Emit is called 5× in CARRIER-6 (3 pre-prune, 2 post-prune)\"* — I counted the calls in the shown bytes and they check out, but the number isn't the point of anything here and the qualitative shadow (\"rejection is side-effect-free\") is inference beyond the bytes. Weak; discard.\n\n3. *\"Tier 'T2' materializes on a row written with only `gen: 2` and no explicit tier (CARRIER-2)\"* — the input/output pairing is visible (`advisoryFact({…, gen: 2})` → `row.tier === 'T2'`), but the mechanism lives outside this unit and the pairing alone is thin. Discard.\n\n4. *\"nodeKey ignores claimNorm and gen\"* — supported by the PREMISE expects (`keyOf(write) === key` with different claim text and generation), but the property is about `keyOf`'s implementation elsewhere; this unit merely asserts equality as a setup premise. Interpretive; weaker than a fully self-contained candidate.\n\n5. **Survivor:** the \"legacy\"/\"pre-carrier\" sidecars are not wholly historical artifacts. Reading the two hand-writing sites — the `writeLegacySidecar` helper body and the inline `writeFileSync` in CARRIER-8 — both embed `identity: IDENTITY_SCHEMA` straight from the live import at the top of the file. Nothing in the unit's name or any signature reveals that the simulated old-shape file deliberately carries today's identity stamp while omitting only `scope`/`tier`. Refutation checks:\n   - Derivable from these bytes? Yes — the `identity: IDENTITY_SCHEMA,` line is present in both writers, and the import is declared in this file.\n   - Resting on a comment's claim about elsewhere? No — I am citing the code line itself, not the `#112` rationale or anything about another module's behavior.\n   - Exact count/enumeration? I avoid one; I note \"both hand-writers,\" which I did locate item-by-item (exactly two such sites exist in the shown bytes).\n   - More than a restatement of name/signature/types? Yes — a reader assuming a fully-aged fixture would be wrong, and the truth changes what they do: edits to `IDENTITY_SCHEMA` flow into these \"legacy\" fixtures, and identity-format back-compat is deliberately outside what this suite exercises.\n\nCandidate 5 survives. STEP 2 emits it.\n\n```atlas-fact\n{\"claim\": \"The hand-written 'pre-carrier' sidecars in this suite are not fully historical: both legacy writers — the writeLegacySidecar helper and CARRIER-8's inline writeFileSync — stamp the live IDENTITY_SCHEMA import into the file, so only the row shape is aged, and any change to the identity schema propagates into these 'old-shape' fixtures.\"}\n```"