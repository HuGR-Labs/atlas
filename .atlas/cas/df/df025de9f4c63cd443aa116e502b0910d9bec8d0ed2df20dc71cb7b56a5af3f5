"STEP 1 — scratch reasoning:\n\nReading the bytes closely, the load-bearing line is:\n\n```ts\nreturn asNodeKey(defaultEncoder.hash(canonicalForm({ tv: unitKey, t: testName })));\n```\n\nThe object literal passed to `canonicalForm` has exactly two properties: `tv` bound to `unitKey`, and `t` bound to `testName`. There is no constant tag value and no third `u` field.\n\nNow compare with the docstring directly above the function, which states the formula as `` hash(canonicalForm({tv, u, t})) `` — three symbols — and describes `tv` as a *tag* keeping the preimage set disjoint from sibling families. The code contradicts this: the property named `tv` carries the *variable* unit key, not a fixed discriminator, and there is no separate `u` entry. So the actual preimage shape is `{ tv: <unitKey>, t: <testName> }` with no constant family-tag slot.\n\nRefutation attempts:\n- Is this derivable from these bytes? Yes — the return expression and the doc line are both physically present; the mismatch is internal to the unit, not a claim about another file. I am not trusting the comment; I am reporting what the code constructs.\n- Does it rest on code I cannot see? No — nothing about negation/relation/transition/node keys is asserted; I make no claim about whether cross-family disjointness actually holds.\n- Exact counts/enumerations? The \"two properties\" count is verified item-by-item in the literal: `tv`, `t`. Safe.\n- Restatement of name/signature? No — the signature `(unitKey, testName): NodeKey` reveals nothing about the preimage's property names or the absence of a constant tag.\n- Would it change what they do? Yes: anyone re-implementing the derivation in tooling/another language, debugging hash mismatches, or auditing collision reasoning from the docstring's `{tv, u, t}` formula would mint different preimages than production code does.\n\nOther candidates considered and discarded: the shared error class for both legs (plainly visible, low actionability); the imports (visible); the `unknown` parameter types (already in the signature); the error-message-vs-guard mismatch on qualifiedPath formatting (true locally, but whether format validation happens upstream in unseen doors is unknowable from these bytes, so any strong claim would lean on invisible code).\n\nSurvivor: the preimage-shape/doc-formula divergence, stated from the code side.\n\nSTEP 2 — emit:\n\n```atlas-fact\n{\"claim\": \"The hashed preimage is exactly the two-property object { tv: unitKey, t: testName } — the unit key is bound under the property named 'tv', so no constant family-tag value or separate 'u' field enters the hash, contrary to the docstring's {tv, u, t} formula.\"}\n```"