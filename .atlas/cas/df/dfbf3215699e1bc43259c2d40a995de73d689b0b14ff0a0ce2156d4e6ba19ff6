"```atlas-fact\n{\"claim\": \"The two check legs parse their text asymmetrically in this unit: splitQuery takes everything after the FIRST '|' as the query's single argument (so a key containing a pipe survives intact in an index-query), while splitAssertion splits on EVERY '|', so the same piped key in an assertion yields extra parts and is refused by admit's exactly-three-parts check.\"}\n```"