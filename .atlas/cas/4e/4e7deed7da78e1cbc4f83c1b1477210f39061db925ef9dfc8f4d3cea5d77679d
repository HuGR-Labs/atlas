"STEP 1 — Reasoning (scratch):\n\nScanning the unit for facts not implied by the path/heading and fully contained in these bytes:\n\n- *Teeth count = 18*: I counted the `teeth:` lines (14 in Block AUTH, 2 in CLI, 2 in MCP = 18), and it matches the completeness table. True, but the table already asserts it — low novelty, mostly a restatement of a table cell.\n- *73 SCNs total*: I tallied AUTH (57) + CLI (6) + MCP (10) = 73, matching the claimed 73/73. Again, the table already states it.\n- *SCN-AUTH-5c-1 as the only type-checker-enforced golden*: verified — \"type-checked\"/\"build fails\" appears only there; every other mutant guard runs at runtime. Real and actionable, but a candidate.\n- *Dogfood-dated regression in 12c*: the literal error string is in the bytes, but the fact leans on an external event (the \"2026-07-25 dogfood\") not present here — per the rules, discard.\n- *atlas-link never driven*: plausible after scanning all Whens, but it is a broad negative claim over 73 scenarios; weaker evidentiary footing than a direct textual clash.\n- **Internal numbering clash**: SCN-AUTH-5a-1 asserts the `PredicateSlot` union has **\"(13 members)\"**, yet SCN-AUTH-5c-1 describes its mutant as \"**adds a 13th member** to the `PredicateSlot` union\" and SCN-AUTH-5e-1 expects \"**the 13th member** is returned\". Under 5a's count, the newly added member is the 14th. Both statements sit in this unit; neither depends on unseen files, comments about elsewhere, or external events. The contradiction holds regardless of which line is deemed erroneous, and it would propagate directly into generated golden expectations. This survives every refutation check.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"The document contradicts itself on slot-count arithmetic: SCN-AUTH-5a-1 fixes the PredicateSlot union at 13 members, yet the mutant scenarios SCN-AUTH-5c-1 and SCN-AUTH-5e-1 describe 'adding a 13th member' to that union and expect 'the 13th member' to be returned — under 5a's own count the added member would be the 14th, a numbering clash that would bake wrong expectations into the generated goldens.\"}\n```"