"STEP 1 — Reasoning scratch:\n\nReading the bytes: two arms over identical fixtures. The CONTROL composes its door by hand and passes `ratifyToken: 'billy'` inline in the `createGovernedEmit` config. The v2 arm calls `composeRuntime(repoPath).promote(AT)` with no such parameter anywhere in sight — instead the test assigns `process.env.ATLAS_RATIFY_TOKEN = 'billy'` beforehand and restores the prior value in a `finally`.\n\nDraft candidate: the two arms supply the *same* literal token `'billy'` through *different* transports — inline parameter for the hand-composed door, ambient environment variable for the composed runtime.\n\nRefutation attempts:\n- Derivable from these bytes? Yes: both literals are in the shown code (`ratifyToken: 'billy'` and `process.env.ATLAS_RATIFY_TOKEN = 'billy'`), plus the save/restore in `finally`. No reliance on `governed-emit-route.ts` or any unseen file — I'm describing the test's own plumbing, not the route's internals.\n- Resting on a comment's claim? No — the comment mentioning `governed-emit-route.ts` is ignored; the env mutation and the inline param stand on their own as code.\n- Exact count / exhaustive enumeration? Avoided; qualitative phrasing (\"inline vs. ambient env\"), verified by locating each literal.\n- More than a restatement of name/signature/types? Yes — the filename suggests a blackbox recall comparison, not that one arm depends on global mutable process state. Operationally it matters: the v2 invocation fails or behaves differently if lifted out without the env guard, the `try/finally` must survive refactors, and the env var is shared ambient state unlike the CONTROL's parameterized injection.\n\nSurvives. Emitting.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"Both arms ratify with the same literal token 'billy', but the CONTROL injects it inline as ratifyToken in the hand-built emit config while the v2 arm delivers it through the ambient process.env.ATLAS_RATIFY_TOKEN variable (saved and restored in a finally) around the composeRuntime().promote call.\"}\n```"