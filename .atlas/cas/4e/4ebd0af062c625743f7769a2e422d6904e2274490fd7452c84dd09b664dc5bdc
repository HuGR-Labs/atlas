"STEP 1 — scratch reasoning:\n\nReading the declarations and control flow:\n\n- `VerifyReq` is a discriminated union with **five** arms: dependency, count, definition, negation, **relation** (checked each literal).\n- `createVerifyFactLeg`'s returned closure switches on all five kinds; the `relation` arm calls `verifyRelation`.\n- `verifyFactVerdict`, the \"SHARED verdict builder\" that any transport calls over the SAME leg, begins with a whitelist guard checking `kind !== 'dependency' && kind !== 'count' && kind !== 'definition' && kind !== 'negation'` — exactly four literals, no `'relation'` — and anything else falls into `reject('unknown kind …')`.\n\nCross-checked: the guard list (4 items) vs. the union arms (5 items) — counted item by item, no mislabel. So a caller going through the advertised shared path can never construct a request that reaches the `relation` arm; only direct `leg({kind:'relation',…})` invocation can. This rests solely on the union declaration and the if-condition in these bytes — no reliance on issue numbers (#99/#220/WP-R7 appear only in comments about elsewhere) or on external files.\n\nRefutation attempts: (a) Is it restating name/signature? No — signatures alone (`VerifyFactLeg`, `VerifyFactOpts`) suggest any `VerifyReq` kind flows through; the mismatch only appears by reading the body. (b) Stale-comment contradiction? The comment even hints relation is wired \"once WP-R7 wires it,\" but the operative fact — the builder rejects it today — is in the code, not the comment. (c) Exhaustive-count risk? Mitigated by naming the exact literals rather than leaning on a bare number.\n\nSurvives.\n\n```atlas-fact\n{\"claim\": \"Although VerifyReq includes a 'relation' arm and the leg returned by createVerifyFactLeg dispatches it to verifyRelation, verifyFactVerdict's kind whitelist covers only dependency/count/definition/negation and rejects 'relation' as an unknown kind, so no call routed through the shared verdict builder can ever reach the relation branch.\"}\n```"