"STEP 1 — REASONING (scratch)\n\nCandidate drafts and refutation passes:\n\n- *Candidate A: \"The store holds exactly 18 invariants (A-1..A-18).\"* Counted the §4 rows — 18, correct. But the header banner already advertises \"(A-1..A-18)\", so it's nearly a restatement of the unit's own preamble. Weak actionability. Discard.\n\n- *Candidate B: \"Advisory facts edit in place; predicates supersede.\"* Present in A-12 and §3.2, verifiable in these bytes. Survives, but it's a fairly guessable lifecycle asymmetry once one knows the two families exist. Hold as backup.\n\n- *Candidate C: \"Grounding hashes the raw source slice (NFC-only), so reformatting the cited unit drifts the fact — an accepted false alarm; the normalizer was refused.\"* Fully contained in §3.1's AMENDED 2026-08-02 note and acceptance criterion 1 (header-above leg FRESH iff blank-line-separated; contiguous leading comment DRIFTS). Strong, but I'd be tempted to lean on the ADR-0014/req-grd citations, which point outside these bytes — I'd have to strip those references and keep only the in-byte rules. Survives in trimmed form; backup.\n\n- *Candidate D: \"No embeddings / no RAG — ever; retrieval is a hashed structural index resolved by scope/dependency/tag only.\"* Checked grounding: §3.6 opens with the hard rule verbatim; invariant A-13… no, A-14 row states it as a MUST NOT with \"two identical queries MUST return identical results\"; §8 item 17 confirms (\"grep the codebase — there is no embedding model, vector store, or RAG call anywhere\"). Three independent in-byte anchors, no reliance on any file outside the unit. The §3.6 enumeration is exactly three numbered resolution methods (scope, dependency, trigger/tag) — I listed them; safe. Not derivable from the path/name `docs/spec/atlas.md`; an engineer assuming \"knowledge layer ⇒ vector DB\" would architect the wrong system. Highest blast radius on behavior.\n\nD wins over B and C on non-guessability × actionability × in-byte redundancy. Emitting D.\n\nSTEP 2 — RESULT\n\n```atlas-fact\n{\"claim\": \"Despite being a knowledge layer, the Atlas is normatively forbidden from using embeddings, vector similarity, or RAG anywhere (invariant A-14): retrieval is a deterministic hashed structural index resolved only by scope (path), dependency edges, or trigger/tag match, and two identical queries MUST return byte-identical results (acceptance criterion 17).\"}\n```"