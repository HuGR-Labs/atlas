"STEP 1 — scratch reasoning:\n\nScanning for facts beyond name+signatures: nearly every distinctive assertion in this file (identity hashing via `negationKey`, drift detection riding subtreeHash, `edgeModel` unenforced status, supersession lifecycle, the type-only cycle with `types.ts`, extraction history) lives in COMMENTS making claims about OTHER files (`negation-key.ts`, `drift.ts`, `types.ts`, docs, ADRs) — none re-derive at these bytes. Discard all.\n\nSignature-visible facts (six-member `reason` union, `'NEGATED' | 'SUPERSEDED'`, optional `obviousness`/`seal`/`authzScope`, field lists) are already in the declarations — not eligible.\n\nSurviving candidate: the module-level structure itself — every one of the five import statements uses `import type` (checked line by line: `@atlas/contracts`, `@atlas/kernel`, `@atlas/grounding`, `./types.js`), and the only exports are two `interface` declarations. No constant, enum, function, or value exists anywhere in the unit. So the compiled artifact is empty: nothing here can be value-imported, and importing it adds zero runtime weight regardless of the (comment-claimed) type cycle. Verified against bytes; qualitative; not a restatement of any signature; changes practical behavior (import strategy, where a runtime discriminator registry must live instead).\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"Every import in this module is a type-only import and its sole exports are two interfaces, so the file contains no runtime values at all — nothing can be value-imported from it, and it compiles away to an empty module regardless of the type-level dependency on ./types.js.\"}\n```"