"STEP 1 — scratch reasoning (not stored): Scanning every assertion in the file: SCN-196a-1a builds a `legacy` AdvisoryNode with no `seal` key and asserts `expect(legacy.seal).toBeUndefined()`, then a spread copy with `'proven'`. SCN-196a-1b/1c/1d each embed `seal: 'proven'` directly in the PredicateNode/RelationNode/NegationNode literals and assert only `toBe('proven')`; none contains a `toBeUndefined` check. SCN-196a-2 compares `nodeKey` outputs for absent-vs-present seal but never reads `seal` back off a node. So despite the suite's framing of absent-tolerance across all four shapes, the absent-value read is actually pinned for exactly one shape (AdvisoryNode). Verified item-by-item across all five `it` blocks; derived from the assertions themselves, not from comments about other files. Not a restatement of the describe titles (which imply broader absent-case coverage than the code delivers). Survives.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"Among the four node-shape cases, only the AdvisoryNode one asserts that an absent seal reads back as undefined — the PredicateNode, RelationNode, and NegationNode cases each hard-code `seal: 'proven'` into the literal and never exercise the absent value.\"}\n```"