"```atlas-fact\n{\"claim\": \"Per-seat memory scoping is explicitly not a confidentiality guarantee: the document states that because Memory is git-native, any repo reader can read any seat's bytes, so MEM-1 is injection-scoping rather than access control (and must not be read as isolation), with true per-seat secrecy deferred to a future opt-in encryption option at a re-spawn/portability cost.\"}\n```"