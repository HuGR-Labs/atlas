"STEP 1 — scratch reasoning (candidates tested against the bytes):\n\n- *“A sameAs peer that isn't a current node is valid”* — rests entirely on the comment's claim about `linkSameAs` in another file, which isn't shown. **Discarded.**\n- *“`governed-link.ts` writes only through `commitProjection`”* — a comment's assertion about a file not in these bytes. **Discarded.**\n- *“`POLICY` maps alice→core, mallory→other”* / *“nodes are keyed n0, n1…”* / *“`fact` ids are `nk-${claim}`”* — true and visible, but essentially restatements of literals a reader gets at a glance. **Discarded.**\n- *“`blindTo` prunes reads only, and even a re-`put` can't heal a pruned hash”* — checkable (`gone.has(h)` short-circuits before delegating), but marginal action-value. **Set aside.**\n- *Head-chaining of the fake commit*: `commitProjection` calls `decide(persists.length > 0 ? persists[persists.length - 1]! : projection)`, and `loadProjection` reports the same tip. Nothing in the `DiskStore` type or the method names reveals that consecutive decisions are evaluated on top of each other's outputs within a single fixture — an engineer assuming every commit restarts from the seeded base would miswrite or misread multi-step scenarios. Verified against the ternary itself, not the comment. **Survives.**\n- *`commitStaging` is inert (reads base projection, drops `next`)* — also code-verifiable and surprising, but the head-chaining fact is broader in effect; only one may be emitted.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"Each call to the fixture store's commitProjection evaluates its decision against the most recently published projection (the last entry of persists), falling back to the seeded base projection only before the first publish, so successive governed writes stack on one another rather than each starting fresh.\"}\n```"