"STEP 1 — Reasoning (scratch):\n\nScanning the bytes for candidates:\n\n- *Header claims about CLI-only commands, thirteen-tool totals, `READ_SURFACE` membership* — all rest on `@atlas/cli`, `GOVERNANCE_SURFACE`/`READ_SURFACE` definitions, and `server-read-tools.ts`, none visible here. Discard.\n- *\"Handler is total / never throws\"* — a property of `@atlas/adapter-io`, not shown. Discard.\n- *\"relationsVerdict enforces `required:['unit']`\"* — enforcement happens in a module I cannot see. Discard.\n- *Routing order / early returns* — real but largely a restatement of the function structure.\n- **Surviving candidate:** trace what actually happens to arguments in `callTool`. There is no schema-validation step anywhere in the file. `RELATIONS_INPUT_SCHEMA` / `NEGATIONS_INPUT_SCHEMA` are referenced exactly once each — inside `advertisedReadTools`, purely to populate the advertised `SdkTool`. The call path never consults them: the relations branch does `typeof a.unit === 'string' ? a.unit : ''`, the negations branch does `typeof a.scope === 'string' ? a.scope : ''` and `a.abstained === true` (strict — `\"true\"` or `1` would coerce to `false`), and the governed fallback hands raw `args` straight to `handler.handle(name as Tool, args)`. So the advertised contracts (`required`, `additionalProperties:false`) are documentation-only at this layer; runtime handling is ad-hoc coercion. Every element of this is checkable by reading this file's declarations and control flow alone. It survives refutation: no library knowledge needed, no out-of-file claims, no counts.\n- It changes what an engineer does: editing those schema constants would not alter call-time behavior; clients/tests cannot expect rejection of extra or ill-typed keys at this layer.\n\nEmitting the survivor.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"Although the server advertises JSON Schemas with `required` and `additionalProperties:false`, nothing in this file validates CallTool arguments against them — the read-tool branches hand-coerce types instead (non-string `unit`/`scope` become `''`, and `abstained` counts only on strict `=== true`), while governed calls pass `args` to `handler.handle` untouched.\"}\n```"