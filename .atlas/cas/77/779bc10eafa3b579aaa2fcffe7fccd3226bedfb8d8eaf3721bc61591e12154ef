"```atlas-fact\n{\"claim\": \"The case-spelling bypass test is gated at module load by `it.skipIf(!FOLDS_CASE)`, where `FOLDS_CASE` is decided empirically by creating `probe`/`PROBE` under `os.tmpdir()` and comparing their `statSync` `(dev, ino)` pairs — so on a volume that does not fold ASCII case the arbitrary-code-execution bypass leg is skipped rather than exercised, not asserted as the opposite outcome.\"}\n```"