"STEP 1 — REASONING (scratch):\n\nScanning the document for facts not implied by the filename/path alone, and testing candidates against the bytes:\n\n- Candidate: \"PBT SCNs are exempt from held-out legs.\" — Refuted as a *new* fact: this is explicitly asserted by the ledger itself, so emitting it is restating in-doc text rather than surfacing anything non-obvious.\n- Candidate: \"TOOLS-11 vs TOOLS-11a ID-scheme distinction.\" — Also directly stated in the ledger's ID-scheme note; restatement.\n- Candidate: \"REQ-TOOLS-14c is labeled happy but its SCNs are guards.\" — True and internal, but low impact and arguably intentional labeling; weak actionability.\n- Candidate (strong): the fixture preamble states `Governance surface (TOOLS-1) = exactly {atlas-init, atlas-query, atlas-emit, atlas-reconcile}` — a four-element literal. Later in the SAME document, SCN-TOOLS-1a-1 requires `exactly {atlas-init, atlas-query, atlas-emit, atlas-reconcile, atlas-link} — surface count == 5`, echoed by 1a-2, 12c-1/-2, and 16e. Re-checked element-by-element: preamble set = 4 items, golden set = 5 items including `atlas-link`. This contradiction rests on nothing outside the unit — both sides are literal in-file enumerations; no reliance on what `tools/ref/store.ts` or ADR-0003 actually contain. It survives: it is not derivable from the path/name, and an engineer implementing the 1a goldens against the preamble's \"exactly four\" would produce a failing/mis-scoped suite.\n- Secondary candidate (also survived initial screening): the 6e/6f amendment goldens carry `gen: conformance` with no `held_out: true` legs, conflicting with the ledger's \"every conformance SCN carries a held-out fixture\" claim. Real, but requires counting across many sections and asserting a numeric totality; the four-vs-five surface contradiction is quoted-literal on both sides and safer.\n\nChoosing the surface-count contradiction.\n\nSTEP 2 — RESULT:\n\n```atlas-fact\n{\"claim\": \"The fixture-universe preamble fixes the TOOLS-1 governance surface as exactly the four tools {atlas-init, atlas-query, atlas-emit, atlas-reconcile}, yet this same document's goldens (SCN-TOOLS-1a-1/-2, 12c-1/-2, 16e-1/-2) enforce a five-tool surface that additionally includes atlas-link, making the preamble enumeration stale relative to the requirement the goldens test.\"}\n```"