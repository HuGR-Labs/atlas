"STEP 1 — scratch reasoning:\n\n- Candidate A: \"materializePoke creates outDir recursively.\" Refute: SCN-POKE-4's title literally states this; too close to a restatement of the test names.\n- Candidate B: \"Path is `<outDir>/<seat>.poke.json`, overwrite never timestamped.\" Refute: directly asserted in SCN-POKE-1/3 titles and trivially visible; low information beyond the name.\n- Candidate C: \"The source may return either a Poke envelope or a bare Pack.\" Partially supported by the two fakes, but the acceptance of both shapes rests on the unseen `materializePoke`/`PhasePushSource` types; the fakes only show inputs, so the fact leans on invisible declarations.\n- Candidate D: \"The pinned golden bytes omit `freshness` even though the fake source's invariant carries `freshness: 'FRESH'`.\" Check against bytes: `FAKE_PACK.invariants[0]` includes `freshness: 'FRESH'`; `G_PACK_CANONICAL` contains `\"claim\"`, `\"nodeId\"`, `\"tier\"` per row and no `freshness` token anywhere; SCN-POKE-1/2 assert the file equals these goldens. This is verifiable by comparing the literals inside this unit alone — not dependent on the comment's appeal to KERNEL-8/canonical.ts elsewhere. Not derivable from the file/test name or call signature (one would assume a faithful serialization). Behavior-changing: an engineer cannot expect per-row verdicts in the landed artifact, and adding such fields means updating named goldens. Survives. Phrasing kept qualitative (no counts/enumerations).\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"Although the injected fake source's invariant carries freshness: 'FRESH', the golden bytes this suite pins for the written Pack contain no freshness field anywhere, so the poke file on disk omits the per-row verdict that exists in the in-memory Pack.\"}\n```"