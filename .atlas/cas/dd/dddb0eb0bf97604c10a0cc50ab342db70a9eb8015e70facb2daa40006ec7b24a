"Reading the bytes closely before answering.\n\nCandidate observations I considered and tested:\n\n1. *\"`draftSupersede` differs from `draft` only in the `authoring` stamp\"* — visible, but it's essentially the function's documented purpose and plainly readable from `build(input, 'ADVISORY')` vs `build(input, 'SUPERSEDED')`; close to a restatement of intent. Discarded.\n2. *\"`requires` appears only when route is `full-ratify`\"* — true from the conditional spread, but largely narrated by its adjacent comment and partly a property of the unseen `DraftOut` type. Weaker.\n3. *\"Tier is always `'T2'` even on UPDATE\"* — solid and code-visible, but arguably inferable once one sees `tier: DRAFT_TIER` next to the obvious constant definition; still a plausible candidate.\n4. **\"`claimNorm` carries the raw claim verbatim\"** — testing against the source: `candidateView` sets `claimText: input.claim` and `claimNorm: input.claim`; later the fact sets `claimNorm: input.claim`. There is no normalizing call, transform, or helper anywhere in the unit — `nodeKey(candidateView)` consumes the view but the *returned* `fact.claimNorm` is unambiguously the untouched author string. Nothing in the visible code performs normalization, and no comment claims otherwise. Survives refutation: it is checkable purely in these bytes, it is not derivable from the name `draft` or the signature `draft(candidate): DraftOut` (the `GroundedFact` internals aren't in the signature), and it is action-guiding — a consumer who trusts the field name `claimNorm` to hold normalized text would be wrong at exactly this seam.\n\nFact 4 survives.\n\n```atlas-fact\n{\"claim\": \"Despite the field name, this planner performs no normalization: the returned fact's `claimNorm` (and the Candidate view's) is bound directly to the author's raw `claim` string (`claimNorm: input.claim`), verbatim.\"}\n```"