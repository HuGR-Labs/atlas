"STEP 1 — Reasoning scratch:\n\n- Candidate A: \"`init`'s path argument doesn't narrow the territory list.\" Present in bytes, but it's a near-restatement of the caveat sentence right next to the step; weaker than other candidates.\n- Candidate B: \"Without policy.json, empty scopes mean no write is authorized.\" In bytes, but largely a plain summary of the conservative-default sentence.\n- Candidate C: \"When `.atlas/` is committed, every command except `init` refuses with exit 2; init is the deliberate exemption.\" Strong, in bytes (refusal transcript + prose). Still plausible a reader could infer it from the repair text pointing back at `init`.\n- Candidate D: The gitignore rule *shape*: the doc states the installed rule is exactly `.atlas/*` plus `!.atlas/policy.json`, in that order, and explains that a directory-level `.atlas/` exclude would make the negation unreachable and silently drop the policy file — the only `.atlas/` file kept tracked.\n  - Refutation pass: Is it derivable from THESE bytes? Yes — transcript shows `denying .atlas/*`; step 3 states the exact pair, the ordering (\"in that order\"), and the mechanism; the repair recipe independently repeats \"with a `!.atlas/policy.json` exception\"; step 4 confirms policy.json is \"the one file under `.atlas/` that is source.\"\n  - Resting on a comment about unseen code? No — the operative claims are the document's own assertions about its own tooling, all present here.\n  - Exact-count/exhaustive risk? The rule consists of exactly two entries, both listed and re-checked in the bytes; \"only file kept tracked\" matches the doc's explicit statement.\n  - More than name/signature restatement? Yes — \"move-a-repo-in\" reveals nothing about rule ordering or the `.atlas/` vs `.atlas/*` trap.\n  - Changes what they do? Yes — a competent engineer would otherwise \"clean up\" the rule to `.atlas/` or delete the odd negation, silently untracking their policy file.\n\nCandidate D survives.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"The ignore rule `atlas init` installs is deliberately written as `.atlas/*` followed by a `!.atlas/policy.json` negation, in that order, because excluding the directory as `.atlas/` would make the negation unreachable and silently drop the policy file — the only file under `.atlas/` kept tracked.\"}\n```"