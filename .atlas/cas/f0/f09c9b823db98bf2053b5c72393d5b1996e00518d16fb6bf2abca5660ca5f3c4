"STEP 1 — REASON FREELY:\n\nReading the bytes closely. Candidates drafted and attacked:\n\n- *“MAX_HOPS is pinned to 2”* — visible as `expect(MAX_HOPS).toBe(2)`, but an engineer reading the surrounding bounds-of-two assertions would infer this anyway. Discarded as near-restatement.\n- *“changedBuckets is 5 out of 500 buckets”* — the numeric point of 12d-1, but that is exactly what the test name advertises (“only affected buckets, never N”). Restatement. Discarded.\n- *“delta varies only the spatial axis; territory/dependency are placeholder nodes ‘tr-root’/‘dp-root’”* — checkable from the `axes()` helper and every call site, but low action-value. Held as backup.\n- *“stateChanged is exercised purely by swapping object versions under an invariant subtreeHash”* — demonstrated by the `obj-v1`→`obj-v2` literals in 12b-1, but the distinguishing behavior is what the scenario name already announces. Weaker than another candidate. Held as backup.\n- *“DepEdge direction and eager-traversal orientation”* — attack it hard. Is it from these bytes? Yes: literals `{from: h('B'), to: h('A'), …}` etc., plus asserted results. Can forward traversal explain the expectations? No: in the ∀-chain test every edge is `{from: nodes[i+1], to: nodes[i]}`, so no edge has `from === nodes[0]`; a from→to walk from `nodes[0]` would return an empty set, contradicting `toEqual({nodes[1], nodes[2]})`. Backward collection of `from` endpoints whose `to` is in the frontier is therefore *proven by the assertions*, not borrowed from the arrow-diagram comment; the branching-DAG test independently corroborates (hop-1 `from`s over `to: h('A')`, hop-2 `from`s over `to: hop1`). Counts used ({nodes[1], nodes[2]}, {B, C}, excluded {D, E}) are explicitly enumerated in the assertions and re-checked item-by-item. Not derivable from the unit's name/signature (the `DepEdge` import is type-only; `eagerRehashState`'s signature isn't shown at all). Action-changing: anyone implementing or reviewing the closure must get the traversal direction right or silently compute nothing. **Survives.**\n\nSTEP 2 — EMIT THE RESULT:\n\n```atlas-fact\n{\"claim\": \"DepEdge fixtures store edges dependent→dependency ({from} names the dependent, {to} its dependency), and the asserted results prove eagerRehashState walks that stored direction backwards — collecting the `from` endpoints of edges whose `to` lies within two hops of the edited node (a forward from→to walk from nodes[0] would find no edges at all, contradicting the expected {nodes[1], nodes[2]}).\"}\n```"