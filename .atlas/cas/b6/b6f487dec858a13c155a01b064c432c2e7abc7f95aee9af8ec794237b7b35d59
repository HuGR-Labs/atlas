"STEP 1 — Reasoning (scratch):\n\nReading the bytes closely. Candidates drafted and tested against the source:\n\n- *Candidate: \"Orchestra is design-first — no code yet.\"* Stated in the warning blockquote, but its truth rests on the **absence of code elsewhere** in the repo — not checkable in these bytes. Discard per the rule on claims about things not shown.\n- *Candidate: \"Every skill in the inventory table is ✅.\"* Verifiable row-by-row (11 skill rows, all ✅, separator row excluded), but low value — it barely changes what an engineer does, and exhaustive-enumeration claims add risk without payoff. Discard as weak.\n- *Candidate: \"S0 is PENDING · CRITICAL PATH.\"* Present in the bytes (item 2), but the supporting detail cites cards (U2/KERNEL-10, `owner:TBD`) living in unseen files; the actionable residue is thin. Discard.\n- *Candidate: internal status contradiction.* Re-read item 1: \"The per-state contracts for S2/S3/S4 + the reconciler predicates are **still to be authored**.\" Then item 5: \"**Per-state contracts S0–S4** — ✅ **authored + cold-reviewed** … **All 5 states specified**.\" Both statements sit in this unit's shown bytes; their tension is checkable purely by reading them side by side — no reliance on unseen files, since the emitted fact asserts only the internal disagreement, not which external claim is true. Refutation attempts fail: nothing in the name `docs/method/README.md` hints at it; it is not a restatement of any heading; it is qualitative (names the specific clauses, no invented counts); and it changes behavior — a reader trusting item 1 would wrongly conclude the contracts don't exist and redo finished work (or, editing the doc, would know item 1's parenthetical needs updating). Survives.\n\nSTEP 2 — Emit result:\n\n```atlas-fact\n{\"claim\": \"In its own backlog, item 1 still says the S2/S3/S4 per-state contracts are \\\"still to be authored,\\\" contradicting item 5, which records all five per-state contracts (S0–S4) as authored and cold-reviewed — so item 1's parenthetical is stale within this same document.\"}\n```"