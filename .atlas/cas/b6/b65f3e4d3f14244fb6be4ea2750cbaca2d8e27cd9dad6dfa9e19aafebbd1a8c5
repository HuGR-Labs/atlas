"STEP 1 — scratch reasoning (not persisted):\n\nScanning the bytes: the header comment makes many claims about *elsewhere* (frozen oracle counts, `escape/target-escapes.ts`, `ast-parser-lifecycle.test.ts`, `#99`, `[teeth: … ⇒ RED]` counterfactuals) — all discarded as facts about other code. The pinned SAFE/ESCAPE verdicts are plainly readable assertions — discarded as summaries of what the test obviously does. Checking each queried identifier against SRC: each of the nine names occurs exactly once, so `nthIdentifier`'s Nth-occurrence parameter is never exercised beyond the default — true but low-impact.\n\nSurviving candidate: the grammar-loading plumbing. Line by line: `(Parser as unknown as { Language: { load(p: string): Promise<unknown> } }).Language` — `Parser.Language` is reachable only through a double cast, typed as `Promise<unknown>`; then `parser.setLanguage(TS as Parameters<Parser['setLanguage']>[0])` — the loaded grammar enters the parser only as a recast `unknown`. Counted in the shown bytes: exactly one `Language` reference, one `.load` call, one `setLanguage` call carrying the second cast. No reliance on any comment; checkable purely from declarations and expressions here. Not derivable from the file's name/imports, and actionable (typing, lint-on-unsafe-casts, web-tree-sitter upgrades all hit this seam). Refutation attempts fail: the claim describes only mechanisms present in these bytes, not motives inferred from external typings. Survives.\n\n```atlas-fact\n{\"claim\": \"Despite importing web-tree-sitter normally, this unit obtains the grammar loader only through an `as unknown as { Language: { load(p: string): Promise<unknown> } }` cast on `Parser`, and hands the loaded language to `parser.setLanguage` only after recasting that `unknown` result to `Parameters<Parser['setLanguage']>[0]` — the grammar never flows through a statically typed Language API in these bytes.\"}\n```"