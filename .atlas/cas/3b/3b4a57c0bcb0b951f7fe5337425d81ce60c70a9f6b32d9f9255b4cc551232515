"**STEP 1 — scratch reasoning**\n\nCandidates drafted and stress-tested against the bytes:\n\n- *\"#202/#99b close abstentions\"* → rests entirely on header comments referencing issues/WPs outside these bytes. Discarded.\n- *\"Empty result exits 0\"* → plainly asserted, near-restatement of the test title. Discarded.\n- *\"`main` takes an injected legs map\"* → visible at call sites, but it's just summarizing how each test invokes `main`; low surprise. Set aside.\n- *\"Flags parse to strings (`toBe('true')`)\"* → genuine and byte-checkable, but a single-literal contract detail about `parse.js`. Kept as runner-up.\n- **Survivor:** comparing the seed literal against every `toContain`/`not.toContain`: the negation row's freshness `[FRESH]` and id `(neg:1)` **are** asserted, but the abstention's `witness: { underApproxSources: [...] }` and its `asNodeKey('abs:1')` id appear in **zero** assertions across all six tests — the abstention render contract is pinned only down to `kind/relation/target/scope/reason` (`scope-open`). This is absence *within* the shown bytes (enumerated and verified), not absence elsewhere; it is not derivable from the filename or signatures, and it materially changes coverage assumptions for anyone touching the renderer or consuming witnesses downstream. Survives refutation.\n\n**STEP 2 — result**\n\n```atlas-fact\n{\"claim\": \"Unlike the grounded-negation row, whose freshness verdict [FRESH] and id (neg:1) are both pinned in the rendered line, the abstention's witness payload (underApproxSources) and its asNodeKey id are never checked by any assertion in this suite — the tested render contract for abstentions stops at kind, relation, target, scope, and the reason string.\"}\n```"