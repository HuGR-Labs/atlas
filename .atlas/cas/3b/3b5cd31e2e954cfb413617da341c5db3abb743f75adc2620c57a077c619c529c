"STEP 1 — Reading the bytes.\n\nCandidate drafts and refutation passes:\n\n- *“No production callers / tracked as task #36”* — REFUTED. That is a comment's claim about other files and an issue tracker; nothing in this unit's code shows who calls or doesn't call it. Discard.\n- *“Byte-identical output every run (KERNEL-1)”* — REFUTED. Depends entirely on `canonicalForm`'s internals in `@atlas/kernel`, not visible here. Discard.\n- *“Path is `<outDir>/<seat>.poke.json`”* — REFUTED as trivial: that is literally the body of `pokeFilePath`, i.e., a restatement of the code plainly visible.\n- *“Same seat + different scopes overwrite the same file”* — weakly surviving but essentially derivable from the signatures alone (`pokeFilePath(outDir, seat)` takes no `scope`). Low information beyond the signature. Set aside.\n- *“Seat is interpolated unsanitized into the filename”* — the absence-of-validation part is checkable, but the interesting consequence (path escape via `..` or `/`) leans on `node:path` join semantics not visible in these bytes. Shaky per the rubric. Discard.\n- **Ordering of effects**: the body's control flow is `source(seat, scope)` → `canonicalForm(surface)` → `pokeFilePath(...)` → `mkdirSync` → `writeFileSync` → `return`. The imports list exactly two fs operations (`mkdirSync`, `writeFileSync`), both occurring strictly after the source call and serialization. Therefore, if `source` throws or `canonicalForm` rejects, the function has performed **zero** filesystem effects — `outDir` is not even created. This is checkable purely from this unit's declarations and statement order, is not stated by any comment (the comments mention “created if missing” but never the ordering relative to the source call), is not in the signature, and changes what an engineer does (e.g., cleanup assumptions, tests asserting that failed pushes leave no trace). SURVIVES.\n\nSTEP 2 — Emitting the survivor.\n\n```atlas-fact\n{\"claim\": \"materializePoke invokes the injected source and runs its result through canonicalForm BEFORE any filesystem operation — mkdirSync and writeFileSync come last — so a throw from source or serialization leaves the filesystem completely untouched, with outDir not even created.\"}\n```"