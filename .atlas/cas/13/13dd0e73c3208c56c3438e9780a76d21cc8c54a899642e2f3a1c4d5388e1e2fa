"STEP 1 — scratch reasoning:\n\nCandidate: \"`validateTemplate` only checks that the input is a non-null, non-array object — it never inspects any property, so an empty object passes.\"\n\nRefutation attempt:\n- Derivable from these bytes? Yes — the entire body is `return typeof fact === 'object' && fact !== null && !Array.isArray(fact);`. There is no property access anywhere in the unit's executable code. I scanned the whole file: the only logic is that three-clause conjunction (checked item by item).\n- Resting on a comment's claim about unseen code? No — I will phrase it strictly around the visible body, not the header's [RESIDUE] note or claims about where the field set lives.\n- Exhaustive-count risk? Mild; I'll phrase qualitatively (\"even a completely empty object\") rather than asserting a numeric inventory, and \"no field/cap logic in this unit\" is verifiable by scanning the shown bytes.\n- More than a name/signature restatement? Yes — the name `validateTemplate` and the `(fact: unknown) => boolean` signature suggest real template-field validation; nothing in the signature reveals that `{}` satisfies it. That gap would change what an engineer does (they cannot rely on it for field-level gating).\n\nIt survives. STEP 2 — emit:\n\n```atlas-fact\n{\"claim\": \"validateTemplate's entire body is just `typeof fact === 'object' && fact !== null && !Array.isArray(fact)` — it never reads any property, so a completely empty object {} (or any non-null, non-array value) passes, and no required-field or cap check exists anywhere in this unit.\"}\n```"