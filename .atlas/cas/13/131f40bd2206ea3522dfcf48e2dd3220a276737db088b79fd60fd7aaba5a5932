"STEP 1 — scratch reasoning (discarded candidates):\n\n- Candidate: \"`runMine` accepts `Partial<MineDeps>` directly as its options argument.\" Verifiable in bytes (`seededNoProposer` is typed `Partial<MineDeps>` and passed straight through), but it mostly restates how the calls are shaped; low action value.\n- Candidate: \"An empty pass exits 0.\" Directly asserted (`exitCode).toBe(0)` twice), but plausible from convention alone; weaker surprise.\n- Candidate: \"'seeded 0' counts admitted facts, not visited sites.\" Interesting (test 2 asserts `'seeded 0'` alongside `FRONTIER.length` sites visited), but the precise meaning of \"seeded\" leans on interpretation beyond the assertions.\n- Survivor: the four-directional pinning of the explanation copy. Checked item by item against the bytes — test 1 asserts `toContain('wiring a model would not change this 0')` AND `not.toContain('no proposer model is wired')`; test 2 asserts `toContain('no proposer model is wired')` AND `not.toContain('0 sites visited')`. All four are literal `expect` calls in this unit, no external code or comment claims needed, no counts enumerated. This goes beyond the filename (\"explained\") because it specifies the explanation must be state-dependent and is guarded by negative assertions in both directions — unifying the copy naively would flip tests red either way. Survives.\n\nSTEP 2:\n\n```atlas-fact\n{\"claim\": \"The suite pins the empty-run explanation bidirectionally: the zero-sites-visited run must print 'wiring a model would not change this 0' and must NOT print 'no proposer model is wired', while the visited-and-all-abstained run must print 'no proposer model is wired' and must NOT print '0 sites visited'.\"}\n```"