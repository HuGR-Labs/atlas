"**STEP 1 — scratch reasoning**\n\nCandidates drafted and stress-tested against the bytes:\n\n1. *\"Unverifiable rows exit non-zero\"* — discarded as too close to the file's advertised purpose (the header and test titles state it outright).\n2. *\".atlas/ existence alone satisfies the entry guard\"* — survivable: the second wrong-dir test writes **only** an empty `.atlas/` via `mkdirSync` and still gets `calls === 1` and `code === 0`, so the guard's sufficiency condition is demonstrated by the test's own setup, not merely the comment. Kept as backup.\n3. *\"Exit code 2 is overloaded\"* — checking item by item against the literals: wrong-dir refusal asserts `code` toBe 2 with `calls` toBe 0; broken-alone asserts `cv.exitCode` toBe 2; unverifiable-alone asserts 2; mixed asserts 2; all-re-proven and empty-store assert 0; not-composed asserts 1. No comment is load-bearing here — every mapping is an assertion literal in this unit. The two exit-2 populations are behaviorally distinct (one never invokes the thunk and prints the resolved path; the other prints per-row verdicts), yet share the code. Not stated by any name or signature; not a count claim; not refutable from the shown bytes. This survives and is the more operationally consequential of the survivors.\n\n**STEP 2 — result**\n\n```atlas-fact\n{\"claim\": \"On this CLI surface, exit code 2 is shared by two unrelated failure modes — invoking verify-store in a directory with no .atlas/ (which refuses before ever calling the injected reverify thunk and prints the resolved path) and a rejected verdict whose rows include broken or unverifiable outcomes — so exit code alone cannot distinguish 'ran in the wrong place' from 'the store failed re-verification'.\"}\n```"