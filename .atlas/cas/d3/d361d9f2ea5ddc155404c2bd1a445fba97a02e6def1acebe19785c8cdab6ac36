"STEP 1 — scratch reasoning (not stored):\n\nReading the two fixture builders closely: `committedProjectionOnlyRepo()` writes `.atlas/projection.json` with payload `'{\"current\":[],\"cas\":[]}'`; `committedStagingRepo()` writes `.atlas/staging.json` with payload `'{\"current\":[],\"cas\":[]}'`. Character-for-character identical JSON — both stores are completely empty (empty `current`, empty `cas`). Yet the S20 describe block asserts the staging variant makes `query` / `doctor hotset` / `node` exit non-zero with `untrusted-store` on stdout, while S20b asserts the projection variant exits 0 on the same commands.\n\nCandidate fact: the serve-vs-refuse decision hinges on *which* `.atlas` file is tracked, not on what the store contains — since both fixtures hold content constant at an empty payload.\n\nRefutation attempts:\n- Derivable from these bytes? Yes: the two identical string literals plus the divergent exit-code assertions are all present here. No reliance on harness internals (`makeFixtureRepo`, `runAtlas` bodies are unseen, but their return value usage and the CLI invocations/assertions are fully visible).\n- Resting on a comment's claim about elsewhere? The ADR-0008/TRAVEL-BY-REPROOF prose talks about replayability and witnesses elsewhere (`read-access.ts`), but my claim doesn't need it — it rests only on the literals and the `expect` calls in this file. Discarding any comment-derived framing like \"unratified candidate material\".\n- Exact count/enumeration risk? \"Both fixtures\" = exactly two helper builders, counted and re-checked; both payloads compared char-by-char and match. Safe.\n- Restatement of name/signature? No. The test names say \"committed (empty) projection\" (so projection's emptiness is named) but never reveal that the *refusing* staging store is equally empty — a reader would naturally assume staging must contain candidate material to matter. The header prose (\"unratified… no replayable witness\") even invites a content-based mental model that these bytes contradict.\n\nSurvives. STEP 2 — emit:\n\n```atlas-fact\n{\"claim\": \"Both committed-store fixtures write byte-identical empty payloads ({\\\"current\\\":[],\\\"cas\\\":[]}), differing only in the tracked filename — so committing .atlas/staging.json trips the untrusted-store refusal while committing .atlas/projection.json is served, meaning the tripwire discriminates on which store file is tracked rather than on any store content.\"}\n```"