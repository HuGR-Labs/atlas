"STEP 1 — Reasoning:\n\nCandidates drafted from a close read of the bytes:\n\n1. *\"tokenEstimate sums claim character lengths over both governing and advisory bands.\"* — Verifiable: the two `reduce` calls cover `bands.governing` and `bands.advisory`, not `advisoryDropped`. Survives refutation, but it's a compound observation and borders on summarizing the visible arithmetic.\n\n2. *\"`resolveFreshness` collapses any thrown value into `DRIFTED`.\"* — Verifiable via the bare `catch {`. Survives, but the catch-and-return-literal behavior is close to what the code plainly shows, and the comment already narrates it heavily.\n\n3. *\"`atLeastT1` is never called within this module; `mintPack` bands exclusively through the imported `splitBands`.\"* — Refutation pass: scanning every declaration and call site in the bytes — `resolveFreshness`, `factToInvariant`, `PackFrame`, `mintPack` — `atLeastT1` appears exactly once, at its own `export const` declaration, and zero times as a callee. `mintPack`'s banding is a single `splitBands(rows)` call. This rests on nothing outside the unit: no comment-trust needed, no counts beyond confirming zero references, no assumptions about `splitBands` internals (the claim only asserts that edits to `atLeastT1` cannot flow into `mintPack`, which follows from absence of any call edge). A competent engineer seeing an exported predicate named `atLeastT1` next to a pack-minting function in a file rhetorically centered on \"bounding at the MINT\" would naturally assume the local predicate is the enforcement point; discovering it has no caller in this file changes what they do — tightening the tier rule means editing `splitBands` upstream, not this copy.\n\nCandidate 3 survives cleanly. Emitting it.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"Although this module exports `atLeastT1` as its stated governance predicate, nothing in the file ever calls it — `mintPack` performs all banding through the imported `splitBands`, so edits to `atLeastT1` cannot affect the packs this unit mints.\"}\n```"