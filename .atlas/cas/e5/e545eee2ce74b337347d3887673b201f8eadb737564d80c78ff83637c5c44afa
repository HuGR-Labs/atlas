"STEP 1 — scratch reasoning (not persisted):\n\nCandidates drafted and tested against the bytes:\n\n1. *\"admitToBuffer has no production caller\"* — refuted. This rests entirely on the header comment's claim about `transcript-store.ts` and other files not shown. Not re-derivable from this unit's declarations. Discard.\n\n2. *\"Non-byte inputs are withheld as empty\"* (`asBytes` falls through to `new Uint8Array(0)`) — derivable from the code, but the signature already promises `Uint8Array`, so a competent typed caller gains little; weak actionability. Discard.\n\n3. *\"Seam state lives in a WeakMap keyed on the returned buffer's identity\"* — visible in code (`SEAM.get/set`), but largely restated by the co-located comment; the interesting consequence (copies sever state) requires reasoning past the shown bytes. Weaker than the next candidate.\n\n4. *\"The gate is not append-only\"* — checking the control flow directly: `back` comes from `known.back` (or `MAX_SEAM_CARRY` for foreign buffers), `keep = prev.length - back`, and only `prev.subarray(0, keep)` is copied into `out`; the region `[keep, prev.length)` is overwritten by `emitted`, which begins with the re-rendered decision of the carry. So the returned buffer's tail can differ from `existing` even before any of the new chunk's content appears. Survives all refutation checks: it is derived from THIS unit's declarations and assignments, not from any comment's claim about unseen files; it asserts no count or exhaustive list (MAX_SEAM_CARRY is referenced by name only, value unasserted); and it contradicts the natural reading of both the name (\"append\") and the signature shape (`(existing, chunk) → existing + something`). It changes what an engineer does: naive diffs between successive admits, or assumptions that the previously emitted tail is final, are wrong — the tail is rewritable up to the carry bound.\n\nSurvivor: candidate 4. STEP 2 follows.\n\n```atlas-fact\n{\"claim\": \"Despite its 'append' framing, admitToBuffer is not append-only: it discards the trailing provisional region of the previously returned buffer (known.back bytes, capped at MAX_SEAM_CARRY, or the widest window for a buffer it did not produce) and rebuilds that region from the re-decided carry, so the returned buffer's tail can differ from `existing` even before the new chunk's own bytes begin.\"}\n```"