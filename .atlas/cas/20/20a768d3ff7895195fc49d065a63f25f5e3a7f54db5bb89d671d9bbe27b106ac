"STEP 1 — Reasoning (scratch):\n\nScanning the bytes for something not inferable from \"sidecar.test.ts\" and not resting on comments about unseen code (mutant notes about `publish`, `sidecar.ts` internals, #112/#83 references, \"measured RED\" history — all discarded as claims about elsewhere/past).\n\nCandidates:\n- \"MAX_ATTEMPTS is 64\" — the literal 64 is here, but it only pins what the test plainly asserts; weak actionability.\n- \"Up to 10% refusals tolerated (≥36 of 40)\" — verifiable (8×5, ×0.9), but mostly a restatement of the assertion on screen.\n- **DIST dependency**: `const DIST = join(dirname(fileURLToPath(import.meta.url)), '..', 'dist', 'src', 'store.js')`, interpolated into the spawned child's import. Consequences traceable entirely within these bytes: crashed/unbuilt children produce empty stdout → the fallback `{\"committed\":[],\"refused\":[]}` → `expect(committed.length + refused.length).toBe(W * K)` fails wholesale; and a stale `dist` means the flagship concurrency regression validates previously compiled logic, not current `src`. Note scope precision: the *parent* imports `../src/*.js` (vitest-resolved TS), so only the child-process leg runs against dist. No contradiction found; not leaning on library folklore beyond ordinary module resolution of a literal path; not a name/signature restatement.\n\nSurvives. Emitting that one fact.\n\n```atlas-fact\n{\"claim\": \"The multi-process concurrency regression does not run against the TypeScript sources: each spawned writer child imports the store from the compiled bundle resolved at '../dist/src/store.js', so the test's outcome tracks the last build — with no dist every child crashes to an empty report and the every-attempt-accounted assertion fails wholesale, while edits to src without a rebuild leave the suite silently validating stale compiled code.\"}\n```"