"STEP 1 — REASON FREELY:\n\nReading the bytes closely. Candidates drafted and tested:\n\n- *\"`tierAtLeast` fails closed for off-lattice values\"* — the fail-closed property rests entirely on the comment's claim about `isWeakerTier`'s behavior in `@atlas/knowledge`, which is not in these bytes. The `unknown` parameter widening IS visible, but the safety consequence is comment-borrowed. Discard.\n- *\"The `GenesisBudget` passed to `escalate` is ignored\"* — true in the body (`_budget` never referenced), but the `_` prefix convention lets a competent engineer infer disuse from the signature itself. Weakens the \"wouldn't already know\" criterion. Discard as marginal.\n- *\"Scope matching is a raw `startsWith` prefix with no path-segment boundary\"* — derivable from the body, but once the one-line body is read it is essentially a restatement of what the code plainly does. Discard as thin.\n- **Survivor**: the interaction between `runsRefuter` and `decideMechanisms`. From signatures alone (`runsRefuter(tier): boolean { return tier === 'T0' }` and the header's \"refuter for T0 ONLY\"), an engineer would predict every T0 candidate gets a refuter. But the control flow of `decideMechanisms` shows `if (!shouldEscalate(sig)) return [];` before the refuter push — so the refuter is emitted only when the site is ALSO `highValue && uncertain`, making `runsRefuter === true` necessary but not sufficient. Verified against the body line by line: early return, `self-consistency` push, conditional `check-synthesis` push, conditional `refuter` push. No counts asserted, no reliance on other files, derivable purely from this unit's control flow, and it changes cost prediction / oracle-stubbing behavior.\n\nSTEP 2 — EMIT THE RESULT:\n\n```atlas-fact\n{\"claim\": \"Although runsRefuter returns true for any 'T0' tier, decideMechanisms emits the refuter mechanism only for candidates that also pass the full escalation gate (highValue and uncertain), because it returns an empty list before reaching the refuter check otherwise.\"}\n```"