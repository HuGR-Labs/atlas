"STEP 1 — scratch reasoning:\n\nReading the bytes closely, candidates considered:\n\n- *\"The unit only ever returns exit code 0.\"* — True in code (single `return { exitCode: 0, ... }`), but the file header loudly announces the exit-code contract, so a reader of the unit likely knows it; and the \"exit 1 lives in cli.ts\" half rests on another file. Discarded as weak.\n- *\"`claimOf` returns '' for non-advisory facts.\"* — Code-checkable, but the function is one line and its doc comment spells out the behavior; borderline restatement of a trivially-read body. Kept as backup.\n- *\"Advisory rows count toward the `facts` gate in `nextLine`.\"* — Plainly visible arithmetic, and the adjacent comment already narrates it as a deliberate fix. Weak.\n- **The `relate:` strip**: `pack.drill.complement.pull.replace(/^relate:/, '')` inside the final `next:` branch. Nothing in the name `nextLine(out): string` or the types hints at this. Crucially, no comment explains it — it exists only in code, so it survives the anti-comment test. It embeds an unstated convention: pull handles may carry a `relate:` scheme that must be normalized before use as an `atlas query` argument. And the contrast is verifiable right here: the same field is printed verbatim on the `complement:` footer line (`complement: ${pack.drill.complement.pull}`), so the normalization is specific to the one call site, not a general property. Refutation checks: derivable from these bytes alone ✓; not resting on a comment's claim about elsewhere ✓; no exact count/exhaustive enumeration ✓; not derivable from name/signature/types ✓; actionable (refactoring the guidance, writing tests, or constructing query commands from pull handles without this would mislead callers) ✓.\n\nSurvivor: the targeted `relate:` prefix strip in `nextLine`.\n\nSTEP 2:\n\n```atlas-fact\n{\"claim\": \"In nextLine, the widen-the-briefing suggestion strips a leading 'relate:' prefix from pack.drill.complement.pull via the regex /^relate:/ before embedding it in the 'atlas query' command, even though the same field is echoed verbatim on the briefing's 'complement:' footer line — so pull handles may carry a scheme prefix that only this one call site normalizes.\"}\n```"