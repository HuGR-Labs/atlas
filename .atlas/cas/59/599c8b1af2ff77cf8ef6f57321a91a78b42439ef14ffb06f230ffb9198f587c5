"STEP 1 — scratch reasoning (not persisted):\n\nReading the bytes closely. Candidates drafted and attacked:\n\n1. *\"verifyRelation has no 'refute' verdict\"* — the union `'proven' | 'abstain'` is literally in the parameter's type annotation, so this is a restatement of the signature. Discard.\n2. *\"id is minted via relationKey, never trusted\"* — visible (`id: relationKey(...)`), but largely a paraphrase of the docstring and reasonably guessable from names; weak \"changes what they do\" value. Discard.\n3. *\"gate-0 runs before the mint so admit stays total\"* — the ordering claim lives in comments about the unseen harness/caller; not re-derivable from these bytes' control flow. Discard.\n4. *\"undefined = advisory fall-through, never a drop\"* — depends on caller behavior not shown. Discard.\n5. **Survivor:** in `trySoundRelation`, the `score` argument is invoked as `score(relationClaimNormFromWitness(witness))` — the scored text is the witness-derived sentence (`${w.sourceScope} ${w.relationKind} ${w.target} (witnessed cross-unit reference, sound oracle)`), *not* `${p.endpointA} ${p.relationKind} ${p.endpointB}` from the co-exported `relationClaimNorm`. Refutation pass: derivable purely from this file's code (the return line plus both template literals)? Yes. Resting on any comment's claim about another module? No — both string shapes are defined right here. Exact-count/enumeration risk? None. Knowable from name/signature alone? No — `score: (claimNorm: string) => ObviousnessScore` reveals nothing about which text flows in. Actionable? Yes: anyone mocking `score`, caching scores, or assuming the two claim-norm helpers produce interchangeable text for dedup/scoring parity would be wrong — the sound path scores a differently shaped, witness-legged string. Survives.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"In trySoundRelation, the injected obviousness scorer is fed the witness-derived sentence from relationClaimNormFromWitness ('sourceScope relationKind target (witnessed cross-unit reference, sound oracle)'), not the endpoint-pair triple produced by relationClaimNorm.\"}\n```"