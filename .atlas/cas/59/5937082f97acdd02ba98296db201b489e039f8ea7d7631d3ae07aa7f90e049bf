"Reading the bytes: this is a private ESM workspace package whose entire content is metadata — name/version, dual `main`+`exports` entry points into `dist/src/`, five `@atlas/*` sibling dependencies pinned with bare `*` ranges, and exactly two scripts.\n\nDrafting candidates:\n\n1. *\"Dependencies use `*` ranges, so the workspace tool resolves versions\"* — the `*` literals are in the bytes, but the mechanism (workspace hoisting) is external convention, not shown here. Weakens the claim; discard the mechanism part. What remains is near-restatement of visible lines.\n\n2. *\"No subpath exports, so deep imports are blocked\"* — true from the shown `exports` map, but it's an absence-summary of what's plainly printed; low action-value beyond reading the field.\n\n3. *\"The `build` and `typecheck` scripts contain the byte-identical command string `tsc -b`\"* — directly verifiable by comparing the two script values in this unit: `\"build\": \"tsc -b\"` and `\"typecheck\": \"tsc -b\"`. No comments exist in the file, so nothing rests on comment claims. No counts asserted. This is a relational fact requiring comparison of two lines, not restatement of any single field. Action-changing: an engineer cannot assume `typecheck` is a lighter no-emit-style check distinct from build — here the two entry points diverge in name only, so editing/caching/treating them separately would be wrong, and whatever side effects `tsc -b` has apply to both.\n\nSurvivor: candidate 3, phrased strictly within the shown bytes.\n\n```atlas-fact\n{\"claim\": \"In this package.json the build and typecheck scripts hold the byte-identical command string \\\"tsc -b\\\", so running typecheck invokes exactly the same full TypeScript project-build command as building, differing from build in name only.\"}\n```"