"```atlas-fact\n{\"claim\": \"In this suite the admit gate is constructed from buildMineAdmission with an empty documents list ({ documents: [] }), yet the live-path seed carrying the deliberately stale subtreeHash 'not-the-current-hash' is still expected to be admitted (emitted === true), with only the deleted-path seed refused — so having no recorded documents does not block gate admission.\"}\n```"