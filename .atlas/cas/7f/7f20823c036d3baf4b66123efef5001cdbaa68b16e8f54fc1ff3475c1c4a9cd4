"STEP 1 — scratch reasoning (not persisted):\n\n- Candidates drawn from the visible fixtures/expectations: (1) holes only gate the prove-branch (test 1: in-scope caller + in-scope hole ⇒ still `refuted`; test 3: no caller + in-scope hole ⇒ `abstain/scope-open`) — but the describe title itself prints \"refute any-world, prove closed-world\", so part of this is arguably given by the name/label; (2) the phantom-target behavior: `callers: [], resolvesTarget: false` ⇒ `abstain('target-unresolvable')` — nowhere in the name, the import, or the call signature is it visible that the verifier cross-checks resolvability rather than trusting the empty caller array; (3) segment-wise scope containment (sibling dir `src/paycheck` neither refutes nor opens scope) — derivable from literals, but the surrounding comments frame it via an external trap number; the behavior itself is still readable from input/output pairs; (4) harness details (`reverseCallers` answers only for the exact TARGET symbol; `pathOfHash` is `String(h)`).\n- Refutation pass: candidate (2)'s comment mentions \"#220\", but I can state the fact without it — the fixture (`resolvesTarget: false`) plus the expected `{verdict:'abstain', reason:'target-unresolvable'}` fully determine it, and nothing else in the bytes contradicts it. It is qualitative (no counts), rests only on literals present here, and goes beyond what `verifyNegation(claim, reverseApi, pathOfHash, isLocal)` suggests — the signature implies caller lookup, not an independent existence/resolvability probe of the target. Candidate (1) survives too but leans closer to the printed describe label; (2) is strictly non-obvious from name/signature and changes what an engineer does (e.g., a fake API missing `resolves`/`definesAt` silently breaks the prove path; consumers learn \"zero callers ≠ proven\" for unresolvable targets).\n- Survivor: candidate (2).\n\nSTEP 2:\n\n```atlas-fact\n{\"claim\": \"An empty caller list alone cannot produce 'proven': when the target does not resolve (fixture resolvesTarget: false), verifyNegation returns abstain with reason 'target-unresvable' — i.e., it independently probes target resolvability rather than treating reverseCallers' empty result as proof of absence.\"}\n```"