"STEP 1 — SCRATCH REASONING\n\nReading the declarations closely, ignoring everything comments assert about files not shown (governed-emit-identity.ts, knowledge package paths, WP numbers, mine-decide.ts, etc.):\n\nDraft candidates:\n\n1. \"Exactly two grounding entries for RelationProposal\" — the type is `FactGrounding`, an indexed access off an unseen `AdvisoryNode`; the two-entry constraint exists only in a comment. Cannot verify cardinality from these bytes. DISCARD.\n\n2. \"`derivation` persists while `scratch` is discarded\" — persistence behavior lives in downstream code not shown; here it's only comment assertion. DISCARD.\n\n3. Interface-name/literal mismatch: `Abstention` uses `kind: 'abstain'`. True and in the bytes, but it's essentially reading one literal aloud — too close to restating the type. WEAK, set aside.\n\n4. Anchoring shape differs across the union: checking member by member — `PredicateProposal.site: Candidate` ✓, `AdvisoryProposal.site: Candidate` ✓, `RelationProposal.site: Candidate` ✓, `NegationProposal.site: Candidate` ✓, `TransitionProposal`: no `site`, has `unitKey` ✓, `TestVacuityProposal`: no `site`, has `unitKey` ✓, `Abstention`: neither ✓. So the claim \"only Transition and TestVacuity carry `unitKey` instead of `site`, and Abstention carries neither\" is fully re-derived from the declarations in THIS file, independent of any comment (even though a comment echoes part of it, the code alone establishes it). It is not knowable from the module name or the union's member list alone, and it changes what producer/dispatch code must do (narrowing on `kind` before touching `.site` vs `.unitKey`). Enumeration verified item-by-item against the bytes. SURVIVES.\n\n5. Negation's `target`/`scope` required vs Relation's optional — true and checkable, but less consequential than #4; only one fact may be emitted.\n\nEmitting the survivor.\n\n```atlas-fact\n{\"claim\": \"In the Proposal union, only TransitionProposal and TestVacuityProposal identify their subject with a bare unitKey field, while the predicate, advisory, relation, and negation variants all carry a site: Candidate anchor and Abstention carries neither.\"}\n```"