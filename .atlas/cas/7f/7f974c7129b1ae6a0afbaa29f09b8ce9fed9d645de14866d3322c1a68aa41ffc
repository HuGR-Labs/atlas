"Looking at this unit closely:\n\n**Drafting candidates:**\n1. *\"CEILING_CAP is 200\"* — the literal 200 appears only in a test title string and comment; the assertion compares against the imported constant, so the numeric value isn't derivable from these bytes. **Discard.**\n2. *\"Resume replays the interrupted site\"* — the `expect(h.visited).toEqual(['s3','s4'])` line encodes intended behavior of `../src/run-controller.js`, which isn't in these bytes; also largely implied by the scenario name. **Discard.**\n3. *\"corruptSite omits `throwOnce` so it throws persistently\"* — true and visible, but marginal action-value. **Weak.**\n4. *Harness mismatch*: the `Harness` interface comments `readonly visited: string[]; // qualifiedPath per visit, in call order`, but the actual `visit` fake computes `const key = c.site.qualifiedPath.split('::')[1] ?? c.site.qualifiedPath` and pushes **that** — only the symbol-id segment after `::`, never the full path (confirmed by assertions comparing against bare ids like `['s1','s2','s3']`, `['sb']`). The same `key` drives `throwAt` matching. **Survives:** derivable purely from these code bytes, directly contradicts the in-file comment (so it's not stale-comment-dependent — the code itself refutes the comment), invisible from the type/signature alone, and changes what an engineer writes (assertions and `throwAt` values must be bare symbol ids, not `file.ts::id` strings).\n\nNo exact-count or exhaustive-enumeration risk; the claim is qualitative and verified against the code and the literal comparison values.\n\n```atlas-fact\n{\"claim\": \"In the deps harness, the visit fake records into `visited` only the symbol-id segment of each site's qualifiedPath (the part after `::`), not the full path as the Harness interface comment claims, and the throwAt interruption option matches against that same extracted segment.\"}\n```"