"Looking at this unit: it's a self-contained HTML design doc, so most of its prose describes a system (`atlas/` folder, seats, MCP surfaces, spec files like `docs/spec/atlas.md`) that lives *outside* these bytes — per my own refutation pass, all of that gets discarded as claims about unseen files. What survives must come from the markup/CSS actually shown.\n\nDraft candidates:\n\n1. \"No script tag loads mermaid\" — plausible gotcha, but the unit lacks doctype/html/body, i.e. it reads as a fragment of a larger file; asserting global absence overreaches the shown bytes. Discard.\n2. \"TOC links match all ten section ids / bar segments sum to 100%\" — verifiable but a plain restatement of visible structure; wouldn't change what an engineer does. Discard.\n3. Theming: checking each dark definition against the base `:root` — the base block defines `--score-paper: #EAE5D8` and `--score-line: #B9B09A`; the `prefers-color-scheme: dark` block redefines paper/panel/accent/seal/etc. but **omits** the score vars; `:root[data-theme=\"dark\"]` repeats the identical set and also omits them. Meanwhile `.score` consumes `var(--score-paper)` / `var(--score-line)` and `.score-cap` hardcodes `#6a6350`, and the mermaid inline init pins light colors (`'#EAE5D8'`). Net effect readable straight off these bytes: in dark mode everything else flips, but the score/diagram panel stays parchment-light. Refutation check: any counter-evidence in the bytes? None — I listed every declaration in both dark blocks; no score-var override exists. It's qualitative (no risky count), derived solely from this unit's CSS, and it changes what a theming engineer does (they'd otherwise chase why the figure looks out of place, and now know exactly which vars/literals to touch).\n\nCandidate 3 survives.\n\n```atlas-fact\n{\"claim\": \"Dark mode never reaches the diagram panel: neither the prefers-color-scheme dark block nor the data-theme=dark selector redefines --score-paper or --score-line (and .score-cap hardcodes #6a6350), so the .score panel and its mermaid figure keep the light parchment palette while the rest of the page themes dark.\"}\n```"