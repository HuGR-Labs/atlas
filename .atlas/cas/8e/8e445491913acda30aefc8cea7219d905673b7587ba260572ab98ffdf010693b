"Looking at the bytes: the boundary test drives the same decision body twice over fresh temp stores, with the rival chain seeded from `next` (so it carries the victim row) up to `top` ∈ {5, 6}. The expected outcome flips from `'wrote'` at top 5 to `'skipped'` at top 6 — yet the post-loop `expect(durable()).toContain('victim')` runs against the *last* (top=6) store, where the rival generations 3..6 were chained from `next`, so the victim row is on disk. Attempting to refute: the `'skipped'` expectation and the durability assertion are both literal code (the loop's tuple table and the final `expect`), not comment claims; the numbers 5/6 and generations 1–6 are enumerated directly in the source, not asserted about unseen modules. The mismatch is real and intentional per the paired assertions, not an artifact of rollback (even if gen 2 were reclaimed, the rival chain seeded from `next` carries `victim`). This survives: it is not derivable from the file name (\"commit-superseded\"), and it would change what an engineer does — anyone auditing report-vs-disk consistency would flag this divergence as a bug unless they know the code deliberately selects the direction that can only under-report at the unprovable boundary.\n\n```atlas-fact\n{\"claim\": \"In the boundary test, the identical commit is expected to return 'wrote' when the rival chain tops out at generation 5 but 'skipped' at generation 6, while 'victim' is asserted durable in both halves — so at head 6 the report deliberately diverges from disk state, choosing the under-reporting direction.\"}\n```"