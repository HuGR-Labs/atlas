"STEP 1 — REASON FREELY\n\nReading the bytes closely.\n\nCandidates drafted:\n\n1. *\"GOVERNANCE_SURFACE has exactly five tools / WRITE_PATHS has two\"* — the numbers appear only as assertions in this test; their truth rests on `../src/handler.js`, which is not in these bytes. The assertion's existence is plainly readable anyway. **Discard** (truth rests outside the shown code; also a plain summary).\n\n2. *\"CLI and MCP both delegate to one handle via `render('cli'|'mcp', …)`\"* — visible immediately in `withDiff()`; a competent engineer gets this from reading the helper. Restates what the code plainly does. **Discard.**\n\n3. *\"The handle exposes both `diff(a,b)` and `render(tag,a,b)`\"* — inferable from call sites, but it is a direct transcription of usage; nothing hidden. **Discard** as plain-summary.\n\n4. *\"The `JSON.stringify(out) === JSON.stringify(Δ)` assertion is stricter than the preceding `toEqual` checks (key-order/key-set sensitive)\"* — real nuance, derivable from bytes plus core ECMAScript semantics, but a careful reader sees the assertion directly; borderline plain-summary, and leans on interpreting serialization semantics. **Weak — set aside.**\n\n5. *\"The injected `source` stub is exact-pair matched and order-sensitive: `diff` returns Δ only when called as `(shaA, shaB)` in that order (compared with `===` on the runtime string values `'cas:aaaa'`/`'cas:bbbb'`); every other argument combination — including `(shaB, shaA)` — silently returns an empty delta (all partitions empty arrays), never an error.\"*\n   Refutation pass: derivable solely from the literal `(a === shaA && b === shaB ? Δ : { added: [], … })` in these bytes ✓; no dependence on any comment or unseen module ✓; the \"all partitions empty\" enumeration is checked item-by-item against the fallback literal (added, edited, superseded, decayed — all `[]`) ✓; not knowable from the name `source` or the `DiffSource` type alone (whose definition is not even shown) ✓; and it materially changes what an extender does — any new scenario pinned to other shas, or to reversed order, resolves to silent empties, producing vacuously-green assertions unless the stub matcher is extended. **Survives.**\n\nEmitting candidate 5.\n\nSTEP 2 — EMIT THE RESULT\n\n```atlas-fact\n{\"claim\": \"The test's injected diff source is exact-pair matched: it returns the fixture delta only when called with (shaA, shaB) in that order (=== comparison on the runtime strings 'cas:aaaa'/'cas:bbbb'), and silently returns an all-empty delta for every other argument combination — including the reversed (shaB, shaA) — rather than erroring, so naively extended scenarios against other shas would resolve empty and pass vacuously.\"}\n```"