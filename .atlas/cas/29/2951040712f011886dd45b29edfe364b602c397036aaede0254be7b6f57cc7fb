"STEP 1 — scratch reasoning (not persisted):\n\nCandidates drafted from the bytes:\n\n1. *\"Never writes, never throws\"* — rejected: that's the header comment's assertion, and \"never throws\" depends on unseen `store`/`policy`/`@atlas/knowledge` behavior, not provable from this unit alone.\n2. *\"`isWeakerTier(tier, undefined)` is `true`, so the empty-derivedTier shape is unreachable\"* — rejected: rests on `isWeakerTier`'s behavior in another package plus a parenthetical comment claiming it; not checkable here.\n3. *Byte-fallback only when the row has no scope property at all* — survives as derivable (`legacyRow = rowScope === undefined; authorityScope = legacyRow ? stored?.scope : rowScope`), but I'll keep it as backup.\n4. *Refusal precedence* — verified item-by-item against the function body: exactly four `REJECTED_*` early returns, in order `REJECTED_UNAUTHORIZED_TARGET` → `REJECTED_UNVERIFIABLE_TARGET` → `REJECTED_RELOCATION` → `REJECTED_DOWNGRADE`. `weakerTier` is computed before the relocation check but consulted after it, so the relocation return preempts the downgrade return. Survives: derivable purely from sequential control flow, not stated by the signature, not resting on any comment's claim about other files, count of four verified against the shown returns.\n\nChoosing #4: it predicts which refusal string a multi-violating write surfaces and constrains where a new gate could be inserted — neither is knowable from the name/signature.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"Inside incumbentDecision the target-derived refusals fire in a fixed sequence — unauthorized-target, then unverifiable-target, then relocation, then downgrade — so a write that both relocates the node to another scope and declares a weaker tier is rejected as a relocation, with the downgrade never surfaced.\"}\n```"