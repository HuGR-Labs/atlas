"Looking closely at the bytes: the interesting non-obvious thing here isn't the attack scenarios themselves (loudly documented), but what the *fixtures* reveal about the persistence model. Checking candidates:\n\n- *Scenario numbering/order* — real (SCN-GE-I5 is declared last, after I11) but merely navigational; low impact.\n- *\"least-disclosure\" negative containment in SCN-GE-I11* — genuinely in the assertions, but heavily pre-advertised by its own section comment.\n- *realKey insensitivity to id/scope/tier* — supported by the premise assertions, but adjacent to what the header already announces.\n- *Where scope lives* — SCN-GE-I8 hand-builds a projection entry as literally `{ nodeKey, family, contentHash, claims }` with **no scope field**, yet expects the door to treat the node as unowned; SCN-GE-I10's `storedScope()` recovers scope by `store.get(asHash(cur.contentHash))`; SCN-GE-I5 makes `get` return `undefined` and the door fails closed with `unverifiable target`. Three independent code sites agree: governance metadata (scope, and whatever the door verifies about the target) is reached only through the content hash into CAS, never denormalized onto the entry. No comment advertises this; it's checkable purely from the literals and helpers in this file, and it changes what anyone writing fixtures or touching the store schema must do. Field-by-field check of the I8 literal confirms the four-key shape.\n\nThis survives refutation: it rests on code literals and control flow in this unit, not on any comment's claim about unseen modules.\n\n```atlas-fact\n{\"claim\": \"A persisted node's governing scope lives in the content-addressed CAS bytes rather than on the projection entry itself: SCN-GE-I8 hand-seeds the entry with only {nodeKey, family, contentHash, claims} and no scope field, and SCN-GE-I10's storedScope() helper reads scope back only by dereferencing store.get(asHash(entry.contentHash)).\"}\n```"