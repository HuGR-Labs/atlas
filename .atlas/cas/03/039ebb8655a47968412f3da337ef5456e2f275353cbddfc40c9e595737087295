"STEP 1 — scratch reasoning:\n\nCandidates considered:\n\n1. \"DOCTOR_SUBCOMMANDS has exactly five members\" — the sort-equality assertion demands `{archive, why, hotset, reground, index}`, but the actual membership lives in `src/doctor.js`, not these bytes; only the *demand* is local. Weak, partially name-implied (\"total\").\n2. \"`hotset` takes a numeric argument while siblings get `k:whatever`\" — visible, but trivially cosmetic; wouldn't change what an engineer does.\n3. \"DEAD_REV's stubs return false/undefined so an unrefused read fails quietly rather than crashing\" — derivable from the literal, but largely restates the adjacent comment's framing.\n4. The differential-outcome fact: the `index` test and the store-legs test use the **same** fixture (`committedStoreRepo()`), yet their assertions demand opposite results — each store leg exits non-zero with `untrusted-store` in stdout, while `index` is invoked with **no DoctorSource at all** (`undefined` second arg, index plan supplied by a third-argument thunk `() => reportIndexPlan(repoPath!)`) and is asserted to exit **0**, explicitly free of `untrusted-store`. Every element of this is in the shown code: the shared fixture call, the loop's two expectations, the `expect(cv.exitCode).toBe(0)` / `.not.toContain('untrusted-store')` pair, and the three-argument call shape. Not implied by the filename, not a comment-only claim about elsewhere (I drop the comment's rationale about `cli.ts`/exit 2, which I cannot see). Enumeration checked item-by-item: the four store legs are archive, why, hotset, reground — distinct and literally listed, so I name them rather than asserting a bare count. Survives refutation.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"On the very same committed-store repo fixture, the index leg is exercised with no DoctorSource at all (runDoctor called with undefined source and an injected () => reportIndexPlan(...) thunk) and is asserted to exit 0 with no 'untrusted-store' in stdout, while each of the other four legs — archive, why, hotset, reground, all driven through a real trusted-gated store — is asserted to exit non-zero with stdout naming 'untrusted-store'.\"}\n```"