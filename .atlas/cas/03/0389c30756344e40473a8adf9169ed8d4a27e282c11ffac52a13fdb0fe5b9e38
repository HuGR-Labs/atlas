"STEP 1 — scratch reasoning (not stored):\n\nCandidates drafted and refuted:\n\n- *Amendment/HONESTY-TAPROOT history*: the amended labels explain past contradictions with goldens-grd.md and commit f2a8659 — claims about other documents/history, not re-derivable from these bytes alone as actionable fact. Discard.\n- *\"isGrounded returns false for empty-hash entries\"* / drift outcomes generally: the assertions are here, but their truth rests on `../src/drift.ts` / `../src/subtree.ts`, which are not in these bytes. Discard.\n- *FNV constants 2166136261 / 16777619 in 10a-2*: present in the bytes, but essentially a literal transcription of what the code visibly does; low action value, borders on restating the body. Weak.\n- *Fixture shape (all nodes forced to axis 'spatial'/level 'item')*: verifiable, but mostly describes what the helpers plainly do. Weak.\n- *Seam-scan mechanics in 10b-2*: survives scrutiny. The guard is implemented **in this file's own code**: a `for` loop over exactly `['../src/subtree.ts', '../src/drift.ts']` (counted: two entries), reading each with `readFileSync(new URL(rel, import.meta.url))` and applying two regexes — while the module-level imports of the very same modules use `.js` specifiers (`'../src/subtree.js'`, `'../src/drift.js'`). Both halves of this mismatch are literal code in the shown bytes, not comment claims. It is not inferable from the unit's path or the test's title (\"no off-seam digest call site exists in this WP source\"), and it changes what an engineer does: it means the guard protects only those two source texts at test time, so a newly added module in this package would not be covered, and the check inspects `.ts` sources rather than anything built. Verified item-by-item against the loop literal and the import statements.\n\nSurviving fact identified.\n\n```atlas-fact\n{\"claim\": \"In SCN-GROUND-10b-2 the off-seam-digest guard is enforced by readFileSync-ing and regex-scanning the raw text of exactly two files, '../src/subtree.ts' and '../src/drift.ts', at test time — addressed with .ts paths even though this same file imports those modules via '.js' specifiers, so only those two source texts are protected and no other package module is scanned.\"}\n```"