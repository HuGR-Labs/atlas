"Reading the bytes: the edge-partition loop in `createDepgraph` guards the reverse-adjacency build with `e.kind === 'resolved' && e.to !== null`, and the `else` branch unconditionally does `unresolvedSources.add(String(e.from))`. So the classification is conjunctive: an edge that is *labeled* `resolved` but carries a `null` target fails the second half of the guard and falls through to the same treatment as `unresolved`/`dynamic` edges. Its source lands in `unresolvedSources`, which means that if that source is ever inside a queried node's scope, `underApprox` flips to `true` and the `coChanged` band for that source is unioned into the result. Notably, the header comment describes `unresolvedSources` as \"nodes with an outgoing `unresolved`/`dynamic` edge\" — the code is strictly broader than that description, so this is a behavior visible only in the control flow, not in the prose. It is not derivable from the name/signature (`edges: readonly DepEdge[]` says nothing about null-target handling), it involves no counts or enumerations, and it rests only on declarations and branches present in these bytes.\n\n```atlas-fact\n{\"claim\": \"In createDepgraph's edge partition, an edge labeled kind === 'resolved' whose to is null fails the e.kind === 'resolved' && e.to !== null guard and falls into the else-branch, so its source is recorded in unresolvedSources exactly like an unresolved/dynamic edge and can set underApprox and union the coChanged band for scopes containing it.\"}\n```"