"STEP 1 — scratch reasoning (not persisted):\n\nReading the bytes closely, several candidate facts emerge:\n\n- *Verdict asymmetry* (`dependency`/`count` never refute): stated plainly in the verdict table — informative but close to a direct summary of the documented contract.\n- *Abstain exits 0*: stated and shown (`# exit 0` vs `# exit 1` literals). Useful, but somewhat guessable from \"honest non-answer\" framing once read.\n- *Segment-wise scope containment* (`src` ⊇ `src/payments`, `sr` ⊄ `src`): concrete literal examples, changes assumptions about prefix matching — viable.\n- *The `--min` routing asymmetry*: the flag bullet says a `≤ 0` or non-integer `--min` \"abstains as malformed\", while the malformed-invocation sentence lists **missing** `--min` (alongside unknown kind, empty target/scope) as a structured error with non-zero exit, and the abstain paragraph pins abstain at exit 0. Nothing in the command name or signature (`<kind> <target> --scope <s> [--min <n>] [--exact]`) hints that an *invalid value* takes the abstain/success route while an *absent flag* takes the error/failure route — the opposite of typical CLI convention, where both would be usage errors.\n\nRefutation checks on the strongest candidate (the `--min` split):\n- Derivable solely from these bytes? Yes — three co-located statements (flag bullet, malformed-invocation enumeration, abstain exit contract) plus the exit-code literals in the transcripts. No reliance on `@atlas/genesis`, the e2e test path, `doc-transcript-guard`, or any issue number; I drop those references entirely.\n- Resting on a claim about unseen code? No — it is the documented contract of this command itself, corroborated by the shown transcripts.\n- Exact-count/exhaustive-enumeration risk? No — I contrast two specifically described input treatments rather than asserting the error-case list is complete.\n- Mere restatement of name/signature? No — the signature shows `--min` as required for count but says nothing about divergent handling of missing vs. malformed values.\n\nIt survives. Emitting the result.\n\nSTEP 2:\n\n```atlas-fact\n{\"claim\": \"For the `count` kind, a supplied `--min` that is <= 0 or non-integer produces an abstain verdict (the honest non-answer, exit 0), whereas omitting `--min` entirely triggers the structured-error path with a non-zero exit — so invalid flag values and missing flags take different failure routes.\"}\n```"