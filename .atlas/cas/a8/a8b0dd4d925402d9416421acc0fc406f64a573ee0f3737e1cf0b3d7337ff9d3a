"STEP 1 — scratch reasoning (not persisted): Reading the bytes, most content is either a restatement of the unit's evident purpose (generate test goldens at state S3, prove teeth via mutants) or pointers/comments asserting things about files not shown here (`DECOMPOSITION-PROTOCOL.md`, `FSPEC-merge §UP KERNEL-10`, IBM field data) — all discarded as not re-derivable from these bytes. Candidate facts about the generation ladder's five generator kinds and the Gate-3 mutant requirement are plainly restated in the procedure/output contract, so they fail the \"wouldn't already know\" bar. The strongest survivor is an internal contradiction fully contained in this unit: the YAML frontmatter declares `next_state: C-roadmap`, while the Completion Report section at the bottom instructs emitting \"next_state **S4**\". Both statements are in these bytes, neither is a comment about external code, and the disagreement is not inferable from the path/name/id alone — yet anyone wiring or executing this state machine must resolve which successor state is correct, so it changes what they do. Refutation attempts: no reading reconciles them (the same field/concept, `next_state`, is given two different values); it is not an exact-count claim; it does not lean on any library or convention outside the shown text. Survives.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"This unit contradicts itself about its successor: the frontmatter declares next_state: C-roadmap while the Completion Report section instructs emitting next_state S4.\"}\n```"