"STEP 1 — Reasoning (scratch):\n\nReading the bytes: four exports (`scrubClaimNorm`, `scrubCheck`, `scrubUnit`, `scrubGrounding`). Tracing each body: every one calls the private `scrubUtf8`, which is `Buffer.from(scrub(Buffer.from(s, 'utf8'))).toString('utf8')`. So all scrubbing funnels through one choke point that operates on encoded bytes, not raw JS strings.\n\nCandidate drafts and refutation attempts:\n- \"scrubGrounding leaves subtreeHash/kind raw\": the code shows this via the spread, but the *reason* lives in comments asserting properties of unseen structures; the surviving checkable core (\"only two fields rewritten\") collapses into a near-restatement of the object literal. Discard.\n- \"scrubCheck treats any non-'index-query' as assertion\": true from the ternary, but its actionable force depends on whether `Check` has more than two members — unknowable from these bytes without trusting the header's exhaustiveness claim about an external type. Discard.\n- \"@atlas/genesis appears only as a type-only import\": checkable in the import line, but that's visible at a glance and borders on restating the imports. Discard.\n- Shared-helper/Buffer-round-trip fact: survived. Verified item-by-item that all four exported bodies call `scrubUtf8` (scrubClaimNorm ×1, scrubCheck ×2 branches, scrubUnit ×1, scrubGrounding ×2 sites); the helper definition is in-file; no reliance on comments about `mine-decide.ts`, issue numbers, or other modules. None of the exported signatures (`string → string`, `Check → Check`, etc.) reveal that scrubbing happens on UTF-8 Buffers via one shared primitive, nor that all legs are therefore behaviorally identical — an engineer auditing or extending a new fact leg would otherwise have no in-file proof that claim/check/endpoint/grounding scrubbing cannot diverge.\n\nSurvives. Emitting.\n\n```atlas-fact\n{\"claim\": \"All four exported scrubbers route their input through the single private helper scrubUtf8, which encodes the string to a UTF-8 Buffer before invoking @atlas/persist's scrub and decodes the result back, so claim, check, identity-leg, and grounding text all undergo byte-identical scrubbing logic.\"}\n```"