"STEP 1 — Reasoning (scratch):\n\nScanning candidates against the bytes:\n\n1. *\"Notes go to `refs/notes/orchestra`\"* — the header comment asserts this, but the CODE uses `NOTES_REF`, an import from `@atlas/persist` whose value is not in these bytes. Refuted: rests on a comment's claim about a constant defined elsewhere.\n2. *Reads are total / return null on absence* — plausibly encoded in the `Forge` return types (not visible); also near-restatement of the `gitOrNull` helper's obvious shape. Weak.\n3. *PR/refspec state is per-instance in-memory* — true (`new Map`, `[]` in the closure), but largely telegraphed by the surrounding prose; secondary to the core mutation semantics.\n4. **Amend semantics of `writeCommit`** — checkable purely in code: `if (head !== sha) throw` forces the target to be HEAD; `git('commit', '--amend', ...)` rewrites the commit; `const atlasSha = git('rev-parse', 'HEAD')` captures the NEW sha; the note is attached to `atlasSha`; `writeCommit` has no `return` and the ledger stores only `{op, via}`, so the new sha never escapes the function. None of this is knowable from the name/signature `writeCommit(sha, trailer, note, via)`, which naively suggests writing into the given commit while keeping it valid. It materially changes caller behavior: the passed sha goes stale, non-tip targets fail loudly, and consumers must re-resolve HEAD themselves. Survives every refutation check: derivable from these bytes, no external-module reliance, no counts/enumerations, consistent with (but not dependent on) the comment.\n\nSurviving fact: the amend-and-supersede behavior of `writeCommit`.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"writeCommit requires its sha argument to equal the current HEAD and applies the trailer via git commit --amend, so the supplied commit is replaced by a new sha — computed internally as the note's attachment target but never returned to the caller.\"}\n```"