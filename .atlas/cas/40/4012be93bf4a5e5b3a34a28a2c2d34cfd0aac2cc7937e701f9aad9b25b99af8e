"STEP 1 — REASONING (scratch)\n\nScanning the bytes: this is a pure test file; `renderVerdict` lives in `../src/render.js` and is invisible here, so anything about *how* it works internally is off-limits. What IS in these bytes: input `Verdict` literals and exact expected stdout strings.\n\nCandidate A: \"The CLI used to drop verdict.data\" / GAP-B history — rests entirely on header/test comments describing past behavior and other modules. Discard.\n\nCandidate B: \"Unknown shapes append no block\", \"territories sorted\", \"byte-identical determinism\", \"rejected verdicts print no block\" — all essentially stated in the test titles themselves. Restatements of names. Discard.\n\nCandidate C: advisory rows print `[?]` while inv rows print real freshness — derivable from input-vs-expected literals, but weak impact; a formatting detail. Weak, set aside.\n\nCandidate D: In the reconcile test, the input `Verdict` carries `ok: true` and the same `guidance` object used throughout, yet the expected output begins `'status: rejected\\n'` — unlike the shared `PREFIX` constant (`'status: ok\\n...'`) that every other explicit assertion builds on — and the `data:` block still renders. Verify against bytes: input is `{ ok: true, data: { ..., exitCode: 2 }, guidance }`; expected string literally starts with `status: rejected` and continues `data:\\n  reconcile: ...`. Cross-check: PREFIX is used verbatim in tests 1, 3–6, 8, 9, and the ok-no-data case, all with `ok: true` — so the status line demonstrably varies independent of `ok`. Refutation angles: (a) external-file reliance? No — the contrast is between two literals inside this unit; the comment's mention of CLI-2/deriveStatus points elsewhere but my claim rests on the assertion bytes, not that comment. (b) Exhaustive count? I avoid \"every/N-of-M\"; qualitative form suffices. (c) Restatement of name/signature? The describe title covers only \"deterministic data: block\"; no title mentions the status flip. Survives.\n\nCandidate D is the survivor.\n\nSTEP 2 — RESULT\n\n```atlas-fact\n{\"claim\": \"A Verdict with ok: true whose data is the reconcile shape (containing exitCode: 2) is expected to render 'status: rejected' instead of 'status: ok' — unlike the shared ok:true prefix used throughout this file — yet its data: block still renders, so the printed status is not determined by Verdict.ok alone.\"}\n```"