"STEP 1 — REASONING (scratch):\n\nReading the bytes closely:\n\n- Candidate A: \"Over-budget runs admit nothing because the throw happens before the admission loop.\" — True and code-visible (budget check precedes the `for` loop over edges), but the inline comment states it verbatim (\"FAIL LOUD ... BEFORE admitting anything\"), and it's close to a summary of the plain control flow. Weaker candidate.\n- Candidate B: \"`relations` can be shorter than `resolvedEdgeCount` because unadmitted/anchorless edges are skipped.\" — Code-visible via the three `continue` guards, but largely a restatement of what the loop plainly does; the docstring already distinguishes \"attempted\" from proven.\n- Candidate C: \"The `target` on a collapsed A→B edge is deterministically the lexicographically first qualifying symbol.\" — Refutation attempt: does this rely on unseen code or comments? No. The mechanism is fully in these bytes: `definedSymbols` is iterated as `[...definedSymbols].sort()` (ascending), and the dedup guard `if (edgesByKey.has(key)) continue;` followed by `set(...)` means the first symbol encountered for a given `relationKey(pathA, 'depends-on', pathB)` wins and later ones cannot overwrite. So for a pair (A, B) where multiple distinct non-local defined symbols resolve to B and are called from A, the surviving `edge.target` (which becomes the R2 oracle leg on the proposal) is the sort-first such symbol. This is an emergent consequence of sort + first-wins dedup, not stated by the signature, and not dependent on the comment's claim about `reverseCallers`' internal ordering (for a fixed pair, each qualifying symbol is visited exactly once in sorted order regardless of caller ordering). It survives.\n- Candidate D: \"`calls` is never emitted\" — rests on the literal type plus a comment's external claim about SCIP; near-restatement of the declared type. Discard.\n\nCandidate C survives: it is checkable purely by reading the declarations and control flow here, it is not derivable from names/signatures alone, and it changes what an engineer does (e.g., predicting/debugging which symbol lands in the proposal's oracle-leg `target` for multi-symbol dependencies).\n\nSTEP 2 — EMIT:\n\n```atlas-fact\n{\"claim\": \"When multiple distinct non-local symbols defined in the same file are referenced from another file, the collapsed A→depends-on→B edge records the lexicographically first such symbol as its target, because defined symbols are enumerated in ascending sort order and the first edge minted for a given (endpointA, depends-on, endpointB) key wins and can never be overwritten.\"}\n```"