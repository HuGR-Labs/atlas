"STEP 1 — Reasoning scratch:\n\nCandidate A: \"The atlas-architecture reference entry's summary is written in Portuguese (`hierarquia de camadas …`) while every other entry is English.\" — Verifiable purely in the bytes; survives refutation attempts. But its behavioral payoff is weak (a style/i18n nit), and the instructions favor the fact that changes what an engineer *does*.\n\nCandidate B: \"There are 7 ADRs.\" — Requires counting; the number isn't itself the point, and exact counts are fragile. Discarded.\n\nCandidate C: The grounding story. The intro asserts reference is \"pinned to `source@sha` and drift-checked,\" yet the spec section, in the same unit, discloses that `spec/atlas.md` and `spec/memory.md` are the \"transitional normative source\" the reference files *currently* ground to, retiring only \"when the atlas **code** lands and reference re-grounds to `source@sha`.\" Both statements sit inside this unit, so the disclosure is checkable by reading the unit itself — no reliance on unseen code. Refutation pass: am I leaning on outside knowledge of the repo? No; both quotes are literal bytes here. Am I asserting an exhaustive enumeration? No. Is it a restatement of the filename? No — nothing about \"docs/README.md\" suggests the headline grounding guarantee is not yet operative. Would it change what an engineer does? Yes: they cannot treat the reference docs as machine-drift-checked against source today, and must treat `spec/` as the live normative anchor (e.g., when editing specs or wiring up future source-sha pinning). Survives.\n\nCandidate C wins over A on consequence.\n\nSTEP 2 — Emit result:\n\n```atlas-fact\n{\"claim\": \"Despite the intro's claim that reference docs are 'pinned to source@sha and drift-checked', this same README discloses that the reference files currently ground to the transitional spec/atlas.md and spec/memory.md documents and will only re-ground to source@sha once the Atlas code lands.\"}\n```"