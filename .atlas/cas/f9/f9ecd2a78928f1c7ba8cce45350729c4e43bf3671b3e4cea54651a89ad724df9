"STEP 1 — REASONING (scratch)\n\nCandidate A: “linkSameAs returns the identical input reference on self-links.” — Refuted: that’s an expectation about `../src/write/link.js`, code not in these bytes; the assertion’s truth can’t be re-derived here. Discard.\n\nCandidate B: “Coverage parity with wp-dedup-2 / the ~27s s16 subprocess.” — Comment claims about other files and process costs; nothing here verifies it. Discard.\n\nCandidate C: “Four non-vacuity counters must exceed zero.” — Code-visible (I did enumerate: withDerivedEdge, withDanglingPeer, withAsymmetricEdge, strictlyWider), but it largely paraphrases the adjacent comments’ intent; weaker than a sharper structural fact. Hold as fallback.\n\nCandidate D (strongest): Examine the generator closely. `PEERS = [...CURRENT, ...GHOSTS]`; node `key` is drawn via `fc.constantFrom(...CURRENT)`, and `peers` via `fc.uniqueArray(fc.constantFrom(...PEERS), {maxLength:3})`. Since CURRENT ⊂ PEERS and there is NO filter excluding a node’s own key from its own peer list (and `node()` stores `sameAs` verbatim), the generator can emit a node whose `sameAs` contains its own key — a self-edge. Refutation attempts: (1) Is any downstream sanitization visible? No — `proj`/`node` pass data through untouched; the folds are opaque imports, so nothing in these bytes removes self-edges. (2) Does it rely on library internals? Only on `constantFrom` yielding values from the supplied list — the pool overlap is plain from the literals here. (3) Is it a name/signature restatement? No — the test name speaks of the subset law; the surrounding comments advertise dangling peers and asymmetric half-edges but never self-reference. (4) Exact-count risk? None — stated qualitatively. (5) Does it change what they do? Yes: any implementation hoping to keep this suite green must tolerate a node naming itself without crashing or violating the law, and anyone tightening the generator should know self-edges are currently admitted. Survives.\n\nSTEP 2 — EMIT\n\n```atlas-fact\n{\"claim\": \"The PROP-SAMEAS-1 generator draws node keys and sameAs peer targets from one shared pool with no filter preventing a node from naming itself, so it can produce self-referential sameAs entries (a node listing its own key as a peer) — meaning the folds under test are confronted with self-edges, a shape neither the test's name nor any surrounding comment mentions.\"}\n```"