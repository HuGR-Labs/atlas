"STEP 1 — Reasoning (scratch):\n\nCandidates drafted from the bytes:\n\n1. *Vacuous checks (HOLDS ∧ zero mutant-flips) are dropped* — real, but the `it` name (\"survives every mutant is dropped as vacuous\") already states it. Discard.\n2. *Obvious advisories are admitted carrying `{rank:'obvious', by:'harness-predicate'}`* — pinned by executable assertions, but the `it` title says \"an obvious one is ADMITTED with a low score\", so largely known from the name. Weak.\n3. *Refine called exactly once at K=1 then dropped* — restates its own test title. Discard.\n4. *Sound-oracle path emits an `advisory`-kind fact with no `check` and no `status` key* — checking the bytes of SCN-GEN-12k-1: the input is `predProposal({ slot: 'contract' })`, i.e. `kind: 'predicate'`, yet the assertions pin `expect(a.fact.kind).toBe('advisory')`, `expect('check' in a.fact).toBe(false)`, `expect('status' in a.fact).toBe(false)`, and `expect(...predicateSlot).toBe('contract')`. These are executable assertions, not a comment's claim about other files (the WP-FIX-6.KNOW comment gives history, but the fact stands on the asserts alone). Verified item-by-item: kind='advisory' ✓, 'check' absent ✓, 'status' absent ✓, `predicateSlot==='contract'` ✓. Not derivable from the `it` name, which only says the type-checker is preferred over a synthesized query — an engineer could satisfy that name while still emitting a runnable predicate/check node. It changes what a consumer/implementer does: read-side grouping must key off `predicateSlot`, and such admitted facts carry no re-runnable check. Survives refutation.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"On the sound type-checker admission path, an admitted predicate proposal is persisted as a fact with kind 'advisory' — carrying neither a 'check' nor a 'status' key, with only the slot surviving as 'predicateSlot' — rather than as an executable predicate/check node.\"}\n```"