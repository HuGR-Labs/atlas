"Looking at the bytes: the predicate block gives four direct assertions — `atLeastT1(inv('T0'))` → `true`, `atLeastT1(inv('T1'))` → `true`, `atLeastT1(inv('T2'))` → `false`, and a loop over ten off-lattice spellings (`'T3'`, `'t1'`, `'T1 '`, `' T0'`, `'T0\\n'`, `''`, `'toString'`, `'__proto__'`, `'constructor'`, `'TIER_ORDER'`) all → `false`.\n\nDrafting candidates:\n- *Trigger mode unreachable end-to-end* — rests on a comment about `retrieval-model.js`, not visible here. Discard.\n- *`advisoryDropped` stays 0 despite ten rows landing in neither band* — checkable, but its significance depends on guessing what the counter otherwise measures. Weaker than a direct admit-set observation.\n- *`tokenEstimate` equals summed claim chars* — pinned only for this fixture; inference-heavy.\n- *`atLeastT1` admits `'T0'`* — survives. The name reads as a numeric threshold (`>= T1`), yet the assertion `expect(atLeastT1(inv('T0'))).toBe(true)` shows the stricter tier passes while `'T2'` fails. Enumerated exhaustively over the tested corpus: admitted set is exactly `{'T0','T1'}`. Fully derivable from the assertion literals, no comment reliance, and it corrects a natural misreading of the exported name's ordering direction.\n\n```atlas-fact\n{\"claim\": \"In this unit, atLeastT1 passes tier 'T0' and fails 'T2' — its admitted set over the tested corpus is exactly {'T0','T1'} — so the 'at least T1' ordering runs by governance strictness (lower tier index = more governed), not by ascending tier number.\"}\n```"