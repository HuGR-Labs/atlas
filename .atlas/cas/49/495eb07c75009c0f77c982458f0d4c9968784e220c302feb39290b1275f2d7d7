"STEP 1 — scratch reasoning:\n\nCandidates considered and refuted/survived:\n\n1. *\"Memoization by object identity\"* — true and in the bytes (module-level `WeakMap<ScipOutput, UnitDepsApi>`), but it is loudly advertised in the `[PERF]` block comment inside this very unit, so a reader of the file already knows it. Weaker candidate.\n\n2. *\"Lexically-first symbol wins on terminal-name collision\"* — verifiable (`if (prev === undefined || resolved < prev)`), but the JSDoc above `resolveDepFor` explicitly states it (\"lexically-first on a rare intra-unit name collision\"), so it is part of the declared contract. Discarded as already-known-from-the-interface.\n\n3. **First-definition-wins is order-dependent** — the `defDoc` loop iterates `scip.documents` in array order and guards with `!defDoc.has(occ.symbol)`, so for a global symbol with definitions in *multiple* documents, the retained defining-document hash is whichever document appears **first in the array**, nothing else. Downstream, the cross-unit discriminant (`defDoc.get(resolved) === self`) reads exactly this stored hash, so whether a unit classifies such a multiply-defined symbol as its \"own vocabulary\" or as a cross-unit dependency flips with document ordering — not with anything intrinsic to the unit. No comment in the unit states this consequence; it is derived purely from the control flow. Survives refutation: checkable in these bytes, not a signature restatement, no counts, no reliance on out-of-file claims.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"When the same global symbol is defined in more than one document, defDoc records only the first defining document in scip.documents array order, so a unit's own-versus-cross-unit classification of that symbol depends on document ordering rather than on the unit itself.\"}\n```"