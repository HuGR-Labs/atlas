"STEP 1 — REASONING (scratch):\n\nReading the bytes: this is a pure data file — metadata fields (`subject`, `model_snapshot`, `run_date`, `fixtures_sha256_16`) plus a flat `verdicts` map over keys T01–T10 and F01–F10.\n\nCandidates considered:\n\n- *Methodology details* (frozen renderPrompt, fresh-context sub-agents, deterministic string-compare scoring): these come from the `_note`, which asserts things about `renderPrompt(fixtures.mjs)` and `subject-score.mjs` — code not present in these bytes. Discard.\n- *Snapshot provenance* (\"recorded from sub-agent transcript usage block\"): again a comment-style claim about code elsewhere. Discard.\n- *Hash value*: `fixtures_sha256_16` — the key name already conveys it's a truncated sha256; the actual value can't be re-derived here. Discard.\n- *Verdict distribution*: checkable entirely within the shown map. Scanning item by item: T01–T10 → all ten are `\"GROUNDED_TRUE\"`. F01=`HALLUCINATED`, F02=`HALLUCINATED`, F03=`GROUNDED_TRUE`, F04–F10=`HALLUCINATED` (nine). So F03 is the sole F-prefixed fixture whose verdict diverges from both the other nine F-prefixed entries and the T/F prefix pattern. Counted and re-checked: 10 T-keys, 9+1 F-keys. Survives refutation — it is intra-file, byte-checkable, not a comment claim, and not derivable from the filename.\n- *Run date / subject identity*: plain literals restating visible fields. Discard as unremarkable.\n\nSurviving fact: the verdict map is uniformly prefix-consistent except for F03, the lone F-prefixed fixture adjudicated `GROUNDED_TRUE`.\n\nSTEP 2 — RESULT:\n\n```atlas-fact\n{\"claim\": \"In the verdicts map, every T-prefixed fixture (T01\\u2013T10) is GROUNDED_TRUE and every F-prefixed fixture is HALLUCINATED except F03, the lone F-prefixed fixture adjudicated GROUNDED_TRUE.\"}\n```"