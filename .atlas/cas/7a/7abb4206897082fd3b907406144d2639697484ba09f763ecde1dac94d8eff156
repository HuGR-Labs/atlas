"STEP 1 — scratch reasoning:\n\nCandidates drafted and refuted:\n\n- **\"spawnRecall returns a fresh copy, not an alias\"** — true and byte-verifiable (`toEqual` + `not.toBe` in 13c), but the `it()` title literally says \"it is a copy, not an alias,\" so it's already known from the name. Discarded.\n- **\"Scoping is by owner AND unit id\"** — encoded in 13b's fixtures, but the describe/it names state it outright. Discarded.\n- **\"versioned() returns a Map consumable directly by respawnFromRecord()\"** — evidenced by `respawnFromRecord(record)` where `record = versioned(...)`, but the truth of the external shapes rests on `../src/respawn.js`, not these bytes. Discarded.\n- **\"Exactly four kinds (task/project/pr/logbook)\"** — countable in the `ALL_TYPES` literal, but the describe name already says \"every type travels,\" making it near-restatement. Discarded.\n- **Header-comment claims** (foldArchiveFromRecord's pr branch deferred, side-store would drop pr) — all assertions about code NOT in these bytes. Discarded per rules.\n- **Unused import check**: scanning the file exhaustively, `TaskMemoryEntry` appears exactly once — in the import line `import type { MemoryStore, TaskMemoryEntry } from '../src/types.js';`. `MemoryStore` is used (three annotations); `TaskMemoryEntry` has zero references in any annotation, expression, or declaration in the body. This is fully self-contained in the bytes, not derivable from the file's name or any signature (a signature-level reader would assume an imported type is used), and it changes what a maintainer does (safe to remove; lint-flaggable; entry-shape knowledge must come from the inline literals instead). Survives.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"Although imported from ../src/types.js alongside MemoryStore, the TaskMemoryEntry type is never referenced anywhere in the rest of this file's code.\"}\n```"