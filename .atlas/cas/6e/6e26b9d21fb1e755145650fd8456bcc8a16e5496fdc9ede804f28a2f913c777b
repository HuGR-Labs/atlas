"STEP 1 — Reasoning (scratch):\n\nCandidate A: \"GATE PASS is not sufficient to seal — touching acceptance artifacts forces REJECT regardless of green.\"\n- Refutation pass: Is this in the bytes themselves, not a claim about another file? Yes — Procedure step 1, the failure-modes list (\"regardless of green\"), the abstain section (\"return to the lead, not a fixable seal\"), and the verdict enum `REJECT(gaming: <acceptance path touched>)` all state it within this unit. Nothing external needed to verify the rule.\n- Does it restate name/signature? The name \"SEAL\" plus frontmatter (inputs, next_state) suggests \"finalize after PASS\"; nothing there implies a scope-based rejection of a green diff. Survives that check.\n- Enumeration risk? The blocked categories are golden, PBT property, harness, `ref/*.ts`. Rather than asserting a count, I'll phrase qualitatively. Crucial nuance verified in the bytes: `(+ non-acceptance tests)` means ordinary test edits are allowed — so the claim must not say \"no test edits ever.\" Checked against Procedure 1 and the self-check bullet; consistent.\n- Contradiction scan: Output contract, self-check, and abstain sections agree on REJECT(gaming) semantics. No internal contradiction found. Candidate survives.\n\nCandidate B (merge_after ≠ DAG predecessor) — plausible but leans on the \"conflict-map\" concept tied to `roadmap/wave-plan.md`, i.e., partially a claim about elsewhere. Discarded in favor of A.\n\nCandidate C (assurance mode FLOOR|PBT|FULL recorded honestly) — true and in-bytes, but closer to a plain restatement of the provenance schema already shown in the output contract. Discarded.\n\nEmitting A.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"A GATE PASS verdict alone is not enough to seal here: any diff touching acceptance artifacts (golden files, PBT property definitions, harnesses, or ref/*.ts oracles) triggers REJECT(gaming) even when every check is green, while edits to non-acceptance tests remain permitted.\"}\n```"