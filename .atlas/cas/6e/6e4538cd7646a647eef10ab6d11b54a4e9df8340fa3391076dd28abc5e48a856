"STEP 1 — scratch reasoning (not stored):\n\nScanning the bytes for candidates:\n- Comments reference other files, tasks (#83, #144), mutants, and history (\"used to expect `unknown node`\") — none of that is admissible; those are claims about code not shown.\n- The R1 table literals (`discriminant: 'unauthorized'` for absent endpoints), the `blindTo` ordering case, and the `sameAsRetracted` / `deriveSameAs(['n1=n2'])` expectations are all in-code, but several are close to restating their own test titles.\n- `DiskStore` is imported as a type and never referenced again in the visible bytes — true, but a lint-level detail unlikely to change anyone's actions.\n- Strongest candidate: the \"THE ASYMMETRY, PINNED\" block. Verifying against pure code, ignoring its comment: a fixture built by `linked()` (pair already asserted); then `for (let i = 0; i < 3; i++)` calls `link('n0','n1')` in assert mode, each expected `toEqual({ linked: true, a: 'n0', b: 'n1' })`; then `expect(fx.persists()).toHaveLength(before + 3)` — so each redundant call appends exactly one publication — and the final two publications are compared via `JSON.stringify([...p.current])` and asserted equal. Contrast, also in code: the sibling test refuses re-assertion of a *retracted* pair with `RETRACT_RETRACTED_PAIR`. Count of 3 is explicit and verified (`i < 3`, `before + 3`). Nothing here depends on unseen modules beyond the support helpers the test drives, and the expectation literals are the bytes themselves.\n- Refutation pass: could the +3 be incidental? No — it correlates one-to-one with the 3 calls. Could \"byte-identical\" overstate? Code only proves the last two match, so I scope the claim to that. Not derivable from the file's name/signature (nothing suggests it pins *assert*-path republish semantics), and it changes what an implementer may do (no de-duplicating or suppressing publications on redundant links; the success/refusal asymmetry between asserted and retracted pairs must be preserved).\n\nSurvives → emit.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"This suite pins that re-asserting an already-asserted pair succeeds on every retry and appends one new publication per retry (three retries yield three more publications, the final two byte-identical), while the same operation on a retracted pair is refused with RETRACT_RETRACTED_PAIR — so the assert path is deliberate idempotent-success-with-republish, not a no-op.\"}\n```"