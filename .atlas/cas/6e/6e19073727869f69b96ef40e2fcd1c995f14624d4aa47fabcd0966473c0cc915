"Reading the bytes closely. The unit is a requirements doc for a genesis/mining block, dense with normative clauses. Most individual REQs restate what the section titles imply (determinism, budgets, grounding). The section that a competent engineer could not predict from the path/name is §GEN-CONC: it constrains *how* concurrency may enter an otherwise deterministic pass — via a single batched S2 dispatch port — while pinning two consequences that dictate architecture: durable writes must stay serialized one-site-at-a-time through the pass loop in rank order regardless of how calls were dispatched (REQ-GEN-CONC-6), and the report of a concurrent pass must be byte-identical to a sequential one excepting only the count of model calls actually issued (REQ-GEN-CONC-2).\n\nRefutation pass: both clauses are verbatim in these bytes, so the claim re-derives here without leaning on the referenced `atlas-genesis.md` anchors or the task number in the header (which I need not invoke). No exhaustive counting is asserted by me — the \"excepting only the model-call count\" scope is the doc's own stated exception, read directly. It is not a restatement of the file's name or purpose, and it plainly changes what an implementer builds (parallelize dispatch only; serialize persistence; keep report determinism modulo one call-count field). Survives.\n\n```atlas-fact\n{\"claim\": \"Concurrency in the S2 pass is admitted only through a single batched dispatch port and changes nothing else: durable writes must still be routed one site at a time through the pass loop in rank order, and a concurrently executed run must produce a report byte-identical to the same pass run sequentially, excepting only the count of model calls actually issued.\"}\n```"