"STEP 1 — Reasoning (scratch):\n\nReading the bytes closely. Candidates drafted:\n\n- Candidate A: \"The customer is an AI coding agent, success measured via agent-trace benchmarks, not surveys.\" Verbatim in the header paragraph (\"instrumented, not surveyed… emits ground truth\"). Survives, but it borders on summarizing the doc's stated purpose.\n- Candidate B: \"Execute (code generation) is out of scope — pain concentrates in Locate/Confirm/Modify.\" Present in the job-map table and its caption. Fully local, plausible.\n- Candidate C: \"Embeddings and RAG are banned; retrieval must be deterministic + structural by construction — a ratified constraint, not a preference.\" This appears literally as a bullet in the Non-goals/constraints section, including the explicit note that it bounds every downstream design parameter. Check: is it derivable from these bytes? Yes, verbatim. Does it rely on a comment's claim about unseen code/files? The operative content (the ban, determinism, structural retrieval) is self-contained here; \"ratified\" describes status but the constraint itself stands in these bytes. Exact count/enumeration? No — qualitative. Restatement of name/signature? No — nothing in \"docs/design/product-definition.md\" hints that an information-retrieval product forbids the two dominant retrieval techniques. Would it change what an engineer does? Strongly yes — it forecloses vector indexes/embedding pipelines in any implementation work and forces deterministic/structural retrieval.\n- Candidate D: \"Exactly 13 FRs, FR-11..13 added 2026-07-17 at extended scope.\" I counted FR-1..10 plus FR-11/12/13 — verified. But the number isn't itself the decision-changing point, and the extended-scope framing leans on register IDs (MEM-*, TOOLS-*) defined elsewhere; less robust.\n\nC survives all refutation checks most cleanly and is the most action-altering fact. Emitting C.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"Although the Atlas is a knowledge-retrieval substrate for AI coding agents, it bans embeddings and RAG outright as a ratified constraint, so retrieval must be deterministic and structural by construction rather than a design preference.\"}\n```"