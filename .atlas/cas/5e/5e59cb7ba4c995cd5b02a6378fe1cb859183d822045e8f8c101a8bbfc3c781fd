"STEP 1 — scratch reasoning:\n\nScanning the bytes for candidates:\n\n- *Header-comment-derived stories* (lucy F1 bug history, #96 exit criteria, \"mirrors wp-96-r-relation-e2e.test.ts\"): all claims about other files, past behavior, or issue numbers — discarded per rules.\n- *\"Every gate in the suite passes\"* — risks an exhaustive-enumeration claim; I'd have to enumerate verify/teeth/grounded/nonObvious/gateHolds and argue completeness. Discarded in favor of a narrower form.\n- *\"Leg 2 bypasses runExtract and calls admit directly\"* — visible in the code, but it's essentially what the test body plainly does and the header already frames leg 2 as a separate admit + door step. Weak \"changes what they do\" value.\n- *Proposal's `nodeKey: asNodeKey(SITE)`* — derivable literal, but its significance (placeholder vs. real identity) leans on unseen `admit` internals. Discarded.\n- *No `vi.fn`/`vi.spyOn` despite `vi.restoreAllMocks()`* — verifiable by scanning, but low actionability. Held as backup.\n- **Survivor**: the PROMOTE leg wires `createGovernedEmit({ store, gate: HOLDS, ... })` where `HOLDS` is defined in this file as `{ gateHolds: () => 'HOLDS' }` — a constant-returning stub with no branching. Both the definition and the wiring are in these bytes; the header comment never mentions the promote-side gate, so no stale-comment dependency. This is invisible from the file's name/signature, and it materially changes how much trust one places in this \"end-to-end\" test (it exercises staging/governance/projection plumbing, not real truth evaluation — and the door as configured cannot reject). No counts, no cross-file claims, survives refutation.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"In the PROMOTE leg, createGovernedEmit is given a locally defined TruthGate stub whose gateHolds unconditionally returns 'HOLDS', so the door's truth check in this suite is hard-wired to pass and can never reject an emission.\"}\n```"