"STEP 1 — SCRATCH\n\nCandidates drafted and refuted:\n\n- *\"Before #152 the binary accepted bad slots\"* — comment-history about past states and other files; not re-derivable from these bytes. Discard.\n- *\"Absent slots were measured across 300 model calls to never occur\"* — pure comment claim about measurement outside the unit. Discard.\n- *\"The guard lived in router.ts/template.ts with zero callers\"* — claims about other modules; not in these bytes. Discard.\n- *\"The refusal reason is checked by first-colon-segment equality because prose quotes other constants\"* — the mechanism (`split(':')[0]` + `.toBe`) is code, but the stated motivation is comment-only. The mechanism alone is close to restating the helper's body; weak.\n- *\"The test drives the real binary via subprocess\"* — visible, but essentially the definition of a blackbox test file; too near the name.\n- **Survivor:** the out-of-vocabulary case pins the refusal's *channel shape* via exact assertions in code: `expect(run.exitCode).toBe(2)` (exactly 2, not merely nonzero), `expect(run.stdout).toContain('status: rejected')`, `expect(run.stderr).toBe('')` (empty string, i.e., any stderr output fails), plus `reasonOf(reasonLine(run.stdout)).toBe(CLOSED_SLOT_VIOLATION)` (first colon-token equals `closed-slot-violation`). None of this follows from the filename `s29-closed-slot.blackbox.test.ts`; all of it is checkable by reading these assertions. An engineer altering CLI exit mapping, status derivation, or how a guard failure propagates needs to know the test constrains the delivery channel, not just the message text. Survives.\n- Runner-up (enumeration of the 13 slot literals asserted in the reason): verified count = 13, valid, but the survivor above is qualitative and carries more decision weight.\n\nEmitting the survivor.\n\n```atlas-fact\n{\"claim\": \"In the out-of-vocabulary case the test pins the refusal's delivery channel, not just its wording: the exit code must be exactly 2, stdout must carry a 'status: rejected' line, stderr must be completely empty, and the first colon-separated token of the 'reason:' line must equal 'closed-slot-violation'.\"}\n```"