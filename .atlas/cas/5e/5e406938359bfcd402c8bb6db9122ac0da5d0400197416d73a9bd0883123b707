"STEP 1 — REASONING (scratch):\n\nCandidates drafted while reading the bytes:\n\n- *Candidate A*: \"`abstained` is a render-hint only — the flag never filters the data payload.\" Checkable (`read` is computed once; both arrays go into `data` unconditionally). True, but close to a summary of the happy-path assignment; weaker surprise.\n- *Candidate B*: \"Empty scope reads the whole ledger.\" Rests on `negationsOf`'s internal semantics, which are not in these bytes. Discard.\n- *Candidate C*: \"`negationsVerdict` runtime-guards `scope` despite the `string` type.\" Visible in the bytes (`typeof scope !== 'string' || scope.length === 0` → `ok:false`), but fairly mundane defensive coding; borderline restatement once read.\n- *Candidate D*: **The freshness callback built in `createNegationLeg` is strictly two-valued.** Verified item-by-item against the code: (1) `if (fact === undefined) return 'DRIFTED';` — a CAS/store miss yields `DRIFTED`; (2) `return resolveFreshness(freshness, fact) === 'FRESH' ? 'FRESH' : 'DRIFTED';` — only the exact string `'FRESH'` survives; every other return value of `resolveFreshness` is rewritten to `'DRIFTED'`. Nothing about this follows from the signature (`KnowledgeFreshness` is an imported type whose full value set is not visible here), so a consumer could reasonably assume richer freshness values (or pass-through of intermediate states) flow out of this leg. The surrounding comments talk about STALE collapsing, but my claim does not depend on those comments or on any unseen module: the ternary and the early return are entirely within these bytes. It changes what an engineer does — tests or downstream branches expecting any freshness state other than FRESH/DRIFTED out of `atlas negations` are dead code by construction.\n\nRefutation check on D: no exact counts claimed, no reliance on issue numbers or other files, no past-tense commentary needed — the two control-flow lines carry the whole fact. Survives.\n\nSTEP 2 — RESULT:\n\n```atlas-fact\n{\"claim\": \"Inside createNegationLeg, the freshness callback given to the negations fold is strictly two-valued: a fact absent from the store yields 'DRIFTED', and only resolveFreshness returning exactly 'FRESH' produces 'FRESH' — every other outcome is rewritten to 'DRIFTED'.\"}\n```"