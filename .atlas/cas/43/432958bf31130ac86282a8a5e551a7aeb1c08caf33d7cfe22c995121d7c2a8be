"Reading the bytes: single `jobs:` entry (`gate`), one runner, an ordered step list ending in `npm test`. Drafting candidates:\n\n- *\"fetch-depth: 0 exists because ears-coamend-guard diffs against merge-base\"* — the reason lives in a comment asserting behavior of harness scripts I cannot see; discard the causal part. The literal itself is visible but merely describes its own setting.\n- *\"Node 20 / npm cache\"* — restatement of visible literals; trivial.\n- *\"#198 / harness/ gating\"* — issue number and other-module references; discarded per rules.\n- Candidate that survives: the **structure** — every check plus the test suite are steps of one serial job, `npm test` sequenced last. Verifiable purely from this YAML's shape (one job key, ordered steps, test step at the bottom); not derivable from the filename `ci.yml`, which doesn't reveal whether gates fan out into parallel jobs or where tests sit in the order. Consequence (a failing gate short-circuits everything after it, tests included) follows from the observed single-job, ordered-step composition. No exact counts asserted; no reliance on comments about unseen code.\n\n```atlas-fact\n{\"claim\": \"Every check, including the vitest suite, is serialized as steps of a single job in a fixed order with npm test sequenced last, so any failing gate prevents all later steps — including the tests themselves — from ever running, and nothing here executes in parallel.\"}\n```"