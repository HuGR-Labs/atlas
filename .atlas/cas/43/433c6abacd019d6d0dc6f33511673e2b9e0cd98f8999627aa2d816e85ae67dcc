"STEP 1 — scratch reasoning (not persisted):\n\nCandidate drafts and refutation passes:\n\n- **Draft A:** \"Authorization is passed via the `ATLAS_ACTOR` environment variable.\" Partially visible (`beforeAll` mutates `process.env`), but *whether* the spawned MCP bin actually inherits it rests on `mcpSession` internals not shown. Weakens under the \"don't lean on unseen code\" rule.\n- **Draft B:** \"The slot vocabulary is pinned to exactly 13 entries.\" The literal `13` is right there in an assertion, so it's checkable — but it's essentially a restatement of a visible literal and only matters if you touch the vocabulary.\n- **Draft C:** \"A successful emit does not surface in `atlas-query`.\" The `.toBe(false)` assertion is in the bytes, but the *why* (T2 tier bound, TOOLS-6) lives in a comment about behavior defined elsewhere; the bare assertion without that context risks being misread as incidental.\n- **Draft D:** The \"black-box law\" test's guard regex `/^import .*from '([^']+)'/gm` only matches single-line, single-quoted, static `import … from '…'` statements. Checking against the bytes: the dynamic imports used *inside* the very test doing the audit (`await import('node:fs')`) cannot match the pattern (line doesn't start with `import`, and there's no `from '...'` tail); a multi-line import block fails because `.` doesn't cross newlines; side-effect-only imports (`import 'x';`) lack a `from` clause; double quotes aren't matched. All four evasion paths are derivable purely from the visible regex — no external knowledge needed. Consequence an engineer would act on: this self-audit gives false confidence; a dynamically imported `@atlas/*` module would pass the \"imports no product library\" law silently. Draft D survives refutation.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"The black-box-law self-check extracts import specifiers with the regex /^import .*from '([^']+)'/gm, so it only audits single-line, single-quoted, static import statements — a multi-line, double-quoted, side-effect-only, or dynamically imported '@atlas/*' specifier would evade the guard entirely.\"}\n```"