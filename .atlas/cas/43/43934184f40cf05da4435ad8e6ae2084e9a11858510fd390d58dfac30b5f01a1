"STEP 1 — Reasoning (scratch):\n\nReading the bytes: `include` is broad — `[\"src\", \"test\"]` pulls in everything under both directories. `exclude` contains exactly one entry: `test/fixtures/neg-collapsed-local/repo`. Counting the exclude array: one item. Verified.\n\nDraft candidates:\n1. \"It references nine sibling packages…\" — I can count the references (contracts, kernel, index, grounding, knowledge, persist, retrieval, genesis, tools = 9), but this is essentially a summary of the references array itself; low signal beyond restating the literals.\n2. \"Tests are included in compilation\" — direct restatement of the `include` key. Discard.\n3. \"Inherits from root tsconfig.base.json\" — restatement of `extends`. Discard.\n4. The asymmetry: the compile surface is the *whole* test tree, yet the *only* carve-out is that one fixture subtree. This is a synthesis across `include` + `exclude`, verified against the bytes (exclude has exactly one element; include matches all of `test`). It changes behavior: anyone dropping additional fixture material (e.g., another intentionally broken sample repo) anywhere else under `test/` will silently pull it into the type-check/build and break the package. Conversely, only that one path is safe to keep malformed.\n\nRefutation pass on #4: Does it lean on external TS semantics? Only the literal meaning of `include`/`exclude` globs, which is what the directives themselves declare. Does it assert existence/absence of unseen files? No — trimmed phrasing avoids claiming other fixtures exist. Exhaustive-count risk? \"Only path excluded\" rests on the exclude array having one element — counted and confirmed. Survives.\n\n```atlas-fact\n{\"claim\": \"Although the package compiles its entire test directory, the only path excluded from compilation is the single fixture subtree test/fixtures/neg-collapsed-local/repo, so any other file placed anywhere under test/ will be pulled into the package's type-check and build.\"}\n```"