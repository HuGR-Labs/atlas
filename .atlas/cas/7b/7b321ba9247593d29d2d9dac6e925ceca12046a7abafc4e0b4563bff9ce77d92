"```atlas-fact\n{\"claim\": \"Within this file, the imported scrub function is invoked only inside the double-scrub idempotence control test; every actual redaction guarantee is exercised by feeding raw, unscrubbed bytes (encoded fixtures, malformed casts, or admitToBuffer-folded chunks) directly into store.put, so the suite is structured so that a store-side gate failure could never be masked by a caller pre-scrubbing.\"}\n```"