"STEP 1 — scratch reasoning (not persisted):\n\nReading the bytes: this is a plain JSON config with three payload keys (`nearDup`, `t0Heuristic`, `authz`) and heavy `$comment` annotation.\n\nCandidate drafts and refutation:\n\n- \"The policy fails closed when absent/malformed\" — asserted only in the `$comment`s, describing loader behavior (`policy.ts`) not present in these bytes. Rests on a comment's claim about unseen code. **Discard.**\n- \"Branch protection / CODEOWNERS lock is off; admin team holds no grant\" — all claims about repository settings outside this file. **Discard.**\n- \"A `$comment` inside `scopes` would brick governance silently\" — depends on validation logic in `policy.ts:183`, invisible here. **Discard.**\n- \"`claimNormThreshold` is 1 = exact-match only\" — the value `1` is literal, but the *semantics* of 1 rest on the comment pointing at `@atlas/knowledge NearDupConfig`. Weak actionability anyway. **Discard.**\n- \"T0 keywords are exactly `invariant` and `security-property`\" — literal, counted, true — but marginal actionability compared to the authz literal. Held as backup.\n- \"The `authz.scopes` map contains exactly one grant: `atlas:mined` → [`seat:orchestrator`]\" — verified by direct enumeration of the shown bytes: the `scopes` object has exactly one key, whose array has exactly one element. This is not derivable from the file's name or shape alone (the path says \"policy,\" not which powers are live), and it materially changes behavior: anyone debugging why writes to other scopes are refused, or deciding where to add a new writer, must know this sole grant exists and that it is held by a machine seat, not a human. Survives refutation: it leans on no comment, no external module, no historical claim — only the literals themselves.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"The authz.scopes map contains exactly one grant — write access to scope `atlas:mined`, held solely by the actor id `seat:orchestrator` — so every other scope in this policy has no listed writer at all.\"}\n```"