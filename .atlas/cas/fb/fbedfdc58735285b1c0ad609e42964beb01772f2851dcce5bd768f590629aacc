"STEP 1 — scratch reasoning:\n\nReading the bytes: `consult(query: unknown)` narrows via `asConsultFilter`, which (a) returns `{}` for any non-object/null query, and (b) copies only `prId`/`date`/`territory` **when they are strings**, dropping everything else. Then `consult` short-circuits: if all three filter fields are undefined it returns `[]`.\n\nCandidate A: \"rejected appends return the log unchanged.\" True and in the control flow, but it's loudly asserted by the doc comments and is close to what the return type plus comments already tell a reader — weaker.\n\nCandidate B: \"`supersede` has no author/existence guard.\" Verifiable (`links.push` unconditional), and asymmetric with `append`; however, the *absence* of an author param is already visible in the signature, so part of it is derivable from the signature alone.\n\nCandidate C (survivor): the empty/malformed-query behavior of `consult`. Nothing in `query: unknown` hints that a no-op filter yields `[]` rather than the whole log — most implementations do the opposite. Verified item-by-item: `consult(5)` → `{}` → `[]`; `consult({})` → `[]`; `consult({prId: 123})` → the number fails `typeof === 'string'`, is dropped → `[]`; `consult({topic: 'x'})` → unrecognized key ignored → `[]`. All checkable in this unit's own control flow, no reliance on comments about other files. This changes what an engineer does (e.g., they cannot use `consult` as a \"list everything\" call, and typos/wrong-typed fields fail silently to empty results).\n\nCandidate C survives; emitting it. STEP 2:\n\n```atlas-fact\n{\"claim\": \"A consult query that is not an object, or that contains none of prId/date/territory as strings (wrongly-typed or unrecognized fields are silently dropped), returns an empty array rather than the full log, so consult can never serve as a list-everything call.\"}\n```"