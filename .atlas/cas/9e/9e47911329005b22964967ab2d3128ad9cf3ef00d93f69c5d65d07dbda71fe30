"STEP 1 — scratch reasoning (not stored):\n\nThe obvious reads (it's a barrel; it re-exports fs/scip/ast/store/git/llm surfaces) are derivable from the path and add nothing. Comments asserting things about other files (\"nothing in the core imports this\", \"the ONLY member of run-git.ts\", \"golden 11a audits textually\", \"consumed by every entrypoint\") all fail the test — their truth lives in code not shown, so I discard them regardless of accuracy.\n\nScanning the code itself, the only structural surprise is in the `from` specifiers. Enumerating every export statement: all sources are relative `'./…js'` siblings — fs, scip, indexer-report, ast, store, sidecar, git-history, git-drift, git-forge, run-git, llm, sidecar-commit, model-config, prompt, unit-source, unit-candidates, unit-count-candidates, unit-def-candidates, index-adapter, poke-file, rev-index, skeleton-source, wire, governed-emit, governed-promote, governed-link, governed-link-retract, author-verdicts, compose, grounding-computer, own-source, doctor-source, relation-source, negation-source, transition-source, test-vacuity-source, verify-fact-source, reverify-store, relation-derive, relation-derive-run, escape/classifier, read-provenance, identity-schema, store-provenance, policy — with exactly one exception: `export type { LinkOut } from '@atlas/tools';`, a bare package specifier pointing outside the package. There are also zero plain `import` statements, confirming the exception isn't shadowed by local aliasing. I re-checked the list; no second non-relative specifier exists. This is checkable purely from these bytes (no reliance on the adjacent comment about ownership), it is not inferable from the unit's name, and it changes what an engineer does: the package carries a real dependency on `@atlas/tools`, and `LinkOut`'s definition must be sought there, not in this package, despite surfacing in this barrel.\n\nSurvives refutation. Emitting it.\n\n```atlas-fact\n{\"claim\": \"Every export in this barrel is drawn from a relative sibling module ('./….js') except exactly one — the LinkOut type, which is re-exported from the separate @atlas/tools package, making that external package the barrel's sole non-local export source.\"}\n```"