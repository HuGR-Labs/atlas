"STEP 1 — Reasoning (scratch):\n\nCandidate facts drafted and refuted:\n\n1. *\"The walker follows symlinks and leaks target contents\"* — REFUTED. That's the defect described, but it lives in `adapter-io/src/fs.ts`, not in these bytes. These bytes only contain prose *asserting* things about that unseen code. A fact about code I cannot see fails the re-derivation test.\n\n2. *\"Mode 160000 (gitlinks) is skipped today\"* — REFUTED as stated. The skip behavior is claimed to happen in unseen walker code; the document's sentence about the working-tree read leg is a comment-style claim about elsewhere. Only the *deliberate-unspecified* stance is internal, and it is nested inside a stronger, fully internal structural fact (see #4).\n\n3. *\"REQ-CLI-5 and REQ-CLI-6 are missing from the numbering\"* — Survives enumeration (I listed every REQ id: CLI-1…4, then 7; no 5 or 6), but it changes nothing an engineer does and the bytes offer no reason, so it invites unsupported inference. Discarded on value.\n\n4. *\"Exactly one requirement breaks the lift pattern\"* — Candidate: REQ-ADAPTER-1e is the sole REQ whose `source:` line cites no `INV-*` invariant and whose `normative-clause:` is \"— none\". Refutation pass: (a) Is it in the bytes? Yes — 1e's own header says \"SECURITY AMENDMENT … NOT a lift from INV-ADAPTER-1\" and \"normative-clause: — none\"; the amendment block states \"Every other REQ here is a brownfield lift of one frozen invariant clause. This one is not.\" (b) Verified item-by-item by scanning every REQ block: every other requirement (ADAPTER-1a–12b, WIRE, CLI, MCP families) carries both an `INV-` source line and a quoted normative clause; only 1e lacks both. (c) Not a restatement of the filename — the filename suggests a uniform lift table; the deliberate exception, its rationale (decision-record so nobody hunts for an invariant that was never written), and the consequence (frozen register flagged for ratification, not edited) are actionable for anyone doing traceability or reconciliation against `freeze/adapters-v0`. (d) Nothing later in the unit contradicts it — the 2026-08-03 additions (9c–9f) still cite INV-ADAPTER-9 and carry clauses. SURVIVES.\n\nSTEP 2 — Emit result:\n\n```atlas-fact\n{\"claim\": \"REQ-ADAPTER-1e is the only requirement in this document that names no frozen INV- invariant as its source and carries no normative clause — the document itself declares it a deliberate post-freeze security amendment (the tracked-symlink link-text rule), meaning its absent provenance is an intentional decision-record, and the frozen reference was flagged for ratification rather than edited by this seat.\"}\n```"