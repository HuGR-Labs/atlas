"STEP 1 — Reasoning (scratch):\n\nReading the two comparison helpers closely, they implement *different* equality semantics despite having near-identical shapes (`(pinned, current) → boolean`):\n\n- `ownUnchanged`: `pinned.every((h, i) => h === current[i])` — strictly positional. Permuting the same set of hashes ([H1,H2] → [H2,H1]) fails the index-wise check, producing DRIFTED. I verified this by tracing the loop.\n- `closureInterfaceUnchanged`: builds a `Map<string, number>` of `memberKey` counts over `current`, then decrements over `pinned`, failing on a zero hit. That is multiset equality — order-independent. I traced the duplicate case myself: pinned `[A,A]` vs current `[A,B]` → current map `{A:1,B:1}`; second pinned `A` hits count 0 → correctly returns false. So duplicates are handled by counting, not masking.\n\nRefutation attempts:\n- \"bodySubtreeHash never enters the fold\" — heavily advertised in the interface docs/comments; discard as comment-restated (even though `memberKey` visibly omits it).\n- \"describeFreshness handles 'STALE' which `freshness` never returns\" — true and code-visible, but weakly actionable.\n- Anything relying on \"drift.ts consumes this\" / \"kernel computes rState upstream\" — claims about files not shown; discard.\n- Exact counts/enumerations — none needed; the surviving fact is qualitative and traced through both loop bodies.\n\nSurvivor: the asymmetric comparison semantics (positional vs multiset) is not derivable from the names/signatures, is fully checkable in these bytes, and changes caller obligations (own hashes must be canonically ordered; closure order is irrelevant; duplicates count on both sides).\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"The two legs of this fold use different equality semantics: the fact's own grounding-set subtreeHashes are compared positionally (so reordering the same hashes yields DRIFTED), while forward-closure members are compared as an order-independent, occurrence-counting multiset of node+interfaceRState keys (so reordering, or duplicating, the same members is decided by counts, not position).\"}\n```"