"STEP 1 — Scratch reasoning (not stored):\n\nReading the bytes of `verifyDependency`'s tail:\n\n```ts\nif (anyInScope(reverse.reverseCallers(target), pathOfHash, sourceScope)) {\n  return { verdict: 'proven', oracle: 'symbol-reverse' };\n}\nreturn abstain(anyInScope(reverse.holeSources(), pathOfHash, worldScope) ? 'scope-open' : 'no-caller-in-scope');\n```\n\nDraft candidate: \"`worldScope` never affects the verdict — it only selects the abstain reason.\"\n\nAttempted refutation:\n- Derivable from these bytes? Yes — both arms of the ternary call the same `abstain` helper, so the `verdict` field is literally the same object shape `{ verdict: 'abstain', ..., oracle: 'symbol-reverse' }` either way; only the `reason` string differs (`'scope-open'` vs `'no-caller-in-scope'`, both literals confirmed in the shown code).\n- Resting on a comment about unseen code? No — this is pure control flow visible here; I am not relying on the header prose about adapter-io, SCIP, or issue numbers.\n- Exact count/exhaustive list risk? The claim names exactly two reason strings; I verified both literals in the ternary. No other `worldScope` use exists in the function body except the malformed-length check, which still yields `verdict: 'abstain'`.\n- More than a name/signature restatement? Yes — nothing in `DepClaim`'s field name `worldScope` or the `FactVerdict` type reveals that the completeness-world leg is verdict-inert; a reader of the signature could reasonably assume an open world changes the outcome. It changes what an integrator does: they cannot branch on the verdict to detect scope openness; they must inspect `reason`.\n\nSurvives. Emitting.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"In verifyDependency, the worldScope/holeSources test only chooses the abstain reason string ('scope-open' vs 'no-caller-in-scope') — both branches return the identical 'abstain' verdict, so worldScope can never upgrade an unwitnessed claim to any verdict other than abstain.\"}\n```"