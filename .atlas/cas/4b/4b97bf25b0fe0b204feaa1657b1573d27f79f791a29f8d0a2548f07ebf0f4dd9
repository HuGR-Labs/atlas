"STEP 1 — REASON FREELY:\n\nReading the bytes closely. Candidates drafted and stress-tested:\n\n- *Candidate: \"The suite saves/restores ATLAS_ACTOR and ATLAS_RATIFY_TOKEN env vars.\"* True and visible in `setup`/`teardown`, but standard test-hygiene a competent engineer would anticipate for a subprocess-driving suite; low behavioral surprise. Discard.\n- *Candidate: \"The squat differs from the legit fact only by the `scope` field.\"* Directly readable from the spread `{ ...legit, scope: ['src'] }`, but that's essentially a restatement of what the spread plainly does. Discard.\n- *Candidate: \"The legit write must exit 0 and produce no 'governance-relocation' string.\"* Checkable (`expect(out.exitCode).toBe(0)` plus the regex on combined stdout+stderr), but it's near-restating the visible assertions; the interesting part is what the first write is allowed to do.\n- *Candidate: \"The test asserts `squat.id === legit.id`, i.e., identity ignores scope.\"* The assertion is local, but its truth hinges on `draftFact` internals not shown; stating it as a fact about behavior leans on invisible code. Weaker than the alternative. Discard.\n- *Survivor: the verdict-agnosticism of the first write.* The bytes contain `expect([0, 1, 2]).toContain(squatted.exitCode);` — an exhaustive, verifiable enumeration (exactly 0, 1, 2 in the literal) meaning the malformed write may FULLY SUCCEED (exit 0) and the test stays green. Nothing in the file name (\"scope-brick\") or the `it` title (\"anchor is still writable\") tells you the test tolerates acceptance of the malicious payload; the teeth live entirely in the second write. Refutation checks: no reliance on comments about other files (the comment agrees with the code anyway), exact enumeration counted from the literal, and it is behavioral — not derivable from name/signature/types. It changes what you do: modifying the CLI to either reject or accept malformed scopes cannot break this test by itself; only the follow-up legitimate rewrite is judged.\n\nSurvives. Emit.\n\nSTEP 2 — EMIT THE RESULT:\n\n```atlas-fact\n{\"claim\": \"The test never requires the malformed array-scope write to be refused — emitFact(repo, squat) passes with exit code 0, 1, or 2 alike, so even a fully successful squat write keeps the suite green and all assertions of substance fall on the subsequent legitimate write exiting 0 without a governance-relocation notice.\"}\n```"