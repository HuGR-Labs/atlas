"STEP 1 — REASONING (scratch):\n\nScanning candidates:\n\n- Candidate 1: \"`foldAstUnits` is imported but only void-referenced in `wire.ts:167`\" — REFUTED. This is a FINDING about `wire.ts`/`ast.ts`, neither of which is in these bytes. It's the doc's claim about unseen code; cannot re-derive here. Discard.\n- Candidate 2: \"`governed-emit.ts` routes upserts on payload `node.id`\" — same problem: claim about a file not shown. Discard.\n- Candidate 3: \"`parse.ts` treats `--at` as bare boolean\" — again a claim about unseen code. Discard.\n- Candidate 4: \"Six file-disjoint WPs cover seven findings\" — countable in the bytes (WP-F1, F3, F2F5, F4, F6, F7 = six; findings F1–F7 = seven, with F2+F5 bundled). Verifiable, but largely restated in the doc's own opening lines; weak \"changes what they do\" value.\n- Candidate 5: Review routing (lucy on all, bobby only F1, billy only F3) — checkable against each REVIEW line, but process detail; borderline.\n- Candidate 6: WP-F6 uniquely carries a \"> RESOLVED\" annotation. Scan: F1, F3, F2F5, F4, F7 sections end at their REVIEW lines with no RESOLVED marker; only WP-F6 has the blockquote declaring the abstention adjudicated as designed behavior. Verified exhaustively across all six WPs. This is about the document's own content (its classifications), not a claim resting on unseen code — the note's specifics about `mine.ts` internals aren't load-bearing for the fact. Not derivable from the filename `e2e-findings-remediation.md`, which implies all seven findings are pending fixes. Changes what an implementer does: don't pick up F6 as open work; treat it as adjudicated/settled. SURVIVES.\n\nSTEP 2 — EMIT:\n\n```atlas-fact\n{\"claim\": \"Within this frozen remediation record, WP-F6 (atlas mine writing zero grounded candidates) is the only work package annotated RESOLVED — adjudicated as abstain-by-design, not a bug — while the other five WPs (F1, F2/F5, F3, F4, F7) stand as open fixes despite the filename implying all findings await remediation.\"}\n```"