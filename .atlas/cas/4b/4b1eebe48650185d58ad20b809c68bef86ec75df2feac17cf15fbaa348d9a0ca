"STEP 1 — Scratch reasoning (not persisted):\n\nReading the bytes: the file is a vitest suite probing `upsert`/`routeWrite` with fresh string data. Candidates:\n\n- *Candidate A*: \"routeWrite never returns REJECT across the 16-cell product; predicate + existing key + changed check → CREATE.\" — Refuted as novel: the test's own `it()` title literally says \"full 16-cell product is total, never REJECT\", so much of this is known from the name alone, and the precedence framing leans on the inline comment.\n- *Candidate C*: \"DEDUP fires for a byte-identical re-emit even after intervening updates.\" — Partially redundant with the first `it()` (byte-identical re-emit → DEDUP); the extra twist is thin.\n- *Candidate B*: In the UPDATE test's tail, `dup` sends `claimNorm: 'c-two'` (already present in the node's claims) but under a **fresh** `contentHash` `'H-a3'`, and asserts `dup.decision === 'UPDATE'` (not `'DEDUP'`) with `claims` still `toHaveLength(3)`. Verified item-by-item: hashes H-a0/a1/a2/a3 are all distinct literals, so the input cannot be a byte-hit; c-two ∈ {c-init, c-two, c-three}; the assertion is plain code, not stated in any comment (the nearby comment only covers the length staying put, not the decision being UPDATE). This reveals the decision classification keys on content-hash identity, not claim novelty — duplicate claims pass through as UPDATE and are absorbed silently.\n\nSurvivor: Candidate B. It is checkable solely from this unit's control flow and literals, is not derivable from the file name or import signatures, and would change how an engineer reasons about dedupe/routing semantics.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"A previously-seen claimNorm re-emitted under a fresh contentHash is expected to route as UPDATE rather than DEDUP, with the node's claim set left unchanged by idempotent set-union.\"}\n```"