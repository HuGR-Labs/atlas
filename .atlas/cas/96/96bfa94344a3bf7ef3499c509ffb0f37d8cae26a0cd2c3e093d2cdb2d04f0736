"Looking at the bytes: this is a held-out golden test file for `runExtract`. Most headline behaviors (ordering, grounding, truth door) restate scenario titles, so I need something the literals pin down beyond the names.\n\nDraft candidates:\n- \"Ordering follows intrinsic fields, not array position\" — supported by the scrambled-input test (shuffled `[h3,h1,h5,h2,h4]` → calls `h1..h5`). Viable, but partially inferable from a sort-based design.\n- \"Vacuous `not.toContain('v2')`\" — no `v2` candidate exists in any fixture, so the guard only fails if `runExtract` synthesizes one internally. True in the bytes, but hedged and low-action.\n- \"Ceiling pinned at 200 for a 750-frontier with exactly 200 calls\" — counted and verified (`length: 750`, `toBe(200)` twice). Solid.\n- \"SCN-GEN-2e-2: only c1/c2 admitted out of 30, yet exactly 20 proposer calls\" — verified item by item: loop creates 30 candidates, gate set is `{c1,c2}`, assertion is `toBe(20)`. This pins control-flow semantics: the low-yield halt cannot truncate the run below the 20-call window (it doesn't stop at call 3 or 5 despite 2/20 ≈ 10% being known long before), nor does it drain the last ten. Cross-checked against SCN-GEN-2d (emitAll → 100% admission → full 200 calls): coherent, no contradiction.\n\nRefutation pass: the 2e claim rests only on in-byte literals and the `toBe(20)` assertion — no external file, PR, or comment-about-elsewhere needed (the \"halts at floor\" comment merely echoes what the code asserts). The exact count 20 is directly present and recounted. The 30-candidate construction and the two-element admit set are both visible. Survives.\n\n```atlas-fact\n{\"claim\": \"In SCN-GEN-2e-2, a 30-candidate frontier whose gate admits only c1 and c2 still incurs exactly 20 proposer calls — the low-admission halt fires at the trailing-20 window boundary, stopping ten short of the frontier rather than draining the remaining candidates.\"}\n```"