"```atlas-fact\n{\"claim\": \"In buildGate, the returned gateHolds never references its `_at: Hash` argument (freshness is re-derived solely against the axes handed to bindGate's drift oracle), and for any node whose kind is not 'predicate' it forwards the string literal 'HOLDS' as the verdict instead of reading one off the node — so callers cannot pin the gate to a historical commit sha, and non-predicate nodes skip the verdict leg entirely.\"}\n```"