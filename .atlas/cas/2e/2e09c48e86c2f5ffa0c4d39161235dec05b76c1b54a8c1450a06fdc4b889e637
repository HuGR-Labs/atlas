"STEP 1 — scratch reasoning (drafted then attacked):\n\nCandidate A: \"Empty `::` pieces are kept, not dropped.\" Traced `descentSteps`: `seg.split(UNIT_SEP)` results are consumed by a bare loop with **no** filter, whereas `pathSegments` explicitly ends in `.filter((s) => s.length > 0)`. Trace `\"a/::/b\"`: `pathSegments` keeps `'::'` (length 2); splitting yields `['','','']`; accumulation produces prefixes `''`, `a/`, `a/::`, `a/::::`, then `a/::::/b` — never `a/b`, which exact-equality child matching can never hit. Contrast is internal to this unit; no reliance on comments about `build.ts` or INDEX tickets. Survives.\n\nCandidate B: \"`coveringPath` includes the root in the returned chain.\" Visible (`[root]`), but an engineer could plausibly guess either convention from the signature; weaker \"changes what they do\" force. Discarded in favor of A.\n\nCandidate C: \"Children match by full accumulated key OR bare token.\" Mechanism is code-visible (`find(...) ?? find(...)`), but the *interesting why* leans on comments citing `build.ts:38` and goldens outside these bytes. Weaker than A; discarded.\n\nCandidate D: runtime `typeof key !== 'string'` guards despite typed signature — true but marginal impact. Discarded.\n\nRe-checked A item by item: the missing filter is a real byte-level asymmetry; the corruption arithmetic (`acc + '::' + ''`) is forced by the shown loop. No counts asserted. A survives.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"In descentSteps, empty pieces produced by splitting a segment on '::' are kept as descent tokens — unlike empty slash-separated segments, which pathSegments filters out — so a malformed key like 'a/::/b' accumulates corrupted prefixes (e.g. 'a/::::/b') and is never normalized down to 'a/b' before exact-equality matching.\"}\n```"