"STEP 1 — Reasoning (scratch):\n\nCandidate A: \"The re-cut contains seven corrections.\" Refuted: the section heading literally says \"seven corrections\", so this is a restatement of visible text, trivially known once read. Discard.\n\nCandidate B: \"#82 is mislabeled completed; EPIC-A5 is unbuilt.\" Its truth rests on assertions about external artifacts (`packages/e2e-blackbox/test/author.ts`, `server.ts:11`, `READ_SURFACE` export sites in `packages/**`, the task tracker) — none visible in these bytes. Discard per the external-claims rule.\n\nCandidate C: \"ADR-0004/0005 gate the build; ADR-0006 Decision 2 is ratified.\" These assert the ratification status of ADRs not shown here. Fact about those documents, not this unit's own content. Discard.\n\nCandidate D: Ledger arithmetic (~25 findings, ~6 resolved). The \"~25\" spans documents outside these bytes; counting table rows gives 15 rows / 17 distinct IDs, which cannot validate \"~25\". Any numeric claim risks miscount/mismatch. Discard.\n\nCandidate E: The acceptance amendment. Check against refutation criteria: the header states the re-cut changes \"not … its acceptance (delete `author.ts`)\", while the corrected-DAG section carries an inline amendment (\"[AMENDED 2026-08-25, owner-decided …] The acceptance is that product-door proof, NOT the physical deletion of `author.ts` (which also carries a legitimate adversarial-fixture role that is kept)\"). Both passages are inside this unit; the operative statement is inline prose, not a pointer to unseen code (the ADR citation is incidental; the substantive claim stands in these bytes). No counts involved. It is not derivable from the path/name, and it changes what an implementer does (do NOT delete `author.ts`). Survives.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"Despite the document's opening paragraph declaring that the re-cuts leaves the campaign's acceptance ('delete author.ts') unchanged, the corrected-DAG section records a 2026-08-25 owner-decided amendment that makes human product-door proof the acceptance and explicitly keeps author.ts as an adversarial fixture instead of deleting it.\"}\n```"