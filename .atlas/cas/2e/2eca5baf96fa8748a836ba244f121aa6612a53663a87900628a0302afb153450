"```atlas-fact\n{\"claim\": \"The insertion-order independence of serNode/serState is enforced only by sorting the outer key collections (entry contentHashes, state nodeKeys) — nothing sorts the properties inside each serialized Event object — so byte-identical output for equal content additionally depends on every event sharing one key-insertion order, which only construction via mkEvent's single object literal guarantees.\"}\n```"