"STEP 1 — scratch reasoning:\n\nReading the code (not the comments) in `gitStoreProvenance`: the `cached` variable lives in the factory's closure, and critically the `catch` block writes `cached = 'trusted'` **before** returning. So any `runGit` throw — missing git, non-repo, transient exec/spawn failure — is latched into the same memo slot as a real classification. Every subsequent invocation of that closure short-circuits at `if (cached !== undefined)` and never re-runs git.\n\nRefutation pass:\n- Derivable from these bytes? Yes — pure control flow of the returned arrow function; no reliance on `run-git.js` internals beyond \"it can throw,\" which the `try/catch` itself asserts.\n- Comment-dependent? The docstring says the function is \"TOTAL\" and \"MEMOIZED,\" but nowhere states that the *failure* verdict is itself memoized (i.e., no retry). The latch-on-error behavior comes from the code alone.\n- Exhaustive count/enumeration? None asserted.\n- Beyond name/signature? `gitStoreProvenance(repoPath: string): () => StoreProvenance` tells you it returns a memoized thunk; it does not tell you errors are permanent. Same applies transitively to `gitSidecarTrust`, which wraps a `gitStoreProvenance` closure — a transient failure on first query pins it to \"trusted\" forever.\n- Does it change what they do? Yes: construction timing matters (don't build/query the seam before git is reliably available), and fail-open-plus-sticky is materially different from fail-open-per-query for anything gating reads/writes.\n\nSurvives. Emitting it.\n\n```atlas-fact\n{\"claim\": \"In gitStoreProvenance's returned closure, a thrown runGit call caches 'trusted' before returning, so the very first query permanently latches the verdict — a transient git or exec failure sticks as the fail-open 'trusted' answer (propagating through any gitSidecarTrust built on it) for the closure's lifetime, with no retry.\"}\n```"