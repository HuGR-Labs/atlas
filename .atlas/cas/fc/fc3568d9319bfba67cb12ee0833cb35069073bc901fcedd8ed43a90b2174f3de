"STEP 1 — Scratch reasoning:\n\nScanning every `runAtlas` invocation in the file: the doctor subcommands actually exercised are `archive` (tests 1, 5), `why` (tests 2, 5), `hotset` (tests 3, 5), and `reground` (tests 4, 5) — plus the failure cases `bogus-sub`, argless `hotset`, and bare `doctor`. I then checked test 6's assertion for the unknown-subcommand guidance: `expect(bogus.stdout).toContain('archive|why|hotset|reground|index')`. Splitting that literal: archive, why, hotset, reground, index — five tokens. Re-scanning all invocations: none passes `'index'` as the subcommand. So the CLI's own enumerated surface (as pinned by this very test) includes a fifth leg, `index`, that no test in this unit ever invokes. The header comment only speaks of \"ALL FOUR advisory legs,\" confirming the mismatch is real and not my miscount.\n\nRefutation pass:\n- Is this derivable from these bytes? Yes — the guidance literal is asserted in test 6, and the complete set of invocations is visible in this file; nothing external is needed.\n- Does it rest on a comment's claim about unseen code? No — it rests on comparing an in-file string literal against in-file invocations.\n- Exact counts admissible? I counted the pipe-separated list item by item (5) and re-checked every `runAtlas` array for an `'index'` element (none).\n- More than a name/signature restatement? Yes — the file name suggests coverage of the doctor surface; nothing in it signals a named-but-untested subcommand.\n\nOther candidates considered and discarded: the mtime-pinning of `projection.json` (visible immediately in `projectionStamp`'s `statSync(...).mtimeMs`); errors landing on stdout with clean stderr (plainly asserted three times); the cross-file move manufacturing mechanical drift (its rationale lives in a past-tense comment about a bug elsewhere).\n\nSurvivor: the `index` gap.\n\n```atlas-fact\n{\"claim\": \"The unknown-subcommand guidance that this test itself pins names five doctor subcommands ('archive|why|hotset|reground|index'), yet every doctor invocation in this suite exercises only four — no test ever calls `doctor index`.\"}\n```"