"Looking at this test file closely, I notice a subtle mismatch between how the node fixtures identify themselves and what the assertions expect.\n\nDrafting candidates:\n- *\"`scopeHasDocs` is hardcoded to `true`\"* — plainly visible in the literal `(): boolean => true`; a summary of what the code obviously does, and the interesting \"why\" lives only in a comment citing #240. Discard.\n- *\"The file never calls `reverifyRelation` despite its name\"* — checkable (imports are only `reverifyFact`/`reverifyStore`), but the header text already points at `reverifyFact` doing the work, so impact is moderate.\n- *Row key mismatch*: The `node()` helper builds `nodeKey: \\`hash-${id}\\``, yet every assertion expects rows keyed by the bare id (`'rel-ar8'`, `'r-ok'`, `'r-drift'`, `'r-nowit'`). Both `node(id)` and `relation(id)` receive the same id string; only the node adds the `hash-` prefix. The string `hash-` never appears in any expectation. So the reported `nodeKey` must come from the fact's own `id` (`asNodeKey(id)`), not the passed node's `nodeKey`. Refutation attempt: could some expectation contain the prefixed key? Scanned every `toEqual`/`toBe`/row literal — none do. Could this rest on a comment? No — it rests solely on comparing the helper's template literal against the assertion literals, all within these bytes. Not an enumeration/count claim. Not a restatement of any name or signature — nothing in `reverifyFact(node, fact, leg, ...)` suggests which identity surfaces in output.\n\nThis survives: it's a hidden behavioral contract (result rows are keyed by fact identity) that changes what an engineer does when correlating reverification output back to stored nodes.\n\n```atlas-fact\n{\"claim\": \"Although the node() helper stamps every node's nodeKey as `hash-<id>`, every expected reverifyFact/reverifyStore row is keyed by the bare id (e.g. 'rel-ar8', 'r-ok') — the rows carry the fact's own id, and the node's hash-prefixed nodeKey never appears in any assertion in this file.\"}\n```"