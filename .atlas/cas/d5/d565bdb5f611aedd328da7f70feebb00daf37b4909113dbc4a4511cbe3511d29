"STEP 1 — REASONING (scratch):\n\nReading the bytes: four tests, each driving either the composed `handler.handle(RECONCILE, {mergeBase})` or the reconstructed `runLeg(fix, seams, rev)`.\n\nCandidate A: \"topic defaults to HEAD\" — rests on the header comment's claim about the handler's contract; only `mergeBase` appears in the call sites, and the defaulting behavior lives outside these bytes. Discard.\n\nCandidate B: \"control test passes `fix.B` because B is HEAD\" — the choice of `fix.B` is in the code, but its *significance* (B == HEAD == topic) rests entirely on comment claims. Discard.\n\nCandidate C: \"`reauthorCount === 1` implies only semantic facts trigger re-author\" — the literal `toBe(1)` is present, but the inference requires `|semantic| == 1`, which `toContain('F_A')` does not establish. Over-infers. Discard.\n\nCandidate D: the polarity contrast between the two mutation scenarios. Checking against the assertion literals themselves (not the comments):\n- TEETH-resolveAnchorAt mutant: `expect(mutant.drift).toHaveLength(0)` and `expect(mutant.exitCode).toBe(0)` — a fully green outcome, matching the no-drift control's exitCode 0.\n- TEETH-reDerives mutant: `expect(mutant.mechanical).toHaveLength(0)` and `expect(mutant.semantic).toContain('F_B')` — misclassification into the blocking class, not a green pass.\n\nEvery clause maps to a literal in the shown code; no reliance on comments about other files, history, or absent code. Not derivable from the file's name/signature (\"compose-recon.test.ts\" says nothing about the two reverted seams behaving with opposite failure polarity). It would change what an engineer does: breaking `resolveAnchorAt` degrades to silent success (nothing alerts), while breaking `reDerives` degrades loudly into the semantic/blocking bucket — so hardening, monitoring, and refactoring decisions around these two seams should not treat them symmetrically.\n\nSurvives. Emitting.\n\nSTEP 2 — RESULT:\n\n```atlas-fact\n{\"claim\": \"Per this file's own assertions, mutating resolveAnchorAt to return undefined is expected to produce zero drift and exitCode 0 (an all-clear matching the no-drift control), whereas mutating reDerives to return false is expected to empty the mechanical list and move F_B into semantic.\"}\n```"