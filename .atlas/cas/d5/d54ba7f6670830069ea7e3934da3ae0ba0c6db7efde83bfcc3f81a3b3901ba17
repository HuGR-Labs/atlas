"```atlas-fact\n{\"claim\": \"importMemory's fail-closed gate validates only the OKF envelope — the format literal is compared exactly, but version is merely type-checked as a number (any value passes, it is never compared to OKF_MEM_VERSION) and records are required only to be an array — then returns parsed.records verbatim without inspecting any individual record's contents.\"}\n```"