"STEP 1 — scratch reasoning:\n\nReading `globToRegExp`: the translation loop has exactly one special-case branch, keyed on `*` (emitting `.*` when followed by another `*`, else `[^/]*`). Every other character falls through to `escapeRe`, which backslash-escapes anything in `RE_SPECIAL = '\\\\^$.|?*+()[]{}'` — a set that includes `?`, `[`, `]`, `{`, `}`, `(`, `)`. So a `?` in a pattern becomes `\\?` in the regex, and `[abc]` or `{a,b}` match their literal bracketed text. There is no other wildcard machinery anywhere in the unit.\n\nDraft fact: \"Only `*` and `**` behave as wildcards; `?` and bracket/brace constructs are matched literally.\"\n\nAttempted refutation:\n- Derivable from these bytes? Yes — it follows from the single `c === '*'` branch plus `escapeRe`'s escape set. No library/convention knowledge needed.\n- Resting on a comment about unseen code? No — the doc comment on `pathMatchesGlob` mentions only `**` and `*`, consistent with the code; the claim rests on the loop and `RE_SPECIAL` literal itself.\n- Exact count/exhaustive list risk? It names the wildcard set; I verified item-by-item: the only character given non-literal treatment is `*` (with its `**` two-char form). `?` is present in `RE_SPECIAL`, hence escaped, hence literal. Holds.\n- More than a name/signature restatement? Yes — `globToRegExp`/`pathMatchesGlob` names suggest standard glob behavior; many dialects treat `?` as a single-char wildcard, and nothing in the signatures reveals it doesn't here. This would change what a caller does (they must not write `?` or `[0-9]` class patterns).\n\nSurvives. Emitting.\n\n```atlas-fact\n{\"claim\": \"In this unit's glob dialect the only wildcards are * (within one path segment) and ** (across separators); ?, [...], and {...} are backslash-escaped by escapeRe and therefore match only as literal characters.\"}\n```"