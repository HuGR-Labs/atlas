"STEP 1 — scratch reasoning (not persisted):\n\nReading the bytes: after the header comment, the module's executable content is exactly one statement — `export { draftVerdict } from '@atlas/adapter-io';`. There are no local declarations, no imports bound into local scope, no logic.\n\nCandidate 1: \"`draftVerdict` fails closed on empty input.\" — REFUTED: this comes entirely from the comment describing behavior of code not present here. Discard.\n\nCandidate 2: \"It was relocated for MCP parity / the ring forbids the import.\" — REFUTED: comment claims about other files and organizational rules; not re-derivable at these bytes. Discard.\n\nCandidate 3: \"Re-export syntax doesn't create a local binding.\" — true, but that's general language knowledge a competent engineer already applies on sight. Discard.\n\nCandidate 4: \"This file is a pure pass-through shim — its entire body is one named re-export of `draftVerdict` from `'@atlas/adapter-io'`, contributing no implementation of its own.\" Check against refutation criteria: fully readable from the code itself (I counted: exactly one statement, nothing else); the module specifier is a code literal, not a comment claim; it does not assert anything about the target module's contents. It is NOT derivable from the name — `draft.ts` naturally suggests the draft builder is implemented here, and the bytes contradict that inference. It changes what an engineer does: edits to verdict-building behavior belong in `@atlas/adapter-io`, not here; this file is behaviorally inert apart from pointing at that specifier. Survives.\n\nSTEP 2 — emit:\n\n```atlas-fact\n{\"claim\": \"Despite its name suggesting the draft-verdict builder lives here, packages/cli/src/draft.ts is a pure pass-through: its entire body is a single named re-export of draftVerdict from '@atlas/adapter-io', with no local implementation, so behavioral changes to verdict composition have no effect if made in this file.\"}\n```"