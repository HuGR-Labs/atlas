"STEP 1 — scratch reasoning (not persisted):\n\nReading the bytes: the fourth `it()` in the first describe performs exactly one mutation between the passing `verify-store` run and the failing one — `rewriteIndex(repo.repoPath, INDEX_CALLER_REMOVED)`, which serializes a brand-new SCIP payload containing only the `util.ts` document (dropping the entire `src/app.ts` document, the sole referencer of `util/greet().`) straight into `.atlas/index.scip`. Nothing else in the shown bytes touches the repo between those two runs: the CAS, policy, and projection are not rewritten, and `rewriteIndex` writes only the index file.\n\nRefutation pass:\n- \"Promote requires curator env\": not provable — the test supplies `CURATOR_ENV` to promote but never demonstrates promote fails without it. Discarded.\n- \"Unverifiable is unreachable from mine/promote\": pure comment claim about other files/#195. Discarded.\n- \"Staging is NOT committed\": ambiguous external semantics, comment-driven. Discarded.\n- Exact-count traps avoided: I do not assert the \"1 of 1\" tallies as the fact; the exit codes 0 and 2 I did verify directly in the assertions (`toBe(0)`, `toBe(2)`) and they are load-bearing here.\n- Is it name/signature-derivable? The filename says \"reverify-store\"; it does not tell you the recheck target is the live `.atlas/index.scip` file, that mutating only that file (store untouched) flips the verdict, or that 2 is the rejected exit code. Those come from `rewriteIndex`'s write target, the sequence in the test bodies, and the literal expectations.\n- Resting on comments? No — every element is checkable in the code: the writeFileSync path, `INDEX_CALLER_REMOVED` having only the `util.ts` document, and the exit-code/status assertions.\n\nSurvivor: the verdict flip is caused purely by rewriting the live index while the durable fact stays byte-identical; exit code is the machine-readable signal.\n\n```atlas-fact\n{\"claim\": \"A sealed, untouched durable fact flips from re-proven (exit 0, 'status: ok') to broken (exit 2, 'status: rejected') solely because `.atlas/index.scip` is rewritten to drop the one document referencing its target — `verify-store` re-proves stored witnesses against the live index file, and its exit code, not just stdout, is the machine-readable verdict.\"}\n```"