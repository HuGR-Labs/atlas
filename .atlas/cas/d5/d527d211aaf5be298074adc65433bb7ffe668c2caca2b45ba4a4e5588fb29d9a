"STEP 1 — REASONING (scratch):\n\nReading the bytes: this is a small tsconfig extending the repo-root base, emitting to `dist`, including `src` and `test`, and carrying four project references.\n\nCandidate facts drafted and tested:\n- *\"Tests live in a separate config\"* — refuted: `include` explicitly lists `\"test\"` alongside `\"src\"`; the opposite is true.\n- *\"Memory depends on sibling packages\"* — candidate survives: the `references` array literally names `../contracts`, `../kernel`, `../knowledge`, `../retrieval`. No comment involved, nothing asserted about unseen file contents — the wiring is declared in these bytes. Project references being the mechanism that orders/prerequisites builds is the core semantic of the field itself, comparable to knowing what `include` means. I counted the items: exactly four, each checked against the bytes.\n- *\"Everything inherits from the base config except outDir\"* — true but nearly a restatement of the visible keys, low behavioral impact.\n- *\"Test output lands under dist/test\"* — rejected: depends on inferred `rootDir` behavior from a base config not shown here; cannot fully re-derive from these bytes alone.\n\nSurvivor: the reference wiring — it is not inferable from the file's name/path alone, it is fully checkable in these bytes, and it changes what an engineer does (where memory's types come from, what must be built first, where to trace cross-package breakage).\n\nSTEP 2 — RESULT:\n\n```atlas-fact\n{\"claim\": \"The memory package's TypeScript build is wired through project references to four sibling packages — contracts, kernel, knowledge, and retrieval — making their outputs build prerequisites for memory.\"}\n```"