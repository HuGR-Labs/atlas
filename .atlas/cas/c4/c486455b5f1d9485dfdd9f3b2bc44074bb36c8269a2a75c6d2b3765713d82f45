"STEP 1 — Reasoning scratch:\n\nReading the bytes of `createRelate`: `dep = boundClosure(rev.facts)` and `dependencies = boundClosure(axes.forward(unit)).closure` are both post-cap results — `boundClosure` slices the ranked list to `cap` (= K = 8 default). When `opts?.coChanged` is set, the exclusion set is built via `structuralIds(enclosing, dep.closure, dependencies, governing)` — i.e., from the **capped** dependents/dependencies arrays plus the raw `enclosing`/`governing` bands. `labelCoChanged` then keeps every `rev.coChanged` fact whose `nodeId` is NOT in that set.\n\nRefutation attempts:\n- Is this derivable from these bytes? Yes — it's pure control flow: the slice happens inside `boundClosure` before `structuralIds` ever sees the data.\n- Is it leaning on outside knowledge or stale comments? No — in fact the comments advertise \"coChanged ∩ structuralBands = ∅\" (RETR-10f) without noting the cap subtlety; the code itself shows the disjointness is enforced only against the *returned* rank-prefixes, so a co-changed node that is a dependent but ranked out of the top-8 can legitimately resurface labeled `coChanged`.\n- Exact counts/enumerations? None asserted; qualitative form used.\n- Restatement of name/signature? No — the signature (`structuralIds(...)` taking band arrays) doesn't reveal that those band arguments are the truncated outputs, nor the behavioral consequence.\n\nCandidate survives. Emitting.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"The node-ID exclusion set that keeps the opt-in coChanged band disjoint is built from the already-capped dependents and dependencies lists (each sliced to the K=8 rank-prefix), not from the full hop-bounded closures, so a co-changed node truncated out of either structural band can still be surfaced under the coChanged label.\"}\n```"