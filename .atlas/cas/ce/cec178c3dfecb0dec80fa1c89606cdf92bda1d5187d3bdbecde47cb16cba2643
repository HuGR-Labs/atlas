"STEP 1 — REASONING (scratch):\n\nCandidates drafted and refuted:\n\n1. *\"Unknown data shapes yield no block.\"* — Visible directly from the final `return ''`; borders on restating control flow. Weak.\n2. *\"`renderRefusal` forces `rejected` status through the shared body.\"* — Plain from the one-line body; rationale lives only in comments about `map.ts`/call sites not shown. Discard.\n3. *\"Freshness absence defaults differ across branches.\"* — Check the bytes: pack branch uses `` `${i.freshness ?? '?'}` `` for both `inv` and `advisory` rows; negations branch uses `` `${n.freshness ?? 'DRIFTED'}` ``. Both literals sit in the map expressions themselves, not in comments. No comment asserts the negation default (N4 comment only names the FRESH/DRIFTED domain); the InvRow comment covers only its own `?`. Same optional field name, two different absence markers in one file — not derivable from any name, signature, or type declaration, and it changes what a parser author or extender of this renderer must handle (`[DRIFTED]` on screen can mean \"field absent\", not \"verdict computed\"). Survives.\n4. *\"Absent `advisoryDropped`/`tokenEstimate` coerce to 0 and print anyway.\"* — True in bytes (`typeof x === 'number' ? x : 0`), but partially overlaps the comment's own framing; less crisp than #3.\n5. *\"Reconcile branch types `anchorWas`/`anchorNow` but never renders them.\"* — Verifiable, but marginal actionability.\n6. Guard-ordering claims (`linked` before `{id}`, relations first, etc.) — either plain control flow or justified only by comments about other files' shapes. Discard.\n\nSurvivor: #3, verified twice against the source — `?? '?'` appears exactly in the inv/advisory map lines; `?? 'DRIFTED'` appears exactly in the negation map line on the non-focus path.\n\nSTEP 2 — RESULT:\n\n```atlas-fact\n{\"claim\": \"A negation row with an absent `freshness` field renders the literal marker `DRIFTED` (via `?? 'DRIFTED'`), whereas invariant and advisory rows in the query-pack branch of the same file render `?` for the same absent field.\"}\n```"