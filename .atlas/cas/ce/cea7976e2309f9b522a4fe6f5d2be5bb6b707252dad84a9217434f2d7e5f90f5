"STEP 1 — Reasoning (scratch):\n\n- Candidate 1: “`calls` never seals proven on a feed that proves `depends-on`.” Refuted as too name-given: the test title itself (“AR-5 a `calls` relation NEVER obtains a proven seal”) states this; nothing new beyond the name.\n- Candidate 2: “Self-edge throws at `relationKey` but `admit` drops totally.” Partially given away by the test name (“MalformedRelationError / dropped”) and it describes expected behavior of unseen code; weaker.\n- Candidate 3: “Witness has exactly three legs.” Restates an inline assertion literal already quoted in the test body; low value, and the exact-shape point is essentially what the test name says (round-trips).\n- Candidate 4: **The string-as-hash smuggling in the fixture feed.** Reading the bytes: `reverseCallers` returns `(callers as unknown as readonly Hash[])` — ordinary path strings force-cast into `Hash`; `definesAt` returns `'src/b.ts' as unknown as Hash`; and the adapter hands the oracle `pathOfHash = (h) => String(h)`, i.e., hash→path decoding is bare stringification. So within this harness, hashes and source-file paths are deliberately the *same strings*, which is the load-bearing trick making the in-memory fake satisfy the typed API. This is not derivable from the file name or any signature (the signatures say `Hash`), rests only on declarations/literals present in the unit, involves no counts or claims about other files, and would change what an engineer extending or debugging this feed does (they must preserve the string≡hash convention or the real oracle stops binding edges). Survives.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"The fixture SymbolReverseApi smuggles plain path strings into Hash-typed slots via double casts (callers as unknown as readonly Hash[], 'src/b.ts' as unknown as Hash) and pairs them with a pathOfHash that is just String(h), so within this harness symbol hashes and file paths are deliberately interchangeable strings.\"}\n```"