"STEP 1 — scratch reasoning (not persisted):\n\nCandidates drafted from close reading:\n\n- *\"Authz keys on actor+scope, never a payload owner\"* — the spoof-guard golden shows `core: ['alice@x']` policy accepting an emit whose fact carries `owner: 'attacker@x'`. Fully in-byte verifiable… but the `it(...)` title itself announces \"the fact payload NEVER sources the actor (spoof-guard)\", so it largely fails the \"not known from the name\" bar. Discard.\n- *\"composeRuntime mutates no global env\"* — supported by the assertion `expect(process.env.ATLAS_ACTOR).toBeUndefined()` after compose+emit, but mostly carried by an inline comment's framing; the assertion alone is thinner. Discard.\n- *\"Each governed test builds its own repo/handler; env saved/restored in finally\"* — plain structural summary of what the code visibly does. Discard.\n- **Assertion asymmetry in SCN-CR-2**: listing every `ok`-related expect in the file — `expect(v.ok).toBe(true)` (SCN-CR-1) and `expect(initV.ok).toBe(true)` (SCN-CR-2). For `queryV` in SCN-CR-2 the only check is `expect(queryV.rejected ?? '').not.toContain('not wired at this seam')`; no `queryV.ok` assertion follows before the `finally`. Same for `q` in SCN-CR-F5. Refutation pass: not derivable from any title (the test title says \"init/query still resolve\", implying parity with init); rests on no comment about unseen code; no numeric/enumerative claim needed — contrastive form suffices; verified item-by-item against the shown expects. Survives.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"In SCN-CR-2 the init leg is asserted ok === true, but the query leg is only asserted to not contain the 'not wired at this seam' marker — query's ok is never asserted anywhere in this file, including SCN-CR-F5.\"}\n```"