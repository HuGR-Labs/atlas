"STEP 1 — Reasoning scratch (drafted, then attacked):\n\n- Draft A: \"kind-ternaries do nothing\" — traced `restrictTo`, `regroundTemplate`, `retireTemplate`; all three have byte-identical consequent/alternate. Survives inspection, but it's a refactor note with weak operational bite.\n- Draft B: \"vacuously mechanical when nothing drifted\" — derivable from `[].every` semantics, but its *importance* rests on the comment's claim about the merge gate in `compose.ts`, which isn't in these bytes. Discard.\n- Draft C: \"`nodes()` throws on untrusted reads\" — whether `refuseUntrustedRead` throws lives in `read-provenance.ts`, not shown. Rests on unseen bytes. Discard.\n- Draft D: plan's execution structure — trace it: `plan` first calls `drift(fact)`; `drift` calls `nodes().find(...)` and `classifyDrift`, which calls `driftedEntries(revIndex, fact, at)`. Back in `plan`, it then calls `nodes().find(...)` a second time, and on the mechanical path calls `driftedEntries(revIndex, grounded, HEAD)` again. The `nodes` closure is `() => { refuseUntrustedRead(trusted); return currentNodes(rehydrateProjection(store)); }` — no cache variable anywhere in the module, so every invocation rehydrates the whole projection fresh. Refutation pass: does every result-returning `plan` call hit the second `nodes()`? Yes — the second lookup sits before the retire/reground split, and only the `item === undefined` early return skips it. Is \"twice\" verified? `drift` contains exactly one `nodes()` call; `plan` contains exactly one more; `rehydrateProjection` appears nowhere else and is unmemoized. Nothing rests on comments or other files; every clause is checkable in these bytes. It is invisible from the exported signature `(fact: string) => {action, emit} | undefined` and changes what a perf-conscious caller/integrator does. Survives.\n\nSTEP 2 — Emitting the surviving fact:\n\n```atlas-fact\n{\"claim\": \"Every plan call that returns a result rebuilds the entire durable projection twice — once inside drift()'s nodes() lookup and once in plan's own repeated nodes().find — because the nodes() closure memoizes nothing between calls, and on the mechanical path plan additionally recomputes the driftedEntries verdicts that classifyDrift already computed during drift().\"}\n```"