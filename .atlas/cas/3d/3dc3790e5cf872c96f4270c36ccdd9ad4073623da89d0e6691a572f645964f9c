"STEP 1 — scratch reasoning (not persisted):\n\nCandidates drafted from the bytes:\n\n1. *\"Trailer uses PascalCase keys while Metering uses camelCase\"* — Survives verification: the `trailer9` literal shows `WP/Model/Gates/Verdict/TranscriptSha`; the `requiredFields` array and `wp9` literal show `model/tokensIn/…/verdict/transcriptSha`. Real, but low impact on what an engineer does next.\n2. *\"`bodyB2.payload` is 960 chars (`'REPORT-B2-'.repeat(96)`)\"* — Counted and true (10×96), but trivial and unlikely to change behavior.\n3. *\"deserialize's total-read contract covers `{}`\"* — Checked the actual expects, not just the comment: `expect(() => deserialize('')).not.toThrow()`, `deserialize('')` → null, `deserialize('   ')` → null, **and `deserialize('{}')` → null**. The `{}` case is the surprising one: a syntactically valid JSON document still deserializes to `null` rather than throwing or returning a partial object. This is encoded in executable assertions in this unit (literals + control flow), not resting on any comment's claim about unseen code. Refutation attempt: does it depend on src internals? No — phrased as what THIS suite pins, every token of which is present here. It would decisively change what someone touching `provenance` totality semantics does (they must keep valid-but-empty documents mapping to `null`). Candidate 3 survives best.\n\nSTEP 2 — emitting the surviving fact:\n\n```atlas-fact\n{\"claim\": \"Beyond blank input, this suite pins that even a syntactically valid JSON document ('{}') must deserialize to null rather than throwing, so deserialize's total-read contract treats a parseable object lacking a trailer the same as empty or whitespace-only input.\"}\n```"