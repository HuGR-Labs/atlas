"**STEP 1 — Reason freely (scratch):**\n\nReading the bytes closely:\n\n- Candidate 1: \"Side-indexes are stripped at every nesting level.\" The code does show the filter running inside the recursive `serialize`, so it applies to every object node, not just the root — but the header comment already announces \"EXCLUDED at every level,\" so a careful reader of the unit likely knows it. Weaker candidate.\n- Candidate 2: \"Huge integer-valued numbers like 1e21 survive the float ban and emit exponent form.\" True per JS semantics (`Number.isInteger(1e21)` → true, `String(1e21)` → `'1e+21'`), but its truth leans mostly on runtime `toString` trivia outside the shown bytes. Discard.\n- Candidate 3: \"NFC key collisions throw instead of picking a winner.\" Real and in the code, but it is the loudest, most heavily commented feature of the file — an engineer reading it would already know. Discard.\n- Candidate 4: **The `undefined` asymmetry.** Two independent code paths handle `undefined` differently, and nothing in the name, signature, or comments flags the contrast:\n  - Object branch: `present = Object.keys(o).filter((k) => !SIDE_INDEX.has(k) && o[k] !== undefined)` — an `undefined` *property value* is removed before serialization, so `{a: undefined}` canonicalizes exactly like `{}`.\n  - Top of `serialize`: `if (v === null || v === undefined) return 'null'` — an `undefined` reaching serialization any other way becomes the literal token `null`. The array branch `[${v.map(serialize)...}]` does **not** filter, so `[undefined]` canonicalizes byte-identically to `[null]`.\n  \n  Refutation pass: derivable purely from these two lines of code (no library knowledge, no comment-about-elsewhere dependency)? Yes — both behaviors are literal statements in the control flow. Does it assert a count or exhaustive list? No. Is it more than a name/signature restatement? Yes — the signature `canonicalForm(obj): Uint8Array` reveals nothing about `undefined` policy, and the divergence (property-dropped vs. array-element-as-null) is invisible without tracing both branches. Would it change what an engineer does? Yes — it means `{a: undefined} ≡ {}` in identity space, while `[undefined] ≡ [null] ≢ []`, which matters when modeling optional fields or avoiding accidental identity collisions.\n\nCandidate 4 survives.\n\n**STEP 2 — Emit result:**\n\n```atlas-fact\n{\"claim\": \"`undefined` is handled asymmetrically: as an object property value it is filtered out before serialization (so {a: undefined} canonicalizes like {}), but anywhere else it reaches serialize and is emitted as the literal null (so [undefined] canonicalizes byte-identically to [null]).\"}\n```"