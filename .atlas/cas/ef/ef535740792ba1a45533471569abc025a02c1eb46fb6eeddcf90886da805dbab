"STEP 1 — REASONING (scratch)\n\nCandidates drafted and refuted:\n\n- *\"Tests drive the real stdio server / real binary\"* — rests on the header comment plus opaque `harness.js`; `mcpSession`/`runAtlas` internals aren't in these bytes. Discard.\n- *\"Mechanical drift classifies as such because bytes are identical\"* — comment's claim about `@atlas/knowledge` logic not shown. Discard.\n- *\"Env vars are saved/restored\"* — derivable from `beforeAll`/`afterAll`, but generic boilerplate; low consequence. Held as backup.\n- *\"Exactly five governance doors enumerated\"* — countable (init, query, emit, reconcile, link in `CALLS`), verified, but it's plainly visible in the array literal; borderline restatement. Backup.\n- *\"atlas-emit/atlas-link get no valid-arg MCP call in this file\"* — absence within these bytes, checkable; decent backup.\n- **Coupling candidate:** scan the literals. `beforeAll` creates `src/foo.ts`, then executes `git mv src/foo.ts src/bar.ts` + commit. Much later, S26.3 asserts `toEqual(['territory: src/bar.ts', 'territory: src/deep'])` — the string `src/bar.ts` exists nowhere except setup's rename and this expectation; `src/foo.ts` appears only in setup. Likewise S26.1's `expect(out.mechanical).toHaveLength(1)` is satisfied only because the sole post-`genesis` change constructed in this file is that rename. Refutation pass: (a) derivable solely from shown bytes? yes — control flow of `beforeAll` plus the verbatim two-element literal; no reliance on comments about `wire.ts`/`marshal.ts`/ADR-0007. (b) enumeration risk? the two-element array appears verbatim; re-checked. (c) more than name/signature restatement? yes — the file name says nothing about a rename-driven fixture tree, and the coupling spans ~250 lines between `beforeAll` and a distant `describe`. (d) changes what they do? yes — editing the fixture files, the rename step, or its ordering silently breaks both S26.1's count and S26.3's exact list. Survivor.\n\nSTEP 2 — RESULT\n\n```atlas-fact\n{\"claim\": \"The S26.3 hard-coded expectation ['territory: src/bar.ts', 'territory: src/deep'] asserts the post-rename fixture tree produced by beforeAll's `git mv src/foo.ts src/bar.ts` — not the files the fixture originally created — so altering the rename (or fixture files) silently breaks both this assertion and S26.1's mechanical length-of-one check.\"}\n```"