"STEP 1 — SCRATCH REASONING\n\nScanning for facts that live in the CODE (declarations, spreads, literals, assertions) rather than in comments pointing at unseen files (`projection-query-index.ts`, `tools/src/query.ts`, `map.ts` — all out of bounds as claims).\n\nCandidates drafted and refuted:\n\n- \"`stale` is an ANY-fold over under-scope facts\" — sourced from header/comment claims about another file. Discard (not re-derivable here).\n- \"`underScope` is segment-wise, not startsWith\" — same problem; the *interpretation* rests entirely on comments about `projection-query-index.ts ~27-34`. The byte-visible residue (fixture has `sr/other.ts`; querying `sr` → exit 0, empty `invLines`) is real but the interesting part is comment-borne. Weak.\n- \"`tokenEstimate` equals summed claim char lengths\" — partially pinned by `toContain('tokenEstimate: ${claim.length}')`, but the describe heading already announces \"char-sum proxy,\" so it's near-restatement. Discard.\n- \"Tests share repos across `it` blocks; S13a test 2 depends on test 1's emitted fact\" — true and byte-checkable (`staleRepo` created once; `': foo baseline'` literal emitted only in test 1, required in test 2). Survives, but it's a structural observation, moderately useful.\n- **The DRIFTED/[FRESH] inversion**: In S13a test 2, the code builds `const drifted: GroundedFact = { ...base, freshness: 'DRIFTED' }`, asserts `emitFact(...).exitCode === 0` (so the overridden freshness did not block admission), then asserts the query row renders `` `inv T1 ${drifted.id} [FRESH]: foo drifted-signal` `` — the literal tag `[FRESH]` hardcoded for the very fact persisted as DRIFTED — and separately asserts `'  stale: true'` at envelope level. Every clause is a literal/spread/assertion inside these bytes; none requires trusting a comment about another module. It is not derivable from the file name or signatures (one would naively assume a DRIFTED-persisted fact renders `[DRIFTED]`, or that emitting it would fail). It is qualitative, no counts. It materially changes behavior: anyone extending the renderer, writing freshness-related tests, or consuming query output learns that the per-row tag does not echo the persisted `freshness` value and drift is observable only via the pack-level `stale` flag. SURVIVES.\n\nCross-check the four byte anchors once more: spread override ✓; `expect(e.exitCode).toBe(0)` immediately after the DRIFTED emit ✓; hardcoded `[FRESH]` in the toContain template using `drifted.id` and the DRIFTED fact's own claim text ✓; `expect(r.stdout).toContain('  stale: true')` in the same test ✓.\n\nSTEP 2 — RESULT\n\n```atlas-fact\n{\"claim\": \"In S13a, the test overrides a drafted fact to freshness:'DRIFTED', expects the emit to still succeed with exit code 0, and then expects that same fact's query row to render the hardcoded tag '[FRESH]' — with drift surfacing only as the pack-level 'stale: true' flag, not on the row itself.\"}\n```"