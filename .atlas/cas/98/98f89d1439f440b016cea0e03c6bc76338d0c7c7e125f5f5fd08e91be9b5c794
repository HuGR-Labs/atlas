"STEP 1 — scratch reasoning (not persisted):\n\nCandidates considered:\n\n1. \"MAX_SEAM_CARRY is pinned to exactly 74 by `expect(MAX_SEAM_CARRY).toBe(74)`.\" — Survives byte-check (literal present), but the surrounding justification rests on a long comment about family floors/prefixes inside `src/scrub.ts`, which isn't shown. The bare number is also fairly guessable once you see the import. Weaker candidate.\n\n2. \"`assertNoSecretRun` only guarantees no run of ≥8 consecutive secret bytes survives (default `minRun = 8`).\" — True and local, but it's essentially restated by the parameter name/default right in the helper's body; low surprise value.\n\n3. \"The differential sweep is deterministic (LCG seeded with 20260801).\" — Local and checkable, but marginal impact on what an engineer would do.\n\n4. Reachability gate: the final describe block reads `../src` via `readdirSync`, filters `.ts` files through `callsSeam` (a probe that strips comments, excludes the function's own declaration and method-style calls), and asserts the result is `[]`; it first proves the probe fires by mutating a locally-read copy of `transcript-store.ts`. Refutation pass: does the truth rest on a comment about other files? The *enforcement mechanism* is fully in these bytes — scan, predicate, `toEqual([])` — and the mutant teeth (`expect([mutant !== door, callsSeam(mutant)]).toEqual([true, true])`) are constructed and evaluated locally. Nothing in the file name (`scrub-seam.test.ts`) or the imports hints that the suite audits its own caller set. Action-changing: an engineer who wires `admitToBuffer` into any production module will get a failure from this unrelated-seeming test file and must recognize it as deliberate calibration scaffolding rather than noise. Survives.\n\nChosen: candidate 4, phrased strictly from the code's own mechanics, no reliance on comment claims about other files' contents.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"This test file polices its own reachability: it scans every .ts file under ../src with a mutant-verified probe and asserts that none of them calls admitToBuffer, so wiring the streaming seam into any production code path makes this suite fail by design.\"}\n```"