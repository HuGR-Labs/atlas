"Looking at this roadmap unit, I need a fact that's buried in the prose, not inferable from \"it's the CAMPAIGN-10 roadmap,\" verifiable within these bytes, and action-changing.\n\n**Candidates drafted:**\n1. *Partition arithmetic (73 = 21+16+10+7+9+10)* — I recounted each epic's REQ list: A1 (1a–e,2a–e,3a–g,4a–d)=21; A2-a (5a–e,6a–f,7a–c,8a–b)=16; A2-b (9a–c,10a–c,13a–d)=10; A3=7; A4=9; A5=10; no ID repeats. Survives verification, but it's a consistency check the reconciler table already advertises — low surprise value.\n2. *EPIC-A4 runs parallel to EPIC-A3* — stated in the DAG check, verifiable, but modest scheduling detail.\n3. *EPIC-A1 deliberately unsplit* — stated, but the load-bearing \"11 guards\" figure cannot be re-derived from these bytes (no per-REQ guard labels shown), so the full claim fails the countability test.\n4. **EPIC-A5 amendment** — the epic's demo line was amended 2026-08-25 so that acceptance is the demonstrated MCP-only author-and-emit proof, *not* deleting `author.ts`, which is explicitly kept. Refutation pass: does it lean on external files? The pointer to ADR-0004 explains *why*, but the operative statements (amended date, acceptance ≠ deletion, retention of `author.ts`) all sit in these bytes. Nothing else in the unit contradicts it, and it postdates any earlier reading of the doc. It changes what an engineer does twice: it defines \"done\" for the entire transport-symmetry epic and forbids the otherwise-obvious cleanup action. Candidate 4 survives.\n\n```atlas-fact\n{\"claim\": \"EPIC-A5's acceptance criterion was amended on 2026-08-25 to be the demonstrated proof that an agent can author and emit a fact using MCP doors alone — explicitly NOT the deletion of author.ts, which is retained (with its happy-path re-point deferred as hygiene follow-up).\"}\n```"