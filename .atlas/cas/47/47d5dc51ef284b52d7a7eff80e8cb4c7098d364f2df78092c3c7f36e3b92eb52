"Looking closely at the bytes: the `node()` fixture constructs `nodeKey: hashOf(id)` where `hashOf` is a SHA-256 hex digest, yet every expected row asserts `nodeKey: 'nk-a'`-style **bare id strings**. Cross-referencing fixture construction with the expected literals shows the report's key cannot come from the `CurrentNode`'s actual `nodeKey` field. Checking alternatives: `scopeHasDocs`/`docExists` wiring and the `docExists` `.some`-vs-\"Set\" comment tension are weaker or rest on comment interpretation; the bare-id-vs-hash contract is directly verifiable in this unit alone (a 64-char hex digest cannot equal the short id it was derived from), survives refutation attempts, and materially changes how an implementer or consumer of `reverifyFact`/`reverifyStore` must source the row identifier.\n\n```atlas-fact\n{\"claim\": \"Every expected result row carries the bare fixture id (e.g. 'nk-a') as its nodeKey even though the paired CurrentNode fixture's nodeKey field is the SHA-256 hash of that id, so the report's nodeKey is provably not copied from CurrentNode.nodeKey.\"}\n```"