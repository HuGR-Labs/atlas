"STEP 1 — scratch reasoning (candidates tested against the bytes):\n\n- *Candidate: \"READ_SURFACE has exactly 6 members in order...\"* — verifiable by counting the literals (anchors, slots, draft, check, doctor, node = 6), but the header comment already announces this, and it mostly restates what the test plainly pins. Weak.\n- *Candidate: \"nodeSource.resolve ignores its address argument...\"* — true in the bytes (`spy.get(seededId)` regardless of `nodeAddr`, returning `{...candidate, id: nodeAddr}`), but narrow; unlikely to change what most engineers do.\n- *Candidate: comment-derived facts* (TS5055 collision, layer-guard scan scope, owner-decided exclusion of `atlas-diff`, guard subprocess probes) — all rest on comments asserting things about files/decisions NOT in these bytes. Discarded per rules.\n- *Survivor: the import mechanics of the write-spy harness.* Fully checkable in the code itself: `WRITE_SPY_STORE_PATH` is built by `['..','..','cli','test','write-spy-store.js'].join('/')`, and `createWriteSpyStore`/`seedSomeBytes` are obtained via a **top-level** `await import(WRITE_SPY_STORE_PATH)` — a runtime-computed, non-literal specifier sitting mid-file, far from the static import block — with their types supplied only by locally declared interfaces through an `as` cast (exactly two destructured names, counted). Nothing in the unit's name or signature reveals a hidden runtime cross-package dependency; refactoring the CLI test helper, running type-aware tooling, or assuming all deps are statically analyzable would be wrong-footed by this. No reliance on comment claims needed.\n\n```atlas-fact\n{\"claim\": \"The write-spy harness is not statically imported: this test builds the specifier '../../cli/test/write-spy-store.js' via an array join and resolves it with a top-level await import(...), destructure-binding exactly createWriteSpyStore and seedSomeBytes whose types come solely from locally declared interfaces in the accompanying as-cast.\"}\n```"