"STEP 1 — scratch reasoning (not stored):\n\nScanning the bytes for candidate facts beyond name/signature:\n\n- *Candidate A*: \"`resolveCurrent` scans only `spatial` and `territory`, excluding `dependency`.\" — True and code-visible (`for (const root of [src.spatial, src.territory])`), but the header comment announces this exclusion prominently; a reader of the shown unit already knows it. Weak on novelty.\n- *Candidate B*: \"`findByKey` treats a node whose `subtreeHash` equals its own `key` as ABSENT.\" — Code-visible (`String(node.subtreeHash) === node.key ? undefined : node.subtreeHash`) and non-obvious from the signature, but the adjacent comment states it outright; low surprise for anyone who read the file.\n- *Candidate C*: \"`isGrounded` is not actually total.\" Refutation attempt: is this in the bytes? Yes — `g.entries.length` and `e.anchor.subtreeHash` are dereferenced unguarded. Trace the throw paths item by item: `entries` nullish → `g.entries.length` throws; entry element `null` → callback runs (`Array.prototype.every` invokes on present null elements) → `null.anchor` throws; entry `{}` → `undefined.subtreeHash` throws. Only `subtreeHash` itself is guarded with `typeof`. Meanwhile the docstring loudly claims \"pure + total … fail-CLOSED on everything else,\" and `driftDetect` calls this first (`if (!isGrounded(grounding)) return 'DRIFTED'`), so the throw escapes the sealed no-throw oracle — exactly in the JSON.parse/MCP/CAS scenario the comment itself says to expect. This survives: derivable purely from these code bytes, contradicts (rather than leans on) the comment, no exact-count claims, and far beyond what the name/signature `(g: Grounding) => boolean` reveals. Actionable: it changes whether you trust the predicate with unvalidated parsed input or pre-shape-validate it.\n\nCandidate C survives; discard A and B.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"Despite its documented pure-and-total, never-throw contract, isGrounded dereferences g.entries.length and each e.anchor unguarded (only subtreeHash gets a typeof guard), so a runtime-parsed grounding with nullish entries, a null element, or an anchor-less entry throws a TypeError out of driftDetect instead of failing closed to DRIFTED.\"}\n```"