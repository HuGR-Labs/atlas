"STEP 1 — Scratch reasoning (not persisted):\n\nScanning candidates:\n\n1. *\"All four scenarios pass an empty dependency-edge list\"* — I checked each `it` block: every `reconcile` call uses `NO_GRAPH` (2 in 15b-2, 2 in 15c-2, 1 in 15d-2, 1 in 15e-2). Verifiable, but it's mostly a coverage observation; plausible but weaker actionability.\n\n2. *\"$0-LLM title only checks determinism\"* — the 15c-2 assertion is visibly a self-comparison of two identical invocations; no spy/mock verifies LLM absence. Derivable and interesting, but partly a critique of the label rather than a positive contract.\n\n3. **Empty-string owner as the \"generate\" sentinel** — in 15b-2, `T('http', 'grace', …)` round-trips to `'grace'`, while `T('http', '', …)` comes back as `'frank'`, an owner taken from `httpBlame` (frank appears on a/b/c; grace on d). So the bytes demonstrate: an empty-string owner field is not preserved — reconcile substitutes a blame-derived owner — whereas any non-empty owner wins outright. Refutation attempts: Does it lean on invisible code? No — both expectations are asserted here. Does it rest on a comment's claim about elsewhere? No — the trailing comment merely paraphrases the adjacent assertion, and I re-derived it from the literals themselves. Did I avoid unverifiable counts? Yes — I dropped the \"majority\" framing (these bytes can't distinguish majority from other selection rules since frank is also the first blame entry) and stated only what is asserted. Is it a restatement of name/types? No — the file/scenario names say \"override beats generated\" but nowhere state that the machine-readable sentinel for \"generated\" is specifically the empty string, which is exactly what a caller constructing a `Manifest` needs to know. Survives.\n\nEmitting the surviving fact:\n\n```atlas-fact\n{\"claim\": \"A listed territory whose owner is the empty string does not keep that value: this suite asserts reconcile resolves it to a blame-derived owner ('frank'), while a non-empty owner ('grace') is preserved verbatim.\"}\n```"