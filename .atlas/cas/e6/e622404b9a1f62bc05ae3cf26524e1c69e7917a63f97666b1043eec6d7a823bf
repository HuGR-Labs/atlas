"STEP 1 — SCRATCH\n\nScanning the bytes for non-obvious behavior beyond \"it's a spec-conformance CI gate\":\n\n- Header/docstring claims (build-first requirement, drift history, amendment dates) mostly describe process history or other files (`drift-patterns.mjs`, `dist` exports, docs corpus) — those are excluded by the refutation rules.\n- The `eq` helper does positional `===` comparison, so surface arrays are order-sensitive — a real subtlety, but it sits inline exactly where an editor of these constants would look; plausible but not the most hidden knob.\n- Line 8 of code: `const REPO = process.env.SPEC_CONFORMANCE_GUARD_ROOT ?? join(dirname(fileURLToPath(import.meta.url)), '..', '..')` — the entire scan root (surfaces import, doc sweep, digest pins, fan-out index all derive from `REPO`) is redirectable via an env var, and the fallback is anchored to the *script's* location, not `cwd`. No comment anywhere in the file mentions `SPEC_CONFORMANCE_GUARD_ROOT`; it exists only in this line. Refutation check: derivable purely from this declaration ✓; not a comment's claim about elsewhere ✓; no counts/enumerations ✓; not implied by the name `spec-conformance-guard.mjs` ✓; actionable (running the gate against an alternate checkout, wiring it into another runner, diagnosing \"wrong tree scanned\" failures) ✓.\n- Considered and discarded: the hardcoded success-message counts (verified true but merely restates literals); the idx terminal-block skip (prominently commented, so not unknown); uncaught-exception paths in the sweep (an absence-flavored claim, riskier).\n\nSurvivor: the env-var root override + script-relative fallback.\n\nSTEP 2 — RESULT\n\n```atlas-fact\n{\"claim\": \"The directory tree this gate audits is taken from the SPEC_CONFORMANCE_GUARD_ROOT environment variable when set, and otherwise resolved relative to the script's own file location (two parent directories up) rather than the current working directory — a knob documented nowhere else in the file.\"}\n```"