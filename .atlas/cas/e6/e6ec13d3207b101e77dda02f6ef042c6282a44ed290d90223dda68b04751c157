"STEP 1 — scratch: Reading the bytes. The unit is a docs page, so the checkable \"literals\" are the transcripts, the exit-code table, and the tool-emitted `next:`/`invariant:`/`reason:` strings captured inside transcripts.\n\nCandidates drafted and refuted:\n\n- *Stale is a watermark, not live re-derivation* — its proof leans on prose citing `packages/adapter-io/src/projection-query-index.ts` and \"Measured:\" narratives about `doctor why` behavior in modules not shown. Discard.\n- *Per-row `[FRESH]`/`[DRIFTED]` verdicts re-derived on read; `[?]` for pre-field doors* — asserted in prose, never demonstrated in any transcript here; `[?]` appears only as a prose token. Discard.\n- *MCP envelope shape / schema history* — the \"Transport differences\" section explicitly narrates what the published schema \"was not\" (past tense about an artifact outside these bytes); the envelope is schematic with `…`, not observed output. Discard.\n- *Empty packs render extra `territory:`/`axisHash:` keys* — a diff between two examples, but I can't tell whether the populated example faithfully shows all keys or merely omits them; rests on absence-in-an-example. Too fragile. Discard.\n- *Exit taxonomy* — check candidate. Evidence is entirely in-unit: the table declares three codes; the `--by graph` transcript shows `status: error` + `# exit 1`; the cover-refusal transcript shows `status: error` + `# exit 1`; the untrusted-store transcript shows `status: rejected` + `# exit 2` **and** the tool's own emitted `invariant:` line stating a governed refusal means the call was well-formed and re-running won't help. No reliance on unseen code — the CLI-3b sentence is captured output, i.e., bytes of this unit. Not derivable from the name `query` or the synopsis line, which suggest nothing about a three-way exit/status split. Actionable: callers must branch (fix args on 1; repair store hygiene, don't retry, on 2). Survives.\n- Counts check: three table rows, each verified against corroborating transcripts; qualitative phrasing preferred over enumerating \"N modes\". Fine.\n\nSurvivor: the error-vs-rejected / exit-1-vs-exit-2 distinction with its \"retrying won't help\" consequence.\n\nSTEP 2 — emit.\n\n```atlas-fact\n{\"claim\": \"A governance-gate refusal is signalled differently from a usage error — status rejected with exit 2 versus status error with exit 1 — and per the tool's own emitted guidance a gate-declined call was well-formed, so re-running it with different arguments will not help.\"}\n```"