"STEP 1 — scratch reasoning:\n\nReading the bytes, candidates considered:\n\n1. *\"The closed vocabulary is exactly 13 strings …\"* — the literal is present and I can count it (invariant … definition = 13). Survives byte-checking, but it largely elaborates the name (\"closed-slot\") and the header already frames it; weaker \"changes what they do\".\n2. *\"`isClosedSlotError` is structural, keyed on a discriminant\"* — the simulated foreign-instance error carries only `message` + `reason`, so recognition must hinge on the `reason` value. Derivable, but the header prose states it, and the mechanism partly lives in the imported module.\n3. *\"Non-string offenders are described by kind, not interpolated\"* — checkable via expects, but narrow.\n4. *\"Absent slot stands aside\"* — the C block's expects are executable in these bytes: `upsert(emptyStore(), req(undefined))` → `decision === 'CREATE'`, row slot undefined, and critically `'slot' in row === false` (key omitted entirely, not explicit `undefined`). This is the pivotal, non-obvious contract: from the name \"gate\" an engineer could plausibly assume fail-closed on missing slots; the suite pins the opposite. It is grounded in assertions visible here, not in comments' claims about other files (I am dropping the comment-derived numbers like \"0 of 300\" and issue references).\n\nRefutation pass on candidate 4: Is it restating the name? No — \"gate\" does not imply omission is legal. Is it resting on out-of-file claims? No — every element (CREATE decision, undefined slot, absent key) is an `expect` in this unit. Does it change what the reader does? Yes — callers may legitimately omit `slot`, and any consumer/serializer logic must handle rows where the key is truly absent. Survives.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"The gate is deliberately fail-open on omission: this suite pins that a write carrying no slot at all is accepted (routed as CREATE) and its stored row omits the 'slot' key entirely rather than holding explicit undefined, so only a present-and-unrecognised slot value triggers refusal.\"}\n```"