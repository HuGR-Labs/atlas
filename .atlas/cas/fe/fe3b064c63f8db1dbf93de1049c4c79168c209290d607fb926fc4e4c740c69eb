"STEP 1 — Reasoning (scratch):\n\nReading `resolveMineSlots`: the unset/empty branch returns the literal array `['advisory', 'dependency', 'count']`, while an explicit value delegates to `resolveMineSlot`, whose accepted union is `'advisory' | 'dependency' | 'count' | 'definition' | 'semantic'`.\n\nDraft fact: the default mining set omits two valid arms.\n\nRefutation pass:\n- Derivable from these bytes? Yes — the array literal and the `MineSlot` type union are both in this unit; no reliance on external files. Counted the array elements (three: advisory, dependency, count) and the type members (five), so the exclusions ('definition', 'semantic') check out item-by-item.\n- Comment-dependent? No — the array literal itself is the evidence; the surrounding prose agrees but isn't needed.\n- Restatement of name/signature? No — `readonly MineSlot[]` reveals nothing about which arms the default contains; an engineer could plausibly assume all valid arms are covered by default, which is wrong.\n- Contradicted anywhere in the shown code? No — the wired path in `resolveProposer` handles 'definition'/'semantic' only when explicitly selected via env or `slotOverride`, consistent with the exclusion.\n\nSurvives. Emitting. STEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"When ATLAS_MINE_SLOT is unset, resolveMineSlots returns exactly ['advisory', 'dependency', 'count'], so the two other valid arms ('definition' and 'semantic') are never mined by default and require an explicit selection.\"}\n```"