"STEP 1 — scratch reasoning (not persisted):\n\nCandidates considered:\n\n1. *\"The old always-merge was removed; adjacency is now derived on read\"* — sourced entirely from comments referencing `docs/design/dedup-identity.md` (DP-1/DP-2) and `deriveSubsumes`, none of which are in these bytes. Discard: a fact about elsewhere.\n\n2. *\"Every golden names the mutant that flips it\"* (header claim) — cannot be emitted as-is; it's a comment's assertion. But checking it **against this unit's own bytes** is legitimate. Scanning the body: the string `MUTANT`/`teeth` occurs exactly once — in the third `it` block (\"NON-adjacent anchor\"), as the trailing `// teeth (MUTANT: reintroduce a write-time adjacency merge …)` line. Tests 1, 2, 4, and 5 contain no such marker (tests 1 and 5 mention the removed always-merge in prose, but carry no teeth/mutant annotation). There are exactly five `it` blocks; I listed and checked each one. So the header's blanket claim is contradicted inside the same file: only 1 of 5 goldens bears the explicit mutant annotation. This survives — it rests solely on comparing two visible regions of this unit, requires no external knowledge, and is not a restatement of name/signature.\n\n3. *\"All requests use family 'advisory'; slots are only 'invariant'/'gotcha'\"* — true of the literals but a near-summary of what the code plainly writes; low action-changing value. Discard in favor of #2.\n\n4. *\"A 2-arg `upsert` exists with an optional cfg\"* — inferable from any signature view plus the test title itself; not novel. Discard.\n\nSurvivor: the header-vs-body mismatch on mutant annotation coverage — verifiable by counting occurrences of the teeth/MUTANT marker within this file alone.\n\n```atlas-fact\n{\"claim\": \"Although the file header asserts that every golden names the mutant that flips it, the explicit 'teeth (MUTANT: ...)' annotation appears only in the third test (the non-adjacent-anchor case); the other four tests contain no such mutant marker.\"}\n```"