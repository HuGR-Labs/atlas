"STEP 1 — Reasoning (scratch):\n\nScanning the bytes for something beyond name/signature: the file is a transcription test pinning behaviors of `defaultLoops`, `runLoop`, `runDeepening`, etc., all imported from `../src/loops.js`.\n\nCandidate 1: \"All runtime symbols come from src/loops.js; @atlas/genesis is type-only.\" — Verifiable from the import block, but the header comment already announces this, and it barely changes what an engineer does. Weak surprise.\n\nCandidate 2: \"GenesisBudget embeds `deepening: defaultLoops()`.\" — True from the `budgetOf` literal, but it's a two-line fixture whose job is obvious; low action value.\n\nCandidate 3: The header FLAG states the facet's `defaultLoops()` placeholder is \"INERT (enabled:false) and asserted by NO test here.\" Refuting pass against the CODE: SCN-GEN-14a-1 iterates `[d.review, d.enrich, d.expand]` (three items, enumerated and checked) and asserts `isOptedIn(cfg)` is false, `cfg.enabled` toBe(false), and `cfg.maxDepth` toBe(0). So the inertness IS asserted, in this very file, contradicting the flag's \"asserted by NO test here.\" Both halves of the contradiction live in these bytes — the claim's truth rests on the assertion code, not on trusting a comment about unseen code (types.ts is referenced only as context for the epsilon point, which my claim does not rely on). It is not a restatement of the file name or signatures, and it would change what a coverage-auditing engineer records: defaultLoops()' disabled/maxDepth-0 contract is already pinned by 14a-1. Survives.\n\nDiscard the rest (test-outcome restatements, comment claims about held-out `-2` fixtures, SEAL barrel wiring — all either plain or about elsewhere).\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"Contrary to the header flag stating that the inertness (enabled:false) of defaultLoops() is asserted by no test in this file, SCN-GEN-14a-1 asserts for each of the three default loop configs (review, enrich, expand) that enabled === false and maxDepth === 0.\"}\n```"