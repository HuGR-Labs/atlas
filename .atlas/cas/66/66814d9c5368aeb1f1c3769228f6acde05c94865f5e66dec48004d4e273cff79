"STEP 1 — Scratch reasoning (not persisted):\n\nCandidates considered:\n\n1. *\"The fake's `definesAt` returns `'src/def.ts'`\"* — true and in-bytes, but no expectation depends on it; low behavioral impact.\n2. *\"Identity hashing means strings are double-cast (`as unknown as Hash`) into hash slots\"* — the casts are visible, but the conclusion that `Hash` rejects direct string assignment leans on type internals not shown here.\n3. *\"Reason precedence between 'target-unresolvable' and 'scope-open'\"* — not exercised together anywhere in the bytes; can't derive.\n4. **Refute-path absence** — enumerating all seven `it` blocks' `toEqual` literals: test 1 expects `verdict: 'proven'`; tests 2–7 each expect `verdict: 'abstain'` (no-caller-in-scope ×3, target-unresolvable, scope-open, target-not-global). Counted item by item: 1 proven, 6 abstain, and no refuted/negative verdict literal appears in any assertion. Crucially, test 6 shows that even when a *real* caller exists (`src/paycheck/billing.ts`) but sits outside `sourceScope`, the pinned behavior is `abstain('no-caller-in-scope')` — not a refutation. This is checkable purely by reading the expectations in these bytes (the supporting comments merely corroborate; the claim rests on the assertion literals themselves, not on comment references to `FactVerdict` or issues).\n   - Refutation attempt: is it just restating the name? No — \"verifyDependency\" invites assuming a prove-or-refute contract; the bytes pin only two verdicts and leave any refutation behavior completely unconstrained by this suite.\n   - Exhaustive-count risk? I re-listed all seven expectations; none asserts a third verdict value. Survives.\n   - Actionability? Yes: anyone mutating or re-implementing `verifyDependency` knows this suite exerts zero pressure on a refutation branch, and anyone porting the fixture knows out-of-scope callers must yield abstain, not a negative verdict — the suite treats false-`proven` as the guarded failure mode, not refutation.\n\nSurviving fact identified.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"No assertion in this suite expects any verdict other than 'proven' or 'abstain' — even when a real caller of the target exists outside sourceScope ('src/paycheck/billing.ts'), the required outcome is abstain with reason 'no-caller-in-scope', so the tests pin no refutation behavior at all.\"}\n```"