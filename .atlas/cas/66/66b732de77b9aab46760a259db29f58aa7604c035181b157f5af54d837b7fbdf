"Looking at the bytes: the fixtures are all defined locally, so most behavior is checkable here. The test name and its title suggest the 13b-2 wave simply *lacks* a why-not (\"neither absorbed nor gave a why-not\"). But reading the literal, the wave object is `{ touched: ['src/a.ts','src/b.ts'], absorb: undefined, whyNot: '' }` — it **does** carry a `whyNot` key, set to the empty string. The `hasWhyNot` fixture returns `typeof w === 'string' && w.length > 0`, so `''` fails purely on the non-empty requirement, not on key absence. That contrast (present-but-empty vs. missing) is only recoverable from these code bytes; the name alone would lead you to assume the key is absent.\n\nRefutation pass: this doesn't rely on `bindProduce`'s hidden internals (I'm not asserting the violation outcome, just fixture semantics plus the literal); it doesn't trust the header comment's claims about goldens files or reviewers; it's qualitative, no counts; and it's more than a restatement of the test's name — it contradicts the natural reading of that name. It survives.\n\n```atlas-fact\n{\"claim\": \"In the 13b-2 scenario the wave actually carries a whyNot property set to the empty string, and it counts as 'no why-not' only because the hasWhyNot seal-leg fixture requires a non-empty string — meaning present-but-ungrounded, not a missing key.\"}\n```"