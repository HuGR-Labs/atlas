"STEP 1 — REASONING (scratch):\n\nReading the bytes: the unit declares `### PROP-GEN-1` … `### PROP-GEN-16`, plus an additional `### PROP-GEN-8b`. Listing the `inv:` fields block by block — 8b's is `INV-GEN-8 (with INV-GEN-12's abstention leg …)` while PROP-GEN-8's is `INV-GEN-8`; every other INV (1–7, 9–16) maps to exactly one block. So INV-GEN-8 is the sole invariant rendered twice.\n\nRefutation attempts:\n- Is the duplication contradicted anywhere in-unit? Yes — the Completeness bullet states \"INV-GEN-1..16 each render to exactly one PROP.\" That bullet lives in THESE bytes, so the contradiction is internally checkable, not a claim about unseen code. Survives.\n- Does it rest on external files/comments? The two `inv:` fields and the Completeness sentence are all in-unit. The surrounding prose about `method-tags-gen.md`, ADR-0012, task #114/#151, and the goldens file was discarded as unverifiable-from-here. Survives.\n- Enumeration risk: I listed all 17 `###` headings and each `inv:` value individually; INV-GEN-8 is the only repeat. Counting 17 headings vs \"the 16 PROPs\"/\"exactly one\" is consistent with this finding; I avoid leaning on the uncountable \"75/75\" goldens figure. Survives.\n- Would it change what an engineer does? Yes — anyone wiring the harness off the Completeness gate would build one property per INV and drop PROP-GEN-8b (the `|rows| = |F|` closure/recorded-outcome check covering REQ-GEN-12g); an auditor would flag the gate. Not inferable from the path/filename alone.\n\nSurviving fact identified. STEP 2 — emit:\n\n```atlas-fact\n{\"claim\": \"INV-GEN-8 is the only invariant rendered by two separate PROP blocks (PROP-GEN-8 and PROP-GEN-8b, the latter adding the recorded-abstain outcome leg and covering req-gen.md#REQ-GEN-12g), contradicting this unit's own Completeness section, which states that INV-GEN-1..16 each render to exactly one PROP.\"}\n```"