"STEP 1 — scratch reasoning:\n\nCandidates drafted from close reading:\n\n1. *\"Tier default is 'T2'\"* — refuted as a candidate: stated in a named constant and repeatedly in comments; a reader of the signature/docs would expect it.\n2. *\"Heuristic is flag-only\"* — restates the comment's governance claim and the obvious filter/map flow; not surprising beyond the signature.\n3. *Module-level `createInit(...)` call in the conformance check* — true and visible, but low impact (only builds closures); wouldn't change what anyone does.\n4. *\"Two independent seam calls share one path with no consistency check\"* — plausible, but partially leans on reasoning about the unseen index implementation.\n5. **Aliasing asymmetry in the return value**: `territories` is rebuilt via `map` + spread into fresh objects; `t0Candidates` is a freshly built string array; but `blastRadius` is bound directly to `index.blastRadius(path)`'s return and placed in the output unchanged — no copy, slice, or transformation anywhere in the function body. Refutation pass: fully visible in these bytes (`const blastRadius = index.blastRadius(path); ... return { territories, blastRadius, t0Candidates };`), no reliance on comments about other files, qualitative (three fields enumerated against the visible return statement, all verified), not derivable from the name/signature, and actionable: implementers of `MoveInIndex` must know their returned array escapes uncopied, and consumers must not mutate it. Survives.\n\nEmitting the survivor.\n\n```atlas-fact\n{\"claim\": \"The blastRadius in init's output is the exact array reference returned by index.blastRadius(path) — never copied or transformed — whereas each returned territory is a freshly constructed object built by spreading a raw territory with the 'T2' tier added.\"}\n```"