"Looking at the bytes: nine export statements — one `export type *`, seven `export *` lines, and exactly one explicit named-list export (`asHash, asSubtreeHash, asNodeKey`). Draft candidate: brand.js is uniquely hand-curated here. Refutation pass: the header's \"FULL public surface\" claim rests on files not shown — discard. The missing API-interface annotation on the jsonl line is a comment-text observation with no behavioral bite — discard. Collision/ordering speculation leans on invisible cross-module contents — discard. The structural asymmetry survives: it's verifiable by scanning every export line in this unit, isn't a restatement of the filename, and matters operationally (new mints added to brand.js won't flow through the barrel automatically, unlike every sibling module).\n\n```atlas-fact\n{\"claim\": \"brand.js is the only module this barrel surfaces via an explicit hand-written named export list (asHash, asSubtreeHash, asNodeKey) rather than a star re-export, so any export added to brand.js itself will not appear at the package root until this list is edited.\"}\n```"