"STEP 1 — scratch reasoning (not persisted):\n\nScanning the bytes: the fixtures in `contestedQ(i)` embed `rankReason: \\`blast×tier rank ${8 - i}\\``, i.e. strictly descending rank strings (for the 8-item array: ranks 8→1; for the 23-item array: 8→−14, still strictly descending). Now I enumerate every `expect` in the file to see whether any pins that ordering:\n\n- 5a: length 12, every-status candidate, zero ratified.\n- 5b: batch size 8, size > 1, `batch.cap === INTERVIEW_CAP`, `INTERVIEW_CAP === 20`, deferred 0; big case: 20 in batch, 3 deferred.\n- 5c: 1 edge, all via interview, bypass 0, `canAutoPromote()` false.\n- 5d: `calls === 1`, seen size > 1, ratified length 8.\n- GEN-9: states, grounding membership/lengths, `unratified` true, `ratified` undefined.\n\nNone of these inspects `rankReason` values or the order of questions/results — no ordered `toEqual`, no index-based content check. So despite the test's own title advertising a \"**ranked** interview\", ranking is never actually verified here; only sizes, the 20 cap, deferred counts, and a single-call counter pin behavior.\n\nRefutation pass: Is this derivable only from these bytes? Yes — it comes from the fixture literals plus the complete in-file list of assertions, not from any comment's claim about another module. Is it an unsafe exact-count claim? The negative (\"no assertion inspects ordering\") was verified item-by-item against all expects shown. Is it a restatement of name/signature? No — the *name* implies ranking is tested; the fact is that it is not. Would it change what an engineer does? Yes: refactoring `assembleInterview`'s sort cannot regress this suite, and anyone adding held-out `-2` fixtures knows ordering coverage is missing here. Survives.\n\nRunner-up candidates discarded: the `{}`-cast skeleton (partially a comment-driven framing), `ref('root','repo')` kind choice (marginal actionability), duplicated `import type` lines (trivial).\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"Although the fixtures feed assembleInterview contested questions carrying strictly descending rankReason strings, no assertion in this file ever inspects rankReason or question/result ordering — the 'ranked' property is pinned only indirectly via batch sizes, the 20-question cap, deferred counts, and a single ratifier-call counter.\"}\n```"