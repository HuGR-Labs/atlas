"STEP 1 — scratch reasoning (not persisted):\n\nReading `marshalEmitNode` closely: the envelope gate is `'fact' in node && 'refusal'…` — correction, `'fact' in node && 'rev' in node`, purely structural. Inside, the mismatch refusal condition is `typeof env.rev === 'string' && env.rev !== at`. Because of the `&&`, an envelope whose `rev` is a number, null, boolean, etc. makes the condition false and falls through to `{ ok: true, args: { node: env.fact, at } }` — accepted and unwrapped with **no rev comparison at all**.\n\nRefutation attempts:\n- Derivable from these bytes? Yes — pure control flow and literals in this unit; no reliance on wire.ts, parse, or any external module. The comment narrative about AUTHOR-7/back-compat is irrelevant to the claim; the claim rests only on the shown conditional.\n- Restating name/signature? No — `marshalEmitNode(node: unknown, at: string): MarshalResult` suggests \"match revs, refuse mismatches\"; nothing in the signature hints that non-string revs skip the gate entirely.\n- Exhaustive count/enumeration? No — it's a qualitative claim about one conditional's short-circuit, verified against the two return paths of that function.\n- Would it change what they do? Yes — anyone treating the mismatch refusal as a general staleness guard (e.g., writing tests or tightening the gate) needs to know that only *string* revs participate; a malformed non-string rev sails through to the door as if it matched.\n\nChecked alternatives and discarded them: \"only the rev-mismatch sets `refusal: true`\" is true (verified across all nine failure returns) but largely telegraphed by the surrounding prose; \"positionals pass through as `ok:true` when undefined for query/reconcile/link\" is real but closer to a plain summary of the happy paths. The non-string-rev bypass survives best: subtle, code-only, actionable.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"In marshalEmitNode, a draft-envelope-shaped emit payload (top-level `fact` plus `rev` keys) skips the stale-rev refusal and is unwrapped to its bare fact whenever `rev` is not a string, since the mismatch refusal fires only for a string `rev` that differs from `--at`.\"}\n```"