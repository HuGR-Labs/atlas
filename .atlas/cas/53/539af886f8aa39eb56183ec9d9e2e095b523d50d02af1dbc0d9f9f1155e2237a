"STEP 1 — Reasoning (scratch):\n\nCandidates drafted and refuted:\n\n- *\"TEETH bypasses `assembleHandler`, calling `readScip` directly\"* — true in the bytes, but the test's own title (\"the raw unguarded read … THROWS\") telegraphs this; fails the novelty bar.\n- *\"`readScipOrEmpty` is never imported/asserted here\"* — checkable, but mostly a corollary of the header's framing; low actionability.\n- *\"Query assertions are shallow (only `ok` and `territory`, no `subsumes`/bounds)\"* — solid and code-enumerable, but narrower in scope.\n- **Survivor:** the `legWired` oracle. Its full implementation is one visible line: `!(rejected ?? '').includes('not wired at this seam')`. Because the whole predicate is present in these bytes, universal claims about it are safely checkable: any rejection string lacking that exact sentinel — including a failure for an entirely unrelated reason — maps to `true`, and `undefined` (an `ok` result) also maps to `true`. So the wired/unwired signal cannot distinguish \"leg works\" from \"leg fails with some other message\". The signature `(rejected: string | undefined): boolean` does not reveal which strings classify as wired; the adjacent comment describes the intent but the permissive conflation of success with foreign failures is the load-bearing, easily-missed property. It changes what an engineer does: editing the frozen close-out copy, adding new rejection paths, or trusting the green `GOVERNANCE_SURFACE` loop as proof of correctness all hinge on this. No counts, no cross-file claims, no library assumptions. Survives.\n\nSTEP 2 — Result:\n\n```atlas-fact\n{\"claim\": \"The legWired oracle classifies any rejection that merely omits the exact substring 'not wired at this seam' as wired, so a governance leg failing for some other reason is indistinguishable from a correctly wired one in these tests.\"}\n```"