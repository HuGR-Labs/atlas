"Looking at the bytes: this is a self-contained styled HTML fragment whose prose makes many claims about an external \"atlas\" system (CAS, BLAKE3, three axes, `refs/notes/orchestra`, spec files in `reference/…`) — all of those are assertions about code/files **not** present in this unit, so they fail the re-derivation test. The footer's \"peça (A) de 4\" enumerates sibling pages B/C/D I cannot see — discard. What *is* checkable here is the CSS itself.\n\nCandidates drafted:\n1. \"No JavaScript / mermaid won't render standalone\" — the file truly has no `<script>`, but this claim's force depends on knowing what `class=\"mermaid\"` means to an external library, a convention not visible in these bytes. Weak per the rubric; discard.\n2. \"Theme palettes are duplicated across blocks\" — true but low-value maintenance trivia; discard.\n3. Dark-mode coverage of the palette variables: I listed the base `:root` (16 vars, including `--score-paper`/`--score-line`) against the `@media (prefers-color-scheme: dark)` block and the `:root[data-theme=\"dark\"]` block — each dark block redefines 14 vars (`--paper` … `--block`) and **stops at `--block: #D67256`**, touching neither score variable. Since `.score` paints with `var(--score-paper)`/`var(--score-line)`, the diagram panel keeps its light cream look under both dark mechanisms, while the hardcoded mermaid `themeVariables` (`#EAE5D8` background, `#9A6E28` border) match that light rendering. Refutation checks: derivable purely from these declarations plus ordinary CSS custom-property inheritance (nothing outside the bytes); not a comment's claim about elsewhere; the \"neither block\" scope was verified item-by-item against both selectors; far beyond what the filename/title reveal; and it changes what a reader does (they won't chase a dark-mode \"bug\" elsewhere, and any new score-panel variable must be themed deliberately).\n\nSurvives. Emitting it.\n\n```atlas-fact\n{\"claim\": \"Neither of this file's dark-theme blocks — the prefers-color-scheme media query and the :root[data-theme=\\\"dark\\\"] selector — overrides --score-paper or --score-line, so the mermaid diagram panel keeps its light cream styling even in dark mode.\"}\n```"