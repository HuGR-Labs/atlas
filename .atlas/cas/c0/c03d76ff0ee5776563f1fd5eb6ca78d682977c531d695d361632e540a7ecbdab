"STEP 1 — scratch reasoning:\n\nReading the bytes closely, most striking details: (a) the exact canonical `claimNorm` strings asserted with `.toBe(...)`; (b) the final test asserting the fact has no top-level `scope`; (c) the third \"TEETH\" test.\n\nCandidate 1: exact canonical claim sentences — visible as literals, but it's near-transcription of assertions; low insight value. Set aside.\n\nCandidate 2: \"fact has no top-level `scope`\" — derivable from the assertion, but the *why* (authz stamping downstream in `governed-emit.ts`) rests on a comment about code not shown, so only the bare negative survives; weaker.\n\nCandidate 3: the third \"TEETH\" test. Its name/comment claims it proves the suite goes red when a witness-carrying drop loses `target`. But the code builds `mutated` **locally** from `witness.slot` and `witness.scope` — the very fields that survive the simulated mutation — and asserts `not.toEqual` against a hardcoded shape containing `target`. Whatever `target` value (or absence) `admit` emits, `mutated` never gains a defined `target` key, so it never deep-equals the full shape and the assertion always passes. The test can only fail if `witness` is wholly absent (destructuring throws), not for the dropped-target regression it names. Refutation attempts: it doesn't lean on vitest-specific semantics (any structural equality behaves the same); it doesn't trust the comment (it contradicts it using visible control flow); no counts or enumerations involved; not a restatement of the test name — it's an analysis the name actively obscures. Survives.\n\nThis changes what an engineer does: they'd know the mutation-coverage claim is hollow and the test needs rewriting to actually probe `admit`'s output.\n\nSTEP 2 — emit result:\n\n```atlas-fact\n{\"claim\": \"In the third 'TEETH' test, the mutated object is constructed locally from the witness's own surviving fields (slot, scope) and asserted not.toEqual against a hardcoded full shape, so the assertion passes regardless of what target admit emits and cannot actually catch the dropped-target regression its name claims to prove.\"}\n```"