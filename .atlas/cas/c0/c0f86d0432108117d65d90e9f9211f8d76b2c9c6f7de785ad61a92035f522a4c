"STEP 1 — scratch reasoning:\n\nReading the bytes closely, the fixture side is unambiguous: `advisory()` returns a full `AdvisoryNode` object literal, `pair()` wraps it in `DriftedFact`, and `classifierWith` visibly returns `mechanical`/`semantic` as **AdvisoryNode arrays** (`drifted.filter(...).map((d) => d.fact)` — projecting the node object out of the fact wrapper). Nothing in the imports or the file name reveals the *result* shape of `createReconcile(...).reconcile(MERGE)`.\n\nYet the assertions tell a different story: `out.drift.map((d) => d.fact)` is compared with `toEqual` against plain string literals `'nk:ceo'`, `'nk:churn'`; `out.mechanical` against `['nk:ceo', 'nk:inv']`; `out.semantic` against sets of `'nk:arr'`, `'nk:runway'`; and `Map` keyed on `d.fact` is queried with the raw string `'nk:ceo'` (which requires primitive-string keys at runtime). So the object→node-key-string reduction cannot come from anything written in this file — the fixtures only ever put objects in, and the expectations only accept strings out. The projection therefore lives in the reconciler under test.\n\nRefutation attempts:\n- Is this from a comment? No — the relevant comment (\"writer is WP-4.12-b\") is discarded as a claim about unseen code; my fact rests only on visible fixture code and visible assertions.\n- Exact counts/exhaustive enumerations? Avoided — phrased qualitatively (\"every result field inspected by value\"), verified case-by-case across 8a–8d.\n- Restatement of name/signature/types? No — no visible signature exposes the result shape; `out` is entirely inferred.\n- Library-dependent leap? Only language-level string-literal-vs-object contrast is needed; the assertion text itself demands string equality.\n\nSurviving candidate identified.\n\n```atlas-fact\n{\"claim\": \"The fixtures feed full AdvisoryNode objects into the pipeline (the stub classifier visibly returns mapped AdvisoryNode arrays via map((d) => d.fact)), yet the result fields inspected by value — drift rows' fact, mechanical, semantic — are all asserted equal to bare node-key strings like 'nk:ceo', so the projection from advisory object to node-key string happens inside the reconciler, not in this test file.\"}\n```"