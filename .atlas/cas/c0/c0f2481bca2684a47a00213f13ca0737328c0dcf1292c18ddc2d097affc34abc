"STEP 1 — SCRATCH REASONING\n\nReading the bytes closely:\n\n- The header comments make many claims about *other* files (`governed-emit.ts`, `fastpath.ts`, `router.ts`, KERNEL-8, KNOW-18). Per the rules, none of those can back my fact — they're assertions about code not shown here.\n- What is actually *in* this unit: the local minting logic in `groundedPredicateFact`, the helpers, and six tests.\n\nDraft candidates and refutation:\n\n1. *\"SUPERSEDE routes differently from DEDUP\"* — rests entirely on comments about `router.ts`; not re-derivable from these bytes. Discard.\n2. *\"A predicate always requires ratification regardless of tier\"* — asserted in comments about `fastpath.ts`; not visible here. Discard.\n3. *\"normalizeCheck trims whitespace\"* — the test asserts same-id behavior, but the trimming itself happens inside `@atlas/knowledge`, invisible here; only the *outcome* (`P2.id === P1.id`) is asserted. Borderline; also it's the file's headline premise. Discard.\n4. *\"Test 4 re-emits P1 to recover its contentHash\"* — checkable in control flow (`emitFact(repo, P1)` appears in test 1 and again in test 4; test 1's `out` is never retained). True and mildly non-obvious, but mostly a test-hygiene observation.\n5. **The candidate-view shape:** inside `groundedPredicateFact`, the object fed to `nodeKey` literally sets `claimText: ''` and `claimNorm: ''` while carrying the full `spec.check` verbatim. This is a concrete, load-bearing structural choice visible only in these bytes — not inferable from the function's name or the `PredicateFactSpec` signature — and anyone replicating the mint (tooling, another fixture) must reproduce it exactly or their `id` diverges. It rests on visible literals, not on any comment's claim about elsewhere. Qualitative, no counts. Survives refutation.\n\nCross-check candidate 5 against the code: `const candidate: Candidate = { claimText: '', claimNorm: '', slot: spec.slot, check: spec.check, ... }` and `id: nodeKey(candidate)`. Confirmed directly from declarations. It is not a restatement of name/signature, and it would change what a replicating engineer does.\n\nSTEP 2 — RESULT\n\n```atlas-fact\n{\"claim\": \"Inside groundedPredicateFact, the Candidate object fed to nodeKey — whose output becomes the fact's id — is constructed with empty-string claimText and claimNorm, while the predicate's check object is carried verbatim into that same view.\"}\n```"