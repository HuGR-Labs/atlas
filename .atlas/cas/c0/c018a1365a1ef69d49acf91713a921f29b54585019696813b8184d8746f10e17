"STEP 1 — scratch reasoning (not persisted):\n\nReading the bytes: the doc's title promises \"why & how it thinks\" for genesis — an engineer could guess it contains rationale, an S2 loop sketch, and a cost argument. What they could *not* guess is the specific mechanics of the VERIFY stage.\n\nDraft candidate A: \"Obviousness is scored, never gated.\" Present in bytes, with the auditability rationale. Survives, but it's flagged as belonging to an external ADR (ADR-0012), so part of its authority rests on a pointer.\n\nDraft candidate B: \"Chain-of-thought is discarded after PROPOSE.\" Fully in-byte (\"scratch — discarded\"), but it's a small operational detail.\n\nDraft candidate C: \"The VERIFY stage includes a 'teeth' gate requiring a candidate's synthesized check to flip to BROKEN on a mutated counterfactual of the subtree; a check that survives every mutant is vacuous and dropped.\" Checking against bytes: yes — \"**teeth:** it MUST also **flip to `BROKEN` on a mutated counterfactual** of the subtree — a check no mutant breaks is vacuous and dropped.\" The full mechanism is stated in this unit itself; the GEN-12 pointer is supplementary, not load-bearing. Not derivable from the filename; not a count/exhaustive-enumeration claim; not a restatement of the title. It would change what an implementer does (they'd have to build mutation testing into verification, not just run the check).\n\nRefutation attempts: Does it depend on unseen files? No — the procedure is fully described here. Contradicted anywhere in the unit? No — consistent with the loop diagram and the REFINE step. Numeric/exhaustive claim? No. Survives.\n\nSTEP 2 — result:\n\n```atlas-fact\n{\"claim\": \"In genesis's VERIFY stage, a candidate's synthesized check must flip to BROKEN on a mutated counterfactual of the subtree — a check that no mutant can break is deemed vacuous and the candidate is dropped.\"}\n```"