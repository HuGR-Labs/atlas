{
  "name": "atlas",
  "private": true,
  "version": "0.0.0",
  "description": "Layer 0 — a shared, grounded, git-native knowledge layer for a codebase (no embeddings, no RAG).",
  "workspaces": [
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "test": "vitest run",
    "godfile-guard": "node scripts/godfile-guard.mjs"
  },
  "engines": { "node": ">=20" },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
